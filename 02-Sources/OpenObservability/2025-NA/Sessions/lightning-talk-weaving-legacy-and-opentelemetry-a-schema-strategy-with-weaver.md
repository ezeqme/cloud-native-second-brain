---
type: session
event: "Open Observability Summit + Otel Community Day NA 2025"
year: 2025
kind: session
youtube_url: https://www.youtube.com/watch?v=38f1EfOaV0E
youtube_id: 38f1EfOaV0E
playlist: "Open Observability Summit + Otel Community Day NA 2025"
playlist_id: PLj6h78yzYM2NFT2PGItX2idBf7v8fHcy7
playlist_index: 27
speakers: ["Andrew Wang"]
topics: ["OpenTelemetry", "Tracing", "Logging", "AI Observability"]
keywords: ["semantic", "attributes", "tracing", "conventions", "weaver", "trace", "opentelemetry", "span", "attribute", "single", "allows", "generate", "create", "schema", "talking", "information", "common", "configuration", "template", "legacy", "solution", "client", "across", "libraries"]
transcript_file: _sources/transcripts/youtube-playlists/open-observability-summit-otel-community-day-na-2025/lightning-talk-weaving-legacy-and-opentelemetry-a-schema-strategy-with-weaver/38f1EfOaV0E.txt
transcript_chars: 11579
status: transcript-downloaded
---

# Lightning Talk: Weaving Legacy and OpenTelemetry: A Schema Strategy With Weaver - Andrew Wang

## Metadata

- YouTube: https://www.youtube.com/watch?v=38f1EfOaV0E
- Playlist: Open Observability Summit + Otel Community Day NA 2025
- Speakers: Andrew Wang
- Topics: [[OpenTelemetry]], [[Tracing]], [[Logging]], [[AI Observability]]

## Transcript

My name is Andrew. I'm a software engineer at Comcast. So I will start today's presentation by talking about the importance of attributes in a tracing solution. And if you have been to the keynote, you have heard that semantic conventions is open telemetries proposed solution to address this issue. And building on top of open telemetries semantic conventions. I'll show how we were able to group together semantic conventions together with uh attributes used by our legacy applications. I'll also be sharing lessons we learned that help us stay the course. So let's start a little bit by talking about why we need tracing. I'm sure we're all convinced of this. Just to go briefly over it, let's say we have a client X that makes an API call to a front end layer and receives the response back after 500 milliseconds. And suppose we wanted to know who was responsible for this 500 milliseconds. We all know that the front layer will be making calls to the back end, but we would have no visibility into the call stack.

It's indeed a black box for which we cannot observe events that are taking place. So that's why we need tracing. It allows us to track a transaction journey. So let's go back to our example and let's say the front layer will be talking to an authentication layer which will be talking to a inventory layer which will be talking to database and if they're all instrumented for tracing the front end can then generate a unique trace ID that will be propagated to the it entire call stack. Each layer will track its own work by using a span ID and send this information to the backend tracing service which when once it has all this information it will help us visualize how the transaction took place uh who is responsible for what part of the call. This is the importance of tracing and from there where do attributes fit? Let's say uh I'm just giving a very contrived example please bear with me. Let's say the front end says that oh yeah the originator of this call is a client IDX and authentication agrees it says yes it is client IDX and the inventory layer 2 agree yes I agree with you it's client IDX but from this you can already tell that if we don't have some common standards at the time we go to the tracing end service and ask give me information about X we may need to do a query like this right this is extremely brittle imagine if a fourth component comes in and have its own idea of how to represent client ID and imagine this multiplied across all these libraries and languages what we are seeing is that tracing solutions tracing platforms inherently cross this domain of ownership the front layer authentication layer inventory they can all belong to different teams and unless we have common standards you'd be extremely hard labor intensive to obtain value out of the solution that we're uh um setting up.

So this is where semantic conventions come in. It allows us to give common names for different kinds of operations and data. And I couldn't have said better than they said. So I just copied this from their read me. The benefit of using semantic conventions is in following a common naming scheme that can be standardized across a codebase, libraries and platforms. These allows easier correlation and consumption of data and not just codebased libraries and platforms any information about the attributes we can generate schemas for database documentations for develop developers. This can all become the outcome that we can get out of our semantic conventions. And how do we achieve that? Well, when we talk about semantic conventions, we're actually talking about a catalog of attributes. This is what the registry is. But together with this registry, we also have a tool called Weaver. It's a open telemet telemetries project that can based upon the target configuration files take in the attributes and render out them in a format we can consume.

So let's dive a little bit deeper into this. The registry is a repository in GitHub at that location and under that repository there is a model folder and this is an example like that we can see under the model folder will have subfolders in each subfolder attributes are grouped together. So let's say HTTP attributes they will all be found under the HTTP subfolder. In this subfolder there'll be multiple files and the registry yaml file is the one in which we will find all the attributes listed together with maybe the type a brief description. So this is the our catalog. This is our base the set of attributes that have been defined by the community in open telemetry. Now let's talk a little bit about the target configs. Well, actually we have example of this target target configuration file say for markdown in this uh path. But before we show this example, let's take a look at what this file uh does.

It's a YAML file and it will point us where the template files are located. It can help us also group attributes, filter out specific attribute we want to use as input. It also perform allows us to perform mapping. Say the type system used the semantic conventions may not correspond to the time type system of the platform we want to use. So mapping is also supported. So um there's a lot more option that support. So this URL is the where we can find more information about the configuration. So this for instance is an example of the uh uh configuration file for markdown. In this file we see pattern template patterns. Actually each pattern is a template file. It's in ginger. The template is written in ginger so that it can render out according to how we want it. So let's take a look at the first one. In the first one it's actually it may be hard to read but it's a attribute readme. It's essentially the purpose of that file is to generate that readmi file that include all the attribute um groups.

So you can see that the application mode is single. So this means that we take all the attribute groups as input and generate one single output file uh for that. However, we may want to generate uh separated right and this is the second uh pattern allows to do the name space uh template file in this application mode each it will take one group and generate multiple output files. So this is what the config allows to do. It help us manipulate attributes in groups and having outputs how we want. Then once those two elements are defined we have the catalog we have the configuration weaver again that's the location of the project we'll take in those two and we'll look okay these are the attributes that I need to work on and this is the configuration file that will tell me how I should render the ginger then I'll do it I'll take it and I'll render it out and the markdown can be rendered this way and that attribute which we selected is rendered out as a row in the table.

We can see that the um the type is shown and the brief descriptions opens up there and this is what semantic when we talk about semantic conventions. This is all included. It's all part of what we can achieve using semantic conventions. Now we have many services for which they have started adopting the open telemetry libraries. So they would be using semantic conventions but again in our company there are some legacy applications for which this is not happening but we want them to be part of our platform. We want them to be part of our journey together. So how did we do it? We have to put them attributes that they require and together with semantic conventions. Well as I've shown you the attributes are grouped under folders. So we actually take advantage of this fact and inject our namespace our single namespace fold in as a folder into the semant conventions. This can be done in the local file system.

We pull down the semant conventions and we inject our name space and together when we run the tool we'll generate a set of attributes that can be used by all our applications. So like and we do it exactly the same as semantic convention does. We have subfolders like created for our specific needs and we create target files. So this example that I have on screen is generating um a schema in JSON for elastic search. So in this example for instance we can we are excluding deprecated attributes. Our final outcome will not have them. We can filter those attributes out again. Then we are also using taking advantage of the mapping feature. The types supported by elastic search are different from the type names used by semantic conventions. We can map them in the way that will be supported by elastic search. So finally we create the ginger template file that will manipulate the attributes and create in the format that we want obeying JSON syntax.

And the fi and then final outcome then is this single JSON that we can be ingested by elastic search to coordinate the schema and also contains all the semantic attributes we want to use together with our uh in-house namespace attributes for our legacy applications. So in this way we provide a single schema solution for our teams to use. Then what are some of the lessons that we have learned and how we envisioned this? Well, definitely what single focus that we had in from the beginning was to we want to have a single source for trace attributes because again if we don't have this common uh standard it would be very hard for teams to come together and get value out of the traing solution. But of course we also needed to have a different format representation. Different parts of our company will need to um access this information different formats. We want to make sure that they are able to have it in the format they need a markdown, YAML, JSON.

And in this journey with this focus, we leverage all that we could. We learned Ginga very well and uh just to give a few example like say you need attribute to be unique. Well, one thing that didn't mention is that semantic convention supports say from a single group reference an attribute from a different group. So if you're just purely rendering the same attribute may show up twice. So we use ginger to help us filter that out and create a uniqueness and as I mentioned before the commas in JSON can also be handled in that way. So having this process in place allow us to create this registry and teams that need different attributes can open PRs and insert their changes. And this is what we are doing as we currently migrate three thou more than 3,000 microservices out of vendor that will remain unnamed and uh definitely very diverse set of teams. One of them uh there are teams that they are they can take on the instrumentation libraries and use semantic conventions and there are teams that they have their unique attribute needs for their visualization or alerting and having this schema strategy in place allows them to move at their own pace right without having to stop their legless application from running which reduce migration friction which is a very good thing.

So how do we envision where we can go from here? Oh well definitely we want to provide we want to see maybe one day we can have the single schema across all observability signal you know create the most common set of attributes across all our signals that take advantage of the power that it can give us and also it doesn't have to spread across all we may create the subset of attributes as needed for specific application needs right not everybody needs to have everything So those are the elements that will be driving our effort in the future and that's how we were able to achieve weaving together legacy application together with semantic conventions.

## Related keywords

[[semantic]] [[attributes]] [[tracing]] [[conventions]] [[weaver]] [[trace]] [[opentelemetry]] [[span]] [[attribute]] [[single]] [[allows]] [[generate]]

## Notes

- Raw note imported from CNCF YouTube playlist. Promote durable insights to topic notes under `03-Topics/`.
