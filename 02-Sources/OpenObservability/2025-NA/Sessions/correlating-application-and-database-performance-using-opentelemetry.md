---
type: session
event: "Open Observability Summit + Otel Community Day NA 2025"
year: 2025
kind: session
youtube_url: https://www.youtube.com/watch?v=IYyQKHmVlac
youtube_id: IYyQKHmVlac
playlist: "Open Observability Summit + Otel Community Day NA 2025"
playlist_id: PLj6h78yzYM2NFT2PGItX2idBf7v8fHcy7
playlist_index: 20
speakers: ["Lin Lin", "Tammy Baylis"]
topics: ["OpenTelemetry", "Collectors", "Tracing", "Logging", "AI Observability"]
keywords: ["trace", "database", "server", "statement", "client", "span", "context", "actually", "log", "commenter", "otel", "tracing", "comment", "collector", "prepared", "opentelemetry", "prepare", "instrumentation", "driver", "application", "support", "execute", "queries", "making"]
transcript_file: _sources/transcripts/youtube-playlists/open-observability-summit-otel-community-day-na-2025/correlating-application-and-database-performance-using-opentelemetry/IYyQKHmVlac.txt
transcript_chars: 18338
status: transcript-downloaded
---

# Correlating Application and Database Performance Using OpenTelemetry - Lin Lin & Tammy Baylis

## Metadata

- YouTube: https://www.youtube.com/watch?v=IYyQKHmVlac
- Playlist: Open Observability Summit + Otel Community Day NA 2025
- Speakers: Lin Lin, Tammy Baylis
- Topics: [[OpenTelemetry]], [[Collectors]], [[Tracing]], [[Logging]], [[AI Observability]]

## Transcript

Thanks for coming everyone. Um, today we'll talk about correlating application and database performance using OTL aka SQL commenter in action. Uh, quick intro. Uh, my name is Lynn. I head the, um, APM instrumentation team at Solar Winds. And here's my colleague. Yeah. Hi everyone. I'm Tammy Bis. I'm a developer on Lynn's team and I'm also a part-time hotel contributor. Back to you. Okay. So uh I'll start with what the uh what was the problem we wanted to solve. Solar Winds has an application and database monitoring um components in their in in our product and we wanted to integrate them to help customers answer questions like is my application request taking long because of the state of the database or conversely from the database point of view uh where are these expensive queries coming from. Our APM is built on top of hotel which is great because OTEL comes out with like lots of instrumentation out of the box for different database server types or database types um and a rich set of data but the the data gathered is really from the client perspective.

Um so compared with the typical um distributed tracing for HTTP where you have service A making an HTTP request to service B and you can see that in the distributed trace thanks to the um trace context that's propagated via the HTTP header. You see in this uh example when service B makes a database call all of that insight is really from the client side only and the database client span really kind of links to nothing. Wouldn't it be nice if it actually could link to something? So a little bit about context propagation. As we saw before for HTTP it's pretty well defined in the W3C trace context spec. It's propagated via the trace parent and trace state headers. For messaging systems, um most uh systems support the metadata header or property concept and we see a lot of uh OTEL SDK implementing implementing them this way even though there's no explicit spec for databases. It's uh a lot murkier. Um there is a discussion happening right now in the PR that's highlighted where which actually gives a lot of insight on how observability vendors tend to do this and uh there are a lot of different approaches and it could actually be very server specific depending on what the server uh supports.

Uh in terms of native server side instrumentation by the database vendor, there are not as many examples in the wild, but my SQL enterprise edition does do um native server side tracing using the my my SQL uh query attributes mechanism and vitess uh which is a CNC CF database project uses um SQL comment which leads to our next topic by Tammy. Use the clicker if you want. Oh, thanks, Lynn. Uh, enter SQL commenter. SQL commenters and observability technique. And essentially, it's when database clients inject structured comments into queries following standard SQL syntax. So in this very simple query select one it's been appended with a SQL comment flanked by the slashes and asterises. And this SQL comment has two key value pairs. The first is a shortened version of a trace parent. The full thing with version trace ID span ID and trace flags. And this other KV is some database driver metadata for what performed the database query.

And in this case it's the MySQL connector uh version 8.40. SQL commenter was originally developed at Google and it's still at the original Google SQL commenter repo right there. It's been partially migrated to CNCF uh to this hotel repo. A bit more on that later. SQL commenter right now has experimental incorporation into hotel for trace context propagation. Uh so it's the instrumentation of database driver libraries or object relational mappers aka ORMS or ORMs that do the injection of the comment and right now configuration is programmatic and opt-in. In this Python example we have the init of a SQL alchemy instrument very popular ORM called SQL alchemy and in the call to instrument we are saying okay enable commenter true which will add the whole SQL comment. If we pass these commenter options, hotel value is true. That'll include the trace parent. DB driver false would not include the D database driver KV.

Uh big table but I'll go quickly. This summarizes the current state of the implementation of SQL commenter for hotel. Right now uh for Golang there is a separate hotel SQL library that was developed by uh Sam Shia or XAM and it will provide SQL commenting or any database interfaced by the database SQL library for Python and NodeJS in OTEL. There are multiple inventors that support SQL commenting. uh for example for the MySQL drivers MySQL client, MySQL connector, MySQL 2 or Postgress like psychopg2 or the OM SQL alchemy I mentioned. Uh they're all there in the public repos for Java Ruby Py Java Ruby Python Java Ruby PHP. It's a bit more complicated right now. Uh, for Java, there's a PR out by Lori T right now to add SQL commenter to JDBC and R2DBC. Always have trouble with that one. Uh, which would enable commenting at the driver level from MySQL, etc. But there is um there are SQL commenters for Spring and Hibernate that are still really in the Google repo and not in language specific hotel repos.

Uh similarly for Ruby and PHP there's marginalia and Laravel SQL commenting respectively in the Google repo still not actively maintained hasn't been released in a few years and for all other languages like net nothing in no nor google what does all that give us uh these implementations let us bridge that gap between the client application and the actual server query so this is the same diagram that Lynn showed before uh but this time we don't end with like a question mark. Um the same trace ID uh generated for the root span at service A uh goes by HTTP context propagation to service B and service B uses the same trace ID in the SQL comment that is included in the statement for the database server. Uh let's look at what a comment actually looks like. Lots of text. Sorry, but we have a we have some arrows in the top is an example of an hotel collector debug span and that comes from the instrumentation of the MySQL framework in Python.

And we see here has a trace ID and a span ID. Um, y'all have probably seen something like this before with an OTL collector. In the smaller box below here, this is an entry in a MySQL general log of the requested database server. And very briefly, a general log in a SQL server is one or more log files that contains records of all queries and server operations. So it's a pretty detailed history if you enable it. Uh so now visually we can correlate uh the span that was generated with the actual query. uh labeled in red. The trace ID starting with CDC is the same in the span and the SQL comment. And so is the span ID labeled in blue. And one thing to consider is that SQL commenter as a feature is really just this simple insertion of the matching trace context and other metadata. Um it's one piece of the trace context correlation puzzle. And Lynn's going to explain more about how we use it. So like Hammy mentioned, SQL commenter puts that um context in the query.

How do you use it? Well, conceptually we could like she showed um look at the server log or we can interrogate some system views and sort of parse it out mentally and try to piece things together. That's obviously not great and tedious. Uh what our product uh built on is the fact that our database um monitoring also samples the queries seen on the server side similar to how um instrumentation will sample requests for detailed tracing. So when our database monitoring detects in a sampled query that there is the trace parent um comment it can parse that out and then we can we start building a correlation mapping between the trace and the query sample. uh this is keyed on the query text and other metadata such that we can say an APM query fingerprint was executed by a particular service at a on a particular database instance. Well, that was simple. Let's strip it. Not really. Um this is software. So we ran into a big limitation in the form of prepared statement.

Prepared statement. So database servers have to parse and then plan and optimize a query. uh when it's received um prepared statements which is supported by certain databases optimized for the case of repeatedly executed queries. The way it works is um kind of simplified in this diagram. The client will make a prepare call with the actual statement text to the server. Server parses it and stores it and then hands back a statement ID. And then to e to actually execute that statement, the client will send just the statement ID and any parameters that need to be bound into the placeholders. So one major bonus of of this is that it prevents SQL injection because the bound placeholders are never bound parameters are not treated as SQL. The big problem for SQL commenting for us is that that uh there is no opportunity for the client instrumentation at the execute point to update the trace context. So it's always frozen as the same one that was passing in the prepare.

Um okay well pro one way to solve this. Let's just not um insert uh the the context for prepared statements. Um that was easy and at that's actually how we sort of go about this for certain languages. We just don't do the injection for prepared statements. But what we found with our APM Java custom dro where we um added support for SQL comment ourselves is that by not inserting into prepared comments a prepared statement it was making this integration pretty much useless because Java OMS like um hibernate use prepared statements heavily. So by not inserting we we don't have any integration. So we had to look a little harder. Uh we looked a little harder and uh understood that a lot of client APIs offer abstraction to provide a uniform API even when the server does not support prepared statements. So um kind of again simplified in this um diagram. The API presented could be something like uh um make a prepare call and this is the API that's offered to higher level um callers like your actual application or your OM.

So this uh database client could is generally like the driver or adapter level client. um you make a prepare call, you bind some parameters, you execute it. Or it could be as simple as you just make an execute call um with the um placeholders. And if it in actual prepare statement mode, it will actually go and do the prepare and execute. But if the client is in emulation mode or if the database server is not does not support prepare statement essentially the client will actually construct a final statement and interpolate in all the parameters and then send it as a non-prepare statement to the server which in this uh emulation mode it actually kind of bodess well for the ability to propagate the correct trace context. So, uh, but wait, there's a tiny bit more. Um, client side cache. Many drivers and adapters support statement caching to, uh, skip the processing work that's needed before, um, actually making the the execute call to the server.

This pre-processing work could be templating the the statement or or actually making a prepare call and getting back like the reference statement ID. Um so this diagram shows the simplified version again that on getting that statement the client will go and check the cache for a processed version and if it only if it does not find it then it will go through the work of processing the statement and putting it in a cache. If the cache key is a statement including the full comment uh it means uh on the good news is that the correct trace context actually could be propagated sometimes even for a server side prepared statement. The bad news is by essentially making every um statement unique, we are interfering with the caching causing um unnecessary processing and um like really rendering the cacheless useless and this would be both on the client side and the server side. Um we took some quick looks at how how it is in the JDBC world JDBC driver.

So my the MySQL connector J driver by default does emulation and does not uh have client side cache enabled. So in this case it actually kind of is probably worth doing. Uh a different story with the Postgress database driver where uh client side caching is enabled and ser actual server side prepare statement is also enabled in which case this would cause quite a bit of performance overhead. Uh, is this all worth it? Well, what I can say is that um the type of end to-end insight that's possible to build on top of this um trace context propagation is pretty neat. And I'll show like a super quick de record demo from our product. Here we have a um trace that has like the database span. And oh, now it's doing the spinning. Um, well, it is a demo even if it's pre-recorded. Um, I don't know. I could skip the demo if it's just not going to run. Yeah. Well, yeah, you know, I'll leave it to your imagination, but uh it's pretty nice.

We can traverse from the span to the database query and database insight and then traverse back. But um yeah, I think so sorry about that. Uh we were gonna show our amazing back end and what it's doing. But anyway, pretend that you saw our great demo and uh we're going to share what could be next based on our own uh experiences using hotel for a backend and what we've seen in the community. So um ideally this is something that all the database vendors would implement which is native support of uh trace context propagation. Um, but it's it's a bunch of different vendors and that would probably be slower than an OTEL project, which isn't super fast either, but um it could be possible. And there's a recent um hotel seam PR that came out last month by uh users since June and it proposes some database context propagation definitions in the SEM conff. Uh this was recently spun off um their same.net net PR to add SQL commenter as well as those uh Java PRs I mentioned by Lori T ear earlier and um this would be great to define a vendor agnostic context propagation mechanism for hotel so that everyone in the open source community can use it too and not just uh people paying for paying vendors.

Um the downside of course is the uh high cardality values like uh trace parent and this this is a link to that that issue uh screenshot on the top right. Um if that gets adopted as an hotel project it'd be great if there were more uh implementations that were resumed uh through more community PRs. So uh merge and release that JDBC PR um start new PRs to uh migrate Java's Hibernate and Spring Ruby and PHP's OMS from Google uh over to hotel. Um this screenshot in the bottom right just shows uh the coverage that the Google original Google secret com SQL commenter provides and that that code is still still there and still valuable. Um, last thing I want to share is kind of specific to OTEL Python because I worked on it a bit and my question is how do we generalize instrumentation uh and context propagation for all the different database client permutations that are possible or who should be writing the SQL comment?

Um, for pretty much all Python applications that connect to a database, you should you have to use a database driver and that's your MySQL connector, etc. I mentioned already SQL company SQL alchemy is helpful but it's actually optional. Um same thing for tortoise or Django's RM for example. Um so just to go back a bit actually there is some SQL commenter implementation for all of the above uh but not all of them and there are some inconsistencies I'm going to talk about next. two black boxes again. This time these are just both two MySQL log entries we're looking at with SQL comments. The first one should look familiar. It's a SQL comment with a trace parent and some database driver metadata. And that's what happens when you enable SQL commenting for Python's MySQL connector driver instrument. This second one's pretty interesting because we we've enabled SQL commenting in the inventors for both both Flask and MySQL connector.

We still get a single SQL comment with a trace parent and driver information, but it's also been enhanced with some web app info like the controller request city is what it's called or the route/ city. And it's pretty neat. Um the flask in instruments flask instrument for hotel uh gathers that web app info at time of tracing and instead of writing a comment it puts it it attaches it to the current hotel context and the SQL connector inertor checks the existing context and enhances the uh comment and writes. So that's pretty neat for those those vendors and users looking for it. Um however if you enable SQL commenter for the Django OM instrumenter and my SQL connector we get the problem of two comments. Uh they both have trace parents with the same trace ID starting with BCA labeled by the red arrows but they have different trace I different span IDs sorry. So, this is a problem um because back-end developers don't necessarily know which comment to use if there's 0, one, or more.

This also contradicts the original Google spec for SQL commenter, which states that if a comment's already there, you shouldn't mutate the statement. Possible solutions moving forward. Should it always be up to the user to decide which instruments are enabled or disabled or which are doing SQL commenting? Should it still be the instrument's job and should they all be like flask where they attach KVS to the current hotel context before the comment is only written by the database driver or should they check for a comment first and skip if it exists something simple like that or or maybe it should be the SDK that does it instead through a propagator and so uh that's why PRs like the open one for the SEMC con are really important um it'd be great to have some standard ization across languages and and database frameworks. Uh to summarize what we talked about today, propagating context to trace to data sorry propagating context to database servers helps support endtoend observability.

One method is su commenter which is the injection of comments into into query statements and currently there is open discussion uh about whether or not the hotel sum should define SQL commenter as a standard. Uh we note that several hotel language inventors already support this but inconsistently and we got to consider the performance impacts. Thank you.

## Related keywords

[[trace]] [[database]] [[server]] [[statement]] [[client]] [[span]] [[context]] [[actually]] [[log]] [[commenter]] [[otel]] [[tracing]]

## Notes

- Raw note imported from CNCF YouTube playlist. Promote durable insights to topic notes under `03-Topics/`.
