---
type: session
event: "Open Observability Summit + Otel Community Day NA 2025"
year: 2025
kind: session
youtube_url: https://www.youtube.com/watch?v=fU6jsw0yaVU
youtube_id: fU6jsw0yaVU
playlist: "Open Observability Summit + Otel Community Day NA 2025"
playlist_id: PLj6h78yzYM2NFT2PGItX2idBf7v8fHcy7
playlist_index: 10
speakers: ["Liudmila Molkova", "Microsoft"]
topics: ["OpenTelemetry", "Metrics", "Tracing", "Logging", "SLOs", "Kubernetes", "AI Observability", "Security"]
keywords: ["telemetry", "library", "dependency", "span", "version", "application", "semantic", "kubernetes", "tracing", "opentelemetry", "logs", "metrics", "security", "metric", "log", "libraries", "actually", "depends", "client", "instrumentation", "observability", "plug-in", "awesome", "upload"]
transcript_file: _sources/transcripts/youtube-playlists/open-observability-summit-otel-community-day-na-2025/no-dependencies-no-plugins-just-native-opentelemetry/fU6jsw0yaVU.txt
transcript_chars: 12852
status: transcript-downloaded
---

# No Dependencies. No Plugins. Just Native OpenTelemetry - Liudmila Molkova, Microsoft

## Metadata

- YouTube: https://www.youtube.com/watch?v=fU6jsw0yaVU
- Playlist: Open Observability Summit + Otel Community Day NA 2025
- Speakers: Liudmila Molkova, Microsoft
- Topics: [[OpenTelemetry]], [[Metrics]], [[Tracing]], [[Logging]], [[SLOs]], [[Kubernetes]], [[AI Observability]], [[Security]]

## Transcript

I'm going to use this opportunity to share we have geni semantic conventions and instrumentation working group in open telemetry which is the same as the project you've just seen but in open telemetry sorry um okay so this is the last talk of the day you're exhausted but let's have some fun um I am here to share what we do in Azure SDKs to deliver the awesome instrumentation that comes without dependencies, without plugins. It's just pure magic. So how do we do this? First let me introduce myself. I'm Lill Mova. I'm work at Microsoft on Azure SDKs on observability. I'm the member of up until technical committee and maintainer of semantic conventions. Sorry, I was running here. I forgot to take my back from the back room. Cool. So why do libraries need observability? Well, it's actually not for us, it's for our users because we don't hate them, right? So it happens more that they want to that user come to us with a question like you know what we're trying to upload some stuff to the blob storage and it's slow but only on Kubernetes why and like we have so many questions.

What data, how much data, which region are you deployed to, where do you upload stuff to, do you see any errors, what are your Kubernetes limits? And endless amount of questions, right? And there is a lot of keystrokes and unhappy users involved in this experience also unhappy library developers, I can tell you. So we don't want this to happen. We want to provide observability to our users. So they can either answer these questions themselves or when they come to us they can answer our questions to them. Right? Okay. So should we do more of this the login stuff? Can you read it? Neither can I. So we prefer we prefer structured queriable unified telemetry. We want to have logs. We want to have metrics. um we want to report them in unified manner. But in order to do this, we need to actually depend on open telemetry. And if you're a library developer, when you hear a dependency, you no just no um straight ahead.

So even a small dependency introduces a huge pain. Um there are like perception problems and there are real problems um with open telemetry API. It's a tiny library. It's super stable. Uh it does not have CVS. It's it's not problematic. We are very serious about compatibility in open telemetry world. But there are other libraries and um for example my team owns maybe more than 100 uh of libraries in every language. So if we depend on something and it has security vulnerability and CV is reported we need to re-release everything with the updated version of the dependency. It's not fun. Nobody likes releasing libraries. It sucks. Um the other problem is the ecosystem problem. It's less of a problem in some environment but let's say in Java we have open telemetry. We also have micrometer tracing and some people don't want open telemetry. They or legacy applications. The other point is breaking changes. We definitely don't want libraries to introduce them, but they do.

They do it in main minor versions, patch versions, they do. And those problems that are more uh perception problems, they don't apply to open telemetry API. They do apply to other libraries. The key problem that still applies to open telemetry and you cannot fix it. It's the diamond dependency problem. It's also the forward compatibility. So let's say let's talk about it more. Um let's say my application depends on library A and library B and library A wants open telemetry API version 1.0. Library B with wants 1.4 1.42. You would imagine dependency resolution mechanism would do the right job. No. Uh well sometimes it's better in Java. I think it's the the worst. It's random. Uh so if you use Maven, what it does, it explores your dependency graph and the first time it discovers a library, it picks that version. So if you place library A higher than library B in your pom file, it will resolve 1.0. If you put library B higher, it would resolve 1.42.

If you use environment like spark, oh god bless you. Um they the spark comes with a preset uh libraries that they use and they don't they ignore whatever you have in your pom file. They will just force something onto you. So in reality what happens is if you happen to use something like span set all attributes method you expect it to be there. Your library depends on open telemetry API that has it but it was not there in 1.0. So you will get some runtime exceptions like no such method. So let's see a demo. I hope it works. Yay. Okay. So here we have some application some library. It's an uploader client that depends on open telemetry by directly. It has some instrumentation in the pom file. You would see open telemetry API. It is actually 142.1. [Music] And we have an application. Let's take a look at the application. Okay. It depends on our library, but it also depends on open telemetry SDK. And the version of this SDK, it should actually 1.42.

Okay. And uh application is super trivial. Let's take a look at it in a second. Here we go. So we just configure open telemetry SDK. We upload stuff to the storage. Let's give it a run. Okay. Upload complete. Wonderful. So now let's go back to the pom file and let's update the version to 151. We don't do anything bad. We just update the version of open telemetry in application, right? It should be fine. Let's give it to run. Oops. So there is some open telemetry API method that's missing in that version that SDK actually expected. So how do we investigate it? Uh there is a tool called Maven dependency tree. There are other tools for other environments. Um my demo should get there in a second. We'll see it. I promise. Okay, here we go. So it's very verbose, very detailed. It's somewhat hard to understand it but you can see that there were the version 142 of open telemetry API what was omitted and oh no it was actually used and 151 was omitted because there is a conflict and by having dependency on open telemetry API version 142 we forced everybody who used this as library to get stuck on this version of API.

Okay. So, how do we solve it? Actually, um there are a few patterns, common solutions. One of them is shading. It's also known as vendoring. So, you take some external library, you bake it into yours if the license permits. You usually relocate classes so there is no conflict. So it comes from like a different package. Uh it works awesome for something like serialization or self-contained stuff. It does not work for observability because you need the same type to represent the context. Right? You cannot create a span as a child of something you don't recognize. Same goes to tracer providers and other things that SD configures. So scratch this shading doesn't work for us. The other common pattern is a plug-in. You see it a lot uh where the labor itself never calls into open telemetry directly. It has an abstraction. You call into abstraction. There is a plug-in. The plug-in contains open telemetry implementation.

Plug-in depends on open telemetry API. Now your users need to install this plug-in first before they get observability. But once they do, does it solve any of the problems? Nope. It only solves the problem that it's very easy to roll back. If your instrumentation causes a problem, you uninstall plug-in, you deploy your application, it's back to where it was. Uh so it limits the blast radius, but it doesn't solve much. So the solution uh we are using and we're very happy with it in our new generation of SDKs is um let's call it optional dependency for the lack of better word. So essentially we use the abstraction the abstract span or abstract metric whatever uh everything calls into this abstraction and at runtime we see oh okay is there a telemetry on the class pass does user application depend on it is it configured let's use it then if we see anything suspicious if we see any reflexive exceptions um and stuff like this we can always fall back to a safer option.

So this way user just gets observability when they install our library. We don't force any specific version onto them. Um and also we provide a nice fallback mechanism. So when things are don't go as expected, if there is some missing API or something, we can still handle it gracefully. Okay. So there are a few ways to do it. In Maven, it's called optional dependency. It still shows up on your pom file. Um we don't use it. In gradal there is a compile only dependency which is kind of awesome. Uh and anywhere in Java and beyond you can use reflection for this. in um languages like Python, TypeScript, you can always use duct typing of some sort too. Uh but the the principle is the same. So let's see a demo. Okay. Now we have a little bit more interesting uh uploader client. It also has instrumentation but this instrumentation does not come from open telemetry. It comes from the package called io.client client core.

It's the new core we have for Azure SDKs in Java. This library only depends on client core. We'll see in a second that it it has no idea about open telemetry at least from the dependency tree. The application is pretty much the same. We have open telemetry configuration uploader client. Um we will run it in a sec. But first, yeah, in the pom file, we'll have the the version 151 that we failed on before. And I promise we won't fail on it again, right? So, we also depend on our new library. Let's explore the dependency tree. Okay. So it's a little bit shorter now. First you can see that the client core does not depend on anything. It's dependency free. Yay. Uh the only version of open telemetry API we see is 151 which is awesome. It means there are no conflict. And let's give this up a run and see what happens. So it succeeds and we will see some traces in a sec. Okay, this is the upload span we created in the library that there is also HTTP span under wonderful so you might have questions and concerns.

I did first it's errorprone right? So imagine your code kind of assumes the open telemetry is there but it's not there in real life. So you need to be sure you always call into the abstraction. You don't call into open telemetry directly. You should test without open telemetry. You should also test separately with open telemetry. And if you want to be robust, you kind of want to test with multiple versions so you know what you know what you're compatible with. Um, you should probably prefer old and inconvenient APIs instead of bright and shiny new ones. Um, and things sometimes will go unexpected, right? Like somebody will bring you up in telemetry 1.0 that you cannot be compatible with. So just fall back to whatever is your default no or we'll talk about it in a sec. the Perf we're using reflection we're using efficient reflection but the Perf uh thanks to Jason talk Jason is not here but he talked about the uh percentage of overhead uh here I don't want to talk about percentages I want to say that our solution which is not completely optimized there is room for optimization we have about 100 ncond overhead per span Then um okay so the last concern the ecosystem right you kind of bake open telemetry into your library um guess what you don't you don't have to bake just open telemetry if you really want we don't you can bake micrometer tracing as well uh but you can also fall back to something and it does not have to be knob so my favorite part about it that okay when user comes for support and they don't have open telemetry, they do have login like chances are some sort of login.

Um so let's leverage this it let's degrade gracefully right. So let's take a look at the demo. Let's take the application we just run and let's remove open telemetry from it. We'll comment out this code. We'll comment out all the dependencies. Let's pretend open telemetry never existed. Okay, I'm just going to run this application. The first thing it won't fail, which is awesome. The second thing, now you see a couple of log records that are written down. There are ugly JSON unreadable but that what logs are already or ugly unreadable JSON when you print them to console. Spans are even uglier when you print them to console. Uh and they are actually spans. We are recording spans as logs. Uh it's hard to read but it's better than not recording them at all. And we even pretend to have tracing not a distributed tracing. we generate the spans and the correlated um the best we can do. So yeah, that's essentially what I wanted to share and uh dependency comes a lot.

It's always a problem and I wanted to find a solution and I'm excited we found it and I hope we can share all this ugly reflection code that we have with somebody else. If you are interested, there are links in the um presentation. You can also chat with me, ping me on Slack, anywhere. And thank you for coming. It's the last talk. You're exhausted. Let's go have some fun.

## Related keywords

[[telemetry]] [[library]] [[dependency]] [[span]] [[version]] [[application]] [[semantic]] [[kubernetes]] [[tracing]] [[opentelemetry]] [[logs]] [[metrics]]

## Notes

- Raw note imported from CNCF YouTube playlist. Promote durable insights to topic notes under `03-Topics/`.
