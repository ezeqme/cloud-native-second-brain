---
type: session
event: "Open Observability Summit + Otel Community Day NA 2025"
year: 2025
kind: session
youtube_url: https://www.youtube.com/watch?v=K766mW0PLq0
youtube_id: K766mW0PLq0
playlist: "Open Observability Summit + Otel Community Day NA 2025"
playlist_id: PLj6h78yzYM2NFT2PGItX2idBf7v8fHcy7
playlist_index: 18
speakers: []
topics: ["OpenTelemetry", "Collectors", "Metrics", "Tracing", "Logging", "Cost Optimization", "Kubernetes", "AI Observability"]
keywords: ["collector", "log", "logs", "pipeline", "telemetry", "logging", "pipelines", "metrics", "semantic", "gateways", "sampling", "gateway", "tracing", "custom", "cost", "otlp", "kubernetes", "cluster", "processor", "otel", "process", "spans", "span", "pods"]
transcript_file: _sources/transcripts/youtube-playlists/open-observability-summit-otel-community-day-na-2025/building-composable-otel-pipelines-ci-cd-testing-team-first-and-sca-anil-kuncham-joe-canuel/K766mW0PLq0.txt
transcript_chars: 20552
status: transcript-downloaded
---

# Building Composable OTel Pipelines: CI/CD, Testing, Team-First, and Sca... Anil Kuncham & Joe Canuel

## Metadata

- YouTube: https://www.youtube.com/watch?v=K766mW0PLq0
- Playlist: Open Observability Summit + Otel Community Day NA 2025
- Speakers: N/A
- Topics: [[OpenTelemetry]], [[Collectors]], [[Metrics]], [[Tracing]], [[Logging]], [[Cost Optimization]], [[Kubernetes]], [[AI Observability]]

## Transcript

So uh myself Anil so I'm a software engineer in the observability team at Door Dash. Hey and I'm Joe also an observability engineer at Door Dash. So today we're going to talk about uh our hotel journey and how we build and manage uh open telemetry pipelines to process all of our obser data. Uh just to pile on the talks that we already have uh a quick 101 on what is open telemetry collector. So, open telemetry collector is like a single bind read that can process one or more uh telemetry types like logs, metrics or traces. Uh it has things like components, uh extensions, processors, exporters, receivers, connectors. It's like Lego. You piece all these things together to build like powerful pipelines. So, life before hotel. So our observability team used to we we used to have logs, metrics, traces we'll continue to have uh and custom events. So before hotel uh every uh telemetry type has its own pipeline.

Uh so all our pipelines are fragmented uh which means each of them have their own infrastructure needs uh different knowledge base, different code bases. there's like less reusability uh and uh to support any new use case is a pain because we'll have to implement uh tightly to the protocol or to the actual destination. Um I I think we had for logs we were we had a stint with flink it wasn't very good uh for traces we were using hotel uh metrics it's custom events it's custom so overall uh we used to work in silos because we never get to be out of that pipeline uh dealing with incidents with adoption of hotel I think things are smooth for us um less incidents less firefighting uh the best part is uh we have unified pipelines and like we all all like we started off with like two to three talks today and we are in the consensus that semantic conventions is a is for the win. So converging on hotel pipelines enabled us to do that.

So we have uh semantic enforcements uh across logs metrics and traces done alike. So it standardized all of our data uh which means now we that opens up for new possibilities. So how do we do better cost control or better cost attribution and now we all work on a common codebase. The entire team is like um I would say motivated uh so uh we have a open telemetry repo which is like a mirror copy of open telemetry collector contrib uh we use lot of uh go tool chain uh improvements. So we are passionate about building that contributing to the common code base. Okay. So now we said we've converged on hotel pipelines. Did it work for us? Yes. So we process around like 100 terabytes of log data a day. Um and our spans we have at 15 million plus or like few days ago it was 18 million uh peak span RPS. uh we have we process around like 5 million locks per second at peak and we serve around 3,000 engineers that use our observability platform.

Okay, now that we have seen all of these things work, how do we actually do it? There's like four blocks or building blocks I would say to build a pipeline, right? So you need to have your repo. You need to know what is an OCB, open telemetry collector builder, a builder config and a helm chart. So open telemetry collector builder is it's actually present in the open telemetry collector uh as a different binary. So it takes input a builder config and our hotel repo. So our our hotel repo like you see in the screen in the bottom is a mirror copy of open telemetry collector country where we uh clearly separate exporter process in their own uh packages. They all have their own go. So each is its own component by itself. The builder config actually tells where to pull the code from. So you can say oh pull it from open telemetry collector repo or collector country repo or our own internal repo. It then generates a binary which is a custom open telemetry collector instance.

Like I said we are passionate about our code. We are also not in a position that we could do statements uh in the configs during incidents and cause more incidents. So we want to unit test how we do our pipelines. We want to add integration tests and do code coverage similar to any other product code that we do. A quick example of how we use unit tests, right? So we have click house exporter uh in our pipeline and we convert OTLP records to click house compatible schema. Um and we used to have unit test which is basically mocking the functions but it isn't great but it's it's there uh probably for feelood for us. So recently we again improved it to use like click house in memory database as a way to um instead of a mock function we have a click house inmemory database that we create per test to actually do the transformation and insert into the click house and then destroy it. So now our unit test actually caught bugs that otherwise we would have seen deploying to prod and integration tests.

So we have uh cloudflare hotel collector which is a custom use case that Joe would talk about soon but we have some input we would have to build a pipeline and assert for an output. So we use hotel collector test package to create these test instances of hotel collector process these uh process a test file and asset for the way that we want whether the output matches the way we want. We do can analysis it's manual but we test it with few pods to make sure things are good now that we have built a custom uh hotel collector image. So how do we actually deploy? So what we saw in the previous slides is a builder config. Now what we see on the right is a pipeline config. So each of the components that we have like logging gateways or tracing gateways each of that would take its own custom pipeline. Um so we use uh upstream Helmchart with few modifications. Then each of the values yml for each component would be overridden by the desired artifact.

Right? So that gives us like uh a logging gateway which is an hotel collector tracing gateway which is another hotel collector but with different pipeline configs. At the end all of them gets deployed into kubernetes. So there are uh we have around 30 plus custom hotel components. Um I've listed here a few of them but the why why we build custom components. For example take a look at Kafka exporter. So we have an upstream Kafka exporter. It's pretty good. But our use case is we have to build a tail sampler in our tracing pipeline and we want to use trace ID as a partition key. And back then the Kafka client doesn't support a custom partition key. So we had to build uh our own Kafka exporter again Kafka receiver. Similarly we we upload our log data as park files to S3 uh the S3 exporter. So we have built S3 park exporter and for click house exporter we want to use cho which is a low-level um click house library to for better uh performance reasons but the best part is the hotel framework allows us to uh do this without having to be um without having to have a bottleneck of oh we are limited by the capabilities.

So this is what is powering all of our repo or the pace that we are building. Taking a let's take a look at few examples. So this is our go-to pipeline design. After few iterations of pipelines, this usually works for us. So we have like workloads. They could be like um hotel Java agents or SDKs. They are all they all are the workloads that send uh data to our gateways. Gateways are open telemetry collector instances. They talk to other uh our internal tools or control plane. They produce to Kafka and then we have consumers which read from Kafka and send it to different vendors or destinations. Let's take a specific example at uh one of our uh implementations of logging pipeline. So we use fluent bit as a log collecting agent. So fluent bit send logs to logging gateways. So logging gateway is again an open telemetry collector instance. So we have fluent bits and logging gateway deployed per kubernetes cluster.

Um this helps us to roll out do a slow roll out of our logging pipeline changes so we don't break uh all the clusters at once. Uh kind of canary but in a limited way. Then the logging gateways do the transformations processing and produce to Kafka. Uh and then on the right you see is again the Kafka consumers which are open telemetry collectors. Uh they read the logs uh and produce and processes them and sends to our vendor or our own internal data stores. But as you see because now everything works the next step is like okay so as road dash grows our log volume would grow and but we need to be disciplined about our log vendor spend. So we have developed a logging control plane. Um so the logging control plane helps us to do like sampling, usage tracking and kota enforcements. Uh and if you see uh on the bottom you would see like the derived metrics. So as uh some of the logs are just metrics but we don't need to store them.

They're expensive in the way we store right. So we just derive metrics out of them and then we could choose to discard the log or keep it. This is uh if we just dissect into what that open telemetry collector instance is. This is how a gateway would look like. So we would get logs and the fluent format. We use fluent forward receiver which converts fluent to OTLP record and then the three comp three process I want to talk is the log parser or the semantic enforcement processor. We have the log analyzer processor and the sampling or the load shirt processor. So the log parser or the semantic enforcement processor would help us. So we know our clients better. Uh for example, we have a cotlin platform team. Uh they manage some of our log clients. We know that we have like let's say tenency label or a field within log that we care for. Um but that doesn't come with uh like a semantic convention. But we we come up with our own name spacing to define that attribute.

So we look for that. We look for log message size. Uh people send 1 MB log line sometimes and we have to tag them that it's message is too big or we just truncate it or we drop it. But when we do so we'll have to keep a track of it because we get questions like oh my log is missing what happened and we should be able to exactly tell them it is not following this semantic convention which is why we we had a drop and then the log analyzer processor. So the out of the metric out of the box open telemetry collector metrics are great but it doesn't give full picture of the services or the workloads that are generating that data. So we need uh this is just a metrics processor you could think we we we split or we break down the logs uh by like which service or app or logger is sending uh the logs. So we keep track of that. And then finally we have the load share processor which helps to basically drop logs in the case of there's an incident or there's a bad deployment which is uh spewing like millions of logs and this causing like latencies in on the Kafka side real incident is so we had a load test there was a noisy log that just is spamming uh probably around uh I would say like uh in the order of magnitude of a terabyte of logs an hour right So we saw an increase in latency in the Kafka.

So we we had to load shed it and that's why it's important to have load shed controls at the edge. Um so you you even protect your Kafka cluster. And then on the right you see is the consumer. So uh the one that is highlighted in the blue section is what integrates with our log control plane. So we have the sampling process and the usage reporter processor. So the usage reporter processor emits usage records uh to our log router which is on another service which is outside of the hotel ecosystem and then the s the log router tracks the usage and computes the sampling decisions and then sends that to the sampling processor. So we have like so we have a daily kota enforcement. Um each service gets let's say a terabyte of log data a day and if a service breaches 1 terabyte then we automatically downsample them uh without penalizing the services which are behaving well. Um so we we do this we do all of this automatically.

Uh that's these sampling decisions are computed in the log router. But one guarantee we give to our service streams is like yeah we never drop your error logs. So all of the ditions are managed by the log router and then the sampling decisions are uh fetched or sampling processor would get the uh sampling ditions from log router and apply in the pipeline. So everything is automatic and log router would have APIs and UI so service teams can go and check what their enforcement is. Yeah, with that I would pass on to Joe. Thanks Anil. Uh so next I'm going to talk about a developer use case that came up and we used where we used an open telemetry collector to solve it. Um so if you can imagine for a second you're a product engineer at Door Dash and you're building a new API that's going to be exposed to external customers. We we'll call it the hello world API right and your callers are going to be external customers.

So this might be the door dash apps. It could be on iOS or Android. It might be at the door dash website or other callers. The way this is going to ultimately get to your single small microser in Kubernetes that you're building is it goes through Door Dash edge which we use Cloudflare as our edge provider. It then gets inside to the Door Dash ecosystem where it might bounce around a handful of other services maybe an identity service, a metadata service, whatever uh it needs to be in the call path and then ultimately it gets to your service and you return your simple hello world response to the user. Um the the problem we're trying to solve is if your customer is broken like anywhere in this chain such as Cloudflare is thinking their bot traffic and rejecting them or the identity service is broken specifically for your use case. You still want to know your customer is broken but those requests never actually get to your pod and so your service level telemetry can't give that to you.

And you might be saying, well, the upstream should have alerts on these. And they absolutely should, but you know, in any large company, there's going to be some gaps. And maybe your requests are 0.0001% of some upstream that's failing your request, so they're not getting paged. So, the way we built this is uh first we leverage Cloudflare's um log push job. It's a service they have that allows them to basically send audit logs of all the requests that go through them. So, that gets sent to an S3 bucket we own. And then we have SQS set up to um generate a new message anytime a file gets there. And then this is where the collector really starts. So the green parts are the main um part of this. We built a receiver, a Cloudflare receiver we call it, which pulls these messages. Um when it sees a new file, it downloads that S3 file, goes through the logs and gets them into the pipeline. Uh we then have some processors that do some the batch processor and group I which kind of does some uh aggregation.

But the main piece here is the Cloudflare processor we which we built. Um, this reaches out to Door Dash metadata. So, it's kind of any company would do this their own uh special way, but we use metadata to essentially identify exactly which endpoint any call should be going to uh so that we can correctly attribute it back to that hello world um API for that team. Uh and then finally it gets to the exporter layer where we export it to various uh stores such as Kafka so other teams can use analytics on this data if they want to our metrics store so teams can craft SLOs's and alerts on this and also to click house where we use graphana to visualize the data so teams can really dive into it if they want uh and the reason we felt like this was a great solution rather than building something custom is our team's very familiar with open telemetry at this point um anybody can kind of jump into it and as an mentioned earlier it's much easier for uh the on call to handle.

And so just to paint a picture of what this looks like uh up uh in the top here I have an example of one of these log files. So it has the uh request that comes in and the edge status code. Uh and this obviously fake example you know uh it's 200s for a while. We get some correct four threes or 400s. Um and then you know we get bobby drop tables come in and maybe that breaks the identity service. And so from there on you only get 500s and again your calls wouldn't actually be getting to your service. So your service looks healthy, maybe there's no traffic or it's only synthetic traffic, but your end users can't use you and you so you want to get alerted. So in this case, the team has set up alerts and SLOs's so they get paged. They can then go look at the raw data in uh via Graphana and they can figure out, oh, okay, like they can troubleshoot where to go and maybe the identity team is already on it, but maybe if this was a cloud flare sheet, they go talk to traffic um and they can help their customers get back online.

Uh so next we want to talk about where we are now and where we're going. Um, so we have about 30ish uh custom collector, excuse me, custom components that we built uh in various combinations with of course open source uh components. We have 16 uh unique collectors running across 190 deployments and across thousands of pods and uh it's now pretty good but when we first started up we had a lot of incidents or what I would call opportunities to learn. So um we've take kind of summarized some of those learnings and now when we build a new one it kind of ships pretty pretty smoothly out the gate usually. Um so as Enil mentioned earlier we want to have volume controls. So this looks like uh when we have degraded performance right we have the ability to say only process 80% of our volume which is much better than being hard down for example. Um as well as if we have a noisy neighbor who breaches their quota we automatically kick in sampling for them.

Um, in order to ensure our engineers are happy, of course, we want SLOs's on on these pipelines. So, we have availability and latency for most of our pipelines. Um, and as part of that, we need to make sure that we can autoscale up if we need to. And, uh, one process we found really helpful is the memory limiter one, which has prevented even when we're overwhelmed, we're able to scale appropriately and not not die from that. Um, and then a little more specific uh, for development uh, sake because we use Kafka pretty heavily. So, uh, we kind of had to make sure we tune that appropriately for both the consumer and the gateway side of our setup. Um, and then to identify bottlenecks, um, we've found profiling in development really helpful, as well as the ability to quickly turn on profiling in production because we're using, um, one image that has all of the components built into it. We're able to basically add it and restart the collectors pretty easily if we need to.

And, uh, yeah, what's next? So, as Anil mentioned earlier, we have Canary analysis. Um, it's currently manual, which really bothers me because if I'm doing the Canary analysis, there's always a chance it's going to fail. So, I we really want to automate that. Um, we want to deal with back pressure management. So, you can imagine if um the Cloudflare collector that I showed earlier, um, let's say ClickHouse is down. It's not able to push. Well, we're going to continue grabbing messages. Um, and we're just going to be basically dropping the data on the floor because we're going to process the message, put it into the pipeline, but the exporter is not going to actually send it where it needs to go. Uh, in addition, we've dealt with uh some painful upgrades. Uh, so we just want to deal, excuse me, want to add a little more automation in our own repo to help smooth those over. Um, and mentioned the integration tests that we have for our collectors.

We really like those but it's currently very manual where we have a codebase that has all the components and then we have a different codebase that has how those components fit together in various pipelines. Uh and we want to basically stitch them together so we can automatically generate tests where we spin up those um those pipelines in CI send some fake data through it and then uh check the results. Right now that that's all manual setup. Um yeah and then the last bit is uh feature flags. So we have some feature flags on these deployments or these collectors, excuse me. Um, but right now there's no dynamic configuration loading in in our setup. So when we change a feature flag, we have to bounce the the deployments entirely.

## Related keywords

[[collector]] [[log]] [[logs]] [[pipeline]] [[telemetry]] [[logging]] [[pipelines]] [[metrics]] [[semantic]] [[gateways]] [[sampling]] [[gateway]]

## Notes

- Raw note imported from CNCF YouTube playlist. Promote durable insights to topic notes under `03-Topics/`.
