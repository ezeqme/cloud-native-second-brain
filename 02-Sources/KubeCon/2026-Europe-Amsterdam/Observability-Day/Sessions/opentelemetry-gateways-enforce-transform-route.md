---
type: session
event: KubeCon Europe 2026 Amsterdam - Observability Day
year: 2026
kind: session
room: "Forum (Ground Floor)"
sched_url: https://colocatedeventseu2026.sched.com/event/2DY7t/opentelemetry-gateways-enforce-transform-route-juraci-paixao-krohling-ollygarden-natalie-ujuk-ig-group
youtube_url: https://www.youtube.com/watch?v=S6z2gd666qg
youtube_id: S6z2gd666qg
match_score: 0.799
speakers: ["Juraci Paixão Kröhling", "OllyGarden", "Natalie Ujuk", "IG Group"]
topics: ["OpenTelemetry", "Collectors", "AI Observability"]
status: transcript-downloaded
---

# OpenTelemetry Gateways: Enforce, Transform, Route - Juraci Paixão Kröhling, OllyGarden & Natalie Ujuk, IG Group

## Metadata

- Schedule: https://colocatedeventseu2026.sched.com/event/2DY7t/opentelemetry-gateways-enforce-transform-route-juraci-paixao-krohling-ollygarden-natalie-ujuk-ig-group
- YouTube: https://www.youtube.com/watch?v=S6z2gd666qg
- Room: Forum (Ground Floor)
- Speakers: Juraci Paixão Kröhling, OllyGarden, Natalie Ujuk, IG Group
- Topics: [[OpenTelemetry]], [[Collectors]], [[AI Observability]]

## Transcript

Welcome everyone to open telemetry gateways and force transform route. My name is Judasi and uh I'm a software engineer at O Garden. My name is Natalie Ujuk and I'm observability lead at IG Group. >> And for the next 20 something minutes, um we're going to show you uh a few options on how to architect your open telemetry collectors as gateways as layers that can solve specific problems and problems that we face in the past. So we'll cover how we how we got here. So what are the problems that we are solving and uh how we use the collector to address some of those problems, some of those challenges. We have a short demo. Uh all the code that we have here is available on GitHub already and we should have a few minutes for Q&A towards the end.

First let me tell you a little bit about IG groupoup. IG groupoup provides investment and trading services to clients worldwide. We're constantly challenging ourselves to build the best technology platform. Today we have over 800 engineers located in tech hubs around the world in London, Crackkow, Bangalore, and Chicago. For us, reliability and performance aren't just nice to haves. They're directly tied to customer trust and revenue. If our trading platform goes down or even performs poorly, our clients notice immediately. So, observability isn't just optional for us. It's absolutely critical. Now, IG has been building technology for quite some time. And like many organizations, we've been always moving fast, delivering features, launching new products, and expanding into new markets.

Over the years that pace has left us with a very diverse technology landscape. We operate across on-prem, multiple cloud providers and our container platform. Our services are also written in a variety of languages, whatever made sense for solving the problem at the time. And of course, we have legacy systems too that can't simply be switched off because they're still critical to live trading. And that's the reality of operating a financial services platform at our scale with hundreds of engineers, thousands of services, and years of accumulated infrastructure. Observability gets messy very quickly. We've run into all sorts of technical challenges. But honestly, the constraint isn't technical. It's human.

Developers are busy delivering features. teams are understandably reluctant to touch legacy code for fear of breaking something critical. The observability engineering team doesn't have the bandwidth to pair with every team and documentation around best practices and standards. Well, we all know that that doesn't get read. And that led us to the key realization. You cannot scale observability by teaching every team to do it right. So what does observability require at scale? First, standards, but not the kind that lives in documentation. Standards that are actually enforced by the platform. Second, we need controls. We need the ability to filter telemetry and validate, react, or even drop the data before it reaches the back end.

Third, we need simple defaults. Developers should focus their time on understanding their systems and their telemetry and not the mechanics of getting highquality telemetry into the back end. And finally, we need flexibility because the reality is that standards or backends can change and you don't want to rewrite application code every time your observability strategy evolves. All of this requires a central control point in the telemetry pipeline and that's where the gateways come in. The pattern we landed on is a centralized open telemetry collector gateway. Applications send telemetry to edge collectors where the configuration stays simple and lightweight. Those collectors forward telemetry into the gateway layers.

The gateway then becomes that place where we can enforce policies, transform telemetry and route data to different backends. This creates a centralized control plane for observability data. Instead of pushing logic into thousands of collectors, we apply it once at the gateway. Here is a very simplified view of our architecture. As I mentioned, we have applications that run across multiple environments. We've got AWS cloud environment, our Nomad container platform, and our on-prem infrastructure. Each server sends telemetry to an open telemetry collector that's running locally or nearby. We have provided patterns for the most common application types for their environment. The collectors then forward telemetry to our gateway layer and in our case through CFKA.

We have separate CFKA topics per signal. one for spans, one for logs and one for metrics. This gives us the flexibility to split our gateways by function if required. But most importantly, CFKA is handling the back pressure. The gateway becomes our central point where we can manage incoming telemetry before it reaches the back end. And in our case, we have over 10,000 edge collectors. And this architecture allows us to introduce control centrally without requiring multiple changes to either application code or edge collector. >> I just wanted to say that the next slides um and before I'd like to call out that quite a few things that we are going to show here they can be done with weaver. So if I were to start today a few of of the things here I would probably consider Weaver but uh this is how it was actually done in production since running right so keep an eye on weaver if you plan on on doing those things but there's quite a lot of things that we can learn from the patterns here.

So the first capability gateways can enable is enforce. And the purpose here is simple. Validate telemetry and enforce standards before the data even reaches the back end. And here are a couple of use cases from our gateways. So first is mandatory fields. We require all developers to send a few mandatory tags with their telemetry. These attributes are service.name, service.namespace and deployment environment. We can then reject telemetry that doesn't include these attributes. Without these attributes, the telemetry becomes meaningless in the back end. Second is validation on the value itself. For example, we validate service.name value against a source of truth. In our case, a configuration management database.

The CMDB for us is a register of all services. If a server sends telemetry with an unknown or invalid identity, it can be blocked. The third use case here is PII protection. The gateways allow us to implement a layer of defense. While we expect all developers uh to not send any personal information through it can sometimes leak. This layer of defense allows us to redact or drop an attribute. This is to show ensure that sensitive data never reaches the back end. So let's look at how PII protection actually works with real OTTL rules. That's the open telemetry transformation language that's running inside our collector gateway. We're using two complimentary approaches. First is pattern redaction for when PII location is unpredictable.

Here we're scanning specific attributes for email patterns using reax. When we find a match, it is replaced with four asterisks. This is defensive. We don't know exactly where emails might appear in the event. So we scan for the pattern. The second approach is attribute removal. For when we know exactly which attributes contain PII, no scanning, no pattern matching. We just delete the keys. We can remove entire attribute trees with delete matching keys. Anything that's matching in this case customer anything will get removed. But we can also target specific attributes with delete key. And in this case, we're just removing the email attribute if it exists. So the key insight here is that the OTTL gives us a programmable policy engine directly inside the collector pipeline.

We enforce data protection policy centrally at the gateway layer without touching the application code. So we have pattern reduction for unknowns and attribute removal for known structures. The second capability gateways enable is transform. The ability to transform at the gateway allows us to modify in-flight telemetry. For example, we can fill in missing attributes when instrumentation is incomplete. We can normalize naming conventions when teams use inconsistent attribute names. We can also add system context that applications don't know, such as infrastructure metadata, environment context, or ingestion metadata. And we can also enrich telemetry with additional data sources. The purpose of transform is to improve the quality and the usability of telemetry without requiring any code change.

Here I'll show you an example of using OTTL to enrich telemetry from another data source. In this example, we're adding a new attribute called frontend.client.ip. The value is extracted from the request body. Then we use a geo IP database to enrich that IP address with location data. This gives us additional context like city, region, and country. Our front-end applications typically don't know this information, but the gateway can add it automatically. The third capability gateways enable is root. The ability to root data allows us to intelligently direct telemetry to different backends or pipelines. For example, we can route based on deployment environment, allowing us to send it to different backends or instances and buckets within a backend.

We can also route to different pipelines within the collector, meaning that we can apply intensive processing selectively to only events that require it. And we can also implement similar for cost optimization purpose too. And the third example is we can distribute traces um sorry and the third example is that we can also route from gateway to gateway. We can distribute traces across sampling gateway instances for load balancing using the trace ID for routting. This is to ensure that all spans for a trace end up in the one gateway instance. The key benefit here is we can change how telemetry flows from a central place. So let me show you a few examples. Here we see an example of selective processing for for performance reasons.

In practice, not all telemetry needs the same level of processing and we need to consider that some operations like patternbased matching via reax can be can be extremely resource inensive on the gateways. if it were to process every single event. So we've implemented a PII reduction pipeline where we're able to limit the scope of the service which actually require the processing. You can see here that the routting logic uses conditions like root where resource attributes um requires service name equals service. This dramatically reduces the amount of processing required and improves the performance. Now another powerful use case for selective processing is for cost control. Some metrics generate extremely high cardality.

So for example JVM per thread metrics or even histogram bucket metrics. Instead of exporting these directly to the back end we root them through an aggregation pipeline. First the rooting logic here again uses OTTL conditions like is match where metric name has a prefix of JVM underscore. These metrics are then aggregated before export in our environment. This has reduced time series counts from 304,000 down to 7,000 which is a 97.5% reduction. Applying this to other metric series can also dramatically reduce our observability costs. >> Cool. So, um I'm running a demo. I think it's really cool what Natalie just showed that um we can route data, we can send data to different pipelines based on the on the services that we are receiving this data from.

Um there are new semantic conventions that we can use for that as well like the service criticality that people mentioned earlier today and perhaps we can have a specific attribute showing this service touches PII. So I go to the to the pipeline that removes PII and everything that doesn't touch PII can go to a fast track as shown. So anyway, um I need a a minute to change the displays here to mirrored and then we we move back and hopefully it works again there. There you go. Cool. Okay. Again, all of this code here is available on GitHub already. Uh and I have a few examples. One is uh implementing the uh the enforcement of mandatory fields. In this example here, we are requiring all of the traces to contain service.name.

Right? So it should not be new and it should not be empty. Um I let me open here the read me and then we have here an example. I'm going to use telemetry gen for that and example is no wrong shortcut. There you go. So we start open telemetry collector contrib examples force 01. There you go. Collector is running. Anyone here needs to know a little bit more about the collector. So what is What is happening here? Who doesn't know about the collector? Okay, cool. I can teach you more later. I don't know. Uh, cool. All right. So, let me start a new a new tab here. Cool. And I'm going to paste at Gen command here. So, what's going to do now is uh I'm going to go back to this. Yes. Cool. Uh and then local host 8080 8 with metrics.

Okay. So it shows me that I received so hotel collector receiver accepted spend. So I received 10 spans and I exported 10 spans. So everything that came in went out. So no filtering. And this is fine because my command here is specify the order service as a service name. Now I'm going to do the same but now I'm not going to send the service name. It is now empty there and wait a couple of seconds we see that we have now 16 18. So we have here 20 accepted spans. So we received 20 spans but we exported only 10. So what happened to the other 10? So they got filtered uh because of missing service name. So I know that there's a missing service name somewhere in my pipeline that I need to track back. I need to track down and fix that because as Natalie said uh depending on the back end that you use service name is really essential.

Um and if you are doing open telemetry service name is essential no matter what you do. All right. So this is one example. The second example that I wanted to show you is the one that I like the most which is the PII routing. like if um redacting PII is really resource intensive as Natalie said and because of that you don't want to do that for all of the telemetry that is passing through your pipeline you want to be smart you want to do some observability engineering here uh you want to know about your telemetry you want to know about the domain that you have and you want to optimize the processing u of your pipeline and you do that by saying in this example here um we are saying okay so if it belongs to know your customer service then we should definitely go through the PII reduction.

So it might be a a source of PII. Who's here confident that their p your pipelines are not leaking PII? Yeah, I thought so. Okay, cool. All right, cool. So the second example is this one. examples um route and 01 PII selected routing. So we all established by now that you are all leaking PII, right? Uh so you can now direct all of your services that touch PII to a specific uh pipeline that that uh clean cleans up that data. Uh and the example here is this. So I'm sending some data um with an email in it. Email addresses should not be in PI or perhaps we should try credit card numbers. There you go. And uh the example here, let me Okay, so in this example here, um are we running the right one? We are. Let's pi I'm going to run the other example then and I need to fix the examples.

Um, so I'm going to run this one here which I tested this morning. Oh yeah. So the Yeah, I'm sorry. So the routing is basically saying um so it it is saying that we went through the gateway pipeline PII reduction. So this is what what it's showing right. So we are going through a pipeline that is doing the PI reduction and uh we are actually redacting the the email address here. So this is the example for this one. And the third one I really wanted to make this one here work on my machine with a Jio IP address because I thought that was really cool uh when he showed me the what you why you do that in production. um but I couldn't get this database. So um what I'm going to show you is uh how to normalize some of the attributes that we've seen.

So in some cases we see service name coming to the to the back ends or service.name or things like that like deployment environment_ame or as I've seen on a on a slide here today deployment.environment and it should be deployment.environment environment.name, right? Everybody knows that. So, transform. So, we use the the normalize uh example to convert the deployment.vironment u um resource attribute into deployment.environment.name which is the new name BA for for based on the semantic conventions the latest version of the semantic conventions. But we also do uh if we see service underscore name we also convert into service.name and same for service namespace and so on. So it is running there. What we can do now is get the readme.

So this is one example. So deployment environment name. So we run here. And there you go. So, and we see that we didn't drop the old value, but we copy that into a new value. So, we can do a phased migration. So, we change our alerts, we change our dashboards, we change the consumers of this one here to the proper one. And then on a a future step we then remove the the old ones. All right. So I think this is what I had for the demo. Let me switch back now the display to shared some great examples of what you can do at the gateway later gateway layer and we've also seen it in action in the demo. But why does this architecture scale? There's the obvious answer. Centralized configuration, no environmentwide rollouts of configuration to 10,000 plus edge collectors.

We've got our credentials managed all in one place. We can apply standards and governance quite quickly if the need arises. And we can change the backend vendor without a long drawn out migration process. We can even test out new observability vendors with live production data with one merge request. That's all great and it's all true. But the real reason this scales isn't technical. It's human. It scales because it takes the pressure off development teams and the observability engineering team. For developers, observability just happens. They point at one endpoint and they're done. They don't think about how to get their telemetry to the backends or need to know credentials to do so. They ship their code and move on.

For the observability engineering team, we enforce, transform, and root. All from our repo. No chasing teams, no mandates, no hoping that people read our docs. We said at the start, you cannot scale observability by teaching every team to do it right. And you don't need to. Cool. So to recap, um you can't scale observability by teaching every single team to do it, right? It just doesn't work. And one of the possible ways of of solving that or the way that uh that it's been doing u um recently has been by making the use of collector gateway. So you apply rules at the collector. So you centralize uh the the the uh the control point between applications and the back ends. So you have control of the telemetry that is flowing through the pipeline that enables the enforcement of standards after data leaves the application.

It enables the transformation of inflight telemetry to fix the instrumentation problems and add some context perhaps and route intelligently uh without changing the application code. Now it is not for free so it does come with some g gotas right so the first one is then your your gateway becomes a critical infra so suddenly all of your telemetry is flowing through these complex layers of collectors um and you have to plan for high availability you have to plan for maintaining that um and it's very tempting to overengineer when you start decomposing things and you decompose too much uh so you start simple and add complexity as you need um don't overengineer on day one and one key thing so if there is one thing that I would tell you uh to get from this session is keep the edge collectors as close to the vanilla as possible keep it as simple as possible those collectors at the edge they should only be shifting data pushing data out to a gateway somewhere now that gateway or that layer of of gateways that is the one that should be doing the the bulk of the processing code examples are available so you can write one uh for on your own and I think we don't actually have time for questions unfortunately um but um I am available to answer your questions whenever we do okay cool thank you nice oh we have five minutes wonderful yes so >> can you Yes.

Oh, okay. Yeah. I would like to ask if the OTTL metrics are stable enough to understand how much uh attribute we removed, which ones we removed and tell back the end user so that they stop sending or so forth the how many attributes we removed or how many transformations we made. So the the transform processor um so uh you can have transformation. So in the example that I that I've shown there you can have um each type of >> number of spans but >> how many attributes I I am removing per day because then it becomes invisible. >> Yeah. >> Because of you are removing you don't have to feedback user to stop sending. >> Yeah. Yeah, I think you want to make it as easy as possible for developers to get metrics in the platform, but we're always challenging the other side of it and that's the cost.

So, they can't have everything that they want. Um, and to give them visibility of what we're actually dropping. We can give them visibility of our repo to be able to see. Actually it's about uh it's a nice catch but uh when you remove a lot of attributes on the gateway or somewhere uh you don't have any indication how many you removed or changed then you cannot fix the root cause in the >> yeah so >> yeah that's the >> yeah no I understand and um in our PII example we had this exact thing where we don't know if we're dropping an attribute um but we're actually tagging the telemetry to say if there is an attribute has this dropped so we can tell we can go back and see if that's a problem. >> Yeah.

So, um there are some platforms that uh when you filter you can you can have a an idea on how many attributes were dropped from that one. Um but uh one way of doing is you don't drop you just uh transform you just redact the information. So you still have this attribute name at your back end but the value is not there anymore. Right. So you can see you you can look for redacted. >> Yeah. Yeah. Thank you. first. >> So we have it's not exactly like collectors at the edge, but the same concept of like the telemetry that comes in needs to be shaped a certain way and work certain way to map to then send to our back end. And the double-edged sword of that like the the nice side is that we control it and we can make changes to it.

It's basically everything you said, all the same strengths, but the the the other side of that is that it's opaque why to it's opaque to people sending to us why things did not work and our team spends an inordinate amount of time taking people's like like here's what my telemetry looked like going in. Why did the mapping not work and we just have to like read through it and like logically try and figure out like oh I guess that's would get mapped this way and then that wouldn't work. like how much so I'm curious how much time your team is spending helping people figure out why their data isn't working cuz I I saw in in that demo it was like if it has service name it gets dropped so like we can see how many things on our gateway are getting dropped by teams sending us things without that but how does the team then know that their thing was dropped and it's because of service name like like how do how much time are you spending trying to like reconcile that >> yeah and we we do have challenges with that uh be fully transparent on that and I think as we are only requiring a few mandatory fields if we don't see data at all it's a few quick checks for them to check if they've got the service name and those few fields um and whether that maps to our CMDB so often we get cases where the application is not present in the CMDB um so it's a very quick checklist of things to do for a developer to go back so we do have documentation we do that's a basic thing that we give to developers.

A majority of times they can do the those simple checks. >> So it's because it's not complex enough yet. It's not become a problem that is >> Yeah. So there there are other cases that are harder to troubleshoot um and where we can if we can put some tags on our telemetry to indicate whether we've dropped something um or we've rejected it um we we'll put a tag on the telemetry. There is there are two other sides to that. One is you can use weaver. So for like for the future you can use weaver to enforce that at the CI level. Right? So it walks at a CI if those attributes those required attributes are not seen. >> So this is one way to catch those problems earlier. One other case that I've seen at uh at another event is somebody is dropping data if they don't see if they well so they're dropping HTTP URL entirely and if people complain about that then you know so you you are you so HTTP URL is a common source of PII leakage right so they're just dropping that and if they need HTTP URL then they have to have a business case for that right so that's uh if they are looking for that that's a starting of the conversation like why do you need that?

You should not you should not be having that anyway. >> Thank you. >> Um we are over time but uh we can answer your question uh here um before the next session. >> Thank you very much folks. Um, yep.

## Notes

- Raw note. Promote durable insights to topic notes under `03-Topics/`.
