---
type: session
event: KubeCon Europe 2026 Amsterdam - Observability Day
year: 2026
kind: lightning-talk
room: "Forum (Ground Floor)"
sched_url: https://colocatedeventseu2026.sched.com/event/2DY9O/cllightning-talk-a-drop-in-system-to-accelerate-metrics-observability-by-100x-using-sketch-based-approximation-milind-srivastava-carnegie-mellon-university
youtube_url: "https://www.youtube.com/watch?v=WVIaMLp9Gds"
youtube_id: "WVIaMLp9Gds"
match_score: 0.851
speakers: ["Milind Srivastava", "Carnegie Mellon University"]
topics: ["Metrics", "Cost Optimization", "AI Observability"]
status: "transcript-downloaded"
playlist: "Observability Day 2026 - Europe"
playlist_id: "PLj6h78yzYM2OXd3i-CyZH4A1QMxLTsKEJ"
playlist_index: 23
keywords: ["prometheus", "essentially", "cost", "queries", "summary", "metrics", "sampling", "sketch", "metric", "promql", "summaries", "agent", "graphana", "dashboard", "dashboards", "observability", "remote", "memory", "depending", "screen", "specific", "compute", "drop-in", "loading"]
transcript_file: "_sources/transcripts/youtube-playlists/observability-day-2026-europe/lightning-talk-a-drop-in-system-to-accelerate-metrics-observability-by-100x-using-sketch-based-a/WVIaMLp9Gds.txt"
---

# ⚡Lightning Talk: A Drop-in System To Accelerate Metrics Observability by 100x Using Sketch-based Approximation - Milind Srivastava, Carnegie Mellon University

## Metadata

- Schedule: https://colocatedeventseu2026.sched.com/event/2DY9O/cllightning-talk-a-drop-in-system-to-accelerate-metrics-observability-by-100x-using-sketch-based-approximation-milind-srivastava-carnegie-mellon-university
- YouTube: https://www.youtube.com/watch?v=WVIaMLp9Gds
- Room: Forum (Ground Floor)
- Speakers: Milind Srivastava, Carnegie Mellon University
- Topics: [[Metrics]], [[Cost Optimization]], [[AI Observability]]

## Transcript

Thank you so much for coming. Um, and this is a short 10-minute lightning talk. So, let's get right into it. All right. So, this loading screen might look familiar to some of you, right? So, you have your observability stack set up. Prometheus is loading a bunch of metrics and then you go to your Graphana dashboard and every time it refreshes, every time you change something on the screen, you have to deal with this annoying loading screen, right? What if there was an alternative to this, right? What if this is not an annoyance that you would have to deal with? So, what I want you to do is focus on that refresh button on the top right hand side of the screen. Just laser focus for a few seconds. You see that? See how fast that was? Um, this is exactly what ASAP query gives you. Orders of magnitude faster dashboards at lower cost. So, who am I? I'm Milen. I am a PhD student at Carnegie Melon. Um and I'm one of the leaders of project ASAP where our research vision is to essentially build systems to make observability and analytics much cheaper and faster and ASAP query sort of one of these uh efforts that has come out of a research vision and that's what I'm here to talk about.

So ASAP query is a drop-in accelerator for a Prometheus Graphana stack. So let's say you have your deployment set up. So a bunch of exporters that you've configured and then Prometheus is pulling metrics from these exporters and then you have a graphana dashboard that is continuously sending queries to Prometheus at some refresh interval. So ASAP query can drop in right into this existing deployment. It implements the same um HTTP query API that Prometheus does. And so Grafana just thinks it's talking to Prometheus. Um ASAP query also needs a data source. And so the way that we integrate that is that we have this remote write connector where you can configure Prometheus to do a remote write to ASAP query. So anytime Prometheus pulls a bunch of metrics, it can essentially do a remote write and send them to ASAP query. And if there's some queries that uh ASAP query cannot support right now, it also has a fallback where it can just pass through that query to the existing Prometheus instance where you still have all of your data and then Prometheus can sort of answer the query as before.

So what's what's the secret source here? Right? So the secret source is something called information preserving summarization. So that that's a lot of verbs uh words here. So let me unpack. So what happens right now in Prometheus and a bunch of other observability systems is that you have your raw data that you have pulled from your exporters and then any query that Graphana or other client sends essentially is going to read all of this raw data aggregate it and analyze it based on the query that you sent and then give you a result. Um instead what ASAP query is doing is essentially computing these very very compact summary blobs on top of the raw data. So instead of serving queries from the raw data, it serves queries from these compact blobs and you can essentially build these specific summary blobs for different kinds of queries. So if you have a summary that's purpose-built for a specific query, um it does not maintain all of the raw data.

It essentially maintains only the information that it needs for that specific query. And that's essentially the the information preserving part. So let me give you an example of what this looks like. One example of a summary here is something called a KL sketch summary. So this is just the name of the algorithm. So no need to worry about it. And this uh summary is able to estimate quantiles on top of data. So let me give you like a some numbers here just to put it in perspective. So let's say you had 100 million raw data points, right? So if you have eight bytes like floatingoint number, this would be around 800 megabyte. you can compute um a summary on top of this that is less than 10 kilobytes and this can give you like 99% accurate quantise on top of that 100 million raw data points. So that's essentially the the order orders of magnitude difference that we're talking about. So let's say just to sort of give you an example over here.

Let's say you have this Prometheus metric which is measuring like the the temperature of certain node and these are like two promql queries that you want to try and estimate on top of that data. So for the first promul query you can essentially ingest like the the top three rows into this summary and then this summary can give you uh the result of that query. for a quantile overtime query. The second one over here, you can essentially have three of these different summaries. One for um H1, H2, H3 respectively, and then use that. So, uh this is sort of what was happening. So, ASAP query sort of does all of this for you. This is not like you don't have to worry about the nitty-gritty details of how these summaries are computed. Uh this is essentially how it fits into a deployment. So like I said it's it's a drop-in accelerator. So there's one module called the ASAP planner which is essentially looking at the queries that are being sent from Grafana to Prometheus you know what are the query patterns uh what kind of aggregations or functions are you using how frequently are queries are repeating etc.

And it computes like this query plan that is given to the next two modules. So uh the second module here is the summary inest. So this is the module that is taking in the raw data um that is being remote written by Prometheus and as the data is being written in uh as the data is being remote written in a streaming manner it computes these summaries uh based on that plan. Um and then you have the ASAP query engine which is essentially sitting in between Graphfana and Prometheus and it takes the summaries as they are being computed and it intercepts the query being sent by Grafana to Prometheus and essentially uses these summaries to give you a result and like I said if it uh if it's like an unsupported query or if it doesn't have the summaries it can fall back to Prometheus. Um so ASAP query is able to make dashboards much faster but because it is using these summaries it also reduces your CPU and memory usage which essentially means like lower lower infra querying cost right so in these days when like RAM is becoming so expensive all that you know uh reduction in memory usage that you can get really uh helps your bottom line all right so let's see if this works so what I did was I just went to the quick start in my repo and just did like a docker composer before I started the talk and let's see if uh we can open graphana here.

Okay, graphan is up. So the quick start essentially has this like preconfigured dashboard essentially give like meant to give you like a sideby-side comparison of ASAP query um and Prometheus. All right. So this is essentially what you see. So you have each row here is a single query. So on the left hand side it's like that query is being sent to the ASAP query endpoint. On the right hand side it's being sent to a Prometheus endpoint. So the dashboards are almost indistinguishable, but you see like the ASAP query uh dashboard is essentially refreshing instantly while Prometheus takes a much uh longer time to run. And there's like a few other queries over here that you can sort of uh look at. All right. So that's that's essentially what I wanted to show you today. So this is still very much um early work that we're doing. Um I would encourage you to try out the quick start yourself, see if it works and share your thoughts.

Um we work with like making it um easier to sort of use this in your own like Prometheus graphana environment. So the quick start essentially deploys like its own preconfigured Prometheus and Graphfana and we're trying to make it easier so that you can use it with an existing stack. Um and some blog post to sort of talk about the performance benchmarks and the internals of the system. Um that's top right is the repo QR code, bottom right is a talk feedback. Um please contact contact me if you like want help trying this out or if you have questions um if you want to explore it on your specific dashboards. Um I'm going to end with one final thought I think. So during the keynote there was this phrase uh that we shouldn't have to uh compromise between visibility and affordability. And I think this is essentially what ASAP query is targeting. So that's it for me. Thank you so much for listening and I'm happy to answer any questions.

>> Um yeah, right. Thanks for the presentation. very interesting actually and this is actually very speaking to us because we do have uh I mean we notice we are a team that uses uh graphana dashboards that are made available by observability team and we do encounter this slowness of dashboards which is very frustrating. Um the question I have is that you are making a summary of the raw data but you said that it's uh it should be accurate or close to the real data. Are we not really compromising a bit? We have to be losing some of the data of course otherwise it's not physically impossible. Um uh have you quantified how much we lose how much accuracy we lose of the of the data? >> So I think one I don't know if you mentioned sampling. So this essentially is not really sampling on the data. It's essentially computing these different sketchbased summaries which are these compact blogs. There is absolutely a trade-off right there's yeah no free lunch here.

So uh I think the the thing with these summaries is that there like you can tune them like you have this tunable accuracy memory trade-off that you can get. So this is like one example of uh a number that would sort of make sense. So depending on the type of summary that you use, depending on the type of query that you want to answer, you can essentially tweak around with this uh trade-off and you can like give it more memory and it'll be essentially very accurate to the point that you might not be able to spot it on your dashboard itself. >> Maybe another question. Um you said that I mean using ASAP query we could potentially save some bucks, some money because we would not be hitting Prometheus each time with expensive queries. Um I I I would still like to think I'm not very familiar with Prometheus but I would still like to think that Prometheus is a bit smart to do some cash in on its own and I mean in case if it doesn't and that ASAP query is that the only way uh to do uh or to to to spend less um aren't we just uh moving the problem elsewhere the the the compute that we are offloading from from Prometheus is being done at ASAP query maybe it's done in a streaming fashion so it's it gets amvertised over time but maybe when the ASAP query damon agent whatever it is is restarted maybe there is a certain price to pay to to recomputee uh the summary each time so just question about performance and cost is it really amortized by using as query or not yeah >> yeah I think that that's a good point so there's like multiple sort of sources of cost reduction that you get here.

So like you mentioned streaming definitely helps because you're over time. Prometheus can have a cache but that cache is essentially going to be a cache of the raw data itself. So yes you will you'll probably save time sort of bringing it from disk into memory. You already have that somewhere but then when a query comes you still need to compute that uh aggregation or whatever the query is doing on top of that raw data. That's essentially that raw compute cost is being saved over here. So I think depending on the type of setting that you have depending on okay whether you are sending completely ad hoc queries to prometheus depending on whether you have like a preconfigured graphana dashboard that's sort of sending predictable queries the cost measure will differ but I I think in a large amount of cases where your queries are looking at aggregates over that original data which is usually what dashboards do you will have some amount of costsaving.

>> Thank you. >> Very cool. Thank you. Any other questions? Okay. All right. Thank you so much. >> Oh, can you >> Okay. Yeah. Yeah. Thank you.


## Notes

- Raw note. Promote durable insights to topic notes under `03-Topics/`.

## Related keywords

[[prometheus]] [[essentially]] [[cost]] [[queries]] [[summary]] [[metrics]] [[sampling]] [[sketch]] [[metric]] [[promql]] [[summaries]] [[agent]]
