---
type: session
event: "Observability Day 2025 - NA"
year: 2025
kind: session
youtube_url: "https://www.youtube.com/watch?v=4mlTSOnrGRA"
youtube_id: "4mlTSOnrGRA"
playlist: "Observability Day 2025 - NA"
playlist_id: "PLj6h78yzYM2NupzP0zYgT3dLzARalT1By"
playlist_index: 20
speakers: ["Ritika Gupta", "Mansi Kulkarni"]
topics: ["OpenTelemetry", "Collectors", "Metrics", "Kubernetes", "AI Observability", "Security"]
keywords: ["windows", "containers", "kubernetes", "metrics", "container", "prometheus", "cluster", "pod", "process", "collectors", "pipeline", "collector", "opentelemetry", "clusters", "promql", "agents", "metric", "security", "applications", "monitor", "telemetry", "exporter", "monitoring", "debugging"]
transcript_file: "_sources/transcripts/youtube-playlists/observability-day-2025-na/windows-container-monitoring-demystified-opentelemetry-in-action/4mlTSOnrGRA.txt"
transcript_chars: 18057
status: "transcript-downloaded"
match_score: 1.08
---

# Windows Container Monitoring Demystified: OpenTelemetry in Action - Ritika Gupta & Mansi Kulkarni

## Metadata

- YouTube: https://www.youtube.com/watch?v=4mlTSOnrGRA
- Playlist: Observability Day 2025 - NA
- Speakers: Ritika Gupta, Mansi Kulkarni
- Topics: [[OpenTelemetry]], [[Collectors]], [[Metrics]], [[Kubernetes]], [[AI Observability]], [[Security]]

## Transcript

Hi everyone. Uh hey, my name is Mani Kulkarni and I am a senior software engineer at Red Hat and today I have Retika Gupta with me. She is a software engineer at Oracle. Uh today we are going to talk with you how to effectively monitor and debug Windows containers on Kubernetes using tools like Prometheus and open telemetry. Okay. So this is the agenda of the talk and we are going to uh cover some interesting topics here uh especially why Windows containers matter and then how to set them up, how to monitor them, how to debug them and then to keep things interesting we are also going to do a demo and we'll see you uh we'll show you a graphana dashboard and then we will jump onto a cluster to do some uh debugging of Windows nodes that will be covered by Rita. Uh we will also have some time for Q&A and hopefully you'll see some questions at the end. Okay. Why Windows containers? Let's see why this matters to us this much.

Uh Windows containers bring cloudnative principles to traditional Windows workloads. Windows you may have heard about many enterprise applications which are built for Windows OSS and are built using very Windows specific APIs. These are those are mostly legacy Windows applications. If you want to take advantage of Kubernetes orchestration and run Windows applications alongside Linux workloads, we need Windows containers running on the Kubernetes clusters. Windows containers allow these applications to run in a containerized manner without needing a complete rewrite for Linux. So now that we've seen uh what Windows containers are, let's see if you have a Windows VM in cloud or on premise, you can bring that up and join it as a Windows node to a Kubernetes cluster. You have a few methods of doing that. Uh the first one is CAB which is nothing but a cluster API provider. Uh we also have cubeadm support and then there is a mini cube prototype that is available if you want to bring up a cluster locally on your machine and add windows nodes to that.

These are all managed by sig windows. So there is a URL that I have pointed pointed there. Uh you can follow that for updates and if you have any concerns or issues that you run into while setting up you can definitely reach out to us on kubernetes sig windows slack. Uh finally these are all uh managed by the user but if you are interested in doing a managed Kubernetes service you also have an option of open shift AKS and EKS uh which will take care of joining the instance uh to your Kubernetes cluster and also provide you support for that. Okay. So let's see where is the gap in Windows and why we are doing this talk. You may know of many solution architects who specialize in Linux contization, right? They are helping organizations replatform legacy Linux applications. But do we know of any architects who would do this for Windows? Maybe not. Lack of expertise is a problem in this area. Second, data for Windows on monitoring is currently scarce, which creates a gap in our ability to monitor Windows systems in a unified interface.

To give you an example of this gap in this in the open shift console that I work on, we had uh the query or the consoles built primarily for Linux and we had to rename our Windows metrics to the Linux counterpart just so we could have some visualization provided to our customers. To put it in a nutshell, Windows applications have always almost been an afterthought. But we don't want Windows applications to be left behind in the modernization process. Observability on Windows node isn't as seamless as Linux today. Metrics collection, process visibility, and debugging require special handling. That's where that's where host process containers come in. So let's see what they are. Host process containers particularly in the context of Kubernetes are containers that operate as normal processes on the host machine. So they have access to host network, storage and devices. This is similar to Linux privilege containers if you have used them.

And why are they important to us? Because earlier services like cublet cube proxy had to be run as Windows services and had to be registered within the Windows service registry. So for every issue you have you have you would have to SSH onto the node and debug it. With this feature addition you have a base image which you can build use to build any service and deploy it as a demon set pod. You would agree that the process of deploying has made comp made this compar with Linux for example. Now we can also use the cubectl debug command which streamlines developer and maintainer experience. So basically with this feature you have a coherent uh solution which fits within the cubernetes e ecosystem. So let's see why monitor and why this is important. While there are numerous reasons to monitor, according to me, uh the top three are make informed decisions, optimize performance and strategically manage costs for infrastructure workflow.

So I feel like these are the reasons that every organizations should put effort into monitoring. Okay. So let's go through a highle architecture diagram of Prometheus. This is from the official Prometheus website and see how all of the pieces connect. Here we have a node uh which sits at the bottom of this diagram and on top of that we have a Prometheus server running uh which will query the node for all of the metrics. Uh these metrics would be a little different for Windows and Linux. For Windows we have the node metrics coming from cublet and the container metrics coming from containerd which would be C advisor in case of Linux. So you would store all of these metrics in a local time series database and you would query them uh using promql query language. Uh these are available over HTTP and you could uh visualize them in a Prometheus web UI or graphana. You can also add thresholds and send alerts to whatever u app that your organization prefers like pager duty email or slack.

And finally since we have metrics coming from a lot of different sources we need a format that is understandable to Prometheus. So we have some agents which are called as exporters which do this for us. For Linux you may have heard about node exporter. Uh for Windows we have Windows exporter. Uh this is a exporter that is added by the Prometheus community. It's not officially maintained by Prometheus itself. uh but it has a lot of useful collectors. Uh as you can see there are some collectors that are enabled by default like CPU, memory, network mostly all of which we could need. Uh but there are also some other custom ones that you can add and some interesting ones like container and processes. You would basically uh configure them as command line arguments when you're setting up the Windows Exporter service uh as a demon set pod. And yeah uh we come to the end of the monitoring uh thing but this is something that the open telemetry integration that we have added in this talk because we feel that it completes the picture that we are trying uh trying to draw.

So I'm going to uh give it to Retika because she is an expert in open telemetry. >> Thank you man. Um appreciate you walking us through why monitoring is important and what Windows Exporter does. Um, building on that, it only makes sense to kind of build that obvious extension out for open telemetry as well to complete the end to-end observability pipeline for Windows containers. Why hotel? Well, the vendorneutral industry standard that's used for instrumentation, um, collecting and exporting telemetry data, all of that, right? Uh let's take a quick look at how we leverage hotel specifically for Windows containers instrumentation. So building on the Windows exporter that's deployed on each Windows node. We basically have a metrics endpoint being exposed at 9182. We deploy a open telemetry operator on open shift. All of this um has been tested and worked on at Open Shift. Um we use the the hotel collector scrapes this metric endpoint and the hotel collector can be configured to export these metrics to a data visualization tool like graphana um Thanos whatever we end up using in a production system.

Uh so on the right hand side well left hand side of this slide you're going to see a snippet which is going to talk about the pipeline and the receivers and the exporters over there. And we specifically have that tweaked to only monitor Windows nodes and a particular uh port that gets exposed. So that kind of like wraps up the whole end toend experience for Windows container observability. Um, now that we understand how all of that works, let's dive a little deeper into understanding Windows nodes and why it gets so tricky to debug Windows nodes and how can we do that using host process containers and cubectl debug command. Um, well, a picture on there is from SIG Windows probably two years old. Um, but it basically shows you all the core components running on a Windows node and how the Windows node helps enable container orchestration. What you're going to see at the over there is the core Windows service that runs on the host Windows kernel which interacts with the HCS and HNS.

That's the host compute system and the host network system. The host compute system manages Windows container life cycle and isolation and the host network system governs container networking, IP allocation, uh container routes, so on and so forth. Now on top of that, you have the CNI and the HCS shim which interact with these host services to enable container management, configuration creation, container resource creation, all of that. In the example over there, you're going to see containerd, but it could be run uh a container runtime, which is used for container execution and image management. Now, finally, tying this Windows node all together is the cubelet, which helps register your Windows node with the control plane of the Kubernetes cluster. Now that we understand what the core components on a host node look like, let's move on to the right side of the image where you have two types of Windows containers that are supported.

The first one, usually everyone runs that for their Windows application is the process isolated container. The process isolated container is a lightweight sandboxed environment with its own user space um but it shares the host kernel. On the other hand, you have the host process containers which have the same privileges like a host as mani dved into that. Hence, it's an extremely powerful tool because you can use it as a Kubernetes pod and you could use it to debug your node components because it shares the same host name space, the host network, host peripherals, host devices, all of that, right? It's um almost like privileged Linux containers, right? Um now that we understand that, let's look at how the cubectl debug command would work for Windows containers. Um and what are the key things to keep in mind over there. So the cubectl debug commands here are a few things particularly particularly to look at that command.

The first one is we're using the host process base container image. Why that image? Cuz it's extremely lightweight image. It's about 7 to 8 KB. Anyone who's worked with Windows images knows that they go into a few quite a few megabytes. Um, so that's why the base image there cuz it's going to enable you to have a fast startup time. The second thing to note there is the custom YAML where we set up in Windows options and the security context that it's a host process container and the other call out over there is the username and the user group. In this example, it says anti-system admin. Technically, it could be a custom userdefined user group that you use specifically for debugging, especially for this example. Um, another thing to note over here, even though Windows containers support Windows node debugging, Windows containers do not support Windows pod debugging due to limitations around ephemeral containers.

Well, moving on. I have listed over there a few best practices and troubleshooting guides on how do you debug the host compute system, the host network system. They're all pretty wellmaintained by Sig Windows group. Um, the third point on there is a custom host process base image with debugging tools already packaged into it. A shout out to James Stutterant over here um for sharing it with the community. The last thing there is our YAML and the setup files that we've used within our demo that we're going to show you next. So if anyone's interested in replicating it or trying it out by themselves, feel free to head to that GitHub link and give it a shot. All right, I'm going to start the demo here. You probably have to go and click play over there. maybe URL. Oh, wait a second. I think I know how to do that. Okay, there you go. Okay, so here I have a cluster which is set up and uh this is an open shift cluster which is brought up on Azure.

Um so here we see a list of nodes. Uh the last two you see there are BTO's worker nodes. We also have a list of pods. Uh yeah. So now we are going to see all of the resources that are created in the my graphana namespace. Here I have a graphana demon set and uh the graphana service and the pods. Um we also have to set up some routes so we can access the graphana dashboard. Um and I have also created a token which is used to uh securely access Prometheus. So yeah this is the uh Prometheus route that has been created. This is going to be used in a graphana dashboard in a minute. And yeah this is just setting up u all of the resources that we need. Yeah. So I am going to show you the cublet uh summary stats API and we are looking at the particular worker node that we have uh brought up and this is just so that we verify that we have all of the metrics coming from Windows Exporter before we jump on to Graphana.

So this is like a verification step. So here you will see all of the container metrics and yeah all of the useful things uh like memory uh network maybe CPU usage core. Yeah, that's the worker node. Yeah. Now let's jump onto the graphana dashboard. Uh I'm going to add Prometheus as a data source because we have uh that set up to scrape the metrics. And yeah, as I mentioned, I'm just going to add the Prometheus URL that we um saw in the routes when we queried that. And here we are setting up the authorization token uh just the bearer token that I've created um in the cluster. So this is just connecting us to the Prometheus API. And now I'm going to import a custom dashboard. This is a dashboard that is publicly available by Prometheus community. Um but this one is specific to Windows. There may be many more that are available. And yeah, so you would see something like this. uh you'd see the two worker nodes there and all of the things that you would want to monitor in one place.

Uh yeah, and depending on your use case, you can add more things to this dashboard and yeah um utilize it. Now I'm just going to quickly show the hotel collector config. uh the receiver and the exporter has been set uh to Prometheus and debug mode here. So we can see all of the metrics on the terminal directly. Um yeah, and we have the hotel pod running. Just looking at the logs here, and we have all of the metrics getting scraped. Yeah. Uh now I'm going to pass it on to Retika for debugging. >> Thanks, man. Well, we're quickly going to see the cubectl debug command that I talked about in action. Anyone who has debugged a Windows nodes knows the pain of having to set up SSH keys or RDP onto the node and look at files. You're going to see here how easy it is to get onto the host. Voila, we are already on the host over there using the cubectdl debug command. Um, once you're on the host, you have full access to everything there.

So, you can look at the container dlogs which usually sit in cir logs folder. We're going to show that. Um, you can run networking tools, scripts, um, you can analyze data, you can kind of, yeah, do a bunch of things. Um, and we're going to walk over a few over here. Yeah. So that's me trying to download the host network systems, collect logs, which is kind of like the suggested approach used to capture data packets, HNS trace, all of that things. And we're going to run it. And you see it does a bunch of runs. It does v it captures your VFP policies. Um it collects traces. Um and you will see that all in that folder over there. And now we're going to also try and use the cubectl copy command which makes it super easy to copy it from the node to your dev machine to analyze it using wireshock or ETL analyzer so on and so forth. Um so yeah that's kind of majorly our demo. I think I do continue to do a run through of the container routes and stuff here which we can see through.

Yeah. So that's doing the cube ctl copy command which can help you get it on the developer machine. Yeah, there's all of the data from the node on your dev box. Uh, and those are the networking files. Um, and there's a lot of information in there. Um, if you're running into issues with your container failing to talk to the node, your container failing to communicate with another container, these are some files which are kind of like the starter pack to understand where your packets are getting dropped and so on and so forth. Um, yep. Those are the I think we're looking at the container IP addresses that get generated. And that's the network routes file for Windows containers that come up. Well, that's about it. Um, if you have any questions related to Windows containers debugging, building observability pipelines for them, feel free to reach out to man and I. We're pretty active on the SIG Windows group in the Slack channel.

Um, and if you have further questions or discussion topics or features, feel free to stop by on Tuesdays to our SIG Windows meeting. We have it every Tuesday at 9:30 PST. Um, and that's the QR code for feedback. Do share feedback on the session if you liked it. And if you'd want to hear something specifically the next time go around, we're open for questions. Thank you. >> Thank you.


## Related keywords

[[windows]] [[containers]] [[kubernetes]] [[metrics]] [[container]] [[prometheus]] [[cluster]] [[pod]] [[process]] [[collectors]] [[pipeline]] [[collector]]

## Notes

- Raw note imported from CNCF YouTube playlist. Promote durable insights to topic notes under `03-Topics/`.
