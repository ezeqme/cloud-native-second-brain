Hello everyone. How's everyone doing? >> Good. Uh hope everyone is having a good cubecon so far. Um my name is Vijay Samuel and I'm a principal architect at uh eBay's reliability engineering group. >> Uh I'm Sep and uh I'm one of the lead engineer of the observability platform at eBay. Um and here we are here today to talk about uh retrofitting open telemetry collectors and Prometheus and u how uh and talk about how we have gone about uh adjusting to the scale at which we operate at eBay uh and how we overcome some of the u scale/design limitations that that we saw in uh both of these uh projects. So we'll talk about three use cases. The first one being uh service graph connector. The next one being Prometheus exemplars and then uh Prometheus itself and how we went about doing clustering. Let's talk about service graph connector first. Um so refresher service graph connector is uh uh one of the connectors available in the uh open telemetry collector contrib.

Um and it's typically used to construct uh uh as the name suggests uh a service graph which uh provides all the uh adjacent nodes uh between the various uh microservices, databases, um message cues that are involved in a distributed trace. Um where would this uh be useful if you're looking to know what the exact call chain is and you're trying to do some automation in terms of okay I have an incident that's going on on a particular service I'm going to walk either upwards or downwards to to see where the increase in calls is coming from or where the most amount of latency is being spent where there are any changes that were executed in any of these services in the call chain for for a lot of causal analysis, a service graph is very useful. Uh and as we go into more automation with uh things like AI, uh a service graph is uh paramount for being able to successfully uh do causal analysis. So what the service graph essentially does is uh it processes all the spans, constructs the call graph, but with a with an important caveat.

it needs the entire trace to complete for it to be able to uh generate a complete call graph and when you're running at scale say 10 million spans per second um and spans coming in from hundreds of cubernetes clusters like how things are set up uh inside of eBay um waiting for the trace to finish at that scale in memory is a tall ask you need a lot of memory because there is a lot of uh uh latency patterns that are there. Some finish at 5 seconds, some finish 50, some finish 5 minutes. Do we wait for that long before we start processing? If yes, that's a that's a large amount of memory that is required. So um what we ended up doing um is we have a trace store um where you have uh open telemetry collectors that are there. they write to an ingest uh gateway that we have written and that gets written into shards. So shards for all practical purposes are a bunch of clicks uh pods that um that that form a click house cluster and we also have um a metric store.

Uh SEP will talk about how the metric store is laid out a little later. Um but you have open telemetry collectors that write to um a gateway u and from the gateway we uh we aggregate and then write into um a metric store which is in this case a cluster of Prometheus pods. So what we did is um we we wrote a receiver uh an open telemetry receiver that reads from click house periodically using simple select star um group by trace ID um and then you uh put in the service graph connector in that receiver. So for when you do this what is essentially going to happen is that you already have traces that are completed sitting sitting on click house you're streaming them into um an open telemetry uh collector construct the service graph at this point you don't need to hold things in memory as and when things are coming in you can construct the call graph and then you can just write the call graph uh into prometheus which open telemetry collector uh supports Yeah.

So, uh because we u uh uh we are doing this we we we wait for the the traces to finish in storage uh and then we pull traces uh with a reasonable offset. So, typically we do 5 minutes because uh we feel that 5 minutes is a reasonable enough time for most of the calls uh to complete. For all the anomalies uh we frankly don't care uh like we'll find a different way to solve it in the future. Um and the custom receiver uh just simply does uh uh select star every every 5 minutes. So this basically allows us to not tamper with the the service graph connector that's available in um in open telemetry contrib um the the the periodic poll is very efficient on memory. Um and given given the scale of which uh microservices are laid out inside of eBay like uh we have roughly about 4,000 microservices and one of the expectations uh of most deployment architectures with the open telemetry collector is that the request starts and finishes within the same Kubernetes cluster.

Um but at scale that is not that that is not uh uh practical to achieve uh given there can be tens of databases uh hundreds of microservices that are talking to each other. Um our deployment pattern is always crosscluster communication is very common. Um so with this pattern of pulling from storage we don't need to worry about that. Um some patterns also what they say is that you can globally route spans to a handful of uh open telemetry collectors at as a second tier um and then you hold in memory and then process. So uh this approach negates the need for that as well. Uh globally routing can be hard. Uh if uh if a part dies then you lose data and things like that. Uh and this pattern can be used for other pro other processes as well. uh for example uh the tail sampler. Tail sampler also has the same expectation. You have to wait for the trace to complete in memory and then you apply u uh sampling constraints.

Uh if you use the same methodology, you can just wait for you can have a hot tier of uh uh spans that are being um stored for say 2 hours, 2 days um stream all of them into a secondary set of collectors, do tail sampling and then store them in a different year that has maybe 14 days, 30 days um or several months. So that's the first one. The second one uh is Prometheus exemplars. Um how many of you have used uh exemplars? Um and um how many of you feel that it's valuable in your triage process today? Okay, few hands. Okay, good. So we use exemplars very heavily inside the company. U because uh exemplars as a as a concept is very powerful. Um a quick refresher on exemplars, they're a point in time representation uh or an event that has happened within the time series. So um if you take uh request latencies as an example um Prometheus histogram represents um the the latency distribution across all the requests that are flowing in but an exemplar is the representation of one HTTP request that occurred and its latency.

So uh it would have a time stamp, a set of labels, the exact latency that that request uh uh uh took and basically that gets stored in Prometheus and on the UI UI uh if you're using u uh graphana or something else that you built uh it would it would it would be represented as a clickable marker. So you click on it and then it should be able to take you to the u uh a representation of the actual request and in most cases an exemplar has a trace ID. So you click the clickable marker and then it takes you to the end to end distributed trace. So um uh a lot of uh the usage allows for drill down uh based triage. So you have an alert, you look at the alert, you look at the series that was behind the alert. Um, and if you said that, okay, if latency goes beyond 10 seconds, uh, uh, alert me, you would go and look for clickable markers that are greater than 10 seconds, click that and then you analyze to see, uh, what essentially is going on.

So in Prometheus uh today uh exemplars are stored in a circular ring buffer uh and uh it's controlled by a flag called storage.exemplars. Um being a circular ring buffer it's really fast because it's in memory. Uh but it comes with a key caveat that um given that it's a circular ring buffer you fix the total number of exemplars that can be retained. So you say 10 million exemplars and that's about it. and the 10 million and one exemplar will basically rewrite the head and it keeps going in a circle. Uh and for us this typically gives us maybe uh few hours or even in certain cases like 15 to 20 minutes worth of exemplar. So you if you're if you're viewing a graph that's say 2 days long. The first 15 minutes has a bunch of exemplars that are there and then after that it is basically blank because there is no persistence that's implemented into Prometheus exemplars at this point in time. Uh there are issues that are um open on upstream Prometheus some point either through contributions of our own or someone else in the community.

uh it should be reality but our hope is to see how we can uh achieve the full potential of uh exemplars without having to modify Prometheus uh itself. So we want to be able to retain exemplars for for weeks. So within within the company we offer 2 weeks worth of uh uh retention for uh for traces and we want to be able to provide at least 2 weeks of retention for uh uh exemplars. um so that we can do the metric to uh trace navigation and then build triage experiences on top of that. So what do we do here? Um we took a a non-intrusive approach for this as well where where we said that we are going to stream exemplars into uh click house and uh we'll we'll basically expose the same query exemplars API in the Prometheus compatible format and for UIs alone when they're requesting query exemplars we'll basically uh based on the user agent we'll layer 7 route the queries into that uh standalone API uh instead of using the standard Prometheus exemplars API.

So that way if uh if some automation wants real-time data, it will still be fed through the circular ring buffer. Um but for anything that's UI which can take couple of seconds longer if need be um it would come from this API that is querying uh click house. Um I think like there have been a lot of talks in the past about uh um what would it take for uh time series to be stored in click house. Um there there are a lot of pros and cons. uh um performance characteristics might vary but one of the things that uh we have always seen in our uh installation is that performance characteristics of Prometheus are phenomenal and it's very hard to beat because it's it's very very u uh purpose-built for uh time series being queried at scale and uh as a result when we first tried put putting this whole thing behind click house the queries were several seconds slower than what query exemplar's API was uh in Prometheus and so through various iterations and applying various uh uh techniques that uh click house or features that click house provides we came up with the query that's there on the right side and uh the gist of it is that we use um dictionaries to store metric names.

So the first thing we look for is if the metric name exists. If it doesn't exist or if the metric name has an exemplar uh only if it has an exemplar it's going to show up in that in that particular table. So we do a dict_get u to see if the metric name exists. If it does not exist we can quickly short circuit. Uh and that was one of the areas where um there were a lot of slow running queries when we were trying to do table scans to figure out um if a certain metric name had had exemplars and in most cases it doesn't like there are very few histograms that actually have exemplars unless someone has gone about instrumenting exemplars for pretty much everything. The second one we we did is uh sampling. uh there can be hundreds or thousands of exemplars but does it actually make sense for all of them to be streamed back especially for a UI use case. How many of those exemplars can you actually uh perceive um or can the dashboard render without choking?

So that that became a realization and what we ended up doing is we ended up uh sampling and the way that we sample is that for every time window and every series hash we basically try to get a uniform sample um which is which is what uh in the query there would be a city hash 64 of time stamp hash ID and then we take 10% of that the very the the third but last line um and we also limit to say thousand exemplars which which uh for the entire time duration might be generous enough to get a good distribution of all exemplars that might have uh exist and uh um going against conventional clickaus wisdom to have a single table with every row and column. Um we went with a denomormalized schema because we observed that had higher performance characteristics than a single table where there is a series hash ID lookup which has the series hash ID with all the key key value pairs the label key value pairs that are there.

Uh we take that and we only do hash ID based lookups on the table that has the actual um uh uh the actual exemplars themselves. So the the benefits as I mentioned like uh we were able to do this uh completely non-intrusive no customizations on Prometheus. Prometheus continues to behave the way that it does. Um but we get the benefit of uh being able to uh look at exemplars for uh the entirety of the lifetime of the traces themselves. Um and we also have u uh another use case that we have talked about in past cubecons where we use the exemplars themselves to do tail sampling. Uh where if the metric system decided that an occurrence of the HTTP request was useful enough to construct an exemplar then we should store the trace for the lifetime of the exemplar itself. So every dots on the dashboard if you click it it it it would still be there because we honored uh honored its existence and we are tail sampling it for a longer period of time and this gives us a very good representative sample um um for all the kinds of requests both both based on latency profile and based on um the unique kind of errors that were seen uh for the given application.

So that said um I'll hand it over to Sep talk about Prometheus. >> Thank you Vijay. Yeah in the next few slides I'll walk you through on how you know how we can scale Prometheus if the data size grows. Uh let's let's take a look at the simple simple simple let's take a look at the simplest form. It's a single TSTB node. uh you know the metrics comes in you just write it to the the single shard what you have it goes to the disk eventually and you can query the data out of it locally it works fine there's there are no issues until you have to scale until you have to add you know more and more data then you know you you'll hit with the limits it could be the disio it's could be the query bottlenecks I'm pretty sure all of us have seen that there is only a certain limit on how much we can vertically keep scaling it up right and uh you know how do we solve this problem right the the ne the the easiest thing we could think of is basically to shard it why have a single monolithic TSTB why not shard it so instead of having a single TSTB here we add more more TSTBs basically and instead of writing it to the one single shard we now have an injust endpoint so all the time series comes into this inester the ingester basically discovers all the shards dynamically through the Kubernetes API server And uh for the incoming time series it does a xx hash on all the time series on all the labels and it finds the suitable shard to write the data to.

Great. So we you know with this we basically uh you know we have solved the scale problem. We can keep horizontally adding more and more shards and scale it up easily. Right. So but the next problem we land upon is you know how how is how what happens if a shard goes down right? We lose a data basically. If a shard is down, we lose the data anywhere. Even if it's intermediately, if the shard is down, we won't be able to query the data which was in that shard. How do we solve the problem? This is where the replication comes in. We basically add two more copies. The ingesttor now instead of writing it to a single shard or a single region, it it sh it it replicates it. It writes the same data to three different regions, right? So even if a single shard is down or even if uh if the entire region is down, we're still good. You know there are other two copies of our query qu the query layer basically can query the data from all the three uh regions two or three regions it can it can decide to pick and it can merge the data and you know it can send the results back to the end users.

Okay, this is good. Uh so we have the higher availability now. Uh so what next? Uh the multi-tenency, right? So uh you know in in the organizations nowadays, you know, we have multiple applications and each of the application could be of different criticality and some of them may you may want to isolate it you know just because they're critical data and you don't want to keep it in one of the default uh tsdbs where the other 100 applications go. All right. So what we what what we could potentially do is add more and more clusters, right? Instead of having a single TSTB cluster, we add more and more clusters and pin certain applications or certain tenants to go to a particular cluster, right? This helps in two ways. One definitely the isolation. You can put the critical ones in a you know an isolated uh cluster and it could have its own retention capability as well, right? That is one of the uh the advantages.

The second is again it helps to scale us more and more right you can keep adding more and more uh uh clusters here great so so what next right uh you know the one of the problems with all of these things what we have seen is you know the data we are bringing in centrally so you know it can be originated the data can be originating anywhere it can be let's say originating in let's say lbs or potentially even or anywhere right so we bring everything to a central region that means there's a cross colo data transmission happening And it also means it go the data is flowing through the load balancers right. So if the data flows through load balancer then then there then there's a potential it's a potential point of failure. It adds the latency as well and it increases the cost as well. How do we solve this problem? There's uh to solve the problem we keep the data closer to the source where the data is originated.

We keep the data there. Right? So within the region uh within the region we can have we generally have more than one availability zone. So we can replicate the data between the availability zone for the fault tolerance part right. So now the data is basically kept in the same region and u uh you know there are definitely multiple regions. So right and uh within the region there is the availability zone for the fault tolerance part. Cool. So this this definitely as I mentioned this solves the problem of the three problems which I mentioned about basically the the latency the bandwidth cost and also the single point of failure. And now you know you could ask the question how do you how do we how do we query the data from? How do we know where the data exists? The easiest option is to uh you know just fan it out pan out to all the charts and all the clusters. But that's expensive that's not going to scale at all.

That's really not going to scale right. So given we have so many shards now so many clusters now how do we solve this problem? The the way we we we are going about solving the problem is basically our query layer is smart enough to know where exactly the data resides. It knows which cluster has the data and which shard has the data. Right? With this we basically can eliminate the fanning out part. And obviously you'll ask the question how how does your query layer know about this? Right? Uh this is where our uh u you know this is where our uh the index filters uh the index stores comes into picture. So this the index store is basically inspired by the the field hint index from the Google monarch paper. And uh the way it works is I mean for every single time series coming into our TSTB we uh we generate the engrams and the fingerprints out of the key out of the key value labels basically and all these data is sent to the index store right uh and uh the index store the query layer before before before it talks to the the shards or the actual TSTVs it talks to the index store for from for the given query it generates the fingerprints and it talks to the index store to ask which charts or which cluster has the data.

So the index store gives all the all the IPs of all the TSTBs which actually has the data and the the query layer just talks to only those TSTBs gets the data out of them merges the data computes the results and sends the results back to the end users and yeah this is how we are able to scale the TSTBs and still make it fast precise and scalable. Um let's let's look at the uh some of the benefits what we are going to what we are getting out of all of these things. Now the TSTB no longer is cardality bond. We don't have to really worry too much about the cardality. Instead of the restb we being scaling it vertically we can do the scaling horizontally. Can keep adding as many shards and clusters as needed and and then definitely there is a more efficient scatter gather. We don't have to fan out the queries everywhere across all the shards. Right? Thanks to our index store, we know exactly where the data resides and we call only those charts which has the data and also we pretty much have an near perfect horizontal scaling with the tenency model which I mentioned and also you know the kind of clustering what we have we can keep growing as much as it's needed right uh and also it's it has inbuilt resiliency because of the replications and we don't have to keep redesigning the system as and when the more and more data gets inserted uh with that uh you uh you know I stop here and uh open for any questions.

>> Yeah. Uh feel free to use the mic in the center um if you have any questions. >> The index store >> u the the I can repeat the question. The question is is the index store uh um stored in all the regions? We we have um region local uh index stores that are there and then they basically bubble up all the fingerprints to a global index store. Um and the global index store is what is being used by the querers to uh make the actual queries. >> Hi there, thanks for the talk. Uh as someone who's not familiar with uh exemplars, I'm curious how sampling is done to avoid a lot of data that I don't care about most of the time. I'm less interested in 200s that complete within expected response times and I care about abnormalities. Is that something that's done? >> It didn't sound like it was something that was handled in your specific sampling logic, but I heard something about how maybe >> uh there is logic to determine what is selected as an exemplar to begin with.

>> Yeah. So um exemplars are selected by the Prometheus or the open telemetry client itself. So whenever you say um observe a certain uh histogram with exemplars um it basically randomly picks uh from the request and then it sends it out. On the query side um we only abide to the Prometheus contract itself. Query exemplars only takes a label selector. Uh so if you say uh application equal to fu status code equal to 404 then it's just going to pass that to us and then we do a label selection that's it. So uh given that Prometheus does not allow you to filter based on u um any binary operation or whatnot. Uh that that is not a capability that we have as well. >> Thank you. uh what's the ingesttor that you're using? >> Uh the injust is nothing but a simple go program that uh uh abides to OTLP. Uh so there are some organization level policies that we need to uh we need to uh put in place like rate limiting um and then deny list, allow list and things like that.

Um, so it just uh exposes an HTTP service with OTLP format and then does all these additional policies and then writes them to storage. >> So you wrote it yourself? >> Yeah. Yeah. Yeah. >> Okay. >> Hey, uh my question is uh how do you uh choose the TTL for the indexer for the data? So how do you know when the data is gone and you don't need it in the indexer? Um so some of it is done through uh series life cycle call back. Um we we we implement that u inside uh our implementation. So the the Prometheus TSDBs themsel is very similar to how uh things like cortex work where they take the Prometheus library and then expose APIs on top of it. So we have the ability to add series life cycle call back to know when to add a fingerprint. uh removing uh fingerprints is uh just with the um the retention policies. So once the retention policy expires then the fingerprints get removed in bulk. >> Thank you. >> Hi. Hi.

My question is about spanmetric connector. So you you mentioned uh benefits about service graph connector but uh did you >> uh test anything about spanmetric connector? >> Oh yeah. So we we use panmetrics connector uh as well uh because u uh if you turn it on then you get uh red metrics for uh every application that's emitting traces regardless of if they emitting metrics or not u as in like purpose purpose instrumented metrics. So it's it's pretty useful and we have it running on all our uh open telemetry collectors as well and they they in turn uh expose exemplars as well. So for every kind of unique span with unique uh um uh status code and whatnot uh we get exemplars for them as well. >> Okay. So your uh spanmetric connector exposes your exemplars. >> Uh they also emit exemplars. So u raw instrumentation that every application uh has that has some exemplars that uh that are being emitted along with the the spanmetric connector as well.

>> Okay. Thank you. Thanks for the presentation. Uh could you please comment on how your query layer compares to uh querers found in highly available Prometheus u solutions in the open source like Mime or Thanos. >> Um we haven't done um a benchmark that compares the two of two of them. Um but that is an exercise worth doing like um the field hint index is not something that uh some of the high high available implementations expose outside if I'm not wrong uh uh I'll need to verify but like if they do not that is a that is a key difference maker because like um um standard uh HA implementations they they do like scatter gather across all shards and when you have um 100 200 300 shots that you have to go go through uh it it can become very expensive. Um so that that is something that we I feel we do differently. >> Thank you. >> In the beginning you talked about uh feeding the traces to a trace store.

What trace store are you using? >> Uh we use Kikos. Click House. >> Ah, thanks. >> Yeah, but but we don't use the standard uh uh schema that is advertised on the open telemetry collector. Um we use a slightly different schema, but for all practical purposes, Click House uh this methodology can work even um for a use case that uses the standard uh Click House exporter. Uh and if there were a Click House receiver that can pull in the same format, then you can establish the same chain that we are establishing. >> And you're feeding directly from the collectors into that. your custom collector. >> Uh so we u we have a custom exporter that writes into our into our ingesttor um and then ingesttor writes into into click house uh because um uh similar to what we talked about uh clustering with Prometheus we have our own way in which we cluster clickos clusters as well where uh we don't have one large cluster but small small clusters uh which we can manage at scale.

Uh so we have a custom injesttor that can recognize that. Um but yeah that's that's how we do it like for for for the customer it's standard open telemetry collector that sits at front in the front and standard Jerger APIs that uh sit uh on the query end uh and in between we have some secret sauce that can that can scale things