Uh hello everyone. Uh I am Punesh Digshit. Uh I work at Google in gRPC team and I'm the maintainer for gRPC go. Uh today we are going to walk through uh gRPC open telemetry plug-in and how to troubleshoot gRPC services uh using it. Just want a quick check. How many people use gRPC here or have heard of it or worked on it? Okay. And how many people uh use open telemetry? All right. Thank you. Uh so just a quick background on gRPC. Uh in the past uh a large monolithic app could have multiple teams working on hundreds thousands of lines of code compiled into one binary. Uh this led to long and difficult build processes, big failure domains and difficulty rolling out changes due to coordination requirement. Uh in the microservices paradigm, the same app is instead made up of many smaller binaries called microservices and each serving a much tighter role and communicating with each other over the network rather than through direct function calls.

Uh but microservices introduce some complexity. uh you have to define and maintain interfaces between your client and server and you have to maintain stateful connections between them and gRPC solves both of these problems for you. So in this picture uh the blue layer talks to the red layer which talks to the yellow layer uh and these RPCs cross network and infrastructure boundaries. uh the region shown here could be an infrastructure segment where RPCs have to cross these boundaries. But let's look at another aspect of microservices where effective use of this paradigm could be hampered by a l lack of observability. Observability in simple words is visibility into the internal goingings uh of your microser mesh to the extent you need to you need the visibility for reliability and efficiency. For example, if something breaks in your complex mesh or is performing poorly, how do you figure out where the problem is and how you fix that problem?

So how do we get back what we lost using the microservices paradigm? Is there any way the infrastructure or the software components can provide that required visibility? This is where gRPC observability comes in. So people choose gRPC for its incred incredible performance. It's fast and efficient. Uh and largely uh because of two key features. Uh first is uh binary protocol uh which is known as protocol buffers and its ability to run multiple streams of communication over a single channel which is called multiplexing. However, this very efficiency introduces a significant trade-off. Uh the same features that make gRPC so fast also make it inherently difficult to observe and debug. First let's talk about the binary protocol. Uh unlike JSON or XML which are human readable text protocol buffer are compact and binary. This makes the gRPC traffic completely opaque. Uh you can't simply glance at a request payload to diagnose an issue.

Uh the second challenge comes from multiplexing over HTTP2. Uh so gRPC sends many independent requests or streams over a single long lived network connection. This is highly efficient but it means that monitoring the health of that one connection offers very limited insight. A critical request might be failing repeated repeatedly, but because it's just one hundreds of streams, the overall connection metrics might still look healthy and you lose that specific request level detail. You need to find the root cause of a problem. Uh another challenge is now we know what are the challenges with gRPC. So how do we uh to solve that challenge we need to have some uh sophisticated observability solutions. But in the past uh getting that gRPC visibility meant complex manual coding. This approach was very brittle and very prone to human error. Uh this created a huge tax on developers leading to endless boilerplate code.

In today's cloud environment, this old method uh creates real operational pain. It makes debugging a nightmare and scaling our services a gamble. Truly effective observability goes beyond just detecting simple errors. It requires understanding the subtle intricate behaviors of a distributed services. So this is what open telemetry plug-in solves for you. Uh so the gRPC observability product consists of three pillars of observability logs metrics and traces and we use the gRPC interceptor framework to inject our observability interceptors which generate the raw data for the three signals logs metrics and traces. Uh just to give a quick background on what interceptors are in gRPC, they are basically uh modules sitting right next to your application and intercept any request that is going out of your application service and coming in application service. So whatever that request uh is carrying uh gets intercepted there and then uh the required matrix or trace information is used by the gRPC uh to provide the observability.

Uh once that happens uh that gets integrated with exporters and backends so that the raw data is processed and sent through the exporter pipeline to an appropriate analytical uh analytics back end. This end to end integration is what we call gRPC observability and this is what the new uh open telemetry plug-in is based on. So let's take a in-depth look at the use case. Uh you have an app developer the character on the left in green building gRPC applications using the latest gRPC artifact supporting observability. Uh the running apps are provided with appropriate observability configuration by the developer. uh the gRPC interceptors pump the relevant raw data uh logs, metrics and trace information through appropriate exporters into the respective backends. Here you see a logging backend, a metrics backend and a traces back end. The backends produce the required dashboard which are used by the consumer in this case the DevOps engineer to get visibility into the internal state of the application and that focuses on gRPC traffic.

Now what is open telemetry? Well at the very basic it's a it's an open source observability framework to create and manage telemetry data. If you just Google uh how do how do I instrument my code or how do I observe my system you will see tons of solutions. Uh but we need to see like why we choose open uh open telemetry. First, it avoids vendor lockin. Uh I think we have heard in previous presentations. Uh instead of having your data and observability solution logged into one vendor, you own the data you generate. Uh there is a single set of APIs and conventions that you need to learn. Uh how the model works is that each vendor can provide its own exporter or use one of the existing supported exporter such as open telemetry protocol or some of the existing protocols like Prometheus. The instrumentation just needs to be configured with that exporter and voila. Uh if in the future you don't like your current vendor, you just switch that vendor.

uh open telemetry is also extensible at the at almost every level that you would want but most people won't care about that and that's a good thing. You can use the out ofthe-box solution with the already written exporters or trace propagators and be on your way. Uh lots of major popular solutions are supporting open telemetry and it seems to be the direction in which things are moving. and it is also supported in many popular languages. Uh now let's talk about the three open telemetry signals traces, logging and matrix. So tra tracing gives us a lot of information as to what is happening in the request of a life uh in the life of a request in distributed system environment. Usually a a small number of requests uh one in 10k or 100k requests are sampled. For each sampled request the trace helps you get a treel like structure as you can see uh and it each span here basically represent individual tasks.

So let's say a user request reaches a server and server further makes a request to other services. You will be able to see this kind of tree along with the time graph as to what happened when. Uh logs are very selfexplanatory and the most popular uh among the three signals and they have been used for a long time uh for debugging uh in uh for debugging and other other aspects of observability uh and yeah matrix which I'm going to dive into dive deep into uh in a little bit. Um okay so now let's talk about the gRPC uh open telemetry plug-in that gRPC team have built. Uh this is also an active area of development for the team and we are curious to know what kind of features would be useful for the community. Uh but before I'll talk uh but before that I'll talk about some of the basics of open telemetry architecture and API. So, open telemetry has divided the library into an API and an SDK. The API is just the interfaces for instrumentation while the SDK has the actual implementation.

Libraries like HTTP and gRPC are meant to use the open telemetry API while the application owner is supposed to plug in the SDK. If no SDK is provided, the API is supposed to be a no op. Uh yeah, so a quick background if people uh who have been using gRPC for a long time would remember that gRPC also has support for open census which is now deprecated and now points towards open telemetry instead. Uh nevertheless, gRPC's open census solution is still a working solution, but we understand that open telemetry is what everyone wants. So we want gRPC to be compatible with it as well without breaking open sensor support. How we have structured gRPC's instrumentation internally is for it to be agnostic to the in instrumentation library. Open telemetry is all the rage but we still want to keep gRPC's hotel support as a plug-in rather than a hard dependency which means that gRPC internally would have a layer of indirection which is how we solve all the problems in computer science with the new architecture we got some major benefits uh here you can see the links to the GRFC's which are basically the cross language designs for uh all the major components that are designed in gRPC and uh the major benefit with this new architecture uh is the stats plug-in architecture is standardized across languages.

So I earlier with open census there were differences in how each language did things and what exactly the metrics meant. Uh but with this new architecture that has been standardized. Uh we can also register both open census and open telemetry plug-in at the same time allowing for transition and we can also register multiple hotel plugins. So let's say for some reason we want one set of data to be exported to one back end while another set to be exported elsewhere. We can still do it. Open telemetry matrix API has uh three major classes uh meter provider. Uh this is also your entry point into the API. This is uh also where applications can set up how the metrics will be exported and if they want to customize the metrics exported by libraries they can do it here. This creates and this will create something called a meter and meter is where a libraries will use the meter provider provided by the application and create a meter for the library.

Now instruments uh is once you have a meter libraries can use it to create actual instruments on which you can record your measurements. The difference between the sync and async instruments is the method of instrumentation where for sync uh as the name indicates you record instrument synchronously while in async you register call backs for your instruments with open telemetry and opal telemetry will invoke your call backs at certain time intervals based on your configuration. This is an example that shows how you would create a meter provider and set it up with the Prometheus exporter. Uh I'm showing C++ as an example but implementation exists in other languages as well uh like go, Java, Python etc. And once you this is something user will do before uh like whoever is using the gRPC. And then uh once you provide that configuration uh as part of gRPC uh channel uh this is what gRPC would be calling.

Now let's talk about gRPC tracing. Uh so open telemetry allows users to specify how context is to be propagated uh from client to server. And uh because gRPC support both open senses and uh open telemetry and for the trace to be useful you have to kind of uh propagate some information from one service to another and that is propagated in a standard format that is proposed by open telemetry. It's called W3C uh context pro uh format. It's also called trace parent. But because gRPC support open census uh there is also gRPC trace bin support uh which is a binary format. So in order to support that uh gRPC open telemetry plug-in uh provides gRPC trace bin propagators for compatibility with open sensus. Uh so you can see like basically a gRPC client which is using open telemetry can talk to both servers which are using open census or open telemetry and vice versa as well. Uh where uh gRPC client using open census can talk to server using open telemetry or open census.

Uh now tracing instrumentation uh in gpc. So the tracer provider is our starting point. It holds all the configuration and is what we use to create new tracer instances. Uh the text map propagator as I said is what lets you connect traces between different services. Uh usually uh the standard uh format proposed by open telemetry is W3C context which is like a 28 by byte uh fixed data. uh it it contains the trace ID which is constant across all the services and the span ID which is uh the id of the span that is created by the previous service and that is how you basically connect uh and you get that treel like structure. Uh so the text map propagator is what lets uh uh transfer that information between services. It injects and extracts the trace context from things like gRPC metadata or HTTP headers. So a request can be followed from end to end. Uh and this is how you will configure uh an tracer provider for the open telemetry plug-in.

Again the example is in C++ but uh examples exist in other languages as well. Now finally once you have the meter provider and uh tracer from the uh meter provider for metrics and tracer for the traces you register the plug-in with gRPC channel uh using this uh and then that's it that is all you need to do uh once you create the channel uh then your gRPC code is instrumented. So before I was mentioning that the user is basically provides the SDK uh and gRPC and gRPC library basically work on the interfaces. So if you don't provide these uh configuration it nothing will happen and you will not have any instrumentation of gRPC code. So the only thing that you are uh giving giving an indication to the gRPC is by providing this configuration that you want observability. Uh these are the examples uh that gRPC repository has. Uh this link takes you to the uh C uh C++ and Python example. But the example exists in Go and Java as well.

Now let's see some example scenarios uh where gRPC open telemetry plug-in can be leveraged. Uh it's a comprehensive suite of metrics and tracing all designed to provide extremely granular insights into how our gRPC uh RPCs are actually performing. Um because we adhere strictly to open telemetry semantic conventions, the data is consistent and interoperable. Uh this allows us to diagnose complex issues that were previously invisible like monitoring the hidden cost of retries and hedging or pinpoint the exact cause of a streaming bottleneck. And the most powerful part is that it enables us to start optimizing this performance directly all without having to make invasive code changes to our application code. So let's quickly look at how this provides uh such deep insight. We break it down into uh three views. First is client per call. This is the big picture. It tracks the total time an RPC takes from the application's perspective including all the retries.

For example, the metrics u the metric gRPC.client.call.duration tells you exactly how long your app waited. But to understand why it took that long, we will drill down to the client per call attempt. This is crucial. It focuses on each individual attempt with that call. uh letting us see metrics like gRPC.client.atm orduration. Now you can finally see if a two second call was one slow attempt or 10 fast retries. So each attempt is basically a retry in case you get errors. So if you have a one single attempt which was slow you you should be able to see that or if you have multiple retries and that is causing the entire RPC to be slow you can see that as well. Uh and finally to complete the picture we have serverside instruments. This view with matrix like uh gRPC.server.callst starters started shows us exactly what the server is experiencing uh allowing us to correlate the client's view with the server's workload.

So why do we bother uh separating calls and attempts? The reason is critical for effective monitoring. The per call metrics tells you what your end user or application actually experiencing. If it says the call took 2 seconds, the user felt a 2C delay. It's the ultimate measure of performance. But the per attempt matrix tells you the hidden story behind that 2C delay. It shows the internal work, the retries, and helps you instantly distinguish between a single slow response and a series of fast failures. It's how you know if you are hitting a brief network hiccup or persistent server side problem. Ultimately, this distinction is vital. It's how we move from just seeing latency to accurately diagnosing the bottleneck and fine-tuning our retry policies to fix it. These are the instruments that gpc provide out of the box. Uh this is a per call instrument and these are per attempt instruments. Uh you can see that there are there are different aspects of it which can help you debug uh and understand what's happening with each message.

By the way, just to uh make it clear, the per attempt is what uh sends the RPC to the server. So the server will only see that particular attempt that is reaching successfully to the server. So when you are debugging, you will be debugging mostly uh in the per attempt view uh and monitoring all these attributes that I have that that are mentioned here. Uh then these are the server instrument that gRPC provides. Uh it's as it's as you can see it is quite similar to the attempt met uh the client attempt because that is what is basically uh completing the client to server uh call. Then each matrix basically also is in uh uh is instrumented with these labels and attributes and that gives you some more details about each metric that is being emitted. Uh so you can see the the RPC method the status of the RPC or the or that attempt uh you get the status of both RPC and the the attempt and the actual uh call and the target and you know all these uh labels basically uh help you debug further.

Now let's talk about client traces. Uh so those of you who know open telemetry know the concept of spans. Uh basically there are there are two spans coming uh into play here. One is a call span and one is an attempt span. Because we have CL uh because we are in the uh because when we are retrying the attempt span will be uh taking into consideration and when when that attempt completes that time you will see uh these traces being filled uh sorry these uh spans the events and the attributes in the span being filled. So you can see that there are uh two very uh important uh uh events that take place in from the gRPC point of view. One is a name resolution uh delay name resolution complete. So that can uh help you understand whether your RPC experienced a name resolution delay or not. If it did then uh you can focus your investigation on the name resolution part. Uh then another uh event as you can see in the attempt span is delayed LB pick complete.

Uh so gRPC provides uh load balancing uh feature as well and if your RPC has experienced uh picking the load balancer that you can see and you can focus on investigation on that. uh and these are some of the these are the server traces uh that JPC instrument. So let's take an example of monitoring retries and hedging. Uh so we get a direct visibility into retries using gRPC.client.tempemetric. First, we diagnose the cause of a retry by looking at attempt or duration. Are the failed attempts very short? That suggests a quick network timeout. Are they consistently long? That points to a real problem on the server side. Next, we can identify which services are struggling by simply comparing the number of attempts to the number of calls. A high ratio is clear red flag that a specific service is constantly having to retry. And this data is immediately actionable. It allows us to stop guessing and start fine-tuning our retry policies like adjusting backoff delays or attempt counts to create a far more stable and performance system.

Uh now let's talk about diagnosing streaming bottlenecks which has traditionally been very difficult and major uh both which is the major problem with uh gRPC observability. Uh so we can narrow we can track the actual data volume with specific message size metrics but more importantly our traces now contain events for every single message sent or received within a stream. By measuring the time gap between a client's message sent event and the server's message received event. We can immediately see if the issue is network latency or a server that can't keep up. This is essential for debugging any realtime streaming application as it gives us a clear continuous view of the data flow and health inside the stream itself. Finally, let's talk about using the data for general uh performance optimization. First, we can easily identify our slowest RPCs by looking at call duration metrics. From there, the associated distributed trace lets us point the exact cause of the delay, whether it's slow microser, a database query, or network latency.

We can also monitor for unexpectedly large payloads using the message size matrix. This tells us when it's time to optimize by improving compression, refining a protobuff schema, or switching from a simple RPC to a more efficient stream. Ultimately uh this lets us use hard data to make informed decisions. We can find and fix performance issues without having to start with risky or time consuming code changes. So uh any questions? Uh um so current status of gRPC open telemetry plug-in is uh matrix APIs are stabilized in the languages and tracing APIs are still experimental. Uh there there are a lot of future work planned. So we have more metrics which are being added like connection metrics, TCP metrics, retry metrics and there are some features also being considered. Uh so stay tuned for tracing and logging support as well and we are happy to uh invite contributions. So feel free to uh reach out if you want to contribute to any of these.

Uh so yeah that's uh that was the end of the presentation. Uh and these are some of the links that you can use to connect or meet with gRPC maintainers or just reach out in any case. Uh we are also having gRPC conf uh in US uh this uh this September I think and then one in India as well. So uh if any of you uh folks are around there please feel free to join and our meetup group usually have all the announcements uh about these events. So uh if you join you will get the notification. So thank you. I I hope you liked it.