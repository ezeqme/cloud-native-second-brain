hi everyone welcome to today's session uh today i'm going to to give you a deep dive on the sixth scheduling my name is i work for ibm and i'm also the current co-chair of the sikh scheduling so today's agenda contains three parts first i will give you a brief introduction on schedule and then we'll share with you some best practice tips and so that you can make the most of your schedule and the third part is i will give you some update on the latest development progress so firstly what does scheduler exactly do so in short-term limit terms scheduler just does one thing which is to put the path to the nose so that means it doesn't evolve into the path creation it also doesn't evolve into the spin of the underlying containers you just find a node for the pending path and technical terms so basically scheduler will watch the pending path which which means the path doesn't have the spectrum name set and then it will look into the part explicit scheduling constraint like cpu memory requests another affinity et cetera and also taking some predefined implicit scheduling preference into consideration and look at the cluster resource usage and make the decision to place the path to the optimal node so this is what scheduler does and inside scheduler there are basically several components the first one we call the caching so to make the most optimal decision schedule has to maintain a cache to have a sort of choose view of the whole cluster like how many nails are there how many tiles are there and how many pv pvcs etc etc so scheduler we run internal fuel scheduling formulas to watch for the api objects so that we can maintain the internal cache properly and the second part is called queuing so if a pi is coming to the queue we schedule it with reasonable order so internally we have a priority queue and if there's no note fit for the path we also have some back off mechanism to ensure the lower priority part has a chance to be scheduled and also the path can have a fair chance to be re-popped up and be retried and also next uh our main scheduling goal routine and binding routine which is a serious processes to do the core scheduling flow so the core scheduling flow looks like this so firstly we will pick apart from the scratching queue and we will go through a series what we call extension point here so for each extension point we have a series of plugins and we will go through those plugins either sequentially or in parallel depends on what phase it is it will first go through the pre-filter and filter to check the hard requirements for the part like how much cpu it requires detain deterioration not affinity etc so if there are some nails fit for the part we will do prioritization on the nails which is to give the nails candidate score for each according to predefined the score plugins and then finally the node has the highest score we have chosen as the final net and then we go to next cycle binding cycle to assign the node to the path so this is the happy path so what about the negative test what if in the filter there's no single node can fit for the path we will go to the postal filter phase right now by default we have a preemption performance which semantically tries to preempt lower priority paths so that to make room for the incoming part so and if it succeeds means we can find a node to serve the path with the cost of preempting some victims then we will also set the path status that nominated no name to the path and then put it back to the queue because we cannot immediately assign the no name to it because the victims are still being deleted so we should have a grace period for those parts to be deleted so this is basic the core scheduling flow and this is the basic introduction of how scheduler does so next i call it make the most of your schedule the first part i call it policy customization so schedule is a box it has a lot of nars of a lot of options so if the by this default option doesn't work for you you can adjust them but one thing to keep in mind is that if you want to use some latest feature latest options you have to use the dash dash config and the following with the scheduler config demo and this yamo is the most standardized and most organized way to use this kind of option the latest version of the schedule config is v1b2 this is the example so you usually have to pass it to the cube scheduler binary and second thing to keep in mind is that by default there's a lot of scoring plugins to like to favor each kind of preference and apply that impact on your final decision and by default the weight is usually one so if you prefer one scoring policy over the other for example in this case i put image locality for example image locality means you prefer to schedule the path to the nodes that already has an image downloaded so in this case i gave a very large weight which is 50 000 for this kind of plugin so that i can impact the final decision for the part so that it will be preferred to be land on the node which has the image already downloaded so this is i call it a plugin specific weight because this weight is so plugging in the plugins configuration context and there's another way to configure the weight using the work work cloud itself like in no dfinity part affinity about entire affinity there is a field called preferred during scheduling ignore during execution there is also a wait for your part to show your favor to the existing part either you want to co exist or not coexist with those kind of paths so this is also a prevalence previous and has a weight associated to it so basically two types plug-in specific and the cloud specific and next thing is that we have a default set of the plugins but that doesn't prevent you to use another plugin to replace the default plugins or you just want to make additional plugins so for example by default we enable the list allocated plugin which means we prefer to schedule the paths evenly across the cluster but in some use cases like cluster autoscaler is not the case it's totally the opposite it prefers to make the use of existing nodes as much as possible instead of like in case case will spin up a new machine which we are waste the money so in case of ca you maybe want to use the most allocated plugin and disable the list located plugin wait you're maybe you're wondering i may don't have the full knowledge on what kind of plugins are there and what kind of plugins should be enabled so what if i enable the most allocated plugin but forgot to or i don't know to disable the list allocated what should i do so in this case this two plugin will work in the opposite way so this is not the the expected behavior so that is one thing we are considering all the time and for this kind of conflict plugins we try to reflect them and come for example in this case we come up with a new score plugin called node resource fit and in any time we enforce there is only one type of behavior is enabled so in this case your you can specify the plugin arguments which in this case is called scoring strategy dot type where it's mostly allocated so anytime there's only one type can be specified so you don't need to worry about the mistake of enable two conflicting plugin at the same time so this is the example to adjust your scheduling behavior by modifying the default or modifying the plugin arguments so here i put a link for you to get familiar with all the plugins and their arguments and what their metrics so basically that's the first recipe i want to share with you to customize your schedule behavior by modifying the default scheduler config like changing the plugin weight and changing the plugging arguments etc the second part about want to talk about multi profile versus multi-scheduler so my schedule you may have know that is that in addition to the default scheduler you can run your own schedule to cover some scheduling tasks of a particular path so what's the problem of running multiple scheduler the biggest problem is resource confliction because these two scheduled banners doesn't communicate with each other so they may compete for the limited resources suppose a node has only one cpu left and default scheduler and your for example full scheduler simultaneously assign their path x and y to the node and suppose both tiles request one cpu and in this case cubelet will be the eventual arbitrator it will reject the latter arriving party suppose in this case it's part y so there will be a very severe and weird symptom which is the letter arrived the part will be show up in this case without of cpu and in api's perspective this part are already finished the scheduling phase it has already has the no name set that implies this part we are behind there forever until the resources in this node get released that's a very unexpected behavior will sometimes even involve operation to manual resolve this so this is the biggest problem of multi-schedulers so do you have solutions yeah we do have some solutions to mitigate this but not perfect for example some users are trying to divide the cluster into several pieces and delegate the thumb piece to one scheduler and others to another so this can work that will cause fragmentation and hope has caused a low cluster utilization another solution is on top of this multiple scheduler they set up extra arbitrator so that each scheduled decision was reported to the arbitrator and the actuator works as a central place to do a decision reject or accept so this works but as you'll see will definitely cause extra workload and also because you introduce another component you have to uh manage the extra efforts better manage this kind of textures so what's the community direction on this so the community direction is is to use a multi-profile schedule much prefer schedule is on steel schedule but inside the scheduler it can define different profiles and each profile you can think of is as sub schedule so because it has its own set of scheduling policies to including which kind of weights which kind of plugins and which kinds of plugin arguments et cetera so in this case i provided a schedule config multi-profile demo and here you can see that we have four kinds of scheduled profiles one is the first scheduler to keep it as is for the regular workloads and the second is image first to favor the image locality so that maybe some service workers care about the code start time so they want to schedule a path to the notes with the image already downloaded and third is called bin pack so custom auto scaler may be beneficial for this because you want to save the cost of running machines the last is called skip score because score phase is the doing the ranking is to uh to do the preference for the path so in some cases like the cluster is very scheduling highly used and there's a lot of cause queueing there you maybe want to schedule the path in most cases low priority or offline workers as quick as possible so i don't care about finding the best note this is the satisfying the heart requirements that works for me so in this case you have four kind of profiles and your workloads can specify the spec the scheduler name to map to the profile name so there's two ways to specify uh secondary name once you express them uh manually the second way is to do that dynamically so you can set up a admission controller and so that it can intercept the creation of the path right so you can programmatically like assign the path to the the schedule profile by for example the path labels or by even by some external metrics like how business the current schedule is behaving right like the how many paths are there are being queued up in the schedule so it's very dynamic you can work on a lot of innovative ideas to do this and in terms of the scheduled config we are also reading up a cap to simplify the scheduled conflict we you know we we will introduce a multi-point plugin so that for a user you don't need to know whether the plugin implements score or filter or postal filter you just define the name and provide that as a practice so internally you will it will smartly detect whether which extension points it it implements and then download them properly so this cab is still being developed so we'll be available in 123.

so this is the second part multiply versus the market schedule running multi-schedule is fine but you should be knowing the limitation and upon the resource confliction maybe some manual operation work will be involved the third recipe is um improve your throughput so scheduler especially on a large cluster you sometimes don't need to iterate all the cluster to find a node to fit the path so there is one option called percentage of nodes to score and by default is set to 50 but not exact 50 is the adaptive algorithm to calculate the percentage by default so for example suppose you have a 500 nails you will the adapted percentage default percentage will be that means each well in the filter phase it will check the nodes and if the number reached to in this case 230 in the filter if two to 230 nails has been available to be a candidate for that for the part the filter phase will stop instead of going through all 500 nails so and then this 250 30 nails will be passed down to the scoring phase and give the score to each node so in this case you can see that you know 200 nails in the most desired case unit only need to filter 680 nails and the hands only score those kind of females so this is the default percentage so in your case you maybe want to tune this potential value according to your cluster size for example if you have 2 000 nails and specify the percentage as 10 you only well score on the filter 200 nails this is the first parameter i want to highlight second one is also follow the same idea to iterate a few nodes instead of going through all of them in the default preemption plugin there is some parameters like minimum candidates no percentage or minimum candidate knows absolute so by defining these two parameters you can define how many nodes you want to go through the preemption phase instead it goes through all the nails another option is called prefer nominate node it's a feature it's not an option it's just a feature gate introducing 102 y and already enabled by default in 22 so it's just a yes or no option you want to use and we believe it's a very useful feature so we don't want to provide as an option it's a shortcut to leverage the status nominating node name so that when the path has been already carried with that field we won't go through all the nodes instead we will just do once to check whether the previous nominated node still fits if it fits i will just assign the node to it rather than going through the regular scanning flow if not don't worry we all fall back to the regular screen flow so that's why i say this is a feature i think mostly will increase the scheduling especially the preemption throughput another parameter is called parallelism so across the scheduling flow there are two phase we run the plugins in parallel and by default we run the nodes in batch so we default to 16.

so the same thing sounds like to corresponding to the cpu cores but underlying you will involve the operation system as well as go through the go long schedule so sometimes it cannot be hard coded to the cpu cores so it needs some tuning to get the best of all performance we used to have the pr2 wanted to set this to the uh cpu cost number but it seems doesn't work properly so we revert up here so if you want to [Music] customize this and want to for example if i have a lot of cpu cores maybe default to 16 cannot make for the cpu usage so maybe you want to tune it and so that adjusted so that's the third recipe i want i wanted to share to improve your throughput optimization and the final one is uh i think it's uh the knowledge that i just want to highlight is that in addition to some regular resource core resource like cpu memory we can model your customer resource in the format of extended resource i give a very easy example so as a user you can model your resource then you can give a key value pair and patch that to the node capacity so because we manage this kind of no capacity in a very generalized way so in this case if you patch that to the null spec status will have the capacity automatically populated so that also the scheduler knows that you have this kind of well it's fake gp information so that you can submit a part request with this kind of gpu with file and the scheduler will use the default plugin load the resource fit plugin to to to schedule your pop so in this case a lot i see a lot of users implement their own screen plugin but it's not necessary a lot of practice already taking the external resources for citizens and supporting them well all right last four recipes i want to share with you so that i hope those recipes help so you can make the most of your schedule do more customized options and without any further coding well maybe you you may say that it still doesn't fit my needs what should i do are there any viable options to fulfill like advanced scheduling requirements like batch requires the answer is yes we do we have been thinking about to supporting more diverse workers two years ago or three years ago so we initialized the scheduled framework design to focus more on expose the internal mechanics to the external developers so that you can use the core scheduling capabilities easily and then easily build your own schedule plugins and so that with your customize the autofortree plugins and plug into the default scheduler you will make it a schedule plus plus so it's still one scheduling binary with extra functionalities your developer the other thing we were thinking about is that in some cases you have to involve other components to facilitate the scheduling for example in terms of batch workflows sometimes you know exactly where and how to control the creation of the path and if you can control the part creation more smartly instead of just dumping the paths immediately when the while the cloud is created then maybe can save a lot of scheduling efforts to fulfill the scheduling constraints of your workload so recently we were thinking about that so either you can define some your own crds so that with some extra controllers and the minutes animation web to control the better creation time of the path also there's we have done some upstream work like suspended job like in the next series we were doing some features like multiple scheduling directory for the suspending job so this in one way we will perfect of internal scheduling mechanics in another way other components can also help so that these two things combined together can make the scheduling experience more smooth to adapt to more diverse requests all right that's pretty much i want to share with you to make the most of your schedule so the last surprise is the update on the development progress the first thing is the schedule component config in 122 we upgrade the version to v1 beta 2 we remove some options that are either not used quite often or doesn't quite make sense and we will also in the next release will come up with a simplified scheduling conflict so that you can have a more easy to configure way to configure this kind of plugins and also well well try to make that totally backwards compatible and there are some features i've already mentioned like prefer normally now like multiple scheduling directives and also in 122 we add a namespace selector for part affinity before in the pod affinity we only provide the option for you to specify a slice of the namespaces but now we support the main space selector the last thing we did is the a series of scheduling queueing enhancement to improve the efficiency like we in the before we re-kill the path in a very brute force way like any any events comes in we will move the parts back to the back to the head of the schedule queue but now we will we defined the series of rules for the path only to be moved to the head on its method events comes in like if a part failed with a pvc or pv restriction one for example irrelevant events comes in the part will not be moved to the head so it can save a lot of scheduling cycles and also mitigate the sort of head of blocking problems in this kind of queue and also as more and more users are using scheduling framework to do their customer plug-in so we also did some work to support some dynamic event handlers so that you can define your crds so that the cd can be also detected by the scheduling framework we internally use some dynamic triangle mechanics to do this and also we provide the option for the plug-in developers to proactively move keypads to active queue it can be sometimes useful for like the co-scheduling plug-in to for example if you have a spark job when the driver part comes in you may want at that time to move associated executor path back to the xq so the executor path can be scheduled afterwards immediately okay so next is about some sub projects the first one is schedule plugin so schedule plugins a repo which we initial edit for we want to engage the different vendors to contribute their design implementations here are two cube contacts you can check out and both are using the scheduler framework to build some customer schedule plugin to fulfill the business need a second sub project is descheduling so this schedule provides some rebalancing policy to redistribute the path according to some defined rules because scheduling decisions is happens on the scheduling time but along with the cluster as time goes some some constraint may not satisfy anymore so this scheduler can instantly regularly check this kind of policies so that if the policy violates it will practically delete them so that they got another chance to be rescheduled and third one is called cube schedule simulator it's a new g-stock project which provides some web ui as well as some simulating sdks for you to easily build your uh your your toolkit to like set up a cluster programmatically and see whether uh you can replace a series of workers and see how the scheduling result is so right now is during the very early phase but yeah we maybe want to to spend more efforts on it to make it very have a very solid sdks so that the integrators can benefit from that all right and you can contact us in the china's in the mailing list and also welcome to join our vehicle meeting you can raise your gender item there we will discover your proposal etc message all right thank you for joining today's session