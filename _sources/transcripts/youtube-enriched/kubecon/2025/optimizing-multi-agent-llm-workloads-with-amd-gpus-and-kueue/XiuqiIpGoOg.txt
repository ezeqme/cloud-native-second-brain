Good afternoon everyone. Thanks for coming. This is Yuchan CEO of uh um sorry [laughter] cognality and uh I have my co-speakers today uh Jodi Su from uh AMD um senior manager of engineering and also Jim Shen CTO of AXeller. It really takes a village to optimize LM serving stacks. Each of us represent a different layer of the stack. So the model application layer, the hardware acceleration layer and the infrastructure orchestration layer. We hope this cross layer close collaboration model in the open ecosystem continue to drive the full stack optimization from hardware to software. So as we all know AI workloads are extremely heterogeneous from small and mighty models to really large ones with meaningful trade-off and this is slide is just a one-dimensional view um with the model size even for the same size model different request shapes like token distribution prompt structure coming in batches can create this really bursty unpredictable traffic burst and resource pottex. For example, you may have um rag that has super long input including both the prompt and retrieve docs but short output.

On the other hand, you may also have um reasoning agents with short and media input but super long output. And on top of everything, you may have mission critical production models running and a lower priority experiments causing resource contentions. Now with agentic workflow it becomes a compound system with more challenges for disregulated serving and also the composible automation. So let's have a quick refresher. When we talk about agentic workflows it typically falls into two patterns. On the left humans are in control defining deterministic code path and chaining different tools and models. On the right, LMS are in control dynamically planning, reasoning and calling uh tools autonomously. So most production system actually use more reliable workflow patterns on the left. In fact, most of the business problems can be solved by simple prompt chaining and structured output. So for this demo, we're going to use a really kind of a simplest most commonly used prompt chaining, which is also the building block for the right side system.

Now the problem we're trying to focus today is actually one simple question. How do we free up more GPU resources so we can pack more workloads such as collocating tightly coupled agents on the same AMD GPU? We solve this problem by fine grain fractionalization uh at the gigabyte level granularity and also combined with Q start with a K uh p powerful quota and priority and preeemption systems. And with this context, I'll turn it to my colleague Jodi Su for a deep dive of the problem and solution design. Thank you. So I'd like to cover to achieve the effectiveness of scheduling these agentic workflows on AMD GPUs. What are the common challenges and solutions? The first challenge is that GPU are expensive. What this means is that you often have limited of them and often in short supply. So if you have to allocate them statically say organization A have 400 GPUs, organization B have 200 GPUs to make sure that each organization feel fair. This static allocation actually lead to high GPU waste. as we are kubernetes.

So the solution that one common solution we come up with is why don't we pull all these GPU cluster into a GPUs into a shared cluster and then consolidate different kind of AI workloads on them and leverage what Kubernetes does the best scheduling and bingpacking different kind of AI workloads such that it allows us to drive up the GPU utilizations. Now that we consolidate all the workloads onto the same shared cluster, we face the next question. These different AI workloads have competing priorities. For example, the agentic workflows uh demonstrate illustrated, they have a user waiting for it. So there's a latency requirement such want to make sure the workflows finish in what the user can wait. And the other type of air workloads like GPU batch processing or training they actually are they don't have a latency requirements but they do want to get as much GPU as possible. So with this in mind then how do and the other thing is like since we consolidate all organizations GPU togethers each orc might want to make sure that they get some kind of fair share out of this shared cluster.

So our proposed solution is to use Q. So Q is really good at g giving priority scheduling such that we can make sure the latency sensitive work agentic workflows can get the GPUs in time to satisfies the user request and we can also use the uh nominal quota to kind of give each organization some kind of minimum quota guarantee and then how much they can borrow so that people feel like okay I'm getting some fair share out of this share pool. Finally, as these agendic workflow or like some kind of online inferencing a user generated request, they're they could be a very bursty. Sometimes there's high peak usage and sometimes there's not that much request. If you always plan for the peak usage, you probably are over spending your GPUs on these uh agentic workflows. So our observation is that by leveraging GPU fractionalization such that you can divide the GPU among this agent and then consolidate them into fewer GPUs, you can free up your GPUs for other competing workloads to run. Next, I'm going to walk you through the demo setup.

So there's a lot of going on. So I kind of want to get hit a few points before I hand it to Zimi for a live demo. So if you're experienced AI workloads practitioner, I hope our demo you can walk away knowing that you can you have Q you have fractionalization that you can play with to make your to get more bang for the bucks for your GPU capacity. If you're new to aic workflows, I hope that you walk away knowing that oh, you can try this recipe on the GPU uh on the cloud that have AMD GPU to know that it will work. So, I'm going to break the demo setup uh into three parts. First, let's cover the top part. What are the hardware we're using in this demo? So, we use three AMD Mi325 GPUs. MI325 is AMD's high-end data center GPU. The current flagship offering is actually MI 355. Both MI325 and MI2 355, MI325 come with high GPU memory bandwidth also with almost 300 gigabytes of GPU memory capacity. What this means is that the GPU can fit massive model on it. This reduces the cross GPU communication and can keep the latency low.

The second feature is the multi-user GPU virtualization through single root IO virtualization. What this translates is that it provide it can virtualize this single physical GPU into multiple virtual GPU and assign to each tenant. The tenant could be each virtual machine or it can be a pod that can see a dedicated virtual GPU and what this gives you is that it can have strong isolation between the tenant. So make it more effective to support multi-tenant with high performance as this is this feature is implemented in hardware. The second part of the uh demo is going to have two competing workloads. The first one is the agentic workflow. We use a prompt chaining like uh uh introduced that have a user input request that go through three agent and then have the output and then we have a background task which is GPU compute intensive. Finally what's installed on the Kubernetes here the Kubernetes we have two we have a Q setup and we have the AMD GPU and memory fractionalization software enabled. Now I'm going to walk through the workloads and the Kubernetes setup.

First let's make this a more real life what it could looks like. So you have a input of a image. The image could be a frame capture from the security camera or it could be a user uploaded image. What the first agent is going to do is it's going to generate a image caption. The model we choose in this uh demo is the blip image caption base and its compute usage is a computer intensive and low memory. The output of the first agent is then going to feed into the second agent which going to reason and analyze the caption. The model that's we're going to show is a quantu 7B model. It's a high compute and medium memory in this use case. Finally, the output of this analysis is going to the third agent which is going to plan action. Imagine in the security scenario, it might be we want to alert the security, we want to log the incident etc. In the third agent, we choose the same model as the second agent quant 7B um model. But as Icha mentioned earlier sometimes even with the same model because the third uh agent needs a long context the same model kind of have a different resource requirements it becomes memory bandwidth bound because it needs long context.

So this a genic workflow kind of setup give you an idea about really each model will have different kind of compute and memory uh requirements. Now let's look at the demo setup. So on the left hand side you will see a resource pool of three GPUs. On the right hand side you'll see a two uh demo queue sorry right two cues. The top yellow one is a demo queue kind of the high priority because the enginic workflows have the user waiting for the reply release and the bottom is a background queue. So also the top uh demo queue we give it a guaranteed quarter of three GPUs because we really need to make sure all three agent run at the same time to be able to collaborate and uh finish this user request. In the demo you're going to see kind of a before and after. Before is a dedicated GPU scenario where we assign each agent to have one GPU. since the agent are higher priority. So Q will uh omit them and then put the background task into a pending state. But in this demo, we're going to show that there's actually not that many user requests coming in.

So dedicating one full GPU is a bit wasteful. Let's see what we can do with fractional GPU. So we divided the memory such that agent one use a bit less than agent two and three but they all can fit into one GPU. At this time Q happily omit the two background task to run on GPU 2 and three. So what we will show is that all five workloads run on the same three GPUs but the latency requirement is still met but we're able to fit in more workloads on the same capacity. Now I'm going to hand uh the presentation to Ziming to show us a demo. All right. Thanks Judy. Right. Sorry, I have to one of my colleague to hold the mic for me. Hello everyone. So really excited to be here to share what we have done, you know, with AMD team. Let me end the slideshow here. So, it's really exciting to be here to share what Exo Stella has been doing with AMD. And today I'm going to show a live demo because why not? Live demo never goes wrong, right? So, like this mic AMD >> just in case um I do have a video recording. So, if anything goes wrong, I'm going to just brand the Wi-Fi of the conference.

You see this? I'm going to brand the Wi-Fi. I'm going to switch to video. But hopefully the you know my terminal is still working. Okay. So you know on the screen right now you are going to see like four different windows. On the bottom right you can see we have you know the current status of the cluster que. If you attended the previous talk you you probably will know that cluster Q is one of the most important concept introduced by Q. Each cluster Q is basically you know it carries the resources quota. It carries the priority. It carries some like scheduling policies. In this case, we're going to use, you know, two cues here. There's one queue called background queue here and another que called demo Q. The background queue doesn't have any coder. Is basically running on the bottle only resource. The demo Q is the one that has the coder on the top right. This is the you know one of the machine we carve out for the demo. And by the way, if you notice probably, you know, on the screen on the bottom right, it is actually a cluster in production.

So what we have done is we just cover a node from the AMD cluster and then we basically assign a 3GPU quota for the demo while the other workloads can run in the original queue. So it's like you know isolated handled by the queue. So this just shows how flexible it is to use Q to basically create different quota different projects you know for different initiatives. So the top right shows the AMD SMI command output which is actually available on the latest and greatest AM you know AMD rock M7 uh software suit. So it shows the GPU um like Jody mentioned is MI 325. Right now there's no processes running is a basically idle machine. On the top left this shows the you know the current status of the demo name space with all the ports listed here. Right now we only have you know two ports running and no GPU or close running yet. So without any further delay I'm going to run the demo script. As a starting point I'm going to show you the background cubes. The background queue is um it's just a like I said it's a queue with zero kota on the resource flavor what that we define and by the way resource flavor is pointing to the node that we just covered.

You can see all the quotas are zero. And by the way, in this quot Q concept, a quot is not is different from the Kubernetes namespace concept, right? Namespace quota is a cap, but for this one is actually a guaranteed resource. When you see a zero kota, it means there's no guarantee resource. It's going to run only on the borrow resource. And there's one extended resource here is a call SDG and SL gigabyte. This is the fractional GPU resource that we define in the cluster. And each unit here is basically corresponding to one gigabyte of the AMD GPU memory and this is how we implement the fractional GPU and this is how we actually integrate the fractional GPU with the queue here. So as you can see all of the code here are zero and then the the borrow limit is also unlimited because we didn't set any borrow limit. So if I go to the next one, the demo Q is different. Demo Q is basically pointing to the same uh resource framers specified here. So particularly for the fractional GPU resource is 765 which is actually corresponding to three full GPU memory.

So this is how we allocate actually three GPUs for the uh for this uh demo uh queue. And after this let me show you actually let me show you know as a starting point we're going to submit uh two background workloads the the background workload is very simple is a pie torch like you know training workload is going to allocate pretty much full GPU memory it's going to do some background job over there once this job is starts running you you're going to see couple processes running on the MDSMI outputs and you can see now we have two process processes, PyTorch process is running. Each one is actually using 230 GB GPU memory. This column particularly shows the GPU number and you can see these two processes are now running on two different GPUs. So this is the background job. But you know why this background job is running and the reason why this background job can be running is because the Kota owner which is the demo Q has no workload yet. So if you look at the bottom right, the cluster queue, the background queue has now two workloads admitted to the queue and then the the demo queue doesn't have any workload yet.

But once we submit a full GPU workload, it's going to start the three agents that we just talked about. Each agent is going to request one GPU and as you can see already the background job are terminated because they are preempted. The jobs are submitted to the demo queue and we are now admitting three jobs to the demo queue. And then the background queue now has two jobs pending. And this just shows in action how Q actually handle the kota and preeemption. And you can see now all the background jobs parts are pending and our agents are running. And while we're waiting for the agent to be ready, let me show you the YAML spec of the agents. Um I want to particularly call out there are a couple labels here. The Q name here you can see is actually from our it's actually pointing to a a cluster a web hook that we define for the cluster and we just want to make it easy for the end user to use the queue. So we actually have our own web hook and our CRD to wrap the Q interface. In this case, you can see the queue is pointing to the cluster queue.

And with this, our system is going to automatically find out which local queue to point to and it's going to configure the CL the local Q. And there are three labels here. It's actually all about fractional GPU and they are all labeling. And once they once you label this, it's going to trigger the web hook and we're going to replace this with the rear resource that we just talked about. But in this case you can see the agent is requesting 255 GBTE GPU memory and then every agent is going to be like this. So this is agent one and there are two other agents in the YAML spec but they all similar. So while this uh let me see if the okay so now all three agents are up and running. So let me switch to the simple demo UI. I'm going to show you how this three agent works. So I'm going to send the URL of the of the image to the first agent. is going to return and tell us like you know the first agent is going to say okay this picture is about a group of ducks walking down a road and then the reasoning agent this output is going to be fed into the second agent and the second agent is going to you know have a reasoning about this picture and you can see it has you know it says the behavior can be attributed to several possibilities and then the reasoning is going to be passed to the third agent and you know the third agent is you can see is print out a plan for this so So this is just a you know a simple very s simple multi- aent workflow.

For the demo I'm going to just start a workload generator with a low cost and quickly show you a little bit the performance and we intentionally tune this workload so that it's kind of like lightweight. So while while this thing is running uh let me get back to the terminal. So from AMD SMI output you can see if we do receive a request we do have some like GPU usage but it's pretty lightweight. So and and the agents are are pretty small. So it's definitely overkar if you allocate such a big GPU for each agent. So then that's why we need a fractional GPU. So we we would like to show you how a fractional GPU solution is going to you know make this hosting better. So as you can see the the locals print out like a little bit about the latency and the throughput we are about like you know 1.7 1.5 you know uh latency 1.7 second and this is end to end latency across all three agents right every request is going through the whole pipeline so let me let me stop this I'm going to switch back I'm going to delete the full GPU workflow so all three agents here will be deleted And once we delete it, Q is going to see that the quota owner just released the quota.

So then the background job will come back to run again. So because once the quota owner release the resource then um the the the background job is going to kick in and with that let me submit a fractional GPU workload. Like I said the fractional GPU is going to ask for a much smaller memory size. So then it will not actually cause any termination because we just need a one GPU to host all three agents together. So as you can see now we have all three agents running and the background jobs are still running. There's no termination yet. While we're waiting for that, let me show you the fractional GPUs. Yamos back. Um we don't we don't need to request a full GPU. So we just change the label here to make it a much smaller size. every agent like first agent is asking for 50 gigabyte GPU memory. The second and the third one is asking they're asking for 100 gigabyte GPU memory each in total they are you know good enough to fit into one GPU. So then let me switch it back. So we are still waiting but while waiting let me show you one important thing.

So the reason why we need a very good like a GPU virtualization especially reflection of GPU solution is because the default behavior for VM and by the way the agents are using VRM the be default behavior is that they're going to detect the GPU size at the beginning and then by default they're going to allocate 90% of the GPU memory for the KV cache and if you don't provide a good enough GPU virtualization solution you just slow them onto a time slicing system all of them are going to detect the full GPU memory and they are going to all try to allocate 90% of GPU memory is going to crash. But with the fractional GPU solution that we you know show here the VRM is going to just detect 100 GB GPU memory that we just allocate for them and then it won't get you know it won't allocate too much. So for example in this case you can see VM says I'm going to allocate like 69 GB and that's because it only detects a 100 GB total GPU memory. So that's that's how we make sure that it's completely transparent to the to the workflow.

Okay. So now I think the agents should be running. So let me switch back to this terminal. Let's send the same request. And you can see the three agents are working pretty much exactly the same as before even though they are, you know, running on the single GPU. I'm going to go back to the low cost, restart the workload generator. They're going to send the request and we're going to get very similar performance. Initially might a little bit high, but it you know it will it will come back. But the point here is that all the agents now are running on the same GPU without affecting the other workloads that are running in the background. So we effectively improve improve the overall cluster usage. Okay, with that um let me switch back to our slides. So what we just show you is um you know a solution to integrate the fractional GPU with the queue and then we can allocate quota. we can you know allow different teams, different projects to share the cluster in a much more efficient way and with that we kind of you know dramatically improve the overall efficiency of the cluster and we also show you how Q actually handle the preeemption with the background and the foreground job and if you have any question like you know we have a booth at 5:42 and feel free to come to our booth and we'll be happy to talk more.

Thank you very much and we'll be happy to handle questions. >> [applause] >> Okay. So my question is that we use Q at scale but we use it in CPUs and memory. Can you articulate the differences between what you show here and what we do with CPUs? I mean so so that it's very very clear what is the exact difference of what you did with what somebody would do without I mean what's the what's the source >> well I think Q itself is general so what we show you is that we actually define a extended resource in kubernetes which is a fractional GPU right each unit is actually a 1 gigabyte of the fractional GPU but for Q you can actually def and with that framework you can define any resource in the cluster and Q will be able to manage it Right. So as long as it's a it's a resource that is under management in Kubernetes then you can actually define in the queue in the cluster queue and then Q will be able to manage it. So in terms of the queue I think it's a general framework. >> Yeah. >> So yeah I don't think there is any fundamental difference here but what I didn't show you is actually you know if the DRA is actually enabled in this cluster.

By the way this cluster doesn't have DRA but if D is enabled in the cluster then the resource will be shown in a different way. But again is ti is basically tapped into the Kubernetes framework for Q itself. It actually doesn't care. >> Okay. So just to summarize, so uh instead of a CPU, you just allocated a fractional GPU. That's that's the only change you made, right? >> Yeah, that's a resource that is visible in Kubernetes. >> Okay. All right. Thank you. Thank you. >> Hi. I had a couple of questions. First one, um which cloud is this available in? >> This is the internal AMD cluster. >> Okay. This runs on vouchure. >> Oh, okay. Yeah. >> And then the second question is um for the GPUs themselves uh how how finely can you um partition them practically speaking? >> 1 GB of uh 1 GB of memory and then compute is 1% compute power. >> Okay. Thank you. >> Okay. Uh fourth question uh does Q enforce what's the amount of VRAM actually a workload can allocate or it's something that just act as a scheduleuling level. >> So you mean enforce the cap?

>> Yeah. >> Yeah. the amount of uh the workload the VRAM that the agent can actually allocate >> that's not enforced by Q although Q will make sure that you know if you set a quota or you set a boring limit Q will make sure that you don't you don't go go beyond the boring limit but in terms of the you know capping of the fractional GPU is handled by a fractional GPU solution that we put into the cluster >> okay so it it can happen that a workload load allocate more than the quarter has defined by Q. >> It's not possible. >> Oh, okay. >> Yeah, because it's going to be kept by the fractional GP solution. >> Okay. Thank >> our time is up. Let's take the questions offline. Our time is up. Uh so just come to the front. Yeah. Thank you everyone for coming. Uh really appreciate it. [applause]