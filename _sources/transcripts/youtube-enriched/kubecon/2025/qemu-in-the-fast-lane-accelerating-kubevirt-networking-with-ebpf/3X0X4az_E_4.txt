Hello and welcome everyone to our talk about uh QMO in the fast lane and how we accelerate cube word networking with the help of ebpf. This talk um in this project uh has been done in collaboration with my colleague Anton Yusi and myself and yeah to set the context a little bit um not too long ago the virtualization landscape has been shaking up a bit. I found this quite a interesting quote from the MIT technology review. In late 2023, a long trusted virtualization staple became the biggest open question on the enterprise IT road map. So a lot of enterprises are now rethinking whether to stick to the current um stack or whether to migrate and modernize the infrastructure potentially going into the Kubernetes space because they have containers running anyway there. And the question is are there any viable OSS solutions with help with that um strategic reset to move the VM infrastructure into into Kubernetes and we think the answer is yes that is a cube word and together with selium to help with the networking and then and throughout this talk I will detail you uh why that is in general.

So the combination of Kubernetes and cube word basically enables users to um converge their infrastructure. So you basically previously you had two stacks you had two logging solution metrics monitoring solutions one for your legacy VM platform the other one for Kubernetes. And now you can migrate everything over um into Kubernetes um for both VM and container workloads. And that helps with the overall user uh experience given it's now unified. Um just to give you a architecture picture in terms of how the cube um networking side looks. So you basically have your Kubernetes node. Cube is launching a Kubernetes pod, a launcher pod for the VM. So your regular CNI is setting up VE devices for example with one V flag in the host name space one V flag in the in the pod namespace and then Cubeword is setting up a bridge device and in a tab and then it's launching QMO KVM as a VM and it's basically connecting to that and the IP address it's getting to a DHCP where you can then manage the IPM So we had the for this talk we basically had the so-called moonshot idea.

Can we re imagine how we do the networking like the VM networking in Kubernetes? And before I go into that I want to go a little bit uh more to the fundamental side in terms of how we solved um container networking with selium to remove the the overhead there for Kubernetes ports. If you look at a standard Kubernetes um pod data path, you have your pod itself as a network namespace. You have a V device pair and traffic going in and out of the pod basically goes through the upper stack to routing to net filter and so on. And over the years we changed that insium with the goal to basically make those applications that are residing in parts as fast as if they are in the host name space. And in order to get there we had to pull off a couple of um techniques. So one was to implement qroxy in BPF for the service load balancing. So we don't have to go through the net filter layer. And then we extended the kernel BPF side longer time ago in order to have like a fast redirect in and out of the parts so that we don't have to go through the upper stack and the host name space.

We can still utilize the routing subsystem out of ebpf. We can do route lookups there. So whatever your routing demon is installing like in terms of routes and and so on can still be utilized but we don't have to go to the upper stack and that helps um and eventually we replaced the vaf devices we so we wrote um specifically for psyllium's use case a new device driver into the Linux kernel uh which is called netkit and um I will do a small deep deep dive um later fun and yeah the combination of that basically removed all the overhead and this is upstream in selium today in the Linux kernel so you can actually try it out. So the core idea for this VEF device replacement was basically to enable better application behavior because VEF devices when the pod sends traffic that should be going out of the node that's like inside the weave driver there's like a per CPU backlog queue where packets get encued and if the system is under pressure the those packets they will be picked up later on by the kernel soft airq demon.

And that causes latency issues because you have like two feedback loops essentially. So in getting rid uh of this internal queueing mechanism for traffic that's going in and out of the node, we can therefore also remove the nets overhead. So the other idea that we have was okay like we device is an L2 device but we actually want to provide the choice for users. So you can have like an L3 device that is the default case when you run Selium together with Netkit or you can also have like an L2 device if you want to use it as a direct replacement. L2 device is um also useful in the context of cube word of course but for the L3 device you then the pod uh network namespace doesn't have to care about neighbor entries. So it doesn't have to resolve them because we resolve them in the host anyway. And netkit still comes as a device pair uh but so but in this case we have a primary device and we have a peer device. So this is roughly how it looks and the inside those devices we can attach BPF and the core idea is the control plane for example selium will program all those BPF programs through the primary device.

So an application in a pot cannot unload BPF from that device. So this is all managed for both devices from the host name space or like from that primary device. So yeah, this got merged into the kernel some time ago and the improvement uh can essentially also being seen here. Um that's a flame graph uh where you can see where cycles are spent if you do performance analysis and in the case of the VE devices you can see like those two towers where some of the work is done in like in the process context and under pressure then the other part gets deferred. So you have like this this double feedback loop and basically netkit we have we move the BPF um logic closer to the application and when we do the route lookup out of BPF we already know that oh okay this packet has to leave the node so I can directly uh push it forward to the physical device and it can send it out. So all of this is basically still in the process context. So the application scheduler can account for those cycles uh in the application and therefore make better scheduling decisions.

So doing a performance benchmark what you can see here like the very left side the 63 Gbit per second is like the standard port data path. So we had like two bare metal machines with 100 Gbit u per second nicks and doing performance testing there. And the yellow one is if if the um iperf or netperf if they would be in the host name space. And what you can see in the purple line you basically get the same performance with the with the with those techniques that I just described with netkit and the same for the latency. So we also get exactly the same latency numbers um as if the application is um residing in the host name space. And shortly after we got the netkit driver merged into the kernel uh actually some engineers from bite danceans reached out and they were trying it out in a PC in their production environment and they saw that with the help of netkit they saw like a 12% increase of their uh connections per second that they were able to handle and since then they've actually migrated and rolled out Netkit uh for their container workloads fleet And the same for meta.

Uh so this was an interesting graph that was presented this year at the Fostam conference from a meta engineer and they compared Reef versus Netkit uh for some of their production workloads and they were looking at the P99 latency and they were seeing uh with the applications they had that in case of the VE device um when the system is under pressure when there's high soft IQ load they saw like a P99 n latency of 12 seconds and when they switched to netkit they this was heavily reduced to 0.1 second which is quite impressive. So yeah they also um they are now using it as well and they basically stated the same what we found as well earlier. So now the question is can can this also help in terms of VM networking. So this is again how the standard cube word pot data path looks like. So we have the uh launcher part um it's connected to the host with V devices packet go to the upper stack there's a bridge there's a tab where QMO connects to and yeah in general if you ask yourself okay like why even pod why even network namespace for that well it's basically in order to control resources on the host right like you still have your croups your name spaces so that is still needed uh to contain in uh the VM process in that sense.

So when we moved this over to netkit, this is essentially what it would look like. Uh this is what you can use today with selium if you use latest selium version and um reasonable u modern kernel uh then you can make use of that. And our first question was well how does the performance look here compared to the standard cubeword um um benchmark. So what we saw is a 13% better throughput in a TCP streaming bulk workload and also the latency improved a little bit. So like 12% lower latency um yeah which is already a nice improvement but we still have like this bridge device we still have the tap device. So like you can remove the network namespace overhead but there's still overhead on the VM side of course. So the question was can we like fundamentally challenge this status quo uh with with something else. The first thing that came to mind is well you could use SIOV. Um, SAOV is probably the fastest you can get because it's hardware virtualization. Like you you have a PF on the on the actual physical device that isn't your host and then you can spawn off virtual functions um and move them directly into the VM.

But there are issues with that because like the network traffic that is going in and out is is now completely hidden from the host ST. So you don't really get the observability aspect. you cannot really do policy enforcement for the Kubernetes service access. Well, you can probably access load balancer services through hairpinning. Um, you can probably access node port, but what about cluster IP? So, really those capabilities depend on the underlying hardware and you don't really get a unified container and VM experience. um you can probably solve some aspects with DPUs but then again you're like hardware vendor dependent in terms of capabilities what this can do right so another alternative that we then looked at was AFXTP so not too long ago QMO got a new backend uh for AFXTP and AFXTP itself is a um technology in the Linux kernel uh that is providing zero copy mechanism so that you can implement high performance networking. It makes use of the XTP data path. So this is basically a layer where you can attach BPF inside the driver itself.

So at the very earliest possible point in time where you can do something with the packet and then you can push this into an AFXTP socket. So this is natively integrated into the kernel. you don't have to deal with out of three drivers and so on and you don't really have a vendor login because all the modern or reasonably modern uh nicks they support AFXTP already and yeah um initially it's actually quite interesting to you know being in the kernel community for quite a while like I think like this dates more than 10 years back where there was like the first initial proposal which was actually targeted at accelerating VM M workloads through a Q bifurcation. So you can like split the network card into different set of cues and then one set of cues you would you would map all the way to user space and you would expose that hardware into user space and then you can use it for QMO but then the kernel community rejected that because of uh concerns that you're exposing the hardware directly to user space and this is not something that the kernel typically does uh in also in order to protect the user from malfun like like from from doing something bad.

Um and then this whole thing led to development of AFXTP. So this is roughly how it looks. Um you have on the receive side when packets go into the node, you have a BPF program at the so-called express data path layer which is inside the network driver and the BPF program can decide to push packets up to the local stack into applications. So this will go into the IP TCP layers and eventually into the application. But you can also redirect traffic into an AFXTP socket. And here you have a shared or like memory mapped um region where the driver will then make use of that memory for for the network packets. So they they can be exposed into user space. Then you have a couple of rings where there's coordination in terms of when the application can read traffic or send traffic. So yeah, this is roughly how it looks. But applying that to cube words, there's a catch because the KMO is basically launched out of its own network namespace. And typically for AFXTP, you need like a physical device to attach to to bind to and you don't have it in a network namespace.

So the idea that we then followed was to extend netkit like the virtual device driver there that we have in the kernel uh with the idea that you have the physical nick and it has rxtxq pairs and you can actually uh create a new rss context. So by default you have let's say like traffic gets uh you know load balanced among let's say 16 RXTXQs but you can reduce that to only load balance between 14 uh RXTXQs. So you have freed up two RXDX cues for something else and then you can install rules to steer traffic to that two other cues basically. And you can do that for example based on a MAC address for example based on the VM MAC address and we then extended netkit uh to allow more than just like one you know virtual queue. So you can for example create a netkit device which then has 64 u cues and those cues they can then be bound or like peered to a physical uh nick q. Um and then the idea and then the idea with this whole process is you can move that netkit device into the um word launcher part and then an application can bind against it and then it can make use of uh functionality like a afxp zero copy but also I uropy or defam zero copy.

So this is roughly how the picture looks like. So you can see here like from the physical device if zero we have two dedicated cues that we then expose into netkit and then an application can utilize it um for the defam like the TCP defam zero copy if you're familiar with that it's like in the um technology to map GPU device memory um for like for the network so it's quite useful for AI workloads um the IOU ring TCP zero copy it got merged actually this year into the Linux kernel and that is quite useful for things like storage and like where you have like um very bulky workloads and then last but not least AFXTP so QMO is uh one use case but you also might have others um so just to do a small detour with AFXTP and IOing because I think it's quite interesting and for silium We are basically with this technology with this uh extension for netkit um planning to support both of them. Um with AFXDP you basically expose the full packet uh into user space. So you can have like DPDK like applications.

Um and here like the upper kernel stack like the IPCCP layer and so on they're like completely bypassed. Um and this is useful for things like QMO as we've seen with the IOuring TCP0 copy. Um that requires newer newer nicks uh for example like the very high-end uh Broadcom one or or Nvidia Connect X7 uh which support header data split. And the idea here is like that the headers they go into a linear section of the packet and the data they go into pages and those pages they are basically memory mapped uh with the IOU ring and um the packet still goes through the upper stack normally like any other packet. So it's not like a bypass but it's basically to get uh that regular packet into the Iuring application to do zero copy. Um and this is really useful for uh having TCP stream uh flows that that go beyond 100 gigabit per second, right? Because like at at some point when you get close to 100 Gbit per second, the biggest bottleneck becomes copying of data from kernel to user space. And here this really allows to to to go beyond that the zero copy mechanism.

Anyway, so coming back to the netkit uh Q binding and how this can be applied to cube word parts. So the idea is now looking like this. Uh so we have the physical device. This has a set of uh cues reserved for netkit. Netkit exposes them in the word launcher part and then QMO is basically being started with the AFXTP back end this time. So you don't have a bridge device, you don't have a tap device and you can make use of of AFXTP. There's still a netkit pair down here. This is like for communication from host uh like like from applications in the host to the to the pod. And we did some uh we did a prototype of all of this and some pre preliminary benchmarks and I think they look quite promising. So for the so basically in this case selium is doing all the heavy lifting underneath what I just mentioned what we need to do like setting up the RSS context um based on the VM MAC address uh creating the netkit device binding it to a physical uh queue uh from the physical device and then there's a cubeword plugin that we wrote to launch QMO with the AFXTP back end which is then bound to netkit in the background.

So with this we got a 37% better throughput and also the latencies were better across the board. So like roughly like 30 35% lower latency what we've seen which is quite nice. Um there's still a lot more work to do like to like uh to be done. One thing that we are uh working on is basically an an XTP API rework in the kernel. Right now uh XTP is still like one of the um infrastructure points in the kernel where you can only attach like one program for a given device and many other uh attachment hooks they allow multiple uh programs. So that is like one thing we are planning to do and also like to specify that you only want to have this XTP program run on specific cues. Um so you don't get into the overhead for traffic that is uh going into the host where you uh enter into the XTP program for no good reason. So this is something we are planning to extend and once we have that the next step that we are planning to work on is an AFXTP TX hook because right now traffic that is going through XTP we can do policy enforcement and observability there.

But one thing that is still missing into the in in the kernel is a TX hook where we can then um drop or observe and and whatnot traffic that is coming out of the VM. So this is um the the plan and um for the Q for the QMO AFXTP back end there are some lowhanging fruits but also some bigger tasks. So like some low hanging fruits for example is to use uh vectorized IO to get traffic um in and out of the like from AFXTP into the VM. Um so this is currently not being used despite this benchmark that is actually a lot better in terms of performance but um there are still like some things where we can even further improve it. And then last but not least I think like the really uh bigger item is to have like full zero copy into the VM itself. So that is um right now there's still a copy of the packet. Um despite all that performance is still better which is quite nice to see. And yeah now I want to show a small demo um before opening up for questions. And in the small demo you can basically see utilizing cube word and this whole netkit qbinding and um where where we launched the QM KVM as axtp.

So, I wasn't sure about the Wi-Fi here, so I did a small recording. Uh, what you can see here are two Equinex machines. They're actually the smallest ones, which is also nice because AFXTP also works with all the devices. Um, and we are launching a VM there. And you can see, okay, those two machines, they can ping each other. Um, yeah. And next uh what you can see here is we reduce the default context of the physical device to only utilize 14 RXTXQs. We create a new RSS context um for our cubeword. And we install a flow steering rule based on the MAC address of the of the VM. And now we launch basically a uh cube word uh VM um with a given MAC address. So that is already a feature where you can specify the MAC address is already a feature that is upstream insium. And we have a new annotation where we want to do this Q binding for the VM. And yeah, now the VM is being spawned. Um it's ready and uh what you can see here is we enter into the VM and we can ping to the outside world. So all that stuff is now going through AFXTP through Netkit and then the physical device.

Uh you can see it has connectivity and if you look into the uh cube launcher pot you can see here there's a netkit device. This is being used by QMO for the connectivity. And if you look into the host name space of the where you have the physical device, you can you can basically dump uh the Q information of the physical device. So you can specify the if index and the in the Q index and you can see that this is actually peered to the virtual device which is in the other name space. And yeah, last but not least, you can see that uh Qword has been launching QMO with the AFXTP back end which was then uh bound against the NK like against the netkit device. So yeah, that's the small demo to show that it works. And yeah, last but not least, I want to um do a shout out to various folks who have been involved uh one way or another, either Netkit or in the AFXTP side or on the Linux kernel side. Um so yeah, huge thanks to all of them. So and this is it like this uh yeah concludes the talk and if you have any questions, please let me know.

Thank you very much. Yeah. >> Do you have to enable that kit for silium or is that the default? >> So the question is whether we have to enable netkit for selium or whether it's the default. So right now you have to specify data path mode equals netkit insium in order to utilize it. But we are planning um in the future that once selium detects that the underlying kernel can actually has support for netkit to automatically switch to it. Um but today you have to opt into it. >> Yeah. >> Is there any >> uh the question is is there any imp implication on the big TCP? Um, so I believe the Verdio net driver might support big TCP. Um, but for the AFXTP side that still needs to be supported. So this is would be on the road map to also get working. But yeah, great question. Yeah. >> Yeah. So in terms of doing things like masquerading and so on to have to have the to have it routable. Um I'm the plan is basically once we have that afpx hook we can port over some of the selium functionality. I mean like the the code in general is quite reusable.

So you can you already have it for XTP versus TCBPF but for the transmit side then we can also use it there and then we can also if needed masquerade. So yeah that's still on the road map. Um >> right. >> Yeah. I mean so currently we have done this through this annotation but yeah that's anyway we can also catch up later or like it's yeah >> jumbo frames. >> Does it work with jumbo frames? Um so for like in in terms of the XTP itself uh some of the devices they only support up to one page but there's also multibuffer XTP where you can have things like jumbo frame support. Um I think like from the AFXTP side that should be possible to do. Like I've seen some nicks that do support it. Once you do work >> multiple on the same consuming multiple from the physical >> correct yeah so yeah um so that really depends on the amount of RXTXQ pairs that your nick exposes some have very high amount others might be more limited but if that is the case you can still always go to the current scheme where you uh can run selium today with netkit but you still have to bridge and tap device.

So that's like the fallback if you don't have this latest um uh in infrastructure for that. So that's still an option. Yeah. >> Does it support VLANs? >> Does it support VLANs? um that would have to be done basically through the BPF program because like uh this is also the case today with with XTP. If you want to handle VLANs um yeah you need to handle in the BPF code. Um so yeah it it can support VLANs >> dependify >> say again or >> I mean it's I would say it's like transparent to that. So it's like technology underneath that and like that's one layer higher. So yeah. Yeah. >> Would VMs with multiple interfaces work in this? >> Uh the question is would VMs with multiple interfaces work with this? Um yes. So it you you can actually have um multiple Q bindings and then expose multiple interfaces into VM through the same mechanism. So it would be possible. Yeah. >> So how would one specify like in the manifest or definition like annotations like which interface to bind to which? >> Yeah. Yeah. All right. Um I'm actually out of time but we can we can catch up after the talk.

That's as well. Okay. Good. Uh, I'm also around at the Silian booth or is booth if you have any other follow-up questions or comments. So, happy to take them. Thank you very much.