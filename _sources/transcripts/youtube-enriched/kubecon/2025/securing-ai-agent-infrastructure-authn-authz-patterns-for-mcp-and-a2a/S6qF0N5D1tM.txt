Hello everyone. Uh thank you for attending this session. I'm Yoshik Tabata from Joso. Today I'd like to talk about securing AI agent infrastructure authentication and authorization patterns for MCP and A2A. In this session I'll share practical authentication and authorization patterns to secure agent infra infrastructure with a focus on MCP and A2A. We'll grant the discussion in the CNC am paper which is expected expected to be published near future and then map these principles to real world implementations using keyroke and spire. By the end of this session you will have a practical blueprint for moving from concept to implementation. So this is today's agenda. First, I'll explain what MCP and ADA are and why they emerged. Next, I'll introduce the CSF IME paper principles and discuss why they matter for agents. Then, I'll present uh concrete authentication authorization patterns for MCP and ATA. Finally, I'll provide a demo showing these ideas in action using keyro. Uh first of all, let me introduce myself.

Uh my name is Yoshik Tavat and I'm a senior consultant at Hitachi. I specialize in securing API and identity for enterprise and clouded platforms with over 10 years of experience designing secure API ecosystems and identity solutions. As a tech lead for CSF tag security and compliance, I contribute to best practices, white papers and proposal reviews and facilitated meetings in the APAC region. I'm also an active open source contributor especially to Kikro as a language maintainer and a feature developer [snorts] and in addition I organize community events such as Kikok on Japan speak at conferences like cubic and write books and articles on IM and Kiko. So let's get started. Uh we'll start with what MCP and A2A are. Uh before AI agent uh LLM could only suggest steps and users had to manually exe execute each action themselves. This limited both uh speed and consistency. AI agents changed this by acting on the user's behalf, automating tasks and human supervision. As the responsibilities of AI agents increased, they needed to communicate with a wide variety of protocols such as SQL, CRIS, REST APIs and many others.

This diversity made integration complicated and often inconsistent. To address this, uh, MCP and ATA were introduced to bring standardization. By adopting these standard protoc protocols, we can reduce the need for custom adapters and make inter operability much easier and more reliable. So first uh what is MCP? MCP is a protocol that standardizes how agents discover resources and exchange context with external tools and services. A key authent authorization feature of MCP is that it requires agent to use the O 2.1 authorization code flow to obtain an access token for accessing MCP servers. I'll explain the MCP authorization flow in detail. First, when an agent wants to access an MCP server, it first sends an MCP request without a token. The server returns a 401 unauthorized and www authenticate header indicating that authentication is required. Then the agent retrieves the protected resource metadata including its authorization server information. Next, it obtain the authorization server metadata to get to know how to communicate with the authorization server.

And then if necessary, the agent performs dynamic registration. After that, the agent uses the O 2.0 authorization code flow to obtain an access token from the authorization server. Finally, the agent makes an MCP request to the MCP server. this time including the access token. [clears throat] By following this standard standardized flow, MCP enables consistent and secure delegates delegated authorization for tools and services. Next, uh what is A2A? A2A focuses on agentto agent communication. It also defines how agents discover each other. As you can see, the A2A authorization flow is much simpler than the MCP authorization flow and the interaction of obtaining credentials is out of scope and ATA allows us to use not only an O to O token but also API keys and other supported methods as credential to access A2A servers. Then how [clears throat] do MCP and A2A work together? As shown in this diagram uh which is referenced from the ATA website, MCP and ADA often coexist in real world systems. MCP is typically used for agent to true communications while A2A is used for agent to agent communications.

However, if A2A use uses weaker credentials such as API keys, it can become the weakest link in the security chain. To maintain a consistent and strong security posture, it's important to align A2A with the same level of assurance as O 2.1 just like MCP. [snorts] So next we introduce the CNCF IM white paper and link it to MCP and A2A. This slide gives an overview of the white paper. The white paper which is expected to be published near future under the CNSF identity access management initiative defines key requirements for authentication authorization in cloud native environments. It introduces two security patterns basic and advanced to address different security needs. For each security pattern the white paper defines both authentication requirements and authorization requirements. Authentication requirements are further divided into user authentication requirements and workload authentication requirements. These requirements are based on several standards as best practices such as O 2.0 open ID connect the Zakum PP architecture model SPIFFy and NIST SP 800 to 63 digital identity guidelines.

So why apply IM principles to MCP and ATA agents communicate to over HTTP APIs. So they inherit classic API security risks and the WASP API security top 10 remains highly relevant here. Four out of the top five risks are related to broken authentication and authorization making them the most critical security risk security issues for APIs. In addition, the OSP top 10 for AM applications also repeatedly pointed points out the importance of authentication authorization. All of these shows that robust authentication authorization are essential for secure sec securing agent interactions. This is why applying IM hot paper principles helps mitigate these risks and ensures secure and reliable agent communications and the white paper defines two target use cases state workload use case and state workload use case. I will omit the detail but MCP and ATA end points are generally treated as stateless from an authentication and authorization perspective. This is because each API request is independently authenticated and authorized.

This is regarding the security pattern. As I mentioned in the overview, there are two security patterns basic and advanced. I'll explain these security patterns in more detail here. The basic pattern is recommended for systems with limited external exposure and nonsensitive data. It mainly addressed external threats such as unauthorized access and impersonation at the parimeter. On the other hand, the advanced pattern is necessary for systems that handle sensitive data, connect to public networks or are used by the by an unspecified number of users. It addresses internal threats such as lateral movement and compromised workloads. In the best pattern, the entire cluster is created as an implicit trust zone and API clients obtain an access token and send request to the parameter workload. This parameter workload validate tokens and delegates authorization to policy decision point over PDP for internal communication between workloads. Token exchange and adding access tokens are optional in the advanced pattern.

uh each workload is treated as on its own implicit trust zone. All service to service traffic within the cluster is secured using spiffy based mutual GLS. So every connection is authenticated and encrypt encrypted. External access tokens are exchanged for internal access token before being passed to downstream workload. At every workload author authorization is enforced by evating policies be the PDP. This approach defends against movement and credential replay providing a zerras environment inside the cluster. From now on I introduce the five key IM principles but today I will not go into the details of each principle due to the time constraints. So for details for each principle please refer to the first paper. The first principle is mutual TS that is a mechanism that encrypts communication and a authenticate both parties using mutual TS. The second principle is O 2.0 token exchange. It is a process that replaces external access tokens with internal access tokens for the stream course. The third principle is OIDC client authentication with SPF sit.

It uses spiffy variable identity documents to authenticate workers to the OIDC OP. The fourth principle is O.0 access token valition. This verifies the signature issuer audience expiration and revocation status of access tokens. The final principle is PDP based authorization decision. It delegates authorization evaration to a PDP separating authorization logic from application logic. Okay. So now let's turn those principles into concrete concrete patterns for MCP and A2A. First uh this is the basic pattern basic authentication to authorization pattern for MCP and A2A. In the basic pattern all workloads inside the Kubernetes cluster are implicently trusted and security controls are enforced only the at the cluster parameter. External clients authenticate via OIDC or ODC and O 2.1 and authorization decisions for incoming requests are delegated to a PDP. Internal service to service communication does not require mutual GS or token exchange making this pattern suitable for environments with limited exposure and nonsensitive data.

Second, this is the advanced authentication and authorization pattern for MCP and A2A. In the advanced pattern, we enforce zero trust by applying mutual GS authentication using specific S bits between all workloads. Authorization decisions are delegated to PDP at each workload and token exchange must also be implemented implemented where necessary. This pattern is recommended for environments handling sensitive data. public network exposure or requiring requiring strict strict internal security controls. Next, I'll show you a demo using keyro and spire. First, let me briefly introduce Kikro. Kikro is an open-source identity and access management solution. It provides all 2.0 0ization server capabilities and single sign features and is currently an incubating project under the CNSF. Kro's major features include support for industry standards such as O 2.0, open connect 1.0, summer boot and more. This also covers specifications referenced by MCP including the O 2.1 O 2.0 zero authorization server metadata and dynamic client registration protocol.

In addition, kro can act as a central authorization service or PDP enabling policy based access control. Next, I'll briefly introduce spire. Spy is an open-source system for workload identity management. It implements the spiffy standard and issues sits to work. Spy is a graduated project under the CNCF. This is the today's demo architecture. We implement the advanced pattern and observe how it works in practice. First uh we used C URL as a simple MCP and ATA client and key serves as the ODP and PDP. Spy act as the spiffy server and spiffy agent. Here is a more concrete demo architecture based on the spire quick start example. This demo architecture shows how secure agent communication is achieved by combining kickro spire boy and ngx. First, the client obtains an external access token from keyro and sends an MCP request to workload one through MBY. Mboy checks authorization with keyro. If approved, the request is forwarded to the front end NGX. Before reaching the back end, Mboy exchanges the external token for an internal token and obtained an X509 SB from Spire.

Then establishes a mutual GS connection to work 2. On the back on the back end verifies the SB bit and again checks authorization with key clock. If authorized the back end NJX processes the requested and returned returns the response to the client. Okay. So let's go to the demo. [clears throat] Just a moment. Okay. So, here the port here are the all ports for demonstration. And first, these are the key clock ports. And this is a spy server port. And this is a spy agent port. And this is a front end port. And this is a back end port. First I will show you front end configuration. [cough] It exposes an MCP endpoint and that force request to the back end and it also has protected server metadata endpoint. Next I will show you the back endx configuration. It also exposes the MCP endpoint which simply returns JSON response. Next, I will show the source code of the MBO external authorization filter. The key function is a check function. It first obtained the access token from the author authorization header. And there are two configuration mode.

Uh one is for the in inbound request and one another is for the uh outbound request. And uh this access token validator with decision mode handles inbound request. It validates the access token for example checking the audience claim and then uh it delegates the authorization decision to key clock. when delegating authorization. Uh first it uh added the external access token to the this audience audience parameter and the external access token in the authorization header and then it gets decision result and if the decision is true the filter response with Okay. And uh second uh this access token exchanger mode handles outbound request. It exchanges the external access token to an internal access token. During the this exchange, it first fetches the JS bit and use it for client authentication to the IDOP and it specifies the external access token as the subject token parameter and it also specify the back end's client ID. Then it gets an internal access token. After that it rewrite the authorization header with the internal access token and returns.

Okay. Next I will show you the key configuration. We use the demo realm and we configure two clients. One is for front end and uh we enable the token exchange feature for this client. And we set we set the client authentication method using the spiffy jit. Also we define authorization logic here. Just a moment. We set the MCP endpoint as a resource and post method for scope. And uh we define some simple user based policy. And this policy just checks user is a white user or not. And this permission enable us to associate the resource with the policy. This permission associates MCP endpoint with the uh created policy and this is the client for back end. The back end doesn't execute token exchange. So we disable the token exchange feature for this client and we configure the client authentication method using a speed feature test bit and we configure authorization logic as the same as front end. This policy is as the same as front end. Also, this permission is as the same as front end. So, let's execute the MCP authorization flow.

First, uh we execute MCP request without an access token. We receive 401 unauthorized response code and www authenticate header. This header in include the pro protected resource server metadata endpoint. So next we try to get the protected resource server metadata of the MCP server. The protected resource server metadata includes its authorization server information. So next we try to get the authorization server metadata. The authoration server metadata includes for example a registration end point this is for dynamic current registration and anomalization end point and token end point for all 2.1. So next we try to register CR dynamically. It's token endpoint authentication method is private keyshot and the request also include JWKS. And this is a successful response and it includes a client ID. So then we use this client ID to request an access token. First we try to execute an authorization request without pixie. Pixie is a mandatory standards for O 2.1. We execute this authorization request. we get an authorization error response and it said that missing parameter called challenge method code challenge method is the pixie parameter.

So I will explain why this check was running and we use kro current policy mechanism here. This mechanism is used to enforce multiple checks required for any profile with one setting. So we don't need to set up a troublesome settings manually one by one. This time we configure the policy that invoked when an authorization request includes the MCP scope and that uh multiple checks required for O 2.1 are executed and this profile include the fixy enforcers. So the authorization request was failed. Next we try to execute an authorization request with Pixie. This time Kiklo successfully display login screen and we log in with the white about the user and grant access to the C C AI agent. This time uh we receive the authorization response successfully and it contains an authorization code and we then we get an access token by using the authorization code and then we execute the MCP request with the access token. this time. Yeah. Then uh we got a successful MCP response from the MCB server. Now let's review the logs of the MBO external authorization filter.

First we check the inbound request for the front end. In the check method [snorts] we can see that the authorization delegation succeeded and okay decision was returned. Next uh we check the outbound request for the front end. In the check method, uh we can see that the token exchange succeeded and an internal access token was returned and the process completed successfully. Finally, uh we check the inbound request for the back end. And in the check method we can see that the authorization delegation succeeded and an okay decision was returned. Yeah. Uh this conclude the demonstration and as you can see during the MCP circle successfully return the response various IM principles are enforced. So thank you This is a summary of today's session. Uh, securing AI agent infrastructure requires robust integration of authentication authorization. Applying CNCF hard paper principles such as OOS for AP access, PB architecture for authorization and his spiffy for workload identity enables practical cloud native security.

And today we showed how to implement them with Grog inspire. That's all for today and I appreciate your feedback via the QR code. Thank you very much for your attention. [applause] Okay, any questions? maybe first of all thank you very much for your presentation. Uh good question that I had is like u Asians uh in in enterprise systems they will need to impersonate people or person that is made making the request uh and forward this at all the chain and then you know like to acquire the permission that you need in the in the edge right the MCP2 that is trying to access a context needs to validate against the person that is trying to do that and if it is doing that through the agent you need to forward the token all over. If you forward the token all over, you will need to do multiple checks, right? The proposition that you did also incorporates that or not really. Sorry, >> just moment maybe you can you refer to this kind of diagram, right? Yeah. >> Uhhuh. >> Because in the in the curve that you are doing uh imagine that you have a third party, right?

For example, OpenAI or another hyperscaler. Uh once you configure the agent there, you probably will emit some kind of master key that has your token that can later on when you do another request. >> Mhm. >> Emit new tokens in your name to then forward through the agent into the MCP so it can be validated at the edge where you need to access the context. Make sense? The your question is we may uh in enterprise uh configuration we may have the back end outside of the Kubernetes and some services right? Yeah. Yeah. That that is uh a good question and uh currently uh only uh tested uh with this uh configuration but uh configure configuring that uh enterprise environment maybe that kind of problem uh occurred I think so and in that case uh currently uh there's no ideas but uh maybe yesterday's sessions there are some uh solutions for exchange the uh this internal token to outside external services.