Good afternoon everyone. Thank you for coming out to join our session during lunchtime. Uh my name is Karen John and currently I'm working as a senior machine learning engineer at Apple. >> And I'm AJ Vora. I'm a principal generative AI applied engineer at AWS. >> So yeah, today jointly we'll be talking about simplifying advanced AI model serving on Kubernetes using Helmcharts. As we all know, AI is evolving faster than ever. But to reliably serve the diverse types of models ranging from LLMs to multimodal models to stable diffusion models and so many more on Kubernetes still remains a very challenging task. So in today's talk we'll be sharing about some of the common challenges uh in bringing up this stack in Kubernetes faced by ML engineers like myself and the operations team and then we'll propose a solution. We'll also show you a demo in the end to see how everything comes together and we also will be sharing a QR code pointing to our GitHub repository for this project. So then you can go home and try it out yourself.

So hopefully at the end of the talk you can walk away with some practical ideas and tips that you can apply to your daily work right away. So this is a quick look into our agenda today. We'll kick things off by sh uh providing an overview of the Kubernetes AI model serving stack and this should be broadly applicable to all types of diverse models. Um and then we'll talk about the challenges in stack standing up such a stack in the realm of fast evolving AI space and then I'll hand it over to AJ who will talk about our solution and with two advanced examples and in the end we'll conclude with a recorded demo. So what does a typical stack look like in serving AI models on Kubernetes? Well, at a high level um at the most foundational layer, we have the hardware clusters consisting of machines, accelerators, network infrastructure pieces and this layer is usually provisioned and managed by the infrastructure engineering team. And then on top of that sits the deployment orchestration workflows which deploys the actual workflows to the Kubernetes cluster.

And this layer is usually operated by the site reliability engineering team or also known as S SR. And finally at the top we have inference servers, backends, the models of our choice and various a IML frameworks to run the actual model inference. And this layer is usually implemented by machine learning engineers and used by application developers. So the focus of today's talk will be mainly on the first layer and that is how we manage and deploy efficiently the diverse setups of AIM ML frameworks in Kubernetes without making any assumptions on how the setups are on the bottom two layers. But before we jump right into this top layer, maybe let's take a step back and just zoom into each of those highle blocks to see what's the inner components that are building up to them. So we kept the color scheme roughly the same on this slide as the previous one, but at the first glance it might look a little busy. So let's dissect it piece by piece. On the top left corner where the red boxes are, this represent the service pod.

This is responsible for running the actual inference service. And inside of the pod, we have the inference server which handles the incoming request outgoing response. And then we have inference backend which runs the actual inference engine or crunches the compute for inference to happen. And then we have the model itself. And supporting that are the various a IML frameworks such as PyTorch, TensorFlow and so on. And a runtime environment. If you're running on GPUs, it will be CUDA and some collective communication primitives like all reduce all gather. So that's the top left part. And if you shift focus on the right side where the grim boxes are, those are the Kubernetes constructs. The demon sets integrate with the cublet so that kubernetes can schedule pods to on the right hardware with the right res with the right storage. And finally at the bottom we have the device drivers which connects the server pod to the actual hardware accelerators. Also the network drivers which enables fast inter node communications.

And like mentioned today we'll only be focusing on this top left corner where the inference stack happens. But in the GitHub link which we'll provide in the end it actually contains the full stack solution covering all aspects including automatic node provisioning using carpenter and cluster autoscaler. So let's dive right into this inference stack. And speaking of which, there's a broad spectrum of setups in terms of how the model uh inference is set up. So if we imagine the spectrum being like a horizontal line, let's say in the middle, we have the anchor point where one model runs comfortably on one GPU. Then on one end of the spectrum, we have the very small models which just need maybe a fraction of GPU to run. And then on the other end of the spectrum, we'll have the huge models which needs many GPUs to run on. So for this talk right now, we're not going to worry about this end of the spectrum where we need a fractional or one GPU per model type of setup. Um we'll just be starting at the point where we have one model which needs to be spread across multiple GPUs exactly as depicted on this diagram here.

So here the model can uh is spread across or sharded across eight GPUs but they're all fitting within the same node. But as the model sizes grow and as the context window length grow, we need more and more GPU memory. not just to load the model weights themselves but also to have enough headroom so that we can have a sizable KV cache especially with the rise in need of aentic AI applications more often than not we start to need uh to have this multi-node inference setup so for example in this diagram drawing we have one model that's spread across two nodes and sitting on top of a total of 16 GPUs And for uh this diagram just a side note uh here under this setup only the headpod in node one will be receiving uh the request and responding to the outside but both the headpod and the worker pod will work together to run the actual inference compute or tensor operations. And we also call this setup having a tensor parallelism degree of eight and pipeline parallelism degree of two. So let's take a concrete example to perhaps illustrate things more clearly.

Uh let's say someone wants to run a large open source model such as DeepS R1. We've been using the word inference server backend models in the general terms. But for this spec specific example, let's say we want to choose Triton inference server as our inference service interface which takes in requests and return responses. And then we can take VLM which is the inference as the inference backend which runs the actual inference engines and compute uh on the under the hardware. And then when it comes to serving the Deepseek R1 model, which by the way has 671 billion parameters, we could arguably fit all the model weights into one big node if that big node has, let's say, eight B200 GPUs under certain assumptions about the KV cache size. But often times we need a large or larger KV cache size to get better performance, better throughput and so on. So in that case most likely we'll need to cross the boundary of one node. In other words, having two of those big nodes of running uh B200 GPUs or in other scenarios maybe we don't have access to the great greatest and latest B200 GPUs.

Maybe we only have access to H100 or H200 GPUs. In that case, we'll certainly need this multi-noting setup. So the bottom line is the larger the model sizes, the more uh window the larger the longer the context length is of your prompt, then you more likely end up needing this multi-node inference setup. So now let's take this picture of uh the DeepSc R1 serving stack and fit that into the bigger picture of Kubernetes serving in general. What else do we have? Well, in addition to the stack we just talked about, we have we can choose PyTorch as our a IML running uh framework which goes well with VLLM and we can choose CUDA as our runtime environment and we can use Nickel which is Nvidia's um communication protocol as our collective communications. So now this picture is fully painted. How do we bring each of these pieces into production? Well, first and foremost, probably some of you sitting here already are experiencing that yourself is we need to figure out a compatibility matrix between the runtime and the device driver.

And if you're running your workload on GPU, that would mean you had to find the right CUDA version, CUDA runtime version that works on your installed CUDA driver. And then we need to set up this hardware interface part. But fortunately this part is usually encapsulated in the OS image itself. So as ML engineers like myself uh we don't usually don't have to touch much and then it comes to choosing the right AI frameworks such as PyTorch um the right runtime such as CUDA and the inference server binaries of your choice such as Triton inference server and then the demon sets but fortunately these circled boxes are also coming in with open- source or vendor provided container images. So again uh we got lucky and usually we don't have to change much but serving a model is more than just having the right server binaries or picking the right Triton server. More importantly we have to have the right set of configurations. Those configs are needed to for example launch the server with the right arguments to define the model input output formats data types to point to where the tokenizer files are things like that.

But this config the challenge here is it varies across different choices of inference servers backends and the models which essentially means this diversity in this inference stack leads directly to the diversity of the configurations and aligning all of them to work seamlessly together becomes our core challenge. So to handle this challenge in uh config diversity, one option we thought of was okay, we can put the configs in the image itself. But that slows down rapid de development because that essentially means for each combination of the server, the backend, the model, you would have to build a new docker image for it. That is not really maintainable. The other option is let's externalize the configs onto a shared file system. But that breaks uh or in introduces challenges in reproducibility because when configurations are pulled out into the share file system, they're decoupled from the Kubernetes deployment manifest, which means we no longer have a single unified record of the model, the code version, and the configs set to launch the server.

So now we've established a dilemma. Embedding the configs in the image itself slows things down. Pulling them out into a shared file system breaks reproducibility. What to do now? Well, the solution is we need to use a declarative packaging tool to templatize the configurations which should not only enable rapid iterations but also ensures reproducibility. And to tell you more about that, I'll hand it over to AJ. >> Thanks, Karen. Okay, so our objective is that we need a declarative way to package everything and have version it and then of course templatize it so we can manage it easily. And that's sort of like the northstar. That's what we're aiming towards. But before we get to that and kind of describe that solution in detail, let's touch upon something basic. I'm assuming a lot of you are very familiar with this, but just as a quick recap, how does an inference server stack actually run in Kubernetes? Well, essentially you're going to have to have the appropriate resources within Kubernetes that map the stack.

And the way you create resources in Kubernetes, as we all know, is you essentially express your desired state to the API server by saying these are the API objects I want and then some controller is going to be watching that and create the resources. So that's just a quick recap, but what kind of APIs are we talking about? In other words, what kind of API objects are we talking about when we're talking about inference stacks? So as it turns out, and this is this is I guess maybe slightly surprising, there's a very small number of Kubernetes APIs that really play a role in the entire world of inference in the context of Kubernetes. And one of the simplest APIs is of course deployment. And this is the API that you can use along with a horizontal pod scaler, a config map and a service um and a persistent volume claim which is the shared file system. So this collection this set can help you essentially work with single node inference very easily and you can also horizontally scale it. So you can think of it as this is the baseline that would work in a lot of cases like for example if you were working with Triton infant server and you wanted to run any of the backends like tensor RTLM, VLM, Python, whatever this would work, right?

Okay. So we talked about that of course with increasing model sizes and context length we need multi-node inference. So one option for doing multi-ode inference in the context of Kubernetes is to use something like a leader worker set which actually conceptually works very similar to the previous case of using the deployment API except you've substituted the leader worker set instead of the deployment and in this case the leader worker set gives you a hierarchy um in terms of there being a distinct leader and then multiple one or more workers and that allows you to create the topology for multi-node inference that you need that the frameworks dem demand. For example, PyTorch requires you to have a master node and then the worker node. So it allows you to map that and this would be perfect if for example you wanted to do let's say uh Triton inference server with the tensor RT LLM as the back end. So this would map very well to that. Um there's another very interesting API which is the ray service API and in in some sense this kind of combines the capabilities of the previous two APIs because in this case the service first creates a cluster the ray cluster which is sort of like gives you the capability that the LWS was giving in the in the previous case and then on top of that the ray serve maps the actual application.

So you basically have a solution that gives you the distributed cap uh capability as well as the ability to run single node in the same solution. And we'll actually look at the distributed the multi-node inference example of this in a bit in the demo as well as in the examples. But this sort of like unifies both the cases single node inference as well as the multi-node inference using a single API which is you know the ray service API. Um so now that we understand what kind of API objects we would need to do uh the inference stack in Kubernetes now we have to basically figure out how do we do the packaging so that we can have versioning and we can have templatization and so for that u we come to the core part of the solution and that's basically helm charts. So the idea behind helmcharts is that they provide you a mechanism called templates where you can define the API objects all the API objects that you want to create in the topology that you want to create it and at the same time they have basically a go templating engine available and that allows you to do the templatization.

So now you have all the pieces that you need. you have the ability to package the APIs that you wanted as well as the configs and all that and templatize it so that you don't have to have that problem that we ran into early on with you know the the images where you're like constantly creating new images. In this case, the helm chart itself will remain unchanged and you can actually work with less than five helm charts across all of the complexity of inference that exists today in the context of Kubernetes and all you have to do is supply the right helm values and then you end up with the solution. So now let's look at some couple of examples. So the first example is the application of the ray service API that we just talked but in this case for multi-node inference. So in as you can see in this case the requests are going only to the headpod and then the model itself again through the pipelining aspect is spread across the two nodes. So like tens of parallel degree 8 pipeline parallel degree 2 and of course there's nothing magical about eight and two just keep that in mind but that's just an example you could have multiple nodes you could have pipeline of four and spread the model across multiple nodes so that's actually the example we will look at in the context of the demo um and uh the next one is a bit interesting from the point of view of um to our knowledge We have not seen this like out there in the literature or in the examples and so on.

And this uh idea is that we basically using both the two uh cluster planes the leader worker set that we talked about first and the ray cluster at the same time. So they are sort of like co-planer and the ray cluster is being created on top of the leader worker set. So in other words, you start off with a leader worker set group where you have a leader pod and then you have bunch of one or more worker pods and then from the leader pod we use MPI run to fire off a cluster like a ray cluster. So now the ray cluster is basically superimposed on top of the leader worker set right and then whenever the leader worker set scales the the ray cluster is going to automatically scale with it but the ray cluster will always be contained within the leader worker set. So you might be wondering u okay but why like what problem are you trying to solve by doing this and the problem that we were trying to solve is we couldn't figure out how do you run something like a Triton infrance server with VLM this might sound like hey that should be easy straightforward but we didn't find that to be the case so I'm talking specifically in the context of multi-node inference with the single node it's easy it's straightforward but with the multiode running uh Triton infer with tensor RTLM back end was straightforward that's like built-in but doing it with VLM was not so then essentially we we basically arrived at the idea that hey if you have a ray cluster already running on top of the leader worker set then what happens is whenever you use VLM VLM is is already integrated with Ray so it's capable of automatically detecting that it's inside a Ray cluster and it uses its distributed executor backend and basically makes use of that ray cluster automatically like you don't have to tell it other than you have to say I want this tensor parallel degree I want this pipeline degree and then it takes care of the rest so you could have four nodes you could have eight nodes which by the way you might need in a lot of cases if you have like a 100 or h100 kind of machines and you have really large models and it will basically take care of that.

All right. Um, so with that, let's actually, so we'll show this again, uh, QR code for the for the GitHub at the end of the talk, but let's, uh, look at the, uh, the demo. Okay. So, this demo is uh, is it playing? Yep. Okay. So the demo is basically two A100 uh based nodes like eight GPUs in each and uh we're running uh let me actually pause that and set this up a little bit better. Um so what we're trying to do is um when you go to our GitHub you will see a whole bunch of examples that already exist. Like there's a think of it as like a little model hub and you'll see a lot of examples with all the different types of things we discuss. But obviously your first question is going to be I have a new model I want to run. How should I do that? So this demo answers that question. And so in this example, let's say if you go to the GitHub, you will not see the model that is here in this example um purposefully so that you can actually you know kind of try it out. And the model in this example is GPTO OSS uh the 120B.

And so you say okay I want to run this model. how would I go about doing that? And so the demo explains how you go about doing that. So >> yeah, we're just starting over. Um so in this case the the the key thing is we're using AI to help us generate the example. So we're using cloud in this case and we're just going to say hey look at an example that already exists and then just give me a new one and here are here's what I want like you know tens of parallel pipeline parallel whatever and it's basically using the uh in this particular case it's using cloud 4.5 sonnet and it's just generating the the code for that example based on the patent that's already there in in the in your canonical example. Um, and once it's able to generate the code, then we basically there's like a Jupyter notebook you use to kind of walk through this process that we just described of like using Helm charts, uh, applying the Helm values and all that, but all the Helm values that you need here are being automatically generated.

Um, and all you have to do is basically just run the notebook and you, you know, you get the answer. And you can do that pretty much with, you know, different types of models. So can you pause the thing for just a second? So one of the we found this gratifying on two fronts. One was if we are talking about a generalizable uh you know pattern then it would be good to know that AI can quickly recognize it and basically spit out multiple examples uh without you having to do them again and again and so on. And all you have to sort of like tell is in a decorative way what is it you want and it should basically figure out the rest and we were pleased to see that it was able to do that pretty much the code ran without changes out of the box and that was uh you know gratifying in that regard. The other thing I wanted to share is as we were preparing this slide deck, we were using different AI models to kind of give them the slide and say tell us like what what do you what do you see in it? And what we were surprised to see is that as we were going through the slides, it started predicting what they will see in the next slide.

So we said, okay, this makes sense. We we're probably on to something. And so we just wanted to share that tidbit. So go ahead and play. So I'll just let you watch the rest of it. So those are the helm values um in a YAML file and that's all you have to I I I would say that's all you have to write but it technically you don't have to write it. You just have to let somebody generate them. And then this is the notebook where you're just going to use those hem values to do what you want. Um, and you still have to do things like build a container if you need a container. So obviously you have to build the base container. Um, then you still have to download the model from hugging face uh the weights and put them on some shared file system. And then after that you basically After that, you just launch the service. And launching the service basically just means you take the the Helm values that were generated. They're going to get applied to the chart. And the chart has the definition of all the Kubernetes API objects you need and so on and the Helm values are going to get substituted into the templates which are going to render and you're going to get the manifests.

The manifests are going to get deployed by Helm and that means you have now a package, one unified package that has everything. You have versioning. It's templatized. It was automatically generated and you can keep going generating as many more, you know, model examples that you want. And all you need is uh and I say that all you need, but essentially all you need is the GPUs, which is the hardest part we all know. But other than that, you can pretty much pick any model from the hugging face. Pick your desired like you know idea of what kind of things you want to do. And of course you need this kind of experimentation because often you don't know the answer uh exactly what you need especially when you're dealing with new models for the first time. Okay. So that's it. Uh any questions? Happy to answer them. Yeah. Yes. >> I don't know if it's >> just ask. >> Yeah, >> it's working now. Yeah, great talk. Um, so, um, I mean, why was Helm your choice and not writing a custom operator for this? I think writing custom operators would solve a problem in a given specific thing and then you are also creating a new API.

Operators go with API and what we were trying to do is not necessarily create new APIs. There's enough APIs that are out there but we were trying to say whatever is your favorite API whatever is the stack you want to do you basically should be able to do it. And uh there is many there have been many attempts to write what I would call yet another API that's going to do it for everything. And we never found that to be the case. And those APIs have to basically evolve over a period of time to kind of just just barely keep up with the rate of innovation of what's going on. And we thought this was maybe a more flexible approach. >> Hi um this is Patrick from Anthropic. have a question about the production scaling. Um what I see in the demo you guys uh launched a particular leader worker set and reser. So that is a single replica. So in production you want to scale. Um how does that work? >> So un like we didn't show this in the demo but underneath the demo is like uh carpenter. So that's the scaling mechanism.

So at the top you will just keep creating replicas of whatever the API objects are like LWS or just raise service and then whenever there's a scaling of the pods it will automatically scale the scaling of the nodes uh at the lower level by carpenter um or cluster autoscaler whichever you want. So that's the scaling mechanism at the infrastructure level. >> Okay. So they work by creating more and more leader worker set. >> Exactly. So that you'll you'll create more and of course you can start a desired state at the beginning um which could be more than one obviously and then the max so you can set the callers uh the book ends but within those bookends it will scale automatically up and down. >> Cool. Thanks. >> Yep. >> Uh super interesting. I'm curious about the templating. I mean that could be considered a spec in spec driven development. >> Do you think of it that way? Uh like clawed code is spec driven. >> Yeah, we thought about it that way. But the problem is as soon as you do that like as soon as you define a spec then it can only handle so many things.

And what we said was keep it just completely open and whatever the chart you define that chart is what you want to in you know instantiate and then whatever the values that chart needs is your values file. And so we just keep it completely open-ended and and uh but having said that when you actually look at the examples you will see immediately there's a pattern and most of the things are like in the same but we didn't go to the next step of defining a spec or anything like that. >> Okay. The second part of the question is if you had done that could you use K agents to automate all of these experiments? >> Um yes theoretically we haven't tried it. So you know there may be a difference between theoretical concept and actually working it out but yes because in some sense when the demo if you saw was already an agent doing the all the work uh we like you know the whole creation of everything was being done by the the agent instead the IDE that the example I was using kirao but you can think of it as that agent was using claw the llm to create the whole thing and then running the notebook was essentially the only bet.

>> Okay, >> thanks. >> Yeah, so we we Okay, last question. >> Um I actually just have a comment on uh on the scale. I think the question was more about like scalability of the solution, not how you autocale. >> Yeah. >> Um I just want to make a comment on LWS. It builds on top of stateful set. So it inherents all the >> uh scalability um you know characteristics of that. So that's a design decision from LWS. doesn't try to reinvent the wheel. Yes. >> Builds on top of >> like bulletproof or like battle tested APIs. >> Yes. And I recognize you from some video. You are you involved with LWS? >> I think so. Yes. >> Okay. Thank you. Thank you so much.