Thank you very much for being here and first before starting with this session uh let's do a little poll I would say so how many people knows about Kafka it seems all the people here and how many people know about stream okay less people about stream so for people who don't know about stream just a stream 101 stream is a CNCF incubating project about running apacha on kubernetes this. So you can use streamy and on so all the operators that stream easy provides for manage your Kafka cluster to deploy your Kafka cluster. So handling the day one operations and also day2 operations like setting up security or um changing configuration or upgrading your Kafka cluster and so on. Now for people who knows about streams already, this session is not going to cover streams itself. So the cluster operator, the topic operator, the user operator, the main core component within stream, but more the the the ecosystem that we have around stream. Streamy is not just these operators, but there are several components within the streamy organization and we will see how all these components can help you in order to kind of complete your solution.

So the solution that you have by running Apache Cafka on Kubernetes. Um okay so the components that sorry u first of all let me uh introduce myself uh I am Paulo I am an uh engineer working at IBM on the data and event streaming processing stuff. So mo mostly working on Kafka and Kubernetes and streamy of course I'm one of the core streams maintainers and I am also CNCF ambassador but yeah one of the most important thing for me is to be fan of formula 1 and moto GP so Ferrari and with me we have Michael. >> Hello. Yeah my name is Michael Morris. I work for Ericson software technology which is a part of Ericson that focuses on contributing to open source projects that are important to us. So as part of that work I contribute to a number of open source projects uh in CNCF and elsewhere and one of those projects is streamy. >> So as you already mentioned um Kafka is not just the operator you can see in this light Azure block the stream operator. So the core component that you have in stream Z but these are all the other components that we are going to show during this session.

Uh let me just thank first of all the contributors because uh a good part of these components are coming from contributors in the community uh even from mentorship for students and then these components grow up and became the the the components that are today. So we will start in a kind of way that um uh let's say we have an Apacha cluster. Let's see how we can uh uh complete the solution of our Aapachi Kafka cluster by putting all these components what kind of problems they are uh solving. So what problems you have and what kind of solution you have with these uh stream components. So this is from where we are starting. We have the streamy operators. You know the streamy operators like as you already mentioned the cluster operator the topic and user operator are used for your cluster management. So you are able to deploy your Apache Kafka cluster on Kubernetes by using stream custom resources or your Kafka custom resource where you describe everything about your Kafka cluster. You can even handle the topics and users by using Kafka topic and Kafka user custom resource and so on.

Within stream we have other components like Kafka connect, Kafka mirror maker. So the support for all the Kafka ecosystems. Uh and on the other side of course we have the clients the Kafka clients which are using your Kafka cluster exchanging data events and so on. At this point starting from here let's see how we can add all the components that are part of the stream ecosystem as I say to complete this solution and solving several problems and with this and over to Michael. >> Okay. So the first components that we're going to discuss uh in this session then is are related to getting connected to the cluster. So you have an application that you'd like to communicate to the capt cluster with. Maybe you want to send some messages on a topic. Maybe you want to consume from the topic or do whatever it is uh you'd like to do. Um so you can use a cafka client that will take care of communicating to the cafka cluster for you. Uh but this client needs to be configured with cluster connection information.

so as you can find the cluster, authenticate with the cluster uh and so on. Um so you need to gather together all this uh connection information in order to be able to get your CFKA client up and running or perhaps maybe have a use case that uh using CFKA client for doesn't is not a particularly good fit. So maybe you'd rather use uh HTTP to communicate with capture cluster or perhaps you have an IoT device in which case you might want to use MQTT to send data to the to the uh cluster. So the good news is Jimsy has a number of components that will help uh with these uh scenarios. So we have the access operator. So the access operator helps with configuring your cafka client by creating a secret containing all the connection details that you need uh to get your client up and running. We also have the HTT bridge then which exposes uh rest endpoints that you can use to communicate with the CFKA cluster via HTTP and we have the MQTT bridge then as well that your IoT devices can use to publish the CFKA via the MQTT protocol.

So we'll discuss these uh components in a moment but just before we do we'll just have a look back again at the picture that uh Paulo showed us moments ago with our CFKA cluster as deployed by StreamZy. So now we'll add in here to this picture uh the components we're going to discuss in in in in this section and start to build up this picture of what the overall ecosystem looks like. Okay. So now we have our HC bridge and MQTT bridge added in here at the top of the picture. So both of these are acting as bridges into the CFKA cluster itself. And down the bottom right hand corner then we have our access operator along with the secret that it creates that can be used then by the CFKA clients uh to to connect to the CFKA cluster. Okay, so the first component we're going to talk about then is the access operator. So the purpose of the access operator is to simplify the sharing of your uh CFKA cluster connection information with your application. It's called an operator fairly obviously because it's a Kubernetes operator.

So in order to make use of it, you have to create a a custom resource. So the custom resource we create here is called CFKA access. And in your cafka access uh resource then you specify the cluster you want to connect to and the user you want to connect as. So the access operator will then go and read the connect the connection information that you need to establish your connection from the various Kubernetes resources and it'll create a single secret then that encapsulates all the connection information that you need. So your application can then use this uh secret as a single source then for the connection information that it needs to establish the connection to the cafka cluster instead of having to go and get bits of data from from different resources. So using the access operator then simplifies uh uh configuring your client to connect to your cafka cluster. So moving on then to the HTC bridge. The HTC bridge exposes rest endpoints that you can use to uh connect to the caf cluster via HTTP.

It supports endpoints for producing and consuming messages and it supports endpoint then as well for some administrative topics as well such as creating a topic. So when the HTTP bridge then receives a HTTP request, it'll map that then into the corresponding uh calls in the CFKA native protocol using the cafka client and it'll send those off then to the to to your cluster and handle the responses coming back for observability. Then the HTC bridge supports open telemetry tracings. This allows you to wire together the incoming HTTP request with the uh CFKA requests that are sent to the brokers and handle responses come back. It also supports um exporting metrics in Prometheus format. So it comes with good support for observability built in. The MQTT bridge then as the name suggests uh provides an interface for accessing the cluster via um MQTT protocol. This will typically be used then where you have an IoT device uh that's sending messages on to an MQTT topic and you want to map those onto a CFKA topic.

So in order for this mapping to work you have to define um how that how that mapping should take place. In order to do this then you specify a mapping rules and you can see here in the slide we have an example of a mapping rule. So you can see um that in the mapping rule we support use of rejects uh including reject uh grouping. So you can see here we've defined the topic the MQTT topic as building slash and then a word and you can see which represents the building name and you can see this surrounding in brackets indicating this is a grouping. Then we have slashroom and then we have a a number with one to four digits in it which represents the room number and again this is surrounded in brackets. So this is indicating this is our second grouping here in our uh rejects. So when the bridge receives a message then on a topic that matches this reject it will uh then forward it on to caf topic and we've defined here in the mapping rule what the named cafic topic would be which we got building dollar1.

So the dollar one represents the first um grouping in our EIC expression which was the uh building name and when it forwards on the message onto the cafka topic then you can apply a key and we've defined here that the key should be room dollar2. So that's room underscore with the uh second uh grouping from our rejects which was the room number. So if message is received for example on MQTT topic building B/room 101 then it'll be forwarded onto the cafka topic building building B and the caf key that will be applied to the message will be room_101. Uh okay so that's uh the components that we have relating to connecting to the cafe cluster. Hand back now to Paulo. >> So we have kind of fixed the first the first problem. So having all the clients connected HTTP client MQ and QT clients but even um simplifying how the Kafka clients are configured in order to connect to the Kafka cluster. Now let's talk about securing and governing access. So let's say that uh uh the the the security team want to know what are the clients that are using Yashach Kafka cluster and they want to use a token based uh authentication system like for using protocol like like Oout and on the other side the other thing that we want to to do is kind of avoiding noisy neighbors.

So we want to start even throttling the clients in order to avoid that some clients are using too much the cluster and they are becoming you know noisy neighbors for other clients and so on. So starting from what we were with Michael uh we are going to add one uh two more components in the picture which are the oath library and the quotas plugin the c the cafka quotas plugin. So the alt library uh as you can see here is placed in the in the Kafka cluster uh and even on the Kafka clients and on the HTTP bridge because the HTTP bridge as explained by Michael it will act anyway as a Kafka client in order to send a message from HTTP to the Kafka cluster and then of course the the quotas plugin which is about throttling clients uh running against the Kafka cluster and using the cluster for exchanging messages. So uh let's give more details regarding the O library. The O library provides two main components as we saw in the picture. There is one component on the client side because on the client side you need to connect to the authorization service and then getting the token the the access token uh by providing your credentials and then you are going to use this token in order to be uh authenticate to the Kafka cluster.

And on the other side you have the server side component for doing the token validation. uh in terms of token validation uh it's really flexible. So first of all the out library is going to leverage the kind of framework that Kafka set up for you. So extending the call back for implementing uh the the client to get the token or the server to to to run the validation of the token and so on and it's using the s out bearer mechanism in order to exchange the token between the client and the broker. So this is something that Kafka provides for you but it also adds this o over plane that we will see in a few and as I said there are flexible ways to do the validation as we'll see in the next slide and the other thing that the stream z out plug-in provides is a token based authorization using a kiklo authorization service so it means that you can specify uh rules permission on kiklo so this client can do this kind of action on this topic etc. And when you are using the plug-in, it's get the token, it's get the information from key lock and then it will allow the client to to run that operations or not like the ACL that you have today in uh in Kafka, right?

So regarding the validation as I said when you are using the SAS out bearer mechanism there are two kind of validation the the first one is the the the faster one is the local JWT token validation which is happening locally on the broker. So it means that the client get the token from the uh authorization server and then it's for awarding the token to the broker and the broker will use the validation by by using public keys from the uh authorization servers in order to verify the the the sign of the JWT token which is a JSON based token which is something that even Kafka provides out of the box in the last versions. uh but the stream out pro um plugin also provides a kind of remote validation by using uh introspection endpoint. So it means that the token is got from the authorization server is provided to the to the broker and then the broker is providing the token uh to the server in order to to to get uh the token to be validated through the uh introspection in the point. The the most important part of it is that you can not just use JWT token but also op token that can be strings or anything that makes any sense for you.

The other addition is the Oout overplay where the connection between the Kafka client and the Kafka broker will go to through plane. So uh the client is going to provide credentials like a client ID and the secret and then the broker will connect to the authorization server in order to uh to get the token from from that server and then starting the validation. So locally or uh remotely it depends and as we see there are the two options that we can use uh with the stream zero out plug-in library. So that's the first issue that we solved in this section about securing. The second one I mentioned about the Kafka quotas plugin. So uh in Kafka there is already a way in order to set quotas uh and it's a kind of way where you are assigning the quotas statically for your clients as we'll see. While this plug-in is going more in uh dynamic direction. So you can assign statically the quotas per broker but then this quotas is distributed across the clients dynamically. Also the plug-in provides some monitoring information like metrics about the throttling about even the clusterwide volume and storage usage that you have across the cluster.

So you know uh about how many um space still you have on the brokers themselves. when I was talking about the difference uh between the quotas that you have with Kafka built in in Kafka and what you have with the with this quotas plugin the main difference as I said is that in Kafka you are assigning this quotas statically so for example I am you can assign the quotas in terms of bandwidth uh or the request rates in terms of percentage of usage of the CPU for the network and the outread and uh for example you can assign per user per client ID or for both, a combination of both and saying in this example that this client can use 10 megabytes per second as bandwidth which is the max that it's going to have. Uh while by using the streamy quotas we are assigning a kind of static quotas for the broker as you can see 40 megaby per second and then it depends on the client. So the the throttling uh happens dynamically. For example, in this case, we are having one client using up to 10, the other one up to 30.

But as soon as another client is joining the the the the cluster, they will be throttled uh differently. So in a dynamic way, you can see here 30 uh.3 megabyte per second and one of them is throttled from from 30. So it's totally different from how the the Kafka quotas plug-in uh works today. So this is an addition that this kind of plug-in provides you in terms of handling the quotas and we let let's move to operations and reliability. Okay, next we'll discuss uh operations and reliability. Okay, so the problems we're trying to address here are how do you drain your nodes for example for scheduled maintenance or how do you make configuration changes in a way that minimizes the impacts on the operation of your cluster. So the tools we have to bring to bear here are the drain cleaner. So the drain cleaner provides a way to safely manage the eviction of your cafka brokers during the node trading process. And we have the kubernetes convincing provider then which provides um a way for doing a bit more advanced uh uh configuration of your uh cafka components.

So here's our picture again that we're trying to build up of the overall um ecosystem and we'll add in now the components that we're going to describe in this section. Okay. Okay, so we have the drain drain cleaner here on the right interacting with the streams and with the pod eviction. Uh it'll become clear what's happening there when we discuss it further in the next slide. Uh and we also have the cube config fiber conf cube config provider in our uh cafka cluster there. Okay, so moving on to discuss the drain cleaner then. Uh so you could just let Kubernetes uh handle the eviction of your uh CFKA pods dur during node draining but Kubernetes doesn't know or care about the replication of your topics. So using this approach could result in the number of replicas in sync falling below the minimum that you specified. This is going to cause problems to your producers and your consumers. Using the drain cleaner instead helps ensure that this doesn't happen. So the drain cleaner uses the admission control feature of Kubernetes.

This allows you to provide a hook that can be invoked when a request is made to the Kubernetes uh API server. And in this case, the hook is configured to react on requests to evict your CFKA pods. So what the drain cleaner will do then that scenario is it'll deny the request. So this prevents uh Kubernetes from uh evicting your CFKA pods and it will instead then add add an annotation to the pods that will indicate to the cluster operator that the pods should be rolled. So in the next uh reconciliation loop then the cluster operator will detect the presence of this annotation and it'll roll the pods. Now, as the nodes that are being drained have been cordoned off, the the pods won't come back up on the nodes that have been drained and be moved off to to other nodes that are available. But, and I guess this is the key point here, when the cluster operator is handling the uh annotation, it will only roll the pods after checking that it's safe to do so that the uh number of replicas in sync won't fall below the the minimum that you specified.

So in this way the drain cleaner helps to protect uh the operation of your topics during the node draining process. Okay. So moving on to the Kubernetes config provider then. So you configure your CFKA components by uh setting properties. H now you could just set them directly to to values or uh CFKA also provides um a mechanism called config provider that you can provide to CFKA to fetch values for your configuration properties from some external source. And that's what we've implemented here with the Kubernetes config provider. So this is a config provider that can be used to fetch values from your Kubernetes secrets or um your Kubernetes config maps. So in the implementation here we have a Kubernetes client that goes to the uh Kubernetes server API uh to fetch the values that that you want to set your uh properties to. Um, now the fact that this goes through the APIs means you don't have to worry about uh config or mounting the secrets or config maps onto your pods. Um, but it also means as well that you don't need to worry about the secrets being in different name spaces or whatever because you're going through the API.

You can you can read them from whatever name space you want. But it also means as well that uh the component doesn't necessarily have to be running in the uh cluster reader. So you could have um a producer that's outside of your cluster and as so long as it has access to the Kubernetes server API, you can still configure that um producer using your secrets and config maps that are stored inside your cluster. So this provides a way to centralize a s sensitive data such as your credentials and your certificates and so on and kubernetes native native resources and that provides all the functionality that kubernetes provides there in terms of um orback controls and encryption to keep your secret safe. This also provides then for dynamic configuration updates. So uh when you want to make a change to your configuration you can make the change and that'll be automatically uh reflected then into the cafka components without the need to do um any restarts. So when using the Kubernetes config provider then you're protecting the um the the um your CFKA components during the process of changing configuration without any need for any restarts.

So next one finally uh let's talk about observability and testing. So the problem that we have in this case is that our SR teams wants to know what's happening on the cluster. So they need some monitoring solution in order to get of course metrics out from your Kafka cluster having some dashboard like graphana dashboard in order to to visualize all these metrics and so on. On the other side something which is not actually use it directly within your Kafka cluster. It's about um uh Kafka developers or for example stream maintainers contributors going and testing their applications while they don't want to run a um real Kafka cluster uh so developing united integration tests uh for a new feature for bug fixes or just testing the Kafka clients and so on. This is another uh problem that you can have. So starting from here finally we are going to add uh the two last components. On one side we have the matrix reporter uh that as you can see here is running both on the Kafka cluster and even on uh HTTP bridge in order to expose matrix to Prometheus and we will see how it works in a few.

And on the other side, as you already mentioned, the the test container component, it's more not really part of your Kafka cluster, but something that you can use in order to test the features, the bugs and all the Kafka applications that you want to uh so that you are developing. Let's start with the streamy matrix reporter. So for people who knows Kafka they know that Kafka is exporting matrix in the GMX format and stream provides a component which is the GMX exporter or actually I should say use the GMX exporter running as an agent alongside the broker in order to get this matrix from GMX and then translating them in Prometheus and it's exposing this matrix on an HTTP endpoint so that a Prometheus server can go and scrape this matrix. Of course, there is some translation happening here. The JMX exporter has a kind of configuration files where you can define what are the metrics that you want to get from Kafka. If you want to do some reabeling, if you want to change some matrix names and so on and of course it can take time sometimes if you are exporting a lot of metrics because there is this translation in the middle from GMX to Prometheus because most of the system these days are using Prometheus as a kind of format to have everything on graphana dashboard and uh with the streammetric reporter it's a plugin that is plugged directly into Kafka.

means that it's configured at one of the matrix reporter that you can have in Kafka and it just read the matrix directly from Kafka and exposing them directly in Prometheus without any kind of translation for this reason is so it can be used even if you're not using stream but just Kafka on your bare metal on on or virtual machine so these components anyway uh expose the matrix the same way so with an HTTP and the point where the Prometheus server can go and scrape and also O provides a way for filtering the metrics. There are some configuration in the allow list where you can say what are the metrics that I want to expose. Of course, everything is configured within your Kafka stream Kafka related custom resource. The last component uh is the stream test container. As you already mentioned, if you are a Kafka developer, if you are a stream contributor or a maintainer, you want sometimes to to test some features, new features or bug fixes. So you need to run some unit and integration test and instead of running a uh real Kafka cluster you can use this streamy test containers framework which is based on the test container framework providing images for running Kafka on container uh in several configurations.

So you can set up the number of nodes of course you can set up the the if the node are going to be because today is just craft support the in the in the last versions there is not zookeeper anymore uh if you want to use a combined node with controller and broker rule or if you want just controller and then other nodes to have a broker's rule uh and so on and you can also configure the Kafka versions that you want to use and then running your unit integration test against it. This is for example what we use every day in streams in order to run our pipelines and also um the Kafka connect support uh is within the stream test container. So you can even spin up a Kafka connect container if you have some connectors or things like that that you want to test and with that we pretty covered the entire full stream ecosystem. So as I mentioned at the beginning, StreamZ is not just the operators. We have the operators for sure for uh all the cluster management that we need. But across the session, we see that the ecosystem is full of other components coming from the community.

So we solved se several problems. We started from the connection with HTTP and MQTT. So having MQTT producer sending a message uh HTTP client using the Kafka cluster or even simplifying the Kafka client's configuration by using the access operator. On the other side, we introduced the Oout library for allowing a token based authentication for our clients, the quotas plug-in for throttling the clients, and then even uh something more about the configuration to allow a more dynamic configuration within your Kafka cluster by use the cube config provider in a Kubernetes native way. And on the other side the drain cleaner to avoid to lose your high availability in your cluster and to to give the streamy operator a way to move the pods uh but having still the cluster working when there is some node eviction happening in your Kubernetes cluster. And finally we mentioned the matrix reporter for exposing the matrix and the test container for the united integration test. So remember if you are using streamy you can complete your solution by looking at all these several components.

They are all under the streamy organization in GitHub. So you can go and find of course all the the source code there and contribute if you want with that. This is a way for joining us, joining the stream community and there are several ways to to engage with the community starting from you know even easy fix with the documentation or reporting issues reporting bugs or even fixing the things that you find opening pull request or um asking for new enhancements like uh providing stream proposal that we can discuss in the community together with all the other maintainers. So it will be great to to if you are already a stream users to have you engaged with us to improve this project and with that these are all the links that you have um to go and look at stream. the official website, the GitHub repo as I said where all the components live, uh the Twitter and the linkadin account in order to get the news about the releases uh or what's happening in the community and even the YouTube channel uh where we publish demo videos or our community meeting that we have every two weeks uh online.

And with that, yep. Thank you very much.