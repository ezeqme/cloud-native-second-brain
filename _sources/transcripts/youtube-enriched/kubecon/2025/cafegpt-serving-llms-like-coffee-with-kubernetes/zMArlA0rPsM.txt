Hi, welcome welcome to our talk. Uh first of all, I hope you had a good CubeCon. I hope you made a couple new friends. If in case you haven't, please meet us after the talk. We'd love to be your friend. Uh so, welcome to our talk, Cafe GPT. My name is Madav. I'm a grad student at UIU. Uh and I'm I do research in in the area of distributed systems and I also contribute and maintain some parts of the Kubernetes project. >> Hi, my name is Karthik. I'm also a grad student at University of Illinois where I research ML systems and really excited to be here and see all of you today. So before we get into it, we'd like to start by talking about what this talk isn't right. This isn't a deep dive into LLM inference or the internals of how Kubernetes enables uh enables serving such workloads. There's many many more wells suited talks on that topic at this conference and many others. What this talk is rather is that we've been trying to educate ourselves on how many of these different parts of the ecosystem sort of fit in together.

And this is us trying to share that picture with you. So that said, welcome to Cafe GBD. Cafe GBD is probably the best cafe at CubeCon 2025. There's this thing called AI that you might have heard of, you know, a couple million times in the keynotes. We have a new customer with a drink request, an ice latte with oat milk. We have we have a manager and a barista to help fulfill that request to make sure that all of our attendees are sufficiently caffeinated. The manager directs the request to the barista. The barista has access to coffee machines to help fulfill that said request. Each coffee machine has many many moving parts that has the grinder, the steamer, the pressure valve and so on. The barista uses a coffee machine to make the drink and finally serves the drink back to the customer via the manager. Our customer loves the coffee and more importantly loves cafe GPT because the coffee was served in a timely manner and it wasn't very expensive to begin with. So with that out of the way, let's draw some analogies here.

Let's make things a little more concrete. What does serving in cafeter are going to call the router that is responsible for delegating requests to appropriate baristas. The barista to draw a fat analogy is the LLM inference engine using resources available to it and then converting requests to coffee. The coffee machines are our GPUs that actually brew the responses. Let's continue making things a little more concrete. What does the life cycle of an LLM request actually look like? Yeah. So with those analogies in mind, let's talk about a customer who's trying to figure out something really important. They would like to know how they pronounce cube control. And so the LM inferencing engine in our case is going to convert this user input into an output by using trained LLM models on specialized hardware. In this case, our model doesn't output the final answer because it does not want to be divisive at this conference. But internally, the way LLMs do this is by splitting up our input sequence into small chunks called tokens and running those through the multiple layers of our large language model.

As it's doing this, there's a lot of internal representation that gets created as these tokens pass through our model. These can be turned as internal states for now. Using these internal states, the model is able to produce the next token as the output. You might have heard that LLMs are auto reggressive. And what this means is that this next token is fed back in. You generate all the hidden states one more time and you generate the next token. You do this again and again until you hit a special token called the end of sequence which is where you stop outputting more tokens. As you might have observed every single time all the sequence prefix that we have calculated so far is repeated on every iteration. This is redundant computation and it increases latency. So how can we do this better? Well, the answer as is the answer to most computer science problems is through caching. In this case, we have this special cache called the KV cache. KV is effectively the key value pair which is one of the hidden states in our model.

So the forward pass the process of generating the output using KV cache looks like first processing the entire input sequence. You generate all the hidden states and you store those in these in this KV cache which is effectively just memory on your GPU. Once that is done, you can forget about your input sequence. And now you just use all the KV cache representations to generate just one new hidden state. And you use that to generate the next token. You repeat this. So you push this generated hidden state back into the KV cache. And just keep repeating this process until you hit the end of sequence. KV caching speeds up LM inferencing by up to five times. But it comes at an overhead of higher memory use which can become a bottleneck for concurrent processing. To help visualize this, if you have an 8 billion parameter model and you're trying to serve it using floatingoint 16 bits, it's going to occupy 16 GB of memory on a 40 GB memory GPU. That only leaves about 24 GB of memory for your KV cache. And if you're using a model with 8K context length, this specific model, every single KV cache can can take up a maximum of 1 GB for each request, which limits your concurrent requests to about 24 per second.

It will actually be even more slightly lower due to overheads. And so as a result, a lot of research has emerged to make better use of this bottleneck. I've linked some papers below which you can check out from the slides which I'll upload after this talk. So with KV caching in place, we now have two separate phases in our serving process at Cafe GPT. The first phase is called prefill. This is where you do all the prep work. You grind your coffee beans and you gather all the ingredients that you need. Next, once this is done once for your request, you can start your decode process, which is multiple iterations of small units of work where you're just waiting for drops of coffee to come out. We'll keep coming back to this analogy to make things clearer. But with this in mind, I want to call out a few important characteristics of our LLM inferencing workload. The first one is that prefill is computationally intensive. It takes effort to grind that coffee, but it lasts a short duration over your entire request life cycle.

Decode on the other hand is not computationally intensive. You're mostly just waiting um for drops of coffee to come out. It's called memory bound instead because the KV cache reading and writing tends to be the bottleneck here. But it lasts a long duration of your entire request, especially if you're outputting a lot of tokens. And importantly, you also don't know when the when the decode is going to end. This is kind of different than traditional web app uh requests. You kind of just wait for the end of sequence to be output out, which is when you realize that okay, this request is finished. From a customer's point of view, there's a few SLAs that we need to adhere to. The first one is a low time to coffee, which is analogous to request latency. We also want a low time between drops, which is especially important if it's an interactive application like chat. And importantly, the customer might want a low time to first drop, which is the first token that they get. From a provider's point of view, we want to serve a high number of coffees per second, which is our throughput metric.

And since we want to serve these at a low cost, we want high machine utilization. We because we want to extract the most out of the machines that we have. And importantly, a lot of different customers might have different SLAs. Maybe you're someone who's in this image who really wants their coffee now. Or you can just wait for it. You don't you're not urgent and it can be a batch processing job. So to work on these SLAs's, uh let's see how you might attempt to serve this. Let's first meet our novice barista. They're new at the job and they can only do one thing at a time. When they get a request in, they first do the prep work, the prefill, output the first token in this image, and then they process the decode tokens one by one. Our novice barista is not able to do multiple things together. And this results in a problem because if there are more requests that come in, they can't do any work on them until the entire first request is finished. And again, due to our workload, this is a problem because we don't know when that is going to happen.

A more experienced barista can batch things together. So the way this looks like is when you have only one request, sure you start with the prep work for that. But when another request comes in, as you're waiting for a drop of copy of the first request to come out, you can start to prepare other um uh you can start to prefill other requests. And this is kind of where GPUs tie into the picture. GPUs are great for batch processing multiple um tasks together and we can make use of this. So in this in this batch we are doing prefill of the second request and decode of the first one together and you can continue to do this for the third request you can batch two decodes with one prefill and ultimately you just wait for all the coffee to come out of your machine. This has immediate gains. The first one is that your throughput can increase by over 10 times um due to batching because you're doing more concurrent processing for each request per second and your request latency also improves because you're not waiting for requests to queue up.

You can start making progress on them earlier. However, one problem that you might run into is that your batch sizes are now dynamic. What I mean by this is that every single iteration can fluctuate up or down depending on how much work you're attempting to do there. If you're just waiting for a coffee drop, it might take smaller amount of time, but you're also if you're also batching that with grinding coffee beans for two, three requests. That entire batch will take longer to finish. This is called interference between pre-fill and decode and can result in fluctuating latencies of your time to first token or your inter token latency and effectively your batch size which is the amount of work that you're doing is what is controlling this latency. So if this is a problem for you, if you really want to serve um consistent uh SLAs's of latency, what you might prefer instead is pre-filled decode aggregation, which you might have heard of in other talks in this conference. The key idea there is pretty simple.

If these are conflicting workloads, you just separate them out onto two separate devices. You do prefill on only one machine and you do do decode on a separate machine. So on the the LM inferencing engine on the left here is responsible for for doing prefill and once it has computed the entire KV cache, it just transfers it over to the second decode. Not only does this prevent interference between two different um prefill and decode uh pairs, it can also help you optimize your machines better. So as we talked about, prefill tends to be computationally intensive. So you can get computationally heavy GPUs that are optimized for heavy compute and and reserve those for your prefill instances. Decodes don't require that much horsepower. So you can just give it a lighter machine instead. So that's all pretty cool and all and I hope you've set up some of the context of LM inferencing. How do we deploy and actually run this in production? >> So can we use existing infra? Can we use the same infra but then run a newer workload on top of it?

Uh I think it's fairly it's a fair statement to make that Kubernetes has sort of become the def facto platform for most infrastructure platforms in companies or the infrastructure layer for these companies. Um and this picture on the right over here is from CubeCon Paris in where one of the core maintainers of Kubernetes said inference is sort of the new web app. And this is kind of interesting because Kubernetes initially was made for web apps and to scale web apps and then over time it sort of evolved its core to support newer workloads and inference is just yet another newer type of workload. So it's an interesting new workload with unique characteristics that we just learned about. Um and it can be served reasonably well without having to reinvent much of the wheel. But to get to this point there's been substantial efforts that have been ongoing for the last couple of years uh in order to better support these sort of workloads. And as I said, the community has been relentless, relentlessly evolving the core uh and ecosystem projects as well to better support uh this sort of workload.

Now when I say this, what exactly do I mean? What do these efforts look like? Right? So let's start building bottom up over here. Let's say that you want to host the model and serve some requests, right? Let's start very simple. Maybe your model is small enough or what we like to call espresso models. They fit into a single GPU. So you have one GPU, you have an inference engine on top of that and that hosts that holds your model in its entirety. A request comes in, it gets routed to that inference engine or that pod in Kubernetes and the request is then served. You can request for a GPU on that pod by using something like uh by specifying resource limits by nvidia.com/gpu 1. This is good enough if you need one single GPU. It may also be the case that you may not even need an entire GPU. Maybe all you need is a fraction of a GPU for your workload. You can also request for just a fraction of the GPU uh using MIG partition devices. So MIG stands for multi- instance GPUs. There are excellent excellent CubeCon talks on this in this CubeCon and the previous CubeCon as well.

U however as a vendor you may need to statically pre-partition your devices. So you may have to say that my one GPU let's say has uh I partition into into two halves or four 1/4s or 1 1/4 and then subsequent number of 1/8s right so you may need to prepartition your GPUs in order to better serve these workloads and then expose these partitions as individual devices this may not however be very resource efficient because if I need 1/8 of an H100 GPU I might be forced to run on a slice that's larger than what I need if the way I've prepartitioned it does not match my actual workload as of today. So this leads to resource wastage and then of course leads to wasted cost. Um the second sort of interesting caveat in this in this form ofuling and resource management is that you may not really care about what type of GPU you want. All you may care about is I want 20 gigs of GPU memory and you may not worry about where that memory is coming from. However, if you sort of stick to the default or the older way of doing resource uh requests, you may end up you may end up with stringent requirements that I only want an A100 or I only want an H100, but you may not have those available.

However, you may have other GPUs available that can actually serve those pods and you you now have wasted capacity. Uh you may have already heard about this effort called dynamic resource allocation uh quite a bit. So DR can help with that as you may already as you may already know. So for example, now this is an enhancement that's currently being worked on. So this is fresh off the press. Um so in case you're interested, you should definitely check that out. It's very very interesting to read through. There's a lot of really good technical details in there as well. But what this allows you to do is it allows vendors or it allows device providers to partition GPUs on the fly. So device drivers can now implement logic that can partition GPUs on the fly. So you don't really need to statically do it beforehand as long as the request is partitionable. So there are some criteria as to when you can partition it and when you can't, but if it is, you can do it on the fly leading to like more um you can pack more resource efficiency into that one node essentially.

Uh the second thing that it also allows you to do is flexible resourceuling, right? So you can express things like can you please give me a GPU which has at least 10 gigs of memory. So having this flexible model of resourceuling is extremely helpful especially in cases where you need to autoscale which we will soon talk about. So now that we've made sure we're getting the most bang for our buck, how do we actually scale this out? So since we want our cafe to be able to handle many different kinds of scenarios, there might be certain problems that we run into if you have only if you're only serving a single instance, we might have surges in the morning and at lunch and slowdowns at night. We can also have surges of specific kind of drinks, drink requests on certain occasions. For example, an increased number of pumpkin spice lattes during October. We might also be serving different kinds of workloads. Maybe some days we have a lot of espresso requests exclusively for LM serving. These changes manifest as conversationheavy versus coding heavy tasks which have different workload characteristics or also an increased number of requests for certain models.

Maybe the recent GP OSS came out to fix the uh to um respond to these challenges. We can make use of horizontal pod autoscaling in Kubernetes. But what do we scale on? CPU or memory utilization is clearly not a good metric for us here because that's not the main bottleneck at all. We're using GPU and GPU memory here. You might try to then use maybe concurrent um number of requests like the queries per second, but that is also not a great metric for LLMs because different requests are created unequally. A summarization request might take in a lot of input but only output a small summary. Code generation on the other hand might have a simple prompt that's outputting entire files of code. Traditional autoscaling metrics as a result are not a great fit for LM workloads. So let's take a look at what works instead. Q length as it turns out is a really good metric of your load. An increasing Q length indicates that our barista is not being able to keep up with their orders. any new requests are going to face blocking until all pending requests are completed which is unknown to us.

As a result, uh autoscaling on Q lens can help boost your processing throughput because the new machine can start to take up some of that load and this can reduce queuing delays in the process as well. However, I'd like to call out that adding a new machine does not lower latency of your processing requests that are already in flight on another machine. You can understand this as if you add a new coffee machine, your speed of the first one is not going to go up. So you can't increase your inter to token latency for example. If that is indeed a concern for you and you want to autoscale on those violations, you might instead consider taking a look at your path size. As you talked about in an earlier slide, the size of your batch is what is controlling how long it takes for you to output a token. And so what you can do instead is configure your serving engine to keep batch sizes low and monitor the number of batched tokens that you're running with. In this way, by scaling on your effective batch size, you can scale earlier than Q buildup and still continue to meet your SLAs's.

One thing to keep in mind with this is that you might overreact to temporary spikes, but maybe that's okay for you. I also want to mention that since we earlier talked about how prefill decode disagregation is what is allowing you to meet your latency goals, you might also consider scaling up your disagregated prefill deployments. So you if you're running with one prefill and two decode instances and you want to scale them up, you have two options. First, you could scale up your instances while keeping this ratio constant. So you could add three more instances, one prefill and 2D code. This can be great if you have a lot of GPUs to spare and also if your workload has remained the same but you're just seeing a lot of surge in traffic. An alternative solution that is available to you is to instead just scale up individual prefill or decode instances. So in this case if you have more summarization tasks which are pre-fill heavy, you can just add a new single prefill instance. This is also useful if your workload is starting to shift more like maybe it's going from prefill heavy to decodeheavy which can happen from conversation to coding workloads.

Finally, since we talked about how KV cache can tend to become a bottleneck, a very important metric to target here to autoscale on can also be how much KV cache space you're using up. You have two approaches here as well. The first one is reactive. You can monitor and scale if your KV cache utilization is exceeding a certain threshold. For example, if you're consistently running more than 90% at your GPU's capacity, your alternative solution is to profile your workload and using the calculations that we did earlier. So estimating how much KV cache you need to serve every request, you can see how much you need to run at a particular queries per second. And you can use that measurement to scale up your nodes because you can estimate how much GPU memory each additional pod is giving you. So now that we've had some autoscaling in place, we've added some more replicas onto our nodes. The one thing that we need to start thinking about now is how do we load balance across these replicas, right? U we do better than round robin.

I think we should do better than round robin. The sole reason being that sending requests might in in a roundrobin fashion especially for this workload might result in extremely degraded performance. And the reason for that is you may end up with hotspots on certain instances solely because of the fact that you don't really know what each workload is going to constitute. You don't know if it's a pre-fill heavy workload. You don't know if it's a decode heavy workload. And then the interference might actually degrade the performance quite a bit in this case. So your inference engine as you recall is already caching process tokens which is what we've termed as the KV cache so far right the router can actually take this information to better balance requests. So one way to actually route requests is to actually route them uh by being aware of of of KV cache uh presence on all of these replicas. So if if a single node if if if if you're doing a multi-turn chat application, you may want to direct all all of those requests to a single replica because that has all of those tokens cached and it's significantly better for your latency.

Um and the Kubernetes gateway API inference project actually supports as of today, right? So it it supports all of these routing models as of today and it's pretty mature. It's getting there as at least. So you should if you if you're interested, you should check that out. And also if you're interested in contributing, I think that they could they could use the help as well. So check that out. Uh that said, you don't want you also don't want to route to the same instance all the time because you might end up overloading it. Uh so you you might want to build an escape hatch in there somewhere. But in what way do you build that escape hatch? Um we can route to the least loaded replica and the way you calculate least loaded can be a can be a couple of ways, right? Uh one of the common ways is KVK utilization that we spoke about. So you rout to the replica that has the least utilization for of KV caches. Uh the second one is based on Q lens and the third one can be some custom metric that that matters to you and your application.

You um existing projects such as LLMD and AI bricks already support such routing strategies and also the ability to plug in custom metrics for rout routing to least loaded replicas and that's through the API inference gateway project. So these are very interesting sort of complimentary projects that are ongoing right now. Uh and they could also use help and contributions. So feel free to jump on those calls, jump on that project and help out. Okay, that's a lot of info. So let's zoom out and look at a look at the picture that we've we've been building. U we have a couple of nodes which have a few GPUs on them. These GPUs may have pods that are scheduled on them and these pods may be inference engines. The cube scheduleuler handles that. Now, DRA is the flexible resource allocation and increase utilization part of Kubernetes that provides theuling and resource management in a flexible manner that we spoke about. You also have a request that we spoke about initially which is how do I pronounce cube cuttle.

Uh the API inference gateway helps route these requests in an in in a slightly more intelligent manner as opposed to doing it roundroin or some other naive way. And then you have ecosystem projects like LLMD and AI bricks that provide such routing and many other such capabilities like autoscaling as well. So our question was how do I pronounce cube cuddle? So what is the answer right? So we decided to ask the latest chat GPT model and it said what it said right? It said what it said. We we're not going to say anything more on that. Um maybe this is the right way. I don't know. We hope we've convinced you that you do not need to be proficient in language modeling and that when you try and work with these systems, you can ground yourself in the fact that serving LLMs is genuinely as tractable as serving coffee is. Thank you for visiting Cafe GPT and feedbacks and tips. Welcome.