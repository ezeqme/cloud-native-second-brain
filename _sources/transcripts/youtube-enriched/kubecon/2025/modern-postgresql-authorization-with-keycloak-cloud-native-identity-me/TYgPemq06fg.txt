Good evening. So, one of my favorite things about being a part of the CNCF is the opportunity for synergy between projects. So, this is a way we can collaborate with other projects to solve hard problems and innovate together. So, welcome to our talk. We are here today with Yoshuki and myself to show you a brand new collaboration between two of these two of these CNCF projects. uh a collaboration that aims to fundamentally improve the way we authenticate and authorize access to posgus databases in cloud native environments. >> Okay. So hello everyone uh my name is Yoshik Tabata and I'm a senior consultant and I specialize in securing API and identity for enterprise and cloud native platforms with over 10 years of experience designing secure API ecosystems and identity solutions and as a tech lead for CSF security and compliance I contribute to best practices white papers and proposal reviews and facilitate region meetings I'm also an active open source contributor especially to Kikro as a language maintener and feature feature developer.

In addition, I organize community events such as Kro on Japan, speak at conference like Cubon and write books and articles on IM and Kro. Yeah. And I'm Gabriel Bolini. I'm VP and chief architect for Kubernetes. I've been using posgus for 25 years and uh I'm a possess contributor. I'm also a data and kubernetes ambassador where I I basically foster the adoption of uh stateful workloads in in kubernetes. Um I'm a devops evangelist. I always say that devops is the reason why I got into kubernetes and the reason why I will move away one day from it. and in the FOSGA space I'm also renowned for for a contribution in the backup and recovery space which is barman and lately in the uh for the clinp uh software of which I'm a CNCF maintainer. So this is the agenda for today. I will give you a brief introduction about clonerpg then yoshuki will talk about keylo and finally we will show how kiko and po posgugust integrate. So um so on the left as I said we've got Kiklo and Yoshiuki uh will we'll be talking about it.

Key is a CNCF incubating project and a complete open-source identity and access management solution. uh cloud netpg is a popular open-source kubernetes operator for posgus uh originally developed by uh my company EDB and now it's a CNCF uh sandbox project and today we actually applied for the incubation u its job is to be a kind of a robot DBA uh that handles the full life cycle management of a posgus cluster from installation to backups to upgrades and it also provides provides high availability with automated failover and uh it manages uh disaster recovery as well. Most importantly, it's declarative and Kubernetes native. So all you have to do is define a database uh that you want in a YAML file and the operator does all the hard work uh to make it reality. So the goal of this talk is not just to show you that you can point key clock to a posgus database uh with clanbg that's the status quo the real goal is to show the value of uh both projects being in the CNCF and we want to demonstrate how this shared community drives solid integration not just for features that we have today but for the powerful new features that we are developing uh together like the one that we are showing uh today.

So for years the integration story has been very simple. Keyclo uses the posgus database to store its data and if you're in kubernetes for example you can leverage cloud npg or any other operator or any other way to run posgus in in kubernetes but as I said before this is a status quo it's a great one but still a one-way street key is the application and posgus is just a data store. The goal today is to show you something new uh that changes uh this relationship completely. So the with the release of posgus 18, we're talking about end of September. Posgus uh itself now has native oath uh support. This is a gamecher and for the first time posgus can delegate both authentication and authorization directly to key clock. This means that it's no longer just a key clock uh using posgus but we can also have our database users authenticate directly against key clock. So our DBAs for example can use psql which is the posgus uh client interface to connect to a database uh not with a password but with an oath token from key clock.

So today we're going to show our future plans to build the the complete this complete end to end o or native database infrastructure using key and CMPG. So we're moving from um uh a simple um oneway connection to a fully unified modern authentication model for our entire stack. So why do we need a new way to authenticate? So let's quickly look at the challenges with the traditional methods in posgus. So first you have password authentication. This is secure now thanks to uh the scram shot 256 but it creates a credential uh silo. So your database password are stored in in posgus and managed separately from your central identity provider which means manual rotation and potentially credential spl. So next there's LDAP. There are also other system. I'm taking LDAP because it's it's meaningful uh in my opinion. So this is step up because it delegates password checks but still has a few issues. First it's only for authentication and not authorization. So you still manage all your grants and roles inside posgus.

And second, the user must already exist in posgus, meaning that you have to rely on some ways to synchronize your users between LDAP and posgus. So these traditional methods end up creating management silos. This is why uh the posgus project has started a new initiative. So what does this new uh feature actually provide? First you know posgus uh 18 introduces the oathbearer simple authentication security layer sazzle mechanism. This is a generic infrastructure. So it's not st it's not tied to any specific provider like key lock. So uh it all it does it generate it delegates token validation to uh an external uh pluggable validator module for posgus. you need a module. Uh so a module that knows exactly how to talk to a specific provider to a specific or even more of them. Okay. So most importantly, and this is a key feature, uh you can uh use take advantage of a setting called delegate ident mapping that allows you to um uh to delegate uh the module to take full responsibility for mapping the user's identity from the token directly to one or more database roles.

So this is how we get both authentication and authorization for posgus in a centralized way. So what's the status right now? So since it's brand new, there are no in the box validator modules yet. Okay, the community will need a little time to build this uh for providers like key clock. But on the client side, the official lipq library supports the interactive device o um uh flow. So this is the typical uh flow that prompts you, you know, to go to this URL, enter this code and then uh you'll be able to be connected. Okay. So this is perfect for end users, human users on PHQL npg admin. So the key takeaway is this. The foundation has been laid. uh this is just the first implementation and future releases of posgus will add more uh like machine to machine uh flows and to be fair I mean TLS certificates are still a great option uh for machineto-achine authentication nowadays okay so but for now we focus on the human uh access so now that we have this generic framework in posgus 18 um that needs an external validator module.

We have a bit you know another challenge now in a cloud native world. How uh how we can u uh provide this module in in a container image that is immutable. So we can just SSH and install a package like uh like this in a mutable system. So uh how do we get this validator module in in a in a in a posgus container? So there are two options. Cloudp uh um uses now a modern uh method based on extension image volumes. So this is a new feature uh that builds directly on a powerful new primitive in kubernetes called image volumes. It lets you take a container image uh not to run it but to mount uh its content as a readonly volume. So uh the this entire extension as a an image workflow is the state-of-the-art and requires modern Kubernetes cluster uh version 133 or higher. But what if you are on an older version of Kubernetes? You can simply distribute the validator module in the main uh operand image of posgus. So uh this means you can use a custom posgus image and uh the key takeaway in in any case is that both methods just make the library visible inside the posgus container and once the library is visible it's just a matter of configuring the validator in posgus as you would set with any other parameter.

So now that we know how we can uh load a module I'm very excited to introduce a new project that we are launching uh with Yoshuki today. It's called the posgus oath validator for key clock. It's experimental and it's available on GitHub and it's part of the cloudpg uh organization is and it's our first attempt for cloudpg and kiklo to work together and also uh work with posgus. Uh so our focus uh although today is for kubernetes this is a standard module and it can work also outside outside kubernetes. So this is the component that bridges the gap and it can be loaded using either the methods that I showed before. So from now on it's on your shoe. >> Okay. Thank you. >> Okay. So uh from here on let's shift our focus to identity and access management. First uh let me briefly introduce Kikro. Kro is an open source identity and access management solution and it provides both 2.0 zeroization server capabilities and sign features. Key is currently an incubating project under the CNSF and the keys major features include first uh support for industry standards such as post 2.0 open ID connect 1.0 2A 2.0 and more and second social integration with services like GitHub X Facebook and more and others.

Third, the ability to connect to existing user stores including LDAP and active directory. Finally, key can act as a central authorization service which is today's key feature. So, by moving complex authorization logic out of the database and into keyro, we achieve consistency, scalability, and easier governance. Let's walk through how centralized authorization works uh with key lock. Uh first the client sends a request to the protected resource and including an access token and the policy enforcement point or PP receives this request and forwards the token to kick rock for validation and an authorization decision and the key checks the token and evaluates the authorization policy and it then returns a decision either to allow or deny that requested operation and the PP enforces key clock decision. If access is allowed, the requested operation is permitted on the protected resource. The result of the operation such as data or status is returned to the client. And if access is denied, an error is returned instead.

Like this, a key clock acts as the policy decision point or PDP, ensuring that authorization policies are applied uniformly. This slide illustrates a concrete example of how the PEP communicates with keyro. When the PEP delegates authorization to keyro, it sends an HTTP post request to kick rock's token endpoint. This request includes the client's access token in the authorization header and specified several parameters in the body such as the grand type, audience, permission and response mode. Hikro receives this request and first validate the access token. Then it checks whether the requested operation is permitted according to the defined authorization policies. If the request is arro key responds with an HTTP 200 okay status and a JSON body indicating that the operation is permitted and if the request is denied Kikro will return an error response indicating that access is not permitted. This kind of HTTP exchange allows keyro to act as a PDP and making a realtime decisions for each request.

And let's explore how POSSQL manages authentication authorization when integrated with an external authorization service. When a client attempt to attempt attempts to connect Kikro possess QL first consults the pgba.com file to determine the authentication method. In this scenario also based authentication is used. The client presents an access token which possibly SQL validates using an O token validator. After successful validation, the external identity contained in the token must be mapped to a local poss. This can be done locally through pgident.com file or delegated to an external service such as key. Once identity mapping and authorization are complete, OSSQL establishes a session under the mapped role. Among these two options, option two is the preferred approach for centralized authorization because delegating authorization and ro mappings to key enables centralized scalable and policydriven access control for poss. This diagram shows how key poss and cloud native PG work together in Kubernetes and the client obtains an access token from keyro and connects to Postgress SQL using that token and authorization is delegated at key.

Cloud PG orchestrates the database cluster ensuring secure and scalable operations. To summarize, use an or authorizator to externalize identity to ro mapping and delegate complicate complex authorization logic to a central PDP for consistency and scalability and kickro allows us to unify token insurance and authorization decisions in single platform and it simplifies governance. Now let's move to the demo using key tokens for possible SQL authentication and authorization. In this demo architecture, we use PSQL as a client and instead of authentication authenticating with a password, PSQL obtains an access token from keyro. Here we use the O 2.0 device authorization flow which is well suited for SRA clients. Once the access token is obtained, PSQL connects to the POSSQL corer and presents the token for authentication. POSSQL uses a custom O variator specifically designed for uh special specifically designed for keyro and this variator is called POSSQL O variable for keyro and is publicly available in the cloud native PG repository.

After the token validation, ro mapping and authorization decisions are delegated to keys authorization service. Key applies its policy to determine which database law and user should assume. Once this process is complete, POSSQL establishes a session under the mapped law. Now let's look more closely at the device oration flow. The client psql initially connects to possql uh without an access token. In this demo uh postsql is configured to require o 2.0 authorization. So the client initiates a device authorization request to keyro. Key responds with a device code, user code and verification URI. The user opens a verification URIs in a browser and enters a user code logs in and gives consent to authorize a PSQL. Once authorization is complete, the client request an access token from keyro using the device code. Keycro issues access token. Then the client reconnect to posgress SQL this time presenting the access token. Posgress SQL validates the token and maps the user to appropriate database ro then establish a session under the throw.

Okay. So let's go to the demo. Okay. So maybe sorry. Okay. This is these are the ports for in this demonstration and this is the cloud native PG port and this PG is the uh postgress scale port that is managed by cloud net PG and these are the key clock ports. And this possess cube airport is used by kickro. And in advance uh we prepare some data for this demo. For example, creating tables or insert some insert some data. And uh the especially the important thing is we create uh this updatabase and also two rows upgrade write and upgrade up read only and upgrade only roles we can select on all tables and with up readr table readrite row we can also insert on all tables and I will show you the kicker configuration we use demo rail and We define two client. First app a client. This is for C this is for this is this client for the psql. So we enable the device authorization grant and another client is poss resource. This client for uh all valid data. So we define authorization logic here. We define resource for database app and we define scopes for app read only and app read right and we define policy uh policy user read only and read right.

This policy is simple uh authorization policy that the user is uh white tata user or not and in permission we associate the policy and the scope. In this permission we associate the uh app read only to policy user read only. And in this permission uh we associate the app read only read only and the read read right read write to the read write and so let's connect to the postgresql first uh here this is a uh version of psql and that is 18 and this is a psql commands to connect up db This is a host name and this is a lo up this time and the database is app and al issure we use the kickrox demo realm and the client ID is app a then we execute the psq command. Then the verification URI and the user code are displayed here. So first copy the user code and open the verification URI in a browser. Then device login screen was displayed. So we paste the uh user code and click submit. Then login screen was displayed. So we log in with white user and we grant access to app. So device login su successful and we can confirm we can connect the app up app up app up app up app up app up app up app up app up app up app up app db and also we can execute select command like this but we cannot execute the insert command.

This is because we can connect we connected to app with read only up read only role. So the next time uh we connect to the up with app read like this and again verification URI and user code display. So copy the user code and open the verification URI and enter the user code again and grant access to A. Then device login successful was uh displayed but this time uh the connection was failed or ver authentication failed for the user upgrade or retire. This is because the key clock position policy denied to assign this operate right uh low. So we next change the policy. First remove right of the user from uh this read only policy and add the white of the user to the read write policy and then uh execute the same command that is uh uh with app read right row again the verification URI and the user code was displayed. So enter the user code here and grant access to up a and device login su successful was displayed and this time we can connect to the up and also we can execute sec command like this and this time we can insert uh we can execute insert command here and we confirmed the uh user gym was uh inserted.

Yeah. uh demonstration is that's all and we can confirm that the mapped ro was updated in accordance with the key author for token based authentication enabling secure model access externalizing identity authorization allows policydriven control and removes static credentials and kickro serves as both authorization server and PDP centralizing governance and combined with cloud native PG on kubernetes we this architecture uh delivers secure scare scalable database success that's all for today we appreciate your feedback via cure call and thank you very much for your attention Do you have any questions? >> Any questions? >> My mic. If you want to ask the question, we can repeat it. How does time out handle this configuration for the >> so the timeout configuration is you ask >> for the user session >> session is established with the database what's the session if I want time out the user >> so that's a psql setting so you're talking about the the time out between so you're asking about the timeout from from the client perspective.

You mean? >> Yeah. Authorization timeout is what I'm asking about. >> So, so that's that there's a normal time out for for the for the session for the connection with with posgus. So, it should be handled like that. So, I don't now I think I need to double check. Uh but I'm pretty sure it's it's a lip pq setting. So that should be the way I'm I'm we can check out later if there are some settings for uh specific for this but >> say for those of you who are not familiar with posgus this is a very big uh step. So posgus uh usually makes um takes you know a few releases to get to a complete state for a feature. Okay. So it's done incrementally. So the fact that we've got oath now it allows us to um as poss 20 so year after year. Okay. So this is just as I said this is just a first step but it already provides a very good use case especially in banks where every time you connect to P with PSQL you actually have to authenticate and get the authorization again. Okay. So this is in very high regulative um regulatory environment.

Sorry. >> So two quick questions. One thing I love about CMPG is everything is declarative um the adding stuff to keycloak if I have a 100 databases or like ephemeral databases let's say do I just do that once or do I have to manually do that for every database? And secondly, um, the entering device code, is there a way to not have not require you to like copy and paste stuff? I just want to click a link and then it authenticates. So, so the right now there's the interactive mode in pos token either. So, it will come. Okay. So, the machine to machine or the non-interactive mode will come. Okay. With with lipq. Um, so the first qu So the do you want me to reply about the first or you want to reply for the first uh >> I can maybe uh answer the second question I think. >> Okay. >> And first was around um the key click manual step. >> We currently uh in this demo de demonstration we use the uh device authorization go device authorization flow. So we can enter the device and not device code not user code in the browser.

So that is uh only for the PSQL and that is suit for the CI tools that means the device authorization flow is a suit for the PSQL tools. So we use uh that flow this time. So if you use the for example uh authorization code grant uh maybe uh that step is not needed. So yeah uh entering user code is not necessary step. Yeah. >> Okay. So that is the aspect on that part. >> And the first question >> no the fir and the first question about uh doing all the rights and then the permission. So the you don't need to have the roles in in posgus uh but you need to have the the roles. Okay. And then you associate the roles with users in key clock. Okay. So that's the biggest advantage. But the roles need to be in in in each database. So cloud netpg at the moment you can you can uh manage roles in a declarative way. So all you have to do is do YAML YAML configuration. Okay. We are improving declarative roles in 129 hopefully with specific CRDs for roles. Right now they're part of the cluster definition but we want to have like similar to what we've done with databases that you've got a database resource we want to have role resources so that we can you know also have you know more more control >> is is the flow support currently only in PSQL or is it in other clients as well and if not educated guess when that might show up.

So I know that PG admin is is I think they have already added support for uh for for this. Okay. So but theoretically yes the idea is to improve lip pq so that everyone will will benefit but otherwise applications can already you know work with key clock as well or you know to to to improve the flow but yeah maybe you know your shuki knows knows more. Um so you showed how to authenticate with uh an access token into um Postgress. Is there a way to apply um I guess uh using the using the claims out of the JWT to uh apply it to role level security or the policies that are enforced in role level security >> I can you can >> yeah so um so you're authenticating into Postgress with a JWT with with an access token right >> um and that's mapping to a specific user. Um, but you have the context of that access token present. I'm wondering if uh in role level security within Postgress if you could use claims or attributes from the token in the role level security policy that's that can be you know applied per table.

>> Oh, I don't Yeah, I don't know how yeah I don't know how to respond to the question. >> Yeah, >> no worries. >> Thanks, guys. So, I'll have to find out. Yeah.