Thank you so much for coming uh to mine and James call talk about like uh our experience experiments with API server. Um unfortunately James is not going to be here like but I'll try to do my best to run you through what we've been playing with uh lately in the API server with a goal of like providing uh users with a native multicluster experience. Um so uh again this talk for the people that have seen maybe last year talk about workspaces from me and James is very much like a kind of continuation of this kind of like research in the API server that we've been doing. Um I like workspaces which was focus on multi-tenency. What we're going to be talking about today is more aggregation of resources across clusters and you will also like kind of see during the talk like some of the motivation for us like to build such features and I'll try to leave like five minutes at the very end like for a question. I've been told that you need to go here in the line. Uh but yeah otherwise we can just meet uh in the hallway truck I guess.

So to begin with the talk I really want to start talking about scalability. Uh the reason why is that for a very long time like for customer or platform team implementing Kubernetes like um the API server has been like a very complicated application to scale. This is because when you start exposing the Kubernetes API to your users and you don't control the interaction there with the API, uh the API server itself like for a long time lack some core features in order like to kind of enable it to scale and and reliably and kind of like platform team found themselves like kind of like wrestling around like this software to protect essentially at CD like from the load generated from the uh users. uh there were there are very good and well doumented like best practices in the community around autoscale kubernetes for instance for large cluster that is to separate events which have a high churn rate from other resources into a separated CD cluster um h prefer like binary encoding via protobuff of like uh like the objects that are returned like from uh the API server in order to reduce like memory utilization and also like drive really like work with your internal customer in order to make sure that every CRD has strong validation in order to prevent like situation where like serialization began becomes again like a problem with memory.

um in the most recent like Kubernetes releases like the community has been doing a fantastic job in kind of tackling some of this fundamental problem in the API server design and in particular the fact that the API server itself doesn't have like strong protection from center client behavior and I really like to call out like the work that has been done around cyber uh uh serialization in particular because like uh CRD object are uh encoded in JSON and whenever like the API server needs to return like this kind of like object to client especially with big list like it needs to des serialize and resellerize against this object and it's very hard like to manage a scale in term of memory. Another very good feature that has been released is serving consistent reads from the cache. This is specifically because when you have clients that needs to list object and then don't use properly the resource version semantic. This will result in all of the load like going to CD and you cannot really scale horizontally at CD.

So therefore like all of the cluster performance is being impacted by these uh uh small clients. Uh last but not least, something that kind of like uh also I've been uh we've been eating is that before like the some the snapshotable API server cache, there were also like some uh performance like or issues when like using server side filtering of object in particular the API server was doing the filtering after returning all of the object from CD which again was causing like lot of issues with memory. So pretty lot of good work lately from Kubernetes. However, when uh when we look into scaling the API server, it's not only about nodes. It's not only about like all of these things in the API server. There are a lot of implication also when that comes when building a platform in particular how you sharp the controller, how you scale them out because the more gen rate of object that you have, the more deep is like the work you of this controller. You might be looking into ways like to kind of sharp your platform and maybe not go for massively large cluster but maybe chunk them in smaller pieces.

Um the other thing which I think is something that for me is always interesting is running a platform team they really want to provide customer with very very well defined full domains. We will see later about this and every large cluster also means like a huge surface of like issues when they come like outages because of configuration changes and things like that. So what we try to do is also like and a consideration is import that is important is how to limit the blast radius of changes. So into the motivation like for us like to do this about the motivation for us like to kind of do this work in the API server is really like kind of like what it means like building a platform on top of Kubernetes. I I've been talking to someone like these days and they kind of said like oh the only grail of the platform engineer is provide powerful abstractions and that everyone buys in and this is kind of really true like it's so hard. I think Kubernetes has been doing a fantastic job in the community and I mean it the room is full on like providing very powerful abstraction around workload but when we build the platform in there is also a lot of like domain specific knowledge that needs to go in and as platform team we're looking to abstract on behalf of our users.

Uh it's very important therefore like to provide like this domain specific workload abstraction uh provide domain specific like topology information to the users so that they can like spread their workload across like four domains or maintenance domain depending on how our platform are configured as a platform team also a very important element and this is a bit of like the fight within the user and like the platform teams is how we drive utilization while still enabling user like not to fight with it like uh again like how many people here have been working around like Oh, tuning resource request and limits and this kind of aspect and in general like um building a platform means being able like to manage the life cycle of the underlying cluster resources easily with your user having minimum impact right and that's like again a very thing a thing that Kubernetes does very well but when you look into implementing in a specific context Kubernetes there are way more feature way more things you want to provide there than what Kubernetes provides uh from default with this in mind like the kind of idea that we add is um in the traditional uh sense and this is what many of us and we also like have been using is like this idea of cluster as a service where a cluster is essentially like a cluster are mapped to teams and each team has his own cluster that he kind of manage.

However a scale something that we have also like consider is what are the alternative to this approach. What if like customer want to scale be beyond the single cluster uh um boundaries and and how do we kind of like enable oursel to slice and dice the infrastructure as we want without exposing this detail to the customer. And the idea that we had was really like can we build like a very thin API server layer on front of the actual Kubernetes cluster that enable us as platform engineering team to have the ability to steer like the customer workload across cluster add cluster remove cluster add nodes scale them as we require without exposing all of this information to them of have to coordinate this effort with lot of customer whenever like new capacity come online. The first building block that we're going to talk about in order to achieve this is like the multicluster scheduling. We will actually spend more time about like the API server because it's also very important. But for the multicluster scheduling essentially what we found ourself is that traditionally the Kubernetes works by having essentially like a a deployment object and like kind of like generating the controller manager a bunch of pod and placing pods across like no nodes.

We wanted like to find like a very similar abstraction but just for workload and build something like that enables us like to kind of like get some deployment workload stateful set jobs and these kind of resources and map them to cluster where the resources are available and then like use the regular Kubernetes scheduleuler in order to kind of place the individual pods uh uh on the specific nodes in our quest like for looking for this solution is where we find multiq multiq is a communicated project generated by the working group batch and it provide us with very very powerful APIs in order to manage that. So the idea here is like as a user instead of submitting like a workload across a individual cluster we'll now submit it against a multicluster API server and then multiq there will uh uh receive this kind of workload reconcile this workload map it to actually this deployment map it is internal like workload representation pretty much similarly to what a pod is. Find a cluster where like this deployment can be running.

Push it down sync up the status and then like in the actual cluster what we will have we will have like the standard like deployment controller generating like the pod object and the standard scheduleuler binding the pods to the node in order to run them with what we find in multiq and the reason why we decided to use multiq for this is because actually like is a very flexible and easy to extend framework. uh it comes with some opinions like there are a lot of use cases for multiq around like in the batch domain we actually use it like a for every workload like it's not only about batch for us it's for any class of workload and really as a way for us to have an API that allow us to h abstract like this workload type and manage like the multicluster scheduling for them um one of the things that we've been really like kind of engaging and trying to collaborate with the community upstream is around like this kind of concept of elastic workloads so how we can support the scale semantic scale that is for instance required for deployment because from the batch use case like jobs are things that you dispatch and they're until completion while for us we want to support like more flexible use cases in order to hide entirely like everything like to our user and provide them a experience which is compatible and with what Kubernetes provided by default.

Another thing that is very important coming back like to the custom workload abstraction is also having the possibility not only like to limit these work to core Kubernetes API type but also do it like for our own specific like API types that we provide our customer as a way to build on top like stateful services beyond what Kubernetes provide by default or like job workload and and orchestrate them across our platform and multiq support and Q and multiq support that is essentially via the concept of external framework that allow you to have a controller that kind of like manage outside of Q uh the logic further orchestration while Q takes care of finding the capacity and placing them in the clusters. Having said that like as as we I just said like multiq has been like the solution for us like to kind of like implement this multicluster scheduling but what about API server and what we found about the API server is that we really we've been looking a lot at the community there are a lot of like kind of like uh solutions but for us one of the thing that was very uh important as a requirement is really like provide customer an experience as close as possible to what Kubernetes provide by default just in a multicluster environment.

Um to do so we ended up like kind of implementing our own um multicluster API server uh build on top of what like uh the core Kubernetes API server code essentially like extending the capabilities of the API server that are there by default and kind of like implement uh some more uh things in there in order like to deal with the multicluster uh scenario in particular. One of the challenges when doing so is really about the listen and watch semantic that the API server provide. Our goal was to enable controller talking to a multicluster API pretty much as they can talk to a regular Kubernetes API and being able to establish a watch uh against like object that now are spread across multiple clusters. So hiding again the placement entirely from them. And uh in order to do so like the uh convention the API server what is supposed to do is supposed to return being able to return a list of object with a specific resource version and the resource version is really the aspect that is very important here we're going to talk more about and because the resource version represent like a um point in time reference like in the underlying storage in this case library default ECD of when the data were produced were returned and data change all the time and is important the resource version because then is what later the controller use in order to establish a watch and get a stream of event server side generated about all of the update that happen in the cluster.cd Ccd is the default technology that enable ids in kubernetes.

In particular, the resource version is the version of CcD essentially like that CD keeps internally of the objects and it's coming real from the raft consensus that happens like in order to replicate like kind of information across nodes. However, the resource version is of a specific CD like server. What when we have multiple cluster there are multiple SCDs. So how do we provide like a similar semantic in a multicluster environment? The kind of naive approach we started with is really like hey can we model in the multicluster API server like this resource version as a vector of the resource version of each underlying cluster. uh there is actually like a very nice uh issue upstream which I linked at the bottom of the slide about why this is hard in particular the fact that lot of clients even if in theory like the API server version should that the resource version is something we don't expect client to parse and make assumption on the reality is that because it's been forever like a int 64 number many client and even in the kubernetes core code have been pass par pass parsing this in order to implement like specific behavior so the challenge was like that by uh implementing like this new resource version concept as a vector.

The problem is that we would have been breaking some of the client assumption around like this specific field and the other problem that we found is that again like coming to the flexibility we want to have as like adding removing like a cluster from like the multicluster API then it becomes very complicated to model this semantic because it's hard like to have a total ordering of event when like you have members of this vector that goes come and goes. Um we essentially move on from this approach and then we started looking into another very naive approach and what if we essentially kind of like were to have a controller like that everything that is doing is essentially watching for object across the various cluster and creating this object in the multicluster API. Imagine like you you have the multicluster that creates like a deployment. you have deployment spread across cluster and then you have like this controller that watch across multiple cluster and create the pod uh a sync back the pod into the multicluster API server um and and CD that again like while this approach actually works like what we found is that this approach had like some fundamental limitation in term of scalability because then essentially what happens is that we have now a giganting sedd that needs to store all of the object of all of the clusters and becomes like a very complicated to scale especially because one of like the thing that we were looking for is also like how can we make this layer as much thin as possible in order like to provide like reliability to be reliable and provide us like the ability to scale beyond what a single cluster can do.

Um eventually what we ended up with we ended up with this idea of kind of reclocking events. And again like if you kind of zoom out from this problem the real thing that we want to do is and the property we want to satisfy is have a monotonically increasing like version number which is true across all of the clusters. So instead of like syncing back all of the object to the multicluster layer, what we are essentially focused on doing was um focusing on like just providing this feature through a CD. So like provide us like with a monotonic increase like resource version number across like different clusters without necessarily having to sync all of the object and you will see what this means. We do have still like some scalability concern about this in particular about the rate of events is something that we are looking into like exploring how we can build on top of this sharding and in particular the idea that we have is potentially combining layering layers of this in order to enable us to to scale uh more.

But what we found out with this approach via our testing is essentially that because like these um uh object representation is very tiny. We can actually have now options to either like offload it outside of XCD using a different storage or keep using XCD and only like kind of because it's very compact like the only domain for scalability is essentially the rate of event and is not anymore the storage which would be been the problem with the previous solution where we copy all of the objects. The fundamental structure that we have essentially to implement this logic is what we call this version marker object as I said and this virtual marker object is essentially consisting on three metadata. The store which is the reference to the cluster where the object is coming from. The object which is like a reference to the metadata in particular the resource version of the uh uh object in the originating cluster and then the template which encodes in there like the metadata in particular the new reserve version resource version which is the unique across all of the cluster like in point in time in which this event has been observed.

that is what we use essentially to imprevent to implement like the list and watch semantic at the multicluster layer. Um you you don't see like in here in these specs like uh only the resource version field we have like this template or object references because some uh um optimization we found like to scale out is by having also like support to label selector at the multicluster layer and that's why there are a bit like more fields into that but at the at the high level you can imagine that these are essentially kind of like just resource version metadata. So how do things work into this uh multicluster work? You have a client that talks to multicluster API and all of like their get and list requests essentially it like this watch cache which is essentially implementing the storage interface of the API server normally is implemented by CD now we're implementing it essentially by this virtual marker store and then what we have we have a request routing layer that essentially takes care of like deciding like translating the uh representation in term of resource version at the multicluster layer to the representation in the individual cluster of the object and that's essentially what we are using in order to reimplement the storage interface with this and provide like our clients with the ability to offer consistent reads at the multicluster layer of the object without eating the underlying clusters.

It's going to be more clear in the demo hopefully. Last thing before we get into the demo is essentially like how can we handle the other set of requests like that we need to do for these objects in particular patch update delete. Uh the idea here is essentially being like yeah this kind of request we are just going to be proxying them through. So again we're going to eat the version marker store we're going to find the store where this object exist replace the resource version that we have observed at the multicluster layer with the actual one in the underlying cluster and forward the request back and this is how again we are enabling like multi request. Uh on top of this we are also supporting logs and sec exact via essentially like the same methodology. Therefore like our user can do whatever they are used to do in a individual cluster with their pod object just at the multicluster level and I'm talking about pod you will see this is beyond pod this is something we can extend like to any kind of resource type that we want to h abstract away from the uh multicluster layer.

So into the demo um let me let me just go through what I have here. So what I have here I have two kind cluster running locally on my uh machine and I am running like locally uh hopefully still up these multicluster API server alongside CD and multiq. Now what uh the first thing that I want to show you is um the kind of abstraction that we have at the multicluster layer to model like the underlying clusters and also model like this uh define the object that we're going to be sending through this version marker like uh storage API aggregation essentially is the API that we use to define which object are going to be we are going to be aggregating up at the multicluster level from the individual cluster and API endpoints is essentially the API that we use to register with the multicluster layer. What are the underlying cluster that we want to sync essentially like version resource version into version marker at the multicluster layer. Um if we look at uh what this means like how these things translate to multiq what we have we have a controller that essentially generate for each API endpoint a multicluster uh multi multiq cluster configuration which is essentially the API that multiq is using in order to do like register the cluster and being able like to do multicluster uh essentially like dispatching of the workload across the clusters.

Now I'm going to do um I'm going to show you like what the virtual marker are and as you can see version marker today is a cluster scope resource and what we have here we have essentially like a series of object and if you look at the column they tell us essentially what is the cluster where the object exists what is the resource version that exists at the multicluster layer of this object and what is the version that exists the resource version of this object in the individual cluster so store RV uh a reference like to the uh actual um cluster where the object is present. Uh we you see here we have two kind of type of virtual marker. We have bookmarks which we use in order like to keep continuously like tracking the version of the storage uh of the underlying cluster. So even if we don't receive any pod event because like in my demo I've not done anything like for 30 minutes like we still keep and been able to resume efficiently watches like when like the um yeah when when there are no events or no changes for this specific store and for the rest we also have like again a mapping for every pond object which is essentially like in this case what is configured for us to sync.

So if I were to create here now a job and a deployment uh what I can do I can do get pods and what you can see here is that I'm going to be able to see the pods and if I do get pods minus o wide uh you will see that these pods have been assigned to different cluster you see like I'm using kind so it's cluster name node name so you see this the this through the same multicluster API thanks to multiq essentially these object have been like assigned to different nodes Um I can I can watch pods um again and hopefully like yeah the job will terminate and uh and you will see that it's going to be here we are like a sequence of events that again like we have been able like to get because the pod object terminated in the individual cluster in this case local a we receive an event at the multicluster layer we register this event and then we've been able like to stream it back to the client and the multicluster and uh Uh let me just get a thing and if we want I can do logs. I can get logs from my pod.

I can exact into my pod and pretty much like uh same way that I can essentially like in a regular Kubernetes cluster. All of these interacting to the multicluster API. All right. So this is pretty much where we are. So we have wired this multicluster logic into the API server and now we're able like to abstract like the placement of the object uh across different cluster aggregating them at the multicluster level. This is not a dumb project. There is a lot of work in this space. A lot of work is being driven by sig multicluster. A lot of work is also like things that uh is important for us like as a platform to provide to our user. A lot of the things are also things that we are not working in sig multicluster and we would like like to kind of start the conversation. In particular, we would like to provide like a way to sh workloads across cluster. Right now, like each workload type gets dispatched on individual cluster. If you have very large batch jobs, you might want to chunk them and kind of distribute them across underlying cluster to increase the ability to kind of fit them in the cluster.

H as we do this networking and multicluster services will become a feature that we need to provide and it's something that we've been looking at. Uh we run a platform that has a mix of batch and stateful workload. One of the area we want to work on is also like a multicluster attach the touch of like uh volumes. And that's pretty much it. We have three minutes for questions. I also want to say that we are writing. So if anyone is interested to this like please come come to me. Perfect. Uh, can you get to the microphone? >> So, it's going to be recorded. Thank you. >> Uh, thanks for the great talks. Um, uh, in your demo, it just shows the pot resources. >> Does it support other custom resources as well? >> Yes. So um I I show it earlier like the key of it is essentially this uh API aggregation object. So you can create a I'll just show you what it is actually. Um the API aggregation object is essentially made this way. It's telling you what is like the kind of the object and the scope of the object and the cluster from which you want to kind of aggregate these resources and this is what drive the thing and the idea here is that the workload are defined at the multicluster level.

So the deployment is stored in at city at the multicluster level but then the part object are actually like sync up and and that's essentially the idea here. >> So that means that you can we can use the other resources. >> Yeah, you can imagine like an example if you have like service accounts and for whatever reason you want to aggregate up service account is something you could do probably not the best one but there are many kind of use cases for that. Imagine in your model you want to push down deployment for whatever reason and just aggregate them up. You can kind of do it with both core types and CRD API types >> and then that that aggregated data will be saved in the as separated storage. >> Exactly. So what you will see here you will see um in the virtual marker the virtual marker are prefixed by essentially API aggregation name. So you will see the virtual marker here. You can also store this and this is one element for scalability that is important. you can store them in different CD pretty much as you do with events, right?

Like and we have a way to route them uh as well. >> Thank you. >> Thanks for the nice demo. Um where do we have a choice to pick which cluster, what configurations it goes, how does it pick the clusters? >> Yeah, so multiq right now is uh multiq provide already like aistic for doing that. Like you can in multiq by default decide that you're going to be sequentially trying in each cluster to place it. What happens is the multiq uses the workload. Let me just uh show you. So the workload as a abstraction object type as an abstraction of the specific application and uh and what happens is essentially then multiq is a multicluster so takes care of the placement. There are actually multiq abilities to extend this like having providing your own placement logic is something that the community is working on like on on providing the flexibility. Some use cases we've been found we've been thinking about we would like to taint a cluster so that no new workload gets sent there because maybe it's a cluster we want to we want to do maintenance.

The idea is that multiq has some abstraction. The community is also very eager like to kind of like figure out these use cases but that's the layer where the the feature gets implemented. So how are you propagating the reconciliation at the destination cluster to the multicluster? >> This is again something that multiq does like today. The way that it does it is essentially is syncing right pushing down the spec and syncing up the status. That's the way the reconciliation works so that you can always have you have two copy of the object but the spec is what you push down and the status is what you aggregate up. So your user virtually have one view of consistent view of the resources. Again, we found because we have like some weird internal APIs that are not really respecting the idiomatic way that we need like some custom logic. And this is something we've been talking with the community on kind of like how to allow to extend this sync logic also for these use cases that are not like super nice but are just API that exists and we need to be able to support.

>> Yeah, this is great from the command line perspective. Do we have any kind of uh similar functionalities where we can see the because the Kubernetes dashboard supports only one cluster UI do >> but the idea the idea here is that like again this you can deploy the Kubernetes dashboard here and it's just going to work like you can use KN&NS KN&NS is going to be just work because this is a Kubernetes API from the point of view of the multicluster API it's like you have one cluster and then again like right now we use node names for like providing the cluster information you can think about like doing whatever you want like potentially labels. We've been talking about this thing but the real thing here the common line is just an example of a client. Cubectl doesn't know that there are many clusters here. It's just like one config and the same way like the the dashboard can work. >> Thank you. All right. Awesome. Oh, brilliant. Go for it. Sorry, one last question. >> Sure. >> Uh, so do you mind explaining like where the components are running like um in the target cluster what part of multiq runs and and what is the API workload or whatever you were describing before where is that running >> right?

So um multiQ is made by two part like you you have so Q is Q Q manager controller manager is the piece and you run two copy of them one copy which is using a cube config pointing to pointing to the multicluster API server and in each on the underlying cluster you run also like Q like without like multiq enabled and the idea there is that Q uses this because again the medium that is used to get the to do the mission of qua of capacity is the workload objects So you need in the underlying cluster Q in order to understand whether the workload can be admitted meaning that there are resources to satisfy it requirement or not. So these are like the two pieces that exist there. So we run two version essentially of of of Q and just using different cube config to point them to different API servers. >> Okay, that makes sense. Thank you. >> All right, perfect. Awesome. >> All right, >> thank you so much. Next session is at 145.