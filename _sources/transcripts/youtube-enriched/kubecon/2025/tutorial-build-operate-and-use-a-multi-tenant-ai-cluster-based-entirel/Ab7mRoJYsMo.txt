Uh, good afternoon. Uh, welcome to a session. So, my name is Olivier Tardieu. I'm a principal research scientist and manager at IBM Research and today I'm joined with my by my IBM Research colleague, Dave Grove, and and Claudia Missaglia. And we're going to talk about how to build, operate, and use GPU clusters for AI, right? So, uh, by now we've all heard about AI and GenAI. We are all probably excited about it, but we're also sometimes a little bit anxious about it for lots of reasons. And as an organization, maybe the first reason to be anxious about AI is to actually procure GPU to do AI, right? It's both time-consuming, expensive, slow, you know, whether we rent them from a cloud provider, whether we buy them, whether we lease them, whether we want a dozen of them or thousand GPUs, it's a big problem. But someday we'll get them. And and then the second panic arises, right? It's how can we take these GPUs, this pool of resources, and somehow share it across all the teams, all the users we have in our organization, all the projects that need access to these resources, so that we can manage them and we can maximize utilization and we can maximize the return on investment.

So, this is something we've been working on on IBM Research on our own cluster for several years now and we've essentially built a point of view, uh, incrementally refined uh, methodology, uh, a setup, and we're going to try to share this with you today uh, with the hope that we can help you with your own, uh, you know, AI journey and also that you will tell us everything we could do better. So, let's get started. So, um, in uh, what we're going to show you today is entirely open-source. Uh, everything is available online including this tutorial. So, if you go to this QR code and this URL, you'll find uh, all the comments, all the scripts, all the uh, YAMLs that we're going to use today and you can you conceivably follow along and and or, you know, replay that at home, right? So, uh, because we're going to talk about GPUs and I don't typically travel with a 2,000 GPUs in my backpack, uh, this is all going to be recorded demos, but uh, you know, on a real GPU cluster. So, I'm going to start by a brief background on AI/ML workload, on the kind of hardware we target, on what exactly are the key components and maybe the key goals of the platform we've been developing at IBM Research, and then we'll get into the the the the the deep dive of the tutorial, how do we set up a cluster, how do we onboard users, how do we do things like monitoring, fault tolerance, and also what kind of workload templates we give our new users to get them started on these kind of of clusters.

So, um, let's start with AI workloads. Uh, again, it's probably not worth spending too much time on this because you've probably heard a lot about that before. There's a essentially a an entire zoo of workload, a collection of things we want to do all the way from having the idea of a GenAI application to actually running this application production, right? And what we're interested in discussing today is this piece of the pipeline. Again, it's more than a pipeline, all kinds of feedback loops, bypass, and so on, that are uh, dependent on accelerators such as GPUs and that are typically batch workloads, right? It's not about running the application at the end of the day. You have lots of other talks at KubeCon about running large language model, running agentic frameworks, and so on. We're not going really to get into that today because, you know, also in sense it's a scope, really there's a lot of optimization and special things you want to do for that. What we're going to focus on is everything that happens before that, right?

Everything starting from things like data preparation, even, you know, like when we do hate and profanity filtering for instance on on our data before we train models, we actually use already machine learning models to do that, so we need GPUs, we need a lot of resources, you know, training models, fine-tuning models, running batch inference for instance for validation or other kinds of workloads. This is the scope of what we're trying to run on these clusters, right? Other than that, uh, what's also interesting about AI/ML workloads is they are very intensive, so they can use lots and lots of GPUs at once and run for a very long time. And finally, um, they depend a lot on AI/ML frameworks like for instance PyTorch and having good support for that out of the box is really critical for enabling users to do anything with these clusters. So, from these requirements, you can see on the other side of the slide a number of of or from these properties, you can see a number of requirements arising such as how we're going to queue workloads, how we're going to prioritize them, manage quotas between teams, how do we deal with large workloads in particular with these uh, kind of hardware, I think everybody in the industry has been reporting that GPU nodes are more complex than traditional nodes, therefore they have more maybe creative ways ways to fail.

We have longer running workloads, how do we do fault detection and all of these things? So, uh, in terms of clusters, uh, what we typically run at IBM Research is uh, large clusters with uh, NVIDIA GPUs like the 100 or H100. So, here you have two kind of node generations we've used or we're using uh, that we've published uh, you know, detailed specs for. So, on the left, the more like the first generation A100 nodes, uh, on the right you have something more recent which is an H100 node. And, you know, the key characteristics of these nodes is they have a high density of GPUs, right? So, they have eight GPUs in both of these pictures and then lots of compute, you know, CPUs, memory, local storage, uh, high performance network, and so on. So, that's the kind of things we want to run with. So, and finally, want to briefly tell you a little bit about what we're going to see in the rest of the tutorial. What is the kind of platform we're trying to build on? And because I think we're all engineers here, rather than starting with abstract goals, let's be very concrete and start by looking a little bit at the components, right?

This is the kind of things we put together in this platform and we're going to review one step at a time, right? Of course, in the middle you have Kubernetes. We start there. And it's not just because it's KubeCon. I think yesterday in Cloud AI Day, we had somebody bold enough to ask the question, why Kubernetes? And I think for us the answer is really simple. It's because of the vitality of the community and the, you know, the the the the CNCF landscape, right? How many projects, how many things we can leverage from the community, contribute to, uh, the rate of innovation that makes if any, you know, is second to none, right? Any other choice would require a lot more work to get to a platform. And so, that means not only we have Kubernetes, we have projects in the Kubernetes community like Q, but, you know, if you take something like PyTorch, which has nothing to do with Kubernetes in the first place, we do get things like QFlow trainer that the QFlow community has developed so that, you know, we can uh, easily uh, run PyTorch on Kubernetes.

We also have the on the right-hand side some of the new projects we as IBM Research have introduced where we found that the maybe the the community landscape was still lacking some some critical capabilities for what we're trying to do and we'll get to that in a few minutes. So, what is, you know, the goal of our stack again at the high level? It's it's to manage workloads and resources on Kubernetes cluster for AI. Uh, it is has to be open-source. You know, the components, the upstream components, the new components, the combination, the composition of these components, everything. I'll repeat that multiple times through the talk. It has to integrate key AI/ML framework. It is about multi-users, right? Within an organization, how do you manage having many users without conflict popping up every every day between them? It is about productive utilization because at the end of the day we're paying a lot of money for these GPUs. They have to be running all the time doing something useful all the time.

And and we insist on fault tolerance and of course, uh, to the side you have monitoring, observability, and so on that always are critical uh, piece of such an infrastructure. So, uh, before I move on as well, again, I said open-source. So, everything we're doing today is open-source. Everything is meant to run on vanilla Kubernetes, but obviously IBM Red Hat work together also provided supported versions of this kind of technology. So, in this space, what we're doing is working on OpenShift AI. So, if you want uh, a supported solution that essentially overlaps what we're describing today, this is uh, you'll find this in OpenShift AI. OpenShift AI is a does more things than what we're discussing today, for instance, looking at how to run applications at the end of the day. ML matches more thing because it's a research platform and we're trying to figure out the next thing and the next thing and the next thing we want to add to OpenShift AI, but again, for the rest of the talk, you can forget about that.

I should also say again to be clear that we're going to run our demo on an OpenShift cluster because this is the Kubernetes flavor we prefer in IBM, but there's nothing that is really dependent on that. So, um, here's uh, let let let's start from the top. Here's uh, to give you a sense of the cluster we're going to run this uh, tutorial on. It's it's it's a small version of what we run in practice. It is a cluster with six nodes. Three of them are control plane nodes and three of thems are worker nodes and, you know, the the critical bit here is that this piece of information is that these worker nodes have a number of Nvidia GPUs, you know, three times eight GPU, 24 GPUs and V link. And what, you know, I had pictured a little bit earlier. This cluster is kind of in between the two generations I was showing you before. So, before we start, you know, right now we have a base cluster, nothing on it. We need two prerequisites essentially. We need to fulfill two prerequisites because before we move on to actually installing ML batch on this cluster.

The first one is we have to teach the cluster how to manage GPUs. As you probably know, Kubernetes out of the box understands CPU, understands memory, ephemeral storage, you know, has a certain number of resources built in into the scheduler, but not GPUs. In order to do GPUs, Nvidia GPUs in this case, you need to install Nvidia GPU operator, you know, was jointly developed by Red Hat and Nvidia and that will add an extended resource type to your cluster, Nvidia.com/GPU and that means now you can start having workloads with GPUs. So, we've done this beforehand, you know, it's OpenShift, so we click on the boot button on the console, it gets deployed. But if you have a vanilla Kubernetes cluster, Nvidia has a lot of documentation online how you can go and deploy a Helm chart on your cluster and have all these set up for you. So, we're not going to review that. Second, we're doing AI. AI depends a lot on data, therefore storage. So, we need some kind of storage solution for our cluster. So, in practice, we want to have high performance storage solutions.

For instance, IBM we use IBM Spectrum Scale, but for the purpose of this demo and what I'm including here is that we can, let's say, just assume that we have storage available through an NSF server on a local subnet and then, you know, just following adding an armor repository and installing a Helm chart, we can make these available on the cluster as a as a as a storage class and from that we can create persistent volume claims and access storage and and so on. So, now we move on to the cluster setup itself. All right. And uh there are two parts to it. First, when we set up a cluster, there's what we do once, you know, what what does the admin do when we create the cluster and, you know, normally never has to worry again about, right? And then there's a second step which will be the team and user setup. How do we onboard teams? How do we create user? How do we give them the right permissions? So, I'm going to review these two things. And to start with the cluster setup, it's it's it's pretty straight forward.

You can do this by cloning the repository where we have all of our supporting again tools or, you know, scripts and so on. We set up priority classes on our cluster. In practice, we've seen that having low, medium, and high priority AI workloads is is enough for what we need. We deploy the key components. Some of them are scheduler plugins, some of them are operators. We're going to actually talk about each one of them in in sequence in the next hour or so. And and so I'm not going to describe them more here. As I said, we can deploy them through OpenShift AI or without. We configure queue, which is our queuing system. We'll talk about that. And we finally have to create a role that is the role we're going to give users to let give them permissions to do, I mean, to run workloads, monitor workloads, debug workloads on this cluster. So, that's the cluster setup. And that's that's pretty much it, right? Then we have the team setup. And again, that's that's pretty boilerplate, right? In our experience, it's makes a lot of sense to associate the team in a namespace.

So, when we want a new team on the cluster, we create a new namespace and then we have to only do three other things. We have to set up a queue for that team, which is where the jobs submitted by that that team will be queued. We have to make this queue the local the default for that particular team, which is the local queue definition on the left. And finally, here we're creating user Alice in team blue. We have to give Alice this this our back this role that I was describing earlier in the namespace blue so that now Alice is officially a member of team blue and can use the capabilities of the cluster in that team. What I forget, maybe, you know, we need to label the namespace so that we say ML batch is managing the namespace. And really, the only thing that is not entirely boilerplate on this slide is what I've highlighted on the right. It's defining how much quota we're giving to this team, right? So, here we're saying, well, team blue should have nominally access to eight GPU on that cluster.

So, I'm saying nominal here uh and we'll get back to that, but essentially, you know, they they will go into great details into quotas. That doesn't mean that team blue is limited to eight GPUs. If nobody else is running on the cluster, they could access the entire cluster, but that means they essentially have guaranteed access to eight GPUs on the cluster. So, I have a video going through that that you can also find on YouTube. I'm going to go through. So, this is an 8-minute video on YouTube because I tried to describe what's going on as I'm running this, but really fundamentally, this is just a minute to do this setup and this is pretty boring. So, I'm going to go quickly. Again, you have the tutorial scripts I've described, you know, I gave a pointer to earlier. And what we do in this in this setup is we go and we copy paste instructions from the left to the terminal on the right. So, first what I'm showing on the right is what I was showing before on the slide that we have this cluster with uh 24 GPUs available.

So, if I for instance connect to one of the nodes, I can see that there one of the worker nodes, I can see I have eight GPUs available here. Uh second, I can check that the GPU operator is already installed on the cluster. So, we can see the operator GPU operator is only already installed, which means if I query the capacity of the nodes of a worker node, I can see here that I have eight GPUs available as the capacity of that particular node. And then I can set up my storage class, which I was again showing earlier. Again, it's as simple as copy pasting the setup. So, this particular case and we're doing something simple, a simple storage class, you know, the way we you would set up storage will depend quite a lot about the kind of storage solution you adopt for your cluster. Also, what's maybe oversimplified here where I have no authentication of any kind. We're just assuming the the storage server is on the same local network and that's where the security is happening, right? Nothing, no credentials and so on.

So, a real setup will have a little bit more than there. And then we move on to the ML batch setup itself. And, you know, this is just running a a long script that has all the components of of ML batch I was describing earlier or will describe later actually. So, it has queue, queue gray, queue flow, and so on. That's that's all it takes to set up the cluster. The last thing I didn't mention in the slides is we're also going to reserve some capacity in the cluster as an admin queue for admin to do maintenance operation. And again, this is something we'll explain in details in 2 minutes. In that particular setup, I'm also reserving eight GPUs for this admin queue. That's it for the cluster setup. Then you have the team setup. Here for the purpose of the demo, we're going to actually set up two teams. Team blue, actually I went too fast. We we we set up team blue with user Alice as we've shown in the slide. We're also setting up team red with user Bob, which is again the exact same thing with the two names replaced.

I think that's it for the video. So, we can close this. That's it for the cluster setup. Before I pass on to Dave, I want to describe the very first component of the setup very briefly. Happy to answer questions maybe after the tutorial if you care about that particular piece. One of the components of our ML batch setup is to actually deploy and configure scheduler plugins for a number of reasons. Essentially, this is about trying to avoid fragmentation in the cluster. So, that's the purpose of the node resource fit scheduler plugin. What it says is if I have, let's say, a workload that requires one GPU, I should try to fit it in a small hole in my cluster, right? If I have a node that has already seven GPU utilized and one left, I'd better put it there than put it on a on a node that is entirely empty because if later I want to run an eight GPU job of my cluster, then, you know, I'm no longer going to be possible if I fragmented my cluster. So, we use scheduler plugin to try to avoid fragmentation.

We use scheduler plugin to do gang scheduling. The idea there is if you have a large distributed AI/ML workload that needs 22 256 pods to run at the same time, we want to make sure we can fit the 256 pods before we actually deploy any of those pods because deploying 250 pods is completely useless for most AI workloads. So, we use gang scheduling for that. Finally, we have an experimental plugin we've also developed at IBM Research to do topology aware scheduling. This is this is something that is coming in the community as becoming important. Queue is working on that. A number of people are working on that. The idea there is to if you have a distributed workloads, we're trying to make sure we distribute it as as few racks as possible to maximize the bandwidth mine minimize the latency of communication between the different components. Dave. Thank you. Very good. Thanks. So now what we're going to do is we're going to dive into some detail into some of the components in this stack starting with how we do queuing and quota management.

Um and for ML batch for queuing and quota management Um why do we want quotas? So quotas we want to set up these large shared clusters so we can make more efficient uses of resources. We can have large clusters with a large number of GPUs on them. We can share them across a bunch of teams and that lets us both share the resource pool so people can have a little bit more fun in the resource pool and we share the control plane and we also share the maintenance the admins it takes to run the run these things set them up. It's easier to set up a small number of large clusters than a large number of small clusters. Um but native Kubernetes quotas by themselves are not enough. Right? So quotas first of all quotas are are done on on the level of pods. Quotas aren't aware of workloads. They aren't aware that you're actually running something that contains a lot of pods. And so the quota needs to be done on a sort of workload granularity not the individual uh fine-grained resource. Secondly, quotas are are are fairly inflexible.

So this picture on the right I have I want to take take my cluster and divide it between two teams. I can either divide it exclusively. A and B get pieces of it and then there's no you know if if A isn't working and B is in another time zone and they're asleep no one's using B's quota. Or I can overlap them and I can have some reserved for A some reserved for B and some stuff in the middle that either one can use. Um but then there's a you know there's sort of no fungible no fair borrowing no fungible quotas and really no tradeoffs that way. And so this quota management is one thing that the Kueue project really does a much better job of. Um so Kueue is a Kubernetes native system to manage queuing quotas and jobs. Um and in our in ML batch Kueue is entirely in charge of for users figuring out when their when their jobs they should be admitted to wait uh when they should when should they be admitted to when do we actually go off and create the pods and resources and start these start running these things when they get quota assigned to them and when things should be preempted.

So when something's already running but something more important comes in or something from another team comes in and needs the quota back. Um so should that job be suspended or or so on. Um so Kueue has a a number of features. Uh for us some of the key features are that it has workload aware quotas. So the quotas are done on the level of the top level jobs not on the individual pods and Kueue knows how to admit that way. Um it it has a mechanism for fair sharing for uh queuing with priorities for preempting jobs and so on. Um and it's also externally extensible that allows people to customize what's going on. And has built-in support for popular job settings. And so what built-in support means is the way Kueue works is each of the controllers for those resource types had had a small extension made to it. It understands a suspension protocol. So when the resources are first created they're suspended and then when Kueue wants to admit them it flips a bit on the on the on the spec saying okay your suspend bit is now false you can go ahead and and run.

And that all the controllers are extended to do that. Um So there's the project there and we have actually Kueue has a booth over in the project pavilion so uh come over there if you want to learn more about Kueue in a lot more detail than here. I'm going to talk a little bit more about how Kueue works in particular to give you a sense of it. So the key body thing is Kueue is that green box in the middle and what Kueue is going to do from a user perspective is interpose on the creation of resources. So the user wants to submit a job um they just you you do the usual cube cuddle apply or create the resource gets created then Kueue has webhooks that interpose on the creation and look at it decide whether based on the namespace and other characteristics of it is that something that Kueue is supposed to be managing. If so it suspends it. It's a modifies the spec before the thing gets created to set suspend to true and that causes the controller to then look at it and say oh this is this is inactive.

I'm not supposed to do anything with this yet for the resource. And so it sits there until Kueue decides that it's time for that thing to run. It has quota. And that sort of admission protocol can involve looking at external resource checks that's the the purple box over on the far side. It can also interact with the cluster auto scaler to ask it is there capacity oh there isn't capacity can you create some capacity for me let me know when it's available. And finally when all that happens then Kueue can decide to admit it at which point the suspend bit is changed from true to false and the underlying controller whether it's the job controller or the PyTorch the training operator whoever's actually really in charge of that resource now sees oh the suspend bit is false. Now I'm supposed to do my normal job and everything just sort of continues as normally the the the controller for that will go off and create the resources. It'll eventually create pods. The pods will be submitted to the Kubernetes scheduler.

They'll be run they'll be run and so on. All right. So the key here is having that interposition mechanism in the middle where you can suspend something and and cause it to wait for its turn. Um And one of the key abstractions in Kueue that we're going to talk about is these is the notion of a cluster queue. So Louis talked about this for a second or a little bit already. So the cluster queue is what is assigned quota. So one of the things the cluster admin does for each one of our teams is he creates a cluster queue for them and the the YAML on the right gives you a sense of what that might look like. In particular the resources that the quotas that's assigned to that team is shown in the cluster queue. Um and there's also some additional detail about there about what are the preemption policies and how do these cluster queues work together. In particular Kueue lets you put cluster queues together into a cohort and these cohorts the cluster queues within a cohort can then coordinate to borrow quota from each other.

So if if there's quota available in cluster queue A and cluster queue B needs it and they're in the same cohort it can actually borrow that quota from it and then the quota can be taken back when it's needed by by the other cluster queue. So it's a way to sort of have a more flexible quota system where these things are grouped together. Um and we don't use it in ML batch but Kueue more recent versions of Kueue actually support hierarchical quotas. You can hierarchical sorry hierarchical cohorts. So you can sort of put these things into a pyramid together of sharing and preemption um and get that all to work together. So your your cluster queue quota cluster queue structure could match your organizational structure where you have teams that are associated with a particular division with a particular vice president they all have their quotas and they're assigned around. And so they can negotiate among themselves how they want their quota assigned and so on. So that's the notion of a cluster queue.

Um A second thing one of the projects we built within IBM Research is this thing called app wrapper and this came out of our experience of running these more complex workloads. And app wrappers really do two things for us. Um so one of the things they do is they allow us to group multiple resources that are logically belong together into a single workload that we can then hand to Kueue to manage. Um so app wrapper implements the Kueue suspension protocol sort of our our life cycle diagram on on the right there. Um so you know they start out suspended if Kueue is supposed to be managed when Kueue admits them we realize that uh the controller then goes through an internal life cycle which we're going to talk about a little bit later to actually create the resources. Um and if it gets preempted it'll be suspended and take those resources back. Um So it lets us group resources together so it can we can put any number several computer resources together anything that's defined using a pod spec template.

Um that includes all the built-in kinds that come with Kueue that Kueue has built-in integration for can be put inside an app wrapper but also additional kinds that Kueue doesn't know about yet but have are defined in terms of pod spec templates app wrapper gives you an easy onboarding path to get those to run in Kueue. And it also lets it put auxiliary resources in there. So sometimes people people's workloads require services and secrets and ingresses and other things associated with them. We would like those to be all sort of uh logically managed as a single unit deployed on the cluster when the cluster is admitted when the workload is admitted by Kueue have all those things created and when the workload is done have them cleanly cleaned up so we don't have lingering services and ingresses kicking around be some someone forgot them. Um the second thing that app wrappers do for us which we're going to talk about in about 15 minutes is they harden workloads. So app wrappers are a key part of our fault recovery process and they allow us to monitor the workload health have a way to reconfigure our retry and our failure policy and make sure that all the resources are cleaned up and and and done like that.

So I'm going to have a demo of that later. Um what I'm going to do now is switch to this demo which is talking a little bit about demonstrating some of the quota and preemption properties of the system. All right. So we're going to start off um for this particular demo we're just going to be driving a sort of synthetic workloads as they're all sort of variations of this YAML that's shown over here on the left. Um and it's so these are just sleep jobs. They they start up they create two pods they request some GPUs. You know the the the four kinds of jobs we're going to be running the the characteristics are shown in the table down here and we're going to use these to illustrate some of the characteristics you know some of the properties of Kueue its its queuing its preemption and how quota gets shared across teams. Um up on the right this thing over here is called Kueue Viz. This is a new visualization tool that's being developed as part of the Kueue project. Um and it gives us an a way to sort of look at the state of the system.

So in particular what it's showing here is the cluster queues for Kueue. So these are the two we have three queues the red team queue the blue team queue and the slack cluster queue. So now we're going to go ahead and start. So Alice comes into work and Alice is going to create a burst of short-running jobs. So each of these jobs is going to run for 30 seconds. Uh they're each going to want create two pods each of those wants four GPUs so she's submitted uh 32 GPUs of work to a cluster that has uh 24 GPUs and so some of them are going to have to queue up. My math isn't very good. 6 * 8 well she's done more than that. But so she's she's created seven jobs. Um so 7 * 8 is 56 uh but only three of them are running. So she's got three jobs that have been admitted. They're using the 24 GPUs on the cluster and the other ones are waiting cuz there isn't enough resources for them so they're just queued up. The pods aren't even created yet. Um and you can notice here that so Alice actually has is using the whole cluster cuz the the cluster's healthy we don't need the slack capacity and the red team hasn't shown up to work yet.

So Alice even though her nominal quota is eight she can run uh 24 GPUs. And we can look in Kueue Viz you can drill down into the local queues. So here's the view of uh the red team and the blue team local queue. This is their mechanism in their namespace for submitting stuff to that cluster queue. Um and we can see that she has those three workloads and Kueue Viz lets you drill down into the workloads as well. So you can sort of drill down and see what each one of those workloads is doing what its state is and so on. All right. So, now some of her short-running jobs have finished and so some additional ones have been have been admitted. Now she only has one pending job. Um but unfortunately for Alice, uh you know, Bob on the red team has just shown up to work. So, she has some Oh, sorry. No, that's what we're doing. All right. So, now what we're going to do is we're going to So, we can talk about queuing and preemption. We're we're going to submit some long-running jobs. So, now Alice has now submitted uh four jobs that each are going to run for 10 minutes.

All right. And so, as the short-running jobs finish off, the long-running jobs are going to start running. All right. So, here we are. And if we look at the pods, we're going to look and see uh First of all, we can see that I can't type. Nope, there we go. All right. So, we can see that the the pods that Alice has in the namespace, some are running, some are completed. Um so, we can see a mix of the short-running jobs which are mostly done and then normal jobs which are starting to to go. And we can see that those are there. All right. So, now I think it's time for uh All right. So, Alice has created a bunch of uh sort of normal priority jobs. Um but then and she's queued up and some of them are waiting. But then she realized she had something really important to do first. So, Alice can use priorities to have one of her workloads jump in front of the queue. So, she just created an important job um and that was, you know, created after all the other ones. But because its priority was higher, queue has already gone in and suspended one of her normal priority jobs to make room for her important job.

So, Alice is Alice and her team are able to be managed to use priorities to organize their own work and to make sure the things they really want to do get done first. And we can see on the on the list there that there are two suspended um normal jobs, one of which is the one that hadn't had a chance to go yet and the other one is the one that was running and then was suspended to make room for the important job. Okay. All right. So, now I think it's finally Bob time for Bob to come into work. Um so, Alice has been using all of the GPUs in the in the in the cluster including eight that are reserved for the red team. But now the red team is here and Bob submitted 16 GPUs worth of work. Um and he'd like to go. And and since the quota belongs to him, one of Alice's jobs that's running using the borrow quota has been suspended and Bob is now running. So, the you know, with the borrowing and and quota preemption system is flexible enough to allow the teams to get the quota that they're assigned to. But when they're not using it, other teams can use it.

All right. So, that's the end of that demo. And I think now we're going to switch to All right. So, now I'm going to hand over to Claudia to talk about fault detection and observability. And hopefully I'll know how to use this. All right. So, we're going to switch a little bit context now. Still in the same area of how do we run uh workloads efficiently and effectively on the cluster. Um but we want not to focus a little bit more on the reasons why we really care about fault tolerance and advanced fault tolerance in this case. So, ideally in the perfect world, we don't need any human intervention whatsoever in a cluster. So, people just go Alice and Bob submit all their job, the cluster is always fine, no GPU breaks, everything is fine. That's of course not how uh things work. Um and therefore to have um to have the cluster to being used as much as possible and have uh reduce the human intervention as much as possible, we need to focus on two things. One is the infrastructure itself. The other part is making sure that the workloads are running.

For the workloads part, we're seeing all the all these um uh the queue management, um fair sharing, so all the good stuff that Dave and Olivier um showed so far. On the other hand, we have the the hardware. So, we have we need to make sure that the GPUs are running correctly. And if they're not, we want to know that. So, even to make sure that all the jobs can run correctly and also the reason why we need to one of the reason why we we need to adjust quotas is because maybe at some point a couple GPUs don't work anymore. Uh so, we need also to adjust the quotas accordingly depending on uh all on the cluster state. And this is not something that's uh new and unexpected. Um I took some screenshots here from um some of the most recent um reports on GPU failures and how they relate to uh AI jobs running on more or less large clusters. Um On the top left side, I think that probably the most famous uh one when AI meta uh released the white paper about the training of the llama 3 head of models. They showed that very nice tables uh showing where how many uh errors have been happening during the the entire uh training period.

And those are they can be software or hardware uh issues. In particular, the hardware ones, they account for 78 I think yes percent of the interruption of the workloads. All those are only hardware. And of this 78% almost 60% is GPU failures. So, you really really need to make sure that you can notice when things happen and hopefully also auto recover the jobs. On the right hand, uh this is maybe like 3 weeks old, 2 weeks old uh technical blog post from Nvidia uh showing the error rates both hardware and software over a 4 months 4 or 5 months period where they've been running uh training jobs on their clusters. And again, a lot of hardware errors. Um On the bottom right and center, um uh I reported also a screenshot from a paper that was just published uh one or two weeks ago um by uh IBM Research and uh University of Illinois Urbana-Champaign uh where that table there is showing how many jobs have been failing because of GPU errors during uh time span of 2 and 1/2 years. And those jobs were more than 4,000.

So, long story short, need to take a look at those GPUs. And not only the GPUs, also uh network. That's another big point of failure. Storage can be also point of failure because you deploy an operator that that's taking care of the uh of the storage of the storage nodes. So, that's also subject to failure. Um So, we've in our system, we rely on two components. Native tooling, everyone knows Prometheus Grafana. Uh for Prometheus is going to provide um node statistics about hardware, CPU, memory, network, uh but also um information about the pods or the workloads that are running, how they're doing, and all of that. Grafana, great tool for visualization. And on the GPU side, um with the GPU operator, they also provide the DCGM exporter, which is creating a lot of metrics that report the status of the GPUs. So, all of that is a great great starting point. Unfortunately, it's not enough. Uh we've been working uh to provide also an extended set of what we call health checks on the GPUs, uh network, and storage.

That's been packaged into this tool called autopilot and that's integrated. So, the results of autopilot health checks are um are used by the app proper to um figure out which nodes are good or not. Let's get into some details now. Um so, why we have this autopilot thing, why we needed to do this. Um so, during the operations and bring up and running all the jobs on the Vela cluster that uh Olivier showed at the beginning, uh we've been seeing uh errors happening or like a training job that would crash entirely or that the the performance would degrade like the token per second and all dependent metrics showing how good the training is going, that would degrade. But if you look at the um at the metrics that are exported regularly by the uh native um operators, you would see that everything is fine. Until there is a real crash, a hardware crash, you don't really see some of the aspects that are degrading the performance of training job. And not not only training jobs, also of course fine-tuning and inference are also impacted.

Um so, we've been seeing where those errors are, what what to look into the GPUs to see why a job is underperforming. And there is nothing new that we are inventing really, it's just a bunch of Nvidia semi commands or DCGMI commands. Um and but those things that we're looking at are not exposed um by the DCGM exporter, for instance. So, we know what we're looking what we're looking for. Um so, the ideal uh the thing we want to do is package all those things and into periodic health checks on GPU network and storage. We run those tests all the time. And we can leverage Prometheus to export export the results that we're getting. And also we label the worker nodes healthy, unhealthy, how unhealthy are they. And those labels are used to uh steer the life cycle of the jobs. Um So, this is pretty much the tool uh like a summary slide. So, we uh we use this to run automatically uh health checks on the cluster. We export data Prometheus and and um uh and labeling nodes and we use this information to have the jobs running effectively.

Um this is a list of the the health checks that we're running GPU um mainly related to GPU also network some benchmarking too. I'm not going to get into the details of that. Um so Okay. So, now we're going to um to see a short demo. We're going to install uh autopilot and the entire um observability stack uh by KubePrometheus stack and the Grafana dashboards and we see how those work. All right. So, let's see if I managed to do this. Okay. Okay, great. All right. So, um here we're installing autopilot. This is a a Helm chart. So, it's pretty straightforward to install. And I'm looking from here because I don't see down there. Um okay. So, it's a Helm chart. We install it and it will run on the GPU nodes. It can also run on non-GPU nodes, but in this case we that's all we have. So, we're running on uh with the GPU setup. So, since we are also creating an NFS client on this one, we can also extend the autopilot configuration and check periodically that also the PVC creation and deletion is still working. So, taking also look at the NFS client.

And we can just add it the uh configuration file uh actually create one uh that we will going to pass to the uh to the Helm chart. This is a plus that is not necessary, but since we have a storage class there that we manage, we want always to take a look at that. So, we just upgrade the Helm chart. Take a look that everything is fine. So, this is going to run a rolling upgrade. So, it's going to take a little bit of time. So, I think I have Yeah. I have sped up a little bit. Okay. So, from the logs you can take you can see briefly what is going on. It has run all the health checks. Everything is fine. So, we it's labeling the node uh with a pass saying that the GPUs are okay. The node overall is okay. Um now we can move on to the monitoring setup. So, we're starting with Prometheus. Uh Prometheus and Grafana they actually come together in the Prometheus community the KubePrometheus stack Helm chart. Uh that's all on the Git repository that everyone you can all find super easily. Links are there. Um so, we just install the the Helm repository the the the Helm chart.

We're running update. And so, this chart will provide Prometheus, Grafana, alert manager, the node exporter, and kube state state metrics. That's all that we need to monitor the cluster. So, it's all there. It's super easy to install. So, in our case since we're running on on OpenShift, we're we need to um change a few things. Uh so, we need to override the the node the node exporter port because that's already in use. Uh and also we need to not deploy the CRDs, but that's something that you may not need to do. So, if you're running on an OpenShift cluster, just remember that that's something that you're going to to need. So, we can just uh create a configuration file for Helm chart. That's usual way of doing things uh as you all know already and we just deploy um the Helm chart. And it will take a little bit, but we're we're speeding up the process. So, on depending on where you're deploying if OpenShift or Kubernetes, you will need to um especially in this case with OpenShift, you will need to add uh the service accounts that are creating by the Helm chart to uh the privileged uh service the service account so um So, not service account.

So, the security context and that will be needed so that the uh all the pods are able to scrape all the the information from the from the nodes. So, we have we have all of that running and the chart will also tell you how to get the um to expose the service the Grafana service to access the uh to access the the web console and will also give you the the default um the way to retrieve that default uh password which can be changed in the Helm chart, but we're not doing that in this case. Um so, in this case we're just exposing uh the with port forward command we're exposing the Grafana dashboard to localhost. Uh so, we're going to access it and default uses user is admin and the password is prometheus operator. All right. And at this point, so this is going to be empty. So, you're not really seeing anything here other than that default uh dashboards that are in Grafana just to showcase the um the the capabilities. Uh but we can uh we can import Grafana dashboards and with that the one that we have created for autopilot that is going to show all the health checks uh that it's running, what is wrong with the cluster if anything is wrong.

And we're also going to deploy the the DCGM um dashboard from Nvidia. And that's a pretty extensive dashboard with that contains a lot of information about the the GPUs. So, we just uh that's super easy to do. Both dashboards are of course on the Grafana um uh web website and you can just copy the the ID. That's all in the in the demo of course um on our Git repository. And you can just copy-paste the ID from the website and yes, that's the Nvidia one. Uh you of course you need to select the uh the source of the information. Might be multiple. Of course, the default is please Prometheus. Um so, I depending on which cluster you are in if it's Kubernetes or OpenShift, you will need to let Prometheus know from where to scrape the data. In the Kubernetes case, you will need to label the service monitor which is an object that you attach to a secondary source of uh of information of metrics. In our case going to be autopilot and the DCGM exporter. Um in OpenShift, you would need to label the name space where those metrics are being produced with the OpenShift monitoring label.

Uh so, it will take for the the GPU will take a a little bit to start scraping the metrics. And and then yeah, it showed up that everything was configured fine. And I think Okay, I didn't destroy it. Okay. Who's next? I'm next. Okay. Okay, great. So, now we've seen how um we we autopilot can detect the faults for us and how it labels the nodes for us. We're going to look at how the next level levels up with the software stack use that information to automate fault recovery. Um So, the we're going to get back to app wrappers first. So, as I said, you know, app wrappers are a mechanism for hardening workloads and ML batch we really push users to wrap pretty much all their workloads inside of app wrappers. Um and you know, there are a lot of reasons why a workload might be unhealthy. Um pods can fail uh and they stay failed for a while longer than a grace period. So, the underlying controller didn't didn't restart them, didn't recover them. Um not enough running or completed pods. So, for some reason pods didn't start.

They aren't running. They didn't finish. Uh again with grace periods. Um autopilot could have labeled a node as evicted meaning there's a severe fault on that node and then the app wrapper controller is looking and it sees there's a pod running on a node that's been labeled evicted and that pod is using a resource in particular GPU that's unhealthy. Um that's an unhealthy workload. We need to do something about it. Um similarly uh the top level kinds like a PyTorch job, Ray job, things like that the job batch job they have statuses and so if one of those goes to a failed status, then that means the workload is failed and we can recover that. And finally the user may go in and delete their top level resource. They may kind of forget there's an app wrapper there and just delete the PyTorch job or delete some secret that was created as part of the job and we interpret that as the user saying there's something wrong with this workload, please get rid of it for me or I forgot about it, but it's it you know, I need to get rid of it.

Um and so the the diving in a little bit more into the picture there. Um the app life the app app wrapper life cycle sort of has three stages when something's admitted. So, the job's admitted and it gets into a resuming state. This is when we're creating the resources. So, we're going off and creating all the resources. We're waiting then for them to get to a to a start running state. Running which is the sort of normal state. Um and then if we had to do a the we kick into this retry state. and in that retry state, what happens is uh sorry, resetting state, and that's where the app wrapper controller will go in and sort of first gently ask the the top-level resources to please delete themselves, and then sort of monitor make sure that actually happens. And after a while, go in and forcibly delete them without a without a without uh giving them a choice to make sure that everything is fully cleaned up before it goes back into the resuming phase and tries again. Um and so this uniform retry loop has a bunch of parameters around grace periods and pauses and how many times you got to go around it.

And we the system is set up so that the cluster admin can sort of set reasonable defaults for these, um and then users can override them with annotations within bounds. So the cluster admin can also say, you know, you can retry, you can do certain things, but you can't make this pause longer than this long because that's against my policy. I want to make sure I get good utilization out of the cluster. Um so that's the app sort of app wrapper piece of it. Um another thing we do is we adapt to available resources. So this picture on the bottom here is showing a cluster where there are actually three teams plus a slack cluster queue. Uh the green nodes represent nodes. So sorry, the the the the squares on top represent nodes. So the green ones are healthy ones, the red ones are unhealthy ones. Um and so we have a mechanism where we reserve the slack cluster queue. And so what we do is we have a reconciler that's watching the status of nodes. It notices when autopilot labels a node as unhealthy, and when that happens, it adjusts uh the what queue calls the uh lending limit on the slack cluster queue so that the available quota to be borrowed by other by other by other teams in the cohort actually matches the the capacity in the cluster.

So at all times, we're sort of dynamically adjusting this slack cluster this slack capacity so that the what we have available to hand out to the jobs actually matches what's healthy. Um we and then and then uh the the this uh gives us the ability both to uh have the thing to to prevent to sorry, this gives us the ability to prevent admitting too many new jobs in the future. Um and we combine this with node anti-affinity. So as jobs get admitted to the cluster, they get steered away from nodes that have been labeled as unhealthy by autopilot. Um it also gives us the ability to reserve the quota to migrate jobs away from unhealthy nodes. So if we're reducing the the the the quota we're handing out, that gives us the room to then uh reset the app wrappers and re- reset those things and have them go back to healthy nodes when they come in. And it also allows us to sort of view maintenance as just another unhealthy node. So if admins need to come in and start doing things, they can cordon the nodes, and the system reacts to that just the same way it does as as if the the app wrapper as if autopilot had labeled something as unhealthy.

It goes ahead and adjusts the quota and sort of start steering new workloads away from those nodes that have been that have been marked as unhealthy. So to sort of sum this all up, we have autopilot at the bottom sort of periodically checking things, labeling the health of nodes, GPUs based on net- networks, GPUs, and and storage, labeling nodes to let the rest of the rest of the system react to that. We have the app wrapper, which is sort of watching the workloads. Uh it's injecting the affinities automatically, so the users don't have to get that hunk of a uh affinity YAML correct. Um it gets it put into all their their workloads for them to steer their pods away from unhealthy nodes. It detects when pods are running on a vic nodes and triggers triggers a reset, and it automates this whole control and reset control and reset and resume cycle on failures. And it also does this capacity management to inform queue of the unhealthy nodes. So now we're going to just go show show a demo of this and oops.

Show a demo of this happening over here. All right. And so in this demo, what we're going to do is we're going to have Alice submit a one single large job. Our cluster isn't very big, so uh it's a three it's a three-node cluster with 24 GPUs. So Alice is going to submit a job that takes 16 GPUs. Um all right, so here she goes. She's going to submit this, and we can see that the job is is running. And then what we're going to do is we're going to go ahead and pick one of these nodes as a victim, and we're going to label it as if autopilot had detected a a severe GPU fault on it, and we're going to see the system then react to that. So we're going to pick this uh victim node, go ahead and stick in the autopilot uh a vic label on it, and then we'll wait for the system to react. This will take about 30 to 45 seconds. And so what we're going to do is we're going to go ahead and watch the app wrappers. So there's the single app wrapper for our job. It's running. And what's happening now in the background is the system has a node monitor.

It's noticed that this node has been labeled. It's then looking to see are there pods that are running on a node this node in particular that's bad um that have that are actually using resources. So is there is there no is there a a pod running on uh this node that's using a GPU, in which case we got to reset it. Um and so once it does that, it'll then go off and from the pods go back to the app wrappers, figure out which app wrappers need to be reset to to to do this, and we'll put them in a resetting state. Um and then and then the uh the the over Here it goes. All right, so now it's reset it, and the resources are being deleted, the pods are being deleted, being cleaned up, and they're gone. And so now we can resume, and the workload is now running again. And if we look to see where it is, the anti-affinities shown there on the on the right or left, whichever side they are, have steered the new pods away from getting onto that bad node. And so now they're running on the on the two remaining healthy nodes of the cluster.

And that's all happened without any human help. All right, this all just happened uh with the system taking care of everything. Okay, so that's the demo of that, and get the mouse over here. Okay, so what we want to do now for the last section of the tutorial is we're going to take you through three sort of representative workloads that we run in this cluster. Um and all of the YAML and definitions of these are available as part of the tutorial, but we're just going to talk about each of them quickly and show them running. Um so the first one of these is the Kubeflow trainer. So the Kubeflow trainer is an operator for uh it's Kubernetes native framework for fine-tuning scalable distributed learning of training of LLMs. It supports a bunch of machine learning frameworks. And for our demo, what we're going to do is we're going to take a bite a PyTorch job and do FST uh FSDP. So fully sharded uh distributed parallel uh training of a small job. We're going to put that inside of an app wrapper for enhanced fault tolerance.

And we're going to go ahead and do that. Um and so we're going to look at the demo. Okay. So again, so this stuff is out here. So what we're going to do is um first we're going to go ahead and do a the kubectl apply for this job. All right, so we're going to grab the command, go ahead and apply this YAML. And so this is this is created an app wrapper that wraps around a PyTorch job that's going to go into this training job for us. And we can see that it's starting to starting to go. created, it's running. Um so we use the Here's the the YAML. It's way too big for you to read. Um but we created this YAML actually using a Kubeflow trainer notebook. So we used a Python notebook to take some fairly standard training uh code for Python code for doing this, and then we used the uh training the training client to actually go off and create the YAML for us, which we then grabbed and used a tool from ML batch to take that YAML and inject it inside of an app wrapper for you. And so all this stuff is available, and the job is actually finished now.

Um and if we go and look at its uh logs, we can see we can sort of see what it did. So it it ran this training job. Um and there's some snippets down there at the bottom. You can see that it ran uh you know, it's a ran with two GPUs inside of each each of the two pods. Um and then it Here's the output from the the actual training. And when we're all done, we can go ahead and delete things and clean up after ourselves. Okay, so that Okay, and I think I'm handing it off to Claudia next, I think, right? Where are we? Yep. Okay. Um so now we're going to see an example of fine-tuning, and for that we're going to use uh KubeRay. KubeRay is the Kubernetes version of Apache Ray. Um it's Ray is a framework uh that goes end-to-end in the entire ML life cycle. So you go from uh data pre-processing up to model serving, and it gives you a runtime for distributed uh work to run distributed workload, and also an API uh with a very nice uh Python um Python module, and also a CLI to run uh to submit jobs, and also create jobs through the the Ray um the Ray the Ray API.

Um so that works uh out of Kubernetes, and in Kubernetes, KubeRay is the one that will work in Kubernetes and the one that that's what that we're going to use. Uh the install is super easy through a um through a Helm chart, and it the nice uh among the nice feature is the Ray autoscaler that will help you scaling up and down up and down the workloads in a very transparent way. Um so uh the fine-tuning demo is an adaptation from a blog post from Red Hat, uh which in turn is an adaptation from uh a demo that you can find on the Ray um repository. Um so we're doing going to do again fine-tuning uh with with Ray, and we're going to train uh Llama 3.1 8 billion on a grade school math uh data set uh with LoRA enablement. So the demo is going to be a set up the environment. We're going to run the fine-tuning job uh through the Ray API, uh and within an app wrapper. And so we're going to submit the job, and we're going to follow how that's go how that goes on the ray dashboard. So. Okay. All right. So, we we need to install some prerequisite.

It's just to set up the Python environment. So, it's a one-liner where we're going to install I mean the major is going to be the the ray CLI from which we're going to submit the job. So, it's a super easy and quick setup. Um to run the job, we also need some storage because we need to first, of course, download the llama model. And that's also where we're going to save the checkpoints where the fine-tuned model is going to be. So, we are creating a PVC. And for the this entire demo, we're going to impersonate Alice. So, we're going to run in the blue namespace and with the dash dash S Alice, we're saying that we are Alice in this case. Uh so, we created the PVC. At this point, we're going to create the ray job. So, here we're using an image from Quay produced by Red Hat with the ray runtime. Of course, you can use the regular ray images provided in the Docker Hub. So, we're going to run on one GPU node. So, we're going to use eight GPUs and we're going to set up set one GPU for the ray head, which is like the master, and seven for the ray worker.

So, we're going to have a total of eight pods. So, we're going to wrap this in the app in an app wrapper and then there is a very nice utility in the ML batch repository that given a any API object will wrap that into an app wrapper. And we're going to use the default uh the default queue, which is the one that Alice can use. So, we create the app wrapper. We just take a look and we see that in the the default queue is the one that's going to be used. And the ray cluster is exactly the one that we have specified in the YAML file. There's there is no change there. Uh so, we're going to submit the job by applying the the YAML file. So, at this point an app wrapper is being created and it seems that the node is is empty, it can run immediately. So, we can see that the ray head and the ray workers are being initialized. It will take a few minutes. Um okay. So, at this point, we can use also the the ray dashboard to monitor the job, which is pretty handy instead of looking at the logs. We can just take a look at the dashboard.

So, we're port forwarding, so you can see that on localhost on the default port 8265. Uh so, at this point now, we can set up the fine-tuning. So, we're cloning the repository where the where we can find the code. That's of course open. Um and through the Python Python API the ray Python API, we are creating the job with the specification that we need. And we set up all other things like where where we where to download the model. In this case, in the HF home, so the Hugging Face home is going to be on the PVC. So, we have one one copy for for all the pods. Uh we're saying that we're running on eight devices in total, which is the number of the GPU nodes in this case, one for the ray head and and seven for seven different workers. So, we we create this very short Python code and we run it. And this is through this ray um this ray API, it's pretty much packaging the entire thing as you can imagine it as like a when you package things into a Docker container. So, we'll create one package and that package contains code and instruction, dependencies, everything that's that's needed for the job.

And that code will be running on the ray head and the ray workers. So, also the head, despite that's the one that's um managing the the job distribution, that's also doing some actual useful work. Um so, we're waiting for the um for the job to be submitted and then we can uh we can follow the the actual work either through the ray API uh on the CLI or through the ray dashboard. So, this is taking 15 minutes or so. Of course, we're not staying here for 15 minutes. I sped up the process. But there are a few things that we can put some attention on. Uh so, in the dashboard, you're going to see on the bottom the logs of what is happening. And those log those logs are in the ray head. So, you can also monitor from the kube CLI. Um there's also the ray core overview where you can see all the tasks that are being executed. And through the cluster menu, you can also see what's going on in in terms of utilization resource utilization. So, we see that the GPUs are working good because that's what we want to do.

And yeah, through the the ray core the ray core overview, we can see which jobs are the total number of jobs, the one that are currently running, how many are in the queue, and all of that. It showed up at some point, but since we're speeding up the process, we may have missed it. So, when the job is succeeded, you can see that on top. And at this point, we're just going to take a look at the uh where the data is. So, in the we're executing a terminal in one any ray pod will work. We are in the ray head in this case. And we're seeing that in the slash model, that's where the PVC is. And we have both the this checkpoint is last one. That's where the the the checkpoint that we've been um with the the fine-tuned model that we just that we just run. And I think that's it for me. Thanks, Radhika. So, we have just one last example workload to share with you today. Another really important thing on our cluster is to run models and inference. So, here I'm just briefly going to show you a uh example batch inference.

In this example, we're running the LLM. You've already seen the LLM mentioned in other talks in this conference. This is an inference runtime originally coming out of Berkeley that is now part of the Linux Foundation. And one way to run a machine learning models and what we want to do here. So, we're going to run an IBM Granite model in a in a pod. And what we want to do is submit a batch of requests and collect some statistics about the behavior of the model. All right. So, the way we do that is sketch on the right. We have an app wrapper that contains a job. This job itself has two containers. Again, one container runs inference runtime. One container runs the load generator that creates the requests and requests collect the responses and so on. And I think as we mentioned before, one of the things with models, they are big. So, we don't want to have to load them every time we run such a job. We want to cache them. So, in this case, we use a persistent volume set up in our bottom here set up here in our in our pods so that the first time we run the workload, we're going to download the model weights from Hugging Face.

But the next time we run the same workload, we won't have to do this again. The details of the containers are here. Uh pretty straightforward. On the left-hand side, we're running the LLM. And on the right-hand side, we're running our load generator with a number of parameters. Here, we're just submitting for simplicity demonstrate we're just submitting random requests and we are measuring what happens with the model. The only non-trivial, somewhat hacky, somewhat ugly thing here is we need these two things to synchronize because we want the server to be running before we start measurement. And when we're done with measurement, we want to kill the server, right? So, different ways to do that. Uh what we found in practice is that users are not really good at building their own container images. There are lots of reasons not to build container images. So, here we will use an upstream container image and modify that we can trace and so on. And then we do whatever kind of synchronization we can manage between these two things.

Uh I could show you a demo, but it's probably yeah, maybe I have a second to do that. So, essentially again, this is exactly the same as all of these demos. This is what you'll find in the, you know, supporting documentation from the tutorial with the specification on the left of the workload. I mean, first time I'm going through the I'm going to skip that the persistent volume definition. But skipping on that, we just copy-paste the workload definition, the YAML you've seen in the previous slides in my terminal here. And then I can run I can capture the logs of the two containers in my pod. The first one is starting to load the model. The second one is waiting for the model to be loaded. Eventually, the model is loaded. The inference can start. You know, the the load generator can start sending requests. We're processing requests. And at some point in the end oops, I went too fast. Uh at some point in the end we get to statistics out of these uh particular test run, and we can compare, let's say, with a previous checkpoint of the model, and say if we're doing better or worse.

So, let's let me uh try to wrap up here. Uh no, I don't want to play this again. Um so, what we what we realized when we started trying to have a Kubernetes cluster with GPUs to do AI is two fairly obvious things. The first one is our admin had no clue. They didn't know about GPUs, they didn't know about AI. So, they needed a turnkey solution, something they would really could deploy in their cluster without having to think about pretty much anything, right? The only and and what we've tried to share with you today is that, you know, the only thing you really have to think about is what are the quotas you want to give to the various teams, and that's essentially the only thing that then admin has to do to deploy this setup on the cluster. The second thing we've realized is that AI ML experts, domain experts, they don't understand anything about Kubernetes, right? So, what they need is pre-baked templates, you know, if you want to pre-train a model, this is what you do, you know, maybe a number of ways to do these, but here's the collection.

If you want to fine-tune a model, this is what you do. If you want to do data pre-processing, this is what you do. If you want to do inference, this is what we do, right? So, what we've tried to share with you today is again uh some of these templates here, right? So, unfortunately, uh we cannot really provision uh a GPU cluster for every member of the audience, so we went through these through video, which I am is not entirely ideal, but hopefully you'll get a sense and the again the key the key idea here is that all of what we've shown you is just turnkey kind of solutions. So, I before we conclude, I want to just share what it means in practice, right? Here's uh one of our clusters we uh manage in IBM Research. It has about 1,200 GPUs, and this is a snapshot I took last week as I was preparing those slides. So, at that point in time, we had about 20 teams uh you know, uh onboarded on that cluster, right? And the quotas for those teams were essentially what you see on the left pie chart here, which is, you know, one you have two teams that more or less have access to a quarter of nominal access to a quarter of the cluster each, and then a bunch of other teams, right?

And there's a bunch of extra capacity that is not nominally associated with any particular team. And what you see in the middle is what we end up with, right? With all these teams submitting a number of workloads to the cluster, we end up at the point I was I captured this particular snapshot of the state of the cluster where 1,206 GPU utilized in the cluster at just a uh a very small fraction, less than 1%, I guess, uh of of 10 GPUs not ac- not currently being used in the cluster, either because we are transitioning between workloads or because there wasn't any really small workloads that could still fit in the cluster, right? So, we've you know, if we were to compare that, for instance, to using Kubernetes quotas to do the same thing, which is to enforce strict quotas and say, for instance, the the blue gray team cannot go above its quota, we would have lost essentially 1/3 1/3 of our cluster would have been idle at this point in time, right? So, what we're trying to say here is with this system, we've managed to do these two really uh antagonistic things, which is one guarantees quota to teams, so for instance, if the uh uh green team at the bottom comes back and say, "Hey, I need my 16 or my 32 GPUs." they're going to get them, but at the same time, we're never really wasting any kind of GPU capacity that we have, that is again really fundamentally unacceptable.

So, uh I've shown these before. Again, we tried to run through all of these kind of key uh uh features that our stack need to have to really be functional, right? Other than what I just said, which is essentially be self uh maintaining and self, you know, no question asked when you deploy and you manage it. And again, uh you know, a lot of that has to do with uh this community and and and taking advantage of all the great work that is doing that is happening in in in many different places, and finding the proper way to get these things together, jointly configure them so that they, you know, work together and and and and give all the capabilities that that we want. So, thank you very much for attending. Uh you can uh look at uh you know, again, you can find all the details of what we've shown you uh videos, everything at this QR code. If you're also interested if you're in generally interested in you know, platform uh for AIs, right? On Kubernetes, we have a number of talks in this in the rest of this week with uh you know, from IBM Research and friends like Nvidia, like Red Hat, and others about different aspects other aspects of this platform, sustainability, compliance, uh benchmarking actually and and and uh data pre-processing also, which is one uh one we have an entire talk about that.

We didn't show any templates uh um workloads for data pre-processing, but the second talk will will have a lot about that. Once again, thank you very much for attending.