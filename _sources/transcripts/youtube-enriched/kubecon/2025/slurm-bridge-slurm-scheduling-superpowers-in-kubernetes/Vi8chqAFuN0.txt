Good afternoon. Um, we're here to talk about the Slurm Bridge, part of the Slinky project. Um, bringing Slurm scheduling superpowers into the Kubernetes stack. Uh, I'm Tim Wber. I'm the CTO for SKMD, the main Slurm developers. And >> Alan mic check. >> I'm Alan Moouse, one of the developers on the Slinky team. >> So, to start with, what is Slurm? Um, slurm is a leading HPC workload manager. Um, the term workload manager uh roughly means it is both a job scheduler as well as a resource manager allocating resources out on individual compute nodes providing orchestration and and management of individual compute processes out on those. Um, this all roughly equates in the cloudnative ecosystem into an orchestrator such as Kubernetes. Um the scheduling part again is kind of this higher level process that is prioritizing deciding what compute jobs what simulation jobs to run on what section of the system. Resource management layer is is really dedicated to the lower level plumbing of getting processes bootstrapped and executing on actual compute nodes.

Slurm itself was originally just a resource manager that was the R and the M out of Slurm. uh but then evolved all of these scheduling capabilities and some of what we're sort of experimenting with with the slurm bridge is getting slurm to behave just as a pureuler and actually being able to switch in kubernetes or slurm's own resource management layers to do the actual process management and process launch. Um slur manages the majority of the top 500 supercomputers in the world. Um this is a list that the HPC space has been maintaining for years. um we kind of look at that as our our benchmark of our our ability to to get slurm out into that audience. Um there is not really an equivalent uh list to point at for a IML workloads although our market research implies that we we do run the vast majority of AI training workloads today because of slurm's unique multi-node job scheduling capabilities that don't really exist directly in the cube ecosystem yet. Slarm also very frequently scales beyond 15,000 nodes in a cluster.

We we again run on these largest systems on the world. Um Slarm is open source under the GPL v2 or later license with an OpenSSL license exception. Um a little distinct from from stuff that's more native to the cloud ecosystem. Sketmd just to step back yet again um we are the developers of Slurm and now Slinky. Um, we actually spun off from Lawrence Livermore National Lab back in 2012 to to support and and promote SLURM's adoption through more than just the the DOE national lab systems. Our two founders, Mo and Danny, are the MD and SKEMD. We are not medical doctors. Um, please do not consult us for medical advice even though our business license says we are allowed to, which is a very weird quirk of uh Utah making a few clerical mistakes there. SKMD does commercial support for Slurm and now Slinky um alongside training consult and custom development. That that's our revenue stream to support all of this work we do. Um everything SKMD builds is open source. Support revenue is is what lets us go and build more of this and and support it broadly.

So what is Slinky? Um this is our very simple little equation. Take Slurm and Kubernetes. Slinky sits at the intersection of these two stacks. Um it is what lets us coordinate between the two sides. So, Slinky itself, we we try to describe it as a toolkit of related components to integrate Slurm into Kubernetes. Um, those different components are designed to be able to mix and match with one another, but they can also be used standalone. Uh, we've had the slurm operator out for a number of years as sort of the first major component we published. Um, slurm bridge is sort of the newer bigger piece bringing slurm directly into the kubernetes scheduling layer. We're trying to build everything here under the Apache 2 license um to make it a little friendlier to interact with the rest of the cloud native ecosystem um not just be solely isolated to more of a GPL space that is not always as as friendly with some of the cloud environments. So why tackle both? uh what what we're seeing and what's driving a lot of the work we do here is this idea of traditional cloud native systems um having a lot more batch type computing work to process.

Um there are a lot of different tricks and prioritization mechanisms and other things that we think can come over from the traditional HPC batch computing ecosystem and better inform how to run these same classes of workloads in the cloudnative space. Um, we also are seeing a lot more demand for for sort of hybrid and converged environments where you have batch computing demands adjacent to maybe production operations. Trying to figure out how to service both out of one unified set of compute hardware rather than needing to build separate silos for say your training regime versus your inference side. Um, and Slinky is meant meant to be our intersection of this and and the way that we promote this this converged model of computing between the two halves. Uh, and in terms of additional capabilities, um, and kind of what we think SLURM can bring to bear in this environment is is a lot of different things that the HPC scheduling community have been been expecting, developing, and putting a lot of thought into over the last several decades.

Um, one of the major things that we're seeing and and what drives Slurm's preeminence in the AI training space is that that multi-node scheduling is a default capability of HPC batch systems. That that is a bakedin assumption. It's not something that's being retrofitted on top that then shows up in support for advanced topological computing. um especially being able to optimize like network layout and network placement for interconnects like Nvidia's DGX systems making sure that the planning for those simulations matches the underlying interconnect and are efficiently placed for best performance. Um then this this other related concept that that's a natural extension of the idea of multi-node workloads being a first-class citizen is this idea of backfill scheduling of needing to plan not just the instantaneous use of the cluster but plan the cluster's use through a broader time frame when you can't instantaneously run everything you want to on that same compute harbor. How do you prioritize and manage everything going into the future?

And with that I'll turn it over to Alan to talk about the slum bridge. Okay, thank you. Um, yeah, so I'm Alan. I'm going to walk through what SLRBridge is and how we're we're managing to schedule Kubernetes pods using SLR. Uh, so this first technical diagram, um, it's kind of a mental model that exists in my head. Um, that we've got this like this multiverse of right now with Kubernetes um, and slurm. And so um, how can we kind of merge them together getting the best capabilities of both? Um and that's where slurm bridge comes in. Um so slurmbridge uh is a kubern it it's using slurm as a kubernetesuler. Um and we're using the kubernetes scheduling framework. Um so kubernetes scheduling framework. Um a couple of the extension points that we're using for node placement would be pre andq prefilter filter and post filter. That's kind of the the extension points where we decide which nodes a pod should run on um and when those pod should run. Uh we're also using a pre-bind hook to generate DRA resource claims um if we see that a pod needs to have that generated for it.

So that's kind of the the Kubernetes feature that we use. We're using the scheduling framework. Um but how does Slurm itself know about these pods? Um and that's where we come up with the step where we translate the the pod resources into what we call a slurm placeholder job. So I kind of view that as like the outline of of one or more pods into this slurm placeholder job. um internally to Slurm. That's also kind of referred to as an external job. Um and that's a construct that Slurm has um to say here's a job. Um go ahead and make a placement decision for this job. Um but when it comes time to actually launch the job, understand that something else is going to do that on your behalf. Um and in the case of Slimbridge, that would be Kublet. Um this idea of a placeholder job is is similar to what's coming out um in a subsequent release of Kubernetes, I think 136, where there will be a workload resource type. Um and that's kind of this abstraction around um multiple pods. How do we represent um a piece of work might not just be one pod, but it might be multiple pods.

Um so we're looking forward to that workload resource type. I think that will help us in how we derive our placeholder job. Um but for now we're kind of having to do our own homework and figure out which pods sort of belong together. Uh so to summarize uh slurmbridge we'll scheduling pods by slurm but they're launched by kublet. Um slurm obviously needs to know what types of nodes are in the system. So you um have a couple different options for what slurmur nodes you have. Um the first kind of variety we have we sort of call like a hybrid type deployment of a slur node where you might have both slurd and kublet on the node. Um and in that way you could still leverage slurmd to run um your traditional slurm batch workloads with slurm client commands like sr run and sbatch and sallalloc um or you could have kublet be um feeling those placeholder jobs and running the pods from um slur and brbridge scheduled pods or um just like we have this external job we have the concept of an external node. Um, so because Kublet's doing the work of launching the pod, um, we can also kind of take that one step further and say we don't really need slurmd running on the nodes.

We could just have Kublet running there. Um, provided we tell Slurm slurm there's some nodes out there. This is what resources it has CPU and memory. Um, but just uh there's Kublet out there running instead of slurmd. This is the scheduling framework that I mentioned before. Not sure how many people are familiar with this. Um, but this is how the main Kubernetes scheduler processes pods. Um, kind of to go over this a little uh quickly. Um, pre-NQ, you've got this first opportunity to kind of inspect pods before they get to the main scheduling loop. Um, those pods are then sorted and then you evaluate them one at a time. And these filter phases are where you're kind of reducing the set of nodes that could run this pod. Um, so you kind of filter down from all the pods in, excuse me, nodes. You filter down from nodes in the system um to say which nodes could this pod run on. And then when it gets time to score it, you're scoring um what's the best node for this pod to run on. Um and then you move on to these reserve and permit hooks before it's time to run.

And you hand it off to this binding cycle, which is this asynchronous process where you finally let Kublet run the pod. Um, so this is Kubernetes like the main way that you schedule pods and these different extension points where you can modify this behavior. But if we kind of recontextualize this in um slurm bridg's perspective, I'm going to kind of reillustrate this this diagram showing where along this path that we're we're operating on um the pods as auler. So we get to this pre-NQ phase. Um we're just making sure that a pod has a toleration. Um it's important for us to kind of fence off some nodes that slurbridge is scheduling for. Um and we do that with this note um no SCAD taint um so that pods don't like the main Kubernetes scheduler wouldn't land pods on these nodes that we're trying to schedule pods on. Um so that's pre and Q. And then we're we're moving on to this pre-f filter phase. And this is kind of the the first point in time that the slurmbridgeuler would look at the pod and say, does this pod have a placeholder job associated with it?

Um, and for the purposes of this talk, let's just say it is the first time we've seen it. So, we'll ask the question and the answer is no. It it doesn't have a placeholder job. We haven't seen this pod before. Um, so now we're moving on to this filter phase where um we're trying to like figure out which nodes could the pod run on. Um what we do here is we let um some of the traditional filter plugins run like taints and tolerations or the um volume claim plugin. Um and this is a way for us to kind of figure out if Kubernetes were to have picked some nodes for this pod um which nodes would be valid for it. Um, and that's important because uh, slurm doesn't necessarily have the primitive of a taint or a toleration. Um, and that's not something we can directly map into slurm. But by letting Kubernetes kind of do this pre-processing to figure out which nodes would be valid nodes for Kubernetes, we can kind of derive this list of nodes that we can base our placeholder job on. Um, and then we just make sure that one of our our filter plugin is the last one to evaluate.

And we can kind of make the make the assumption that if we get to our filter plugin, we know it actually would have been an okay one for Kubernetes. Um, and our filter plugin is kind of just a simple check. Does it have this special annotation that says Slurmbridge gave this pod a node? Um, so again, because it's the first time, none of that has happened yet. Um, so we say no, it it it failed the filter check. Uh, but we get to now post filter. And traditionally post filter you might be doing some preeemption where you're clearing some space off the system if your pod has a higher priority. Um you can kind of eject some lower priority pods and make space for your pod. That's not what's happening with slurm bridge. What we do um is we use that input from the filter phase. Um what nodes are acceptable to kubernetes and then we create this placeholder job in slurm. Um so we now created a placeholder job. slurm is able to then um schedule that pod using its main scheduling algorithm and then we kind of get back through this feedback loop to pre-filter.

It can ask the question again um and maybe there is a placeholder job but maybe it's not running yet because slurm has a full um backlog of work and it's not getting to this placeholder job yet. So it the answer could still be no and that you have to um kind of reevaluate this loop over time. Um, but to get to get things progressing, let's say that yes, the placeholder job does finally run. Um, so we kind of do this work where we hand out some nodes to the pods associated with that placeholder job. Um, and from pre-filter, we can just return the the single node that we're approving from this pod. And then at that point, the filter check becomes this kind of rubber stamp where we say yes, Slimbridge approved this uh node for the pod. And then we can proceed to the binding phase where we actually let the pod run. So that's how we're using uh the scheduling framework to kind of create these placeholder jobs um so that slurm can understand how to schedule them. This diagram shows um what's running on a system when you're when you're using slurm bridge.

Uh to highlight here we've got like an admission controller just to check that pods have some some sane defaults or it has that um toleration for the taint that I had discussed. Um the slurbridgeuler itself running as a pod. um as well as a controller. No Kubernetes component I think would be complete without a controller to do some reconciliation. Um but this is really important because we have pods that we've now replicated in Slurm as a placeholder job. Um so there there must be uh some entity in the middle kind of reconciling both of those worlds. Um you could imagine like if your slurm placeholder job had some sort of time limit associated with it. um and that no no uh job should exceed a certain time boundary and your placeholder job um gets terminated by slurm um it's up to the controller to go look on the other side in Kubernetes and say uh that placeholder job you're associated with is terminated and so you need to now um terminate as well or vice versa maybe all your Kubernetes pods um complete they finished their work it's equally important to tell slurm um that placeholder job is no longer needed um we've completed the work so we can free up resources and slurm can kind of chip away at that backlog of maybe other placeholder jobs that are pending.

Um within Kubernetes there's also uh kind of a in this diagram a little mini slurm cluster where we have the rest API and a controller. So, slurm restd is kind of fielding the the API requests and then the controller slurm ctldd. This is kind of the brains of slurm. This is what's actually doing the um scheduling making the the decision on where to run those placeholder jobs. And then I've just represented the two kind of flavors of nodes that we've talked about. You can kind of have this hybrid approach with both Slurmd and Kublet running. Um, and depending on what type of work that node is meant to to use, you could have slurmd actually running regular slurm work or you could have um, kublet being fielding um, pods that are placed there by slurm branch. Um, and again there's also a slurm external node type where you don't need slurmd. You just let kublet run on there and we just tell slurm, hey, there's this external node over there. Um, and you can place work there. I think I've kind of gone through this controller, but again um the controller in the middle reconciling um pod events.

So or or pod updates, we watch the the pod resource type and if we have a placeholder job associated with it, we kind of act on it. Um so if all those pods completed, turn over to slurm and say, "Hey, Slurm, that that placeholder job should be gone now." or if there are updates to the slurm client cache, we can generate events and then reconcile on the other side to Kubernetes to make sure that we're um keeping both of these realms in sync. We also do have some cleanup work here with um DRA resource claims. If your pod has generated a resource claim um or slurmbridge has generated that um we do clean that up because we know that the slurbridge placeholder jobs are representative of of those resources and we'll re recreate them if needed. So to summarize with this kind of design in place using the scheduling framework and placeholder jobs uh slurbridge gives you a workload aware multi-nodeuler. Um so those pods that come in we can kind of um derive what resource type they came from. Is it a pod or a job or job set or pod group or leader worker set?

Um in the case of pod group or leader worker set it makes sense that multiple pods could be the same placeholder job. Um so like leader worker set has a concept of a group where in order for that group to be successful all the pods need to be running or you should just not run all of them. You should you should wait until all of them could run. So multiple pods could map to a single placeholder job. And that's how we kind of get slurm to be able to talk to Kubernetes and do this multi-pod multi-node placement. And if you configure your slurm cluster with a topology file, the topology definition of your system, slurm is just going to intuitively make uh a topology decision on how to place your pods. So we have uh topology aware scheduling and we do have this uh DRRA support. So when we are kind of doing this translation, we say um like either just a regular device plugin or we're kind of keying off of this DRA capability, the extended resource um feature where you have this little prefix and we can kind of find out that that would map to a DRA um device class.

So let's translate into that translate that into something that slurm understands which is a GRE resource type. Um, so we model that as GRZ and slurm and then if needed we can generate a DRA resource claim. Here's a look at the translation kind of an example translation between a pod and a slurm placeholder job. Um, so you can see there's some annotations over here that we support. This is just um honestly a pretty simple example of just overwriting the job name in slurm. So um you can see in slurm the job name is has been overwritten. Um, maybe a more interesting thing to do would be to apply your time limit here. If you want to make sure your pod doesn't exceed a time limit or maybe it's automatically applied that placeholder jobs only are allowed to run for a certain amount of time, um, that annotation could be used there. We're then translating the resources into the placeholder job. So, how much resources does this pod request? Um, and then here was that DRRA um, style syntax of an extended resource claim where we can key off of that and find out what device class type it was.

and then translate that into Slurm's GRE annotation and a quantity. Um, so when Slurm makes this placeholder job, it's also accounting for like uh say the GPU requested by the pods. So kind of a static snapshot view of a system. What does this look like when it's up and running? Um, in this example, I've got three pods here. A a job pod is running and then two these sleep dr pods. These were actually generated from a pod group resource type or rather associated with the pod group resource type. Um we can see that some resource claims were generated for those pods because they requested GPUs. Um and then if we look at this kind of third block here that's um slurm's representation of the placeholder job. It's the slurm Q. Um it's showing the running placeholder jobs. Um, so you can kind of see job ID 8 here lines up with that job pod and then we just have a single other job uh job ID 9 and that's the pod group pod. So that single placeholder job um is representative of the two sleep pods. You can see that slurm picked these nodes six and seven for these two pods.

Um and so that's where the the sleep pods are running. Um and then this final block showing slurm's node level view of the system. which nodes are allocated and which nodes are idle. So we have two idle nodes. Um what would happen if a new set of pods burst onto the scene um and they wanted three nodes um and they can't get three nodes. Well, we can say um imagine that a leader worker set scaled up to do some AI inferencing demand um in response to some demand. And so these VLM pods come up, but they're pending. Um and the reason they're pending is that this placeholder job in Slurm is pending waiting on three nodes. So, Slurm says, "I'm waiting on more resources to be available." Um, so these pods remain pending. Um, and you can kind of see, yep, there's only two idle nodes. So, those pods would just stay pending until the slurm decides that um it's time for the placeholder job to run. So, if we've kind of let the the previous or the first three pods complete their work, those go complete. The placeholder job, job ID 10, and slurm um starts running.

And then we can see it's got these kind worker nodes. Kind's kind workers five, six, and seven. And those line up to the same pods that are or same nodes that are assigned to these pods. Okay, that was kind of this this static view, but I can kind of show um a system up and running. Um this is running locally on my machine. We have a little script in our repo. This kind.sh um with-d example that's installing the an example DRA driver. Um, so you get some nodes with some fake GPUs to play with. Um, got some leftover things over here. Let me just delete that real quick. Um, we can show with this uh setup you get a um a few kind nodes and then we've got within slurm five of these nodes are registered as in the slurm partition. So those are the nodes that slurm's going to use to schedule against. If we look at the pods that are running here are some of the pods that we've talked about. So slurmbridge we've got um the admission uh the controller and theuler and then I'm using another slicky component slurm operator to stand up my own little mini slurm environment in this cluster.

And if we look in the slurm namespace this would be like the bare minimum pods that you would need from slurm to do slurm bridge. Normally you might see like um slurm accounting or a slurm database. Um, but here I just have the controller and the rest API. Um, and I'm just using external nodes. So there's no slurmd running. Um, so just controller and rest API in this environment. Um, let me just apply one of these resources. So I'm going to apply a job that creates one pod and it it does make a DRA resource request. So we get a DRRA resource claim generated here. Um if we wanted to so this is doing s control show job on the placeholder job which is job ID 38. So here we can see kind of a blown up view of the placeholder job. Um kind of some things to point out here is like this requested node list that was generated from that filter um kind of feedback loop that I highlighted earlier where those nodes are valid nodes that Kubernetes could have used or or would be valid for that pod. So we hand those kind of um feed that information into slurm.

Um and we can see also we leave a little bit of a breadcrumb here in this admin comment to know from slurm's um point of view which pods is this associated with. Um we can do like this pod group example our two sleep pods here with a single placeholder job. Um that one I would guess would be 39. So here's the the multi multi-node multi-pod example. Now we have two pods associated with this single placeholder job. Um and if I were to say scale up this let's say the the leader worker set needed to scale up. We could scale that up. Now we've got these three three pods that came onto the scene. And in this case there was there were enough nodes available so they could immediately start running um with this placeholder job. Okay, this is the the demo I had. I guess we can open it up if um folks have questions or interested to hear more. >> Hi, thanks for your talk. Um I'm curious to know I think there's is it the flux operator that's out of lnl Um, I don't know. There's a bunch of projects in this space that are all kind of trying to do the same thing.

So, I'm curious if you can just say a few words as to how Slurmbridge compares to those. >> Okay, I'll just hand the mic back and forth. Um, no, there there's quite a few different projects um all have slightly different spins on it. um uh flu flux uh flux specifically theuler flux not flux cd not the other half a dozen flux um is is interesting it actually has a bit of shared lineage with slurm but they have a slightly different take on a lot of stuff here um uh we're not directly trying to compare what we're doing with bridge to to their approach it it is a little different and and sort of we're we're assuming and and positioning slurm to be the be the top level scheduler across the whole converge set of infrastructure which is a little different. So instead of uh I mean it looks pretty complex especially like trying to sync uh context between two different worlds trying to do or achieve similar stuff. Is there a better way to do achieve the same thing without having so much complexity where like you run something like a placeholder pod on each node and then from there manage all resources where slum takes over and then Kubernetes is just a means of deploying stuff and slum is a means of doing the actual job and scheduling and managing resources.

take this. I mean, I think ultimately we do need a way to represent the pod in slurm. That's kind of foundational to how we do that scheduling. So, we do need to kind of model it in slurm and that kind of introduces um that need for the reconciliation between the pods and the placeholder job. So, I think that is kind of core to to what bridge is doing. to to expand on that, the the part that from my level is is sort of the most nuanced is the the work that we've had to do to translate all of the different representations of multi-node work that are kicking around in the Kubernetes ecosystem. job set, pod group, leader worker set. Trying to to map those into slurm's canonical representation of multi-node work is is where a lot of the complexity of what we've had to implement and bridge comes from. Um, we've been working with a lot of the scheduler groups. Um, there was a full day of of discussion kind of around a lot of this about what would it mean to go introduce really a true multi-node primitive into the cube ecosystem.

Um, something like that would actually help reduce a lot of the complexity of what we're doing in Bridge. The the actual reconciliation loop is really not that difficult. It it's just our way of of communicating back and forth. Slurm doesn't natively have a bunch of asynchronous hooks where we can just dial directly back and forth. Um, that's something we may pursue which would move us away from this reconciliation model to something that's a little more event driven. Um, but that's a little further down the road. this was just sort of the the easiest path to pursue for now. >> I see. Okay, cool. Thank you. >> And that um I'm getting told we're we're out of time. Um thank you all. Um Alan and I will be sort of floating around in the lobby. We do have stickers here if anyone wants to grab them. We don't have a booth this year. We might in future, but uh that's our whole swag pile is up here at the moment if anyone's interested or uh feel free to to tag us and say hi later. Thank you guys.