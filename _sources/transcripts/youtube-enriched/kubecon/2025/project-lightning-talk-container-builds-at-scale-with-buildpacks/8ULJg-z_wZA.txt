All right, we're going to talk about container builds at scale with build packs. Uh we're going to look at how companies like Salesforce, Google, Heroku, Bloomberg, and others are using cloudnative build packs to make managing container builds on their developer platforms more efficient. Uh so build packs is an incubating CNCF project. Uh we've applied for graduation. Uh so we're starting that review process. Um you can learn more about that uh in our talk tomorrow which we'll give you some details on. Uh so when we talk about scale I want to be clear that I'm I'm talking about the order of tens of millions of images uh for some of these organizations. But the problems you encounter at that level of scale are for the most part the same as the problems that you have when you're managing thousands or even hundreds of images. Uh build packs work across this whole spectrum of scale. Uh, and they also stay with you as you grow. And we'll talk about how that works in a little bit. Uh, so real quick, my name is Joe Cutner.

I'm an architect working at Salesforce on our internal developer platform and I'm one of the co-founders of the BuildP packs project along with my other co-founder, Terrence. Hi, I'm Terrence. Uh, also co-founder on the build packs project. Uh, and I'm an architect at Salesforce. Cool. So, what are build packs? Well, at a very high level, there are tools that translate your application source code into a productionready container image without the need for a Docker file. Now, you can use Docker files with build packs. There's a thing called build packs build pack extensions that let them work together. Uh, but in reality, most people that use build packs aren't doing this. And that's because when you adopt build packs, you're really embracing this model of modularizing the logic that's required to build your applications so that you can then reuse it. Uh this is in contrast to docker file where uh the only equivalent is basically copying and pasting a docker file across dozens or more repos and then trying to keep them up to date.

Uh but with build packs you can put that logic into a module. You can use higher level programming languages to do that. And then you have an API that you can use to create layers intentionally. Again, in contrast to docker file where the layers are created sort of arbitrarily from statements in the docker file, uh build packs let you be intentional about what's going into each layer and also provide metadata about what's in that layer. So the end result is a container image that maps logically to the components of your application. a layer for your application binary or whatever it may be like uh a layer for your dependencies or even for each dependency in different parts of your application. What that unlocks is first and foremost uh some really advanced caching mechanisms. Uh but it leads to many other benefits. So we talked about modularization, talked about higher level languages. Uh these lead to encapsulation. So you can take the little bits of logic that represent different parts of building your application, encapsulate them, reuse them.

uh that in turn gives you composability because these build pack modules are designed to work together. So a NodeJS module and a Python module can run uh together on the same uh service or repository. Whereas the only mechanism you have with docker file is very linear, right? You also get that linear caching layer thing uh where changing a layer up here because you edited the authors in your palm xml means you blown away your maven cache or something like that. Uh with build packs uh the more advanced caching mechanisms let you be again more intentional and more structured about caching. [clears throat] All that said it gives uh it gives you as a result uh standardization for your container images. So by reusing these modules to build container images across all of your services, you end up with images that are standardized. They look similar across your platform. Ultimately, that makes managing them uh far easier. So if you haven't thought about it yet already, why do you want to use build packs?

It's because it's a more sustainable container build option. And I really do mean that in the like eco-friendly way. uh because of these advanced caching mechanisms, you're actually using fewer resources in terms of CPU and memory uh to build your applications and to store the images because the images themselves are constructed more efficiently. Uh but you don't have to take my word for it because Terrence is going to tell you how some of those caching mechanisms work. Thanks, Joe. Uh I'll be quick. So one of the things that uh people get really excited about with build packs is a concept called rebase which is essentially updating the base image of of your application. So it's pretty common you have more than one container image in your business you probably have tens of thousands um or millions of them and what happens when you have to update an OS the standard docker file thing is this is like the first line uh in your doc file which blows away the cache so you have to like rebuild your entire image.

Uh Joe talked about uh how things are standardized and one of the things that's nice about that is uh with that standardization with that metadata we know the separation between where the build pack layers start and kind of where the OSM image layer ends. And so that allows us to with the AI compatibility with a lot of the kind of DRO um operating systems uh you can kind of do a lift and shift where we're able to without doing a rebuild replace the underlying OS with those layers as a J JSON manifest update without doing a rebuild at all. So this goes from an a rebuild to basically a text file change. Upload that to the registry. It also makes rolling those things out because you just have to roll out the OS layer changes and then it's just a JSON text manifest. You'll notice that the Shaws for the app layers are the same, right? Because we didn't actually touch them. Um so the you know the fast way to do something is to not do work for it. Um and so that's what you get for some of the day two stuff.

Um but how do you get started with build packs? This a lightning talk. Uh the easiest way is pack. uh get that installed uh and then you can run pack build uh and then kind of image name. Um uh but most people don't actually need to go build their own build packs. A lot of people start with existing uh what we call builders um that are out there. Uh you know we I work at Heroku um at Salesforce. So obviously Heroku has a set of builders. Uh there's also the Pquetto builders as well and then Google has a set of builders as well for basically doing a lot of the language kind of operations. So you can start from there and then kind of modify without having to start from scratch of writing your own build packs everywhere. Um and you know I mentioned pack as local CLI kind of uh easiest way to get started but there's a bunch of different platforms um for using build packs as well. We have a Kubernetes operator called KPAC as part of the project. Um it's integrated into Spring Boot itself uh for containerizing your application.

If you've ever done that, you actually using build packs under the hood. And uh more importantly uh at CubeCon uh we have a maintainer track talk tomorrow uh at noon uh that Joe and one of the other uh contributors uh Joey will be doing if you want to learn more. And then we're at the project pavilion uh the entire week uh in the afternoon. So please come say hi. And uh we're on the CNCF Slack uh in the build packs uh Slack channel. So uh please hit us up there. Uh thank you.