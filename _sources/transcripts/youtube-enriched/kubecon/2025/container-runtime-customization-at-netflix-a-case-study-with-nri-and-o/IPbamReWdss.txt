Hello everyone. Uh, thank you for being here today. I know it's been a long day. Um, and there were many interesting talks in the time slot, so I really appreciate you being here. Um, hope you've enjoying your time at CubeCon. My name is Erikson and I work on the computer runtime team at Netflix. And today I'll be talking to you about container runtime customizations at Netflix. Uh in terms of the gist, uh I'll be sharing how we use runtime extensions such as the node resource interface plugins and OCI hooks to handle some of the unique platform requirements of Netflix's compute platform, Titus. Okay, so here's the agenda of what I'll be going over today. Uh I'll start off by describing a problem we faced around the same time last year around this time where we were trying to migrate years of accumulated custom runtime uh code to a more standard Kubernetes data play implementation consisting of standard tooling like stock cublets and containerd. Then I'll kind of share some uh our guiding principles, philosophies, and strategies for kind of migrating user workloads to this new modern stack and how that kind of influence our implementation.

Next, I'll kind of go into detail of the runtime extensions we leveraged, chiefly the NRI plugins and OCI hooks. Then I'll give some concrete use case examples. uh think for things such as CPU isolation, trying to run systemd in a container and trying to uh control the workload life cycle. Then I'll share some uh experience of us running these runtime extensions at scale in production and some of the challenges we faced. And lastly, I hope to share some key learnings and takeaways from our modernization projects. And hopefully by the end of the talk, I'll have some time for questions. By the end of this talk, I hope you'll walk away uh with ideas on how to handle your own unique platform requirements using these runtime extensions. There's no need to for Kubernetes or rewrite your workloads. Uh there are tools available for you to to achieve what you need to do. So let me start off with some context. Uh you might have seen this slide in some of our uh previous presentations from Netflix. Um so what is Titus?

Uh Titus is Netflix's container platform. It's been running production workloads for almost nearly a decade. Uh originally it was conceived in the early days of cloud native uh before Kubernetes was kind becoming more mainstream. Uh originally built on patch messos and when kubernetes became more dominant uh we swapped out the underlying control plane with kubernetes but critically uh during that time we decided to leave the data plane implementation intact. This becomes a rather uh important detail which I'll kind of dive into later. Uh so today on Titus we run hundreds of thousands of pods globally at any given time. It powers both our streaming and non-streaming applications in Netflix. It hosts a very diverse set of workloads. So we run batch jobs for encoding. We also have data pipeline jobs and more recently uh more relevant today we have a lot of machine learning workloads uh that powers our recommendation systems. So what was the problem we're trying to solve? Uh when we adopted Kubernetes control plane uh back in 2018 we did that migration we still had a lot of unique uh platform requirements coming from our users uh and we had to implement that in our data plane that just did not lend well to the Kubernetes model.

Um we have developed our own patterns and methodology methodologies for orchestrating containers uh with multi-tenency features before granes back practices and cloud native conventions have solidified and to migrate our data plane imple implementation back then we use a tool called virtual cublet. Uh virtual cublet is this open source framework that essentially lets you masquerades as a cublet to the control plane but you can uh dispatch container execution to a custom backend. Uh this was the mechanism that allow us to preserve our existing runtime implementation at the time. Uh so the custom the custom runtime layer was called ties exeutor. It's it's this very monolithic uh runtime layer. can think of it as the CR implementations of today. Uh that essentially did everything to set up a Titus container uh to run it through Docker. And this is al also before Docker Shim was a thing. And this particular piece of software had grown rather unwieldy as we kind of piled on more business logic, more platform requirements over the years.

And this custom stack had created had been increasingly becoming hard to maintain. The way we were using Docker as an underlying container runtime for running TI's job had also proven to be somewhat brittle and not very scalable. And by tying our runtime to docker, we we couldn't adopt standard case tooling and new innovations in the container runtime space. And more most critically with Docker long deprecated in Kubernetes, we just really needed to modernize and get with the times which then leads to our goal. Our goal was to migrate to a more modern runtime stack. And what does that mean? We wanted to align on industry standard tooling versus relying on these bespoke homegrown container runtime components. And what that really meant was adopting stock cublets and community as the main runtime components in our data plane. And by adopting real cublets, what we were able to shed was all this undifferiated work we did around pod life cycle management and all the interfacing with the control plane that we had to have custom code around.

And by swapping out Docker from containerd, this opened the door for us to many new innovations like different snapshotters that we can try out for improving image pull times and unpack times. But this goal doesn't come without its own challenges. The big challenge here was that we had years of custom runtime logic to preserve. Many of Netflix's workload come from the EC2 era where they expected VM like envir environments. So you have systemd running as a nit as a service manager and users of our platform also expect platform services to be running under systemd in the same container namespace. We also had multi-tenency requirements such as user namespace isolation which fortunately has been supported in Kubernetes in recently in 1.33. Another thing was CPU isolation and workload tearing. Another core goal of the project was that we mustn't disrupt our existing user workloads. Maintaining trust in Titus as a compute platform was paramount for us and the reality of situation is we built Titus really early before cloud native has so truly solidified.

We adopted containers early and develop our mo methodologies that made sense at the time and our users have built their software and deployments and and additional abstractions on top on top of these patterns. So the key question became how are we able to preserve all this behavior while modernizing our runtime stack transparently to our users. Our migration philosophy essentially came down to balancing two goals that our intention on one hand we wanted to align with industry standards. As mentioned earlier, we want to adopt real cublets containerd, use standard kubernetes primitives wherever possible and identify anti-atterns and remove them to our best our ability. But on the other hand, we also had the mandate of essentially trying to keep uh zero user disruption during this whole process. We want to ensure a transparent migration where existing workloads just continues walking on Titus. It will just keep working even after we swap out the engines. So in the end the strategy that we settled on was let's just take the biggest step we can towards industry alignment without breaking our users.

Let us build a solid foundation on standard runtime components first then we can iteratively move towards a more modern more conventional methodologies. Okay wait. Oh there we go. Um so tactically how do we apply the strategy? Rather than rewrite everything from scratch, we took a lift and shift approach. We broke up our existing custom runtime code and relo relocated each piece of function functionality to the appropriate runtime extension point. This approach has three key benefits. First, we minimize risk by reusing the same battle tested code that we've been running in production for years and we have extensive integration tests for. Secondly, it allowed us to easily validate the new runtime architecture with real workloads without forcing major change on our users to change however they deploy or however they package your software. And third, most critically, we can reduce operational complexity. Because we have an transparent migration, we could switch user workloads over quickly and avoid having to maintain two different runtime stacks in parallel for too long.

And here I have an excerpt from a excellent blog post by one of our fellow engineers on the computer runtime team Kyle that talks about how to migrate complex systems in infrastructure and it reads minimize the time you roman ride at all cost. Do not underestimate the burden of Roman writing. Roman writing is a practice of trying to balance on two horse that galling forward at full speed. In the context of platform engineering, we are the person balancing on two horses and the horses are our new our two different systems that try to accomplish the same functionality and the burden of operating those two systems. It doesn't just grow linearly. It's more of like a 4x increase because of all the interystem uh interactions and all the contact switching you have to do when you're operating two systems. So moving on, that's enough of context. I'd like to talk about the actual extensions we use for modernization projects. One of the first things that cluster administrators consider when they need customizations at the pod level is usually admission web hooks.

They're widely used for policy enforcement, injecting sidecars, injecting environment variables and volume amounts. Many platforms rely heavily on web hooks, but web hooks have a fundamental limitation here. They run at the case API level before the pods actually get scheduled onto the node and they can only mutate the declarative pot spec. For us working on Titus, we need to do do things like inject processes into existing container name sources at runtime. We also need to dynamically determine for CPI isolation and also control workload startup timing. You simply cannot express these runtime operations in a podspec. We have to manipulate the container for after it has been scheduled onto a node. And this is where runtime level extensions like the node resource interface plugins and OCI hooks come into play. So the two key runtime extensions we leverage are the node resource interface plugins and OCI hooks. NRI plugins are these long running processes that register themselves with the OCI supported container runtime like containerd.

They can modify the OCI runtime spec before the containers gets created. And they also can perform additional actions at specific pod and container life cycle events. And these events usually map to different CR requests for creating and deleting containers and pods. And because they're always running on the node, they're great for coordinating runtime operations that need to span multiple life cycle events. And they're also great for making no-level runtime decisions. OCI hooks on the other hand are short-lived executables invoked at different container life cycle moments like pre-start, post start and post stop which are defined in OCI runtime spec. These hooks defined by by the OCI runtime specification depending on depending on which hook the operations can happen directly in the container namespace which we do use for things like creating sentinel files, directories, sockets on our on our users's behalf. Another neat property of OCI hooks is that they're guaranteed to execute by the lower level runtime like run C as long as configured in OSI runtime spec for each container.

This makes them particularly reliable for critical uh operations like cleanup and tear down. Even if other uh components fail and the container is running, the hooks will still run as as long as they're declared as part of the runtime spec. Next, I'll show how these extensions fit into our new runtime stack. So here we have a table that shows the different runtime extensions that we leverage as part of our modernization efforts. So we do still use emission web hooks. We do use emission web hooks for policy enforcement. So for example, if a pod comes in with a ridiculous request for memory or CPU, we just reject it and not let it just sit in pending forever. We also have we also do uh postutation to add pod annotations that eventually drive runtime behavior. Next, we also have our own CNI plugin. We use it for IP assignment container networking uh to adapt to uh some rather special requirements of uh Titus' container networking story and uh we have a talk back in 2014 reinvent that kind of goes in detail if you're ever interested of how we kind of do IP addresses assignments so I recommend that talk [snorts] next we have NRI plugins as mentioned before we use it direct we do use it for OCI OCI OCI runtime spec u modification and we also use it for coordinating the startup sequence of our tus workloads.

Next, we have OCI hooks. Uh, as mentioned before, we rely rely on them heavily for cleanup whenever containers stop and we also do direct container modifications when the containers actually are created. Uh, lastly, we have this uh little piece of software called the run wrapper uh which fills in any last mile OCI spec adjustments that NRI uh doesn't currently support in terms of adjusting the OCI runtime spec. And this piece particular piece is something that we don't intend to kind of maintain long term. It's more of like a place where for things like secon policy for example that hasn't been that wasn't re recently supported in adjustment API we had to kind of do in a rusty wrapper. But as things more uh as ni support more adjustments we'll eventually get rid of this layer. Next I'll walk through some concrete examples of how these extension points work in practice. So for example we have systemd support as I touched upon earlier a lot of Netflix workloads expect systemd to be present supervising their workload processes in the container user also expect this familiar debugging setup whenever they get into their get a shell entered into their workload runtime environments.

So things like systemctl and general ctl our users are very familiar with using these tools for debugging their workloads. They also expect services providing platform platform functionality to be in the same container. So in the end what do we have is a multipprocess container with systemd doing a net and service management and this is all decidedly anti-attern but in the interest of our users enabling transparent and painless migration to a more modern stack we needed to support this use case. In order to run systemd in a container it needs specific mounts configured at the OCI runtime spec level. An example is systemd that expects dominion over its own croup fs. So we mount one in the main applications container. Uh if you can just note the code over here, you can see the type here is actually croup. This type of low-level mount configuration is simply not available in the pspec. So this is an example of a straightforward OCI runtime spec modification use case demonstrating how nRI can be used to support unique workload requirements.

Next we have CPU isolation. The CPI isolation example here demonstrates no level runtime decision. In terms of flow, how it works is that the ties control plane sets a CPU tier via pod annotations uh dictating whether the particular workload should use exclusive CPU or shared CPU pools. The NRI plugin then removes a cubit's default CPU settings and delegate to a no local service that we call Tus isolate. Titus isolate is responsible for tracking CPU allocations. For the exclusive tier, it assigns dedicated CPUs via CPU sets. For the shear tier, it signs quota and a shared pool depending on the workload priority. This differs from the CPU uh Kubernetes CPU manager which only works for pods with guaranteed QoS settings and static policies. Our our approach supports multiple workload tiers and decisions at the container creation time based on what's currently available on a node. We cannot determine this at pod emission time and I instead is able to call out to a no local service with full visibility into this current CPU allocations and the topology of the actual node.

Next, I'd like to share how we run these platform functionality services. How we run them today is also an anti pattern we're lifting and shifting. We run per pod platform services as systemd service instances on the host for things like an instance metadata proxy service secret management metrics and logging. We have individual platform service uh instances for each of those. The NI [snorts] plugin is responsible for managing life cycle of these platform services. They handle handles setting up volume containers for pulling down these actual binaries and starts the actual systemd uh service instances and [snorts] these instances exact directly in the container's name space. Uh after we confirm that the platform service has fully started then is also responsible for telling the workload in the container that they're actually able to run. In terms of the uh shutdown path we leverage OCI hooks. OCI hooks is responsible for starting stopping the systemd service instances when the pod actually terminates [snorts] in the long term.

We want to move these to containers which is more aligned with the cloudnative approach but extension points let lets us preserve the existing pattern during migration while we build the foundation for our new modern stack in a transparent manner for our platform users. We can evolve towards psychards incrementally. Now let me show you how all these pieces come together. >> [snorts] >> So here are two diagrams showing the application life cycle orchestration. We orchestrate both at the pod startup and tear down levels through extension points. For startup, we inject tiny a minimal init system that also customize into the entry point for each container. Containers are created with tiny as pit one and configured to pause the application on startup. This gives us time through NRI to wait for all the podiers to be created, attach to storage, start platform services surfaces and do all the necessary leg work to actually get the workshop workload bootshaft. Once everything is ready, then we use nRI to send a signal to tiny through a socket to [snorts] unpause and hand over to the container's actual entry point.

For tear down, when a container stops, we use OCI posttop hooks to handle all the cleanup sequence. We detach the storage. We stop the platform services as mentioned before. We remove the container from CPU isolation and we we clean up resources like volume containers and directories uh that we all need for all the setup earlier. [snorts] We do clean up almost exclusively in OCI hooks rather than NRI because OCI hooks are guaranteed to run once they're part of the OCI runtime specification and run C has created a container. In contrast, the NRI plugin has to be up and running to be handle events actively and there's always the potential for miss events if the plugin is not available. This orchestration pattern of using Tini for coordination, NRI for setup, OCI hooks for tear down is how we compose all these extension points into a complete system for our new runtime stack. Next, I like to share some guidelines uh that we use ourselves for kind of deciding when to use each extension point. So for emission web hooks, as mentioned earlier, we use it for modifying the posbacks before scheduling.

for things like injecting platformwide configuration, enforcing policies or setting resource codas that doesn't need runtime decisions. They all run at the API server level before any node is actually involved in NRI. NRI is for used for container runtime coordination. We use it whenever we need to modify the container's OCI runtime spec at runtime. We orchestrate container startup sequences. We make decisions based on the actual state of the node. For OCI hooks, we use them for very simple life life cycle event operations and for things that we actually need to modify the container uh container directly for. Uh they're great for cleanup when containers stop or any pre percontainer operation at life cycle boundaries. They're stateless, they're event driven, they're guaranteed to run because they're part of the OTI runtime specification. The key insight here is being able to choose the right extension for your particular use case. Next, I'd like to share some experience of us running these uh runtime extensions at scale in production.

So far, we've successfully migrated to this new architecture at massive scale. We've been able to migrate the vast majority of TI's workloads to a new run stack in pretty short amount of time, and I believe by the end of this week, we'll probably be about 100% on this new stack. There has been zero major incidents from the migration itself, which is a huge win for such a fundamental infrastructure change. We also now have better observability. We can integrate tracing across all these different components. The separation in all these distinct extension points make it much easier to understand what's happening when things actually go wrong. Maintenance is significantly simplified too. The clear separation of concerns make the codebase easier to understand and modify compared compare uh compared to the old monolithic esoteric runtime layer that we had before. Beyond proving the extension point uh approach uh works, the broader migration to uh stock cubits and cont has been great for us. We now have access to alternative snapshoters for faster image pools and now we also have the space to innovate and opportunities to cut back upstream and participate in open source uh container ecosystem.

We did have some challenges along the way. The first thing here was the NRI plugin availability problem. If a pod actually gets scheduled to a node when NRI is down, they will not actually get any customizations. And since cubits does not know about NRI, it'll happily just support having that pod running on a node. We solve this by using a node problem detector that watches the NRI systemd service on the node and taints it as unschedulable if it ever goes down. We also ti tied the life cycle of cublet to both nrii the nrii plugin and containd so cublet won't start accepting workloads unless everything is ready. The second challenge here is that we ran into an interesting issue with a global lock within the container's nri implementation. Contin's ni implementation uses a global lock which means it can only process one RPC at a single time. This is understandable since it gives a strong guarantee that events will be processed in the order they're received. But our Rscale with thousands of pod events happening when we when we're quickly binacking worker nodes has caused significant delays.

As you can see in this picture here, we show a trace trace that shows how much how long it took to to do a stop pod sandbox operation. Uh so that span shows 9 seconds and of those 9 seconds you can see the ARI lock span takes up a full 7 seconds and this was during very during a very busy time on the node when we're scaling 96 pods on a 96 CPU machine with thousands of operations happening at the same time. We worked around this issue with a custom patch that enabled per pod locking instead of a global lock which allowed us to process different pod events concurrently. We've opened an upstream issue to services this problem and discuss potential solutions with community and actually we actually uh recently actually opened a PR to kind of propose the solution for this as well. So here are some key learnings from our migration journey. First thing we learned was to balance pragmatism with platform purity. Extensions like NRI and OCI hooks let us meet Netflix unique workload requirements. things like multi-tenency thick containers that run systemd that pure kubernetes patterns cannot handle at least without needing having the need to fork the key here is migrating existing workloads transparently to first maintain trust with users which builds the foundation for us to then iteratively move towards the more standard patterns over time second our lift and shift strategy was crucial for reducing risk by reducing existing code to extension points before rewriting we could validate the architecture with real production workloads.

This gives us confidence that the new data plane worked correctly before we can actually we actually decide to make improvement iterative improvements. It has enabled a seamless transition that users did not even notice. Third, observability. Observability was absolutely crucial. Uh with such a distributed system spanning, cublets, uh containerd, nrii plugins, OCI hooks, having endto-end tracing was essential for debugging issues. My colleagues Ardum and JP gave a talk earlier called where's my pod that goes very deep into how we build the reservd across the stack. If you missed it, I highly recommend watching it before uh after bitcon's available online. It's a great companion to this talk. So here are some resources and links. I have some reading material on documentation on uh NRI implementation containerd uh the OCI runtime spec and I also share two blog posts here. One is uh for an issue that we ran during the modernization project uh that's not very uh that's not related to runtime extensions but actually containerd how it actually does username space support in terms of scaling mounts if you have an image with many layers it just causes you know the kernel fallover has just like binpack 96 pods um and and and then also the blog post on complex migrating complex system infrastructure it's also linked there on the right side uh I have a sampling of different contributions that resulted from this uh modernization effort that we made to upstream.

We have many many NRI PRs open uh and and actually merged and we also have some community ones that has been um great showing how like we've been able to kind of coordinate and uh collaborate with upstream maintainers to solve some of our issues. Uh next I'd like to give a shout out to all the contin maintainers uh Christian Antonio uh Sam Karp, Mike Brown and and many others for their guidance and helping Titus start embracing the cloud native ecosystem. And with that um that concludes my talk. Uh thank you for staying through and uh here are here are some QR codes. So on top one please give me feedback if you have feedback and the bottom here uh we're hiring. If you if you think this talk was interesting, please take a look at all the job listings. Uh, thank you. All right. [applause] And I'll open the floor for questions. >> I guess uh >> Oh, sure. Oh, go ahead. Sorry, can you speak louder? >> Sorry. >> Okay. >> Yeah, I think it's um as long as it's expressed. Oh, sorry. The question was Sorry, what was the thing that you're trying to add?

>> CD. >> Okay. CI devices. >> Okay. >> Okay. So the question was uh is there a way for me to kind of add CDI devices to the posback or to the container uh through through this these mechanisms. So I guess like since I'm not really familiar with what that is but as long as um there are ways to kind of express that in the OCI runtime spec then you can use inner eye to kind of plug those in or if it's actually device and then then there might be things you can do to kind of directly kind of share those devices with a container at the OCI run hook level or inner eye. Uh it also all depends on the life cycle hook that you can actually use. For example, if you need a container to be created, you can use NRI to kind like interact with it. OCI if you need to. Yeah. Okay. And let's see. Uh this gentleman over here, I'm not sure. I guess I'll walk over. [laughter] All right. >> Yeah. >> Appreciate sharing the experience of Actually I do have questions in recent I do see that you have an example with right you guys right have the same issue with meta and I think in the recent version of like continity 2.0 something like that and the new version of you can also configure like doing some extra hack.

We also struggle with that. >> Yeah. Oh, thank you for sharing that. Yeah. Uh that's all part of the modernization effort just so we can actually leverage you uh uh solutions like that. So, thank you for sharing that. Uh so uh rather than a question it was a gentleman actually sharing a a great solution for kind of being able to modify croup profess through uh runc interfaces in the in the newest release versions. Uh over here >> hi uh thank you for the presentation. Uh first >> I'm wondering if there is anything any of these available open source or somewhere for me to replicate because yeah I'm more or less in the same situation of getting out of EC2 into Kubernetes. >> Yeah. So um right now all of this code is mostly uh I got you zero I'm out of time but um I can just stand on the side here if you like and then we can talk about it. Uh thank you very much again. Thank you. [applause]