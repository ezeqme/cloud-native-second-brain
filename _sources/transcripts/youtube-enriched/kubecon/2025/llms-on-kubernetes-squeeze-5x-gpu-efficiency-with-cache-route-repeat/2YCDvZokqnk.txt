Awesome. Um, welcome to the talk uh, LLMs on Kubernetes squeeze 5x GPU efficiency with cache route and repeat. I know it's the probably the last talk of the day. Uh, thanks. I'm really grateful to everyone who showed up here. All right, cool. Uh, let's start. Uh, I'm Suraj. I work as a principal software engineer at Microsoft. Um, and yeah. >> Hi everyone. I'm Yuan. I'm a fifth year PhD student at the University of Chicago working on system for machine learning. >> All right, cool. Uh before we talk about any efficiency or anything like that, let's first like get some basics clear, right? So what's what's inference that the diagram that you see it's just a placeholder I I would say like you'd see there's a client a pod running on node and that pod is occupying all the GPUs there is a sample request going back and forth so uh okay so what's inference right inference is uh just a forward pass of the of the model so there are no gradient updates just just the prediction and it's it's like it's like running the any other micros service on on Kubernetes right u instead of instead of just uh handling API requests what it's doing is it's there is it's generating text token at a time and as each request like grows u as in when the output grows sequentially it's uh the latency also accumulates and then that's that's the whole generation process and uh since each request is like long running it it's behind the scene it's also consuming the GPU memory that's why like all the GPU efficiency matters and the the the analogy that I like to give is uh think of think of uh model training like building docker images and serving like using those docker images on on on kubernetes so and and before we understand uh like how to gain the efficiency.

Let's see what metrics we are trying to optimize. One is the throughput. Throughput is token per second of whatever the deployment system looks like. So we are trying to maximize that and the other is time to first token. Uh we are trying to minimize time to first token. So if you ask what time to first token is you know how you like write a prompt in chat GPT and then you wait for some time before the the first word shows up. So that time between sending that request and waiting for the first word that's the time to first token. Um we'll we'll we'll see more there have been a lot of talks that have gone into the depths of each of uh like what these metrics are. Uh I have a slide at the end that you can you know take a picture later look at the recordings if you have not attended those. All right cool. So uh what are the various serving engines out there? So VLM by far is the is the most popular one. uh like all the open source models that come out they come out with like support of VLM out of the box.

So people when like because this is the this is the inference engine uh that since it supports everyone try everyone uh gives it a try. The other one is SG lang and then there is one from Nvidia Tensor RT. This is by no means a exhausted list. Uh I I'm pretty sure there are more but I think these are like more popular in the cloud native ecosystem. So okay before uh let's let's look at from a client point of view like what really is happening uh when you send a request to a inferencing engine. So what you usually have is like a chat window you type a prompt but behind the scene what's going on is a JSON like this is sent to the inference engine. So this is like uh a lot of details have been removed from this but uh I'm just giving you uh like like what you have typed and what shows up in the JSON you get some response uh and then you send another request but when when when that when you try when you type another prompt like everything in that context is sent again to the inference engine. So you can see how every time the chat increases this JSON is growing like more and more.

So keep this in mind because this is one of the crux of how how we do optimization. Okay. And you get some response that's just placeholder just to show you that it works. Okay. Um okay. So like what what happens from the server side of uh the picture, right? So there is GPU memory involved of course and right off the bat like when you uh when you start any inferencing engine it it first downloads the model weights loads into GPU memory so that's non-negotiable you cannot get around this like you have to load the entire thing uh into GPU memory it has to stay there it's always resident and then the inference engine also configures some space for storing its KV cache and this grows dynamically as more requests will come in but it already uh like assigns certain amount of space. So and then there is some space uh left for like other types of uh overhead system overhead. So you can see like this is and these numbers are by no means uh you know uh representative of anything. It's just to show you that you can see like a ballpark uh assignment of GPU memory like this.

So uh let's see like a query like what happens like in a query's uh lifetime right uh so the like when you send those when you send that prompt uh like all those tokens are loaded at once and while that while the while the inference engine is processing those uh tokens it's generating something called as KV cache we we'll look a bit deeper later what KV cache is but understand it's some metadata that is generated that that that is then used for the decode phase. So until that decode phase start and from the time you send that request that's the whole time of time to first token that I mentioned earlier. So decode starts like one token at a time and it's using that KV cache that was generated during the pre-fill phase right. So that's a single query in in action. And to give you some high level overview of KV cache, I mean uh I'm pretty sure this is not exactly accurate. But you can think of uh each word like a a token here. Again, that's also not accurate. Each word could be a token or could not be.

It depends on the tokenizer of the model. But think of each word as in this example as as one token. And with with uh each word there is associated key and value metrics behind the scene and that's generated per transformer layer. So this is being generated in the prefill phase and the decode phase then uses this right. So so this is this is going to be one of the uh main topics of how we optimize u inference. So the challenges of vanilla approach right like like what whatever we saw so far that's that's vanilla there are no optimizations uh here so it all of this is fine when there are few requests but as concurrent requests show up the and long prompts show up like we saw before the prompt gets uh you know uh it it grows quickly uh the GPU KV cache also starts to fill up so when that happening the the inference engine has to decide okay like what KV caches are not relevant for particular request I'm serving. Uh it will it will remove all those KV cache that are not relevant. So and if the same request comes again then it has to process the entire uh request that you that you sent.

It has to generate that KV cache again. Uh that that work continues. So the the downside is the time to first token increases and GPU is redoing the same work that you have already that it has already done. So uh this inefficiency like it it it compounds really really fast. So u so yeah uh so how do we reduce this extra work right? One of the ways is like you can move that KV cache somewhere or load that KV cache from somewhere. You can I mean do both. Uh so you can use like tiered storage right? Uh like GPU being the fastest its memory and then CPU memory and then uh a disk or a remote storage. So u yeah the idea is that use GPUs for active generation and not as a storage like use something else for storage. Um and yeah this this point I I wanted to make like in computer science we keep seeing these problems right like uh caching is not nothing new uh we just revisit it resolve it at a different layer we have been doing this for a while now um so similarly we'll we'll be seeing how to do uh KV cache offloading so so yeah the idea is uh instead of recomputing re uh do the reuse so how do you do it uh for you know in the in the context of LLM inference So that's where LMCH comes in.

Like I said, uh there have been other talks that have gone into the depths of LMC as well. Uh I'll I'll I'll like show you at the end. Uh so very quickly what what is LMCH? Uh it's a open source project. It acts as the caching layer uh that sits between the inference engine and various storage including GPU if we like GPU's high bandwidth memory. So it extracts those KV caches that are uh normally in the GPU memory and then stores it to wherever you have configured it to. It could be CPU memory uh remote like local NVME desks or remote storage. So and and it has this tiers where like all the warm caches or the hot caches will be in the always kept in the GPU memory. the warm ones could be configured to skip in CPU and then remote ones and uh it it today it works with VLM and SGLAN uh like you can uh use with both inferencing engines and so the whole idea is that since you are not recmputing it you're bring it from somewhere you're uh you all the redundant requests don't happen hence the prefill becomes really small as in like time to first token becomes uh really small And then that's why you get higher throughput.

We we'll see those uh results benchmark results as well. to give you like a higher level overview like this could be a mental model you can think of where there is uh inference engine like VLM or SGLAN and at the bottom there are various storage tiers and LM cache sit uh cache sits in between like it's transparently doing loading uh like offloading those caches to other memory and then uh loading it back in when they're needed. So u yeah uh for further talk or further uh benchmarks and other stuff I'll I'll let Yan take over. >> Okay uh thanks Raj for giving the high level overview of what on cache is. So um now I'll talk about some real benchmarking results that shows how on cache can uh improve the performance of inference. So here for the setup we use emulated uh multi-run QA question answering and uh so it has like 40 concurrent users and uh each user asks questions uh for multiple runs uh on based on a 10k long input context and uh for uh the models uh we use the minimum number of GPUs that can host the model weights and uh we uh here we show a bunch of results on llama 3.18B, llama 3.170B and Q1 um 32B and uh so here we enabled on cache CPU offloading feature where uh KV cache will be offloaded to uh CPU memory whenever it does not uh feed in GPU memory and uh so here um next uh next um we let's look at the results on llama 3.1 be instruct And here in this figure the x-axis is the QPS which is basically the arrival rate of the uh request and yaxis is the time to first token which includes the time to uh prefill the new query and the time to load uh KV cache uh from GPU from CPU memory back to GPU memory.

So here this is where uh the latest VM is at and uh this one is uh by default enables VLM GPU pre caching and uh this is uh VM native CPU offloading and this is where cache is at. So here we can see that uh at the same level of TTFT alon cache can improve uh the throughput by more than 2x and on this figure I'll show uh QPS versus ITL which is basically the time between uh each consecutive output tokens. So this basically measures the latency for each decoding step. And in this figure, this is VLM. This is VLM's native uh CPU offloading. And this is on cache. And as we can see here, we can alach can both improve TTFT and ITL. So here I'll show a result on a a larger model on llama 3.1B. And uh in this figure, this is VLM, VLM, CPU offloading, and cache. So here we can see at the same level of TTFT uh on cache greatly improves the throughput by 5x and similarly here uh impro in terms of IPL um this is VLM VM's CP offloading and on cache and under the this scenario on this larger model on cache can also greatly improve the performance.

So uh I also here I'll show the results on another type of model which is a Q1 uh 32 billion model and uh on this figure as for TTFT uh this is VLM VM CPU offloading and on cache on here at the same level of TTFT we improve the throughput by 3x and similarly in terms of ITL uh this is VLM VM offload CPU offloading and al cache. So just to summarize uh al cache can improve uh greatly improve the performance of all inference uh across this uh different sizes of model and different types of models. Okay. So um by far we have seen like how on cache can improve the performance for a single instance of um all inference. So what if we have a distributed inference setting and by that I mean like if we have multiple replicas serving the same same model. So the key question here is how to determine the right replica to send the query to and by that uh I mean for example we have a user which send request to uh say like there are three replicas serving say llama 3.1A. So we need to determine which replica to choose uh for for this type of request.

And uh to determine this we need a request router that you can uh smart ideally can smartly determine uh where where the request should be routed to um among all this replicas for the model. And since we have our own cache, we can store our own c uh kv cache in uh like say the local memory of each replica. So here it adds another level uh of consideration in this router. So with that I want to briefly introduce KV cache aware routing and uh for KV cache aware routing for example the user first sends uh request number one and uh the router randomly picks a replica to send uh the request to and for example it sends request to replica one and then on replica one um the LM generates a KV cache of it and it stores to the um local memory uh managed by uh Alon cache on this replica one. And uh next time for request number two um it has a prefix match with uh request number one. And so now what should we do? So this uh router here for KV cache over routing will um will see like which replica has the information uh has the cached KV and uh with this you need to enable we uh we'll enable the KV cache manager uh written in cache and basically this manager keeps the information of which location the cache uh the KV cache sits in and then the uh KV cache manager of cache told the router that it's uh the KV cache is in replica one.

So the router send the request to uh replica one and then the KV cache will be loaded uh from the uh local memory uh to the GPU memory of replica one. So with this the TTFT can be largely reduced because we can reuse the repeated computation um that happened before on this replica. So uh this features uh this KVRware router uh is uh enabled in another of our open source project called VLM proion stack. And so VLM proion stack it's a open-source uh project for Kubernetes native uh VLM deployment. And uh so it has all the features such as smart routing logic uh and uh autoscaling based on LM specific uh metrics such as load uh and uh KV a uh KVL awareness. Yeah. So uh with that I want to show a demo for KV aware router. Okay. So, um, wait. So, how can I see? >> Maybe you'll have to like look back and go. >> Okay. >> Starting from the I think it's full screen. I think this is fine. Okay. So in this uh demo I'll show KVLware routing. So first you need a uh you'll need the helm chart value to specify the deployment and here we are using uh GBOSS 20B and uh we run we deploy on four replicas and here in the router spec you need to specify KVLware as a routing logic and then you can do uh helm install to start the deployments.

And you can also uh use cool control gap pods to monitor the status of the pods. So here I'm accelerating it but um now you can see that the pods are ready. Okay. And we can monitor the uh logs, the live logs of the router pod. Okay. Now we do a port forward to enable requests to be sent to the router. And now we send a request uh to the router asking like what's the capital of France? So here um in the log you can see that there are lucum message uh to query the on cache KV cache manager to see if the request the uh KV cache for the uh for the input prompt has been uh stored in any of the instances before and since uh this is a new deployment there hasn't been any instance uh process the KV cache before the return message is empty. So next uh let's send this request again. The same exact same request. Oh, by the way, the request uh is routed to this IP address. Okay. So then we uh send the the request again. And what we can see here it's uh performing the KV cache lookup. And here in the return message we can see that um the KV cache manager returns uh the instance name that had uh has a KV cache for the input prompt.

Uh so the uh this is the instance name and it also returns like the the story device that stores the KV cache for the input prompt and it returns the number of uh KV cache the match uh tokens uh in the in the back end. So uh now the router based on this information routes a request to the same exact same IP address as the first request. Okay. So with that that's all about the demo and yeah I think it's right here. Okay. Um and you know how stretched part shows like uh the prefill and decode phases can happen in one pot. Now I'll briefly talk about prefield decode disarrogation which is like you can separate uh these two faces on different GPUs and this has been proven to uh improve the efficiency of uh our inference as well. So with that um so if the user send a request to a router and uh the router will send a message to tell the prefiller engine to start the prefilling phase and then the prefill engine will uh generate the KV cache and send the KV cache to the decoder engine. Then the prefiller engine will tell the router that uh the KB cache is sent and after that uh the router will send a message to the decoder engine to let us start decoding.

So um the decoder engine uh it will use the KB cache generated and transferred from the prefiller engine to uh do decoding. So uh this PD disagregation allows prefill and decode phases to be scaled separately and this can achieve much lower SL violation rate. So uh by that I'll show a demo for uh PDD surrogation as well. Okay, so this demo is also recorded with uh BLM production stack and to to uh start the deployment you also need this helmchart values and we can do helm uh upgrade or helm install to start the uh deployment. event and we can monitor the status of the pods. Uh and here uh we need uh we have three pods uh one for the router, one for the uh decoder instance and uh one for the preiller instance. So uh let's take a look at the helm chart value file um to see uh the configs. So basically we'll need uh separate configurations for prefiller and decoder instances. And for example for this prefill call llama prefill um like the the most important thing is like in the on cache config in the llama prefill uh pod you'll need to specify the KV row to be KV producer and what this means is that uh it's a prefill engine and it will generate the KV cache and transmits the KV cache to the decode uh engine.

So we use Nixo to do this KV trans uh transfer and uh this uh PD lama decode engine service this specifies the uh destination uh of the KV transfer which is the decoder engine. And for the llama decode uh pod we also similarly we need to specify the um alon cache alon cache config we need to specify the role of the engine which is a KV consumer and this means that it will take in the KV cat transferred from the prefer instance and do the decoding. So uh and it also has a config for nixo and in the router config uh the router spec you need to specify uh disagregate prefill as a routing logic. Okay. We can uh use cool control get pods to see the status and uh we can also um get services to see uh the uh started services and this is needed uh for both prefill and decode because the prefiller engine need to transfer the KV cache to the decoder engine via this service name. Okay. And we can also check the log. Uh this is a decoder log and this is a prefiller engines log. So we can see that the uh pod has been started and uh it's ready to take request.

Okay. So you can see that they are all ready and uh in the router part you can see the log that discover uh new serving engine parts uh called llama preill and llama decode and we do a pull forward uh to send requests to it. Now we send a request uh to to this uh router pod. So you can see that in the router it shows like the time to um the time at the prefiller engine side and in the prefilled uh part uh we have the log that it shows like uh it stores uh the KV cache uh for uh all the tokens in the ion cache back end and it uses uh Nixo to uh transfer the KB cache Um and you can also see the latency for that. And let's take a quick look at what the code uh p look like. So here you can see that it retrieves the uh 10 tokens KV cache from the uh cache back end and uh it's sent from the prefiller engine. Okay. and back to the request. So you can see that the request um I it's uh successfully finished. Yeah. And so this is a demo for uh uh PD disagregation in production stack. >> Yeah. >> Yeah. Uh we have >> Sorry, go ahead.

No, I was going to say uh the first the first talks about LM cache in like in depth. So you should check that out. The second one talks about how uh inference gateway can be used to do prefix matching. And the third and fourth goes into the benchmarks of it. So yeah uh that's our talk. Thank you for coming. If you have questions, I guess we'll we'll be here. Yeah. Thank you. >> Thank you. >> Yeah, good question. So the question >> pick the question. Yeah. >> Uh yeah the question uh is um how does cache uh improve if we apply on cache on other types of infant engines. Um so the answer is we actually have some uh benchmark results on cache integration in SGAN and it's in our uh technical report. Um I can share the paper with you. So u uh on that results we have found similar like improvements in terms of like the uh inference throughput. So um yeah does that answer your question? >> So which um based on your experimentation which engine is the best for performance right? >> Uh you mean like um comparing the inference engine itself performance >> correct?

um >> using curious. >> So uh it's a question comparing like the the two inference engine uh the many inference engines itself's performance or like asking are you asking like how um like which one uh on cache has the best in performance >> which one has the best performance >> for on cache. >> Yes. >> Okay. Uh so um I would say like for uh VM we have the best uh performance and uh but for SGNA we also have uh performance gain compared to uh it's like uh vanilla SGAN um and we are on par with uh SGLAN's um native CP offloading. Yeah. Okay. Thanks for the question. So um if there are more questions we can talk offline. Yeah. Thanks everyone for coming to the print uh to the talk.