Okay, hello everyone. Thank you very much for being here and especially thank you for sticking to the not only the very end of the pres of the conference, the end of the last day and for choosing to come to this session. Uh lay back, relax and allow me to tell you the story of how we go from code to cluster in my organization. My name is Andrada and for the past five years I've been working with ING Hubs Romania as a DevOps engineer. To clear a bit things up, what is ING Hubs Romania? We are part of the broader Dutch ING group which is a global bank with a strong European base. The hub in Romania where I work at delivers solution in software development, data management, compliancy, non-financial risk management to other hubs in to other units of ING worldwide. I'm not going to tell you about that. I'm going to tell you about the pipeline. Uh the agenda for the next half hour is going to look like this. I'm going to tell you about the context, what was happening in the organization around five years ago and how we got here.

We're going to look at the solution layout, its building blocks in how it actually looks like. Uh I'm going to tell you about some of the features we implement and some of the ones we built. Second to last, we're going to look a bit at the risks and how we dealt with them. And finally, you will see a recorded demo of the pipeline because I lost my face in the demo guts. I'm not taking any chances. Hopefully, we're going to have time for a few uh Q&A for a few questions at the very end. The context. It was 2019 when inside our organization a technology shift was happening. The stack consisting of GitLab, Jenkins, TFS, and Enible tower was being replaced with Asia DevOps and Open Shift. Due to this applications were being migrated from VMs to containers. Uh before this engineers will would create the pipelines however they saw fit but they were respecting some guidelines and policies. A need for a more standard deployment approach was identified. To take advantage of this a small group of engineers collaborated with the developers of a tier zero application from ING to create a P.

The idea to create the Kings Road pipeline was born then. If we were successful with a very complex application like that one, we would be successful with all of them. The next year in 2020, the tier zero application went live in production in ING's private cloud using the Kings Road pipeline. They became our main promoter and soon other applications and teams in our department were showing interest. In 2021, the word spread to other countries in ING as well. We soon got to markets like uh Spain, Poland, Romania uh and the Philippines. Similar initiatives were happening at the same time in those countries, but we were the first ones to go live in production. The next year, we kept growing organically in the organization. However, the number of deployments was being reduced to focus more on the resilience and reliability of how those deployments were happening. But those were things the Kings Road pipeline was already offering. This was a catalyst that allowed us to have a much bigger growth in the following years.

In 2023, the pipeline became mandatory in our tribe. This allowed us to grow to 1,000 on boarded applications the next year. Sorry, wrong button. And finally, we are in 2025 when we started refactoring the solution to employ a more global approach. So, what is the Kings Road pipeline? It's a set of templates developed for Aszure DevOps. Its goal is to automate the CI/CD process of applications using the ING internal SDK and the service mesh. To give you some a bit of a perspective, there are around there are a couple thousands applications in the service mesh. I have a nice chart where I'll quickly show you how we grew in the last five years. Started with one consumer in 2020, around 30 in 2021. We reached over a 100 the next year, 400 in 2023. We skyrocketed to 1,200 in 2024. And finally, today we stand at around 1,600 on boarded applications. The solution layout, the actual flow is the standard one. We build, deploy, and test. For building, the pipeline is tailored around Spring Boot applications. To use the build templates, you must package your application into a fet jar.

We then rely on Maven to do the heavy lifting and actually build the application. Additional to this, we need the configuration files in a special format which we call the configuration zips. We have a the next slide is dedicated to those files. After this, we'll building the Docker image. We start from an approved and vulnerability free image base image from the infrastructure team. inject the jar there, scan it again, of course, and push it to an Asia container registry. The deploy template is plugand play. It can be used with any kind of application as long as you provide the correct input, a vulnerability free Docker image and your configuration files. Here we employ a continuous delivery approach rather than a continuous deployment one, so we still have some manual validations in place. Lastly, the test templates are similar to the build ones. The tests must be packaged into a fet jar. They are then released in open shift to run and the results are back in the pipeline using a feedback loop. Our developers have access to an additional testing tool which we call the conformance test kit.

This is an internal tool which can be run both in the pipeline and locally and it can mimic the production environment so developers can better test their features. It can benchmark your application, run performances and some other features. The configuration zips. This is a very important object from our pipeline setup. We use Helm to do the deployments in Open Shift. This allowed us to impose a configuration standard that is easy for the consumers to create and for us to use. So as a consumer, you have to provide the following. a zip archive containing a directory for storing certificates and key stores, a directory for storing application configuration files and the values YAML file where you have some flags available to configure. To complement the Kings Road pipeline offers you the templates directory which contain every uh template for the Kubernetes objects you're going to need for your release and the chart information. Y of course because we use Helm we can deploy and control in isolation the application release uh common namespace objects certificate release chron jobs sorry this effect was much uh was sped up when I last rehearsed this monitoring and autoscaling releases two deployment strategies are available for our users the first one is rolling update this allows a user to roll out a new version along alongside the old one, switch the traffic to the new and clean up the old.

The second one is the canary release. This is the default strategy that we impose to our users. This strategy makes use of a baseline, which is a fresh instance of your old version and the canary, an instance with the new version. During the release process, traffic is split between the old version, the baseline, and the canary. We do this to assess the behavior of the new version compared to a fresh replica of the old one. Uh this ensures a clean slate for teams to properly compare the metrics performance and other indicators of how the new version behaves. Let's say once you have the technical go live, the canary is scaled up, the baseline is cleaned up and the rest of the traffic is switched from the old version to the canary. Lastly, our canary is promoted to live to running version and the old version is scaled down. Some of the features we have and we implement part of the pipeline we have configurable features um sorry default features and configurable ones. Looking at the default right now at Kubernetes deployment object level we enforce the deployment strategy on rolling update with a max unavailable of zero.

So we don't have any downtime. We make sure you have the affinity rules are in place to accommodate the HA configuration of ING's private cloud. We also make sure you have all the needed network policies to actually connect to the service mesh. And lastly, we also control the image pool policy. As a user, you can configure your ports, livveness, readiness, and startup probes. You can use chrome jobs, multiple containers. You can use the horizontal pod autoscaler and the custom object which we call the kota autoscaler. Has anyone from you heard of the kota autoscaler CRD? Okay, perfect. Let me tell you a bit about it because this is an open-source custom resource definition created by ING. Its purpose is to automate the flow that engineering teams had to perform which was the scaling of the namespace. So in the past to scale your namespace you had to go to a resource management portal follow some steps and wait for the namespace to be actually scaled up. If you needed to scale down you needed to do that all over again.

With the code autoscaler you simply add a configuration in your pipeline. This is how you do it for the Kings Road pipeline in your values file and this gets deployed in Open Shift. This is how the actual manifest looks like. The one that gets applied in open shift. Um based on memory and CPU utilization, your name space is scaled up or down. Other cool solutions we implement part of CNCF ecosystem. Prometheus monitoring of course because we all want observability and metrics and we want to see them in the graphana dashboards. Native Helm roll back because we want our pipeline to be reliable. Automatic certificate management with search manager because we are tired of approving manually all of our certificate requests. Open policy agent to run policy as code because we don't like doing them in the GUI. And lastly, anible releases because we also have consumers who are still on virtual machines for one reason or another. We couldn't have gotten to 1,600 on boarded applications without supporting some custom use cases.

Let me tell you a bit about some of the features we built for those users. The first one we call autopatching. So every 30 days the application image needs to be renewed even if there are no real application updates. Of course the our engineers reported this activity as toil. uh to ease their work. We developed this feature which does exactly that redeploys your fet jar using a brand new uh base image and ships it to production. The next one is the configuration only deployment. So incidents might happen because of configuration changes and most times the same incidents can be mitigated by another configuration change. To do this going through the traditional flow takes uh lot a lot more time. So we created this feature that allows you to rebuild only the configuration zips, takes your latest production image and ships everything to prod application bundles. Teams can build and deploy applications of different flavors and different configurations and the same name space at the same time. A standard playbook for our ensible users they can use as a starter point for when they want to start using the Kings Road.

And lastly, a custom docker base image. As I mentioned, our goal is to be used by every application inside the service mesh. But the beauty in be of being in a global organization is that not everyone is using the service mesh and not everyone is using Java. So to broaden even more our sites, we made available the usage of a custom docker based image for applications which are non-Java. Almost to the end, we are going to I'm going to tell you about the risks. Some of them I would define our organization as one with a very strong risk culture. Staying in control of the risks and threats your own applications can create is essential for a reliable environment. We contribute to this by enabling several checks and validations in the pipeline with Python. We make sure your application resources and limits are equal because we don't want Kubernetes killing off your ports because they're consuming too much memory. We make sure the deployment branch you use is a stable one, a main or a release one. We also make sure that the last merge on that stable branch is approved by a secure code reviewer.

And lastly, we you validated that the base image you're using is vulnerability free and approved by the infrastructure team. Because the happy flow isn't always the case, deviations might appear. Let's say that you're in a major incident. The SLA is almost breached and you have to deploy from your bug fix branch, but that's not a stable branch. So your pipeline will fail. For this, we enable the break the glass mechanism. It's a simple manual validation where you give a justification for your deviation. And of course, all of your stakeholders get informed and then you'll have some emails to respond to. We don't only use Python to uh do checks and validations. As I mentioned before, we also use open policy agent. Using Python, we created our own assertion framework to enable us to correctly run policies written in Rego natively in the pipeline. With OPA, we are verifying the internal SDK version you use. So, it's still supported by our team, by the team that develops it, sorry. We also check the Kings Road version you use because we also have a strict support window.

And lastly, we look at your application configuration files. We expect teams to use certain properties to mitigate possible security risks. And lastly, it's demo time. Cool. I'll start by showing you how the actual pipeline looks like. We're going to start at the top with the check marks code and the dependency scan. Okay, now it's fine. Starting at the top with the check marks code and dependency scan. This is one of the tools we use for scanning our code. In my case, it it failed. Uh, by the way, if the colors looked messed up, it's because I inverted them. I know that some some of the other speakers who use the dark mode, it wasn't really visible on this. So, the colors are inverted. That's why it looks a bit shady. In my case, this stage failed because I'm using a moch application. I don't really care about my dependencies right now. It's just for the sake of uh the demo. The next stage uh I will show you is the building and publishing one. This is the one that actually builds the fet jar uh your application and then it publishes it in the pipeline so it can be later consumed by other stages.

The one after that is the one that builds the docker image or in my case the docker images. I'm also releasing a chrome job so I need an additional image for that. After everything is scanned, it's going to be pushed into an Asure container registry. And lastly, the the big bad wolf from this pipeline is the deployment stage. I called it like that because it has 28 jobs. I know it looks intimidating and a lot, but they all are very valuable. So we start by looking in the name space to see what exactly we have there at what version we're running, what components we have. Then we do the canary deployment of the baseline with the old version and the canary with the new version. That usually takes the longest. And then we start switching the traffic from your uh old version to the baseline and the canary. This is where also the canary analysis done by the teams should happen. Let's assume that this was done. All of the routings were approved and once we reach that technical go live I told you about before, we are scaling up the canary and cleaning up the baseline.

Okay. After this, I'm switching the rest of the traffic from my old version to the Canary one. And in the very end I make sure that I pass a very important gate which in this case is the blue one with canary duration. This is a gate that if this were a production pipeline would enable break the glass. This validates how long I stayed in the canary state. So how long I did my analysis. Um because this is test it passed. I don't care about this right now. But for production it's very important that teams perform this analysis as thoroughly as possible. At the end if once this gate is also passed I'm scaling down my old version and the pipeline is on green or purple in this case. This is how my YAML looks like. This is with the minimal amount of parameters. Of course you can add many many more but this is the minimal minimum required for it to work. The first stage here is the buildup container. This is actually a wrapper a wrapper template around the the three the first three stages I showed you initially.

So the check marks one the build and publish and the docker build and scan. We did this so teams don't have to call three templates with similar parameters. They just have to call one. And the second template is the one that actually does the deployment. I'm giving it all the needed parameters. This block right here is the one where I'm describing the environment where I want to do the deployment. I'm telling it I want to go to test. I'm telling it the data center where I want to go. Giving it the service connection, Asure subscription. An important note here, I'm telling it to start with two replicas of my deployment. And this block right here, the release components is telling the pipeline which Helm component it should release. It should deploy the application, the certificate, maintenance, monitoring. In this pipeline, I deployed everything, all of them. Now, we're going to go to the terminal to show you a bit more about that cool object, especially the Kota autoscaler. So, before I started my pipeline, I set a watch on my resource Kota.

The resource kota tells the namespace the hard limits of the namespace. And the Kota autoscaler actions exactly on this object. Um I will soon one second. Yeah, it looks at the limits of the CPU and memory. But for the sake of simplicity, we're only going to talk about the memory limits. In the very beginning, I had zero consumption. The code autoscaler is the first one that gets deployed. Then the act the deployment starts rolling out. So in the beginning, my namespace was empty. I had zero consumption because the kot autoscaler saw that initially it started scaling down the the name space. You can see that in the beginning I had 4 GB and a bit down I have 2 GB but my name my deployment started rolling out. So I started to consume resources. So I am at one out of 2 GB going uh a bit down two out of two I reach my maximum. So the quot scaler kicked in again and scaled the name space up and this keeps happening until hopefully actually not hopefully I reach the hard limit that I defined inside the autoscaler object.

Yeah I see four out of four three out of five four out of five sorry. Okay, the next next thing I'm going to show you are some of the events of uh how we use search manager to request our certificates. I cannot show you how the certificate looks like for obvious reasons, but we can quickly look at those events. So for the certificate request, I can see that my certificate request has been approved by search manager. Okay, the order has been created a bit down and my certificate has been successfully fetched from the issuer. Looking at the events uh for the certificate, I see that the secret does not exist yet. And a few seconds later, a few a few more seconds later, the certificate has been successfully issued. If I do a get on the secret, it's right there. Uh I also already know that this happened successfully because my pipeline didn't fail because obviously without the certificate my application will not start. Now this is how one of the most important bit of configuration from the autoscaler looks like.

This is where I'm defining the behavior. So the cottoscaler as I said scales the namespace down uh based on the CPU and logical and memory utilization. So if the CPU and memory utilization are uh above 65% the name space is scaled up. If they are below 85%. Sorry I'm nervous. It's the last presentation. I know I'm nervous. Sorry. So it scales up if the usage is above 85 and down if the usage is about above uh below 65 with a maximum threshold of 15 GB of memory and a minimal one of 2 GB. I also have my horizontal pod out of scalar. How many of you have used this object in your setups? Okay, then we're going to skip to that because you know what's happening. How many of you understood the algorithm? Because I still struggle to to understand it. I still read the documentation and yeah. Okay. Last thing is how easy we can do um roll back with the pipeline. In my case, I rolled back the maintenance uh release meaning the chrome job. It's here. It ran for around 33 seconds doing validations to see what's on the name space doing the actual roll back.

And if I go back to the terminal, Andrada from the demo, please go back to the terminal. Yeah. And if I do a Helm history, I see that I am at revision three, which is actually a roll back to revision one. Okay, wrapping things up. So the Kings Road pipeline is a set of templates we wrote to automate the CI/CD process of applications inside our organization which use the ANG internal SDK and the service mesh. We provide build, release and test templates written for Asia DevOps. The build is tailored for Spring Boot application. The release is plug and play and you can use it with any kind of application as long as you provide the correct input the docker image and the configuration zips. You can make the release you by using um rolling update deployment strategies and canary release. We also implement some CNCF projects like Prometheus search manager uh helm open policy agent but we also built some in-house uh features for our consumers automatic image patching configuration only updates bundle deployments and the custom docker file as I said at the beginning my small challenge for you is that tomorrow when you open your laptops think about can you implement such a solution in your organization.

Thank you for your attention. Any questions? Yeah. Yeah. Um I'll repeat it so everyone can hear. So how can a team on board to use the pipeline? We have a separate repository with a JSON file where you describe some of your configuration and some of your stakeholders that should get informed and then you just reference the templates from our central repository and that's it. It's a very good question for which I don't have the answer because we were not the ones responsible for doing for choosing it. We are making the best out of it so far. So good. >> Yeah. >> Mhm. Yeah. >> So the question is how do we allow teams to add additional steps, stages, jobs inside the pipeline and how do we govern that? Yes, we do allow the stages are not tightly coupled to one another because otherwise people wouldn't be able to use only the deploy part or only the build. Um for some of them you can add the pre and post jobs. You can add stages between them. We have no governance on them. If you add them it's your responsibility what happens to them and what is their output and input and I think one more question if anyone else wants.

Okay. Thank you so much for being here. Hope you had a nice conference and see you next year maybe. [Applause]