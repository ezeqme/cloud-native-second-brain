All right. Um, thank you guys. Thank you all for coming. So, I'm Robert. I'm going to talk a little bit about the uh, so I've been working on an open source project called Ray. And I'm going to talk a little bit about the open-source software stack that we see emerging for uh, supporting today's AI workloads. And that involves Kubernetes plus Ray plus PyTorch and VLM. And I'll talk a little bit more about how they relate to each other, you know, how they get used to with each other. Um, kind of the different responsibilities that each of these and different problems that each of these solve. But, uh, I'm going to start by trying to motivate this a little bit with how AI workloads have changed because the, um, AI workloads that we see people running today look very different from say 5 years ago. I'm going to talk a little bit about what Ray is because actually can you raise your hand if you've heard of Ray before for what about if you've used Ray personally. Okay, so smaller number. Um I'm going to share a little bit about just Ray's uh actor abstraction because I think that's um one of the things that uh well that I really like about Ray and then I'll talk a little bit about the how it fits into the rest of the tech stack.

Um so this is an open source project we started back at Berkeley actually we were doing originally not working on systems. Um, so this Philip and Yan and myself, we were or Philip and I were um doing reinforcement learning research and deep learning training research. And it became pretty clear early on that the existing distributed systems that people had at the time um weren't really designed for reinforcement learning, weren't really designed for large scale AI workloads. You know, we tried building some of this stuff on top of Spark, we tried using various HPC systems. Um, but everyone in machine learning at the time was kind of building their own distributed systems in order to run their experiments. And so we got started building tools not because our goal is to build tools, but actually uh we just thought we needed it for ourselves. And everyone was excited about reinforcement learning at the time for gaming um and you know Atari and Alph Go and things like that. And of course, um, so it's very interesting to see things coming full circle and everyone, uh, you know, reinforcement learning really being used now to build reasoning models and agents and and being so impactful.

Um, but at the time it wasn't that big of a community really scaling AI workloads. Um, and the real uh inflection point in Ray's adoption actually happened in the last year or so. Okay, so we've been working on Ray for a while, but Ray was really designed to solve the challenges that are emerging today. Okay, and I'll talk a little bit about that. So, it's really over the last year that a huge number of companies have started um using Ray and one of my favorite examples, we had uh our Ray summit, our conference just last week in San Francisco and cursor uh actually can you raise your hand if you've used cursor? Okay, so a lot of people have used cursor. So they just um built a new frontier coding model called composer and it's all post-training with uh you know using reinforcement learning to um get really good at software engineering and that was all built with Ray along with the rest of the um you know software stack for for AI. Um so that's some really impressive use cases in in my opinion.

Um okay so how is AI different today compared with five or 10 years ago? Um each individual when I think about different AI workloads I tend to think about preparing the data you know doing data processing I think about training the model and I think about um deploying the model and all of those are still happening today but in each case there's additional complexity. So in the case of data processing um we're seeing a lot more multimmodal data, video data, PDFs, images and so forth. In the case of training, we're not only doing regular training, we're also doing a lot of rein we're starting to do reinforcement learning. Um and in the case of serving, we are uh not only deploying regular models, we're deploying really large models and we're composing them together to build multi- aent systems. So each of these different uh workloads is is still around but is growing in complexity. Um and I want to go into a little more detail on just a couple of these. So with data processing the trend that we are seeing is with the the the different users that we work with um is that most of the people have always had a large amount of data right companies have been doing big data processing for a long time with systems like Spark or Flink or Hadoop um to do analytics to make decisions get insights from their data and most of the data they're working with is data that is nicely formatted in a table or in a database.

because not because that's the only type of data that um is interesting or has you know is useful but because it's the easiest to work with. Now if you think about all the other types of data in an organization that a human might consume whether that is a meeting recording like a video recording of a meeting or a PDF like some contract or all of the an email thread or a slack thread all of that um there's a ton of information and insight and signal in these types of data but they're really it's really unwieldy to work with. It's just hard to what are you going to do with a video, right? You're not probably not running a SQL query on it. Um, but we're starting to see a transition from uh or an expansion from tabular data to also all different forms of all different data modalities. But that's forcing a change in the way we do data processing because the way you work with a video is not by running a SQL query on it or or simple analytics. You're often running inference on on that um this various forms of data, right?

And the reason this is possible is that we have now we have more powerful multimodal models right um embedding models vision language models uh also you know new powerful multimodal models that let us actually get insight out of all different types of data or to structure it or to to make it searchable and the thing about inference is that inference tends to run on hardware accelerators like GPUs and so taking all of this together we are seeing a shift in the nature of data processing right in the process of getting value out of data from SQL queries on CPUs um you know on homogeneous CPU clusters to inference plus regular processing on a mixture of of GPUs and CPUs right so this is a pretty big shift um it's but it's enabling organizations to just get value out of all new type tons of new different types of data that they weren't working with before um and so I'll talk a little bit about the systems challenges that this introduces but the second workload I want to talk about is reinforcement learning now uh this is a a figure from a blog post from some researchers at UC Berkeley um we don't really need to understand all of the boxes the main thing I want to convey is that there are a lot of moving parts if you're doing reinforcement learning with regular training this um the pink box which says learning algorithm if you're doing regular um you know training a model that's that's the pink box um all the additional stuff is well so in reinforcement learning part of what you're doing is training a model and you're using frameworks like PyTorch um or Megatron or Torch Titan um but you're also using the model to generate new data to then um and then you use the new data to update the model.

So how do you generate new data? um it's actually a mixture of running inference on the model with a fra perhaps with a framework like vlm or sglang and uh that model is interacting with some environment like if I'm trying to train a coding model I may have some container running some dockerized coding environment where I'm actually the model is saying exe you know clone this repository and um you know cd into this directory and then apply this patch and then um you know run this test and so it's going back and forth forth with the model and then the environment and they're going in this loop to actually try to solve some problem that generates new data. Then the new data gets sent back to the model to update the model. Then the new model gets sent back to the inference part to you know to generate new data. So there's just a lot of moving parts right from a systems perspective. You're managing a bunch of different process groups. There's a lot of data shuffling between them, a lot of coordination and these are um you know introduce hard systems challenges.

Um, so I want to talk a little bit about how So I'm going to transition now to talking about what Ry is by introducing one of Ray's low-level APIs and to give a sense of how Ray can support these types of reinforcement learning workloads. So this is um uh totally different topic. This is a detour to explain Ray's actor API. So imagine I want to do reinforcement learning. I just mentioned I have there's two different parts. So I've got training with uh for updating the model and I've got a generator that's going to generate um new data and I also need to move the weights from the trainer to the generator throughout the algorithm. So I have some get weights and set weights methods. Now what ray this is the ray part. So, Ray lets me add this decorator and all of a sudden I can take a Python class and um add this annotation, maybe specify some certain accelerator types, some memory requirements, um other scheduling constraints um you know and we can use labels, right? You can specify if um label the like uh labels that Kubernetes attaches to the pods um you can specify that in the in the ray decorator.

So, they integrate with each other in this way. Um, and now when I instantiate this class, instead of just spinning up a local object in my Python process, it actually spins up a new process in my cluster um, with a copy of this object. And so I could spin up one of them, I could start a thousand of them, I could start a 100 thousand. And they can talk to each other. They can invoke methods on each other. And so all of a sudden you you have the building blocks to um, you know, build pretty complicated distributed applications. So to now to actually do the reinforcement learning part there's a few parts. There's a training part and then um generating data and the way we um you know kick that off with ray is you can just call the method and then add remote. So that kicks off the training uh asynchronously right you can wait for the results if you want. um you can call generate.reote to kick off uh data generation, right? And underneath within each of these methods, they might be um calling a bunch more methods on a bunch of other actors, right?

So there's a lot of stuff um hidden under the hood there or a lot of application logic that can exist. Um and then in between I need to to move the weights from the trainer to the generator and I do that by um calling get weights on the trainer and then passing that into set weights. generator. So this basically um is how you can uh you know just by passing one object into another method call um sort of transfer data in Ray and this you know these processes might live on different machines they might use different hardware accelerators um and this will happen the transfer will happen under the hood. So one recent development with Ray that I want to share is that the way this code is written is not ideal because the weight transfer to do this weight transfer the way it's written the weights have to first move to CPU memory and then are transferred over the network and in a typical reinforcement learning algorithm you'll actually want uh the weights to go directly from GPU to GPU over RDMA and so Ry recently added uh native RDMA support and you can use it by annotating the methods something like this.

Basically say ray dot uh you can specify the different transport backend. Uh it's designed to um be a pluggable backend. So you can use collective based backends like nickel and so forth or you can use um RDMA based backends like Nixl. And this is something we're uh you know planning to add more backends and and this is like a a big collaborative open source effort. Um, so just wanted to share really here with Ray. There's uh only a few different APIs that we're introducing. It's like the ability to take a Python class and u spin it up as an actor. Same thing with Python functions. You can take a function and execute it asynchronously in a cluster as a as a task. Um, and this is why Ray ends up being very general. So if I think about a lot of other distributed systems, you have systems like Spark which are designed for data processing, right? You have systems um designed for model serving, you have systems designed for um model training. Ray is really um fairly unique in that it it can run all of these different types of workloads.

And that comes from having a low-level um actor abstraction. Um, now since we're talking about reinforcement learning, I wanted to share there's been a renaissance of open source RL frameworks emerging and from companies like Nvidia, Bite Dance, Alibaba, a ton of different um, AI companies are open sourcing RL postraining frameworks for LLMs and the vast majority of them are built using Ray um, along with frameworks like VLM, SGLang uh, and a number of other uh, uh, frameworks. So, and the reason for that is that there's just if you look at back at this picture, there's a lot of process coordination and management and data transfer and coordination that needs to happen to implement this. Uh, and and Ray's actor abstraction provides tools for doing that. Okay. So, next I want to talk about how Ray fits into the overall um tech stack for for AI. And I just talked about RL about multimodal data processing. Um, and there's just a huge amount of complexity with these AI workloads, right? It's much more complex than when um when we started working on Ray.

Oh, whoops. Um, and let's see. So, that is um um, of course, all of this is powered by GPUs under the hood, right? And CPUs and other hardware accelerators. And not only is there growing complexity at the workload level right at the top, there's growing complexity at the bottom. You have more GPU failures, more uh greater scale, more heterogeneity. There are a lot of different types of hardware accelerators and new ones coming out all the time. Okay? Not only do you have uh chip failures, you have networking failures, right? You have unreliability across uh in many different places. And because of the growing complexity both at the top and on the bottom, um you see there's an emerging need for a software stack that connects the applications to the hardware, right? And that software stack has to solve a bunch of problems. Problems like scale, reliability, heterogeneity, um all of these different types of things. And so, um, we've worked with many different companies, hundreds of companies that have been building out this kind of software stack to scale their AI workloads.

And we've, um, seen a common pattern emerging over and over. And so, I want to share what the different layers of that look like. So, at the top, you've got the training and inference framework, right? This is this layer is responsible for running the model efficiently on the GPU. And of course, PyTorch is the dominant deep learning framework. Um, and surrounding PyTorch, you've got a rich ecosystem for transformers, right? For um, par different parallelism strategies. So, um, these are some of the common frameworks that we see. But fundamentally, this layer is responsible for squeezing the most performance out of running the model on the GPU. Now, underneath that, you've got the distributed comput engine, which is where Ray sits. And this layer is responsible for solving the systems the distributed systems challenges of scaling. So we talked about process management and coordination, data ingest and and data movement, failure handling, uh resource management, autoscaling, those types of considerations.

And then all of this is typically running on top of Kubernetes or uh some type of container orchestrator which is responsible for managing containers, provisioning compute, handling multi-tenency, uh job multi-tenency, user multi-tenency and and much more. And so um one of the things that's been exciting to me is that not only have we seen these different layers emerging, but we've actually seen them co-evolve with each other. and work better together. And I'll give you two examples. So, VLM is an inference engine for uh running LLM. And LLM inference has become much more complex because models have gotten larger. And squeezing the most performance out of LLM inference for large models really requires doing a good job with at both the inference engine and with the process orchestration. And so the Ray and VLM open source communities have collaborated to enable fine grain placement of VLM workers. And why does this matter? Well, this is important for enabling features like prefill decode disagregation, right?

Where you separate out uh the pre-fill stage which processes the input from the decode stage which generates tokens. Those each each of those different um uh components are often sharded across a bunch of GPUs. you may need collocation of of corresponding uh workers in in the two stages uh for performance reasons. It's been um this is important for wide expert parallelism support. So think about a large mixture of expert models where you have uh alternating attention um and fully connected layer and expert layers, right? right? And where the fully um connected or the attention layers are maybe replicated across each GPU and then the fully or the the expert layers are often sharded across bunch of different GPUs and you have to route queries um from one GPU to another in a complex way based on which experts are activated. There's a lot of of course there are a lot of different um parallelism strategies there. This is important for LLM aware routing. So you may um you often have a lot of queries going to your LLM that share common prefixes and so you want to do routing based on where things are where KV uh cache tensors are are cached, right?

So there's a lot of um uh details that are LLM specific and are important to get right in order to really get the most performance. And so this you can you can sort of see that implementing these features requires solving problems across different layers of the stack. um want to talk a little bit about Kubernetes and Ray. And this is um um a really nice combination because Kubernetes has is has um capabilities that it can do to manage containers uh that Ry is not able to solve at its layer and you know around the workload and Ray sort of handles everything inside of the workload uh and so has knowledge and awareness of the workload that Kubernetes um doesn't uh have access to out of the box. For example, both When it comes to autoscaling, right, your workload needs to scale up and down. Both Ray and Kubernetes have um the ability to you know autoscale by adding more nodes or removing nodes from the cluster. But um what if you you know to do a really good job uh if you want to be able to vertically autoscale a pod or a container right to add more memory so to an existing container um kubernetes has that capability and ray has awareness of what the um what's needed inside of the container what the workload wants right there are certain when it comes to failure handling um you know kubernetes is necessary for removing the bad GPUs and and things like that from the cluster uh and or provisioning more compute and getting more um compute for a given workload.

But then Ry has to work with whatever it's given, right? So Ry can request uh maybe you're running a training workload and and one GPU fails. Um Ry can try to request to get a new GPU from Kubernetes. Um but it doesn't know for sure if that if that request is going to be fulfilled or not. And if it's not fulfilled, it needs to be able to reconfigure the workload to uh continue running with the reduced capacity. And so these are the kinds of things that you can do if you have um knowledge of both the what's happening inside the workload and the ability to control things around the workload and provision compute. This is also important for um um you know a lot of companies run training and inference. Now inference one simple thing you could do is statically partition your GPUs and use some for training and some for inference but um you know inference demands go up and down and so you need to provision for peak capacity and then that's going to be inefficient so it's really it's preferable if you can shift GPUs between training and inference right as needed and based on you know different types of workloads have different priorities um to do that well you need the you know great ability to provision compute prioritize workloads uh and allocate resources between them.

You also need the ability for uh to to express the workloads in a way that is um uh you know can handle uh compute being preempted and can handle and can expand elastically to fill whatever compute is available. And so that's uh these are the kinds of things you can enable with Ray and Kubernetes working together. Now um the next thing that we recently announced is that Ry joined the PyTorch and Linux Foundation. So u we're very excited about this because well when we started Ray we started it as an open source project. It's we've had uh a large community of contributors pretty much from uh the beginning and a lot of these a lot of companies have worked together to really um de design the road map and and uh of course we were grad students developing Ray in grad school but we weren't actually running it in production at a company right so a lot of um the early requirements came from working with our with various users that were actually um relying on it in a missionritical way and So um you know the need for the complexity around AI has just grown so much and the need for this software stack has really emerged over the last year and so we're you know from the Ray community like we're very excited to work with um the rest of the open source AI ecosystem to really build out this this stack.

Um the last thing I want to well one thing I want to share is that Ray I shared Ray's actor API right it's a low-level API for um taking a Python class and spinning up an actor but the benefit of having we really think of Ray in two layers there's the core API that is um lowle but imagine now I want to do reinforcement learning or or you know a bunch of data processing or uh distributed inference on top of Ray well if I'm using that actor API there's a lot to build, right? I can't just um like there's a lot of application logic and I don't necessarily want to build all of that logic. And so we have we think of Ray in two layers. There's the core APIs and then which are minimal and flexible and then there's a library ecosystem on top and some of those libraries um you know I've been working on some of them we've been working on at any scale. Uh some are are you know natively in the Ray um repository. But there's what I'm really excited about is this rich third party library ecosystem built on top of Ray because um and this is something that is similar to Python, right?

Part of the reason that Python is so powerful is that not just that you know it's um it has nice APIs, but rather there's so many amazing libraries built on top of Python, right? Pandas and NumPy and and and PyTorch and and everything. And so similarly with Ray, the thing that excites me is people building powerful libraries on top of Ray like for for reinforcement learning, for data curation, for distributed inference, for agents. And um we've seen this from companies like Nvidia and bite dance and many others. And so um that's something that really excites me about building Ray. Um last thing I want to share is that we started any scale as a company in 2019 to commercialize ray and continue developing ray and there are ray solves a lot of the challenges like for running the workload in the cluster what we've been focused on at any scale is a lot of the stuff you need to build around ray to run it in production right so providing a native multi cloud experience right being able to deploy in your own compute or on any kubernetes cluster, right?

Being able to do that kind of uh workload prioritization where you can shift GPUs between training and inference like tons of rayaware observability tooling, right? This is the observability is the kind of thing where um it's you you need container level observability to understand resource uh usage, resource pressure. You also need sort of workload aare process level um observability to understand where's the bottleneck in my application like where are tasks being cued where is data being cued where is the which objects are slow to transfer all of these kinds of things um and so these are some of the things we've been working on um and I'm very excited to you know continue collaborating with everyone here on this open source project so thank you all very I'll I'll repeat the question right does also for example how what kind of work is done so that they're not at each And second question is >> well let me start with the first question. So well actually what what was the second question? >> Second question is >> okay the second question is what can kubernetes do more to help like AI workloads work better out of the box.

Okay, so the first question was um Ray and Kubernetes seem to do a lot of the same things, right? And what's the overlap basically or or do they um and this is actually a question we've gotten a lot like is Ray similar to Kubernetes? Are they different? Um because they both seem to do scheduling, they both seem to do um fault tolerance and autoscaling and and things like that. This is happening at a different level of of granularity, right? Um, Rey is thinking entirely about what happens inside of a workload, right? Like I already have my containers which I got from Kubernetes and now I'm thinking about the process level inside of uh, you know, the coordinating all the processes inside of a container like scheduling um, individual functions across um, different containers, things like that. And so whereas Kubernetes is thinking at a different level of granularity, thinking about jobs, right? thinking about containers. Um similarly at the for fault tolerance um the way Kubernetes uh you know thinks about fault tolerance is at the container level right it's at the uh but but if you have a bunch of different if you have a workload so it's it's one thing if you have a service running inside of a container and it's replicated a bunch of times and you can just restart the the container, right?

But what if it's some big data processing job where I'm I'm loading I have hundred you know or 100 thousand containers that are all um running different parts of a single application right and where data is being shuffled from one to another um and um you know some are running um yeah some are running um like inference on GPUs some are running some like are decompressing your videos uh And it's it's not like a a thing that can just be replicated. It's actually just all one big workload and and you need every piece. And so in that case, if a container gets taken away or a new container gets added, something within the application has to understand um do I what do I do with the the the thing? Do I need to rerun some stuff that was previously executing on that container? Can I um load balance stuff across that new container? like what can I um there has to be logic handled like inside of the workload. >> Yeah, >> makes sense. >> And then um >> I forgot your second question. >> Yeah, the second question was like how can Kubernetes get better like since you are >> Yeah, there's a lot that's been happening.

You know, some of the things we talked about, how can Kubernetes um uh better support AI workloads? I think you know a lot of the right things are are happening. Um and uh you know I think we've been collaborating closely on um you know these types of use cases better supporting RL better supporting um train offline workloads right um great support for all different types of accelerators and and sort of um you know topology aware scheduling and these types of things uh so I think a lot of um I think this the support has been getting really good. Okay, cool. Thank you. >> Oh, thanks for great talk. Um I'm just curious about the direction Ry uh will take with uh take together with open source community. Uh more specifically uh what does it mean that Ry joined to PyTorch Foundation and also is there any foray is there any plan to bring it into CNCF project? Um great question. So what does it mean that Ry joined the PyTorch Foundation? Um I think most successful open source projects join a foundation right there are exceptions like TensorFlow um React I think is not maybe not in a foundation but um if you look at really foundational um open source or infrastructure projects whether it's Kubernetes PyTorch um you know Linux all of these different things um it's very it's it's it's a natural home for an open source Um especially if you want something to really play um you know be very broadly adopted across companies because these things like um at each of these different layers you tend to you tend to have a standard emerge like there used to be many different deep learning frameworks right but that's it's has consolidated quite a bit more um people are standardizing on kubernetes people are standardizing on pietorch I think if you want something to, you know, you tend to have a standard over time, that standard tends to be open source, especially when it comes to infrastructure software.

Um, and people are more likely to standardize on something if it's um an open source project in a foundation. So it's very natural. Um, and so why join the PyTorch Foundation and Linux Foundation? Uh I mean this is PyTorch Foundation is just uh and the Linux foundation overall is home to foundational AI infrastructure projects that Ry is um used with all the time and so it's just very natural we're already collaborating um uh you know just as open source communities and it just made sense to double down on that. And then you asked about um Cubray. So, Cubray is a Kubernetes operator that makes it easy to deploy Ray on um on Kubernetes clusters and uh it's a it's a separate repo in the overall Ray uh you know Ray GitHub organization and we are as part of the process for uh moving Ray into the um PyTorch Foundation we are you know there are um you know hundreds of repositories in the overall organization and we are going through one by one and and figuring out which ones are uh you know makes sense to move as part of that donation and and um we're still figuring that out with Cubray.

I think they're only a uh Ray is kind of the the really substantive part um and but uh uh Cubray is is um is uh super widely used for deploying Ray on Kubernetes. Yeah. >> Thank you. >> All right. >> So that would conclude the meeting. Thank you everyone for taking the meeting this wonderful meeting. The rest of the question will be have to held outside. We have another section going.