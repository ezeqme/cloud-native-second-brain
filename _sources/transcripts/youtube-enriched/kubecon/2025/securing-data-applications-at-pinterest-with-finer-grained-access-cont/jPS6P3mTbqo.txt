wanted to thank you for joining the uh next to last talk at CubeCon. So today we're going to talk about securing data applications at Pinterest with finer grain access control on Kubernetes. Um I'm S and I'm joined by my partner in crime will. We're both at Pinterest data engineering and we've worked on I guess the intersection of big data um data privacy and ML for quite a while. And so uh um we're just going to talk about um how we built this very large scale nextG uh big data platform and how we resolve some of these security data access issues. Okay. So our talk is divided into four parts. The first part we're going to give you a little bit of the landscape of data at Pinterest. So talk a little bit about uh the data platform, talk about Mocha which is our Spark on EKS. um based batch processing system and then shift on to some of the finer grain access control uh concepts and then I'll hand it over to Will to talk about more about the implementation of finer grain access control or FGAC that we call for short and then we'll have some conclusions and next steps.

So Pinterest um if you think about it, one way of thinking of Pinterest is it's a very large um ML-based recommendation engine. Users deal with pins and boards and uh Pinterest will make recommendations based on their activities. So what you're seeing here is a very datacentric view of how you know Pinterest collects data and processes and makes it available. Right? So you have apps, browsers kind of starting at the top. You have user activities and then there's a API service running at the back end, right? Very large scale collecting user activity information. There's also third party data like say merchant catalogs stuff like that. And then it further flows downstream. it's picked up by say either uh uh our change capture system CDC or Apache Kafka from which it can either go directly into S3 or it'll go into our big data platform. So there are two parts there is the streaming part and then there is the batch processing part. Uh we'll focus more on the batch processing this time around but a lot of the conclusions and the techniques we use will apply to streaming as well.

And then the output of all of this would be recommendations um building cleansing data for data warehouse um models and signals. So zooming in even further. So this is Mocha our batch processing platform to uh at at at Pinterest. So we're in the middle of a or almost maybe towards the end of a transition from our previous batch processing platform which was which was Hadoop based uh to Spark on EKS and and by the way through the purpose of this talk you know we'll kind of interchange Kubernetes and EKS we use AWS services within that a fair amount and that'll feature more as we kind of go forward. So here uh what you'll see is users will um compose jobs say using airflow or they'll submit interactive jobs via Jupiter or querybook and then um underneath the covers we'll decide whether to send it to Hadoop or to Mocha. By and large these days it's it's mostly Mocha at this time. So why did we do the move? Um there are the usual reasons. um we were consolidating compute frameworks um cost efficiency was a factor.

We wanted to futureproof um against Hadoop. It was getting old. People are moving on to other things. But the biggest reason for us was security and containerization what's provided by Kubernetes amongst others. A lot of the Hadoop security controls are flaky, tough to maintain like um Keraros, KDC, so on and so forth. And uh we really wanted to leverage all the security controls that come with Kubernetes. Um also um uh AWS has built-in services into EKS that we wanted to leverage for example identity services and then finally we built uh Pinterest infrastructure internally there are internal services for providing security that we wanted to leverage um a quick kind of painting of where we are in terms of um Mocha. Uh we have about 12,000 EKS nodes about 30K spark jobs per day. Uh in S3 we manage around an exabyte of data. Um about 80 pabytes of data process daily. Um the cluster size we're still working on. That could be a whole separate talk about scalability issues with the Spark and uh EKS. Um and then um not pictured here but we run about at peak about 350k pods daily at peak right during the day and by the way this is only EKS and spark doesn't include the Hadoop numbers and um for finite grain access control we have a bunch of these um running in that but not all okay so uh here's the architecture of of of Mocha in general.

Um you'll see it's a fairly complex collection of uh systems um and and and services all kind of surrounding the EKS clusters, right? So um on the left you'll see ways in which um users can submit jobs into Mocha. So there is Jupiter, there is querybook which are mainly used for dev use cases for interactive jobs and then users can write their own tags using airflow which then make their way downstream. So let's walk through what that looks like. So Airflow jobs and then interactive jobs come through Livby and they make their way to Archer. So Archer is kind of the key for us. Archer is a job submission gateway into EKS and what it does is it'll create a Spark application CRD and uh submit that to the correct EKS cluster which it will select. So within uh within the EKS cluster we have the spark operator running which is a CNCF project and uh what that will do is it'll take the CRD translate that into the right kind of um spark uh pod driver pod which will then talk to unicorn which is another or rather Apache unicorn I should say and uh that will take the resource request assign the right Q and Unicorn by the way is a open-source batch scheduler for Kubernetes that we use extensively and Unicorn will then actually uh get the right assign the right resources find the right queue and then get the job running.

So once the job runs in the various set of pods, it's going to consume data from S3 and also uh shuffle data using Apache Caliborn. And then finally at the end, it's going to uh push the data out back out to S3. So that's a very quick run through of of of Mocha. We'll now transition into talking a little bit more about um the concepts behind finer grain access control that we wanted to incorporate into mocha and why we wanted to do that. So the reason we wanted to do that is um there are bunches of data sets at at at Pinterest right but we don't want everyone to have access to all these data sets. So we want to differentiate so that what will may have access to is not something that maybe I would have access to and vice versa. So the primary concept we started off with is every user and workload would have an identity and these identities would be authorized to access resources. Identity one may have access to a certain set of resources in which in our case these are S3 data sets. identity 2 may have access to a different set of resources.

That's kind of the idea behind this uh diagram. So kind of building on that further, workflows, users have um identities associated with them and then we map these identities to access policies. And so Will may have access to a certain set of um access policies. I will be mapped to a different set of access policies. Maybe we might have some in common. And then we would take these access policies um per user and then map that into the right access credentials. So this is kind of the basic concept behind our system which is users and workloads identities are passed securely onto Archer. So the way we do this is via from the clients on the user side we securely transmit the identity using MTLS and certificates and then when Archer gets it it will um take the user identity and it'll inject pod annotations into this workload into say the Spark CRD or maybe the Flink CRD or the Ray CRD. Once um the actual job is running when it say tries to access an AWS service in our case S3 it'll use the AWS SDK.

Now within the SDK it's configured or will end up using a credential provider that actually accesses the AWS um metadata service that's uh running on the EC2 host. So that's the AWS IMDS service. Now within EKS the standard pathway is actually not this right for users there is URA there is OIDC um stuff like that we actually opted to bypass that and use an approach where it tries to go directly to the uh IMDS the which is the host level EC2 host level metadata service but what we do is it doesn't necessarily make it all the way to the local IMDS intercepted. We have a um identity access management proxy at Pinterest system. So it intercepts that call and then we route it internally to our own credential vending service. And so within the credential vendetting service um we now have access to what the what the uh identity is of the request that's being made and we maintain the policy mappings and so we'll use that to generate a credential really an STS token. So STS is AWS's security token service.

So, it's a temporary STS token that goes back all the way to the user or the workflow that requested that um that access to S3 and it's a customized SDS token and then um it now has the ability to access the S3 bucket. Okay, so that hopefully gives an idea of what the basic concept is behind access. I'll now transition to Will to talk more about the various components. >> Cool. Um, great. Now that we've covered the uh the basic concepts, let's take a deeper dive into the components that powers fine grain access control for the platform. Uh, first let's do a quick background on the standard credentials provider um that provides credentials from an IM role attached to an instance profile. Uh so first a request um from the client goes to the on-host instance metadata service. Um this calls AWS to fetch credentials um which IMDS returns back to the client. Um this means that all clients of the host share the same credentials which doesn't work well when running a multi-tenant or operating multi-tenant platform.

Um especially where um on that platform each tenant may have varying levels of data access. Um so instead what we want to do is be able to vend credentials on a per pod basis. And um one thing you'll notice in this diagram is that we include IP tables. Um you'll see why shortly. Cool. So um let's go through the flow for a fine grain access control enabled client. Uh so requests begin um the fine grain access control life cycle at the credentials provider. Uh for us a majority of requests go through the S3 file system um library which is configured for us uh to be using the instance profile credentials provider. Um but this will actually work for any standard um AWS library. So um instead of the request going directly to IMDS like the standard way we just talked about two slides ago um the credentials are provided by the instance prof or where the credentials are provided by the instance profile. Um the request here is instead rerouted via IP tables to drum and drum here is a lightweight security process um which runs as a Damon said on Pinterest Kubernetes clusters.

So, drum fetches the metadata for all pods on the host via the local Kublas pod endpoint and determines if the requester is coming from a Kubernetes pod. Uh if the requester isn't running on Kubernetes, Drrome will simply forward the request to IMDS like uh the normal instance profile based credential approach. However, if drum um or if uh the pod is running on Kubernetes, drum will check its own local credentials cache uh for an entry matching the pod's metadata. And if there is a match, it'll just return the cache credentials back to the user. If it's a miss, drum will pass the um request to volance uh via MTLS with the associated pod metadata. Um volance here on the right hand side is a centralized security service uh which authenticates with drone and fetches credentials from either AWS or our internal uh credential vending service uh which we will also get back to shortly. Um, Volance maintains a cache of running pods on all EKS clusters. And so when a request from drum comes comes in, uh, Volance validates the originating pods metadata with either its cache or the Kubernetes control plane.

Um, Volance will then reach out to AWS or our internal credentials vending service again, which we will get back to shortly before returning the response uh, back through the chain. Great. So um now that we've covered all the components, let's go through the entire fine grain access control request flow. So uh to recap, first uh the client using a standard AWS library uh makes a request for credentials to the uh IMDS. Uh this request is rerouted to drum via IP tables. Uh Drrome will then query the cublet to confirm the request is coming from a Kubernetes pod and fetch the corresponding pen uh pod metadata. If the cache has magic metadata, it will return the cache credentials back to the client. Otherwise, Joe makes an MTLS request to Volance with the associated pod metadata. Uh, Volance will confirm the request um or will confirm whether or not the request is in its cache. Um, if an entry is present, um, it'll just return that back to the user. Otherwise, it'll query the Kubernetes control plane.

And depending on the annotations on the pod, Volance will fetch credentials from AWS directly or through the credential vending service. Great. So um now that we've covered the endtoend uh fang access control flow um and most of the main components we can talk uh quickly about why drum and volance are necessary. So drum runs as a lightweight damon set which only needs to handle queries for pods or processes running on the same node. Um whereas volance um on the other hand is a centralized security service um that handles uh drum requests from all EKS clusters. So uh utilizing both drum and volance we are able to transparently intercept requests from native AWS providers and inject our own custom credentials that aren't tied to an instance profile. Uh because this is compatible with the standard uh AWS SDK uh it's not tied to any specific application logic uh which also means there's no security specific logic within the within the application uh which simplifies and hardens the security model for any application running on Kubernetes within Pinterest.

Great. So, uh, we skipped over this earlier, but now we can go back to the credential vending service, uh, which we touched on in a previous diagram. Uh, so the credential vending service or CVS for short, um, is a data privacy service which maps Pinterest LDAP users to permissions, um, in AWS and STS tokens. So CVS takes in authenticated and um, authenticated requests and looks up the LDAP group for the requester. uh the LDAP group in this case can either be a human employee or a service account representing a service or workload. Uh CVS maintains a mapping from LD groups to S3 policies. Uh so what CVS does when it receives a request um is it initiates an assume RO request with AWS and it passes in two values or two key values. Uh the first is the RO ARN. Uh this is the static IM role uh which actually has admin level priv or admin like privileges um to all of Pinterest S3 data. Uh the second is a policy ARN. This is a session policy for the group. So the results of these two is a scope down role equivalent to the intersection of the admin IM role with the union of all the IM policies and as a safeguard so we don't accidentally vend um the admin like privileges to any client.

Uh CIS requires that there be at least one valid session policy uh present and then these uh scope short-term duration uh credentials are then passed back to the user. Oops. So um in order to get this working on Kubernetes, we actually utilize two key uh Kubernetes constructs to enable fine grain access control. Uh the first is annotations and the second is a service accounts with a scope down um arbback policy. Uh so when Archer receives a request for a job with uh FACK or fine access control enabled um it sets several key annotations um on the application which are then passed down to the pods. Uh so the first is the FGACK env or the environment. Um this is the environment that the pod is running in and it's used for um or used to determine which CVS back end we're going to hit. Uh the second is the requesting user. The third is a fang access control spiffy ID um for the pod uh which uniquely represents a workload within the platform is actually associated uh with an X509 certificate within the pod.

And so um the SAN of the X59 certificate is actually this BIFY ID um set here. And then the fourth is an IM RO. Um this is mostly here for legacy purposes. Uh in the FGA world, we're not really using uh Ursa or any IM ro related uh policies. Um but for FGAC, we use an IM RO um with no policy attachments. And we run all FGAC workloads using surface accounts that don't have any um Ursa annotations. And we scope down the arbback policies as much as possible for the service account type. And so if you notice here at the bottom, we have the um I guess the um arbback policy which is uh part of like the standard spark operator. Um but here we scope it down quite a bit from um what's uh available in the open source. And um yeah, so lastly we wanted to talk about application support. So uh this slide is actually kind of a gotcha because with how we've implemented uh fang access control there is no application specific logic required at Pinterest we currently have uh fine grain access control enabled for a large portion of spark and flink applications and um for next steps in the short term we're looking to onboard all spark and flink uh to fine grain access control and incrementally roll out uh support for array and pytorch applications and then longer term uh we're looking to support columner fine grain access control to restrict access to columns um in data sets that have mixed data sensitivity.

Great. So um in conclusion with fine grain access control we ensure that all workloads have an identity at the pod level. All data access uses temporary or temporary credentials with scoped access. Um we ensure compatibility with all pretty much all standard aws libraries and um there is no workload specific implementation required from the user. Great. So um yeah that concludes our talk. Here's a QR code to the session where you can download our slides and also leave feedback for us. Thanks all for coming. >> Yeah, thank you for coming. >> Hi. Hey. >> Um so the the AWS implementation is very IMDS specific right on AWS. How might you do this if you were on prem or another cloud? What would what would be like the um >> So I think that Got it. So you you're asking if we were so there are certain things right um that are tied to uh tied to AWS specifically um you know this notion of intercepting calls to IMDS and then um and then the actual vending of the credentials itself the SDS token um those would be the points if we wanted to move to another cloud for example or try something else those would areas we would want to change so those are the train dependencies.

>> Okay, thanks. And then the second part is like where do they set is it dynamically figuring out what per policies to scope down to or they set the policies for the job in another place and then CVS grabs that policies depending on the user like where do they declare what exact policies they need or is that dynamically figured out? >> So so um if you look here the light blue box the LDAP group to policy mapping this is actually a set of passes policies. So the mapping itself is is checked into the code. So it requires code review from um another engineer. Um so it's kind of like a static mapping in a sense and these mappings go between say like service accounts production service accounts um to all these different policies that we have. >> So the TLDDR is you know there is a review process by which this stuff is added or removed. >> Uh you said every pod has a X509ert. what's adding that and what are you using that search for if you're using the pod's metadata to basically store the delegated identity anyways >> we have um it's not in this diagram but we have another security service which runs on uh every host within Pinterest called Normandy and so this is able to issue certificates well previously issued certificates just for the host it ran on uh but now that we have support for like subhost identity it's able to issue certificates >> at the pod level >> yeah at the pod level Um so the other reason why we have these certificates within the pod is for like service to service communication.

>> Yeah. >> Um so that's like you're basically one of the things with FGAC is like you can't just have FGAC in one part of the system. Every interconnected component also needs to support this identity based like uh access policy and so for us these certificates are mostly used for serviceto communication. >> Cool. Thanks. Yep. Uh so you said um so this works for both uh hu users as well as jobs as identity. That's right. >> So uh how does the LDAP group mapping look for like nonhuman users? Is it like a generic service ID or something? >> Uh you were asking about like for for uh production surface accounts like a production workload or something like that. >> Yeah. No, I think the distinction between how we handle usernames versus workflow names. >> Yeah. So they all have existence in we create entries for both in LDAP users we have already right if you're on Pinterest right if you employed Pinterest you'll have an LDAP entry for the workflow stuff you know we will have to add that right but we have to ensure and and that's part of an automated process for workflows so that say when you're writing your DAG you know um you will put in your workflow name as part of the DAG config and then it goes through a review process to make sure that you are who you say you are.

I'm wondering have you considered other open source tools to replace this cuz like with STO you get MTLS for free and you get SPF ids for free right so you can do that and then also for user authentication like you can just pass around a jot which contains like the users you know scopes and that so have you considered those options >> um I think it's fair to say there are multiple ways of attacking this I You know in the previous talk uh from Uber we saw a way of using envoy right as a way of assigning service ids. So I think what works for us is simply that it's largely transparent uh to the user. If you recall in the previous conversation um you know uh the Uber team had a lot of trouble getting people to use that system right. So I think what we wanted to do was something that was as transparent as possible. And so the real entry point for users is if as long as they use the AWS SDK which they have to do right to access the services this provides us with a clean way of intercepting and assigning the right tokens.

That was the main rational.