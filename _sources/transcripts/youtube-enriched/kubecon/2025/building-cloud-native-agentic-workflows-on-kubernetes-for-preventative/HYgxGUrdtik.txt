Welcome everyone. My name is Ben Consalo and I'm an AI engineer at AMD. So unfortunately Darren's not able to join me today, but I'm happy to to give you this talk and show you some of the internal workings of a cloudnative preventive healthcare system. So all the way from the hardware to the model deployment, the infrastructure, and then to a front-end UI. So, if you're thinking of taking notes, uh, just know that all these slides are posted on the sketch.com website. Um, so, you know, if you want if you want to stick around and ask questions afterward, that's that's great. But, um, feel free to use those slides, the PDF of these slides on there. And also stick around to the end of the talk because I have, uh, some free AMD cloud credits for our GPUs. So, you can take advantage of that. So healthcare organizations need to reach thousands of patients for preventative screenings. Uh but manual outreach often doesn't scale and automate automation lacks personalization sometimes. And so the solution that uh I built was building an agentic AI system to generate patient specific criteria with the help of a screening prompt.

typing in um a prompt and and then looking at a patient database uh and ultimately to write these um outreach emails to suggest to patients that they schedule a screening. So here I'm using a cloudnative stack to build the backend model deployment uh that's secure and can sit on uh the hardware of your choosing whether you have like an on-prem cluster or you want to uh deploy it on the cloud either way it sits on your own infrastructure and all the services would sit there. So um this I want to go over the the hardware stack uh so or sorry the the full software and hardware stack. Uh so from hardware to healthcare the the software stack is entirely open source. Uh the hardware that I tested was the AMD Epic CPUs, Intel Xeon CPUs and Intel Gaudy accelerators. Um the orchestration is done with um Kubernetes and Helmcharts for declarative deployment and API 6 provides the API gateway authentication traffic management. Uh Prometheus monitors all the inference endpoints and then Graphana pulls that data from Prometheus for monitoring.

And then Autogen is a Microsoft open source framework uh that's for Agentic AI if you're not familiar with it and it's it's what's used to define the actual screening criteria filter the patient records and generate those personalized emails. And then finally on the front end is a Streamlit application hosted on HuggingFace uh CPU and but you could host this locally or or wherever you'd like. So it's a fully cloudnative open- source stack that any healthcare organization could deploy. So the main inference script is uh just inference stack deploy.sh. It's a bash script and sets everything up according to the config which I'll briefly go over. Uh and near the bottom of my slides I point to the source in the GitHub repo where you can find what I'm talking about if you want to look at it later. So the main components of the deployment bashcript are setting up the Kubernetes cluster and the config, the engineext controller as the foundation to API 6 uh keycloak and uh API 6 setup with anible and then uh using VLM for uh inference for the for the LLMs and then observability with Prometheus and Graphfana.

Then the the application layer on the the front end is is set up separately from the deployment script because it's actually using the OpenAI API endpoint. Uh that's that's um yeah it's pulling pulling from that deployment the backend deployment. So this is just an example or part of an example of a deployment configuration file and apart from just some beginning kind of admin uh parameters you just kind of set on or off on what you want to deploy. So after declaring some of those administrative variables um we are uh looking at you know on the at the bottom of that script deploy Kubernetes fresh on so deploying the Kubernetes server the engineext controller uh the keycloak for authentication API 6 API gateway built on top like I said of engine X um and then you have your observability and deploying the LM models like I said with VLM. So I did also on the on the right hand side show the current models supported but you can also bring in your own hugging face model. Um these are just the ones that kind of out of the box we know work on all these uh that we we know that we've tested but um yeah you can pull in any other hugging face model as well.

There's an API for that. So, as I mentioned, the hardware um starting at the bottom is native for both AMD and Intel, at least tested so far. It should work really on um any data center hardware. Um haven't yet tested it on the Instinct GPU, but I've tested it on the Epic CPU, Zeon CPU, and the Gaudy machines. So, um yeah, I've yet to hook it up to Rockom and test it on AMD GPUs, but shouldn't be too much of a leap to make it work there. Also, so now I briefly want to show you the Kubernetes dashboard. You might be familiar with this, you might not be. It is automatically deployed when the inference deployment script is run. So here's just a screenshot, but I will walk through a short demo video rather than doing a live demo uh showing how I pulled it up after deployment. All right. So yeah, it's the text is a bit small but first I'm just looking for the deployed dashboard and then doing port forwarding to make it available on our local web browser. I expose the port in my IDE and VS code and then I need to create a uh so yeah we visit the URL here um that's you know hosted through uh through this Kubernetes cluster and we need to make a service account and a cluster role binding for generating the token that it's asking for.

Um, so yeah, that's that's what uh doing here. And then once we get the the actual token, we can copy paste it back in and do all of our Kubernetes monitoring right from the the dashboard. So I'm pasting that token in and then yeah, here's our Kubernetes dashboard. It's it's really the equivalent of doing cubectl commands from the command line but um just in a dashboard form and you can kind of view everything quickly without having to type in the commands all the time. Uh yeah, so that's that's the dashboard and it should show all all the resources um in your cluster there. Uh first for those who are not familiar with API 6 and engine X um API 6 is a cloudnative API gateway handling API traffic providing caching authentication um enabling efficient and secure access to deployed AI models and then engine X is serving as a foundation for API 6 um has high performance proxy and load balancer for traffic responsible for routing incoming requests and ensuring access to deployed AI models. So, a API 6 transforms chaotic AI model access into enterprisegrade API management.

So, think of API 6 as the intelligent traffic controller for your AI infrastructure. So, now let's dig a little bit deeper into API 6 or API gateway. Uh so like I mentioned it's built on top of engine X and uses CD for configuration storage. Um it makes it truly cloudnative and dynamically configurable. This very brief code snippet I'm showing is part of a Helm chart um configuration and it shows we're running three replicas of API 6 for high availability with the ingress controller enabled to automatically manage the Kubernetes resources. API 6 gives us advanced features like rate limiting, circuit breaking, authentication, uh, and request transformation, all configured as plugins that can either be, uh, yeah, enabled or disabled. So, VLM, uh, if you're unfamiliar with it, is an open source library optimized for high throughput LLM inference on data center hardware, both on CPUs and GPUs. So let's dive a little bit into how we actually deploy these models. This is the the Helm command. This Helm upgrade install command that you see uh that deploys in this case a llama 3.1 8 billion instruct model um to our Kubernetes cluster.

So this Helm command abstracts away all the complexity of creating Kubernetes deployments, services, config maps and secrets. So one one command really deploys the the whole model endpoint and you can actually midway through your deployment you can you know provide this helm upgrade command again if you wanted to change something um without having to redeploy your whole cluster the the hugging face token allows us to download the model from the hugging face hub directly into the cluster. Um, a couple key parameters here are enabling both the API 6 integration and the Prometheus monitoring and the CPU equals 96 is we're allocating 96 CPU cores and then we're allocating 256 GB of memory. Remember these CPUs have quite a large memory footprint. um using tensor parallelism with size two to split the model across two numa nodes uh in this case and then the force command will delete and recreate resources if you want to include that each time the command is run. All right, so here's what makes this deployment unique.

The VLM configuration optimized in this case it's for x86 CPUs. Um so the the 40 here the VLM CPU KV cache space uh means that we can run more this is for 40 GB and uh for the KV cache and we can run more requests in parallel when we have a bigger cache and the VLM CPU SGL kernel one equals or sorry equals 1 means that we're using a small batch optimized kernels for low latency requirements like online serving and then below that the um extra command args that that model config section shows llama 3.1 specific settings uh or you know specific model settings. So we're using Bflat 16 precision to reduce a memory footprint while maintaining our accuracy and setting a max sequence length of 8,192 tokens and using multipprocessing as the distributed backend. The chunked prefill feature um allows VLM to process long prompts more efficiently by breaking them into chunks and we're batching up to 248 tokens. So the beauty of Helm is that we can customize these values at deployment time or use the the model specific defaults.

So moving up the stack, let's look at observability. Observability is critical when you're running production AI inference. You need to know what's happening inside your models and your cluster. So we use the standard Prometheus plus Graphana stack and Prometheus scrapes metrics from our VLM endpoints. Things like request latency, token generation rate, Q depth, memory usage, CPU utilization. So these metrics are stored in Prometheus's uh time series database and evaluate evaluated um against alerting rules. So if something goes wrong, say if token generation latency exceeds 2 seconds or if the error rate spike, you can have it ping your Slack or send you an email or you know whatever alerting mechanism you choose. And then Graphfana provides the visualization layer. So you can build custom dashboards uh to monitor things like real-time request rate, model specific metrics like tokens per second, resource utilization across the cluster. And here is an example of a model dashboard. I know the text is a bit small.

Um, but this is kind of what it it might look like where you can see things from time series, the time series database like endtoend request latency, token throughput, time to first token, time per output token, cache utilization, even prompt length. So, it's a great way to be able to observe uh what's going on in your cluster. All right, so now moving on to the application layer or the the UI on the front end. So m like I mentioned using a combination of autogen for the agentic framework from Microsoft u hugging face for the uh hosting the actual uh hosting the actual front end and then streamllet is the the front end um UI. So this this application uh uses natural language understanding from from prompts for the agents on how to act and it uses an OpenAI compatible API endpoint that I I mentioned was deployed with the the backend infrastructure and the uh you can you can take uh a tour of the application yourself actually by visiting the app because it's hosted on hugging faces CPU. I have it hosted on my own spaces there.

Um, yeah, you should be able to just search for it to preventative healthcare consolv. So here I just want to show you a short video demo again of the front-end application that I built. So it is a multi- aent system that uses natural language prompts for each of the agents. So there's an agent for the the user um that represents the user. There's an agent for the epidemiologist which decides which patients to target for screenings. There's a doctor critic agent uh to review the epidemiologist's suggestions. And then there's um finally a LLM to write some draft emails um for the for the patients. So then when we uh scroll down from there on the lefth hand panel uh we have like our our sample data set. It's a synthetically generated data set but you could use your own data set or hook in your own database. And then you provide a some kind of screening prompt like here I have breast cancer screening and then yeah we can put in some contact information for the email at the bottom. And then yeah, here on the right it's showing a log of what's happening in the interaction between the agents and and then generates these emails um that are essentially like drafts of emails.

I wouldn't recommend just blindly sending out these emails. Um, but it provides pretty good um drafts of emails and it has looked at the patient data um both the descriptive data as well as like gender, age um to to uh decide which which uh patients to reach out to. So, and then yeah, here I'm just showing kind of a readme that uh if you wanted to get this set up uh yourself, you could walk through some of the steps here. Um, but the simplest thing would probably just be to clone this hugging face space. Um, and then, you know, try it try it yourself. All right. So, what did we create? We connected the dots between a useful application for healthcare and a robust cloudnative stack for model deployment. Uh we built an agentic framework with Microsoft's autogen to save countless hours of patient selection and manual outreach for preventative preventative healthcare screenings. The backend infrastructure is completely open source from the Kubernetes orchestration, the VLM model serving to the observability with Prometheus.

uh open- source models like Llama 3 and Deepsek R1 distilled are models that anybody can use or you can host your own closed source model or one that you've fine-tuned for your use case. So, let's talk about business impact because that's what healthcare organizations care about. First, uh clinician time. So, this should free up clinical staff to actually see patients, their core job. The preventative screens might include things like colonoscopies, cardiovascular assessments, mammogs, um etc. These uh comprehensive preventative care becomes more feasible uh when you have like I said thousands of patients. So the cost is a big factor in any model deployment. Um this this can run on both commodity x86 CPUs that you might already have um as well as uh GPUs. So the entire stack um like I mentioned many times is open source. So all the all the parts of it and there's no per token API fees because you're you know you're hosting all the infrastructure yourself. Uh, no vendor lockin, no surprise bills from OpenAI or Enthropic.

All right, so if you want to get started, um, like I mentioned, you can actually visit the application itself that I've hosted on HuggingFace and then the enterprise inference stack that I went over the whole backend that's also available on GitHub. So that second link and then here I just wanted to uh let you go ahead and try AMD's latest Instinct GPUs on the AMD developer cloud. So this QR code is uh good for $100 in cloud credits. Um so you can feel free to take a snap of that and uh and use that or like I said you can visit these slides afterward and and find the same link there. Yeah. So that's that's all for my talk and appreciate your time. Uh if you have any questions, be glad for you to come to the the microphone there. >> Hi, my name is Jonathan. Uh I too work in heavily regulated industries and um some of the feedback that I've gotten from my you know stakeholders have been concerns around the long-term maintenance of a fullon open-source stack. We we definitely want to avoid vendor lock in but at the same time in a lot of cases there's a lot of overhead associated with maintaining something on your own and particularly in organizations that don't have the depth of skill set in this area.

What sort of feedback did you get in general as far as the long-term maintenance and support of some of these tools, particularly around Keycloak as well, which is so core for IM? >> Yeah. No, I I haven't actually personally gotten feedback on on that yet, but um I guess just taking a stab at it. Uh yeah, there's there's often times where you need to customize and um build build something that is a little bit more tailored to uh the organization and then maintain that yourself or or yeah, hire a contractor if you're a healthcare or organization to kind of maintain that stack. Um but >> thanks. >> Yeah. >> Hello. >> Hi. As far as um the the patient interaction, what types of things did you use to kind of verify that it was giving information that is acceptable like like patient safety especially in healthcare is a huge issue and concern. So like was there anything that you did in that that space? Yeah. So, I did find especially with these LMS at first that there were a lot of hallucination and uh strange things that it was coming up with and had to put some guard rails and some tooling to ensure that you know the output was appropriate and suitable um and not yeah coming out with uh a range of u just stuff that it it shouldn't.

So, I put some pretty tight guardrails on what the output of the the email should be and um really restricted it a lot uh with tool calling uh specific tool calling to um yeah to make sure that uh it wasn't coming out with um yeah some some hallucinations. >> Did did you use anything to observe as well? Like >> any what? Sorry. >> Like so like to observe it like let's say yeah you you've tested it, you feel that's good, but then when it's out in the wild, right, how do you ensure that it's continuing to behave as expected? >> Yeah. No, I don't I didn't have uh anything on followup on that front, but yeah, that would be definitely helpful. >> Thank you. Yeah. Yeah.