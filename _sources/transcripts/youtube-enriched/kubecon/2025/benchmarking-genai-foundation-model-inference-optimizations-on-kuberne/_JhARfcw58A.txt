Good morning everyone and welcome to our talk titled benchmarking foundation models um inference optimizations on kubernetes. I know it's a mouthful but we'll break it all down in the next 30 minutes. So about your speakers I'm Sachin Vargas. I'm a lead machine learning engineer at Capital One. I'm also a stream lead at the Linux Foundation's GI comce and I'm oh and I'm Brendan Slave. I'm a software engineer at Google on GK's AI inference team. So a disclaimer that this talk is not about proprietary models or optimizations at Capital One or Google but rather structured around an open source project that myself and Brendan have been working on. Um so with that let's get started. So uh the topics for today we'll talk about genai models an intro like a primer on model inference. We'll talk about a few different optimizations if you are deploying or hosting LM models on your own uh systems. Uh finally, an intro to um a benchmarking tool and for validating the performance gains from these techniques and also some example results and finally our path forward.

So foundational models are at the heart of generative AI. These models are trained on large data. u they learn complex patterns and generate content. This could be text, audio, images or any any other forms of data. This generated content then forms the solutions for many generalized problems like question answering, sentiment analysis or even automating agents. So let's understand how these foundation models work under the hood. Um here we consider textbased auto reggressive models. So text in text out. These models usually generate one token at a time. For simplicity, we can imagine uh these tokens to be equivalent to an English uh word in this graphic. The model inference itself has two stages. The prefill stage and the decode stage. The prefill process all your input text or tokens and generates a single output token. um and the decode state generates subsequent tokens one at a time in an auto reggressive manner. This entire process is hugely accelerated by the usage of a KV cache that stores results from previous computations for the next token generation step.

The two key metrics that we would use to measure the efficiency of this process would be the time to first token and the time per output token. These are the two latency metrics that we'll try to optimize in the next few slides. Now let's consider the same inference process in a real-time production system handling multiple concurrent requests. Given the computational difference between these two stages, we can employ a static batching to combine the prefill and decode stages from various simultaneous requests. The peripheral batch will be processed first and then the decode stage begins. So when a new request arrives, it will be cued and processed only after the existing decode stage have been completed. This optimizes time per output token, but the time to first token increases due to the additional queueing time. In the same scenario where the batch request have a different output token lengths, the decode batch can be extremely inefficient in terms of hardware utilization even with high number of concurrent requests basically because the the new requests are waiting.

This is particularly a drawback in case of large foundation models considering the high-end expensive hardware that is used for inference and you don't want it to be underutilized. So how do we solve that? This is where continuous batching that is the ability to batch at every token generation step with a prefill first approach comes in. When a new request comes in now the decode batch is interrupted and the new prefilled batch is prioritized without any queueing. The interrupted decode stage are continued in the next cycle. Unlike static batching, this mechanism with this mechanism, the GPU utilization can be improved with more concurrent requests. Further, this optimizes time to first token due to the prioritized prefill. But the interrupted decode stages leads to higher average time per output token. Most inference servers today, the VLM, TGI, you name it, most of them use some form of continuous batching. So can we get the best of both worlds? So this is yet another technique where instead of processing the entire prefill in a single step, what if it can be distributed over multiple chunks?

So with this optimization, the concurrent decodes of other requests can produce one output for every prefilled chunk instead of just one token output for the entire prefill phase. Thus, chunk prefill improves hardware utilization as decode stages can now be combined with shorter prefill chunks. Larger chunk size will optimize for time to first token and a smaller chunk size will optimize for time per output token. So basically your chunk size configuration acts like a knob for you to choose which metric to optimize for. Apart from puning the efficient batching and utilizing the most performant uh kernels post-raining quantization can be very effective for reducing latency. Quantization uses low precision model values for you know less computation and reduces the memory foot uh the model memory footprint. Consider a 20 million parameter model. um an 8 bit quantized version only uses a quarter of the GPU memory to even load the model compared to a full precision variant. With lower models, they have the uh lower latency due to less computation.

But any form of quantization is a lossy compression, right? And that will have an impact on model performance. Most techniques hence require calibration and accuracy benchmarking to validate the efficiency of quantization for the specific uh use case in context. So next we talked about quantized models or smaller models in general being faster but they struggle in terms of accuracy in comparison to bigger models. Speculative decoding is yet another technique that pairs a larger model with a smaller model or a lighter draft mechanism to propose many tokens at once. So in step one here the draft model is generating quickly many tokens and then in the step two the original model verifies these proposals in a single forward pass then accepts the prefix that matches its own predictions. If none of them match, it discards all the draft tokens to generate at least one new valid token. So basically in a single forward pass, you generate at least one token which is equivalent to the previous situation.

So compared to model inference, this techniques lets the system generate potentially multiple tokens at once, cutting the latency and boosting throughput without an impact on accuracy. But the efficiency of this technique depends on the ability of the draft mechanism to emulate the large model. There are many variants of this like the engram look ahead, medusa heads and eaglebased sampling. So we talked about um many inference optimization techniques that improve the latency of the model uh without the need to upgrade the hardware. Now I'll pass it on to Brendan to talk about how we validate the performance gains from such techniques. Cool. So um back to back to infer. So um why did we build this? Um the core challenge is that generative AI serving is generally a blackbox. So even a lot of the optimizations that Sasha was talking about the specifics of why like how how these affect performance is is gener you you'll generally see diminishing returns as you start to look into these more with more detail.

So um given that we're seeing explosive demand with generative AI serving solutions um and there's two there are generally a growing number of choices for models model model server t model model servers tunings and serving stacks um the thing that we identified was that there's no there's no clear standard way to evaluate performance which makes it complex to compare options. So the result is that deciding on a serving stack is a very expensive task. So that's the gap that we're trying to plug with inference perf. And again this this diagram will shows a bit bit bit about the architecture. So on the left um we have an API we have an API that takes in that's it's it takes the form of a config file that just takes in your data like the data sets or like your input data sets your traffic patterns any any anything around your model server config that all goes into that one single config file that's used to configure your data generation. So your so you're choosing slashfiltering the set of requests um your load generation so that's responsible for determining the URL to either identify determine the URL of the production model of the production stack or your the model server that you want to test.

Um it also helps configure the the request like the request times the weight times based on your load on the load pattern that you specify and then from and also metrics collection. So and then finally like report we have report we have an API for report gen so the so the so you could specify the granularity of the the reports that you get back so um d diving a bit deeper into the load path so diving a bit deeper into inference perf inference perf's capability for generating load um the first thing that we want to highlight is that it's highly scalable so for anyone look so for those of you who are looking at who are considering large scale like large scale production inference deployments such as LMD and Dynamo. Um this is going to be of interest. Um we're aiming to generate different load patterns to test specific scenarios such as like burst traffic um scaling up to and past saturation which is the point at which your the point at which the model servers you you start to observe request queuing.

And we're also work there's work ongoing to make infrasperf able to handle more complex autoscaling and routting situations. And again like that's all specified like that's all you you're able to specify that via the API and with respect to data gener with respect to the to the set of prompts um we support we we we support inferring the set of prompts from either real world data sets like shared GBT CN and daily mail or synthetic data sets like mock where it is essentially just the same prompt over and over again random where like random where you're essentially just a random string of tokens or prompts with a shared with a shared prefix And again like to get even more granular um you can specify the exact input and output distribution. Um specify input output length constraints um define distributions like galaxy or fixed length and provide then provide the number of shared prefixes. So um with so um given all this what do we get back what do we get back from inference perf? So how do we like how do we describe the performance and the data we collected gets gets compiled into performance reports.

So inference per provides these reports at several granularities. Um a high level summary which is the um metrics metrics ac for all requests across the across the inference perf benchmarking run. Um you can get per stage metrics. You can get optionally you can opt in to get requests to get data from a at a per request level. Um we and the reports also do include request life cycle metrics which are metrics like metrics about the request responses request and responses as the user would observe them and per and prometheus metrics as like self-reported by the serving stack and the the request life cycle metrics are the main data point are the main data driving inference perfs model of inf serving stack performance which is the latency throughput curve that's that diagram off to the right and this is an ide this is what we'd consider an ideal example the latency throughput curve. We're essentially plotting just out we're essentially plotting throughput and in output tokens per second against the mean time to first token.

Um this is this is critical because it's showing like the performance under various loads such as like pre-saturation and post-saturation where you start to see request queuing which you'll which you can see as see off to the right as the throughput levels off. And again um one thing to call out is inference Perf is not is is not the author is not for qualitative benchmarking. Um we're this is very much around this is very much for a tool for performance perform performance benchmarking. So qualitative benchmarking is sort of out of scope here. Now um we can walk through a more practical example of like how you would use inference perf to evaluate variations of a VLM VLM deployment. So um in this case the experiment setup was pretty simple. Um we ran inference perf as a Kubernetes job. Um a side note here um inference pfs we we we do support like running inference perf locally and in cluster are both very well supported path. We have documentation on this. Um it sent traffic to a VLM service which rooted that to a single to a single VLM inference server pod that hosted that hosted llama 3.18.

And I think my battery just died. Thank you for bearing with us. Um so uh now now we can go back to um walking through like that that example. So again um just to just um recap um inference perf as a job inside the cluster single service routing to a single VLM inference server pod. Um metrics are being collected by Google manage Prometheus and we are uploading all the results to a Google cloud storage bucket. And as for the load, um, we had a 128 token system system prompt, so the shared prefix and that's made up of randomly generated tokens plus a 64 token user prompt made up entirely also of randomly generated tokens. And the output length, the output length was we specified was 64 tokens. And then we we specified a base case and then we spec and then we tested a series of optimization/variations of that base case. So using the vzero back end from an older version of VLM um enabling prefix caching and chunking um increasing the batch size uh using dynamic quantization and using speculative decode with an EL3 model.

So next few slides we'll be going over the results. So um the v1 engine this is what we're using as our baseline. Um so these are the model these are the model server arguments um that that we specified besides the other than the default other than the defaults um GPU utilization 90% max model length of 512 and max number and max number of sequences of 128 and also note for this for this base case we disabled pre we disabled prefix caching and chunk prefill. So um and this is this is the this is um the shape of the the shape of this latency throughput curve is what we expected. The latency throughput curve is the one on the very left. Um again like we expected latency metrics like time to first token and time per output token to slowly increase until the until the load hits the max throughput. And at that point latencies rapidly lab latencies will rapidly increase. And that's sort of that leveling off and that knee shape that you'll see that you see. Um so uh first variation is the v 0ero engine.

So we used vm 0.9.0. Um base case was 0.11 and again we just specified that environment variable. We superimposed the um we superimposed the old results in blue. Um the new results are in red and again like we we we expected performance degradation and that's what we observed. um that there again like a little bit of background um v 0 to v1 for v the the move from the from v 0 to v1 was a significant architectural change so again we we expected improvements and yeah that's exactly what we saw and yeah just generally higher throughputs at similar latencies and this is this is a good example of inference perf treating two separate serving stacks as black boxes with respect to comparison since in cases like this where specific architectural changes sort where where where the specific architectural changes that influenced these results may not be obvious or trivial. Um for the next experiment we increased the batch size. So just increased max number of sequences from 128 to 192. And again blue line base red line the red line is the new config.

And the the expectation was that increasing the number of concurrent sequences would improve performance. And we did see some, but we did we did see some the the gains were more marginal than we would have. They were more modest than we would have thought or than we would have expected. Um, and so that tells us that simply feeding the GPU more data by increasing the batch size couldn't force it to process faster because something else is the bottleneck and that's more likely just doing the math itself. So this is hinting that the work the original the original the workload in the base case was computebound. So um for a more obvious improvement um we enabled we we enabled prefix caching and chunking um again blue line blue line is base and again the performance gains were much more substantial. Um the results were due in part to the characteristics of the load like remember back to to the initial slide where all request all requests shared a prefix. So relying on the premputed KV cache values massively massively reduce the prefill time and yeah you'll see that you can see that in the middle graph where time to first token is massively increased.

Um chunking also increased GPU utilization which contributed to the high to higher throughput overall. So peing at around 10,000 output tokens per second as a up from 6,300. And again this is more reinforcement that our that the original workload was computebound. Um next we tested inflight quantization. So we used again base argu base case arguments. Um we configured VLM to use the bits and bytes quantization um using the quantization using the quantization flag. And again we saw very similar results as the very similar results that we saw with the um prefix caching and chunking. So again a little bit of background quantization allows for just faster computation faster computation on a comput. So on a computbbound workload we expect to see we we expect to see much more we expected to see stronger improvements and the data agrees with this. And again one one thing to call out here is even though we are seeing improve we are seeing improvements in performance um the we are we are deliberately ignoring the quality responses because again inference perf not not not a qual not a qualitative benchmarking tool and then and then lastly we we tested speculative decode.

Um so again we just added the speculative decode config to our base case arguments redeployed and we set the we set this number of speculative do tokens equal to four and we just picked a rand we picked a random equal 3 model that wasn't fine tuned for our use case and the v we did and in this in this particular case we did actually see performance decrease so which wasn't entirely surprising so the expectation is that the eagle 3 model would introduce overheads which which was known but we didn't have any particular expectations around performance gains because we suspected the giraffe token acceptance the the the tok the next speculated the the the rate at which we are accepting the speculated tokens is going to be low given the fact that the prompts that we're generating are essentially just random tokens and that's exactly what we observed. So in this case the overhead of running the draft model was greater than the savings from accepting the draft token. So this could this should demonstrate the impact of how a draft model could affect performance for your particular load and how inference perf can be used to evaluate more more of these niche these these niche model server tunings or picking a draft model.

So um as as for our road map um we are planning to build a deployment API to to to help automatically deploy these different servings different serving stacks for test for testing um other features we're also working on um traffic routing for different for testing different model servers or Laura adapters um adding multi-turn chat support um adding support for trace replay and support for real world and for using real world trace data sets um we also want to expand a multi modal benchmarking. So non-tokenbased inputs/outputs. So diffusion workloads fall under this fall fall under the scope of this effort and improving accelerator metrics collection. And again like it goes without saying like one of our highest priorities is continuing to add support for more model servers, stacks, data sets, the like. And again um lastly we just want to say this is an open source project under Kubernetes sigs and we're we want the the aim of this for this project is to be the the source of truth for inference benchmarking and we're so to that to that end we're always welcoming new contributors.

So um for for anyone who's interested either copy down the link or scan the QR code um feel feel free to try it out and again thanks to everyone for joining um and for anyone who's interested in learning a little bit more about inference proof. Um there is another talk going on in ballroom 2 at 4:15. Um we have a few other contributors including including um Jason Cranberger um Sasha and I work pretty closely with him. Um he's he's part of that presentation. Um they are presenting a more comprehensive tutorial on how to use inference perp for benchmarking. Um, right. So, I think with that we can move on to Q&A. >> Thank you. >> This works. Um, I guess question for you like are you just sort of like running these things from like your Oh, there we go. Are you running these from like your laptop to just like direct against like the GKE infrastructure and that sort of thing? Are you using like other systems to kind of help I don't know manage the hardware and everything that's under the covers? >> Um, so you you were asking about the experiment setup in particular.

>> Experiments themselves. Yeah. >> All right. Um, it was a so the experiment setup was pretty controlled. It was essentially just a an inter like an internal use only GKE cluster. >> Gotcha. Okay. Have you guys looked at using some other tools to kind of help manage like lots of people running experiments simultaneously and that sort of thing? like rather than just like oh yeah everybody gets a cluster to kind of deal with this and like batch them and cue them that sort of thing. >> Uh we haven't but we do make an effort to make sure that the like when you when you are running inference perf when you have multiple inference perf jobs going at once that the result that the metric scraping is isolated um there's making sure that there's no spillover. >> Awesome. Cool. Thanks. >> Hi. Uh so if I understood your network architecture diagram correctly, instant curve is both running the test and then reading the output from Prometheus or is it is it reading the metrics from Prometheus? >> So it's it's automatically scraping metrics based on your your particular model server or or serving stack.

Um well there's there's two categories. There's there's the request life cycle metrics which were which are calculated just based on the like input length, output length, >> right? >> Like start time, end time, all that. And then yeah again Prometheus metrics are it's it's it's essentially just an auto automat automated um metric scrape. >> Okay. Uh is there is there a hard requirement on you already having configured your Prometheus endpoint or does this work in any uh Kubernetes uh native cluster? >> Um so metric scraping is optin. >> Okay. So it's optin >> right. Yeah. Right now we we support um self-hosted self-hosted um Prometheus or Google manage Prometheus. Um again it's one of those like we're just sort of looking for contributors for for other for other manage Prometheus platforms. >> Okay. All right. So so there's a there's a requirement that like your uh cloud endpoint is having a managed Prometheus cluster or that you configure your Prometheus endpoint within your like silo cluster. Right.

Oh, I'm sorry. >> So, there's a requirement that you're like if you're running like uh I forget what the AWS service is, but AWS's native Kubernetes service, GKE on on Google or OCI's native Kubernetes service, like those already have the Prometheus like endpoints configured. If you're not running within those uh managed endpoints, you then need to configure the Prometheus server in order to run the >> correct. Yes. >> Okay, cool. That makes sense. So, all right. So that there's just like what I was getting at is there's a hard requirement that that Prometheus has already enabled in order to run this these uh tests. >> So if you can if you configured inference perf to scrape metrics then yes that's a requirement. >> Okay cool. Thank you. Sure. Right. Thank you all.