Good afternoon everyone. Welcome to our talk. We're here today um to talk about Kmata in action. Specifically, how you can use Kromata to overcome the massive challenge of scaling AI workloads across multiple Kubernetes clusters. My name is Tessa Fam. I am a senior software engineer at Bloomberg. And joining me today is my teammate Way Changai. Oh, >> hi. I'm Way Changai. I'm the software engineer at Bloomberg working on AI platform. Hong Kai Ren who is our Chromata maintainer and expert couldn't join us today but we're excited to walk you through all the Chromata 101's and 2011's today. So the question of why we want to run AI workloads on multiple clusters is no longer new. Um first AI models now are huge and they are critical parts of applications. They are um production services. So it requires multiple region high availability for a continuity during any disaster. Secondly, when you run multicluster strategy um you can serve the you can solve the challenge of low latency because right now real-time AI inference demands very very low latency.

um you need to use geo distribution in order to have your model server as close as possible to the end users or to the data sources. Third, we want scalability and cost because we need to handle spiky demands by being able to burst capacity and utilize cheaper resources across different clouds um to save costs. Next, we'll get to centralizing management of diverse and expensive hardware like A100s and T4s. Um, with a multicluster management, you can pull them into one logical resource view. And finally, by diversifying your hardware and cloud providers, you can use multiple clouds and even onrem simultaneously to give you the infrastructure independence and avoid vendor and platform lock in. Multicluster strategy now is used quite widely for two key AI use cases. One is distributed training. So if you have a really large model um that you want to train parallelly across clusters or if you're running your infant services and you want to serve that model um globally in different regions. While the idea of multicluster AI workloads is very tempting and appealing, we do run into massive challenges.

The first one is obviously the operational complexity. It is hard to manage independent independent clusters including upgrades, patching, monitoring um and is quite labor intensive to do so manually. You will have to worry about synchronizing all the configs. So you need first consistent configs like arbbacks network policies across all clusters and if changes are made to the configs on one cluster then it can easily lead to drift over time. There are also difficulties in making deployment and scheduling decisions. How to decide where to deploy a workload based on several factors capacity cost latency. You will also lack a unified resource view if you just rely on individual Kubernetes schedulers because they're cluster local. So you cannot see the total realtime pool of resources that you have across all clusters and that will prevent you from being able to make optimal decisions to schedule your resources. And with that um you it's also hard to do automatic scaling across clusters. If your platform is currently relying on standard HPAs which are also cluster local then is is impossible to burst workloads seamlessly to other clusters without interrupting your workloads.

Um when one cluster runs out of resources and you have to find capacity somewhere else. handling dependencies between um clusters and between services clusters is uh also comes into play and that can be hard as well. And finally um with policy and uh yeah finally with policy and governance like syncing um configs and syncing policies like I have talked about before um that is a headache to manage. Now on top of these general um multicluster challenges they are magnified when you deal with AI inference. Some unique challenges um to inference is um one critical um latency. So uh when you run inference you do require sub u millisecond response times. Um, and with that you will have to think about where to place your workload and you have to be late very latency aware with your traffic routing. Now when you have a very large models to serve um and you run you run the models on multiple clusters, how do you distribute them and how do you keep track of their versioning? Right? And next, AI models now are become way more complicated with very specialized hardware.

And it's very rare that all of these hardware GPU types like T4s, A100s, um, TPUs, it's very rare that they exist in just one cluster. So with the heterogeneous hardware, how do you manage scheduling your infant services or your model so that it land on the cluster that has the hardware requirements? And finally with incrance you do run a lot um into very rapid unexpected spiky demands and um and that is unpredictable. So then your platform needs to be able to handle this dynamically by being able to burst it into other clusters. And so in this talk we want to talk about crata as a solution to our multicluster headaches. If you're here you're probably already familiar with crata or wanting to learn more how karma can help with your platform. Um it provides a centralized control for um it provides a centralized control by um having a unified endpoint to deploy and manage applications across all clusters. Kmata is a is essentially a multicluster scheduling platform. So it can intelligently place your workloads based on available resources, labels or any policies defined by the users.

And with that you get high availability out of the box with uh is automatic failover and ability to reschedu upon any failures of your clusters. Comrada also offers crosscluster autoscaling which way will also talk about um later but it can automatically adjust the number of replicas across clusters based on demand and this will also handle the spiky demand um problem that we mentioned before and finally it is built on top of Kubernetes so it is interoperable with Kubernetes it acts as a Kubernetes API endpoint and therefore easily integrated with other tools and resources. within the ecosystem. Let's go over the high level architecture of Carmada. Um first we have the control plane. So this is the central brain of Carmata that has the API server and there's a controller manager that manages multiple controllers um and there's uh the karma scheduler that will make the scheduling decisions. And outside of the control plane, we have member clusters. And these are standard Kubernetes clusters managed by Crmada.

And this is where your final workloads will land on and run on. For a deep dive into the architecture and also the core concept of Chromata, I have linked here previous talks. We have uh talks at KCON that we do go with more details into the architecture. But today we want to dive more into the policies and also some new features of karma and also karmmata for inference as well as we have seen it um being used more in the past year. So multicluster scheduling in this diagram let's assume that we have a given um arbitrary resource called training workload. This is not a native Kubernetes resource. So it will need to be interpreted in order for KMADA to understand. You do this with a resource interpreter. Um after interpreted into what called a resource binding, the commoduler will take this resource binding and go to propagate. Um if the resource has dependencies and in the propagation policy you um set the propagate depths to be true then it will try to sync the dependencies of these resources first to the member cluster and then it will go to propagate the resource.

Um and before the resource actually lands on the member cluster, there is customization that you can further use to fine grain to control um have more fine grain control over um what the resource looks like when it's federated. And finally in the case um where the current cluster cannot um handle um the current workload or it fails then we have uh rescheduling and failover. Quickly uh on resource interpretation um kubernetes is very extensible with CRDs but that also means that kromata will not understand your resource if it's a CRD. So you will need to write an interpreter to tell Carromata how to interpret it into replicas into the dependencies and how to inter uh how to interpret the status. Um and that's essentially what it does. Um it identifies replicas um dependencies um how to aggregate the um health status across all clusters, what it means for it to be healthy and running. Um there are two types of interpreters. You can write a resource interpret interpreter customization which is a YAML and in that you can have a Lisa script to basically translate onetoone um do a onetoone mapping of your custom resource um to what karma understands or you can write a custom web hook to do this uh interpretation.

So yeah um in this diagram you can see this resource will go to the interpreter um and gets translated into this blob right here that karmmata will understand and will federate the resource accordingly. So as you can see, Karma offers a policydriven orchestration scheduling decision is guided by propagation policies and and then next we'll have uh before the resource actually lands on the cluster you'll have overwrite policies. These are optional but you can further customize the resource. This will tella what to change about the resource before it lands on the cluster. And in the case of again the current cluster cannot accommodate the resource for some reason then kmata very smoothly does cluster or application failover or rescheduling of the application. So the propagation policy a answers the question where and how to deploy the resource. Um the example here I'm using an infrance service. Um I want to gear this to more towards inference. Um but basically this is a a very simple YAML of an infant service that serves the um this PyTorch model called called GPT.

All right. And these are all the um resource limits and requests. When you bring this to KMAD and after it gets interpreted and and then it gets brought to Kromata, Karma will use the propagation policy that you define and answer the question. Okay, where can I put this infant service? What cluster would serve it best or what clusters cannot serve it? So you will use the cluster affinity field to to um select the clusters. You can also use tolerations to exclude clusters if you want to reserve those clusters for something else or for specific teams. There are dependencies um to tell Cromada whether to sync them first. the member clusters spread constraints um can be used to uh evenly distribute the replicas based on topology. So you can distribute it over regions or zones. And finally the replica scheduling strategies. You can either duplicate a set of replicas or divide the total number of replicas across uh clusters. And um you can also assign it priority so that it divide based on there's a weighted division.

So um some use cases for AI workloads um our adopter scatterlab um they use karma and they are deploying the AI chatbot um using the cluster affinity to deploy them to two clusters in two different regions um and that allows for a multi-reion HA and low latency. Um you can use affinity to do uh hardware aware scheduling. So only landing the resource on the clusters that have that hardware requirements. Um there's weighted replica distribution. So again giving priority to the primary regions or to the primary cluster so that it receives more replicas of your uh say infant service than the secondary region. Cross cluster automatic scaling um can be used um when you use it with federated HPA and with the dividing replica strategy. So then it can scale up and down and using that priority that you gave it. Uh and finally um you can also version models with uh cluster affinity to target environments with different model versions. And the override policy specifies what to change about the resource that lands on the member clusters.

For example, uh here we have two different clusters. Cluster B is onrem and it has CPUs and it has um 8 A 100s and cluster C is AWS um and it has eight L4s. So before this resource the infant service lands on the cluster you can modify it um modify the image path um for for example this storage URI right here um you can modify commands arcs labels annotations plain text or or even fields. So if you want to be more conservative with um the the cloud provider cluster then you can have lower number of replicas and have it scale to zero when um yeah when there's no traffic. So a few use cases for overwrite policy for AI workloads is one canary rollouts. um you can modify the container image to deploy a specific version um to to specific clusters that are um running um say the blue deployments. Um and you can also do overrides for GPU and hardware um by injecting structural fields like uh note selector or tolerations um for specialized hardware of the cluster um and make specific configurations like model or uh image paths and finally adjusting the resource request and limits and you can do that with overrite override policy as well as I have shown in the previous slide.

So now I'm going to hand it over to uh Way Chang to talk about federated HPI uh HBA resource quotas and cluster failovers. >> Oh okay. Yeah. Thanks Esa. And building on their foundations uh let's look at why colout management gets tricky in real cluster AI environments. So first every region looks different in really cluster environments and hardware might mixes vary in GPU counts, CPU and memories and this difference change over time as we add or retire nodes. And now if we try to reuse a single resource template across clusters, it quickly breaks because a koda size for a region with four GPUs for example like won't fit a region that only has two. You either underutilize a large cluster or fail admissions in a smaller one. And today like many many team paper over this by crafting per cluster called us and pushing them with their own workflow or with Carmono's propagation policy then patching them per target with over a policy and it it works for like handful of clusters. However, like this becomes a policy spur at scale like dozens of near like duplicate objects, constant overrides or like manual recalibrations whenever capacity changes and that leads to configuration drift, the noisy alerts and uh like there also like maybe some jobs were starving in one region while resources sit idle in another.

So like karma provide a lightweight fix like it provide a feature called federated resource koda and instead of like head hunting kodas in every region you can create only one uh single federated resource code per namespace and in that object uh it will list how much each cluster should contribute with the state assignments and like it will also shows the aggregated usage and hard limit for each clusters. So from there, Carmada will fence that single intent out and creates the normal Kubernetes resource cod objects inside each member clusters. And the admission for this feature, the admission still happens locally with the native resource code. And but what you get is you can you only need to define one resource and it will distribute everywhere without like babysitting a pile of per cluster policies. And you also get the visibilities uh such as aggregated resource. So you get you know how which cluster use uh how many of each cluster use and you don't need to like hunting through dozens of objects or dashboards and this is actually like nice for heterogeneity for example like if region A has four GPUs and region B has two and you can capture that difference in the in in a single resource and you don't need to uh continue checking like each of different resource koda.

So then you you get a single source of truth and zero policies for and and the ne and for more advanced usage uh we also provide another uh feature gated features uh called federated kota enforcements. uh with it on you can define the global hard limits in your federated resource koda and you don't need to have per slice uh pre-sliced uh those limits per cluster. So here's the flow like a user submits a workload to the federated control plane and before theuler fins anything out command will runs a koda validations uh against the federated resource koda and it looks at the current aggregated usage. The federated resource kod controller will aggregate all the usage up to the karma control plane. So on in the controller it will knows the current aggregated usage and if the total exceed the namespace global cap the creator update will be rejected immediately. So nothing will propagates to member cluster and you you can avoid like no easy rollbacks or and partial placements and to keep that check accurate like Carmona will continuous synchronize that.

So and w without any uh extra configurations and the the validator and scheduleuler will both uh working uh rely on this information that information to make sure the the resource is correctly used and uh guards. So this give you like the fail fastest protection against over subscription and you you get far less per cluster tuning and consistent koda behavior across the tragenous bridges. And next like uh in some cases when the cluster runs even hotter and kodas don't answer the questions like who run next you might need a a more advancing global admission mill Q mill cluster Q where jobs from every region line up and are handled by priority fairness and capacity not just by whichever cluster is least busy and karma volcano has um calibrated and provide volcano global features. It builds on top of karmmada and stretches volcano's familiar jobs semantics from one cluster to many aimed at large scale AI training and inference and feature wise you get crosscluster scheduling and Q priority job priority and uh capacity so scarce GPUs go to the most important work while tenants still receive fair share and in summary this gives you like a single policydriven admission layer on top of the karmmada and it provide uh priority aware and capacity conscious for you.

And next let's talk about like autoscaling. The the native Kubernetes HPA is excellent within a single cluster but it watches it only watches uh signals uh it only watches signals as CPU memory or custom metrics and askuler to add or remove replicas in the single cluster in the same clusters. But in militi cluster AI environments that scope become uh normally becomes a hard limit because the the bottleneck isn't the target value on metrics it's GPU availability and when the region is hot for example when nodes are full or colas GPU limits are reached the HPA can request like more replicas but they will sit pending because there is simply no room to place them meanwhile another region might have plenty of hair native HPA has no mechanism to spill over to the other clusters and the outcome might be cues and throttling in the hard region and idle capacities elsewhere and you will need a lot of manual intervention or overprovisioning to compensate the situations and this sets up the need for something broader and we let's talk about it in the slides about federated HPA so in karma the federated HPA runs in the control plane and reads metric through the standard Kubernetes APIs And the Carmona m met m met m met m met m met m met m met m met m met m met matrix adapter scraps each m uh member cluster and rows this aggregate those uh metrics and rows this numbers up and hence the controller one federated view of the of the uh across the clusters and from the picture FHPA can decides on a single desired replica count for the workloads and Carmona scheduler can places those replicas on the cluster that still have headroom and this uh This feature especially work pretty well with the uh propagation policy because when federated HPA count calculate the desired replicas when it's doing when karma is doing scheduling it still consider the propagation policy the policy that we define.

So you can uh karma can uh scale those based on maybe weighted policies or like the toleration policy that you have defined. And for AI inference normally uh the simplest one might be from the getting metrics from the applications and uh to measure like uh requests per second or time to first token and you can simply use uh measure this like you are using a single cluster HPA. You can just use Primedia's adapter to export those and Carmala adap Carmala matrix adapter will fetch those metrics on on the Carmala control plane or you can also choose use a GPU based policy uh like fetching GPU territory from DCGM or like from your uh third party environments as long as it's exposed from the Prometheus adapt adapter from the custom matrix command will can can get those information from the Carmala matrix adapter. And so in summary, you can choose the signal you trust either like applications or from GPU or from third party uh applications and Carmala will turn that signal into one decision for the entire fleet and you avoid like per cluster guess work and you're you can place replicas where there's extra headrooms.

So uh besides the electricity, let's talk about the uh reliability on that Carmada provides. Carmatada provides like the u uh cluster failover features like it will continuously uh watches member cluster health through cluster status and if a cluster flips to not ready or unreachable, Carmada marks it unhealthy and isolates it. So uh and also if it's it can also help evict the uh workload from the unhealthy clusters and then migrate them to a healthy clusters. Those are like working out of the box and recently and recently we some of our uh adopter found has some observation on the on this on this features like they found that the real world is actually uh messier than than than we imagined. So the the previous uh cluster failover features is not maybe not like uh cannot meet all the requirements because in some cases many issues are partial like it may be a short control plane blip or like just a network partition that trains one node pool or a storage class hiccup or even just a a GPU driver crash on a subset of nodes.

So if we use these features out of the box and react the same way to every symptom, we can we probably might widen the blast radius. We might be like moving too many and too fast and sometimes even amplify the problem by pushing turns into other regions. So uh uh karma has provide another uh new features called fine to provide fine grain control over the both the failure signals and the tense we applied and and the cluster temp policy is the is that control now that for the new f feature from the new features with this cluster temp policy you can declare when to t a cluster and when to clear it like you can choose the target cluster on add uh and also define two rule sets with add on conditions and uh remove on conditions and for example like you can add a 10 when ready is not true and network available is not true and you can remove the 10 only when both return to true those are matched and signals are also aren't limited to what command computes you can publish thirdparty conditions from your own detectors for example uh the cube component healthy eats published from our own own uh service and Carmala can also match on those as well and those are and these are a couple of like there are uh a couple of operational guard rules to avoid like surprises.

The controller can also can evaluate a rules first and remove rules second and but if both will match the removal will wins and if the you later delete your or change a policy existing tens are not auto cleared. So your operator can stay in control of when an isolated cluster re-enters the pool. So uh in summary like this uh cluster 10 policy gives you fine grain and policydriven isolation which you can plug in with your uh custom uh condition detector or writer and you can uh Carla will respond differently from your uh fragrant uh cluster temp policy. I think the time is running out. So I'll just quickly go over this. uh in in a lot of case like for AI inference you might you will also have a lot of uh uh dependencies with your applications and Carmada's uh application failover can also help you migrate the dependency with your with your ai inference application and it and you can uh controls what happens to the copy and you can also define it uh with the interpreter framework so you can also bring your CRD with this pro with this feature and you can design your custom definition of your unhealthy conditions.

So in summary like if the cluster is fine but the app is not uh you can use this feature and it will help you migrate just the workload and the dependency of it. So you stay available with our original Wi-Fi over and so let me read quickly read up the playbook. So first placement so you can declare what goes where with propagation policy and shape regional differences with override policy and next when you run CRDs you can bring them with the loop with a resource interpreter and right now uh the community also working on supporting like race services from cube ray chain drop from cubeflow so and also fling drop those are you can use it all out of the box you can also use it with your custom CRDs and for guardrails you can use federated resource koda for at uh uh admissions fair scheduling you can use volcano global and for elasticity federation HPA can aggregate the metrics across the cluster and help you uh scale across the clusters and finally you uh car plan for failure as a normal case.

So we provide cluster 10 policy uh application failover for you and right now community is also like focusing on scheduling efficiency control plane efficiency and multicomponent workload support to improve placement accuracy and cola calculations. So so feel free to reach out us on karma slack and thanks everyone and enjoy the rest of come. Thanks. [applause] open for Q&A. >> Hi, we don't have time. >> Okay. >> Yeah. Yeah. >> Uh sorry, you have a question. >> Oh, >> yeah. I have a few questions here. So, I'll start with the first one. Um since Carmada is essentially a control plane that has to run over multiple clusters, is it recommended to run Carmata on like your Cappy clusters that are already managing pieces of that? Uh no we normally recommend to run karmmada on the single host clusters and then link to the already running clusters because yeah because kamada will need will need to aggregate a lot of information from those member cluster. So >> uh normally we'll use some of the some amount of the resources.

>> Okay. Okay, so like let's say I have a control cluster that's using Cappy to say all of my multiclusters I have over multiple clouds run the same version of Graphana. You wouldn't want to put Carmato with that Cappy cluster. You would want to create a whole another control cluster that would reach into all those other ones too. Is that correct? >> Sorry, can you say it again? So to just put uh make an example, if I have a Cappy cluster that manages my services and all my clusters, my worker clusters. >> Yep. >> Would you put Karma in that Cappy cluster or would you create a whole another cluster for Cappy to reach into all those other clusters? >> Like uh you mean heavy cluster like >> uh Cappy as in the uh uh cluster? I just blanked on it. Can someone help me on that one? I forget what >> it's a heavy cluster is the >> the cluster API a cluster API uh cluster >> he has like a host cluster to control like worker clusters where it is we uh where does we in >> oh like I think it depends on like the it depends on a lot of case like your reliability availabilities or or your how how how large your uh host cluster is.

If it has enough resource uh enough uh resource you can just put Carmala cluster on that resource and that it manage all the member clusters. Gotcha. >> And also you can even run it like active active you can have like two milla cluster running on two different host clusters and they can work together because clada is is also designed as like act is also support active active deployments. >> Awesome. answered my next question. And then my following question is because these CRDs require interpreters. Um, is there a place like ArtifactHub that I can find writers or are those all custom or how would I go about finding interpreters that are already written by the community? >> Sorry, >> where can you find the interpreters that are already implemented by command? >> Oh yeah, you can. There are a lot of uh examples in Carmada repository for custom resource uh for custom resource and provided by the maintained by the communities and we also provide a like a playbook for how you uh implement your own uh your own interpreter.

Got >> for those CRD. And then my last question is since we have a bunch of clusters that would be essentially running the same workload, is there a concept of uh like bulkheads or blast radiuses with Carmada or something that you're working further for other than just the cluster tanks that you're working towards now? >> Oh, um I think uh I think there are some of the I'm not pretty sure about that. I can get back to you on that. Okay. >> But they some of the maintainers are working on enhancing that that features as well like adding more tents, adding more different kinds of tents like not because currently we only support like no execute no schedule and right now they are also working on adding more tents to support like more fine grin and more complex cluster failover scenarios. >> Gotcha. Yeah. Thank you. Yeah, there's a there's actually issues that you can follow up on on Carmada repository about about the cluster failover features. Yeah, we were happy to hear your feedback on that. Yeah. >> Awesome.

Thank you all so much for coming and listening and thank you for the questions. >> [applause]