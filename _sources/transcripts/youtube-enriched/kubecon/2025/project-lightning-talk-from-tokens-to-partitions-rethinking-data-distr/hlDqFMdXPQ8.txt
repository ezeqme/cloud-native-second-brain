My name is Daniel. I'm one of the maintainers of Cortex. And I want to try something different here. I'm going to try to dive deep in one of the problems that we are we have. We have a bunch of challenge in Cortex. I want to see if you guys get interested of what we are doing there. Uh just an overview because I want to talk about Cortex and what we do. Cortex uh I don't know how much of guys are familiar with Prometheus. Prometheus is a very well-known CNSF project for handling metrics. It has a injection matrix. It has a query metric that you can use promql and cortex is basically a scalable promethus. That's what we are trying to achieve. Uh we have high availability. We have multenants and we have long-term storage in cortex. Uh which on top of promeus. We actually use promeus a lot as engine for ourselves. So how does cortex works and that's maybe be a bunch uh but what we try to do is is split promeus in a lot of micros microservices. So we have a bunch of microser for each for each type of request that you are doing.

Uh I want to focus here in the remote write path which is basically the remote right for prometheus. So in cortex we have main uh two services two microservices for that area which we call distributor and in gestures. The distributor is basically a proxy uh you can take a look at as that it receives a request. It shards the information and inside gestures. The in gesture is normally a fleet. When you're when you're talking about flips, we are saying that a cluster cluster core text can receive 500 millions one billion time series. So it's a a huge receive this data and it basically is a storage layer. So it's receive the data and putting on disk and the way that they communicate is using this ring which is in the middle like thing. Uh so what what happens is inest when it joins it's putting in the ring distributor looks at the ring and knows where to send the data. So how this ring looks like? So right now we use tokens in the ring. So when we in gestures is starting uh we add tokens for it. We generate random tokens for that in gestures.

Let's say token four here. And in gestures one itself adds itself to the ring and it basically owns that part of the ring. That's that's basically how it works. And when distributor receive information, it hash that information with that hash. It looks at the ring to see which injector should receive that information. So let's say that you have hash three that information go to inesture one and so on so forth. So how a request actually works for for cortex. A request has multiple samples uh time series whatever you want to call it. It's actually different but let's call time series here. Uh and we shard each information by time series. So the idea is you receive a you receive a request with a th00and 5,000 time series depends on how you're sending it and each time series is shed for three gestures in this case because we're using a replication factor of three. So we have to have two out of three for a success result and this information here creates this mesh. So we have each one go to three random in gestures which works fine for us but the problem comes when we have two in gestures down for example.

So if there's two in gestures down and one information is going from these in gestures, we basically have a failed request. Even though it's just one time series inside of the request that's failing, we have to reply as a failed request because we can't confirm that two out of three has a time information. The problem here with this token approach is when we are talking about a cluster with 500 million time series, which a request comes with 5,000 time series and we have a thousand in gestures. uh this comes basically to 100% down down time with us. Uh if we have two in gesture down the chances of one time series going to two in gestures is pretty much 100%. And that brings our fleet to 100% down. So how do you try to solve this? Uh so we are adding this idea of partitions. So now when you look at the ring the inesture doesn't have tokens in the part in the ring anymore. The idea is the partitions itself has tokens in the ring and then in gesture joins a partition. So when you have think about partition one, it actually has a token for and it's actually receiving that data.

So how does request looks like now? So if you look at the request for for a request that we are seeing before, now the time series is actually for a partition and that information is sent to the same three gestures every time. So we we stop with that mesh that we had it before and now we have a pretty cool thing for us which is a replication of data between a so we know exactly where this information is going from going for and why this is so good for us on on terms of availability and that's what we are trying to do with cortex is if we have two in gestures down let's say in gesture two in less than three doesn't matter which a not they are not impacting us because we know that partition two still have two out of three and partition three still have two out three. So the only impact that we have is when we have two inje and what's the results from it? What's the numbers? So I try to do some math. I hope that's correct. But in the current approach if you have two failed in gestures as I said before it's basically 100% chances of downtime.

Uh in the new approach in partition approach if you look at uh 10 partitions a cluster of 15 partitions it's basically 10% of chances of going down. So, and what's neat about this is with scale and that's the idea of cortex when you're scaling Prometheus when you start to add partitions when you start to add in gestures your chances of going down actually decreases a lot because you have more in gestures the chance of they being the same partition decrease a lot so that's pretty cool for us that's a huge achievement for us so how about Cortex now a little bit more uh we are just releasing today version 1 there's a bunch of cool stuff there uh we are trying to get in touch with Prometheus 3.0. So we are supporting remote write v2. Uh there's park spark format which the community is working a lot against about it. It's super cool to speed up queries and latency for for it. And I hope you guys like it what I show it what the challenge that we are working on. And if you guys want to talk to us there's a bunch of uh channels that you can reach out and we're going to have also a kiosk in the pavlon tomorrow and Wednesday.

Thank you go for