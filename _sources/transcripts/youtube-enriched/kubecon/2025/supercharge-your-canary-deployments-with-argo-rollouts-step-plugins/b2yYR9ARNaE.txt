Hello everyone and thanks for joining us our session. Uh today we're going to talk about a new feature that came out uh with Argo rollout one version 1.8 and it's the ability to create custom canary step with the help of of a new plug-in system. So first things first, my name is Alexandro. I'm a staff software engineer at Inuit. I'm also an Argo CD maintainer and a partial contributor on Argo rollout and with me is Kostas. >> Hello, my name is Costis. I'm working as a developer advocate at Octopus deploy and I'm also a member of the Argo team. >> So in today's presentation, we'll first explain uh what is Argo rollout for the people that are not familiar with it and what's the status of the open source project. uh then we'll explain the different mechanism that exist in Argo rollout today to extend its functionalities and integrate with your infrastructure during your deployments. Uh after that we'll get down to our main topic the step plugins and we'll explain the different use cases uh that are solved with Canary step plugin how you can write your own and uh that's it on that note I'll give the floor to Kostas to talk more about progressive delivery so Algor is a controller for progressive delivery so let's define this first just to make sure that everybody's on the same page progressive delivery is a set of advanced deployment deployments where instead of sending the new version straight away to production, you do something smarter.

So in the case of blue green deployments on the left, you create a new version, but you do not send any traffic to it just yet. Then you can test it, make sure that it works, and then when you are confident, you switch the traffic to it. In the case of Canaris which is on the right, you do a similar thing but instead of having a single point in time where you do the switch, you instead gradually send more traffic to the new version as you gain more confidence. So you start let's say with 10% then 20% and so on and so on. So this is progressive delivery in general and then uh this is how you do it with Kubernetes. So we are working with replica sets. So whenever we use blue green we start a second replica set that defines a new version but all the traffic still goes to the old replica set and once we are confident we make the switch and in the case of canaris um we do this in a gradual way. So we use a traffic manager where we send let's say uh 25% of the new version then 50% then 100% and once we are ready we destroy the previous replica set.

So this is how it works uh for kubernetes. So, Argo is a Kubernetes controller for uh doing progressive delivery in Kubernetes and it supports those scenarios I explained blue green deployments and canary deployments but you can also do other things with it like u experiments AB testing and several other things that you need to check out the documentation. Argo itself is Kubernetes native. It's running inside Kubernetes and just to make it clear it's a self-contained project. It's completely independent. You can use it on its own. I'm saying this because a lot of people are getting confused and think that you need to have Argo CD in order to use Argo. This is not true. You can use both of them together or one of them without any um issues. So if you are interested in Argo, make sure to check the documentation for more features. These are some of the numbers. Uh it's a popular project, maybe not as popular as Argo workflows and Argo CD. If you remember, the Argo family is actually four projects. Argo CD, Argo workflows, Argo events and Argos, Argo workflows and Argo CD are maybe the most popular ones, but Argo also gets some love and it needs to get some more love and this is why we're here uh today.

It's used in production today by major companies uh including uh into it. So once you install Argo rollouts in your cluster, you gain access to a new Kubernetes resource which is called a rollout. And there are two ways that you can use this roll out. Either uh you take an existing uh deployment, a standard Kubernetes deployment. You take all those fields, they are exactly the same and then you add a brand new field which is called strategy and defines your progressive delivery strategy. This is what is shown on the left. Or you can just reference an existing deployment that you already have and then the roll out just contains uh your deployment strategy. So this is this example of the first case. On the left I have YAML and all the fields are exactly the same as you know from a standard Kubernetes deployment but at the bottom I have defined a canary and if I remember the example I'm saying send 10% of the traffic to the new version then wait for one hour where maybe I want to run some smoke test or some integration tests then send 20% to the new version and then wait and we will do something else.

Argo rollouts also comes with a UI like all the other Argo projects. So you can see the state of your canary. You can see where exactly it is right now. How much traffic you sent to the canary. You can also see the number of pods for each version uh the old replica set and the new replica set. So this is great you know if you want to show this to uh developers. The whole point of using Argo roll outs is to have fully automated deployments and fully automated rollbacks. That's the the end result. So we want to start a deployment with Argo rollouts and then give our metrics and I assume you have metrics already to Argo where you define exactly what a successful deployment means for you. So then instead of having a human looking at metrics you have Argo make the decision and it can detect your metrics and say okay everything looks good I promote the new version or no we have a problem let's roll back. This also answers the very popular question if you can deploy on Friday at five o'clock and the answer is yes with Argo you can deploy at Friday on Friday at 5:00 then you go to the pub you go to the cinema you go to your friends and after 10 minutes 15 minutes your metric should instruct Argo lounge what to do so either the deployment has succeeded or it has failed and there is an automatic roll back while you're drinking your beer or your wine or watching AV and that's it for the introduction.

>> All right. So, well, as us just explained, we need to evaluate the health of a rollout and we need a way to direct the traffic to the right place with the right percentage at the right time if you want to enjoy your weekend. Um, so to be able to do that, roll out has to integrate to a lot of third parties. uh and we need to integrate to different traffic router, service mesh or load balancer to be able to direct that traffic. Uh obviously these are a lot of third parties that needs to be implemented natively in the Argo rollout codebase. So to avoid doing that and to keep growing uh Argo rollout we had to create a custom plug-in mechanism so we can create uh traffic managers with a common interface and it's the same story for metrics provider uh rollout has to run analysis. It has to know about the health of the new version and that's done by quering metrics. So that's why rollout integrates to different thirdparty uh metrics provider and we have the same issue. So we created a custom plug-in interface to integrate to more metric providers.

Uh this allows us to keep the core codebase of rollout pretty small without uh limiting its capabilities. And the Canary step is the la latest addition to this uh triad of plugins. So maybe you're wondering why do we need more Canary steps, right? We're all doing rollouts today hopefully. Um and that works pretty great. So if we look at the existing steps that we have today, usually your deployment strategy uh will look like this. So we'll use a mix of set weight to control how much traffic we send to the new version and usually we'll use the pause step to control the timing and add a buffer within each steps so we can uh correctly evaluate the health of the new version. If we look more in details into all the steps that currently exist in rollout, there are seven steps. So the set weight I just explained it's to control the amount of traffic goes to the canary. Set canary scale is a bit more complex. It allows you to decouple the number of pods and the amount of traffic. So the main use case is to scale a couple of replicas of your new version with 0% of the traffic.

This allows you to test to run some E2E test suite or some smoke tests to your to your new version with zero person traffic. So you're sure to not affect any uh production load. Um then the pause step is selfexplanatory. Uh and we have analysis and experiments that are more complex. An analysis will allow you to uh query your metrics provider. So let's say you don't want the new version to have uh to to increase the amount of latency. So you're going to query your latency metric. Run an analysis and make sure that the new version doesn't increase latency. You can do that to make sure it doesn't increase the error rate, the CPU, the memory. You can query any metrics and evaluate your new version against it. And the last two steps, they're a bit more um specific. So they're only used with STTO and gateway API. Why is that? It's because the Canary steps are the only way that we have to configure uh logic in or rollouts. So we know where this is going. Uh we're going to need to add more and more canary steps for narrower use cases because it's the only thing we have.

Um so a solution to that is to keep building plugins. So we know that plugins are good because we can integrate to third party. We can also integrate to any use cases with plugins. It also allow us maintainer to keep the Argo rollout uh codebase smaller. So it's easier to maintain. And an another advantage is uh to have a different life cycle for each plug-in. So each plug-in can have their own maintainers be released individually, updated indiv individually. So they you don't need to wait after the Argo rollout release to have your new features. So with that said, uh I'll let Custus explain all the different use cases that we have for new Canary steps. Okay. So why do we need Canary steps? Uh here I'm just going to show some use cases. And as just to be clear, I'm going to show imaginary plugins. They don't exist yet. This is our vision. This is what we want to do with uh plugins. But we think that some of those are interesting to to everybody. So here I have the classic example where it's very easy to create like a roll out for a single application and roll it back.

That's easy. But sometimes you want to do something smarter, especially if your company is using microservices. So here I have two applications, payments and authentication. And each one of those has a back end and a front end. And I want to deploy all four of them at once. And then I want to roll back all four of them if any of them has an issue. So today this is not possible to do with a vanilla arouch. This is also a popular issue for Argo CD. But we could imagine a plug-in that I write an imaginary plug-in uh where I'm defining my canary and just before the end I'm saying hey before you finish the canary check what everybody else is doing. So if any other of your sibling services has an issue then you need to roll back as well even though this service itself might be okay and I can put this in all four services and have a much smarter way of deploying my microservices. Another similar use case is for multip multicluster uh setups or different regions and again this is a very popular issue in um Argo CD as well where I want to control the order of deployments.

So I want I want to send something to many clusters and I want to be certain about the order they go there and also the order that they roll back or maybe have some dependencies between them. Today this is a bit difficult to do. There are some ways but it's mostly let's say uh hacks. So again, I could imagine I have a plug-in. This is not a real plug-in where I'm saying start the Canary and at several points I want to sync. I want to wait that everybody else is on the same place as me so that I have the guarantee about the ordering um of services. Uh another use case is combining the CI and CD words. So Argo CD is great but once you adopt it you have a clear separation between CI and CD and sometimes you need some things from the from the CI world. The most classic example is you want to have a deployment and then run some integration tests or smoke tests or component tests that they exist in your CI system. So you want to combine these two words and say to algor start deployment and then for verification I want to run a subset of my tests that exist in my CI system and changings of course is just an example.

So you could imagine a plug-in here where in the middle of the canary I go and say hey take my uh end to end tests run them and see what happens or my component tests or my um smoke tests. And these were just some examples like the vision that we have for the Canary step plugins. But of course you can have many more choices. Essentially we gave you a way to run any kind of API call any API go to an external system. So you could contact your uh Jira instance call your CRM go to a custom notification tell Graphana that you are starting a deployment. Essentially there are so many use cases even things that we have not imagined ourselves. So how does it work? All right. So now it's time for technical details. Um, so under the hood, a plugin is just a go binary and we're going to execute that go binary as a RPC server within the Argo rollout controller. So at the start of the Argo rollout controller, there's going to be a config map that I'll show later where you configure your plug-in. the Argo rollout controller will download the plugin and uh then it will execute that plugin during the reconciliation.

So the config map looks like this. Um you can see there's a list of step plug-in that has to be configured. The first setting is to give the plug-in a name. You can choose the name of your plugin. It doesn't the binary doesn't come with a name. So as a rollout operator you configure what is the name of your plug-in. You configure a location. So the location can be a either a local file or a remote URL. Uh if it's a local file you can set that up with an inet container. You can have a volume mount and uh you can even bake that in your container image. Um if you don't want to download something at the start of the rollout controller. Um another setting is a disable. So you can disable a plugin because if later that plugin is used and you remove it from that config map, it will cause error in your deployment. So to make sure that a reference plugin uh cannot be called the operator can disable the plug-in uh in the controller configurations. Once the plug-in is configured there then you're ready to use your plug-in in your rollouts objects.

So you can see here an example where you have a list of steps and in the steps you're going to give your uh you're going to give your plug-in a name. The name must match what has been configured in the controller. As I said if the name doesn't match it will be like if your plug-in doesn't exist and the rollout will fail to progress because that step plugin will not be found. Um and the second setting is the config object. So this is a untype object. So you can put whatever you want in there. That also means that Argo rollout won't validate these configuration. They won't be part of the schema. Um so your plug-in will be responsible to validate the user input. Um and that's it. That's how you configure uh your plug-in. But the most important part is to implement the binary, right? Um, so this is the interface that you'll need to implement in your plug-in. There are three very important methods that you need to implement. The first one is the run method. This method will be executed whenever rollout reconciles and needs to execute your plug-in uh in the steps.

Your run method will do your business logic and then can return different status. uh it can return successful. The effect of that will be that rollout will progress to the next step. It can return failed. When uh plug-in return a fail status, the rollout will abort. What does that mean? It means that rollout will roll back totally to the um previous stable version. Your plug-in can also return the running phase with a backoff. So this allows you to run a synchronous operation. So this is called over RPC. So if the logic of your plug-in takes a couple of seconds or a couple of minutes, you don't want to hold the RPC call. So you're going to return the running phase with the backup, let's say 20 seconds. And the rollout controller will won't call your plug-in again until 20 seconds later. and then it will call your run method again and eventually can call your run method multiple times until it returns either successful or failed. The terminate method is called when you fully promote a roll out. So let's say you want to skip all the canary steps that exist.

You just want your roll out to be fully deployed. Um, if there's an ongoing plug-in that's running, uh, you will use the terminate method as a chance to cancel any asynchronous operation that might be ongoing. Uh, and the abort method will be called when a rollout is aborted and that can be for any reason. It's not only because a step plugin failed. It can be because the user manually abort the rollout. It can be because a progress deadline is exceeded. So whatever configuration you have that cause your rollout to be aborted, we're going to call the abort method of your plug-in in a reverse order of the execution. So it's to give a chance to your plug-in to basically revert or undo the operations it did. So, I'm not going to go into more details into how to implement uh the plug-in because we gave a talk uh earlier in the week at Argo where basically that one slide is explained in about 20 minutes. Uh so, if you still have questions, it's normal. Uh go watch the Argo talk. The recording is not yet available, but uh it will be on YouTube, so I'll update the slides um before giving it it to you.

And then um the second step after you've watched the talk is to implement the API. So in the other presentation we're doing a live demo and we implemented a step plugin with Genai. We ran three prompts and the interface was fully implemented the way we wanted it to be. So it's quite easy actually to write your own plug-in. And after that, well, you just build a binary, write documentation, publish that to a public repository because we love open source. Uh, and you can add your plug-in to the Argo rollouts documentation. We have a list of the existing plugins. Right now, there are no existing plugins that are available. So, that's why we're presenting the use cases today. Uh so if it sparks something in your imagination, you can contribute uh your own plug-in and then be a maintainer of this plug-in if it gets uh adoption. And that's it. >> No, no, no, no, no. Wait, >> that's it for the presentation. >> That's it for me. >> Yeah. So takeaways, but we are at the end. Essentially today we wanted to announce that we have completed the trilogy.

Argo has a plug-in mechanism for traffic managers so that you can use your own traffic provider. There is a plug-in mechanism for metrics so that you can use your own metric provider. And from today there is also a mechanism for your own canary steps. So this means two things. First of all, it makes the job a bit easier for us because you can focus on the core code of Argo and it also makes things easier for you because now you can take your own custom use case that is important for you and for your company and implement those um requirements in plugins and then these plugins can have a a completely different life cycle. You don't need to wait from us anymore to release Argo in order to get new functionality. You can be your own maintainer and create your own plug-in. At this point in time, there are also several open issues in Argo in GitHub where somebody is asking for a specific use case and the answer until recently was oh, this could be a great idea for a plug-in, but we don't have a Canary plug-in mechanism just yet.

So now we have this mechanism. So you need to go over all those issues. And if you have this idea and we said it's a plug-in, now you can start h creating it. Uh so if you want you know to help argor create your own uh plugins but also if you want to help us with the core code you are free to become a maintainer and we are talking about Argo today but all the Argo projects need help Argo events Argo CD Argo workflows. So please join us. Uh the QR code is going to the CNCF Slack instance and there you should find different Slack channels for um all the Argo projects and this is where discussions are happening. This is where you will find us the maintainers. This is also where you can ask questions about the projects and also explain your brand new idea about an Argo Lounge plug-in if you want uh feedback. And now that's it. [applause] So we still have time for questions. If anyone has questions, please come to the mic in the front so we can hear your questions. So, this question is kind of a general one and it may be one that is answered.

I just don't know. What is the recommended way to pass secrets into plugins for Argo CD since I noticed there wasn't any way to do that with the innit? >> Yeah, so great questions. Uh, so since this plugin runs as a uh RPC server, it runs within the Argo rollout container. Um, so that means this plug-in has access to the same thing as the rollout controller. So if that plug-in needs any kind of secrets, you can provision that secret in the Argo rollout namespace. Uh, and Argo rollout basically will have uh your plug-in will have access to get that secrets with the Kubernetes API. Thank you.