Hi everyone. We're going to talk about running large language models on Kubernetes because we live in 2025 and generative AI is everywhere. If you have been doing Kubernetes for as long as I've been doing it for almost 7 or 8 years, running large language models is your job guarantees. Because running stuff on top of Kubernetes is the equivalent of writing COBOL code 25 years ago. This thing is going to be around for a while, so don't worry about it. All right. So, we created this talk a while ago with a colleague of mine and we called it this provocative title. Yes, you can run LLMs on Kubernetes. And the idea was would would be that if somebody reads it the title, they would be like, "Who said you can't?" And the idea is not you can't. The idea is how can you do it? And I'm not going to focus too much on actually running large language models on Kubernetes itself, but focus more on the challenges of running running large language models. My name is Abdel. I'm a cloud developer advocate, as Casper said, at Google.

So, by show of hands, how many of you knows Kubernetes? All right, good. You are at the right conference. Awesome. How many of you have run large language models on Kubernetes? Oh, okay. If there was more hands, I would have said, "Thank you," and walk off stage. But apparently there is none, so. >> [snorts] >> Generative AI is evolving. It's evolving very fast. And it's evolving also in a pace and in a manner where people care more about local open large language models, not only cloud-hosted ones. You can't use cloud-hosted LLMs all the time. Gemini, ChatGPT, Claude, all these things are not always useful for you. Either because they cannot do the things you need them to do, or for regulation reasons you cannot use them, or for latency reasons you cannot use them. And so therefore, open LLMs are the alternative. They are open because they are either open source or open weights. Open source means you have the code that used to be that used to train the model, or open weights means the weights themselves are available.

And there is like a whole, you know, ecosystem of large language models. Everything from Google to Llama to Gemma to Mistral to whatever to DeepSeek to all of these things. The challenge is that these models are getting bigger and bigger and bigger and bigger. They're not only bigger in size, but they're also bigger in terms of the the complexity of the model itself and what it can do. So, before I move on, I want to spend a little bit of time talking about some basic concepts so we all understand what I'm talking about. I think large language models everyone knows what it is, right? I'm going to be using the term inference, and I know that a lot of times when people talk about inference, they kind of mix it up with inference and serving. They say we are doing inference on the model in the sense of we're doing serving on the model, but these are completely different things. So, inference is essentially the process by which a model produces a result for the first time after it have been trained.

Inference is the usage of the actual model. It's how the model processes the input to produce an output. Serving is the process by which you host the model. The easiest way I explain serving is I tell people think of serving as when you have a website. A website is essentially CSS CSS, JavaScript, and HTML. If you want to run a website, you need a web server, right? Without a web server, CSS, HTML, JavaScript are useless. So, a web server to a website is what a serving engine is to an LLM. A large language model is essentially a file, usually a text file with a bunch of gibberish code in it that you wouldn't understand. But it's not a binary. You cannot execute it. So, in order to make it useful, you need to run it inside something that can take input from you as a user, translate it into how the model works, inference part, and then return the results. So, that's the serving part. So, a serving requires a serving engine, and today on the market there are a couple of very popular open source serving engines.

I'm going to talk about one in a minute. Accelerators, I think everyone knows what they are. If you have accelerators, you have money, GPUs, TPUs, whatever. Um special non-CPU type uh chips, essentially. Quantization. Quantization is essentially a a precision characteristic of a model. A model has a quantization value, usually around the 16. The higher the quantization, the more memory footprint the model needs, and the more precise it is. The lower the quantization, the less the less precise it is. So, if you take DeepSeek, the biggest model, 67 675B parameters, like 675 billion parameters or something, you can't run that on your M4 MacBook on your at home or on your um well, on a Mac at a lower quantization. The model is going to be less precise, but it will require less memory. Weights are the numerical value that describe the connection between the neurons. I'm going to talk about that in a bit. Context window is very important to understand. This is actually some This is where people who are doing platform engineering or DevOps start to be a little bit it's it's it's becomes interesting for you.

Context window is essentially how many tokens a model can process at a single shot, right? That's the size of the input, essentially. And why context window is interesting is because large language models are stateless by default. If you take a large language model and you ask it one question, let's say, "What's the capital of Denmark?" it will re- re- reply Copenhagen. It's Copenhagen, right? Okay, cool. And then, if you start a new chat session and ask the question, "How many people live in that city?" For us humans, we understand that if I ask you how many people live in that city, you know that the context is Copenhagen, right? But a model across two different chat session wouldn't know because it's stateless. It doesn't have memory. So, typically the way you make a model look smart in a chat session is each time there is a new prompt, you send the previous questions and previous answers, right? That's what we call the context. The context is essentially the full history of what you have been talking with the model.

And as you are chatting with the model, the context gets bigger and bigger and bigger and bigger, right? So, if you want to make a to run a model that looks smart for a chatbot, you need a model with a very high context window. Context window is always expressed in tokens, and tokens are essentially the the way models process input. A token could be a word, could be a number, could be a combination of letters, could be anything, right? Then the last thing is multimodality. So, I want you to keep in mind these two last things because we're going to talk about them in a bit. Context window is very important. Multimodality essentially means that the model understand more than just text. It understand audio, it understand video, it understand images, right? Some of the most commonly used open source model servers are these that you see on the screen. vLLM is one of the most popular ones. It's written in Python. Hugging Face from TGI, sorry, from Hugging Face. Nvidia has a model or a serving engine called NeMo.

NeMo is a little bit particular in the way it works because vLLM and TGI allows you to run any open model that is compatible with vLLM and TGI, while NeMo is a packaged solution. It's a packaged container that contains the serving engine NeMo and the model. So, when you're downloading a NeMo from Nvidia, you download a NeMo that runs Llama 70B, blah blah blah, for a very particular model, right? So, what what Nvidia does is that they package the model and the serving engine and they optimize them, and they ship you a ready-to-use container. It is a paid solution, by the way. Ray Ray is an open source Python framework for doing large end-to-end large language um end-to-end machine learning including training, fine-tuning, reinforcement learning, etc. And then Ray has a Ray Serve engine, which relies on vLLM under the hood. Then you have JetStream and Ollama is probably the one that you all know you're familiar with if you have run it before on your laptops. Um there is an engineer at Google called Clayton Coleman.

He made a talk about a year ago, I think, at KubeCon, where he made this statement called large language models is the new web app with a little asterisk at the end. And we're going to see a little bit how large language models kind of similar to to web apps and how they are very very different from web apps. Just keep this in mind. >> [clears throat] >> So, running large language models in cloud can be done in any of these three ways, right? If you go all the way on the left side of the screen, you have fully managed, fully automated solutions. If you run on cloud providers, Bedrock, Azure AI, Vertex AI, any of these solutions. Fully managed, you don't see how it works. You just give them a model, they give you an URL, an endpoint, and that's it, right? Um you can do things your way, which is on the right side, so virtual machines, bare metal servers, you know, low-level Linux stuff. Kubernetes gives you a little bit of that mixed between the fully managed solution because you have the self-healing, the automation, and all the stuff that Kubernetes brings, and then the the flexibility of doing things or tuning things your way.

I don't I don't think I need to to to to to spend too much time on this, but why do we want to run large language models on Kubernetes? For many reasons. The automation part is obviously scalability. Kubernetes started about 2 3 years ago actually adding more capabilities for large language models. One of them is device management for managing GPUs. There is something called device plugin on Kubernetes, which allows you to claim GPUs using Kubernetes primitives, so using YAML files. Um there is another API that that became GA actually very recently called DRA, dynamic resource allocation, which allows you to claim GPUs, claim accelerators for large language models or for machine learning models, and multi-cloud, so you can run it anywhere, right? So, why would you want to run large language models locally? Well, many reasons. Regulation would be one of them. You run on a regulated industry that doesn't allow you to use cloud. That would be the easiest one. Latency, you want the application and the large language models to be as close to each other as possible.

Um special use cases in in in in a case where that large language model, which is hosted by cloud provider, doesn't work for you, so you need to fine-tune it, which means you need to start from a large language model, tune it specifically for your use case, and then run the tuned version. Privacy is another one. You simply don't trust the cloud. Um cost would be another one. Most of cloud hosted large language models charge you for input and output tokens, and those can go out of hand very, very quickly, especially if your developers don't know what they're doing. So, you want to control cost, or you want to have predictable cost, in which case you're going to buy a hardware up front and then large run the large language model, or any combination of all these reasons, right? So, let me show you really quick how that could look like. I mean, >> [snorts] >> if you are a bit like me and you're a big fan of YAML, cuz let's face it, somebody who does Kubernetes is just a a YAML engineer at heart. Um, this is how a deployment could look like.

This is a running vLLM. Um, so, nothing that different. Here we have the container. Uh, one thing you will notice is that we're running here vLLM OpenAI. So, OpenAI uh, came up with a an API spec um, few years ago called the Open the OpenAI API spec, and that API spec basically standardizes the way the model or the API gets presented to you as a user. So, the way you would call the model will be standard regardless which model is running behind the scene. This particular container that I'm using here is vLLM plus the Open API spec um, uh, activated on it, right? I have my resource requests. One thing you will notice here is this. This is part of what Kubernetes offers out of the box, device management. You can actually request a GPU. So, here I'm saying I want one GPU. I have a bunch of um, uh, arguments to start my container. One of them is which model I want to run, how many tensor parallel size, so how many GPUs I have. In this case, I just have one. Um, and then where does vLLM start? Just one thing to keep in mind quickly, those are arguments to vLLM itself.

They're not arguments to the model. They're they're arguments to just the container itself, right? Um, so, in this particular case, what's going to happen is vLLM is going to start and then it's going to download the model from Open Face um, from Hugging Face, not Open Face, because I passed the dash dash model flag, right? Um, also, I have here a token. This is a Hugging Face token. That's how it will, you know, authenticate to Hugging Face and download the actual model. And here I say I want [clears throat] a node selector. Uh, my cluster that I'm running in this particular case has couple of nodes that has GPUs, couple of nodes that doesn't have GPUs. To make sure my my my workload ends up on a node with GPU, I just have to add a node selector. You could also do taints and tolerations um, if you're running on top of a cloud provider that offers a man a fully managed Kubernetes solution like um, uh, Autopilot and GKE or I forgot the other one on on uh, on uh, on AWS. You can they have their own ways to allow you to say I want this particular type of nodes to be provisioned for me.

Um, so, this will run the model for me. So, if I go and look it look it up, I just have a a pod running. Uh, one thing I want to do with you here is I want to quickly check the logs of that thing, and I'm going to I'm going to >> [sighs] >> explain to you why in a second. So, let's see how this container started a while ago, right? So, let's go back in the logs and see how it started. Um, so, I have my um, uh, uh, vLLM starting. This is the the the the the first log item of the vLLM container. Um, keep in mind this value here. We're going to talk about it, 518, right? Uh, then it did a bunch of things, blah, blah, blah, and then this is where it started downloading the actual model. So, this is um, downloading the model from Hugging Face and uh, loading the model into the GPU and doing a bunch of things, and then where it says it's ready, it's basically there. Here. This is where vLLM says I'm ready to serve traffic. So, it took about roughly 2 minutes between the model that the container actually starting and the application being ready to serve traffic.

One thing to keep in mind here is I'm doing something a little bit stupid. I'm not using any sort of like health checks like liveness probes or readiness probes. So, in this particular case, when my container starts, Kubernetes flags this as ready, but if I try to talk to it, it's going to return nothing for a while, right? So, cuz the model is not loaded yet. So, if you're going to large run large language models on Kubernetes, one recommendation is obviously to use um, something like uh, health check probes. These are the health check readiness probes and liveness probes for uh, vLLM itself. So, vLLM has a {slash} health end end point, and then it will return 200 on that when the model is ready to be served to to serve traffic. Um, so, now if I go and um, port forward my container here and then run another curl command somewhere uh, like that. Um, I'm going to show you something real quick here. Um, so, I'm going to run a curl on my local host, obviously. {slash} v1 {slash} chat {slash} completions.

That's the OpenAI spec. That's essentially how OpenAI defines its end points. So, the {slash} completion is the actual end point where the model is. Notice here that I have to pass the model ID as part of my uh, body of the request in the JSON payload. And um, I have to pass a bunch of messages. My messages are an array, which means I can actually append them. So, if you're trying to create uh, a real chatbot, you will basically append the new prompt to the array each time the user types a prompt in the application, right? So, you will send a very long um, uh, a JSON file. And that essentially is the the context window that I've been talking about, that that that array of messages. And the reason why here it says raw user is because most models also support something called system prompts. So, system prompts are the instructions that you as a developer will send to the model to tell it to behave a certain way. If you want to limit or make the model focus on a very specific problem, you can send a system prompt to tell the model, "You are an accounting expert.

Please answer these questions, blah, blah, blah." And then you can tell it very specific instructions. So, in in you don't want those to be sent by the actual user, you send them as part of the application system prompts back to the to the model. Um, so, if I run this, it's going to return an an answer about why is the sky blue. It's going to take a little bit of time, cuz this is a very, very basic deployment with no optimizations whatsoever. And I'm going to talk about what those optimizations are in a bit. So, now I have my return and you know, my prompt, very, very, very easy. So, this is like the most basic bare-bone way of running a model on Kubernetes. A regular deployment, run the model, and that's it. Um, um, in case I have I mean, in this particular case, I just have one pod, right? So, it's pretty basic, but um, let's say I start suddenly to have more traffic on my application. So, the obvious um, response is I would I want to scale it out. I want to like add more replicas of my pod, so I can handle more traffic.

It's not that that simple. >> [snorts] >> You remember I started by saying that large language models are the new web applications. Now, we're going to talk about why they are not the the new web applications. Well, one of the most obvious ones is the fact that they require GPUs. Web applications doesn't require GPUs, right? Obviously. But, the other way is the fact that the model servers are actually pretty big. So, these are a bunch of This is a benchmark I did on a bunch of these very popular uh, model servers. You can see the vLLM OpenAI is the second one. It's about 5 GB in size. Um, when we started when Kubernetes started 10 or 11 years ago and containers probably 12 years ago, this entire time everybody was just trying to optimize the size of container to make it as small as possible, and now OpenAI is just like or uh, GenAI is just blowing that out of proportion. You can see like if you go all the way down to Puddle, it's 14 GB. What you see on the last column is the average image pull time.

So, if we take the example that I showed earlier of running Gemma with vLLM, between vLLM starting and then the model being ready to serve traffic, it takes about 2 minutes, right? Then it takes another 2 minutes roughly, 155, to pull the model. So, essentially between the time you do a kubectl apply on a vLLM with a Gemma 1B, which is a very small model, on Kubernetes, it takes roughly 4 minutes for the container to be ready to serve traffic. So, you can imagine in a scale out scenario, this would be super slow, right? If you have a horizontal pod auto scaler um, targeting that pod, looking at some metrics, and then scaling up the pod horizontally when there is more traffic, it will just take more time or it will take time essentially um, for your container to or for your models to scale out. >> [snorts] >> And things get a little bit more complicated as you are going up in the size of the model themselves. So, this is uh, a little table that shows couple of the open source models on the left side.

So, we have Gemma, Llama, Falcon, uh, Flynn, I think. Yeah, and DeepSeek. And you see the model variants, so that's number of uh, billion parameters, and then you see the precision, the the quantization. Uh, I want to draw your attention to the last lane. A DeepSeek 685 billion parameters, so that's the largest variant DeepSeek model that exists today, at a full precision, requires 1,370 GB of GPU memory. 1.3 TB of GPU memory, right? Today, there are no GPUs in the market. I dare you to challenge me on this that has that much memory. The highest The biggest GPU you can get on the market today is an H200, I believe, which has 80 GB, right? So, we can do a little math question, which is divide 1,370 by 80, to figure out how many GPUs we need. We're going to need 17. So, to read to run DeepSeek 685 uh, [snorts] billion parameters at full precision, you need 17 H200 GPUs, right? And I don't know how many of you are as old as me, but if you have done any sort of hardware work in the past, you can't fit 17 GPUs on a single server.

There is a physical limitation to the number of PCI Express ports available on a single server, which means to run DeepSeek higher precision, you need more than just one node. You probably need two because most probably you're going to have a limitation of eight PCI Express ports per node, which means you need two nodes to run a DeepSeek model the the largest variant at the very highest precision. Um and that creates a bunch of challenges. >> [snorts] >> One of them is how do we do multi-host serving? Um what I showed earlier is a single node, single GPU, so that's one Gemma 1B running on a single node on a single GPU, right? But you might actually run into situations where you might do single node multi-GPU, so that's the same model is loaded on multiple GPUs on the same exact node, which is a a a pretty easy way, but it's relatively Most serving engines like vLLM already knows how to do that. But splitting a model across multiple nodes create another host of problems which I'm going to touch on in a bit.

Before I move on, another way in which large language models are not like web applications is the way traffic or network traffic looks on those particular large language models. Uh web applications tend to have uniform small requests that are very fast, right? Microservices mostly have these kind of characteristics. A lot of requests, very small, very fast. Large language model could have requests of variant sizes. It could be a very small prompt, "Hi, how are you today?" or it could be a book to summarize, right? It could be a video, it could be an audio, it could be any sort of um of of this kind of variations in how the request looks like, which means that the request load would not be distributed evenly, and if you use a regular load balancer, an HTTP load balancer, you might end up with what you see on the right side, which is instances that have a lot of load and instances doesn't have a lot of load. So, uh I'm going to let's skip this >> [snorts] >> to this. So, examples of how those kind of network traffic profiles can be different um are things like a code assistant.

A code assistant will have very very large inputs because as part of using a code assistant large language model, you're sending your entire code base and then a question, right? So, the input is very large. Um a chatbot um uh might also have the same characteristic, very large request, especially as the user is speaking to the model and then asking follow-up questions. So, the more questions they're asking, the bigger and bigger and bigger and bigger that input is, right? Uh a generative AI media, so and large language model that can generate text or video or or audio can also have very large inputs um either a video or like a picture or something like that. Um or sometimes you would have just have small prompts. So, if you're familiar with this uh API in Kubernetes called the Gateway API, I hope it's 2025, I hope everybody knows what the Gateway API at this point is. Um the Gateway API was created to, you know, to replace Ingress in a way. Um and then few years ago, a new extension was introduced to the Gateway API called the Inference Extension.

The Inference Extension is meant specifically to solve the problem of uniform or un-uniform or not uniform uh network or prompt inputs across um instances. So, how does it solve it? By introducing two new objects, one is called an inference pool and one is called an inference objective. So, an inference pool is just a parameter or like an object that tracks multiple model servers. So, you would have multiple Gemma 3, multiple Llama, multiple Falcon running, and then you group them inside an inference pool, kind of like a node pool in Kubernetes. And then an inference objective is essentially the way you define policies. That's how you tell the inference extension, "I want to prioritize this traffic versus this other traffic or I want to route traffic from this particular point um to this particular point." An example here would be um body-based routing. You remember here when I said um parts of the inputs for OpenAI, you have to specify the model in the in the JSON payload. So, the question is, "If I have multiple types of models with multiple different models and I want to expose them behind a single load balancer, how do I make it sure that when the request comes to the load balancer, the load balancer knows where to route the request based on the model ID?" Um the Gateway API itself out of the box does not know how to process the body of the request.

The Gateway API can only look at the request path, but it cannot read the request um body, the the payload. So, that's why the Inference Extension added something called the Endpoint Picker Extension or Endpoint Picker EPP, and the Endpoint Picker is a component that when the request gets to the Gateway, the Gateway will send the request to the Endpoint Picker, the Endpoint Picker will pull the body of the request, pull the model ID, and then exact returns back to the Gateway API which endpoint to forward the traffic to depending on the body of the request. And that's essentially where you will also define some policies. You can also define um prioritization policies. You can say, "If requests are coming from my chatbot application, I want to prioritize this versus requests coming for another type of application, right?" Um so the the the Inference Extension extends the Gateway API to make it large language model aware in a way. And then last but not least I'm going to touch on is something called LLMD.

Large uh um doesn't stand for anything, it's just LLMD. It's a open source project, it's collaboration between Google, Red Hat, ByteDance, and uh Nvidia. And what it is, it's essentially taking the Inference Extension in the Gateway API, taking vLLM as a core um uh serving engine, and creating well-defined configurations that make sure that when we run a large language model on Kubernetes, we run it in an optimized manner. So, LLMD defined this thing called well-lit paths. So, a a well-lit path is an opinionated way of deploying things, and as of now it has two well-lit paths, something called intelligence intelligent inference scheduling. This essentially deploys vLLM behind the Inference Extension with the Gateway API, and then introduces a new endpoint picker. So, it's a a revised version of this component here called the Endpoint Picker, which intelligently knows how to schedule the requests based on multiple characteristics. One of them is the the body of the request, so the the payload of the request, but another one is how busy are the models in the back end are.

So, the Endpoint Picker that the LLMD introduced is actually able to read metrics from vLLM to know how busy a particular pod is. How and by busy, the the way we define busy in a large language model sense is not CPU and memory utilization, it's GPU utilization, it's a queue depth. So, a lot of these large language model serving engines, they they will queue or they will batch the requests. So, as the requests are coming, they will group them in a batch and then they will process them in one go. So, that creates a queue, and vLLM can expose a metric that tells you how big the queue is, and then the the Inference Extension in LLMD can read the size of that queue and decide to forward the request to one pod versus another depending on the size of the queue. It also gives you the flexibility to add some policies like say, send traffic Uh for example, um let's say if you want to um if you want to skip any intelligent processing for small prompts, so if the prompt is like, "Hi, how are you?" I can define a policy that says if my prompt size is less than a certain number of tokens, just like process it right away, don't don't do any sort of smart scheduling, but if it's bigger than a certain size, then you do some some sort of smart scheduling.

So, that's LLMD. The second one is it defines something called disaggregated serving. Um I don't want to go too much into the details here because I don't have time, but disaggregated serving is essentially a process by which you split the inference process of a large language model across two steps, a prefill and a decode. Very quickly and very loosely speaking, prefill is the process by which you take a prompt, you read it, and then you generate the tokens, and then you put them in the GPU memory. Decode is the process by which you generate the actual tokens. So, prefill processes the input, decode generates the output, right? So, if you like what what the example I showed earlier, I'm doing prefill and decode on the same instance. LLMD will split or has a capability to split these two um processes across two different instances. You will have pods that are specifically dedicated to do prefill and pods that are specifically to do decode. So, the pods that are specifically doing prefill will generate the tokens, load them into a cache, transfer that cache to the pod that can do decode, and the pod that can do decode generate the tokens.

It's a I've done a 3-hour talk about this yesterday in Belgium, so I'm not going to go into the details here. I'm happy to answer the questions later, but just LLMD has these capabilities, intelligent scheduling, prefill decode, disaggregated serving. There's actually a white paper about this if you want to read it. This is kind of roughly how the architecture of LLMD itself looks like. It uses the inference gateway, which is Envoy-based. Um it has the inference pool in the back, so that's where your uh pods will be running, and then on the side it has the inference scheduler, so that's the intelligence component which routes traffic to a specific instance based on utilization, how busy it is, the the queue depth, the utilization of the cache, etc. etc. Very quickly, couple of things. We talked about single host serving, single host multi-accelerator serving, multi-host multiple accelerator serving. The example I showed earlier of deep 6685B parameters which require 17 GPUs. There is an API Kubernetes called LWS, leader worker set, which allows you to actually do that.

So, leader worker set is the API in Kubernetes that allows you to do multi-host serving. So, running a model across multiple hosts, right? And it it it it it looks a little bit like Kubernetes in the sense that it has a leader and a set of workers. The workers themselves are stateful sets and the leader is a component that is tracking where the workers are and where the neural network is, right? And so, leader worker set is how you would do multi-host serving on Kubernetes. So, I think this is my last slide, hopefully. Yes. Um I want to just recap everything I've been talking about. Running large language models on Kubernetes is similar to how you would run a web application on Kubernetes because the it's all YAML based. But, the challenges you're going to have to solve are very very different. And all the layers of optimization that we know for web applications, caching, horizontal scaling, um etc. doesn't really necessarily work for large language models. Just taking a large language model which is slow, running it on top of a faster GPU is not going to give you more better results.

You will have to think about it as multiple layers, including the accelerators they're using, the serving platform, the large language model itself, the underlying storage, etc. etc. So, this is the end of my talk. We have this QR this QR code that will take you to a GitHub repository. We wrote a demo that basically runs like 10 large language models on top of Kubernetes and gives you a single web interface to talk to all of them at the same time if you want to give it a try. If you have deep pockets and enough GPUs to burn, this is a great demo to run, actually. And with that, I'm running out of time of time, right? All right. I'm going to stop here. This is my QR code to my Linktree if you want to connect with me. Thank you so much. I hope this was useful. >> [applause] >> Thank you so much.