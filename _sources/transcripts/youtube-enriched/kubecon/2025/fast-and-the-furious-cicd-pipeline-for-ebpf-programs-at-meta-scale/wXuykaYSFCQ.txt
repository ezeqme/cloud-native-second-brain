So yeah, so the topic for today's presentation is like fast and the furious CI/CD pipeline for EBPF programs at metas scale. Basically I first want to like start by setting up some context for the talk. These are all the people who are involved in developing and supporting our net binary which basically manages and deploys a bunch of ebpf programs across various attach points doing multiple tunings for packets and connections and also managing the entire congestion control algorithm in the entire meta fleet. So last year we published a paper for this and this talk is adding on top of that paper where we will go a little bit into details on like how we are able to manage so many ebpa programs at metas scale. One thing to remember throughout the presentation would be our majority of EBPA programs are are actively changing the status of the packets and the connection not just actually monitoring and obsering the states. Okay. So about us. I'm pranker. I'm a software engineer. My partner in crime Theo unfortunately he has to attend an urgent um teaching commitment in the Bay Area.

So he had to like skip the event. One thing if I want to like talk about myself is like um I started by writing the Rocky V2 RDMA AI transfer protocol in like assembly language into the nick firmware. Then I moved on to like the kernels and now I'm doing all the network optimization at like the user space level. So over my 12 plus years of career I have been very deep into like the tech the network stack. Cool. So yeah, recently well again recent is of course relative a lot of companies have started adopting a BPF a motivating factor being impressive performance wins and now eBPF is becoming crucial and crucial for the cloudnative workloads especially in the AI area where the the demand of the cloud and the efficiency requirements are going off the charts. At Meta, we have had several significant wins across various metrics and the services. For example, we were able to do like 40% improvements in the P99 latency for commerce and research apps, 30% faster image generation, 100% faster storage read.

A little bit of caveat, these metrics might be a little bit old, but I'm using it as it is because the recent ones are not made actual public yet. So motivated by these wins, we want to use ebpf almost everywhere. So the graph here on the right actually shows like the ebpf deployment. So at like a 99% of all our machines, we actively use ebpf by default and we are constantly evolving our ebpf infra to support our evolving applications. The graph on the right here, we have at least like two, it shows like at least like two versions of our BPF programs are being rolled out in a month to actually support like our rapidly evolving AI services and stuff. So basically to support a rapidly evolving BPF ecosystem, we need to like build tools to rolling out frequently our EBPF changes into production with confidence and they should be like reliable. A little bit of introduction of eBPF. It's a technology in the Linux kernel that allows us to like run the sandbox programs in the kernel space enable enabling dynamic safe and efficient customization of a Linux kernel at whether it's like networking observability or security.

But the most important thing is like it does it without you needing to change the kernel. So no reboot, no disruption of the host and stuff. only programs that so eBPF consists of like these two main components the kernel verifier and the just in time compiler. Only programs that are passed through like the verifier checks are allowed to run which basically guarantees you whatever program you are loading it onto a host will not actually crash the kernel. So verifier provides thread guarantees to you and the just in time compiler translates the ebpf by bite code into like a native machine code for the host CPU which greatly improves the performance of your user space ebpf program to run as fast as like the kernel code which makes ebpf a practical for like high throughput and low latency use cases. Now let's go into like what are attach points or hook points. Hook points are where EBPF programs connect to the kernel to extend or observe its behavior. For example, TC, XTP, Croup, SIS calls, trace points, etc., etc.

And we have EBPF maps. These are like fast and flexible data stores that enable communication and the data sharing for EBPF programs. So basically these works as like a communication method through which your user space uh can program your EBPF programs with some policies and then later on if you want to like share a state across EBP programs you can use EBPM map to actually share that state as well. Okay. So now I want to like take a step on like discussing a concrete example of a eBPF related issue at META. So basically what was happening it was a combination of like various things happening at the same time. We had like stacking enabled. We have like jumbo frames onboarded onto specific services and we have like various ebpf programs running on a host which are doing MSS tuning and our own congestion control enablement and stuff. So based so with this all of these things we were able to like identify this entire combination resulted into like five distinct service issues. Few can be classified as like throttling MSS match happening services seeing latency issues and stuff.

Another thing was like pixie started failing during like provisioning time. So basically it's an example we will go at the later slide on like how we solved it and stuff. Um so when you are talking about like building an eBPF CS3 pipeline you need to account to like test for these like multiple combination of scenarios which can happen and also build a proper reasonable tools for like monitoring and detecting a broad set of metrics for troubleshooting and most important we need to have tools for like fast recovery or mitigation. Okay, so basically EPF changings can impact seemingly unrelated services can have a huge blast radius and these are generally really really hard to triage until and unless you are you know what you are looking for and where you are looking for. So ebpfentric pipelines and observability tool are crucial to build the confidence for like pushing out these changes like frequently into the fleet. Okay. So in this talk we will talk about like challenges and our solutions and like the observability.

So first I would like to discuss how's rollout and the testing infrastructure works before highlighting any ebpf related specific challenges and then we will go on to like discussing what tools we built to actually overcome those things. Okay. So at Meta we already have a CI/3 pipeline infrastructure that works really well for like large microservices at scale. We call it a conveyor. This allows us to run specific test designed by the service owners but for deducting bugs and issues before production rollout and during the production rollout we use the standard phased out approach. We roll it slow and then catch it up later on. It's a standard tradeoff between like speed and the safety. But for eBPF the challenge here is with the test. We can definitely write a test but there's no guarantee that what we are testing will actually hold into production or not. It's because of like a strong coupling between BPF and the kernel. We need to improve on this existing infrastructure and add uh add new things to to support our eBPF.

Okay. So now coming on to like challenges. So here's an example unit test. We all are aware these are like individual tests which run in parallel in like control and isolated environment. Uh these are very helpful to like test BPF interfaces and specific code path leveraging the homogeneity of our software stack and they provide an a very good signal before the code is actually committed. Here one thing I want to highlight is like we have accumulated a large database of existing policies to manually generate a combination of like the packets and the connections and so we can have a lot of combinations to exercise both the different code parts as well as the different options provided with the various ebpf interfaces and since these tests actually run in like sufficiently constrained environment we can validate their behavior and deduct failure using like simple thresholds. Okay, so for controlled and isolated environment the kernel provides us with like various services like croups and the network name spaces which are awesome for like few of the hook points like TC croup and stuff and will result in like testing what we want to actually test.

But BPF requires like more new services because few of the hook points do not actually support like multiple programs with the same aim. And when you are talking about a broader set of like the attach points which we use internally at meta unique challenges will come into play when which we will talk about here as well is in the next slides. So here's an example of like using the strructtops attach point. Basically we use it to actually write our own congestion control into ebpf and then actually roll it out. So here the problem happens is like since we have already rolled out our congestion control into the fleet every host had a BPF program loaded. Now when we are running the unit test which is again a service in like our container it identifies okay I have the same a BPF map already available onto the host. So the structs won't actually work as it is for a unit testing. So we had to like invent tools of like dynamically changing the name of our test BPF map to like run it and like verify whether it's like working as expected or not.

So here on the graph on the right what we are showing it it's representing the diversity of like BPA programs at like different attach points. At P 75 we have like at least four attach points available at a time into meta scale. So significant diversity in kernel parameters provided at each attach point demands actually new tools. Again an interesting case in meta we actually roll out our kernel like really really fast and crazily. So on the graph here you can see like at P50 we have at most like 16 different kernel versions at a time into the fleet. And when you're talking about like a P90, we have like 100 plus kernel versions. Uh, and each ebpmf hook point is essentially an insertion into like the kernel. So it's very dependent on the kernel changes. And since we have this like a hetrogenity in like the kernel versions. So services also gets this hetrogenity where a service can have like multiple kernel versions at a time in the fleet. in production and while testing we have identified many instances where a regression into the kernel resulted in like weird behaviors of EBPA program.

Uh a a point over here we actually take the upstream kernel version we don't have any uh in-house in-house standard kernel version. So in essence the heterogenity of the kernels as well as the hetrogenity of the services thinks of like us to like think in like what additional things we have to add for building our pipeline. This is also a very interesting challenge. So we talked a little bit about like how different teams owns like v different BPF programs and most of the time they will eventually be attached onto like the same hook points. Okay. So here is a graph which shows like 50% of us servers actually run over like 180 BPF programs managed by like various other teams. we manage like 25% of the BPF programs out of these. So here the issue happens right. So uh let's take an example of like the croup attach point from the kernel point of view they just provide us a standard a five for Q. So whoever attaches actually first it would be attached first and later on. So we have like multiple services starting onto like production host in various random order.

So that if they attach like ABC program on like host A on host B it can be BCA. So the order of these programs will change on host to host but like I told earlier most of our programs are actually changing the state of the connection and packets and if other teams are also doing that they will overrite onto yours they will step onto you and things. So things will go very very weird into production if you don't account for all these things. So yeah in order to test all these scenario we needed to like some shadow pro like behavior basically. Okay. So now we will describe and go in a little bit details on like our various stages of our CI/CD pipeline. The first stage it's a standard one the unit testing. We are able to easily validate the behavior because the unit test environment it's it's like well contained and we can use mechanisms like absolute threshold to actually detect failures. It just like plain load BPF program generate the traffic simulate packets and stuff identify all the various combination of tests and verify the code path is executing or not.

Although these tests are very simple but they offer one of the most accurate signals we have in our release pipeline. But again the main drawback of these test is a difficulty of like defining appropriate alert thresholds which are not binary given the diversity of hardware and the service type. There is always a balance between like alert noisiness and accurate thresholds to detect actual problems. [clears throat] The next stage of our pipeline is integration testing. What we try to achieve in this phase is like do a coexistence testing between multiple features owned by our binary. So what we try to do is like we try to like reserve uh dedicated servers with a little bit of heterogenity based on like the either the hardware the kernel type the location because we have like various services which require inter region connections or like intra region based on the scope of the connections and here we try to like test like multiple features running concurrently onto a host we generate like synthetic traffic between these host using IPF and the network and stuff given that these servers are dedicated solely for the test.

The challenge lies in selecting a small enough pool of servers to provide high enough diversity to explore all the feature-to-feature interactions. So in order to account for these we took a little bit of measures with our next stage in our pipeline which is called like production simulations. Uh so the idea behind this stage is to like test production like setup either from a host config perspective like stack jobs production like traffic which can be like shadow workloads and stuff. So for example on a stacked machine mean like stack machine is like when you have like multiple containers running on a host. So we try to like analyze whether our our BPF programs are working as expected or not. when a service actually comes it will we verify if the BPF map entries are populated correctly or not and when the services go out those map entries are purged right or not because this has a dependency on like other teams binaries as we collect various host attributes like the kernel version and all those things the number of jobs running onto a host from them we are able to like test compatibility between like the other other team's binaries with are recent changes and stuff and in this stage we try to extract early signals of a test by using other people's binaries as well.

So again the main challenge over here is again it's a relative approximation of production and the shadow workloads are mostly controlled by the services which may not be up to date all the time. Okay. So it's our last stage of the testing before we actually roll it out into production. Uh it's our mini production release. Basically the thing which we did here was like uh on daily basis thousands of servers with the representative set of hardware types the kernel version and the services which have opted into like a doier. We tend to like roll out a binary and test. So basically assume like we want to like roll out a new feature which is specifically for a specific service. We try to like collect few of the production jobs and put it into like the document tier and on a daily basis we are extracting information of like how is our feature actually doing is the service behaving as expected after a feature or not and in that we also mix a little bit with light. So like I told you like other teams also control a lot of ebpa programs.

So we try to like take host from their RC releases going into production into this tier. Try to like do all the combination of testing to like have a little bit of under understanding about like if our change will actually break anything which can be caught quickly or not. Here we also get a lot of good signals about any regressions into like the resource profile usage like CPU, memory usage, regressions and all those things. In previously discussed testing environment covering all the possible hardware kernel and application traffic characteristics is a challenge. Here we try to address that by performing AB testing on like production devices with like production traffic. One caveat with this approach is that any signal that is random or or if it takes weeks to manifest will of course be missed. For example, we had an issue where uh uh SS threshold corruption was happening into the fleet but it took like four or five days to manifest into the fleet. We cannot actually catch it into a pipeline.

Okay. So, so we talked about like all the challenges and what we did to like solve all those challenges and stuff. Now, let's talk a little bit about like once we rolled our binary into like production, what tools and the systems we had to like develop to understand how our software is actually doing it in production. So, the first one is is like for the short-term trends uh exporting metrics to understand the how is our EBP program actually doing. So bubble up all the eBPF related metrics the features error counters and everything. One thing we learned the hard way was like don't put all your eggs in the same basket. We have to like decouple our metric collector then exporter reason being another interesting use case. So here we as I told you we are controlling a lot of um a lot of connection settings into like the meta fleet. Now we want a data plane or our BPF to always be running while a control plane our binary may crash or being killed or whatever. So we had to like decouple our control plane with our data plane basically.

So once you do that we need an external service to extract all our BPF related counters export it. We use like various inbuilt uh services to like export it to ODS Kuba for like journal monitoring and observability and like building alerts out of this thing. So yeah we basically extract all the data relevant for us to understand issues from like various other sources like packets connections RT maps uh stale entries into the map. Another another important use case was like uh a feature was onboarded onto like this com reboot where we decouple the control plane with the data plane and we identified like there was an issue in which the stale map entries are like remaining in the BPF maps. So and the issue had like a huge blast radius because it was impacting all the virtual machines where we developers actually work. So whenever you want to like migrate in Hashin the traffic was getting black hole. Once we got an idea of this issue we were able to like quickly identify the issue by extracting the BPF H map entries associate the time to live with those entries and were able to like figure out okay this is an issue with the control plane being agnostic of the data plane.

Okay. So we also have an auditor which validates the intent of a policy or a feature rollout. For example, assume like we are rolling out a new feature which is specific to a scope of a connection or like for a particular um service itself. Our auditor validates the intent of the change and alerts if the intent is not honored. So we will have an auditor. We we would task it to like okay if you identify any connection which is not part of this service and you're using this feature raise the alert. Uh the auditor leverages the data from like various external source of truth then telemetry FB flow and other things to reach to that actual decision. So it provides us to do an end to end validation over long period of time. Uh here's an example of like an issue where we got alerted uh based on like the violation of the data points and the criticality of the alert. As soon as we were able to identify the issue, we were see the steep lineup right we were able to mitigate it really really fast in production.

So basically designing solutions and tools for quick mitigation is essential for eBPF infrastructure because like we discussed earlier it can have huge blast radius and unintended problems at scale. So going back to our case study again during the scenario we identify our existing pipeline was not actually doing the things which we wanted it to do. So we had to like uh change it add additional testing based on like the stack jobs and stuff. So we have to account it for that. So TLDtr ebpf pipeline will always be continuously evolving due to like the increasing complexity of our use cases and of course the production will always be high west. New things will come into play for which we need to always be up to date and keep on evolving our pipelines into the future. Okay, cool. So, so yeah. So, the key takeaways for the audience I would like to do would be like eBPF CSD will always needs tools and infrastructure tailored to your own eBPF use cases at it has a larger wider variable set. So different kernel interfaces, attach points, hook points, variability of like the different kernel versions, different expectations from the services for the same feature set and multiple teams managing their own programs which can overwrite or step on your programs and stuff and issues will always cascade up and manifest in like various different ways.

Not only we need to like build infrastructure for confidently rolling out our changes frequently uh but also invest in like developing tools to actually troubleshoot these issues and most importantly help with like the quick recovery because the blast radius is usually usually very high for these changes. Yeah, that's it from my side. I will open the floor for questions. [applause] Any questions? >> Talk about your testing strategy for the current. [snorts] >> Sure. So uh so the question is like regarding about like how do we test a kernel? >> Yeah. So uh it's kind of taken care in in a dog food tier. So basically we try to like uh take the host where the kernel team is also actually rolling out but at a time in our fleet we have like three major kernel versions rolling out at the same time with like various minor kernel versions. So of course we try to like take those into account. Also we have this like a good VM testing framework in which like we have like uh these are part of the unit testing where we have like various containers installed like various VMs of like various kernel types.

So we test it across all those things. But with the unit testing it has its own um caveats. We can only test like few of the things. But with in the dock food we are actually testing it in a production like setup across various kernel versions. Sure. >> Yeah. Sure. Yeah. >> Any other questions? >> Sure. So the question is like regarding like if if I understand it uh right how do we monitor when we roll out our eBPF changes into scale. So like I said mean like it requires uh the tools you have to like build based on your ebpf use case. So right now most of the ebpf stuff in in Kubernetes what I was able to understand is mostly based on like observability and monitoring and stuff. Exactly. Right. So those are like pretty straightforward in my opinion. I might be wrong and stuff but when you're talking about you are actually actively changing the state of the connection mean like we are actually changing the congestion control on the fly. We can actually bring the entire meta in front network actually down with like hunt change.

So we had to like build tools to like uh highlight to make it available to all the users and stuff. So you have to like build like specific tools on like how we are able to accept the right signals based on like RT because we are ch hacking. So it totally depends on the use case of the EBPF. So your tools will change because every attach point every use of ebpf is like totally different. >> Sure. Um yeah um thanks for the talk. So I had a question regarding uh deploy in in a deployment like how do you ensure that um like certain hook points in eBPF uh can only have one program attached right >> no you can yeah understood so yeah >> it's really hard so um it's a it's a very deeper topic mean like we have built our internal tools of like chaining all these EBP programs in a particular order and we have like built monitoring to understand if one program is trying to overwrite the other program behavior and then export it and and stop it basically. So building all these tool we built our own ebpf chainer and stuff which we plan to open source eventually but let's see how it goes.

Um I had a similar question about uh let's say you have different teams developing EBPF programs. How do you coordinate uh for example like one program let's say stops piping buckets to the next one it can break everybody else. >> Exactly. Yeah. >> So do you do you have you you mentioned something internal tooling for this? >> Yeah. So so it was the same issue which we identified during a congestion control rollout. We had a security team had their own ebpa program. They were not doing anything changes into the packets and stuff but the return value they were passing was incorrect. So from the kernel point of view if the reply field is actually one it will override the the results. So for that we had to like enforce a particular ordering. For that we had to like develop a a user space chainer in which like we assign priority based on like okay how crucial is your setting. If two programs are trying to change the one of the setting who should have the priority it would have a lower priority and we also enforced it like if the other program in the chain is trying to change it we will bubble it up through our a monitoring and stuff and we will stop it.

So you should not actually do that but again we should not block it. So we have a a pre-chainer and a postchainer function which understands okay something along the lines actually changed anything so it will overwrite it at the postchaining function. >> Okay thanks >> cool any any other questions? >> Sure. >> Yeah if nobody else has them. So about the observability uh can you elaborate a little bit more on the metrics like what kind of metrics do you bubble up and sure >> how exactly do you capture them? >> Okay so if if you're talking about like from BPF infrastructure all the metrics related to like failure of like attaching a ePF programs and stuff are owned by the control plane. So it will bubble up as part of the control plane. And in and we have like BPF level maps where for every function we we increase a pointer or like increment a value into the BPF map and we have like a data exporter which is acquiring all the all these like counter maps and exposing it so that we understand okay uh if the function is actually behaving the right way or not.

But again when we are talking about like millions of the servers we have to aggregate it and then the aggregator will take care of like stuff. Yeah. >> Cool. Awesome. So I would be over here. It's outside the room. Feel free to like ping talk to me or ping me on my LinkedIn. Happy to answer any of the questions. Good. Okay. Thanks a lot. [applause]