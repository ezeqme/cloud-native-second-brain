My name is Aman Singh and I'm a maintainer of a new and upcoming CNCF project called DRSI. So today we will look at two demos of changed-driven scenarios and we will see how Drai can simplify the architecture for us. Now with everyone excited about AI, I want to open today's discussion with chat applications. So how do we build a chat application today? Well, we simply wrap the LLM model in our application code and that allows us to ask it questions like what is the largest country and since the model has been trained on large amount of internet data, it is able to give us a factual answer. But what happens when we want to ask it questions about our own data? Say we wanted to build a chat application for an online electronics store where the product spiker was maintaining its information in a Postgress database and the review service uh writes to a separate MySQL database. So how would the model respond to a question about the laptops in the store? Now the base LLM here has a problem. It has no idea about the products in our store since this private data was not part of its training.

So out of the box, the best it can do is give us a generic unhelpful response like this, admitting that it cannot access our internal databases. So how do we bridge this gap? A common pattern in use today is retrieval augmented generation or rag where we first join the data from our sources and generate text documents from that and then feed those documents into an embedding generator which uses another AI model to convert the text's meaning into numerical vectors and finally those vectors are loaded into a vector database like quadrant and that serves as a knowledge base for the chat application. Now when the user asks the question, the app would first query the vector store to find the most relevant documents which are then sent along with the user's question to the model and that is what allows the model to give an accurate datadriven answer containing information from both the microservices. But this brings us to the real challenge. What happens the moment the product price or description gets updated in our Postgress database?

Suddenly the answer from the chat application is wrong. And it's the same story when new reviews get added to the MySQL database. Now, our smart assistant is confidently giving customers bad information, which is arguably worse than giving no information at all. What we need here is a real-time rag system, one that automatically updates the vector store when the data in our databases changes. So, how do we make this system real time? Well, one option is polling. We could set up a simple batch job that reads all the data from Postgress and MySQL. Generate text documents using a template. Use the embedding model to generate vectors and then completely overwrite the vector database. Now this is simple and we are guaranteed that the results will be always consistent but the drawbacks are not trivial. Our results will be stale by the length of the polling interval and it is quite expensive to compute embeddings for all of the data most of which probably has not changed. So how can we make this a bit better?

Well, one option could be to pull only for the data changes, right? So we still use the batch job, but this time we track the state of our last run and we find the delta of what's new and what has changed. So what what what about the data that has updated, right? So and then we call the embedding model only for those changes in the data. So we track the state and compute updates and generate embeddings only for the updates. Now this is better as it saves some of the embedding costs. But now we have traded infrastructure cost for code complexity. And we are still stuck with the fundamental trade-off of polling. If we pull too frequently, we are hammering our source databases. And if we pull too slowly, the chat application will be serving stale answers. So what could we do instead? Well, the real-time experts in the room might just tell us to use event streaming where we subscribe to the databases change logs using tools like Debzium, publish those changes to a message broker like Kafka and then we can write a stateful stream processing job using a framework like Flink or Kafka streams that'll join these two streams together and that can give us a new stream of correlated updates.

But we are not done yet. We still need to write more code to process these updates, generate the embeddings and write that to the vector store. This architecture however is powerful. It is near realtime and has a low impact on the source databases. But the operational complexity is massive. We are now responsible for deploying and managing CDC connectors, Kafka cluster, Flink cluster. And we are still not done. We must write and maintain two separate pieces of custom code like the stateful stream processing job here and the whole embedding pipeline. So now we have polling which is simple but slow and we have streaming which is fast but quite complex. So what if I told you there's a better way? What if you could get the real-time benefits of the streaming solution without all that complexity? And what if you could replace that entire pipeline with a single component? Well with DRSI this is how simple the architecture becomes. No more writing custom code or maintaining multiple systems. All we need is a set of config files and DSi handles the rest.

So let's see how that would work. So we start by defining our sources. So these YAML file tells how to connect to our data sources. Now notice here that we don't need to copy our data into DRSI or some centralized data lakeink. DRSI meets your data where it lives today. For example, this is what the YAML would look like for our Postgress database. So it has some basic connection details like the database name, the table name, pretty standard stuff and with that simple YAML file deployed to DRSI, it is now connected to our Postgress database. We need another one for MySQL which is pretty similar. And when we apply that, Dassi can handle the connection and the underlying change detection for both data sources. Now once connected, DRSI allows us to query data coming from all these various multiple disparate sources as though it was all part of a single virtual graph. And that allows us to write continuous graph queries like shown here. Now notice that the query here does not care about streams, windows, joins, watermarks, none of that.

It is simply declaring the final joint result set that we want. For instance, here we are for for instance here we want to match the products to their reviews and we want to calculate some aggregate review statistics. So now normal database queries give us a result set which is a snapshot in time. Right? So when you run a query you get the results based on how the data looked at that particular time instant. RSI queries are a bit different. So they act as if they are running continuously. Basically, these queries maintain a perpetually accurate result set. So, DRSI is always watching your data sources for incremental changes and as those changes happen, DRSI will always keep your query results up to date in near real time. Now, the queries are maintaining this live result set, right? So, the question is what can we do with this live result set? Well, the final piece of config tells Rasi about the actions that you want to take with this live result set. And we can also tell Rasi what to do when the result sets change.

So this is the simple YAML file for our RAG scenario. So here we are telling Dassi to use the embedding model from OpenAI store the embeddings in a local vector store all of that. Now with that reaction in place Dassi can handle the rest. The document generation using templates embedding generation using models updates to the vector store everything end to end. Now let's see that in action. So here I have a demo where I have set up a MySQL database which has the reviews information and I have a Postgress database which has the products information. I also have a quadrant vector store. Let's jump inside the Postgress database where we have the products table containing some electronics items like laptops, phones, tablets, etc. And in the MySQL database we have a reviews table which has reviews for the same products that we saw earlier. Now for the quadrant vector store, we can use the curl command to investigate the contents of the collection. So I've written here a simple script that will fetch the product knowledge collection from the vector store and if found it will report back the total number of points found in the vector store and the most recently updated ones in the collection.

Now all of this is wrapped inside a watch command. So that'll give us the results every 2 seconds. So let's go ahead and use the script. So right now the product knowledge collection does not exist. So let's use drai to build that. So we start by going to the drai website and use a simple command to download the CLI for rasi. And once cli is installed all we need to do is run a simple command called rasi in it. Now this will install drai onto our existing Kubernetes cluster in an isolated namespace. And with that drai is up and running and we can start writing our sources. And here's a simple YAML file for our Postgress database. It has some basic connection details, the table name just like I show earlier. Similarly, we have the second YAML file telling DRSI about the MySQL source and the review table inside that. Now, we can use the DRSI CLI similar to cubectl. Just use the drai apply command to apply the sources to drai. And with that, these sources are now available for us to use. So, we can write start writing our continuous graph query.

So we start by declaring the two sources that we want to use which is the same one that we saw earlier. So the progress is providing us with product entities and remember now Postgress is relational database but grassy query is going to be graph based right. So each row in the products table will be represented as a graph node for the purposes of our query. Now, similarly, MySQL is another relational source that will create graph nodes for every review in the reviews table. And in the join section, we can tell Dassi how to connect data coming in from all these different sources. Right? So here we are telling DSI that we want to create a has review relationship between the products and their respective reviews. And we we can tell Dassi explicitly that we want to connect product node with ID B and the review nodes that have the same value B in their product ID field. And with that graph model in place, we can now write the simple graph query that looks like this. Now we have a match phrase here that captures all the products and their connected reviews together.

And we can then project out some of the fields for the product nodes and compute some of the aggregates over reviews. Here we are computing the average rating and the number of reviews. So let's go ahead and deploy this query using the CLI. So when this query is first deployed, it goes into a bootstrapping phase where the indexes are being built. So it gets online in a couple of seconds and once it's online, we can use the drasty watch command to see the live result set that it maintains. Now what do we do with this live result set? Well, we want to materialize uh its embeddings in the vector store for which I'm going to use the final piece of config, this reaction yaml. Now this has also the text template that will basically have values embedded in it from the drai result set and which will be sent to the embedding model. And here I define that I want to use the Azure OpenAI embedding model called text embedding 3 large. And I'm also providing the API key and the endpoint for my model in Azure as Kubernetes secrets.

Finally, I'm also giving details about the local quadrant vector store that I'll be using. So let's go ahead and deploy this reaction with the CLI. And on the left side of the screen, we see that the correction gets built in real time and nine entries are added to it as the reaction comes online. So that's basically being done done by the drai reaction. Now with that vector store populated, we can use this chat application that I've built here. And this application is connected to the local quadrant vector store. So that allows us to ask questions like what laptop options are there in the store. So here we see that the app first found the three relevant documents from the vector store and that allowed the model to basically reply back with two laptops and their specifications and the aggregate review statistics. Now if we look back in our Postgress database we can see that it has the very same two laptops and in the reviews table we can confirm that the first laptop indeed had two reviews and the second one had only one.

So how about some updates? So let's try some updates. Now on the left side of the screen, we are watching the vector store collection which has nine data points. Now if I connect to the databases and add a new laptop in the postgress database and add a couple of views for that. So we should be able to see the collection get updated immediately and in a couple seconds we see that the new data point got created in the vector store for the new laptop. Now if we switch back to the chat application and ask the same question about laptops this time we see that the third laptop appears in the result set. So right now it has a perfect fivestar rating across two reviews which we just inserted. So let's add a couple of mixed reviews for that. If you head back to the MySQL database and add a couple more reviews a three star one and a fourstar one and try the same question one final time. We again get back those three laptops but this time the review rating for the laptop has been updated. So we saw that with just a few config files has helped us build a synchronized vector store.

And this is a perfect example of a very common and powerful pattern a derived view. Now a derived view here is a materialized view of our data that has been derived from one or more sources and has been transformed for a specific purpose. In this case for fast retrieval by an AI application and this need for derived views is everywhere in modern architectures. Now we often have data scattered across multiple systems and we frequently need to create a unified view of that data to power new scenarios. Now the vector store for rag is just one example but drai can help you with other derived views as well. For example, what if we wanted to build a realtime web-based dashboard that shows product analytics? For that we would need to pull data from multiple services right so we need uh data from product microser inventory microser reviews microser etc and we need to provide that information in real time combined together through maybe a websocket server for our front end now the web-based dashboard in this case could also be viewed as a derived view on our data that is spread across multiple services and DRSI can help us keep that in sync one of the pre-built reactions that we have in Dassi today is a websocket server so With just a couple of few YAML files, we can have a custom websocket endpoint ready to power your front end.

Now, within microservices, another common pattern is called CQRS or command query responsibility segregation. Again, here the read models are also just another form of derived views that are optimized for fast queries. And can help us with that. Now, this brings me to the bigger picture. Maintaining derived views is just one critical pattern within a broader category of software design called changed-driven architectures. Now we define change-driven architectures as those where a system must perform a timely action in response to critical data changes. And that is the core problem that we have set out to solve with DRSI providing a new simpler way of building these kind of systems. A more cloudnative way of building these kind of systems rather. Now another manifestation of a change-driven architecture is when we need to trigger realtime actions in response to changes in state which may be distributed across multiple systems. Right? So when data changes that could be a trigger to start a new action or workflow.

Now think about a building management scenario where you may have sensors deployed in different rooms and if temperature of one or more rooms increases we want to activate the cooling or the HVAC system. Another example could be automatically executing a reorder workflow if your inventory levels drop in your inventory service. What about a scenario that is very relevant for many of us in the cloud native space? Alerting teams when a container image with known issues is found to be running inside your Kubernetes cluster. So let's dive deeper into that one. The scenario is classic. We have a development team and a separate security team. Now the development team is deploying their services and pods to their Kubernetes cluster and meanwhile the security team maintains a list of known risky or non-compliant image tags that they don't want to see on the Kubernetes cluster. Right? So this is being maintained in let's say a completely separate postgress database. Now the change-driven problem here is this.

The moment the security team identifies an image to be risky, we need to find all the pods or services that are running with that particular image in the Kubernetes cluster and trigger an alert for the respective team. Similarly, the moment a new pod or service starts running with an image that matches an entry in that security team list, we again need to trigger an alert for that particular team as well. So without Trasi, you might be forced right back into the world of writing custom polling logic that deals with the Kubernetes API, maybe even a Kubernetes operator, a controller, all of that. Or maybe you might think about writing a custom stream job that correlates these two data sources, use DBZM, Flink, Kafka, and all that. But all of that is just too complicated. And we now know that there's a simpler way for designing change-driven solutions. So how does the architecture for uh this scenario look like with DRSI? Well, it's the same simple architecture that we saw earlier. We have YAML files telling DRSI about the Kubernetes cluster that we want to watch and also the security database and we declare the condition that we want to monitor with a simple graph query and then we can configure the reaction to send alerts wherever we want.

This could be Slack, Prometheus or Pager Duty, whatever you want. Now let's see that in action as well. So we have a Postgress uh database running here which has a list of risky images which is maintained by the security team uh including the image tag, the reason and some mitigation steps. Now I also have a K3D cluster running which will be the Kubernetes cluster owned by the development team. Now we this time instead of the CLI I'll use the drai vs code extension. So the VS code extension shows us the sources, queries and reactions that are already deployed and it also detects the YAML files that are in my workspace already. So let's start by looking at the sources first. So here just like we we've seen before the Postgress YAML file telling DSi about the Postgress database and some basic connection details. The second one is a bit more interesting which is telling DSi about the Kubernetes cluster. So here what I've done is I've captured the cube context of the K3D cluster and stored it inside a Kubernetes secret.

So that's what I'm using here. Now with that drai can watch our K3D cluster. So let's deploy these two sources directly from within the editor. So I can do that because I'm using the Rasi VS code extension. Now we see on the left side the sources are coming up and once those sources are deployed we can start writing our query ammo. Now we start with this by declaring our sources. The first one being Kubernetes which is creating pod for every clust a pod we which is creating a graph node for every pod in the cluster and we also extract out the containers from those pods creating a node per container each of which has an image tag and then we also have the postgress source telling about the graph creating the graph nodes for the images that are deemed to be risky and in the join section we tell drai how to connect these two. So we have a has image relationship that will connect the container nodes with the risky image nodes only when their image tag value matches. Now with that in place we can write our graph query like this.

So we have a match phrase here that selects all the pods and their respective containers only when the has image relationship exists. So this remember this relationship will only exist when the image tag of the container and the risky image uh that is being fetched from the database matches. Now if we deploy this query and watch the live result set let's see what happens. So we don't see any pods right now. So that's because I've not deployed anything on the K3D cluster. So I'm going to deploy two pods here. The my app one which is using an image called my app version 0.1. And if you see in our Postgress database, this has a known security risk as identified here. And I'll also be deploying another pod called my app 2 which is which uses the image my app version 0.2 which does not have any risk so far. And as soon as I use the cubectl command, we will see that the draw query instantly detects that uh my app 1 has a security risk. Now what can we do with this kind of live result set right? So as I said we can send alerts to Prometheus alert manager pager duty.

But here I'm going to use an HTTP reaction. Now this is very versatile. It allows Rossi to invoke any arbitrary web hook. For instance, here I'm going to uh invoke a Slack URL, Slack webbook here, which will help us send messages to a Slack workspace. Now, we tell Dasi here that when new results for this query are added, we want to send a message saying risky container detected to the Slack channel with these particular details. And when the query results get removed, we want another message saying risky container resolved. And here is the simple uh Slack channel that I've created. And as we can see, I've enabled the web hook for this particular channel. So as soon as I deploy the reaction, we should be able to see a message for the my app one pod in this channel. And there we go. So we see that the message has been posted in the slack channel with all the relevant details. Now let's say the security team identifies an issue with the image version 0.2 as well. Say they found a critical bug with that image as well and they updated their database like this.

Now they don't need to talk to the development teams or find the pause. They just need to update their database and the drastic query results sets update and we also see the respective message get posted to the slack channel automatically. So that is pretty real time. Now let's say the development teams decides to follow the mitigation steps listed here. So what can they do? they can use the cubectdl set image command like this to update their pod uh to update the image version that they're using to let's say 0.3 and as soon as they do that we see that the drai query detects that in real time and the reaction will post the relevant message to the slack channel. So we have seen two scenarios here where we wanted to react to changes happening in our data and RSI was able to simplify the architecture significantly. We just use simple YAML to configure our sources and reactions and then we declare the business logic in intuitive graph queries. No need to worry about individual changes. How do we track those watermarks windows all that or even writing custom code right?

We just use reusable components called sources and reactions and write our declarative query. And we have pre-built sources today for a wide variety of systems. And we also have custom SDKs that allow you to build your own sources. Now, with these sources, DRSI allows you to power new scenarios that rely on joint data coming in from multiple systems without forcing you into a big data migration project first. And reactions are also ready-made code components that you can use to act in a wide variety of systems. Now these allow you to build scenarios without the headache of maintaining custom code. Now because of wipe coding you can always write custom code but maintaining code is another problem right and we also have SDKs to build your own reactions to and share with the community now. Drai is open source. So I would like to invite you all to join us in the endeavor of simplifying changedriven architectures. And with that I would like to thank you all for joining us. Please check out these links and you can also find us at the project pavilion in the exhibit hall tomorrow.

And please use this QR code to provide us feedback on the session. And yeah, I can take a couple of questions now. [applause] >> Hi, thanks for the talk. It was really really cool. Um I was curious about scalability. Have you um run some load testing on um you know for instance a database that gets um or I guess the the question is more on the um I didn't quite see if there was like a configuration on the frequency on the queries uh that you're you're running >> right so the qu uh so the query it's the name is query and it is written as a query but we don't repeatedly run that Right? So it's uh it's acting as though it's running continuously but effectively what we are doing is we are maintaining all of this data in our indexes intelligently. So what we are doing is think of uh if you're thinking about relational databases for example like postgress right so think of the replication log of postgress or the write ahead log of my SQL so we are just following those logs and whatever delta we get from those databases we just apply those to our indexes updating those uh the result set and that's what is used to trigger the reactions.

>> I see. So yeah, sorry. Uh and and so speaking to that >> um for for a database that gets updated you know thousands of times per second for instance like what would that look like? >> Yeah. So uh we have tested it already at least up to around a thousand changes per second in different databases. So yeah that's the scale that we have tested so far and we are actively developing new techniques to increase the performance further but yeah so there's no like uh tweak in the YAML itself that that you need to control you can go with whatever scale you have and then if you look back at this architecture so we have three components right uh the sources the queries and the reactions right so the sources are the scale units for your uh sources right so you may have let's say a very heavy source that changes changes very frequently right so you have let's say a postgress source that is changing very frequently and maybe the MySQL one is changing less frequently. So those are the scale units for your data sources and those gets deployed as individual components like pods inside your Kubernetes cluster.

So you can control the scale there. >> Oh, I see. Uh so the components inside Dassi the controllers they autoscale or or you can configure scaling manually. >> Yeah. So so the the scale story like the autoscaling story is still not complete. So we are actively working on that. But yeah, in terms of scale units, so the sources are independent scale units and the query container is in another independent scale unit. So let's say you have a query where you need more and more resources because it's detecting very uh important changes for you which are happening very frequently, right? And maybe there's another query which is not so important and maybe data changes once in six months. So that can have less resources. So so we have tried to isolate these three in uh like separate scale units. So you have reactions, sources and queries. So that's what the scale units are today. We don't have like controls in the YAML today, but yeah, that's a good idea. Maybe we should add a couple of controls there. >> For sure.

Yeah. Yeah. Uh, one last question regarding I guess it's more on the rag based uh example that you have, but I can imagine for our company. >> Um, you know, a a lot of concern being on the permission model on on the data. Mhm. >> Um I was I was curious if if uh you you've implemented that in Jassi or you leave it up to you know that separate u rack component >> right so this is a very interesting point right because uh even with agentic rag more and more getting more and more popular right so what if the agent can write as SQL query directly so would you even need the vector database in the first place is one of the questions that people ask right and that's that is kind of tied to the question that you asked right so Now let's say if we gave agents directly access to all of our production data they might leak some sensitive information which we don't want people to have access to. So which is why you may have like separate tables right so where you only have the relevant data and then whatever data that you want to give access to the agent you can provide that particular role to drai to access that particular table.

So you can use like manage identities in Azure or a uh AWS etc. So we can use the IM capabilities of those cloud providers to manage that and you can also use like on-remise databases and their own security model for that. >> Yeah, that makes sense. Thank you. >> Yeah. >> Hello. I just had one question. First of all, great demo and not in just in terms of like technology that you have built but in terms of logistics was super easy to follow. Great job on that. >> Yeah, thank you. one small question like do you store any state between uh when the queries are done and the reaction consumes them >> right so I think the your concern is more about like will we miss any changes right so let's say the query has detected some changes and then we deploy the reaction later will we miss out on some of those changes in between right >> uh my question is more like is the query portion of drazi stateful >> it is yes so basically the the query result sets are maintained in our graph indexes Now the these uh these indexes can be persisted in different kinds of stores and we provide a pluggable compon model model for that.

So you can use either the in-memory model for testing or development but for production scenarios you could use rox db or reddis or garnet. So those kind of stores are available for use. >> Got it. Yeah makes sense. Makes sense. And is there any limitations on like what sources like do sources need to support some kind of change driven on their end as well? Like is there any limitation on that? So uh no limitation per se on the drai side. Um what I would say is that if your source already has a change feed so that is a very natural fit for us right so for example let's say Azure Cosmos DB or multiple different databases have change feed ready for for us to use right so that that is a very natural fit but we we can also use other systems like for example we saw in the second demo we were we were watching a Kubernetes cluster now Kubernetes cluster doesn't necessarily have a change but it has a watch API that we can use and it also has those resource version timestamps that help us track what is the change happening between uh different states, different versions and the resources that are deployed on top of the cluster, right?

So if that kind of information is available through some kind of APIs, we can of course use that to build the RSI source. >> Got it. Makes sense. Thanks a lot. Super cool stuff.