and welcome to our talk the good, the bad, and the ugly. Hacking three cloudnative AI services with just one vulnerability. Last year, our team at WH found a critical container escape vulnerability in a little known but very popular software component found in basically every AI cloud provider. And with just one vulnerability affecting basically the entire cloud ecosystem, we set out to find to find out how does each vendor handles a brand new zero day vulnerability. So let's dive in. So just a quick who are we before we begin. My name is Narfield and I'm the head of vulnerability research at Whiz and here with me on stage is Eli Ben Sason, one of our senior security researchers. We are both security researchers based in Israel and we specialize in cloud security research. But actually in the last two years we actually shifted our focus from cloud security research to AI cloud security research. And what does that even mean? It means looking and finding vulnerabilities in the entire AI pipeline from AI data sharing vulnerabilities where we uncovered 38 terabytes of internal Microsoft training data or finding an exposed deepseek database exposing user prompts and predictions to finding service stakehold vulnerabilities in the AI in the biggest AI as a service providers such [clears throat] as highace replicate and SAS SAP and and after finding all of these vulnerabilities, we decided to shift our focus to look at the AI infrastructure itself which be the focus of today's talk.

We found vulnerabilities in radius invon and inv container toolkit. So before we can start talking about AI infrastructure vulnerabilities, we need to understand what's AI infrastructure. basically answering the question of how do I run AI? Okay, we're a company and you want to start training your own AI model and serve it. So the first thing you know you are going to need to do is gather all of your data and put it in some some sort of a database. Then once you have the data you you'll use some kind of a training framework to turn this data into a model. And once you have a your model ready, you can use some kind of of an inference server to serve and infer your model. Okay. So after researching and finding critical vulnerabilities in most of these products, we thought what if there's a level deeper something beneath all of this that is in common with everything [clears throat] related to AI. And the answer was quite simple. Nvidia GPUs. Nvidia GPUs are basically everywhere when it comes to AI. They are the one common factor between all AI providers.

So with that in mind, we thought what are the interfaces that these Nvidia GPUs expose to developers? And because we're security researchers, what we actually asked ourself is what's the potential attack surface. So our first stop was the Nvidia GitHub page where you can see that we have a lot of source code to go through before finding vulnerabilities in Nvidia. But we decided to focus in NVIA container toolkit. NVIA container toolkit is a container runtime developed and maintained by Nvidia that allows Linux containers to interact with Nvidia GPUs. This means that if you have a Linux container that needs to access a GPU, you are probably using Nvidia container toolkit. And because it's CubeCon, I don't need to tell you that containers are basically the def facto way to ship and run software these days, making Nvidia container toolkit highly popular in cloud environments. So with that in mind, we created ourel a new LV container toolkit container and compared how does this kind of a container differs from a regular Linux container.

And one thing that immediately popped up to our mind is that the mount list in an container toolkit instance is very very long much longer than a regular mount list. So for those of you who are not familiar with what amount is, it's basically a way to make host file system files accessible inside the container. This means that when you create a container can actually map the files from the host into the container and when the container is going to be initialized, this mount operation is going to happen making this file accessible. But it's also very important to keep in mind that this operation needs to happen in some sort of a safe environment. >> One sec some sort. >> Can you hear me? >> Great. [clears throat] So this operation needs to happen in some sort of a safe environment because at this stage the container file system isolation is not yet fully initialized. But in in container toolkit they have this regular mount operation but they also have mount operations happening from the container to itself.

Basically in an attacker controlled environment where the attacker controls both ends of the string both of the source and the destination of the mount operation. This means that an attacker could potentially craft a container image that looks something like this. We have our source directory but it's not any directory. This [clears throat] time this directory actually points to the root file system of the host and when the container initialization process will begin a new directory will be created and this new directory will point out to the root file system of the host. Now this is kind of a trivial attack and Nvidia actually thought about this exact attack vector and put some mitigation in place. But after some file system funue creating some directories, some link sim links, we're able to craft a container image that allows us to create a special directory that after the container will be initialized, a new directory will be created granting us full file system access to the host. So just to recap, we have a critical container escape vulnerability that grants us full file system access to the host from the container.

Basically, all we need is to control the container image and we win. Now let's see it in action. Let's create ourself a new invia container toolkit container with our own malicious image. Now, just to show you that we're indeed inside the container, we'll show that the etc shadow file is the default etc shadow file and doesn't have any interesting entries and that the process list is basically empty. But now let's let's uh head over to our newly created directory that has the full file system of the host and once again access the etc shadow file. Now you could see that the etc shadow file has much more entries including the ones Eli, Andress and Neil. But now let's take this file system access to the next level and achieve code execution on the host because we have full file system access to the host. We also have access to the Docker Unix socket. So let's take the Docker Unix socket and create ourselves a new privilege container. a container that basically shares all of the namespaces of the host.

And now when we run the PSOX command, you could see that we basically achieve full host compromise. We have code execution and full file system access of the host. So we have one vulnerability affecting the entire cloud ecosystem, which made us wonder, how does each vendor handle a brand new zero day vulnerability? So [snorts] we actually tested this exact exploit across the entire cloud ecosystem. But today we're going to share three different case studies. We named them the good, the bad, and the ugly. And you can decide which is which. Let's begin with Azure container instances. So Azure container instances is one of Azure's flagship services offering customers an easy way to create containers in the cloud. Now, this this is a fully managed service, but it has a bunch of configuration and settings. One of them is the ability to create GPU containers. So, naturally, we uploaded our new malicious container image and ran it. Our exploit worked. We had full file system access to the disk. Let's scan it for interesting files.

Our first Our first stop [laughter] was the PFS file system. That way we could enumerate the process list of the host and see if we share the same host across a different number of customers. But unfortunately, no. It was very very clear that the host is dedicated only for our tenant. But it's still it's an infrastructure host. Maybe it has some some artifacts, maybe container images with secrets and and source code. But once again, we didn't find anything interesting, but it's a Kubernetes node. It has Kubernetes credentials like take these credentials and access the Kubernetes API server. But once again, we were out of luck and we had no access to to the Kubernetes API server due to network restrictions. But we still had code execution in place. So we could just escape the host and bypass all network restrictions. We created ourselves a new privilege container that shares the network name space of the host basically bypassing all network restrictions. Now we can use the cubectl command and get all of the nodes in the cluster.

But we see that we are basically the only node in the cluster. This was very very surprising because we already know from a previous research by the done by this guy from our team on the left that the Azure container instances behind the scenes look something like this pod running on a dedicated node. But alongside this node there are other customer nodes running on the same cluster. But still using the cubectl command we can only see our node. This must mean that our service account is very weak and can't access any resource that is besides our tenant. So we need to get ourselves a stronger account. How do we do that? So let me show you the the playbook of getting a cloud strong cloud credentials. Basically you sniff traffic on your node. You click all of the buttons in your CSP's portal, something usually happens and then you profit. So, let's do exactly that. Did you ever wonder what happens behind the scenes when you press the logs button in your CSP's portal? So apparently if you do that while sniffing the traffic you'll discover that behind the scenes the CSP's infrastructure sends an HTTP request directly to your node and this HTTP request in this case also came with a Kubernetes token and when decoded we figured out that this Kubernetes token is actually from the Kubernetes cluster default namespace the same namespace shared across all customers.

So once again, let's take this token and run the cubectl get nodes command and we get the same response. What's happening here? [snorts] So apparently when we access the Kubernetes API directly, we received an eder that we didn't didn't expect. It turns out that the server header pointed out that this Kubernetes API is written in net. Now, I don't know much about Kubernetes, but I know for a fact that it's not written in .NET. [laughter] So, what's happening here? Apparently, the uh the [clears throat] the not the cluster architecture changed a bit since Yuva's previous research and today it actually looks something like this. We have our pod running on a dedicated node and there are still other nodes running on the same cluster. But in the same cluster, we have a new component, a virtual API server, which means that using this virtual API server, Azure make it makes it seems like we are running on a dedicated cluster while there are other nodes. The way it actually works is something like this.

when we execute the get parts command our instead of uh our request reaching the Kubernetes API server directly it first comes through the virtual API server and if it's an allowed request it it will actually rely our exact request directly to the real Kubernetes API server in the cluster but if it's a disallowed request or it's unknown by the by the virtual API server it will basically drop a request before it even reaches hopefully for the less time [laughter] before it actually reach. Okay, so basically we when we run the get parts command and it reaches the real Kubernetes API server. But if we run a command that is not allowed or unknown by the virtual API server, it never reaches the real Kubernetes API server. And moreover, when it comes to responses, when we send a request and it did relate to the real Kubernetes API server, the response is actually filtered by [clears throat] the virtual API server only showing resources related to our tenant. So this is actually a pretty strong um security measure against crosseten attacks.

when we try to bypass this virtual API server by looking for the real Kubernetes API server in the cluster, looking up the real Kubernetes API server all over the internet using Showdown and certificate transparency feeds and actually trying to find vulnerabilities in cubectl in order to redirect the traffic from our node to other customers node. None of it worked. Which means that despite escaping the container, bypassing network isolation, escalating our privileges, and all of the rest that we did, we still weren't able to bypass the virtual API server, which means that even when we equip were equipped with a zero day vulnerability, Azure defensive def approach stopped us at a virtual API server. So huge kudos for them. And with that, I want to invite my very good friend and colleague, Eli Benson, to walk you through the next two vulnerabilities. Thank you, Neil. Let's see how long my mic survives. Okay, so let's talk about our second case study for today, which is a platform called Replicate.

Uh for those who aren't familiar, very uh popular AI uh AI model gallery service. People can upload their own AI models, their own applications uh and basically have people interact with them, write their own replicate uh being used by uh many big name AI providers. There are some the screenshot probably open AAI Salesforce by dance many others. Uh so once again we wanted to take our NVIA exploit and upload it to replicate and see what's going on inside there. But in order to upload something to replicate replicate will only accept AI models in a format called cog which begs the question what the hell is a cog? So we went to the GitHub page for the cog format and we saw that immediately cog was described as containers for machine learning. So does that mean that a cog is just a fancy name for a Docker container? Yes. [laughter] Essentially that's exactly what it means. So we wanted to take our malicious container, a malicious Nvidia container and make it so it will be a cog compliant container. The way we did that is by running the cog init command, creating a new empty container.

Then we wrote a weaponized cog script which basically inserted our exploit into the cog uh the cog model and then we ran cog push in order to push that model to replicate and the way we wrote our malicious AI model was actually quite cool. We wrote it. So when you go on replicate to our model and you see a little text box that basically allows you to prompt uh ask a question to the model and instead of the prompt you write for example the ID command our model will interpret that prompt as a command to run. it will run the ID command and return the output. In this case, showing you that we're running with root privileges. So now that we have our little AI model/Shell and we also have the NVIDIA container toolkit exploit running, then that means that we now have access to the host file system. Amazing. So the question is what's next? Once again, like the previous time, we want to try to scan the file system and find what interesting files might be uh hidden inside. Uh and the first stop in here was once again uh the procify/prock the list of all processes running on the host.

We want to see what sort of technologies are running there. Uh what sort of interesting information might be hidden in those processes. So we took a look at the process list. It was very long, lots and lots of processes. But process caught our eyes immediately specifically because of this flag a connection string to where this database. And the reason that this caught our eye was actually for two reasons. Reason number one, hello. >> [laughter] >> There's a big plain text password right there in the command line. So of course this is the first thing we noticed. But there was actually another reason as well. The reason was that interestingly enough we're actually already familiar with this exact radius database in replicates network because of previous research done by this guy from our team also on the left. We actually were already able to take control over this database in a previous research that was unrelated to this vulnerability. So we already know what's the role of this reddis database in the cluster in the network.

This reddis was actually being used as a task queue. Every time that a user goes to their favorite model on replicate their favorite AI model and asks it you know is the sky blue prompts a question inside that prompt will go to the reddest task and the reddis will distribute it to the relevant AI model. The AI model will return the response back to Reddus and then reddus will return it back to the user. Now the reason that I'm telling you this, the reason that this is interesting is because if you take a look at the right side of this diagram, you'll realize that this radius database is connected not only to our AI model but also to all the other customers AI models as well. It's a shared radius database. So let's try to imagine what we think uh that the service architecture looks like uh from our point of view. From what we understand, there's our pod that runs our exploit inside of it. That pod runs on some sort of Kubernetes node. And in addition to our our pod, this node also hosts several other customer pods.

In addition to this node, there are several other nodes also running customer workloads all running at the same Kubernetes cluster. And in addition to all of those, in the same Kubernetes cluster, there is the Reddis task Q. And the special thing about it is once again everyone is connected to the same queue. So in our previous research we were uh as I said able to take over this radius uh database and we found out what the impact of it was. What can we do while taking control over this radius database? This radius database gave us access to all the prompts and the predictions going on uh the replicate platform and not only the ability to read those private prompts and predictions but also the ability to interfere with them in real time. Meaning that a user goes on replicate asks some question. Not only could I have seen that question, I could have also returned my own malicious answer instead. And that was not only true for the public models on replicated, but also for all the private models as well.

So [laughter] we already knew how the reg story ends. We just did it and we now proved that we can do it again because we have a working password. So we wanted to see if there's maybe a different direction that we can take, another uh another different way in order for us to take over the environment and prove that we have access to sensitive data. So we started to take a look at the Kubernetes angle trying to see if we can somehow laterally move using uh the uh new privilege account uh that we found of the node. And when we started doing that, we start suddenly encountered a major plot twist in the form of this error message that appeared in a replicate account followed by a very nice email uh from the someone from the replicate security team reaching out and basically saying, "Hey, we noticed uh there was some alerts triggered on your environment and they wanted to figure out if this is some malicious activity or legitimate security research." So at this point, of course, we were able to prove our impact.

So uh we summed everything we stopped the research sum summed everything up and sent the report to the replicate security team. So kudos to them for having some very good detections on their environment. Quick recap of this entire attack from start to finish. We started out as the quote unquote attacker deploying our own malicious cog container to the to our own replicate pod. Using the Nvidia exploit were able to escape to the underlying replicate node. And at that replicate node, we're able to disclose a password to the centralized radius database. That radius database gives instance to all the prompts and the prediction on the replicate platform and including the ability to interfere with them in real time. All right. Now for the final case study of the day, uh a platform that needs no introduction, Digital Ocean. One of Digital Ocean's many many services is a service called Paperspace. Uh this is once again an AI based service, which makes sense. This is what people use GPUs for. Uh, and once again, it allows people to update their own AI models and run them using Digital Ocean's infrastructure.

So, you already know what's coming next. You want to take our Nvidia container exploit and deploy it to Digital Ocean. This is exactly what we did. And we landed in an environment that we think looks something like this. We have our own pod that runs our Nvidia exploit. That pod runs on a Kubernetes node. This is once again a Kubernetes environment. Now we don't know this for sure yet but we suspect that in addition to our pod there are several other customer pods running on the same node meaning this is a shared node and also we think that in addition to our node there are several other nodes hosting customer pods which are all being ran in a shared Kubernetes cluster. So we landed in this environment once again with the Nvidia container toolkit exploit we now have access to the nodes file system. This is Kubernetes so we already know it's a node. Uh and once again we want to try to scan it for and look for interesting files. Now because this was so apparent that we're looking at a Kubernetes environment that is potentially shared.

The first thing we wanted to uh take a look at this time was do we have access to any interesting Kubernetes credentials? And the answer was yes we do. Using our exploit path that we showed you earlier, we were able to go to etc Kubernetes SSL and this folder contain basically the nodes access tokens uh that we can just read and use. Uh now the reason that this is interesting in particular is because our original pod or customer pod doesn't have access to anything interesting. We have a service account that is not interesting can't access anything which makes sense. It's a customer pod. But this node is not meant to be accessed by a customer. So we wanted to see does it have any interesting privileges that we don't. We the pod don't. So this is exactly what we did. We took those token and we started uh to query the environment to try to see what environment we're looking at. The first thing we wanted to know is to run the cubectl get nodes command. basically uh to see how many nodes do we have in this environment to try to gauge uh if this is a shared environment or not.

The result was over 700 different nodes. So we already knew immediately that this was not a dedicated environment for ourselves. We don't require that much compute to run our uh our Nvidia container. So now that we know that this is some sort of shared environment, we wanted to see what sort of access do we have to those other customer workloads. Uh we tested this out uh on our own secondary accounts because we didn't want to access any real customers data. So we just created multiple accounts. Uh and on our account we were basically able to access pretty much everything inside that pod. We were able to read files from that pod. We were able to read environment variables from that pod. Basically a really privileged access to all those other customer pods. But the last line here was actually the thing that really interested us the most. Image pool secrets. Because when we created our account on digital ocean, we were prompted to enter our cloud credentials. In our case, it was AWS credentials in order to pull private images from our private container registries.

So we wanted to know do we have access to those customer secrets and in our case our AWS ECR secrets. The answer was once again yes. In the screenshot you can see or it's censored but you can imagine my real life uh AWS credentials in plain text which gives access to my private AWSCR repository. Now in our case this enabled read this is enabled to read all private images but in many cases this includes write access as well. So that means that if an attacker uh would have conducted this attack, not only could this attacker have, you know, attacked Digital Ocean's infrastructure, but they could spread from Digital Ocean servers to digital ocean customers environments, really significantly increasing uh the bladius of this attack. So once again, quick recap uh from start to finish. We started out as the attackers deploying our own malicious container into our own digital ocean pod. Using the Nvidia exploit, we're able to escape to the underlying digital ocean node. Uh in that node, [clears throat] we were able to gain access to a strong set of Kubernetes credentials and those Kubernetes credentials enable to us to access basically everything in the paper space service to read models to read private source code, private files and also to read customers cloud secrets basically exposing entire cloud environments uh to the internet instantly.

So these were our three real life case studies. Now I want to sum things up and talk about uh some of the takeaways that you can take from this research in order to secure uh environments when you actually build them. So uh first off in terms of responsible disclosure uh needs to be addressed. Of course we reported all these issues to the the relevant vendors as soon as we found them. This was the CVE number uh that Nvidia assigned to the underlying issue and they also fixed it on this version. Now, of course, we collaborated not only with the security team of Nvidia, but also uh the security teams of Replicate and Digital Ocean in order to help fix these issues as much as we can, and we're happy to uh make these libraries and services a little safer as a result of our work. So, kudos to those uh security team for working with us. Now, uh to sum things up, what we saw here today was a single vulnerability that basically affected the entire cloud ecosystem. The entire cloud cloud native SAS ecosystem was affected by one single vulnerability.

And perhaps most interestingly, every service that we looked at today responded to it wildly differently to the exact same vulnerability. In the Azure case resulted in virtually no cross attacks, a very low severity at the end of the day. In the replicate case, we were able to laterally move and access sensitive data, but that movement was actually flagged by the blue team. And in the digital ocean case, this resulted in a full service takeover. Now, the difference between all those didn't rely on the vulnerability itself. It was once again exactly the same. Ultimately, it all boiled down to what it really always boils down to the guardrails and the best practices that each service deployed. So, which brings us to our takeaways. First off, we believe that containers containerization should never act as the one single security barrier when separating between different tenants or different environments that should be separated because they can be broken in a myriad of different ways from misconfigurations that we all often see in our research to corner vulnerabilities that are becoming more and more common all the way to simple logical vulnerabilities like the one we uh talked about today.

uh and in particular when separating between different environments that should be separated like different tenants or between data plane or control plane etc etc you should always use multiple types of guardrails diversify the types of guardrails that you use that can be be based on compute on virtualization authentication authorization or many other different types of barriers uh in the Azure case for example we had uh the container security barrier but once we bypassed that one we were able to we were stopped at the barrier of the virtual API server which proved to be very effective so essentially We believe that you should always assume the first line of defense will eventually be bypassed by someone and you want to make sure that it's not the only barrier that separates between potential attackers and sensitive data. Uh and finally uh I think the uh most interesting takeaway that can be taken from this is that a strong security posture a proper security posture could literally be the thing that stops zero day attacks that stops attackers even when they're equipped with equipped with zero zero days.

uh principles like virtualization based isolation, like strong network rules, credential hygiene uh can literally be the make or break when faced with an unknown zero day threat uh and uh in order to find out more information about this, we actually have a very neat open source resource that we wrote about this. It's called Peach uh basically uh types of uh guidelines that we learned in our research in order to how to build uh strong strongly separated multi-tenant environments and of course this is open source so contributions are always appreciated. Now before we leave, I want to leave you with one final thought uh a teaser if you will uh to the next episode of Whiz Research because uh this vulnerability that we showed you today was actually found many months ago. Of course, we went through all the process of responsible disclosure and making sure all these services were protected before we could bring it to you today. But actually very recently, like a month or two ago, we were able to find another vulnerability in the same environment tracked with the CVA number.

And interestingly enough, this new vulnerability is both both much simpler and much stronger than the previous vulnerability. This is how the exploit looks like. Three [snorts] lines of Docker file, super super simple. You provide your own uh your own arbitrary code. That code immediately runs outside of the container. This is of course also patched. But uh what we're trying to say here is that this is not like a once in a lifetime vulnerability and it's also not a twice in a lifetime vulnerability either. It's an attack vector. basically uh attacking cloud providers and SAS providers using one of the most fundamental building blocks which is the container runtime. So this is uh the two vulnerabilities we were able to find. But the interesting question is who's going to be the next person to find a new vulnerability that really breaks out of every single cloud service? And if you think this [clears throat] is you, then we have a very exciting announcement to make. uh we uh our team uh in Whiz research in collaboration with the big three cloud providers AWS and GCP and Azure are launching uh an open source hacking competition called zeroday.cloud.

Uh in this competition we're basically trying to put a spotlight on the open source projects that deser that really power the entire cloud projects like Nvidia container toolkit we talked about today but also many many other projects that many of them are here on the floor in cube. Many of them are CSF uh sponsored projects or graduated projects like uh containerd uh kubernetes obviously like fluent bit uh graphana prometheus basically all the open source projects that really power the cloud uh ecosystem but are often but often don't have the opportunity for big bug bug bounty programs. So in this pro uh in this uh competition we're offering a price of almost $5 million to people that can find vulnerabilities in these uh and basically help make open source a little safer. So uh if you think that uh this is you and you want to contribute to open source then go to zeroday.cloud and we'll be in touch. [sighs] Thank you very much and if you have any questions feel free. Thank you.