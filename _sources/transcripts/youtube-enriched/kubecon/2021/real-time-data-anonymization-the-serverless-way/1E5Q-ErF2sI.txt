Welcome to our CubeCon North America 2021 presentation. It is our pleasure to meet you virtually in this event. In this talk, we will walk you through our serverless architecture to anonymize personal sensitive data to preserve data privacy. Here is the agenda for today. First, let's look at the global legislation landscapes on data protection and privacy preservation. What's their impacts on our industry and how should we address to these challenges? Next, we present to you the solution architecture to our target use case, that is to anonymize personal and sensitive data from images that are in a hosted storage environment. The rest of the talk will deep dive into each of the building blocks. We will show to you how we use adapt and fine-tune these technologies to meet our scalability and security goals. Data protection and privacy is an increasingly important issue for global data controllers. Care must be taken to process, exchange, or store sensitive personal identifiable information and honor privacy provinces.

So, here we have a diagram from the United Nations uh dated year 2020. About 66% of the countries in the world have data protection laws. For reference, just like half of year before that, the number is about 50%. So, we anticipate more and more countries in the world will have data protection laws in the future. The IT industry addressed to such requirements by using two techniques, pseudonymization and anonymization. The difference is whether the transformed data can be traced back to their original forms through another process. For example, data encryption and decryption is a form of pseudonymization. Once encrypted, the data can be used in computation, um transmission, and persistence. In fact, data protection laws such as GDPR recommends uh pseudonymization because it protects data privacy. But, data encryption and decryption often incurs computational and operational or sometimes even financial overhead. So, this is not a solution for all use cases. In fact, in many use cases, especially machine learning, the privacy of the private personal privacy privacy information are not used at all.

So, they can be completely removed from the original data form. And this is the process called data anonymization. According to EU uh GDPR recital 26, data anonymization does not fall into the scope of the law. And the rest of the presentation, our use case is to anonymize uh data that is in hosted uh storage environment. Here is the high-level overview of our of our solution architecture. As you can see here, we use mostly CNCF projects, namely Rook and KEDA. Rook is the infrastructure orchestrator. We use Rook to create Ceph cluster and create a Ceph S3 bucket and also enables bucket notification mechanism. So, activities within the buckets are pushed to the message queue endpoints. We use KEDA for serverless as a service framework for our execution engine. So, KEDA or Kubernetes event-driven uh architecture is a lightweight and uh service provider oriented. It's very suitable for our use case here. So, the KEDA serverless functions use the machine learning algorithms to detect objects of mostly personal and sensitive information in within the image.

As there are so many um mechanisms around, we use a hard feature classifier from OpenCV and deep neural network from TensorFlow with less loss of generality. So, the whole workflow is like this. When the images are uploaded to the Ceph S3 bucket, the activities are pushed into the message queues. The serverless that are subscribed to the queues read the events and downloads the images from the buckets using um object detection algorithms to find region of interest and anonymize the personal and sensitive information. The anonymized image at the end replace original images in the bucket. The whole workflow is running on the MicroShift cluster. MicroShift is a lightweight implementation of OpenShift and Kubernetes. It's very fast to start, usually in seconds versus minutes in regular Kubernetes and OpenShift clusters. This consumes very little resource. Uh it can run on the small single node cluster with only two CPU cores and two gig of memory. We will spend more time on these components in a deep dive sessions.

So, now that we've seen the use case for bucket notifications, let's look deeper into the details. So, bucket notifications are built from several building blocks. The first one is a topic. Topic tells us where to send the notification to. A topic can aggregate different notifications from different sources to specific endpoint. The endpoint could be a Kafka message bus or more precisely a topic inside a Kafka message bus, a topic inside a RabbitMQ server, an HTTP server, and we're going to add more like this. We're going to add AWS SNS, AWS Lambda, AMQP 1.0, and using some scripting and and customization, you can add even um a NATS message broker as an endpoint for notifications. The second building block um is the notification itself. A notification itself is what ties together the topic with a bucket. It ties them in a way that tells us which notifications we're interested at, which kind of events, creations or deletions or both, and we can also add uh filters that can filter out um different objects from being sent or not sent as uh notifications to the endpoint.

The last building block is the event itself, and this is the information that we're sending to the topic based on the configuration in the notification. So, if you feel like setting up the system with the bucket notifications and the endpoints is complex, you're probably right. But, here we have something that should help us, and this is Rook. Rook is a storage operator for Kubernetes. It's a graduated CNCF project that provides different storage providers for Kubernetes and has both file, uh block, and object storage. We're going to focus on the object storage. Now, what do you get in Rook? You get kind of two in the price of one. First of all, your storage solution is actually hosted inside Kubernetes with all the value of orchestration and management that come with it. The second thing is that it gives you storage for your Kubernetes applications. Now, I'm going to show how in five simple steps using five simple YAMLs, you can actually build the entire system that we described before. The first thing that you have to do, you have to define the Ceph object store.

When you define the Ceph object store YAML, you give it couple of parameters saying what is the size of the replica set and how replication is done, different pools that you have, and what's your object gateway. Once you have that set up, you need to define the storage class. Storage class is the type of storage that you have. You can have multiple storage classes in your system, uh but each uh bucket that you're using has to belong to a specific storage class. Storage class has something like a claim policy and other parameters that distinguish the different the the way the storage should be handled by those buckets belonging to the storage class. Now that we have the object store and the storage class, we can define an object bucket claim. An object bucket claim is what eventually generate those buckets. When you define the object bucket claim, you can give it couple of parameters. Very important one is the storage class that it references. Um you can have give it a name or a name generator. And an extra bit of information that we can give here, and this is related to what we are trying to achieve, you can define its labels that notifications that we want on this bucket.

So, the only extra piece of information that is needed at the bucket level to enable bucket notifications is to reference the notification via label. Labels are very useful here like on the side note, just saying that whenever you want to let's say look for all the buckets that use a specific notification, searches are becoming much much easier. And also this is not very intrusive into the actual YAML of the object bucket claim. So now that we have the object bucket claim, we actually need to define the topics and notifications. Uh you probably know everything here is declarative, so you can define the bucket before the notification or after, it doesn't matter. Everything eventually will fall into place where all the information will be available to our operator. So another step that we take here is define the topic. As we said before, the topic is the endpoint. Here we define the RabbitMQ server as the endpoint, and we're giving a couple more uh details of information. Some of them are related to um the actual topic like the exchange we're going to use or what is the acknowledgement level that we're expecting from from the from them.

The others are more generic like whether we're using destination or we can pass opaque data and so and so forth. So now that we have the topic, the last bit that ties everything together is the actual notification. Notification here just declares the association between the topic that we just defined and what kind of filters and events are interesting for our notifications. The association with the bucket doesn't exist in the animal of the notification. Instead, this is just here in the label below as part of the object bucket claim. You can see here that we have to reference the topic and give some extra information about what kind of objects we're interested at. We can filter on the keys of the objects or we can filter on metadata, we can filter on tags, there are many many options here. And we also say what kind of events we're interested at. Now let's see what it actually means to send notification to the serverless function. The notification, based on the configuration similar to what we described before, is sent outside of the Ceph cluster into in this case the Kafka K-native bus.

Now we have a serverless function that is subscribed to the topic that we publish to Kafka and is getting the notification on the other side. This is an end-to-end solution. It is reliable because Ceph is going to store the notification uh using Redis until it get the ack from Kafka. Kafka is going to store the notification until the notification is being read by the serverless function. And the solution is also scalable because Kafka is scalable, Ceph is scalable, and the serverless function is going to spin more functions as more messages are coming um on the topic that they're interested at. So this is a whole solution which is reliable and scalable. So if we have a solution which is both reliable and scalable, why isn't that enough? Why do we need something else? Well, this works very well if the processing done by the serverless function is simple. Sometimes this is not the case. Those serverless function can have processing that takes a very long time and may fail midway. Uh they can have processing that involves multiple steps that involve multiple functions that invoke one another.

And each one of them can fail in the middle or just go stale or crash or not be responsive anymore. For those complex cases, we need something else. We need the ability to know that something or some piece of work is never going to be processed in the end even if it was fetched from the queue. And we need a way to make sure that this piece of work is being processed later on by some other um consumer that is able to process it. And we want to do that without ideally introducing a whole set of complexity into the system as another message broker or another system that allows that. Given those requirements for those cases, we would like to introduce a native message queue API where the actual message queue is embedded inside Ceph. So let's try and summarize the two different modes in which we can operate. The existing push mode, the messages are sent to an external message broker. The serverless function programming model is that it's based on the queue that stores the messages. Scaling is based on the utilization of the serverless function.

The reliability of the producer, which is Ceph, is the fact that we're storing everything in Redis until it's being acked by the message broker. And the reliability of the consumer is based on whatever reliability the message bus gives them. Now in the new mode, in the pull mode, the notifications are stored inside Redis itself, so not in an external system. Whatever reads the messages is based on an out-scaling trigger which sees what is the size of the queue, approximately that, and uh spins as much consumers as needed based on the size of the queue. The reliability of the producer is just Redis. And the consumer's reliability is is based on the fact that we have a mechanism where if somebody doesn't ack the the end of the processing uh within some given timeout, then we're making those notifications available for another consumer to consume them. We've chosen to model our messaging based on the AWS SQS API. The reason is that it fits very well with our requirements. First of all, it has an at least once guarantee, which means that whenever a consumer reads a notification from the queue, the notification is becoming invisible.

So other consumers can keep on consuming other notifications. This guarantees the scalability of the solution. However, as we said, processing could it could be complex, and therefore we have a visibility timeout. So after this timeout expires, if we didn't get an ack from the consumer telling us that the processing actually ended for this notification, notification is becoming visible again. So any other consumer can pick up on that, and this allows for reliability in case of client failures. We also have a retention period, which means that stale or obsolete notifications are being deleted automatically from the queue, and we're making sure that they're not consumed by any client. Another reason for for selecting the AWS SQS API is because anyone which is already a user of Ceph probably has some kind of a client SDK that support all the AWS API because we are AWS compliant for S3 and other things. So by using this API, we're making sure that the solution is uh simpler to integrate and um doesn't require any special additional SDKs or clients.

We're using Redis for the scalability and the availability of the solution. And we're based on the AWS SQS, but we're not a general purpose message queue. We implemented that only for the sake of notifications. However, in the future we may expose more APIs to make it a more general purpose uh message queue solution. I know that I said you won't don't want to add more complexity to the solution. This is why we want the um internal message queue or the native message queue. But sometimes you would like to use an external one. This could be for the reason that this external solution is already there, or could be for other reason. For example, your serverless function or your scaler in the case of KEDA already want to support a certain type of message queue. You can use Kafka, and this would be for a scalable cloud solution. You can use the RabbitMQ for a more lightweight compact solution. Both of them are coming with uh operators that makes deployment in Kubernetes much much easier. Kafka has Strimzi, and RabbitMQ has the built-in Kubernetes operator.

So currently, there are two CNCF serverless frameworks, K-native and KEDA, Kubernetes event-driven architecture. Both supports scale down to zero through auto scaling. Uh the difference between the two is that the K-native is more network oriented, and KEDA is more service oriented. So here we have the two diagrams from the architecture picture. On the K-native side, we see that the serverless function, defined as a K-native service, is going to manage two different concepts called routes and configuration. The routes will uh split the traffic into different revisions, and revisions will have much different endpoint ingress endpoints implemented in the networking stack. When K-native starts, it's using Istio for its as its default networking stack, but it uh is expand beyond that and supports multiple plugin model. So it's currently you can use Istio, Gloo, Solo, and Corio, and probably even more. The nice thing with the K-native is that the serverless functions is HTTP triggers. You only need to focus on your business logic, how to decode those cloud events, and handle events.

You do not have to worry about how the events is sent to the serverless functions, or the events routing is happens in the background. On the other hand, KEDA is very service provider oriented. It still works the service provider such as Apache Kafka or some other mechanisms. It's where you use metrics as a heuristics for scale up and scale down. It also have this its own implementation of scalar. That's where I just as a bridge between the current detection and the service function invocation. In the KEDA service functions it is more complicated. You have to deal with the events and you have to decode and you have to receive it. In some certain cases you have to remove it once the the event is already handled. But KEDA is very lightweight. It does not require a lot of dependencies. And we will see in the next slide that it is very suitable for our use case as a standalone single purpose service deployment. We chose KEDA for our service framework in this solution because it has the following features.

First of all, the service functions to process the images have to be long-standing. Because as long as they are image uploaded to the RGW buckets, the service function have to stay alive. Otherwise, scaling up and scaling down could cause overhead and it is not quite efficient. Long-running service functions will become a issue for auto scalers. This could mess up with auto scalers calculation and prediction. But this is not an issue for KEDA because KEDA does not require external auto scalers. Second, KEDA is very lightweight. It does not need any dependent networking components. This is very suitable for edge computing or single-purpose applications. Third, uh because KEDA is not actually HTTP triggered, the service functions do not need to have external endpoints. External endpoints are not always available because of the network connectivity issues or security issues. And last, KEDA preemptively auto scale service functions to meet incoming requests. So, this can make the real-time processing a possibility.

Now, let's look at the anonymization service function. The service function first read the message from the message queue and find the location of the such self RGW bucket. It downloads the image from the bucket and detects if that if there are any personal and any sensitive information such as faces and license plates. And blurs the region of interest using rectangular box. And then replacing the original image in the bucket. As you know, there are many object detection algorithms around such as Haar cascade classifier and deep learning neural networks. All these algorithms have similar features. They both detect well-defined features at different scales and they match up to the predefined object. The Haar feature cascade classifier is fast is very fast but can only do one detection at a time. It can only detect one class. If you have multiple class, then you have to have multiple classifiers and this is obviously not quite convenient. The DL models on the other hands can detect multiple classes but they are more complicated and often require high-performance hardwares like GPU and ASIC.

So, without loss of generality, the solution uses the Haar feature cascade classifier from OpenCV to detect faces because faces have well-defined landscape. And also using the pre-trained TensorFlow model for license plate detection because license plates have different forms and not all of the forms are the same. So, deep learning models is the better choice in this case. And the last, let's look at the cluster orchestration system called MicroShift. So, MicroShift is a lightweight implementation of our OpenShift and Kubernetes. It is optimized for edge computing use case that have small factor devices with resource constraints or for environments that only serve single-purpose workloads as this one. It provides a minimal yet customizable OpenShift experience. When it gets put up, it only starts up very minimal OpenShift components but you can add more and customize it with ease. It is a single binary responsible data and the control plane. So, you can deploy it either as a RPM or as a container.

It runs on Linux, Mac OS or even Windows. Currently, it supports both AMD64 and ARM64. But the community have already developed the binaries to deploy on RISC-V and power systems.