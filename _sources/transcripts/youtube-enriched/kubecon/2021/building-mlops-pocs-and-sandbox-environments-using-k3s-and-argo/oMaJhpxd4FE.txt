welcome to the cubicon at cloud9com europe 2021 this talk is about building envelopes pocs and sandbox environment using ktris and nargo i am sergio mendez a little bit about me i am a priority system professor i am clown native enthusiast i really like to play with cloud native technologies i do some devops at jello i am the organizer of clownery guatemala i am a link or the hero let's get start with some cartoon first showing how we depend on technology and these things and how machine learning is going to substitute or works in the future basic concepts you have to see some basic concepts before jump to the technical part poc approvals concepts pocs used to evaluate different kind of technologies solution and context and the learning good goals and that kind of things so we use a poc to compare different technologies in order to solve a problem so we experiment and do things to determine which technology is better to solve that problem a sandbox environment is pretty similar than production but it's just for testing purposes so you can run trusted software without risk there is a control environment and can be used for testing pocs and you can have access to some similar data from production each computing in the other side refers that let's say that you are close to the source of data so you want to process the information there let's say that you are close to the data and you want to process information with your smartphone let's say it could be possible uh but the that doesn't mean that the cloud is going to be disappeared that means that the cloud is coming to you because you are going to process the things with your own devices and niche computing reference to the distribute geographic computing that things some use cases for age computing could be machine learning iot applications data processing games or any workload talking about devops devops refers to the collaboration between the operations and the developers and this kind of workflows where you are going to build tests really software in a really fast way with ci cd pipelines and that kind of things a pipeline that is a really common concept using on devops um here is a concept that shows what is a data pipeline but it's pretty similar that let's say that it's a series of steps that maybe the output of one asset could be the input from another step so it's really awesome to understand that pipeline in that way the console envelops in in the other side refers more to the automation of the model generation talking more about how to design build and manage these processes that you are generating a model getting predictions in order to push your ml power software in some way this diagram refers pretty pretty close to the way there are some diagrams on internet about devops and refers really really nice but in the context of envelopes envelopes contains the data the model and the code let's say that the data change you have to regenerate the model maybe you can do some modifications to your code maybe you you want to regenerate your model too and this this includes the whole cycle of the envelopes in general so there are difference between devops and envelopes let's classify it there are different users for devops you are going to see developers operators let's say sre devops cloud engineers envelopes guys data scientists develops engineers and that kind of things so devops focus more than developers and ml ops more than the science team spartan data so they are pretty pretty different the artifacts that devops generates or they are getting touch are more like github repositories or give repository source files made with multiple programming languages and envelopes is more focused on machine learning models and data sets devops use different technologies common to hear that people mention front-ends front-end frameworks back-end frameworks languages databases like bjs titan java mysql etc in mlaps context the people mentioned more models about data and technologies like python scikit learn tensorflow that are libraries to generate machine learning models jupiter notebooks for the experiments apache spark for data processing and the whole ecosystem of hadoop and why not airflow it has different goals devops looks more for the software deploys some application on the cloud and have some quality assurance for the software fast deployments cicd pipelines but envelopes is trying to automate the model generation the predictions the inference and optimize that kind of workflows they are like let's say two kind of experiments like the static experiments that are ready to be passed to production environment so the code is not going to change anymore it's really stable so maybe you want to schedule this kind of experiments periodically we claim only yearly there are dynamic experiments that are under development by data scientists the code is constantly changing it's unstable and that kind of things there are some benefits for envelopes in the context of the containers so the containers can bring you package the logic of the experiments can freeze the library versions can give you that portability for the experiments and maybe you can share these containers with experiments across the different teams of data scientists and why not to implement that kind of githubs to generate these models and these experiments or how to make these experiments let's remember that mlaps is not only technology it's people this experiment is data you have to organize that that people that experiments and data in order to get valuable predictions and in order to implement successfully envelopes environment or culture there are some technologies for poc some machine learning pipelines that you can use k2s for example is a certified kubernetes distribution that can use erm architecture that is ready for iot and age computing it has support for intel architecture too the amazing thing is that k2s includes the whole components of the regular kubernetes clusters in only one binary it has several different backends like mysql etc includes ingress branstaller helm support network support continuity for the containers and please remember that if you are using a erm architecture or devices you have to recompile your libraries or applications if needed in order to support that architecture and then you will be ready for edge computing because this kind of architecture so common videos on this context of face computing and compile or performs this kind of workflows so the companies right now are migrating to age computing to reduce goals for their workflows argo ground source in this site is a piece of software to create this automation these pipelines and is designing to run on kubernetes there are different features for argo uh you can execute that step inside that container your workflow or your dac [Music] can be defined in a file like a simple directive a cyclic graph pretty simple syntax using demo argo is kubernetes native cloud agnostic um you can use argo workflows for different things instead of machine learning just to implement a regular ci cd with less complexity argo cd in the other side is a complement for our workflows you have to use argo cd just for continuous delivery and deployment and it's designer for cuber names this supports different formats of configuration like hemp chart customize json files demo or why not your custom plugins for your custom configurations don't forget argo events and rollouts it's part of the whole argo ecosystem to define a really really nice ci cd or envelopes workflows you can use k3s as a lightweight environment plus our goal on the edge or maybe for pocs argo could replace apache airflow maybe beans luigi ml flow or other ml solutions like sagemaker or maybe could be a complement for your current solution the benefits is that with this kind of technologies you are going to spend less money for pocs instead of a cluster why not a virtual machine with k3s um using k3s and argo undergo workflows and cd they are prepared for age computing and iot they are cloud agnostic pretty lightweight and high scalable and they are no bender locking because you are using open source the demonstration we are using argo workflows and argo cd installed on k3s onenote cluster let's say it's not a cluster and we are going to execute some simple pipelines and we are going to show the code that we are using for that the architecture of this demo is like their first step for the basic envelopes pipeline is reading a csv file with the scores of students are is going to predict the final score um in the extraction transformational loading step step number one is reading this information cleaning this information deleting columns generating a processed csv file that is reading for the step number two for the training and generate this model with that file and this is going to generate this model and these files are going to be uploaded to a bucket on google cloud plus temporal storage and the step of the deployment is going to read this model and it's going to call argo cd that detects a helm chart that creates a deployment using this model to expose this model as an api with an endpoint with an ingress controller and for the inference we are going to access this endpoint to get these predictions and generate a final csv file with these predictions let's call it the inference so let's move to that part so right now let's let me start our good workflows let's access the ui so right now it's ready yeah so right now we have the argo workflows here's the dashboard the things there are several current workflows that were executed in the past here is argo city you have to configure your repository on github that detects the help chart and you have to create an application and here this is going to show your deployment regular deployment with the reference revisions and argo is going to perform that deployment and let's execute a very simple pipeline from our goals so in this site let's move to the repository let's say argo workflows has a cli that's pretty pretty awesome let's clean the cli so it's going to show you the steps here and in the command line interface so it's really really nice um so you can't visualize this thing if there are workflows right now here let's reload the this part basically it's going to show you let's zoom a little bit this uh this step is for getting two steps that is running parallel it's going to show the log here and basically call a whale container with a custom message in the logs so each step has a wrong log with a custom message here shows the same thing and yeah that's a pretty basic basic pipeline and me show you the the basic envelopes workflow that we already have here okay here is the whole envelopes pipeline that is going to execute the process that we explain in the architecture of the demo so let's call it q q com test one we are going to execute this part so this thing maybe will need more space to show training and these things and maybe it's going to need a little bit of more space here showing the whole process it's executing the etl process the model the training and these things so right now is showing a real time missing the part of the inference right now the other steps are in green crawlers so are in progress right now yeah so it's done let me open here so run real perfectly and we can see how this thing calls let's call it the test number two how this thing goes argo cd so right now is in the ideal process [Applause] [Music] so right now is in the training the next step is to deploy the model so let's move here it's going to change in some moments yeah right now it's changing it's replacing the old bots and that kind of things so right now is refreshing the things yeah so right now it's moving everything looks fine he's deleting pots updating the things and yeah that's it it's ready uh let's say that for example we can execute another another let's call it the test tree i don't know if i executed um yeah it's this part it's going to execute the same part and in our god you are going to see like a kind of history well right now is trying to deploy another let's say right if the data change or the goal or something like that right now it's changing the things and what if there's a mistake or there's a need that returned to that previous version so here's um an option in the history rollback and [Music] you can roll back the previous version right now it's in the part number three the cubans test cubic contest too let's roll back to that to that version right now is doing that rollback and yes he's doing the things right now i i can show you then a little bit the code of the things well we have here the helm chart this is a report that i can share with you people the part of the pipelines is the envelopes simple pipeline here so you define the variables and these things this is optional if you want to use token ferrargo so you parameterize the things you include the different steps the tl the training the serve the inference on each step you have to create that kind of template really similar like tecton and so you call a container and send parameters on values above get for example and you have to parametrize the whole thing and let me show you another part of the code well here in the part of the containers um there's the atl process that is basically python code let's see a little bit the index here it's a pretty basic python code that do the cleaning thing with using pandas and that kind of libraries then let's move to the model training maybe it's like uploading the things downloading the processed file and generating the model and uploading the model here this part all the experiments are based on the scores of students and in the part of the inference is calling the endpoint that this parameter is here in the function of prediction so in the end it's going to generate this inference file that we can see here on our repo let's see here in our storage or cubicle you can 21 project on google account so here's the inference file yes the initial file the model and the processor and the health chart is in this part [Music] this helm folder is going to be read by argo in order to deploy the thing and that's it that's the general argo workflows argo cd and k3s some of the resources that i use for this demo is the official websites k3s and argo workflows and cd the slides if you want to see my slides um the repository at github you can replicate this whole experiment and understand step by step my personal email and i'm in the social networks uh sergioram gpl and thank you very much for the opportunity