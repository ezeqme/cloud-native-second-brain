hi i'm kohei tuk-naga from ntt corporation i'm a reviewer of contenary and a maintenance of build kit i'm joined today by tao peng and group today we will introduce and deep dive into faster container image distribution technique discussed in the community which is called lazy blink first i'm gonna give you a brief introduction to lazy blink playing continent image is known as one of the time consuming steps in the container lifecycle this has been affected various kinds of use cases including batch job execution and building images their efforts to minimize the size of container images but not all images are minimizable research shows that playing packages accounts for 76 percent of container start time but only 6.4 percent of that data is read the root cause of this issue is the current ocl images in these images each root fs layer is formatted as star and optionally plus compression tar is not seekable and cannot perform parallel extraction so when we start the container we first need to download the entire image content to the node then extract each layer sequentially if the image is large starting up the container will take long for solving this issue several osseo alternative image formats are discussed in the community they aim to speed up container cold start so they are sometimes called as accelerated images accelerated images enables a sort of technique of container image distribution called lazy blink this allows container runtimes to start up continuous before the entire image contents being locally available instead necessary chunks of content like files are downloaded from the registry on demand in this talk we put focus on two of image formats and deep dive into how lazy playing is achieved instagram is also a compatible image format for lazy blink this comes with prefetch optimization and content verification this is proposed as a backward compatible extension to ocr image specification nidus is a lazy buildable image format with perfect chunk deduplication and e2e dental integrity this is compatible with osi distribution spec and oci artifact spec this is incompatible to all set image specification so this is proposed as ocr image spec v2 format first let's take a look deeper look at each third digit image format easter egg is an image format for legibling this is backward compatible to the current ocr image specification so this is lazy poolable from oci compliant registries as shown in the following figure and even legacy and lazy playing agnostic runtimes can run this image as a non-laser image e3 is now usable on a variety of tools including kubernetes k3s constantly cryo podman build kit kaniko ko buildpax.io container registry etc we will look at how easter eggs did look like with these tools in the later slides if installation comes with performance optimization and content verification prefetch optimization allows important files to be prefetched to avoid network overhead ethernet is discussed in continental storage's snapshot project this is based on started image format proposed by crvs but comes with performance optimization and content verification as shown in the following figure it started is comparable to gzip and usable as a valid oci or docker image layer each file is compressed as a separated member and the offset to each member in the layer is recorded in toc file appended at the tail of this probe this enables random access to fire entries and http range request is used for the random access to the layer blob stored on the registry estrogen supports prefetch of files that are likely accessed during container startup these files are called prioritized files before continuous setup container runtime prefetches them to mitigate the network related overheads for reading them other files are fetched from the registry on demand they are also fetched in background to make the image locally available as soon as possible this shows the time to take photo python 3.7 image among three types of formats legacy images cannot start container before the entire image contents become locally available so startup takes accordingly it started no opt is eastern image without prefetch optimization this shows the time to take for startup but there is a random performance drawback because of the overhead of on-demand fitting ethers can mediate this by profiting note that creating container takes longer than optimized estrogen because prefetch occurs during this step glassfish server image also shows performance improvement of pool by e-sergeant image and same as the python image optimized eastern visit mitigates runtime performance drawback of on-demand fetching lazy blink can be enabled on kubernetes as shown in the following figure plugins are needed for cri runtimes search sergey snapshot is used for quantity and sergey store is used for cryo we will introduce these plugins later there is a real-world use case of cern for speeding up analysis pipeline k3s supports discipline of ether digit this patch is already matched to the main branch so will be included in version 1.22 third snapshot provides a kind note image that enables lazy playing of ethers event this is a demo of running hello world golang ci as a workflow on k3s here i use the upstream k3s that supports lazy playing of easter digit and run it in k3d which is his racing docker this workflow builds hello world golang binary it runs golang ci lintel and gosek then builds a binary and finally it executes the binary in alpine ubuntu and federal continents apa shows the workflow that uses ethereum image for the runner image of each step each step runs without waiting for the pro completion of the runner image it takes about 52 seconds lower shows the workflow that uses normal lazy image each step needs to wait for the pro compilation so it takes longer than the workflow with lazy blink in this case it took about 94 seconds for continuity search the snapshot plugin enables laser blink this is implemented as a remote snapshot plugin of quantum d as shown in the following figure sergey snapshot mounts each solid layer on the node using fuse each file contents are downloaded from the registry lazily started snapshotter communicates with quantity via dlpc api and provides field mounted e-suggested layers to quantity using this configuration you can perform lazy playing on kubernetes another ctl i took a compatible cli of constantly also supports lazy link so you can perform lazy playing on your time now as well this is a demo of lazy playing of hello world is started image on continuity with another gtl the upper console shows lazy playing of ether did it and the lowest shows normal non-relatable each stage that enables content to run the container without waiting for pool completion so it finishes in about 5 seconds no lazy image needs to wait for the pro completion so it takes about 32 seconds for pottermore and cryo storage store plugin enables lazy blink this is implemented as an additional layer stop logging available from podman version 3.3.0 and cryo version 1.22.0 additional layers to allow sportsman and cryo to acquire container image layers without pulling them from the registry as shown in the following figure sergeants to amount extracted each suggested layer on the host using fuse each file contents are downloaded from the registry lazily third this tool communicates with sportman and cryo via file-based api and provides fuse-mounted e-suggested layers by using cryo with this configuration you can perform lazy playing on kubernetes if you use podman with star digit store you can perform lazy playing on your time now as well this is a demo of lazy pulling of hello world estrogen image on podman the upper console shows lazy playing of ether did it and the lower shows normal non-lasable portman finishes running its third in about five seconds already exists message shows that the podman acquired image layers from the studies store no lazy image needs to wait for the pool completion so it takes about 25 seconds buildkit experimentally supports latency playing of e30 that formatted based images since version 0.8.0 in general every build starts from preparing based images by from instructions of dockerfile when you build an image using large-based images that aren't cached on the node they need to be ports here and it can take a long time this lazy playing feature is trying to solve it you can use this feature via docker build x as shown in the command example in this slide as shown in the following figure builds run without waiting for the full completion of from instruction necessary files or chunks of the image are pulled on demand during copy and run instruction a variety of tools in the community start to support creating e-surgery images buildkit and canonical support building e-suggested image buildkit supports building is as one of the compression modes you can use this feature through local build x compression equal e strategies option enables this feature kyanko supports building easterners this is an image builder runnable in continents and kubernetes gtcr experiment is searches that equal one environment variable enables this feature there are image converters as well ctr remote is a cli command for quantity this is provided by starting the snapshot of project so you can fully customize and tune the e-suggest image including prefetch optimization now the ctl is a compatible cli for quantity large ctl image convert sub-command converts an image to each star with it not ctl is docker compatible so it has not ctl build sub command so you can build arbitrarily started images using build sub command and convert sub command together co-container registry and plane also supports image conversion this is the library and the cli command to interact with registries ggcr experiment ethernet equal one environment variable enables conversion from an image into each stylization tools like canico co and buildpacks.io is called container registry so these tools also support easter eggs creation as well as discussed in this talk ether data is now widely usable on tools in the community for keeping interoperability of estrogen among these implementations we are proposing its strategies to oci image specification because each stage is comparable to the current oc image spec we are proposing this as backward compatible extensions to the current ocl spec feel free to join the discussion from the next slide tao introduced nida's image format thank you kohi hi everyone my name is tarpan i'm a staff engineer at time group i'm going to introduce the letters image service to you another simple services and an image acceleration service that we built in-house and time group and deployed largely on our production system when we started building letters we make sure that we do support this will be daisy fully but we still want more from just the discipline so here's the official register we have the letters firstly it can support ziplining with medium prefixed policies while we do when we do poor images nadas image from standard region 3 we want to make sure that even if the network goes down accidentally we can still access the data locally so we have so we we want to pull the data prefix data before even if the application doesn't access it because it can it may access it later on so we support provisional privilege and we have different privilege policies such as semantically or hintedly or biochemical just background prefetching and another thing we support is chocolate duplication with natural build cash what is chuckling about duplication we we can increase it to duplicate the ratio of a computer image not very dramatically and with native build case we can it improves image conversion from a central oci image to let us image very very largely and we support end-to-end data integrating that means we we can optionally verify the data contents when we read any data locally or from this which is to make sure that it is actually it is actually the data that is being built into the image so the this reason is that and sometimes there may be a local disc break or or some or there may be some park there accidentally changed in the core image cache with also which the data will the the application will never know will never know it but we want to provide the capability to find out such such situations so that in that as we can optionally verify it and let us support reproducible image building the ambience we can build another image from source heading machine it will be you it will not be affected by the conversion environment also with we support we they also the latest image format is compatible with oci dissipation spec and artifact spec so it is the reason that the lettuce is usable with a standard cloud registry also nadas has switched container ecosystem intuition it works with kubernetes docker container d build kit hardware and drink fly and different runtime different container runtimes such as currency and color containers it is very resource efficient and production ready it costs you you we have a very large scale deployment and group and the service is available via alibaba cloud service so this is architecture of anas in the middle we have a fuse demon and it provides both endpoint and verticals in the port field fuel enterprise allows the traditional zoning container to exist the image data directly and the water ifs does the same for cataclysm and it has shared uncompressed local cache so the docker cache is uncompressed though when being pulled from from remote remote storage and it will you it only needs to be uncompressed once and it will be shared by different data streams on the same host also we support singleton board with extremely low membrane footprint we have less than five megabytes per instance for each net schema and we can use dragonfly between a registry and and another service so the image data can be distributed by the p2p network so this is the core of the lattice image series we have which is cfraps which is the acceleration file system there details american stream method it divides each image into two parts one barcode three metadata layer it is circumcised verifiable by imbec definition and also it has chunk chunk share data layer so different emitter different images can share the same data chunk on the data layer also to be oci speak competitive to to be hostile compatible we introduced we just changed one field in ocie manifest so we as we introduced a meter new meter type for the another probe and and the rest of the manifest is compatible with the outside artifacts and the swing spec so that means it is compatible with which is three functionality as well and it is vital widely working with many container registers such as in many colored registries and it works with docker registry and also it works with harbor and this is uh there's a putting benchmark for where we've done for nadas as we can see for oci image the end-to-end time for oci the oci image creation their time increases and their as image size increases but with net us the time is mostly consistent or below 4 seconds for different images and the that nets works with variety of tools first we provide another stem short plug stem shoulder plugin for container d so this is a link for the stem shoulder and with with this plugin that works with building meeting container compute container ecosystem components such as kubernetes very quickly it works with ci control it works with nerd console engine and it works with ci remote or ctrl ctr remote tool and also we we provide tools to manually convert an oci image to another image incentive the tool is called another letters effect and in a bit many quality is to download the image from remote switches and convert it to another letters format and then push it push it to our remote storage so it's very easy to use just call another natasify convert decision source and then change target and also we provide another tool we will provide build kit support to manually convert to manage build uh let us image so this is we have a pr there for build kit and you you can build another image directly from the docker file and push it to an embedded registry we you know even with manual conversion people will ask for automate automatic conversion so we we start to work with the hardware team to provide automatic support for letters in the hardware project we have a design there and we are working on the implemented implementation so the general idea is when when user push push ocrv1 image to hover and the hardware will call image conversion service to convert it to two letters also the image conversion service is image formation image format and velocity so right right now we are working with this style with cohere together on the usage support for for image conversion service as well in future we may support more image format for the same component and on the standardization point we are working with the oci community to push all the artifacts many manifest spec to the to the community this manufacture functionality is tuned back and the stone map and oci image manifested to an artifact many manifest so we can connect let us let us manifest to the original oci image manifest and it will this will help which is to check the relation between different images and we we are propos inside the pr we are proposing lettuce image as an artifact type is called cncf rc1 the the this manifesto is is beneficial to his bone signature net us and scan with us etc it is very it is readily usable for different artifacts also as we also we are discussing with the ocr community um proposing the v2 image spec so we there is a brainstorming on what is uh what what should the v2 ocim spec.click address in in their discussion folks they also the oci community listed the several of key features such as reduced duplication reduced reproducible image building experiences faces from objects and the metadata runtime data integrated multiple phases format and the native laser phase support and we've we've analyzed the many many open source projects out there that supports this featuring or improved data distribution of the image their distribution such as p2p projector dragonfly and kraken or all these projection deck that part of the requirements for the v2 support v2 image format but as we can see that that us already supports both all these requirements naturally okay we let's let's do some recap we've introduced uh both strategies and lettuce in this presentation and the reason we do it is this mutual we we are aware that the outside images are large and slow we need we need to do the business pulling to start containers quickly and the prefetching is a good friend to daisy pulling because we we can get data before even before their container actually use it and we we we acknowledge that container ecosystem adoption is very crucial for for this new image format for for your strategy it its key feature is background by capability and it's an extension to the existing oci image spec for that is more future looking and it's a proposal to the next generation of ci units bank and here's a pair are some information for the strategic project it is 100 also a compatible image format for nasa pune and it is a sub-project of cncf graduated project container d there is ending for his resource and stack slang page also there are there are pre-converted images here and for the analysis project it is an image acceleration service with various improvements over the existing projects and it is a csf incubator projector general fly sub project and care are these links to it is source it's deck and a tutorial so thank you everyone for watching this video and we are taking questions and we will answer everything later thank you