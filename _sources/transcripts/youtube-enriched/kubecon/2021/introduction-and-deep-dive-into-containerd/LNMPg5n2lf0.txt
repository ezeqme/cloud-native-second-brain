hi i'm kohei tokunaga from ntt corporation i'm a reviewer of continuity and the maintenance of non-kwasa project sergeant snapshot in containery i'm joined today by akihiro soda from ntg corporation he is a maintainer of contrary project today we will introduce and div dive into constantly first i'm going to give you a brief introduction to continuity quantity is a consumer runtime project it is the fifth cncf graduated project containing manages resources about containers including container processes images file system metadata and resource dependencies then it provides clean interface to upper tools i will introduce internal architecture of continuity data as well quantity is tightly scoped but it's highly extensible in community there are many projects that leverage this quantity with extending it extensibility is also one of the major topics in this session i will introduce some examples about how to extend continuity quantity is now widely used by container-based tools in community including kubernetes and docker let's now focus on the adoption status in community from the next slide container is widely used in community including docker's use of continuity continuity is 83 percent of container usage according to the survey in 2021 container is used by managed services as well as open source projects including managed services like gke aws fargate aks ats development tools like docker mobile build kit humanities distributions like k3f kind mini cube cube spray micro ks and k3s and functional service like fasti if you are using either of these tools you are already using continuity so how quantum d is used in community quantity is mainly used in three different ways it's used as a cri runtime on kubernetes as a component of docker and as a general content management tool by various content id based tools let's keep a look at each of them one of the most well-known use cases of quantity is as a cri runtime on kubernetes as shown in the diagram quantum d runs on each node when cubelet on the node detects spots events from cube api server cubelet invokes quantities concern management functionalities the api used by cubelet is called cry as a cri runtime content reports images from registry and manages spots contents and images it pulls and stores images from the registry and executes them as contrast using low level runtimes including run c devices and cata containers etc it is now the default standard cri runtime for kubernetes it's used by various managed kubernetes services and kubernetes distributions quantity has been used as a component of docker duca relies on continuity for content execution and management images proved and managed by doc are executed by contradi as continents different from kubernetes the api used by docker to invoke contrast functionalities is called quantity api so quantity has two types of apis cri and quantity api then quantity manages contrast using low level randoms continuity is also used as general control management tool several applications are developed based on continuity they include build git fasti nod color and porch container continuity api is commonly used by these tools containing provides go client library and utilities for continuity-based content management applications can use them for integrating to quantity quantity can also be extended using plugins without recompilation as discussed later as shown in the previous slides containing manages images containers and parts so how is it done let's take a look at quantities internal components contradi has client server architecture it provides apis and content management functionalities to the client continuity project provides core client library for easily integrated to content client calls server via content api through unix socket in addition to containing api quantity also provides cri for kubernetes as well kubernetes calls continues functionality vcri quantity supports various low level runtimes including run c devices and cada containers it also supports non-oci runtimes including firecracker continuity is highly extensible so users can customize its functionality using various low-level plugins custom services or custom client library we will discuss about extensibility in more detail later quantity project provides quantity api client library it is called smart client and contains rich container related utilities including quantity api bindings container registry client pulling and pushing images image unpacked and creating osa config for oci runtimes using the client library there are several container declined implementations in community ctl is a cli client for quantity this is developed in contently project it's like a content api wrapper instead of control management utilities it's mainly used for developing of content or training new features not cutter is a docker compatible cli for quantity because of the compatibility it's easy to use for docker users in addition to docker compatible features it also provides quantities cutting-edge features including lazy blink and image encryption we will discuss about narrow cutter in more detail later with akihiro as mentioned in previous slides various quantity-based tools are developed as contrary client in community leveraging client library and continuity api content details or core provides content management functionality to the client via glpc api the content decor is implemented as a set of microservices and the set of apis of these services are provided to the client as content the api for example as shown in the figure there are several services like container service for contaminated management image service for image metadata management task service for performing container execution and cri service that implements cri among these services there is a shared metadata underneath it's bible-based key values tool it stores metadata of contents images contents file system snapshots etc it also manages reference graph of these resources for performing garbage collection cri for kubernetes is also implemented as a micro servicing quantity initially cr is implemented as a separated client binary but it's currently built into content decor and the code base is also merged to quantity repository since content 1.5 as shown in the figure cri service provides image service api and runtime service api defined by cry these functionalities are implemented based on other contender services and external plugins other external plugins cri plugin and nri plugin are currently used cni plugin is used for network interface preparation for pods and an arrive plugin is used for managing node resources like cgroup because cri service relies on other services it contains contaminated client calls for talking to other contender services and plugins based on these services sara implements parts contents and images management in content decor there are several services that provide low level role of control management content store is a store for image content including manifest and layers it stores these contents as its bases so without the compression neural extraction these contents are content addressable and killed by the content digest snapshotter also manages image contents but it focuses on the file system snapshots snapshots are extracted in stacked view of continuous root file system layers snapshots are created by snapshot and passed to ocl runtimes as root file system of contents there are several snapshot implementation per packing file system overlay fx snapshot is one of the most used one but there are also other implementations like butterfa snapshooter a uefa snapshotter and fused-based snapshot etc runtime service executes low level runtimes via sim shimmy is a wrapper demo for ocr runtime and manages the container's lifecycle and logging different from one short oci runtime cls sim is long learning process that has the same lifetime with the container so it will also do runtimes that need to manage stateful resources like virtual machine of cattle containers this figure shows how an image is executed as a container in quantity connecting to the registry and downloading image contents are clients responsibility registry related utilities are implemented in the contender's smart client library once the client pulls image contents it stores the contents to contain the course content store as its basis so without decompression your extraction image layer contents stored in the content store are decompressed and extracted by this service and the stacked boot file system view is managed by snapshot client library provides unpacker utility for this process then snapshots are used by task and runtime service and passed to oci runtimes and they are used as continuous root file system task service can be used via quantity api bindings by the client as discussed in previous slides continuity is highly extensible let's look at how it's done and some examples of extension for levitating quantity you can extend quantity by plugging external binaries into continuity without recompilation co-plugins can also be used there are two types of external binary plugins one is the plugin talks to contain the via unix socket proxy snapshot and proxy content store are these type of plugins another is a plugin as an executable binary string processor and sim are disabled plugins quantity api is also extensible by implementing their own custom service for example file clock quantity has new apis called control api by their own control service from next slides let me introduce some examples to extend constantly using these plugins using plugins you can enable lazy blink on quantity remote snapshot plugin is a plugin enables this feature remote snapshot allows quantity to perform lazy playing of images from arbitrary remote store lazy playing here means quantity can start up contrast without waiting for the entire image contents being locally available instead necessary chunks of image content are put on demand as soon in the following figure remote snapshot plugin has a responsibility to fetch image contents from the back-end store and provide continuous root file system snapshots to quantity as mentioned in the previous size snapshotter can plug into quantity via unique socket so no recompilation is required there are several remote snapshot implementations in community for example strategy snapshotter is a remote snapshot developed as a non-course project of quantity this enables content to lazily pull images from the standard registry content handles image layers for creating containers root file system the image layer formats are defined by ocr image spec and gzip that standard and blender layers are currently supported however not limited to ocl layers contradict can handle actually layer formats like encrypted layers even if they are not supported by the oci image spec the plugin enables this is string processor as shown in the following figure this service can recognize several stream processors this service converts image layers downloaded from the registry into root file system snapshots with chaining these during processors each stream processor converts the media type of the layer into another diff service can handle actually media type of layers as long as the corresponding stream processor is plugged into this service for example image script stream processor enables containery to handle encrypted image layers string processor binary can be plugged into quantum d as a separated binary so recompilation is not needed quantity can integrate to arbitrary low level runtimes low level runtimes are not limited to oci runtime but no oci runtime like firecracker can also integrate to continue the plugin enables this is called sim sim works as a thin wrapper of low level runtimes each low level runtime can integrate to continuity by implementing their sim following the defined sim api run system provided by continuity project also supports pluggable logging destination feature this enables sim to stream container logs into arbitrary destination like fifo and pipe external binary and file from an exercise aqui akihiro will talk about the concern the client hi my name is akihiro suda i'm a maintainer of contribution in my project i'll explain how to implement your own continuity client for fan and profit to implement your own country client first you need to choice api from two apis the first one is contingencies relative api and the second one is cri api the negative api is used by several projects including docker mobi build kit first d nerd ctr and other center party projects on the other hand cri api is used by kubernetes these apis are similar and the boss obsidian use crpc over unique circuit as a transportation mechanism but there are several notable differences for example the native api is container oriented while cri api is part oriented so the native api does not have first class support for port objects and the native api has several complicity complexities compared to cri api but the selling point of the native api is that it has more flexibility and features such as pushing images to registries so so it's hard to tell which one is better but i suggest using the native api of ctrl d because you probably want to be able to use all the features of ctrl d but if you prefer simplicity you may want to take a look into croi api first to use these apis in theory you should use any programming language however in practice you have to use go language for the controller native api because the native api model depends on the smart current smart client library that is implemented in go wrong ways especially for pulling images from registering currently it's really hard to use other languages such as rust or python to implement your own client we welcome contribution for supporting more languages the next topic is an example of contrary client you can find example at https constructors control dot io docs thrush getting solid in this example you first create a current object using conjunctivity.new with a demonstrate at thresh run slash and then you create a ctx object that is associated with a contrary namespace string and then follow the radius image from localhub using client.pro then you will create container named ready server with several options ctrl d dot with image continuity dot with new snapshot and continue dot with will speak and we have more with with options such as oci dot with process x or specifying the quadratic quadrant strings to be executed in the container and oc ocid with mounts for mounting data volumes and oci database memory limit for limiting memory resources and second with profile for specifying a second profile at this point your container is almost functional but in addition to the client you will have to implement oci hooks and and also rural binary in muscle uses an ocr fook is a custom command there is called on the host on creation and duration of the container oci hook is typically used for setting up and tearing down cni bridge networks and port map configurations using oci hooks is optional but necessary if you want your continuous to be restarted automatically on post reboot this is complex so i will show the code on the screen but you can find an example called in the narrow city ripple and if you want to see the rocks of the container or transfer the rocks to through energy or something else you also have to specify a ruler boundary the new cta repo has an example code for showing rows as json files and actually nato ctl is almost full implementation of docker except swarm self it was spun out from the code of ctrl but it has more practical features compared to ctr such as automatic resetting of containers and protocoling loading and rudeness more and it also supports basic brain using strategic substructure and also decryption of oci-crypt images we carefully designed the source code of nano ctl to be readable to beginners so you may copy the source code or narrow ctrl as a structural part to create your own client application the rational the last topic is updates in country 1.5 and future plan the version 1.5 of concern d is expected to be released by the end of april this talk was recorded in early april and brother 1.5 was not released at the time of recording but i guess it will be released by the time of broadcasting this new release supports cstd as an image compression algorithm in addition to gdp this algorithm is much faster than polish traditional 3d project 1.5 also adds support for nri node resource interface nri is similar to ci controller networking interface but nri is for managing resources such as c groups in this release we also enabled decryption of oci crypt by default eccrypt itself has been supported since browser 1.3 but you have to provide a custom configuration file to use ocrypt in previous releases and we also recently put nero ctl into the country as a sub project this is a docker compatible cri i talked about this just a minute ago and in this release we also merge the label of the crm program into the main repo this change is not visible to end users but this monolingual model simplifies process for contributing to control d we also switched away from blender.conf into go modules in this release so next is future plan in the next series we are planning to support fire system culture such as x inverse quarter whose root five system of the continuous we are also trying to support user name species for cri so that qrs can launch put as a known user that is different from the user account of content e this is similar to router's continuous which means running everything including container a knowledge user but this is different from windows containers and does not cover it either we are also trying to support port as first class objects and we also need to eliminate the post image for port sandboxes we also need to have more documentations and we need your help the last topic is recent updates of certain deployments last year dragonfly released neighbors substitute the problem for quantity neither substructure is similar to surgical substructure and enables basic print as well as denomination of the images but nadas uses a file format that is incompatible with ocr tables and alibaba recently released overlay bdd samsung programming pd means broke devices overlapping is similar to urban effects but uses icelandic block devices we also have several new runtime programs rank you by tonight hashmaster is a runtime for running linux containers on mac os using ljl linux kernel library run j by samuel corp is a runtime for running free bsd jails as oci containers let me reach out to this session continuity is a default channel runtime for tribalities but not only for tubantes it's used to buy docker mobi buildkit puzzley and several other projects continuity is designed to be extensible with programs we have many programs such as runtime programs some sort of problems showing stream processor problems and loading programs and recently we added a new sub project called nero ctl this is like docker but with full features of contender this is also like ctrl but with full user experience of docker that's all thanks