hello everyone I'm Alex and today's topic will be how to secure both your build and your Cloud by utilizing IDC tokens in your pipeline so let's start with the agenda before diving into any solution let's start with understanding the the problem let's start with discovering how modern sdlc pipelines are working how they are built and why there could be a possible issue with the cloud Authentication after understanding the problem and that ODC could be the solution to several of these issues we'll understand what is oledc open ID connect what are the its concept and how it can be used in cicd environments then we will build some really simple real world scenario which will go through the slides and finally we'll show you several demos and how we can you can build a more secure authentication to your Cloud environments using IDC I'm a head of security research in scicode previously I led I led the Marvel research team at a checkpoint where we're doing a lot of reverse engineering for the complex pieces of malware discovering a campaigns for a cyber crime and apts and nowadays inside code we're a researching vulnerabilities in software supply chain security and hopefully also mitigations for these vulnerabilities I'm security research at cycode used to do API security and iots before I came to recycle okay so in short the cycle road is a cyber security company based in a Tel Aviv that they are doing complete software supply chain security for organizations and in all various of kinds of security both in searching hard-coded secrets in your code securing your pipeline misconfigurations Cloud security and many more so let's start with the problem so after investigating and understanding how most of the modern sdlc pipelines works we can divide it to several several parts the first is the developer whether it's a contributor a maintainer collaborator doesn't matter instead the actual person that pushes the code the second part is the SCM system Source Control Management mostly a git based it could be a GitHub gitlab bitbucket Azure devops doesn't matter and and most of the code is pushed through one of two ways either a direct push which is usually not recommended or through a pull request or marriage request depends on the system where you suggest a piece of code that should be reviewed by multiple developers and before it's been pushed to the to your Cod Repository and whenever uh you push a new piece of code usually which is the most important part in the recent years usually there's some trigger event that they create some autonomous procedure in some CI pipeline in some CI system so there's plenty of system nowadays GitHub actions GitHub CI Circle CI Jenkins and the list goes on and usually these pipelines are doing a predefined procedures and taking your code checking out compiling it packaging it pushing it to some artifact registry some a container registry doesn't matter and also doing the deployment phase a creating resources in our cloud and actually creating your production environment so to create this all this complex say pipelines you need a list of first part Integrations in your cicd pipeline for example it could be artifact Registries a Pipi Maven Gradle and PM Docker it doesn't matter what could be also a cloud provider's Azure gcp AWS and to create this procedures you also need to authenticate securely with these providers so the popular way to do that nowadays is mainly through through tokens tokens of just similar to username and password but tokens is that like the popular method you generate some some credentials in the system and you save them in some secret manager of your CI system so you could access this securely from within your CI and how popular this method from simple search in in GitHub for accessing to this Secrets there's like hundreds of thousands of occurrences of this access to Secrets it it is a suggested in like almost every best practice paper it's better than hard coding the secrets of course and we can safely say that this is like you know the most popular method nowadays from in your side pipelines to access some different services so you could think yourself what what is the issue if the best practices suggests this method and they are encrypted and they are not hard-coded so what is what could be the possible issue with that well it's better than hard coding them but it's still it's not enough and the first reason is circle CI bridge in in January 4th circus CI a really popular CI system announced that it was going through a security breach they discover it after uh one of its GitHub token that was saved in circlesia system that belonged to one of its customers was misused maliciously used and then they understood they are going to from going through some security breach which that means that all the tokens that saves in the system are actually compromised and so immediately when they discovered that they announced and told all their customers to reroll all their tokens which means that they could be compromised furthermore when they finish investigating this this bridge they went with several best practices and one of these best practices they and they said that to use oil IDC tokens wherever possible to avoid storing long-lived credentials in circle CI this is the first case the second case which is a more popular which is the code Bridge happens happened two years ago the code cop is really popular a code coverage tool that usually runs inside your SCI system and two years ago the installer of codco was compromised and every workflow that was running codecov actually run an additional malicious bash comment that exploitated all the environment variables of your CI system to some attacker control server so why it's relevant to their secrets because usually when you're using Secrets inside the CI they're stored as environment variable so it's another another reason why we need to improve the the security of authenticating inside the CI so what is the solution or maybe the solution open ID connect or IDC so let's understand what is what is IDC so it's built upon on the oauth 2.0 2.0 framework which is a widely popular framework in the web Authentication and it actually extends its capability it mainly extends it by allowing a third-party application to verify the identity of the user in the authentication scheme which is a Json web token so it both are the same off using the concept of tokens and Scopes so token which is this JWT is similar to a train ticket that you could show it to do something it allows you to do something right in Scopes it is it defines what the user can do for example you can use this train ticket to board specific train so how JWT looks like so one of the benefits of of RDC that it's standardized some of the claims and some of the fields in this token so the most popular one is a subject which is the identity of the user or maybe the machine it could be used to Grant you some permissions an issuer which is the The Authority that issued this token it also contains the the token issue time expression and many more fields so another a very important fact that all this JWT have signatures that were signed by the issuer so um so they are assigned rather than being encrypted so let's see how ODC can be used in web Authentication let's say I want to consume some resource on some web application and I'm I can click on this login with Google button on the on the application so it will follow me to the identify identity provider of Google which will raise me some authentication consent this small small window that asks whether I'm authorizing this application to use my identity and when I accept this so a Google issued issuing a new a new token a new jettability token that's signed by them containing my identity that is passed through the web application and the web application can use that to understand who I am and Grant me the proper permissions so you configure it why why is mad world application because we're talking about cicd so let's see how ODC can you can use in cicd so it's it's a not a coincidence that in the last month months or so most of the popular cicd systems starting started to support IDC natively furthermore gitlab even in the in the past weeks even extended its support to the IDC but by allowing it allowing you to define a custom audience in your pipeline so how it works let's say I'm some CI system and I'm creating a new job a new job that doing some something that's related to my development pipeline so in that job I'm able to request the identity provider of the system let's say GitHub or gitlab so request a new token that that contains the identity of the job of the pipeline so I'm requesting it with the editor provider it signs that token and give it back to me I'm dead I'm the job and then the job you could use this token to pass it to some for example cloud provider to STS security token service of all the cloud providers have this service and it could they could use this identity to issue me a short-lived credentials for the cloud provider it could be AWS token gcp credential Azure doesn't matter and then I could I could use this short-lived credentials to access the resource I want in the cloud so this is a high level schema we soon will see how to implement this in details so to do that we created a really simple example really to explain the concept this example we will have a GitHub and gitlab we show how to implement in in both and we want to authenticate with gcp and then we want to access a really simple resource in gcpu that will be a GCS which is the cloud storage of gcp similar to S3 and and we showed how to implement that both in credentials based authentication and in identity based Authentication so I will pass it to a lab okay so in this simple flow we'll upload the configuration file from GitHub and from gitlab and a two gcp bucket so let's con let's start the firstly we'll create a service account the service account will have a permission to upload upload a objects there to the bucket and we'll export the key of the service account so we have now A Long Live credential that we will have to put in our CI system in order to authenticate so let's start with GitHub a common workflow will look like will authenticate to gcp using the Json credation file and we'll upload the configuration file at to gcp next one let's see the gitlab workflow and this time we'll use the gcloud client but this time the best practice for gitlab is using the ashikop vault so if we don't have a Chicago fault we'll have to save the gcp as a protected or mask variable when we will navigate to desktop and add the the gcp secret we'll get a warning says the regular expression does not match to the Json there are some work around to that we can basically a base 64 and code this Json but it's not the preferred method and pretty much every website we go and it says that we should authenticate using different methods and that's different methods currently it's the YDC meaning we'll not have to rely on Long Leg credentials and but we'll use short-lived credentials instead so how do we do that firstly we'll create the identity pool it will add the identity pool will have to provide us the GitHub provider that will contain the issuer the default issuer for GitHub next we'll create the gitlab provider that will also contain the gitlab issue so what we have until here we have the different the identity providers of GitHub actions and gitlab and also we defined a providers for gcp and for dedicated for GitHub and gitlab and we still have the same service account we defined before but this time we'll not export the service account the secret key that's long-lifting will use the YDC method let's start with GitHub the first thing we see is the permission the permission is changed and we're telling to the GitHub pipeline to ask for oidc the ID token to be used nextly we'll use the identity provider full path and the service account email to a complete this authentication and then as before we'll upload the configuration file to Cloud as we can see and the main thing the change here we are not using the secret we used before so well the secrets they're gone let's see a little demo we have the same workflow we defined earlier we will rerun the workflow let's go to gcp cloud and we can see the our identity provider identity pool and both of the providers in this time we'll see in GitHub with this default issuer uh we'll go back to GitHub and we can see we have uploaded the configuration file the workflow and the workflow has succeeded and when navigating to our bucket we can refresh and see that our configuration file is there and all that without credentials without the long-leaf credentials nextly gitlab with the same flow but this time in gitlab will use the ID tokens keyword the IDC token will be saved under the IDC underscore token and will be used in the same way but this time we'll use the gcloud a gcloud client so is that it we defined our identity pool with we defined our providers but it's not it and we it seems that at the default way without configuring any mechanism to filter request any signed oidc token will be accepted meaning that if gitlab Ci or GitHub action will sign the IDC token it will be accepted and will grant the service account permissions so we can see the identity providers and service accounts are all over so we need to use that mechanism and that mechanism will be using attribute conditions so let's in and deep dive into the oidc structure both GitHub and both gitlab Define some custom claims in addition to the standard claims and each one of them defines its own a particular environment attributes like the repository path and their reference the the ref name like the the branch name and the emails and many more so we'll have to choose which one of the standard claims all the custom claims do we want to trust that will be the mechanism that will fit a filter a quest to our cloud account in gcp we'll use CDL context expression language that will allow us to deny all accept connections and to choose what environments do we trust to do it was the certain repository do it just a certain email do to ask any of that and we'll have to choose so an example as an example to strict conditions are conditions that use IDs for example the repository ID the subject are very strict conditions that cannot be malformed an example to a wide conditions and easily bypassed are conditions that includes that start with contains that can easily buy tempered by a attacker to create and repository that contains a certain world and bypass the attribute condition so let's move on in this case we have the same workflow but this time we'll add a simple thing we'll add an actual ID and attribute expression that filters and certain users and this time the actor ID will be handled my ID is different from handle so let's rewind the workflow so again in this time it's request an IDC token from the identity provider and giving it to the cloud provider but this time it failed it fails because it says that the attribute conditions denied this connection and uh it will deny all the connections that will not contain in the IDC the ID or we defined before so let's look on a high level what happened in this video we have we asked the CI system to give us an IDC token we send that the pipelines and the YDC and the SDS checked the attribute conditions and all the other stuff and so that the attribute condition returned and false expression meaning the attribute condition failed and it retained a return Then deny message and to the API that says the attribute condition did not match to the oidc science token and so it seems like this is like a perfect technology and there isn't a single reason why not every CI will use it nowadays but it's far from the truth uh we we showed you examples for GitHub git lab and Cloud accounts but many services nowadays are not supporting RDC based authentication one of them a really popular one is Docker Hub there's even an open issue for Docker Hub to support this and until that if every uh every login every authentication the docker Hub in the CI would look like this it should require you to to create tokens and and access them in the CI and this could lead and this could lead to handling many tokens in the system imagine you you have different tokens for each type of CI with the different Privileges and this could create a Real mess there's a pretty good solution to handle that as well and it's called the external secret manager and there's very popular a secret manager one of the popular is hashicup Vault and we will show you how to implement this also through the gcp secret manager and so in this uh in this schema uh it's similar to the previous setup but we will create this Docker Hub token we will save it in a gcp secret manager and then we will have a service account that will Define it to access to Total allow to access this secret manager and this the service account will be exposed through the uh through the identity pool and the providers that we that Ella show how to define previously and how how to do that in the gcp which is we just access the secret manager and we're adding the tokens there and we're just defining this service account so so it will have the permission to access these secrets so the code is pretty simple this is the case with the GitHub action we could use a really simple action that developed by gcp that's called the get secret manager Secret and it's it's in the sentence in the syntax you just specify the path for the token in the secret manager in gcp and the name of the variable that should be that this token should help should hold and then for a for the subsequent steps that you want to access Docker Hub we just mentioned these variables that you defined previously it's pretty simple in a gitlab we recommend to use the Vault because it has a native support for Vault and so you could you just Define the the token the IDC token in this example it's called oibc token and then in the secrets part you just Define the path the secrets is involved and the environment variable that you want it to be called for example Docker Hub user or Docker Hub passwords and then in your script you just you can use this environment variable that was fetched From the Vault and you could log into any service you want in this example it's Docker Hub but could be npm by pi or any other service so the the great benefit of using secret manager in this case is that instead of managing tens or even hundreds of different tokens and you can even create a single token for example in Docker Hub save it in sequence manager and you can rewrite periodically to secure it from Bridges so this is only the tip of the iceberg in ODC tokens usages there are many more some of these uh I'm mentioning here for example a really popular usage is authenticated to kubernetes Cluster through IDC let's say I want to access the kubernetes API server I need to do some authentication and login to to the API server there are many methods to do that the most popular I think is through a certificate but I could also Define it to use my my identity to log into the API server and receive the proper privileged for my identity there's a plenty of tutorials how to do that it's out of scope of this talk in additional use for for our DC tokens is to access Cloud resources from within the cluster let's say where I have some part that need to upload some configuration file to S3 or to Google bucket so I could create a similar procedure but instead of trusting the GitHub or gitlab issue I will be I will need to trust the kubernetes service account issue and will enable use the identity of the service account to access of the kubernetes service account to access to the cloud a a little bit a more complex scenario that we could Implement that is fruit stiff Aspire I'm sure you heard about Swift is fire in this conference there were several talks about it uh and the third first use case is also very popular nowadays is to use their six store or a cosign a sister is a great framework for uh to ease the container signatures and you can sign containers using your identity either personal identity through some I know Google authentication or the CI identity through the similar steps that we previously showed you could also sign commits through a git sign which is also a great tool from the sixth Oracle ecosystem okay so let's recap this talk so we started first with explaining about the problem how sdlc how modern pipelines looks like what is the importance of cicd systems and how many uh how they are using these third-party vendors to uh for for the workflow and the popular method nowadays for CSD system to authenticate is through tokens because it's easy and it works and which obviously it could be a problem because of the and the latest breaches show that then we explained about IDC open ID connect why it's so important and how cic systems nowadays use that and then we showed you the steps how to configure gcp and GitHub and gitlab to create this complete flow of IDC authentication and then we also showed you what to do with providers that don't support native iitc authentication and that will be through secret manager like gcp has one Azure control AWS has one and and many more so thank you very much you're welcome to check the full technical details of the stock in our blog you're welcome to scan the QR it will take you to the blog or you can check any other technical information in our blog there's plenty uh thank you very much for listening and that's it foreign [Music]