hello folks Welcome to our talk today we'll be sharing our story in running 10,000 Argos applications and sharing our journey in tuning the performance of our Argus CID instance my name is giri and this is my colag Udi both of us are infra Engineers from go goto Financial goto Financial is a financial arm part of financial arm part of uh goto group the leading digital ecosystem in Indonesia we provide various service offerings from ride hailing service using motorcycle food delivery service package delivery service e-commerce platform and many other services to start off um I'm going to give you a brief uh overview of the state of current of our kubernetes and Argo City we maintain around 50 kubernetes clusters across NWS gcp and private data center in Singapore and Indonesia region this consists of 700 compute notes 15,000 CPUs 120 TB memories and more than 30,000 pods this Argo City dashboard uh snapshot we took uh two weeks back there's an interesting story behind this snapshot when we were preparing for cubec con proposal this St few months back we were at uh 7,000 aru CD apps and looking at the growth rate of our agdf app creation we predicted today we would reach above 10,000 that's why the title 10,000 arusd apps and today we made it it's now uh 11,000 arusd applications these 11,000 applications are coming from 6,000 repositories across 60 different projects Argo City watch uh more than 30 380,000 total objects on our largest cluster we run 2,000 applications and Argo CD watch over 40,000 objects we adopted a simple centralized Argo CD instance model which is technically a push model or some of you refer it as Hub and spoke model in hubit Spoke model we have management cluster where we run our single Argo CD instance this Argo CD instance reads a common git repository sorry git uh git providers and then using this uh the the the manifest stored in G repository the aroid instance push objects across all the Clusters that get registered under aroid instance there's couple of benefits with this simple centralized Argus cidd instance it's very easy to maintain and upgrade because we only need to upgrade one argocd instance regularly it's very easy to integrate with our automation at platform internally we maintain a developer platform that is tightly uh related to argd instance so maintaining only one argd version makes it very simple to write our integration logic in the platform it's very easy to manage centralized arback because everything is in one place and we have a nice single dashboard to view entire Argo CD applications that we have across entire clusters that we man manage internally we have a developer platform that became the primary interface of our product Engineers we don't let product Engineers to create argd application by themselves so um this developer platform leverage uh the standardized Helm charts that we maintain the platform team maintains and the platform generates the Manifest push it to uh repository as well as generate the aroid applications our developer platform has some sort of grouping mechanism so that sets of aroid applications point to the same repository in some cases one AR CD application can also point to one repository the design uh in our platform is that one service can contain three to five argocd applications these sets of applications can have different different life cycle so for instance if a user or product engineer create service in uh through our developer platform we would uh generate one Canary application and one stable application the stable application points to uh let's say a stable container image V1 and then the canar application points to uh an updated version of image for instance V2 the separations of application makes it very easy for us to perform our canar rollup strategy and do promotion to the stable and uh canar roll back if necessary makes it very easy now in our platform we enabled sto by default which means we inject esto side cars into every pot that we make that we manage in our clusters we manage uho sidecar objects or this uho uh sidecar configurations as a separate application so things like viral service object destination rule object are maintained separately in another application in this this application we configure the traffic routing let's say canar 5% or stable 95% the reason of this separation is is that we um each of the application we can control it independently and in some cases if the service wants to expose domains to public or third party Partners we expose it we control it uh we make configurations through another application uh the Theo Gateway application which contains Gateway object another use case that we l argd a lot is to maintain our cluster runtime components in this cluster runtime components we have standardized components across the 50 clusters that we that we have we left argocd app set or application set and the app of apps pattern on a monor repo so we only have one repository that manage and configures runtime components for all 50 clusters that we have in the root of repository we maintain the the the root apps set which generates the parent app of each cluster so in this example there's cluster one parent app each parent app manages the base cluster configuration as an app as well as the runtime component as an app set this base app uh contains like basic configurations like arbac or limit range and so on that are standardized across all the 50 clusters that we have if um if we need to apply uh if we need to customize the cluster beyond what is standardized we do it through another application which is uh server s side apply patches application these sets of uh applications are replicated for the rest of the Clusters that we have and by the way way we also manage Argo CD on Argo CD there are few challenges with this simple centralized Argo CD instance first challenge is this Argo CD instance require connectivity to all the target clusters that it manages so the consequence of this is we need to establish tunnels or peering connectivity between the management clusters and the entire workload Target clusters if and sometimes it's not always possible to establish tunnels and peering we do it through a public network over mtls another challenge is the limitation of Argo CD functionality first in Argo C we need to maintain unique application name globally there's no separate uh so even even for different different projects the application name cannot be the same for us luckily we always generate application name from the platform so we we can have some sort of convention uh to add let's say the customer name or team name as a suffix or prefix of uh aroid application name so each team um in our platform can have conflicting namings and then uh the next challenge is this uh centralized Aro CD instance is a single point of failure another challenge that we encountered a lot is performance issues of central Argo CD instance along the way we en encountered slow reconciliation problem and sync issues we look at the argocd or Q dep metrics and the app reconcile metrics for this I'm pretty sure a lot of you are also experiencing this if you manage uh more than thousand applications the UI starts loading very very slowly for us it took uh around one or two minutes to load the entire homepage of arili dashboard this is quite obvious and we got a lot of complaints from the product Engineers we Face frequent repo server o kills by looking at the cube events we have an alert for this we Face a high rate of our git API calls for both LS remote and fetch we look at git request metrics for this and we encountered High repo catch Miss in the repo server by looking at the repo server locks it turns out later on we found out that um this High repo catmas is the root cause of why our Argo CD is making very high rate of git API calls and in our controller sharts we see imbalanced resource consumption by each charts and and noisy cluster problem next uni is going to give us walk through on our journey in tuning the performance of our Argo CD instance thanks G now let's talk about our Performance Tuning journey in the next 20 minutes we'll discuss all the config and parameter tunings we have done so far to support and scale our AR CD uh for 11k plus apps as a note the tuning in this presentation are not in chronological order of when we implemented them instead we'll group them into uh each of their own components so first let's take a look at argu CD components so we'll use this diagram from the official Argo CID documentation in Argo cidd there are four layers first layer uh we have the UI layer uh which is mainly uh for user interaction here we have the web app uh and CLI the web app we uh use with our web browser and CLI through allows us to interact with Argo CD through the terminal next we have the application layer uh which consists of uh the API server or we also call it argu CD server uh it serves API request from the UI layer and then uh next we have the core uh layer uh these components are the main uh Argo CD functionality components here we have app controller upset controller and repos server the app controller uh mainly uh reconciles and uh synchronizes kubernetes objects uh according to their state in the repo and then we have upset controller which is to generate applications based on templates and then uh we have the repo serer which receives manifest generation request from the Argo CD server and the amp controller and then uh next we have the infr layer uh Argo CD depends on these components uh for its functionality we have Ries for caching Cube API to uh watch and uh apply Cube objects and get Helm or customized repos and also deck for authentication okay now let's uh start first with argu city server so as G mentioned before uh we had very slow UI load uh so it could take anywhere between 15 seconds to 2 minutes for us to load the the Aro CD homepage uh depending on our network connection so as a solution we enabled gzip compression feature uh in Argo CD server so uh what we need is to set this environment variable to true so uh in our case it improved load Time by uh in average by 5x and and uh data size to seven times smaller next uh stillo Aro CD server and it's actually not a tune but uh more of a tip for us the Argo CD UI users we can actually use selectors to filter out only the applications we want to see using their label values their projects or their name spaces uh and in this example we select one project uh in one name space and we only show uh fraction of the the total apps that we have uh which under uh our selectors here so uh one great thing is that the selectors are safe the next time we load our argu CD Wii which is quite handy next uh it's about kubernetes CPU limits so this is actually not specific to Argo CD uh but more of a kubernetes mechanism and it can be applied to other use cases uh outside C uh Argo CD so the problem is that we noticed and uh through our monitoring uh system that all our Aro CD components got throttled uh CPU throttled uh and uh it kind of slow had an impact to our reconciliation uh latency so uh in kubernetes CPU request and limits are implemented using c groups uh which uses the CFS or completely fair schedular from the Linux kernel so uh CFS could guarantee or could total container CPU depending on the proportion of the container CPU shares or quota in in the N we we actually don't uh really have much time to talk about more about uh this mechanism but if anyone is interested to have uh deeper look we attach some references so uh as a solution to the CPU throttling we lifted our kubernetes limits we decided to leave it and uh our app controller and uh other components uh did not go throttled uh went throttled anymore next uh we move to the repo serer so uh giri mentioned about oh I'm killed happened to our repo server so it happened very frequently like then so as a solution we increase the replicas and uh use HPA so that the repo serer Parts automatically scales with its memory usage so uh with this we distribute more requests uh to into more pots so each pot actually get uh less uh manifest generation request uh from the server and app controller uh translating into less memory usage or alternatively we can also use the uh parallelism limit flag on uh the repo server to control how many manifest generation requests that can be served uh in parallel to help avoid uh The Kills however there's one thread of of this approach is that the throughput of manifest generation will also be low as we limit its parallelism next uh so the Aro CD serfer and up controller uh talks to repo serfer right they are the clients of repo serfer they uh talk to repo server for manifest generation and uh they have timeout configurations so as we uh grow we started seeing uh those clients timeout errors in our locks uh we see them when uh We sync and refresh apps so as a solution we increase uh the configuration in both the Argo CD server and the app controller one thing to note is that we really need to set on both uh components because I've seen people only setting it on uh app controller and uh Missing the server so they still see uh the time out errors next uh continuing on repos Surfer again uh so from our repo Surfer Matrix uh G metric we consistently saw uh High very high git fetch requests so Argo CD cashes generated manifest uh in redish and they uh have 24 hours expiry by default so in uh cases when when remote change often even though the repository T hasn't changed uh for example when we do get push force or when we update a Helm chart with the same version uh shorter expiry uh time uh will be more desirable uh to pick up those updates uh quicker uh faster than 24 hours but in our use case uh our Helm uh customized and G remote uh references are already hermatic so they uh we use the tack and we don't force push or uh force update to attack so uh we can actually use higher uh value for uh the expired time we can do it by setting this uh infir variable and after uh extending our expiry time we uh immediately saw uh dramatic drop in our G request uh G fetch request from the report server next uh we move on to monor repo so we mentioned that we use uh app of apps pattern and the uh application Set uh in our monor repo We additionally we also use the multisources apps feature uh of Aro CD which allows us to have uh these uh multiple features uh when defining an app so uh when we implemented this right after we immedately saw very high git Fetch and git LS remote requests uh as seen in uh the two screenshots here so we investigated it and uh found that it's potentially bug in Aro city so we implemented an an undocumented work around for this which is uh can be accessed in details in this GitHub issue and uh our GIF request Dro D dramatically after that uh which is shown in at the right uh screenshot but we are still seeing High uh LS remote request uh so we think this uh is still an open issue so if anyone is interested uh please uh check out the GitHub issue still about monor repo uh so uh we usually might want to use uh web Hook from our uh repository to notify argd so argd can pick up uh updates uh faster uh every time we commit in a monor repo uh Argo CD web hook server May refresh all applications when it receive uh a web hook even though uh the news change uh might not have any relation to uh all those applications at all uh it could be just a subset uh of the parts that we really care of but uh the web hook will uh refresh anyway all the apps in the refresh process Argo CD invalidates the cash for all apps and calls kubernetes API to annotate all application objects with a special annotation for Aro C refresh and uh this operation is a network bound uh process which may slow the update process overall especially uh when we start having uh more than 1,000 apps so uh we can actually filter out or uh define specific Parts when we receive uh web hook so that Argo CD will only refresh uh applications that are Chang uh in specific parts we can use uh this special annotation called manifest generate parts and uh we Define the specific Parts here after using this we spad up our uh refresh process after uh getting a we request next let's move to the app controller so one of the first uh problems that we had with app controller is about uh work so uh our app controller work Q depth uh started to pile up and it did not go down at all so uh we investigated and we decided to increase the number of operation processes and Status processors uh an app controller because uh there are the number number of concurrent uh reconciliation and synchronization that can be uh happening uh concurrently at one time so we can use these parameters uh to tune them and in our case we use these numbers one rule of Thum we can use uh for uh configuring these values is that for every 1,000 applications we use 50 status processors and 25 operation processors next uh about scaling so uh the app controller can actually be scaled uh into multiple shards so it's horizontally shable and uh the charging algorithm in is uh on the cluster level so different set of clusters will be assigned and uh will be served by different charts so to uh scale the app controller we need to increase the replicas uh of the stat full set of app controller and we also need to set this uh environment variable arusd controller replicas with the same value and after implementing this we started to distribute uh into more Aro Parts after implementing the uh sharding we noticed uneven sharts un even shart CPU usages some sharts uh has higher CP usage and some has significantly lower uh compared to others so as previously mentioned Aro CD shs per cluster not uh on the up level so uh large clusters could be hosted uh by the same Shard likewise smaller clusters could also be uh hosted by the same Shard as well uh resulting in the uneven uh CPU usages the new round robbing Charing algorithm uh that's just available recently uh might not help much either for our use case here because there's still chance that large clusters could get into the same Shard when uh Argo CD round Robins uh the Clusters into the sharts so we decided to do manual a manual shart allocation instead to uh fine tune the our shart resources to do this we can use the cluster Secret uh which is uh used by Aro CD and uh we add a new field called shart and uh we put the shart number uh into the cluster secret and after uh implementing it we saw more even CPU usages across all our shards so perhaps one discussion about uh sharding on Argo CD uh sharding on the application Level uh with think would be a really great feature and uh might be a good uh solution for the uh noisy cluster problem we have here there's an open discussion about this uh on GitHub uh if anyone is is interested please check it out next uh even after all the app controller tuning we did we still uh saw very high app controller CPU usages uh and what we thought was uh I think we think was slow reconciles so Argo C uh the app controller watches all field changes of track objects and uh if any field changes and uh differs to the bifast that's in the cach Argo CD will uh start refreshing the apps uh those objects are kubernetes objects which uh maybe as we know kubernetes Fields could get very concise and some Fields might get frequently updated uh even even though those fields uh are fields that we don't really need for Argo CD to reconcile so we actually only need the fields that we have in our uh repositories uh in our manifest right those fields uh for ex example are the uh status fields or the resource generation field or also the resource version uh field so in Aro C uh there's this new fairly new feature uh called ignore resource updates uh which was uh only available since V 2.8 uh and ignore differences features we can use these features to filter out uh the fields we don't really need uh for Aro to uh do that uh reconciliation process uh or we can call them high turn objects uh so in our uh case here we use uh this uh configuration uh I think it's a bit small but I hope uh you can see it uh for example the uh we ignore HPA annotations uh which frequently updates we also ignore some replica Set uh annotations we ignore and coin slice uh which could get very noisy and uh we also ignore the whole status Fields uh status field uh of our AG uh of our kubernetes objects one thing to note about status is that uh if you have uh custom controllers your custom controller might uh want to use some fields in the status field so uh you might want to specify more explicit uh Parts here uh you want to ignore in our case we can uh just ignore the status fields and upon uh implementing this our uh CPU usage uh dropped dramatically almost by half and for one uh fairly simple feature I think this was uh really great feature and uh shout out to the contributors and mainers maintainers for making this feature happen next uh last but not least the uh about API client so our in-house developer platform implements Argo CD AP library and as we scale we started seeing http2 go away errors from our platform so we investigated found uh and fixed a bu in the AP CL Library when using uh grpc web mode uh which is shown in this uh GitHub issue we uh fix that error uh and also actually for Argo CD CLI it may fall back to JPC web mode even though the flag is is not uh specified so you might want to check your UHC config argd config file to see if there's any uh configuration defer or alternatively we can just use the native grpc because this bu only uh affects uh grpc web mode okay that's all the improvements uh sorry the tuning that we can share and we do have uh more improvements to ourd that we look for to which will be uh presented by gir so we explored uh different alternatives to our centralized Aro CD setup the first alternative we look up to is the decentralized Argo CD model instead of uh centralized model uh this is a pool model where each cluster uh installs its own argd instance so if we have 50 different clusters there will be 50 uh different arusd instances responsible to reconcile in its own local cluster there are a couple of benefits which we uh which uh wins against the centralized aru City the first benefit is the application control workloads gets distributed across the Clusters nicely so it's easy to scale with this uh model and then second benefit the argd instance doesn't need to have access to all the kubernetes API servers that we have in the infrastructure so this model has a better security post than the centralized model however there are many challenges that we don't like um instead of maintaining one instance we now need to maintain and upgrade 50 different Aro City instances for us small team of platform Engineers we already regularly upgrades 50 clusters 50 esto uh components and if we implement this we need to add more Works to our our small team and our platform if we implement this we need to maintain multiple ago decline versions and know in order to talk to which cluster we need to uh use different arus decline versions it makes unnecessary complexity in our platform logic and we no longer have centralized dashboard to control and view Argo applications in a certain cluster we need to open different dashboards we would be having 50 different argd dashboards and anyway if the cluster gets large uh we still require tuning of that particular agus instance another alternative that we saw was um agent based argu city or a hybrid model between push and pull that we saw in aquity platform in this hybrid model each of the cluster still has Aro CD instance but there's a central argd control plane uh which oversees uh the entire application States across the Clusters and the dashboard is still centralized we really hope this model gets uh contributed Upstream in the community we've seen uh couple of work in progress in the community like the first uh pool request got merged couple of months back it enables optional pool mechanism for application set with this PR um the application set can generate application objects to a remote cluster and then let the remote cluster reconciles on the application object that they belong locally in the cluster now second uh second feature is still an open issue in GitHub it's uh it's an effort to to to implement centralized UI for multiple Argo CD instances and what we are also excited about is the Argo CD UI Improvement there's an open issue right now to support server site pagination on the Argo CD dashboard which will significantly uh boost up Argo City uh load page and the home page and this is already on aquid and they plan it to bring it Upstream I hope soon it will be available Upstream we are heavily inspired by uh um Tik Tok and adobe's uh journey in managing their thousands of arusd applications and we learn a lot uh from uh Argus CID best practices uh uh from Alex and also um the amazing Argo CID documentation that provides us clear guidance on the performance tuning uh Journey thank you for your time we are now happy to take questions we still have uh two more minutes and please scan the QR here to give us feedback thank you [Applause] hi thanks for the talk uh I wanted to ask how fast and easy is it to short app and down on the application server sorry I didn't get it how fast is that how easy and how easy and um fast is it to short up or down on the application server how easy is it to sh up or down right you were showing you had four application servers running shed out controller sharts the controller sharts right okay it's very easy you can yeah uh actually it's a simple just a simple uh replica uh change and to change the enironment variable we uh shown in the uh presentation so aru CD app controller is actually pretty stateless uh it can build up its own uh State again uh from scratch so uh we might perhaps see some a little bit of slow Rec consults right after we sh up or down uh the app controller but uh it's like just two to three minutes uh in our experience I hope that answers your question hi uh I have a question about the centralized Argo CD maintenance so what type of guard rails do you have when migrating from from one Argo CD version to another say for suppose uh recently we did a small migration from 2.7 to 2.8 and we found the performance issues and we rolled it back and uh in in case of centralized Argo CD if something breaks uh the whole deployment system might break so what kind of guardrails did you uh take when you are maintaining this okay uh upgrading Argo C uh uh a actually each argd version could have their own uh special treatment we need to uh tackle so they're actually uh like described pretty clearly uh in each upgrade uh documents for example I think from 2 uh 7 to 2.8 we had a pretty smooth uh upgrade and I think it was 2.8 to 2 2.5 to 2.6 right where we we had uh some major changes and we needed to uh so it depends on the the fs but generally uh it's not that operationally intensive uh and uh they have pretty clear guidance for us to do all right I think uh we are running out out of time uh if you still have questions let's do it in the hallway thank you