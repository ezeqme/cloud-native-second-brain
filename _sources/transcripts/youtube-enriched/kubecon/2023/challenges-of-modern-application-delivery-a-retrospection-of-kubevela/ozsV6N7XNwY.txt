hello everyone engineer from Alibaba today I and my colleague girl Inda will share the topic for you I will introduce the first part and the rest will be introduced by my colleague okay let's start so kubela is a modern software platform that makes delivering and operating applications across today's hybrid multi-cloud environments easier faster and reliable it is an application-centric control plan and also familiar by the community as implementation of open application models it is a powerful glue engine on the left side it can connect with your traditional CI systems or the modern githubs then it provides infrastructure agnostic layer to render or straight deploy and manage your software and finally deploy to the right side of the picture both kubernetes clusters multi-cloud services or iot Edge scenarios and equivala is a control plan that can provide best practices for your platform engineering on the upside it can expose many ways to manage it provides API CLI and also a very user-friendly UI console and you can use skid Ops to connect with it and it provides fully observability for the whole platform the most important is it has a large catalog of add-ons that can connect to the whole community so here comes a question that why we built covela process improvements over the past two to three decades have significantly increased the eligibility of software application and product teams offering some flexible services for both infrastructure like compute Network and storage as well as develop services like builds tests delivery and obsibility the autonomy and the process Improvement has also has the effect of gradually shifting more and more responsibility for supporting services to protect teams forcing them to spend more and more time and the cognitive energy on infrastructure concerns and reducing their time to produce value relevant to their organization so here is is a workflow that in a usual application delivery pipeline they must need to concern effects workloads for different kinds and they have to learn observability and security details for better stability and avoid risks they need to handle rollout and traffic splitting and learn these complexity infrastructures but all of these things are hard to learn well for example one developer learns the Ingress with V1 beta1 API that can work well as a Gateway in kubernetes 1.21 version but it suddenly failed when the cluster upgraded to 1.22 due to the API duplication they shouldn't care about all these things but they have two in this case so uh we developed the open application model in 2019 and it provides a consistent model for application delivery and in the open application model we provide infrastructure agnostic model that the developer just need to focus on components trades and after they compose whole application they can deploy it to different kind of runtime platforms so here comes the first challenge how could the model adapt to so many complex application delivery scenarios uh we firstly Leverage The kubernetes API and the crd ecosystem and it can provide a very detailed capability to connect the real world but we can't do everything to write crd operator so we Leverage The Q configuration language to program the logic in a very lightweight in the example we use a q template as a component definition it defines a user level component and expose the fields called image after that the user just needed to fill the image in the application and finally it will render the real resource its kubernetes deployment so in this way the platform Builder can provide the best practices and avoid lots of complexity for the end user the platform Builder can also create trade for the end user and modify the fields in this example we expose the replica Fields with the replica field we can modify the component and the underlying resource is the deployment so after that the Google application is infrastructure agnostic it won't be affected even the underlying API version is changed the next big challenge is how to balance the extensibility and the user experience when the scenario becomes more and more complex we will have lots of definitions and since these definitions are all extensible the end user won't know which fields are there so we developed lots of ways to help the users to discover how to use these definitions the first one is the API system who is the API the past platform Builder can build their UI console and integrate the covela system with their own platform and provide which fields are contained in the component or trade the second one is stocks we have developed the Valor show command line and with the Valor show command line you can check all these fields to learn the feature of these definitions which fields are contained and how they can be used and what's the type of these fields these documentation are all generated medically and the third one is the UI console the kubella ecosystem has a project called better ux it provides a UI console for you and every component and trade definitions can be converted in the uh UI console and the end user can have a click based UI and just a few the forms in the UI console the third one that we have recently developed the SDK for developers now users can use the golang SDK and with the validation API command line it will generate the SDK from the definition and we are also developing the Java JavaScript Python language sdks and also we have provides the accessibility for all these definitions that we can check the topology for any definitions and the relationships for example if you have a crd operator installed you can Define the relationship between the custom resource and the underlying kubernetes native resource and after that the value ux will show the topology graph automatically for you and provides the health status so after we solve that we will usually have the third challenges it is when we have so many extensions in the community how can we discover them so we have developed the catalog for all these extensions we call it add-on an add-on is combined of all these OEM definitions and its back-end capability providers for example if we want a Helm component then we need to define the helm component definition and behind that it may be powered by flag CD operator so the covella addon will combine the helm component definition with the flag CD operator and bundle them as a package and then users can discover the clown in the community registry and reuse them and download and install them very very easy The Click house operator is also the same the community have already built more than 50 add-ons and we also have the quality assurance for these add-ons some of the add-ons are official ones and some of them are experimental ones so as a result the workflow of kubavela can be divided into two parts the first part is prepared by the platform engineer they can enforce the best practices and provide the deployment confidence usually they can install add-ons from the covella community and provide different component types and trades and register them in the covala control plan and finally the platform users which is also called The End user can enjoy a past like experience they can just choose the targeted environment and compose their application by choosing capabilities from different kinds of definitions that prepared by the platform team for example they can choose a web service component and the database component just a few the images replicas and necessary Fields they need to know and finally it's composed as an equivalent application and deployed to the control plan and the covella will handle the rest of the thing it will render the user application along with all these definitions and translate them into the kubernetes API of course it will contain different kinds of custom resources and we also developed something like terraform controller to connect the cloud resources uh in the introduction above we have introduced the abstraction of the open application model but next challenge is the modern application delivery process is very complex it will not only include containers but also need to integrate with the cloud resources and the SAS apis for example in a general workflow maybe we need to run CI in the CI service and gets the image ID and packaging them into effects and then do some Canary deploy after that maybe we want to connect to the accessibility service to check the quality gate and do some conditional check and finally promote the workloads to multi-clouds or multi clusters and after all of them succeed we may trigger a notification to slack or email so this can be a very complex orchestration and integrate with lots of third-party services how can we orchestrate and manage them in a United Way is that possible this is a very big challenge and we have managed it the first thing we have managed is we can naturally Define multi-classed deploy in the model we will treat multi-cluster as first class citizen in kubavela we have extended the policies field in the open application model in in these fields we can Define the topology of the application that defines how we deploy the application in which clusters and how we can override the parameters when we deploy to some specific clusters and we have developed developed a component called class Gateway provide a United Way to integrate with the different multi-cluster technology we will connect the kubernetes clusters by just using their configurations and deploy resources another way is a pro model we will Leverage The Open Class management project for us and the Open Class management will have an agent installed in the cluster and it will connect to the class Gateway in the class the Gateway layer we we will have a United Way to speak to all these kinds of multi-cluster Technologies the second thing is how can we customize all these steps how can we connect these third-party services the solution is the same that we Leverage The Q configuration language as a glue code we call it the workflow step definition and in the definition we we write q1 code and and we have some building operations uh something called Vala slash op and with the with this Opera operations we can allow the resources apply component and do some something like conditional weight and the rest operations speak to the Clusters do something like send emails and connect to the third party services and the workflow of kubera application has the rich capabilities and and process control it can also do the schema checksum in the with the help of the qlan and it also shares a same mechanism with a component definition and trade trade definition it is fully extensible and programmable and reusable so as a result that you can deploy the component a and leverage the underlying kubernetes crd maybe it is a terraform controller and deploy it on different clouds and the component B can be a Helm chart and the componency can be an open Cruise workload and it has some trades that can do auto scaling leverages the cutter project and do some mesh leveraging the E steel projects and it can also do some custom steps with the help of the cube and finally it can do some notifications and the application delivery behind covella is a consistent programmable declarative workflow okay the rest part will be introduced by my colleague Inda hello everyone and Thanksgiving and my name is indah and I am the maintainer of covella and also a senior engineer from Alibaba cloud and next I will go on to discuss about how cubilla handles the day two application management as we mentioned previously covella leveraged the open application model to Define application for users to handle the delivery of kubernetes resources as as we know that's one of the main design principles in kubernetes is the declarative API the declarative API cares for both the day one delivery of resources and the data management and so does covella so covela also follows this principle and makes declarative API for the application so as shown in this slides you can see that there are a wide range of K2 application management capabilities we have been provided in the kubela and it includes like configuration drift prevention automated garbage collection resource sharing Version Control and customized observability and something about this stability and scalability for the control plan so uh later I will give a brief overview for all these features and there are also more advanced features like the security part and you can go to the official documentation site of covella to see more features okay so uh the first thing I would like to introduce is that one of the most important features that makes kubela not only a delivery tool is that it consistently watches the applied resources and make sure that they do not diverge from the declared state so this prevention of configuration trip helps to better users to have a strong control for the underlying resources dispatched by the application so if anyone changes the resources managed by the application will bring it back for example if one of the deployments is applied by the covella application and someone outside changes the deployment's image field and convert application will watch that changes and found that it is not the declared the desired state of the deployment and it will bring back the image field and in this case the uh the kubera will help users to enforce all edits to be made from the application engines and another thing to to mention is that covelop uh although this is the configuration shift prevention is the default behavior for Google's application there are also customized rules for uh covela application users to to declare that we can manually specify some results to allow a configuration chip to some extent for example uh in the HPA scenario you need to let outside controllers to dynamically adjust the replica field of deployment in that case uh users in covala can specify that the replica field of the deployment is mutable so that kubera will keep other fields to be always updated with the disaster but let other other controllers or users to be able to edit some specified fields like other delivery tools covella also handles the garbage collection automatically during the upgrades of applications so I'll use applied resources in new version of applications will be recycled once the application has been successfully upgraded so for example in the first version of Google application we use the deployment X but when we upgrade that application to version 2 it changes to use deployment Y and the deployment X is not being used anymore so when the version 2 or the application successfully entered that new version the old resources will be recycled automatically it is also possible for Google users to customize the strategy for garbage collection for example to let some resource or only be recycled once the application is deleted notes upgraded for example we have seen that there are scenarios that covella users use application to deliver some uh storage for example like the persistent volume claim and this uh this storage since is intended to be kept even if the equivalent application is deleted so at this time it is possible for user to specify which garbage collection strategy should be used for persistent volume claim type resources and also uh another feature for Cuba application is that it is it supports resource sharing across different applications and in some cases that multiple applications need to use the same resource like they need to share the namespace or some config map and they want to say that oh if the resource does not exist I will create it but if it does does exist I will reuse it instead of create a new one so at this time we can use the resource sharing capability into various application to let multiple applications to be able to deploy the same resource and it will support only one writer and multiple readers for that so once the writer goes and the next reader will be the successor for that writer and only the writer will be able to mutate the desired results and and the last one that uses the resource will take charge of recycle it when it is deleted okay another key capability of covalent application is the Version Control so Cuba application keeps a certain amount of revisions for the past application States and users could make compare reasons between different versions and you can see that we have some tools to help users to inspect the changes across different versions like we can see that the image field is changed in this application for the current revision and the last revision and also it is possible for users to declare that uh the new when the new version failed he can roll back to previously succeeded version or just specify some old version as a republishment so that the user can reuse some successful version and quickly manage it to let it to go to a successful stage so that's the in production usage it will be more easier to handle exceptions and so there is the observability as the first class citizen in Cuba system and covella integrates the community projects via add-ons and help users to build application-centric observability like use permissions to collect metrics and use grubner to make dashboard and see how the metrics changes or how logs are passed and okay so we see with the abstraction layer powered by queue as we mentioned in the X definition previously it is also similar easy to code your observation rules as covella definitions and therefore provides out of box infrastructure for developers to use for example in some case you can Define how to collect the the components Matrix and automatically transform them into some aggregated Matrix and then plots them in on graphner dashboard and you can make it as a trip definition to attach to your component so that you do not need to uh to add too much fuels to that trade and your applications component will be able to generate dashboard automatically on your graph now uh besides uh with a large number of companies deploying thousands of Google applications in production environments today the stability and scalability issue are increasingly being discussed among the whole community and the system monitoring rule tools like the dashboard and the metrics provides a comprehensive View for the health needs of the kubernetes control plan and you can see that with only like to enable some simple add-ons in Cuba you will be able to get it out of box monitoring dashboard which can see how your controller works and how many applications are running your system and how much of them are healthy and if there is too much CPU or memory usage for your controllers and also there are a lot of auxiliary dashboards for example you can go to see the latency of your kubernetes kubernetes API server and go to see each deployment how the resource consumption and also you can see the details of ports like the network i o and statistics IO as well so these dashboards and metrics the out of box of the observability tools can help system operators like to see the exceptions and performance bottlenecks are behind the system and beyond it you will be able to uh like to make customized tuning strategy uh and depending on how your metrics exposes to so depending on the different usage there are different tuning strategies for users to use to optimize the equivalence control plan and as for the scalability we have used a load testing of covella to show that it has the capability for both vertical and horizontal scaling so users could not only boost the system capacity by giving more computation resources it is also possible to add multiple charts to the control plan and let each controller to handle a particular part of the application so it will be possible for users to like to make multi-tenant system and let each controller to handle particular tenants applications okay so another benefit of using the horizontal scaling is that it can reduce the explosion radius for unexpected disasters for example if you run multiple controllers on different nodes so if one of the nodes uh like fails and it will only affect the applications currently managed by that controller and other applications and other controllers will not be affected so with this strategy it will be capable for the covela controller to like to make soft isolation for the runtime of the application and limit the uh possible disasters and Designs we have you also use the load testing to show that corvetta is almost agnostic to the number of nodes and clusters in the kubernetes and for on the control plan so we have been testing like it will be possible for one single Google control plan to handle over like 200 thousands applications upon uh hundreds of kubernetes clusters and it will be feasible to do that so we have a lot of production users now only a few of them faces some performance issues and they all have some customized tuning strategy strategy provided by kubela to solve and improve their performance as well and uh the ecosystem of kubera is beyond the simple basic application delivery uh for example the Bella CLI and Bella ux provides different ways for users to access the covers application so like if you would like to use web UI to access your Google's control plan and manage your class manage your kubernetes applications and it will be possible to use the value ux and there are also a lot of uh out of box commands embed in the Bella COI which helps you to to have a very detailed uh View for the application and for the system as well and also we have developed work Zone the value workflow is embeds the basic capability to orchestrate the delivery process and the core engine is used in the equivalent application as we have seen above but it is also possible to set out a separate workflow run to only use it to make some orchestration for your delivery process not only for the application but also for other tools like you can use some trigger to trigger events and let's see walk around to run some daily jobs and it is light weighted and simple to use and also to empower the users to manage Cloud resources and multiple clusters on Google's control plan we have the terraform controller and cluster gateway to help integrate different uh different cloud types of resources on the Google's control plan and also it is possible to integrate with other community projects like open yacht or could Edge to make the edge devices and iot devices accessible from the Google's control plan and also as similarly you can use Google add-ons to like to install some popular popular projects like the Argo CD and also black CD as well and they are already over 50 Community add-ons out of box to use so you can like like to install it without too much customizations by far has been applied across various areas like commercial Banks car manufacturers and Cloud providers and etc etc so some of the users use this covella as an underlying platform to manage multi-cluster resources and some others make customized development based on the community version and provides application platforms for their internal Developers and uh so the community of Cuba nowadays has grown a lot since starting three years ago and there are over 300 contributors participated in the covella community from various countries and we hold bi-weekly Community meetings to welcome open discussions of new features and ideas so uh welcome to the equivalent community and we are looking forward to new participations and your new ideas as well so that's all for my sharing uh thanks everyone