okay so uh hello guys my name is I am a software engineer from daily bricks and today my topic is uh experience with heart multi-tennessee in kubernetes using Color container um so starting from some introduction of Who We Are so database provides a unified and open platform for data and Analytics so we have our own data storage solution which combines the data warehouse and the data Lighthouse as a single concept and we call it lake house and meanwhile many of our services are built on top of spark for data analyzing and we also have our own machine learning products for AI and data model training so you know we're classic infrastructure model we separate our own services and customers infrastructure infrastructure so we integrate with cloud storage compute and Security in our customer's cloud account and we manage and deploy those cloud infrastructures and customers we have and also we always provide services on top of multiple Cloud providers such as like AWS Azure or gcp for example so that's the traditional way that we provide our services and recently one of our revolutionary aspects of our products is moving our services to a serverless mode so what is Thrill is mode uh the meaning of the server list is we migrate all the info infrastructure management to our own account instead of like inside a customer account so that it eliminates that over has to manage those core providers assets from the customer account meanwhile there are a lot of benefits for surveys such as the service provisioning time will be super fast and the service will be super elastic so with serverless we usually can provide our spark cluster to customer in less than five seconds also since we take the ownership of the new fraud management we would have some more flexibility to play with it and lower customer's infrastructure cost so with these serverless mode we want to use kubernetes as our infrastructure infrastructure due to its great portability and accessibility for continuous workflows and the kubernetes is cloud and agnostic and it has a rapid growing ecosystem so we mark it as our number one candidate each customer's workloads will be a single pod or a set of PODS basically a deployment or a demon set in um you know or kubernetes cluster however like one of the main difference with traditional kubernetes cluster is requires the hard money tenancy so what is hormon tendency it means that basically the tenants within a single querness cluster might comes from different companies and they do not trust each other and also the infra infrastructure do not trust the tenant so in this case isolation of the data plane and the control plane are both very critical and meanwhile data breaks also deploys our own service into the same cluster so we differentiate the service into two parts the services deployed by our team our company is a first Party Services which is in trusted group and any pass that is running customers workloads are untrusted so the default security boundary between Niche paths are the container laundry basically in the traditional kubernetes environments and the tricky part is in our environments customer can run arbitrary code like whatever they want one example is like the recent products that we recently released is our machine learning products where a customer can train in their own machine learning models in shared environments with other customers and databricks has no idea like what customer is doing so in this case the container boundary might not be good enough because it has a pretty large attack surface and it's not safe for a hard multi-tenant environment so think about case if there is a malicious user uh that write a program to break out and break out a container what it can do so basically like they can enter basically they can enter other customers container and access their data they can attack the node kernel to affect the services inside other customers pod or they can directly attack the kubernetes control plane for example or they can even like attack the database trusted services basically like after the container breakout they can do whatever they want as a result we need to build additional security boundary around the traditional container and pause to ensure the hard multi-tenancy so one of the solution is using a cloud provider provided services for example like the fargate from AWS however it turns out that those Services cannot 100 fit our requirements for example like some of our spark workloads requests to build a spark cluster with multiple paths right and uh each part needs to connect to each other to serve as a single spark cluster so using the cloud provider Services is not that flexible for us to build our own Logic on top of it as a result we want to seek our own hard multi-tenancy Solution on top of kubernetes so we do some Exploration with multiple directions and one of the direction that we explore to achieve this hard multi-tenancy solution is by using Color container so what is color container the high level description of product container is it is a secure container runtime with lightweight virtual machines that feel and perform light containers with the macro VM it provides stronger workloads isolation using hardware virtualization technology as a second layer of the best instead of like a traditional Criterium boundary which is based on the software solution so the security Advantage provided by cat eye is obvious it is provides a VM boundary instead of a container boundary however like one of the trade-off here is uh when we use a color container we require the club providers VM has the capability for the virtualization basically it means that we can create VMS inside Cloud providers node as a result not all the instance types from different Cloud providers can support that so with the cutter container each part will has its own CPU its own memory and it has its dedicated disks kernel and it is pretty hard for customers to break out a VM boundary so that is for the computation security and meanwhile for the network access control we rely on the kubernetes native solution which is Network policy to ship shape the customer's traffic we also build a simple layer with uh Cloud providers native firewall Solutions such as the network security group it's just for the defense in-depth purpose so this is a single note view after onboarding color container so we use larger machine machines to hold multiple color VMS from different customers each color VM will consume their own compute and storage resources for each machine we also need to reserve some cores for the system and database sources such as kubelight Community for example like there is logging services and our metrics emitting Services Etc so as you can see in the right graph the boundary between each paths are from Caribbean and there are no shared resources between Niche customers as well as with our first Friday services so along with the uh Kara this is the network policy layer that we built so basically a pod can only talk to pause from the same customer so we build the network policy basically disabled network connections between different customers we also disable the access of the kubernetes control plane and we also disable the Pod to talk to any other Cloud providers VMS especially like the open ports of the equivalent from like the nodes in the in the fleet and we also only allow a one-way connection from our trusted services to the customer's part so one additional thing that I want to mention is color Container makes the network policy more secure in multi-tenancy environments so the way that thing I really handles Network policy is by translating the policy rules into either iptable rules or like the ebpf functions and apply those rules onto the host directly however like without kind of container if a customer breaks out a container and earn the host a root privilege it would be pretty easy for them to modify the iptable rules and then bypass the network policy however like after using Color container the VM boundary makes it almost impossible even the customer breaks out its own container it can only access the iptable rules for its own Caribbean the host IB table rules is almost immutable by any processes inside the Caribbean so that's for the uh how we build the network and integrate with zakata so due to the high compatibility between color container and kubernetes onboarding color is pretty simple it's just a special container runtime right so what we did is after installing the Cara artifacts on the Node we just add a special runtime name inside our pulse back and also at the runtime handlers inside our connected configuration and then like in the runtime the community can automatically figure out the right runtime and create the kind of VM as the power sandbox and function net wise it just works but is that good enough for Kara to directly running inside of our production environments and the answer is definitely no I will talk about the main challenges that we explore during weeks for the direction of the color container let's start from the biggest problem which is the performance so after onboarding vanilla we find out that our spark work clothes has 3x to 6X performance Slowdown this means that we definitely cannot directly use a vanilla color inside our production environment so what is the reason that um there's a performance problem why there are slow down so the nature of our spark workflows are both Computing as if memory intensive and I O intensive so by after onboarding Kara it introduced additional virtualization layer and it makes all of these aspects more complex for example our workload will run on another layer of virtual CPU instead of like the CPU on the host when executing instructions the CPU has to jump between kind of guest VM and host VM we call it VM exit and it took time and also we have to rely on the word IO protocol to provide our virtual block devices into color VM so all of these vectors will introduce additional overhead for both the computation and the arrow path so how do we solve this problem let's start from the storage performance tune so spark requires a pretty fast disk for the i o intensive workloads as a result instead of like using a call providers remote disk we are leveraging the local SSD on each machine to support such space so you know our current Passback we have a PVC statement to ask kubernetes to handle this special amount so without kala the vanilla story support for PV and PVC is just the amount that local SSD to a folder on the host namespace and then bind Mount this folder into the container namespace but after using Kata the default way to support such scenario is by using a component called lfs so this complement virtualizes our file system on the host and build a shared process inside the Caribbean as a result any i o happens on either host or gas will be synced in real time so this is similar to like Brian Bond photo inside the container however like the performance is not that good because like during a single IO there are multiple connect switches between host user and kernel space and the hour packet will go through multiple files and layers like one layer is from the gas then we have to go to go through another layer inside a host so these facts will introduce additional latency for every single IO and it also shrinks the total throughput so here the technology that we use to solve the problem is called spdk so uh what is stdk so the full name is called storage performance development kit so it is a open source project which provides a set of tools and Libs for writing high performance scalable user mode storage application so the most advantage that sddk can provide us are first it introduce a polling mode instead of like the traditional mode like creating the system interrupts to trigger the actual i o second it can bypass the Kernel's file system layer on the host and directly talk to the current device driver or like directly talk to the device itself for example like the meme device so with these Technologies the io path can be extremely simplified and the performance improves a lot so the way that we integrate with sbdk data container and kubernetes is by implementing our own CSI so with our own CSI when the new part creation request cards the Google ad will first ask our CSI to prepare the PV for their pod so during this preparation process our CSI will talk to sddk to create the necessary virtual block device the control circuits Etc and then it will utilize the direct quality functionality provided by the color runtime to record such virtual block device for that specific pod and then the coupon will Max call the CRI to create the sandbox and the containers and during the sandbox creation the kalashim will hot plug the virtual block devices directly into the color VM and more on that bind on that into the container namespace so that it the any all the process can see such block devices mounting to a specific folder so that is the whole process like how we iterate SDK inside the Caribbean and the container inside kind of VM so with the integration of the sbdk this is the disk performance we tested we saw that both read and write has significant performance improvements and the SDK disk performance is pretty close to what we have with uh Native disks outside of the color container so besides the storage we also did some exploration for the CPU and memory tuning first for all the CPUs that we got assigned to the color VM we isolated them from the latest scheduling it prevents the scheduler to assign other host processes onto this set of CPUs and also repeat Niche color vm's virtual CPU to a dedicated and isolated core so that every single virtual CPU the process get assigned inside the color VM can statically get assigned to a fixed according on the host so these two tunings can benefit us a lot from both performance perspective and the security perspective it prevents the frequent contact switching between each threads on each single host core so that the CPU can have be more focused on a single color VMS workflows and has a better locality meanwhile it prevents the customer share any computation Resources with each other which further prevents some side Channel attacks for example and meanwhile also we we did some CPU stay tuned including for example like enable the CPU performance mode and tune the CPU power management side option for like lower SQL latencies and another interesting optimization that we explore is about the pneuma control so so I'm basically introduction of what is pneuma so the full name is called non-uniform memory access basically like when cloud provider provides us a large instance type it usually contains multiple physical processors and multiple memory slots some processors is basically closer to some memory slots which can provide the best memory access latency however like when a processor is trying to access a remote memory slot it will has longer latency so a single pneuma node contains processors and memories close with each other and with a large instance type or like environmental machines called provided by the call provider the node really contains multiple numerals as a result like when we try to do what we try to do is to make sure that for every single calendar the CPU and memory resources that assigned to it comes from a single pneuma node so in this case the memory access latency will be short and consistent and meanwhile we are also trying to balance the load between different nominals on the same host for example like in this case we saw that pneuma 0 has two VMS but new model one has only one VM and when there is a new Caravan comes we will make sure make sure the upcoming rvm falls into number one to balance the load so the way that we Implement such scenarios by introducing an unknown services for physical resource management the Resource Management Services is responsible for bookkeeping the physical resource usage metadata it will record which color VM is using which core and which numas memory then every time the color runtime creates a new VM it will first ask this service for a hint and the hint contains a set of course that we want to Ping to this color VM as well as like for example the Numa ID and then the color runtime will use this hint to call the hypervisor with additional parameters in the newly created kind of VM will consume their targeted resource that we specified so for the human control we also explore some kubernetes native Solutions such as like CPU manager or like topology manager but it turns out that they cannot meet all of our requirements for example like for the CPU manager the smooth manager can specify like which color VM use which CPU says by specified inside the C group however it cannot pin each virtual CPU to a dedicated core and also like the topology manager is not compatible with kind of container as well so we finally like decide to implement our own services on load Services instead of like using a native solution so with all the performance improvements uh that I introduced before we improve our workloads end-to-end performance from 3x to 6X slow down to less than five percent slow down so this indicates that performance wise Kata is qualified to support our hard multi-tenancy infrastructure so in our exploration of character container we also Identify some potential risks besides the performance for example like the noise neighbor problem so inside a single node there are still some resources that will be shared by multiple kind of VMS for example like the L3 cache the memory bandwidth and maybe like different partitions from the same disk or like the network bandwidth from the same Nic exact word so when multiple kind of VM for access these shared resources at the same time it is possible that a resource contention will happen and it might cause a performance variation so that's one of the potential risk and another risk is the additional cost of the infrastructure so our original architecture is like assigning a single pod onto a single VM and the VM size is just to fit the size of my Single part but after using Color we have to use large machines available machines and we have to allocate multiple color VMS on top of a single machine so the scheduler by default will balance the load across the node within the kubernetes cluster and as the time goes by sample will come and some some part will go and the node cannot always be fully utilized like the graph are showing below some of the node might have some fragmentations and these fragmentations makes more infrastructure costs from uh or like the SAS company like us so those are some concerns and potential risk that we found during our exploration and there might be some other problems for example like uh when if we onboard a color container into AWS we have to use the variable machine and it turns out that the instance type capacity may not that large enough for the bare metal machine compared with uh like the normal VM and um allocate additional we we also need to allocate a digital resource in the node to cover the virtualization costs for example like after onboarding spdk there are additional CPUs that will be used to busy pulling the iOS and we also need to allocate memorys and CPUs for the hypervisors and some of our product scenario is not well covered by the current color VMS such as like what if we want to do some machine learning data training model with the GPU we have to device password do the device password for the GPU inside a card at the end so those scenario is not well supported apparently in a kind of Upstream so but well it's a conclusion then so the conclusion is the color container is a great project to support the hard multitudency equivalents it provides the VM boundaries of a container boundary and make the container make the customer hard to break out again break out a container and by fine-tuning the performance color container can reach similar performance level to Native technology however on the other side color container brings its own complexity if we want to use it in production some additional efforts is required to make the infrastructure as consistent performance and cost efficient so that's all for today's share and finally I will do a short advertisement so databrick server list is now in public preview for both ALS and azure and we were short goes to GA and welcome to shoot a trip our products and it's super amazing so that's it for today thanks for attending the talk