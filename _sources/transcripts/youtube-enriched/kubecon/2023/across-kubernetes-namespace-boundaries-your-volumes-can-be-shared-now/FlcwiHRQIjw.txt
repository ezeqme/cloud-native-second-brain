hello everyone thank you for coming today we are pleased to have the opportunity to make this presentation our topic for today is across kubernetes namespace boundaries your volume can be seen enough okay let's begin let me introduce my first my name is I have been contributing to the kubernetes community by implementing prohibition volume from close namespace snapshot my name is masaki Kimura I work for statue I have been contributing kubernetes Community to make grab Rock bottling feature and CSI feature GA and I designed and propose a cap that takahumi is implementing today we'll talk about the feature in the cap in detail just a disclaimer before we go any further this session is explained the specifications designs and implementations which are under discussion and development depending on the discussion they are subject to change our talk is divided into six parts first I will explain the existing kubernetes Concepts and features related to the issue that we are trying to solve then I will share the issue use cases and discussions around this feature after that takafumi will take over explain the current design and implementation show demos and then conclude okay let's begin with the existing concept and features related to the issue the first concept is namespace in kubernetes namespaces provide a mechanism for isolating groups of resources within a single cluster namespaces separate a cluster virtually for multiple users to share resources without the interference for example as described in the diagram Resources with the same name like part one can be created by multiple users if the namespaces are different like ns1 and ns2 and if their artwork is set properly we can make that only user run can access to the port one in the ns1 to prevent other users like user 2 from accessing to the pod the point here is that namespace is used as a security boundary to prevent Mauritius users from accessing to the resource in a namespace there is one more point that I must refer to not every resource is namespaced kubernetes resource consists of namespace resource and crosstalk resource namespace resource are usually consumed by normal users examples are Port deployment service and so on on the other hand Crest scope resource are usually managed by admins so normal users won't have permission to modify such resources examples node Street crust persistent volume and so on then I will explain storage related features from our namespace viewpoint let's begin with PVC and PV in kubernetes volumes are managed through persistent volume claim PVC and persistent volume PV PVC is a namespace resource that users create and PV is a crystal scope resource that admins or provisional create and manage our user create a PVC for specifying their access mode volume size and so on then a corresponding PV that meets the specification is created and bound to the PVC so that the user can use the volume through the PVC because PVCs are managed namespace resource and uses access to the volume through PVCs volumes can only be accessed by users with the right permission let's see the detail behaves behind the scenes once a PVC is created by a user first a CSI provisioner detected a PVC is created then a provisioner called CSI create volume method to the CSI plugin third the program creates an actual volume in the backend storage first the provisioner creates a PV and set a reference to the actual volume after that by Direction binding is created between PVC and the PV by kubernetes the volumes becomes ready to be consumed by users through the PVC next feature is volume snapshot kubernetes has a feature to take a snapshot of a volume in a simulated volume snapshots are managed through burning snapshot vs and bottom snapshot content VSC Fierce is namespace resource that users create and VSC is a cross-scope resource that's snapshooter create and manages a user creates a previous with specifying which PVC to take a snapshot from then a corresponding p with for a correspondence sorry vs VSC that means the specification is created because previous are namespaced resources and the user access to the snapshot through vs or certain snapshot can't be exited from other namespaces let's see the detailed behaviors behind the scenes once a Prius is created by a user first or CSA snapshot detects that a pref is created second the snapshotter called csri create snapshot called to our CSI plugin third the plugin creates an actual snapshot in the Bracken storage fourth the snapshooter creates a free SC and set the reference to the actual snapshot after that by directional binding is created between vs and VSC for snapshooter the snapshot become ready to be consumed by users through previous the rest feature is creating a PVC from a 3DS or PVC just taking a snapshot won't be useful we need to create a value from the snapshot to actually consume it also we can create a volume from an existing body for users to specify which vs or PVC to create a volume from users need to specify vs or PVC in the data source field in the PVC as shown in the yaml file an important Point here is that namespace can be specified because there is no namespace field in data source field so data source need to be in the same namespace let's see the digital behaviors behind the scenes the difference from the empty problem creation is marked in red first a CSI provisional detects that a PVC specifying data source is created second the provisioner called CSI create volume method with specifying foreign content service to the CSI plugin third the plugin creates an actual volume by copying data from the actual snapshot in the backend storage other steps are the Same by using this method the data is copied to the newly created volume but the data source needs to be in the same namespace or the related features are explained let's move on to the issues and use cases as explained a PVC can be created from an existing previous or PVC in a different namespace it is simply because namespaces can be specified in the data source field however the reason behind this specification is that just around to copy any problems in other namespace causes a security issue namespace should be your security boundary to prevent Mauritius users from accessing to namespace resources on the other hand there are some use cases that requires passing data Beyond namespace boundaries let me explain two typical use cases in the next slide the first use case is copying data from production namespace to a development namespace as described in the diagram let's assume that there is every 1.0 pod which is writing data to a PVC in the product namespace if a developer is developing every 1.1 in the dev name space and would write the test with the actual data copying data from prod to Dev is required the Second Use case is using guarding image in one name space to from multiple other namespaces the most common use example would be VM image for keyword the same Garden image are expected to be used from multiple namespaces so putting the same theater in each namespace will be inefficient therefore Garden image are expected to be shared across namespaces before sharing details on how we are solving this issue let me share a history of discussion around this issue this issue and use cases are not nuanced actually they have been discussed since four users opened five different caps to continue to try solving this issue I really would like to say thank you to all the people involved let's quickly share the initial approach to solve this issue and challenges and continue using this approach the initial approach was transfer of PVC or BS by rebinding actually if you are an admin and have a permission to modify the PV you can manually transfer PVC by using the steps described in the diagram in this sense this approach ultimates the way that crusty admins can do for users to transfer PVC one in the ns1 to the ns2 the manual steps are first create a new PVC in the ns2 sort of binding by binding a new PVC and the PV and unbinding the original PVC and the PV third there is the original PVC if you need a copy of a PVC you can clone the PVC in the same namespace after transfer as a fourth step to make normal users achieve the same operation with a security issue we plan to define a request approvers type API and make a controller transfer volumes only when there is an agreement on transfer between namespaces after discussions this approach wasn't chosen so let me quickly explain the challenges for implement this approach in the next slide there are two big challenges the first challenge was that it is difficult to lower back if errors occur or changes happen during the transfer process in this approach five resources are involved on the adjustment changing by Direction binding is not an atomic operation so it is difficult to keep the status of all the resource consistent the Second Challenge was that it is difficult to handle Secrets referenced by PVC or vs in some CSS drivers secrets that are referenced from PVCs are used to process operations in the backend streets a secret that is assumed to be in the same namespace cannot be transferred as a result such a PVC may cause an issue due to the lack of Secrets now let me hand it over to takafumi in this part I will talk about current design and implementation current design utilize two features that were recently added it for another purpose first for existential data source we utilize any volume data source feature second for granting close namespace we utilize reference Grant API first I will talk about extension of data source to existent data source we utilize any volume data sort feature as as we can see in rest diagram existing data source field only around specified part system volume current and volume snapshot so if other resource system PVC and Bs is specified it was regarding regarded on inverse data source however Sig storage found a use case that user defines crdf needs to be specified as a data sources so any volume data feature is additive any volume data thought feature allows any resources to be specially be a newly added data source rate field to maintain compatibility behavior for special build data source Fields remain unchanged this feature is as itself alerts only the same namespace to be specific as a data source so it doesn't directly extend the field for our purpose but it opened their way to extend the existing field keeping compatibility to allow special namespace as a data source of a persistent volume claim named fifth Alpha skill is additive to data source left for compatibility purpose existing data soft field doesn't accept namespace therefore users are required to pass namespace via data source ref field next I will talk about reference Grant reference Grant is added in Gateway API it is used to allow Gateway API to access to the resource across namespaces a resources owner creates a reference Grant to accessibility around that reference for example I'll show you a diagram to allow access from ACP root in the ns2 to a service area service in the next one the service owner Creator reference Grant in the ns1 in the reference current HTTP loot should be fed at the kind in the front section and service should be fed the kind in the two section also the service owner should specify the namespace in the front section controllers that Implement Gateway API check the reference Grant if the access is granted avoid Mauritius access in this slide I will talk about how to Grant growth name space access by utilizing reference grants reference Grant was originally for Gateway API however we decided to extend it for our use cases in our use case what we need to do is to allow access from PVC to BF in another namespace so for example as showing a diagram to arrive from a PVC in the ns2s in the NFL the biggest owner created reference Grant in the reference current persistent volume gram should be fed as a kind in the front section and volume snapshot should be fit as a kind in the tooth fixture also the previous owner should be special either named beef in the from section to achieve it we need to implement a logic to CSI provisional to check if the access is granted by reference Grant note that we kubernetes Community Fund generic use kit for uterine reference Grant so reference Grant is planning to move to New sigos API Group in kubernetes 1.28 with some enhancement it is currently being handling as a closet work with Gateway API sigos and six storage thank you for all the people involved next readme hub let me explain how this feature works by using examples first I will talk about the behavior from user Viewpoint red assumed that a user won't promise on the PBC one in the NST from the previous one in the NF1 the owner of data source must run such an access so the biggest ones owner create the reference Grant in the ns1 in the reference Grant persistent volume increase should be fed at the kind in the wrong section and Podium snapshots should be fit as a kind in the two section then the user can probes on the PVC one from the previous one by specially in the previous one in the data source review note that namespace is special about the existent Alpha field by this way if close namespace volume data source feature is enabled in the cluster users will be able to approach jump volume from data soft in another namespace I will talk about details CSF provisional Behavior behind the scenes CSI provisional behavior is divided into five steps first a CSA provisioner detects that the PVC spacing a data source in a different namespace is created second a CSF provisional confirms that the data source is allowed to access via the reference current in the ns1 is not allowed to access the special data source the professional will output an error and the body will not be created if she is a provisioner is allowed to access special data source it will proceed to the next step other steps are the same as existing Behavior so no change is required in the CSA plugin to use this feature next I'll talk about development status this feature became Alpha in kubernetes 1.26 we plan to move to the feature to better in kubernetes 1.29 after reference Grant become better in singles and maybe TA in kubernetes 1.3031 the scope is only provision of PVC from close namespace data source not include transfer of PVC or foreign snapshot currently supported data source is volume snapshot and persistent Emporium cream in this feature we plan to support any volume data source and feature get name is cross names with volume data source if you want to use this feature if you need to enable this feature in both kubernetes and CSI external provision next I will show you a demo in our demo we will use kubernetes 1.260 and external provisional 3.4.0 we will deploy WordPress input with empty plugins take a snapshot and deploy another WordPress in table World Dev from the snapshots we divided this demo into six steps and I will explain each step with the diagram we will make sure that the most environment is configured as expected there are three point first extent version of kubernetes and external provisional deployed is a feature get enabled second the prod and the name species are created third Link spot is deployed in the default namespace for accessing the WordPress now Refugee demo first let's make sure that it's a deployed its expected version of kubernetes and cross-native volume data source feature get is enabled next let's make sure that the CSI driver is deployed and cross names the volume data feature get is enabled on the external provisional and let's make sure that the prod and the namespaces are created and no no unnecessary resources are deployed in each finally let's make sure that link is deployed in the default namespace we can confirm that it is deployed as expected now let's follow the demo scenario the first step deploy products in the product namespace by using the step describing kubernetes official document both while the press and my skin have a deep have a deployment that manages one port and it did Active Space service and has one PVC attached to it also note that the PVC are empty when deployed now refit demo beef and overlay are defined as a customized templates and WordPress and my scale are defined to be deployed is the same definition in the prod and the namespace the only difference between deep and plot in the best file is just the namespace field is defined as plot and we don't don't special data Thought Field in the PVC definition because it is Created from an empty volume we should check the PVC definition file and confirm that the digital Source field was not specified after that let's deployment in the prod after the deployment let's wait a minute and check the port status speaker running and that they have started normally the second step is accessing the prod WordPress this step right right data to the PVCs by accessing to the WordPress on first time access the user will see the rest web page which prompts the creation of a WordPress on second time access the user will see the right way page now residue demo first connect the product breath using silver links so log into the ring spot then connect to the product WordPress site we are connecting the product WordPress for the first time so need to feel required information for example is a site title cubicle Europe 2023 after that install WordPress and complete the initial setup first step is taking a snapshot this step take a volume Snapshot from persistent volume correct for both WordPress and MySQL in the Pro namespace now refi demo create a volume snapshot Jam for HD take a snapshot and Define its persistent boiling current information Etc and deploy red with a minute check with the volume snapshot was taken successfully fourth step is creating reference grants this step creates a reference grant for allowing access to the snapshot in the front name speed from the PVC in the div namespace now graffiti demo in the reference Grant confirmed that persistent volume currently affected the kind in the front section and volume snapshot filter the kind in the two section after that confirm that the reference current young was deployed and created successfully fifth step is creating a deep volume from broad snapshot and using theme this step deploys about press in the Deep name piece and pvcip used from WordPress are provisional from the snapshot in the prod namespace therefore PVCs are created by special snapshot in the data source left field demo first check the yaml file in order to deploy Devon space in the dev namespace we should specially support volume snapshot in the data soft black field confirmed that David WordPress and the MySQL definition file are correctly special persistent volume claim data source refill it could be it could be confirmed that follow the volume snapshot was special in Daily WordPress after that let's deploy debutant press in the table namespace after the deployment rate with the minute and check its support status became running and that they have studied normally the sixth step is access to W1 press and checking data the data is copied from Pro to them so if we access to the table one place we should see Quebec on Europe 2023 instead of the initialization page now graffiti demo foreign press now we now we see click on Europe 2023 other result it could be confirmed that the WordPress was able to use production data in another namespace finally in conclusion there are two issues in kubernetes 1.25 or prior First Data in production name effect can be copied to development namespace for testing second golden image in one name fifth can be shared can can't be shared from other namespaces as a revolution you need to use kubernetes 1.26 overrated p and PVCs can be provisioned from PVCs or snapshot in a different name Space by using close name fifth volume data field feature for cluster admin to enable the feature close name fifth volume data source feature get is required to be enabled for both CSI provisional and kubernetes controllers for users to utilize this feature owner of data source needs to create a reference Grant to allow accessing a data source from a PVC then a user in another namespace can create a PVC by speeching a data source Refuge with namespace finally it is important for us that we want YouTube to write close negative volume data feature and give your feedback on this feature to six storage please thank you so much for your kind attention foreign