hello everyone I'm Tommy thank you for joining these sessions I'm the senior software developer at IBM working in the open technology teams and today we're going uh to go over the complexity on scaling Mii pipelines on C using tons so to begin with let's talk about what is ton pipelines so ton pipelines is a project that provides CID style resource for declaring cicd style pipelines and it's mainly you know construct with you know few main crds namely task pipeline task run pipeline run and custom runs um these are three you know major CD that we use within P ton pipelines um for example the pipeline run it defines the execution of the pipeline itself that compos multiple tasks a task run that defines the execution of a task which is a a set of build steps and also we also have a custom runs that allows you to instantiate and execute Uh custom task which is able to implement with you know uh any custom Cass controller which you could Define your own um task Logics and why want to use ton pipeline to build our ml infrastructure well the main reason is that like we are need to build our ml infrastructure on top of open shift and open shift container platform is the industry leaders uh is the industry leading Enterprise ku8 platform which it brings out the box of many featur for developer including CICS uh and the cicd for open ship um platform is open ship pipelines and open ship pipeline is based onch te ton project which offers native Integrations with the open share platforms and provides smooth experience for the developers and out of the box uh open shift peline also certified by red hat for open shift and also have Enterprise version a available on open shift as well and that's why we have this is the main reason want to run you know secure pipelines for ML and AI on open shift and this is the option we have chosen and when we actually look into what techn have out the box we see a lot of the good things so techon will able to help us run anable floats a to construct and create new parts for each of the task that us have defined able to have like condition that um uh able to skip task when the condition you know is not match and able to pass PR very easily so any of the inputs and output can be passed in between multiple parts and it has like um API to connect you know custom controller so you want to have your custom Logic for your task you could also do that and were able to optimize you know workflow workflow very easily on the controller levels and able to provide abstract the template which it help us when we deploy the pipeline on different environments it's easy to have a different configuration settings that defined on the pipeline levels and as we kind of build you know um ml pip on top tectons we see like this F feature we want to have it in tecton and our IBM team actually work with the T Community to accomplish these kind of features so uh first of all like we have the T finally it would help us you know like Define any like arror handling cleanups if the pipeline is finished or failed and we also have like very standard API um definitions and have a lot of way you could abstract the spec so you could actually Define a global or clusterwise specs that able to share across model pipelines or share across all your test inside the same pipelines and we also have ton workspace which it help you to define the common you know volume across all your pipelines and you could also Define volume within the same pipeline so all the task share the same you know workspace definitions and of course we have the uh ton custom task which allow you to have any custom logic that is not provided by the um ton pipeline out the box and we also have different termination Logics where if you want to cancel pipeline in machine learning sometimes you want to just get rid of all the resource that you're running to just free up all the resource we have that Logics but you can also do the traditional cicd way where you want you know the current running task to complete first then you exe uh exit the pipeline you can also do that so there's multiple way you could terminate you know ton depending on your need and of course um with machine learning we have a lot of parameters so having different like Matrix and able to Loop over those Matrix is very helpful and with the new um ton release it actually introduce a new common expression language um conditions so now not you not only able to you know have condition just do like string matching you go like um have simple common expression language to do the conditions on the fly so that's very helpful with all these fantastic features on tons um we're able to build you know um very good workflows right using tons basically just create a pipeline object you know to the G control plan it creates the um pipeline CD and then with in the pipeline CD we create you know Tas crd that basically just construct with parts and all those part will run you know each of those step which is a uh container by itself and this is the Fantastic you know cicd platform and we're able to use them to you know build like simple you know uh ml pipelines in this case but we see the limitation when we try to scale and give more complex ml pipelines um so some of the limitation we found at the very beginning is that like ton we have no caching out the box it makes sense because it's actually just a controller handle workflow there's no stor for uh store and cash and there's no good you know uh python SDK capability let's say for data scientists it's very difficult for them to you know compose a pipeline just using yo so it's good to have a python SDK to help them compose pipelines and with this know like all the Box garbage collection so as you run more experiments uh those problem actually you know leave on kubernetes which kind of was a lot you know this resource in our case and there's no lock archival so when you actually try to clean up there's no way you could you know retain all those locks and of course um when you have more complex pipeline you might want to share you know pipeline parameter across model pipelines um currently there's no good out the box way to do on tectons so we see that as a limitation where we have to have all the pipeline that compos in single taton pipelines which limit our scale abilities um and that comes to the next um line where you compost everything in one single pipelines with the kuet unit XD store you kind of limit on let's say 1.5 megabyte on your pipeline definition spec which limit the scale of the uh pipeline size itself and lastly because um ml pipelines right you need to understand what kind of inputs and output what kind of artif produce um so we need some tot of metadata to track all those information so data scientist could easily go back and understand what they actually you know run and produce within that pipelines and because of that we actually load into like all the different you know open source projects and we found like ke for pipelines and ke for pipeline is actually aimed for um data scientist to run any runtime any framework any data types so for example you want to just you know trade a models using pytorch or using python to do like promt turning you could easily do on K for pel as well there's no limitation on uh any library that blocking you um and it also provides you know very easy interface for data scientists um so for examples right it has the python DSL so the scientist could Define their container spec or their Workforce spec um using python itself and because with a native python it's easy to just pass input and outputs between you know different python function and it will just translate into um this input output and condition to ton directly and then um in addition why because we also have looping capabilities um uh user could able to Define programmatic Concepts and those concept easy translate into like C native pipelines um such as parallel loops and in addition to that P pipeline provides you know uh multiple layers of optimizations so first of all um the runtime of P actually leverage any cicd cicd runtime you have provides so at the very beginning uh GI for power was built on AOS before ton was introduced and our team see that ton is very useful for building M pipelines we actually introduce a new runtime to K for pipelines for tectons and with um k for pipelines right you're able to like provide us a common storage for storing experiment and data metadata tracking so uh user could able to track all the experiment um and any kind of like inputs and output also being stored in this case and once the pipeline is finished right we also have like garbage cleanup and garbage collections where we able to archive all the um pipeline history into a myle database so we able to create clean up all the uh space from kubernetes but you could still have all the information that store in the database for analysis and F future reference as well and on top of that we also add like um caching capabilities so uh anything you have ran before and um you have no like um you want to like just cash the steps and not waste resource um once let's say your p is fa you just re want it and you want to cash out the previous step you can also do that with k for Pipelines and with this you can see like G pipeline provide a good way to clean up all your um old pipelines in the um kuber control plane and provide a good way to you know cache um your workloads uh inside the existing tecton pipelines and what we have implements you know kful Pipeline on ton V1 is more like an extension of the existing ton features so uh we are not we are really not modifying the ton pel itself too much if we are mostly just do mapping you know the U user pipeline definition pretty much one to one to ton pipelines but adding you know additional optimization features um so we have see that like um T garbage collection so it that helps just reduce given any um HD dis size you know provide a python dsls and API server so any user request could go to like the um Q for p API server and reduce the number of qu uh user need to do uh to the kuber API directly and uh because we actually want to do like cashing without modifying the ton pipeline itself in the first version we actually using part mutation for caching which uh even uh with Al for p you could leverage this feature with Native ton just by adding two annotations um so there's no impact to the ton controller Logics and um at the very first uh version when we uh Implement ton backends um to K4 pipelines there's no um you know common expression language and P Loops but we can easily extend those feature using custom test controller that's how we actually leverage custom Tas controll at the beginning and um f f also provide ways uh common storage for you to store the common artifacts and we also take that advantage and store all our logs Al our logs in that com storage as well and with the Pyon dsls it's easy for G scientist to produce any helper function and let other data scientists will use so it's easy to produce common helper function and able to compose you know multiple common function into one simple Q for pent task um for example we have like user that um produce logic that wait for all the other file to be ready then execute that could be a simple python function that defines in Q for Pipeline and then in the compiler code because it's commonly used we could just take the same Logics and apply to other pipeline that uh other user have been using and finally it also produce um provides a very simple preliminary metadata tracking so it actually gives you an idea what kind of data is actually thrown in and what C data is been consumed for each steps with all this awesome features uh we still see this limitation on the scalabilities namely um when we do the caching um because we don't want to interfere the tecton controller logic but from the beginning um the caching only can be done for part mutation which we still have to like um schedule the part we still have to create the part construct the part that is the big bottle next and because our power user actually have like tens of thousands of tasks in each pipelines um with tens of thousands of tasks even though you try to like cash them you still have tens of thousands apart so that actually bottom next our uh schedule as well for allocating resources and because of the um um big size of pipelines um we are not able to store you know anything that's more than let's say 2,000 TS inside a single pipeline because of the size of XDS and as we actually put all these Tas togethers um by by the nature of tectons you want to break tecton into multiple pipelines and just have them all connected together and passing problem between those pipelines it gets very complex and when you try to compose that complex pipelines traversing that graph and you know validating that graph and passing PR around that graph is very difficult um so this is why we kind of see um even just to Traverse like a very good big graph in ton we see bottle next doing the scheduling aspects so this is why we kind of move into a new design of kful pipeline called K4 pipeline V2s so instead of you know having kful pipeline to be a small compilers that Maps you know um user um Define the Python's uh definition one to one to tectons we actually just have a smart runtime where um the KP DSL KP SDK or whatever your local no code uh interface that you compose the pipeline or compile into a intermediate representations and behind the scenes this intermediate representation have all the uh lot all the uh graph definitions all the uh requirements for composing the pipelines and let the backend itself to optimally just uh map their own subset of the pipelines to this uh complex pipeline itself so U from a tecton perspective you could have multiple tecton pipelines to compos one you know uh complex user intermediate repr representations so from a user perspective you could still see the pipeline be represent very complex but behind the scene it would break into multiple pieces so tecton will not have the bottle neck of like sculpturing a big you know graph and Traverse a big graph and to achieve this uh we actually introduced you know uh New Concepts called driver executed Publishers so basically um in the drivers right so we actually uh storing you know uh PR Dex and contacts in a driver so it knows uh okay which note is this execution supposed to do um and then once the execution finished uh there will be a publisher that uploads the outputs artifacts and Status um to our common you know um metad service called mlmd so so all the pipelines no matter which pipeline you use could all you know share the same um parameter metadata artifacts Statics using just this mlmd metadata servers um and this is very powerful because this means we actually could break down ton into multiple smaller pipelines and reduce you know the complexity of the graph itself um from the runtime representations and uh with the Q4 P V2 another um enhancement we have done is uh because we have uh Brun time for Aros and ton as well we also create a abstract interface for future runtime so let's say you want to bring in airflow it would be very easy for you to just uh add these three different components one is a compiler convert you know the Q for peline intermediate representation into um the representation you want run time to run and then you have those you know uh files right like you could convert uh your P into model pipelines and when you run it you have to just create a execution clients to run those you know resources you have produce and then you also have execution specs that's where it helps you to um modify it um your underlaying runtime specs when you actually need to say update parameters or update um a small subset of the specs and um when we kind of dive in into what k for power V2 actually provides um we could see the driver publisher model actually um separate into like kind of two categories so when we actually say driver and publisher on the you know uh graph level the direct exec graph levels you could see the driver actually just produce the parameter artifacts for the parent contents and then see whether or not um our sub pipeline then because user could also uh produce multiple sub pipelines we could see whether or not this supply line has been execut and we can we cach it if so we could actually SK the whole subd and just reduce the complexity of the graph itself and then once the graph is finished right the publisher could just takes the information um from the sub line have produced and just publish it back to the um metad service and then on the task level when you actually won't need to run a contain 't run a part for that particular task um the same driver and execution logic exist so the driver I check whether or not this Tas has been executed before uh fet all the parameters um compu the conditions and then if the cach is exist then actually we just skip the execution part if not then you uh it will just create execution with the publisher embedded so in this case the publisher is actually a binary inside the user um part itself so we are not adding you know any extra step we're not adding like um extra containers that um uh reduce you know the user run times and once the user you know execution is finished we could see like from the publisher aspects um um before the um user action is uh created uh the publisher main job is actually to put all the artifact in needs for the user task to run and then we place all those parameter with the necessary um parameter from the MMD then run the user execution commands and once the execution command is finished then we grab any other effect that needed to be upload upload to the uh um uh object storage that's designated and then publish you know all the metadata that's related where the artifact is stored and what the parameter is being produced back to you know the MMD medata servers so this way we don't have to rely on kuber API at all to you know s all those status um you know know where the parameter is been um located uh all this information actually store in MMD Med server so uh we actually could reduce the bottom net from the city control plan itself and from a very high levels um when ke4 pipelines um on ton first designed it uh we have all these kind of driver publisher task um actually is a task in tons and as we can see like this all bottom next when we create a lot of task inside a single yo and when ton schedule like a lot of task it actually create a lot of bottle next on the schedule on the ton schedule itself and this is why we as we kind of improve over time we try to merge all those driver Logics inside our um task controller and P controller itself so our our current design is actually able to merge um the driver and publisher logic into our task controller itself so the C task controller could just go ahead and evaluate whether or not uh this cast this task is been cached and also evaluate all the conditions and if if there's a need you uh you need to run a part you could actually run a part to to accomplish a task but we're also aware that for M workflow sometime you might need like distributed trainings or let's say you want to run a ray workflows we we could just simply just create a ray crd so you don't have to have a part that's dedicated to Runner clients uh we could just create a c for you and um the r the r controller will just be aware the C and run your R drops so it's very easy to integrate and reduce the number of part in integrate in these scenarios um so to summarize um you know K4 p on ton V2 it brings the way how you run you know uh custom task to handle caching skipping conditions and handling parameters all in one place and we also have the publisher binary you know run along with the user codes so able to upload all the user task parameter into the ml metadata service so actually able to you know reduce the uh limitation from tons as you know ton parameter actually pass via the um C the yo itself so as you put a lot of you know parameter inside ton you will have a limit on you know how big your parameters but with this new approach um the uh the publish P just push the parameter to the MF metadata servers which is backed by um current in the open source MySQL or postare SQL database so your limit is much higher uh in this scenarios and lastly we also you know publish a uh also upload all the problem status and graph structures all in M metadata service so we actually don't have to retain you know the the graph structure in the uh pipeline uh the tecton definition and on C itself so as we actually executing the pipelines we can actually clean up the pipelines um because all the information will be uploaded as you know the subset of the pipeline is being complete and with this uh enhancement we could see that uh we able address the po you know creation issues um when we do the caching so all the cach right now you don't need to you know create a new part so it would be very efficient from a cach pers perspective because of the pipeline could be you know decouples into uh very small sub pipelines graph traversal is very simple with in ton because you only see like subset of the pipeline it doesn't have to understand the whole complexity of the gigantic ml pip floats um and all those information are stored in MMD so MMD you can see it stores all the par and P status and it also um dramatically reduce the limitation from XDS so you can actually break into a lot of um smaller Pipeline and store into xcds right um only small limitation we still see is that um as our power users still have tens of thousands of tasks um each of those tasks still represents by custom resource on kubernetes so as the number of P grow we could still see some limitation on XDS but this is kind of similar to the concept of checkpoints from machine learning where um all each our test is actually we quot those status um in the persistent volumes um so we we could just actually um have the machine learning pipeline to be more condensed and for any parameters any status doesn't need to be store in um persistent um volumes we could um maybe just have a more advanced compilers to combine those Logics and only pass those parameter in memory in instead of you know swing all those information in MMD which you know reduce our um run times um so this is kind of a future enhancement we're planning to do um and with that we're going to show you a quick demos on how K ton 2.0 is been accomplished you could see we're using you know the same SDK UI no matter you're using AO um tecton back end and currently with the G pipeline 2.0 the implementation have't optimized the backend to run driver tasks um using HTTP templat so all the driver Tas on Argo currently still running on part but the committee is working on uh converting the aggo back and also to use um more like um long running server approach which it will significantly able to reduce the caching time as you can see without that um ago has to take you know like at least create three or four additional part which dramatically uh increase the duration where in in t time we could simply just run a cach pipeline within two seconds so with this I'm going to show the demot so uh when you go into the Q4 pipeline interface you could see this a pipeline you have created um once you click on this uh you could actually see you know uh we also have versioning for the pipelines you could um pick different versions in this case I would just um first run a version where uh we have all the um tasks um being cached so let's just create a simple run um so when the p is being cached you can see it could uh execute it uh fairly simply um over here and complet it very fast so should be running in seconds yes so once the you know pipeline is being scheduled everything is just pop out instantly um then and as we kind of go back um we can see I think the startup times for the controller itself takes a bit a little bit so it it's been a second but it's still like very fast um in the scenarios um and the fastest usually uh in ideal environment you could see it could gets down to like two seconds when you run this pipelines um and when we actually get the pment Run itself you could see Department actually completes you know within a second even in the uh ton um U resource perspective and you can see we actually run stuff in this pipeline so we actually run at least four tasks to evaluate um the context of the graph and then also evaluate whether or not we have the cach hit and all these four Tas is complete you know within like seconds and with q for p is very easy for you to just pick uh which task um been cached so let's do a scenario let's say we are not caching the training step because um you know sometime training you have like Randomness uh in this case so we might not want to uh cat this so you could just like run the same step multiple times but um because the preprocessing is always the same because you have same data coming in and you do the same transformation so your output is always going to be the same um so in this scenario you could see you know we could just cach the pre-processing part and then just take the same environment um that produced um by by the cast outputs and do the trainings so it should see yeah it will able to grab the artifacts right from the cach um output and then do the training it actually running the part do trainings um and you could see from the output itself could show you um the artifact produce in this case just simple message um artifa it produce and then and once it produced you can see this is the new model in being uploaded to um the Min storage so it's very easy to you know navigate in this case and you could simply just decide which test you want to cach and which test uh you want to always run so with this uh I complete my demo so we want to talk about some of the future optimization we're going to do um with our current designs um because our current designs kind of rely on these driver and publisher models and our initial implementation actually just um connecting all the roof node um behind the driver uh test and uh connect all the leaf noes uh um connect the publisher after all the leaf notes it actually creates a extra layer of complexity when we construct the pipeline s so the next pH we actually try to have our controller to handles all the you know like driver and publisher logic itself as well and only let ton handles the core um pipeline execution so um this way we actually retain the complex U retain the same you know p uh tecton structures but able to you know add those uh extra capability to do caching upload status right to the med service in the controller itself um and as from the community size um we're actually working on a more mature St IR so we were able to you know upload um the graph level status more mature as well so this is why kind of uh we have some delays on you know migrating this uh approach to the new you know controller uh all Handle by controller approach and then I think lastly we want to Auto optimize the looping crd where we see looping is basically repeatingly execute the same um task multiple times so we actually want to enhance the uh Lum capability to have a uh all the looping task to run in a long running server and just input different prameters different sets uh of inputs um so we actually able to reduce the number of Tas number of resource um that is redundant in the P itself um so with that um it completes um our talk today so here are the links to uh the ton pipeline project Cy ton project for all the optimizations open shift pipeline for all of you want to run you know this project on open shift and feel free to just um come to our slack Channel have you have any questions and you wondering how this open source project is Implement in our product also check out Wason X which is also compos all the open source Technologies in our product itself thank you very much any questions any question in the audience if not I could take it offline as well thank you very much