yeah so today uh um you're going to present the case of uh the state and the future of the uh Cloud native model serving um so my name is Dan uh I lead the Emir inference team at Bloomberg and Nick is uh um with IBM Nikki want to introduce yourself um hi yeah I um I work for IBM in Watson Core Group um we uh maintain sort of common ml infrastructure components for different products and uh young contribute to queso cool um yeah so uh the case of was our main primary Creed to solve the challenging model deployment issue online kubernetes so deploying models on kubernetes uh it's not a very difficulty problem uh so um user have to learn like model theorizations and how to build model servers and uh understand the HTTP grpc protocol how to continualize the model server uh and you need to know all the kubernetes concepts like deployment service like how do you like Auto scale your like art departments with HPA vpa or like um kpia and in order to keep the uh the service um here also you want to know like set up like a written is your driving is props and then all these like in order to secure service you need to understand like a service mesh and uh um open the time like um nowadays like the models are getting bigger and bigger and over the time they're serving the models and CPUs are no longer can set for your latency requirements so you'll need to reserve your models on gpus to reduce latency so um so as you can see like a deploying models on like homeless it has a um you need to learn a lot and besides your data science like a skill set um so um all so cases like we initially create a caser mainly to address and this uh these challenges like we want to like make the deployment really easy and um we just want to if user can give us a model then we can easily turn it into a HTTP endpoint user can call easily to encapsulate all these like infrastructure details um so yeah so case server is a highly scalable and a standards based serverless model inference platform um on kubernetes um that encompasses all the complexity um deploying models to production and now the project um Personnel is now hosted in as an incubation project in Fai data Foundation um and so we moved to the Fai early this year um yeah so um the case of can be deployed as a standalone or you can also deploy case server as an add-on component with Q flow on any cloud or on-prem um so yeah some states about the case surf um right now we have around like based on the latest surveys uh there are 60 of the qflow users on either case serve and Kev serving kept serving was the um previous name um and um we have around like 100k case of Docker image downloads um and uh right now the project has uh around like 20 uh core committers and we have total 172 contributors who have been contributed to like k-serv and uh we have a of a long list of adopters from various companies like our Bloomberg IBM Nvidia um and uh along with other companies um so some backstory for a case serve so we actually started the case of uh the previous name is cave serving actually it was under the qflow during the first coupon Summit so we we started the project today uh around the uh after the the we presented the idea initial idea and then we kicked off the project after the first qflow summit that was back in 2019 8.5 remember correctly um and we first presented the case of uh previously care of serving uh at kubecon um 2019 uh San Diego um and uh in 2022 we have Nvidia contributed to the major um B2 standardized inference protocol took the K7 project which is now getting a lot of a lot of attraction in the industry uh where we have many uh our industry leading model servers implemented this V2 protocol uh and in later 2021 iubm open source the model mesh project which they have been internally running on production for a few years it's now uh open source to uh the queso project um um and earlier this year we moved as the project grow much bigger we decided to move the uh the K7 project to the Linux Foundation as an incubation project another project was uh yeah wasn't really name wrong kids serving two kids serve and K server is now uh independent GitHub org which hosted a couple of uh sub projects like a model mesh case original case serve and uh um models web UI which is used in qflow um yeah um and next let's take a look at the current case of landscape so um we at the call like we have with the core inference components we support deploying the inference service with a Transformer predictor and now we we can um the default case the case serve comes with a set of outbox serving runtimes such as Triton infant server TF serving touchsurf and we also we also support a custom runtime SDK you can easily use it to build your own model server and of course the most important one is the V2 impress protocol which is standardized the uh the inference across the multiple serving runtimes so can you can pretty much like a seamlessly switch from one server runt and to other because they speak the same protocol uh another major feature is okay okay so provides is serverless auto scaling uh so we support uh request based Auto scaling which works on both CPU and GPU device um and so we also support skier down and from zero so we also support all the major cloud storage with a PVC um besides the core inference we also support a wide range of like Advanced inference capabilities such as the uh model mesh you can deploy multiple models at scale which indicate we'll talk about later and we recently introduced the inference graph for as the pro like a more advanced model routing to build like a martyr stage like influence pipelines and we also do features like payload logging request batching and the canary route um um so case server also comes with features like a model explainability and more monitoring which is like also important to Once uh on production where once the model is deployed uh you want to keep monitoring the model and see if their model skating uh the data getting drifted or the model response get adrifted and so you some users also want to like explain the um the model results whether to make sure that they are producing a reliable influence results um so uh we have uh introduced a lot of features this year in 2022 uh we um in zero case serve 0.8 we start introducing the serving runtimes for a better flexibility and extensibility to um adding customers customized serving runtimes um so and also in zero.9 we introduced the inference inference graph as model mesh project joined the k-serv um GitHub Mark we have been this year we have spent uh quite a significant effort to unify the inference service API for both k-serv and model model mesh so you can use the same inference API to deploy models both to K server and mono mesh um and and there's also um unifications for like a how storage works for our case serve and model mesh along with Model status and in other work we have done this year is like support ml mflow model format so the models saved with ml4 you can also easily serve on case of to deploy the the on the influence inference service um and yeah we did a bunch of improvements on Raw kubernetes deployments which is a a new deploy model is the product since uh 0.7 and uh um and then we also unified the outer scaling figures on both serverless analog deployment mode yeah for more details you can always check our release product 0.

mainly exist.8 and 0.9 release um so uh so just want to go through a few like important features we introduced this year so um so we and the main feature I just mentioned is like the uh we introduced a serving runtime as a new uh customer resource where you can Define the um the pro the power template for uh for your serving runtime to um and choose the product virus different model format so on the serving runtime you can Define the um the model format to use the power for your survey runtimes and you uh um most importantly and it defines the key Fields like uh the serving router images as well as the resource resource like definitions um with the uh um so you can use this survey runtime as a template to simplify your uh your inference service yaml so on the infra surface yaml all you need to Define is the um is the model format name which can be like a skeleton pytorch tensorflow um popular ml framework framework you can also optionally Define the uh the versions of the framework um and so by default the case serve Will based on the the model format to pick up the uh disturbing runtime can support the the corresponding model format or the the version so um and you can also always override the runtime version to pick up the serving runtime you want to serve the given model format so this really gives the flexibility and the Simplicity on the yaml which you exposed to your end user so you don't need to user don't need to write the lensity like continuous back which is a very kubernetes kubernetes oriented and then all you need to tell is the what is your model format what is the version of the the framework you use to train your model and and point to the the model you on the cloud storage and and then queso can deploy the model out of the box um so um this is a um a matrix about the support Matrix about all the serving runtime we can um out of the box the front cases so um the major seven runtime we support this ml server and Triton touchsurf they all implement the B2 influencer protocol and so can you can switch between these survey runtime it's pretty uh seamlessly um so we with this default seminar we can cover pretty wide ml Frameworks um and with both uh with the the V2 protocol another major features we introduced uh this year is the inference graph um so um as there are use case where like um people want to build a multi-stage influence pipelines where um typical examples is like uh image processing pipeline where you first want to detect for example the the cad dog example where you want to like first classify the image whether it's a cat or dog and they're like you you further once you detective as a talk then you may want to like uh send to another model to further classify the dog breed um which you need to um sequence the two models and uh to uh produce the uh to finer influence result um so the caser flagger is in a unique position to like uh to build this like distributed inference graph with its uh the native like integration with the inference Service as well as uh you can leverage the uh the serverless auto scaling to ask each model independently and we have a uh the impress router which is a can write your request among these models like Autumn out of the box for you as you can see you can even specify complex conditions where based on based on the previous model you can you can write by the expression to draw two different models based on different conditions um or you can so the inference graph is highly composable so it's a It's Made of like a with a list of routing nodes where each node can consider a set of routing steps um which can be either a rod to an inference service or another node um and uh it can support uh contract against the port different types of routing nodes such as sequence which Ensemble where you want to like a combine the model result from multiple models uh or you want to like split the traffic among different models um so it's a it's really a powerful like um where you can define a yamo and then you don't need to uh write any of your like routing logic to um um deploy the uh the inference graph like with a complex pipeline yeah next I will hand over to Nick to uh and give a brief introduction for mono mesh thanks Dan um so yeah model mesh is a I guess a technology that we had been um running in production in IBM for a few years and I think it was about a year ago we contributed it to a case of since we were already working you know as part of the case of project um and it's really um kind of satisfied a particular uh kind of use case which we're seeing more often now where the the you know much larger number of models maybe personalized per per user of a service and they're changing quite often maybe getting retrained a lot um maybe they're like fine-tuned models on the on a larger model and this automation instead of the sort of k-native model where uh there's a container spun up or a deployment of containers per per model this is a sort of more static set of PODS uh where each pod loads multiple models at the same time and so it's sort of like a high density um idea so model mesh is like a layer that manages um uh the you know the setup models like a Liu cache and it sort of decides where and when to load them and routes the request to them um and it does it kind of abstractly so it works with uh lots of different model servers as long as they provide the kind of load and load type semantics which most of them do now um do you want to jump to the next slide maybe oh I think there was one in between there we go um yeah so it's useful when uh you have like a much larger number of models then you may want to serve at a given time so it has sort of serverless type uh function instead of spilling out pods there it will load dynamically the models when needed within the within the pods but it will try and keep the space filled with the most recently used ones so you know when we run in production maybe only 10 of the models in use are actually loaded at the time but it it manages all of that automatically so um you know you can really optimize the footprint uh data and then it will auto scale um also you know without needing any configuration across those pools so it'll load the same copy of the model in multiple pods and balance between them automatically if it's more heavily used and less heavily used ones might get evicted or not not loaded altogether um has a bunch of other stuff like Prometheus metrics um we've got integration out of the box like I said with a bunch of the same model service that K serves already supported like Triton ml server torch serve we added recently um and Intel uh it wasn't on The Matrix before but Intel's open VNA model server also integration um and you can create your own via the same sort of serving runtime abstraction the crd that Dan mentioned before you can create you know multi-model type serving runtimes and and then the regular single model types so we've been working to kind of align some of the features um and uh and functionality and the interfaces so the same crds are used whether you're doing a sort of k-native or uh deployment or a model mesh deployment you can sort of deploy the model in the same way um yeah and this is sort of illustration of how we're how the two uh sides are kind of coming together right now we have they're sort of separate controllers and you can um you can install independently uh one or the other but they um you know they also work together and sort of will uh only process the crds that um you know intended for that particular usage um and uh yeah we're sort of aligning the storage the way the storage configuration works and the capabilities there um we recently added cluster serving uh or sorry cluster scope support to the model mesh controller so it works like the same way as the case of controller which it didn't have before and um you can use the case of Transformers now along with the model mesh predictor so you know K7 will deploy a Transformer that you might have in the same way as it does uh and then the predictor within that same inference service can be running within a model mesh cluster so that's another integration that we did not too long ago okay um I'll hand back to Dan I guess to cover so yeah so um next year as a um I think case of girls more uh mature so we um next year uh we want to um graduate the project to the 1.0 version um so we currently have been discussing with the the case of community about the road map so um we might still need to finalize the the adventure roadmap but I got these are the items come up like uh during the previous like uh Community meetings so we um the most importantly we want to promote the influencer service CR which is kind of the V1 beta12 V1 and uh and then graduate the uh inference service graph and a serving runtime 2v1801 um and uh we look into like a fully support the resident grpc V2 protocol across all the Frameworks uh we are almost there there's still a little bit more work but we are very clear us and also the cloud storage unification for all the major Cloud providers between both like a case server and mono mesh we have did most of the work but yeah um hopefully we can like finish like a um early next year um and uh um and the model mesh we um want to promote Veronica the current Alpha data to the beta state um um yeah I think we already done most of work um yeah we should be able to do uh promo to the beta um Brazil um and other other side like uh um we want to improve the case of observability and um I think mostly there we just like a country is like a model of a lack of documentation uh how to set it up so we're looking to like improve the documentation like on in terms of like observability and as well as the authentication authorization like a setup uh in the um for the from the security perspective like uh um yeah this is also like around like a documentation it's like people find like difficult to do but yeah I think we just need to like improve our documentation to um with maybe like some we can publish some blog too um for the um the setup and some other features we are looking to support is batching this and which is currently I think it's uh um mostly the the one major feature which is missing Verona caser but yeah we look into like a um makeup proposal like a how we can support a patch inference next year