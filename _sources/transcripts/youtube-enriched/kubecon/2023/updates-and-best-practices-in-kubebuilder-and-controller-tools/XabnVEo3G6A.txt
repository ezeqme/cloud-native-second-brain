hello my name is Camila macedo but that is not important at all what matters is the context prepared by us to share with you I will be kicking start on this journey however if you want to know more about us please see the hair forensic Link at the bottom you can check out our profiles on the kobecon page following the talks that we'll cover here it is only to let you know what it is a presentation is about to begin with will you do a short overview and introduction about cookie Builder as the projects used by each and we are trying to cover operator partner so we can speak about good practice and common suggestions waving after that we will share the Kobe Builder updates think we will let you know about your personal plugins and how they can help you accelerate the development of your Solutions we also is speaking here about the Kobe Builder plugin ecosystem then we will tell you more about controller chain and controller tools by less we will say how you can interact with and to be part of the community your help is more than welcome we are willing to see you around we all hope that you'll find this information very helpful let's start shall we probably many of you here are familiar with the projects avoided however we would like to do a small very facial review just to ensure that we are all in the same page you may also discover very cool things about the Bluetooth itself so let's go what is copy Builder Kobe Builder exactly a common design tool that aims to help create projects extinct the kubernetes apis commonly follow the operator pattern it's design architecture is based on plugins that allows users be able to choose if they want opt-in or not for specific features helpers and sometimes Integrations I will not do a spoiler here to better covered into stock that's it for now I just would like to share this is also possible using could be built as a library and then take advantage of its features and implementations for example imagine if you'd like to create a tool to provide specific scaffolds or to do customizations and Integrations on top of your watch is done energy provided by copy Builder itself operator we added here a PC of information from the kubernetes documentation UK news could be Builder to build a solution that you not follow this principle however could be Builder is designed to help you watch if it is gone kubernetes is very nice and very cool but we need to make our Solutions work on it as you know it's not English task and definitely there's more than a way to skin a cat one is by extending the kubernetes apis following the same principles adopted by kubernetes itself then take advantage of its unique possibilities but how this idea mainly works we have a controller responsible for managers and specific kinds of resource such as a deployment then we declare a desired state which means we Define what we want persuade the energy of the deployments we declare that we want a deployment its containers how the security contacts it should use anti Etc therefore the kubernetes API you raise evidence in this case to notify our design to create the deployment in the controller we'll be watching all create events Hazard for the specific kind the controller you see Ops I have a new average that matters to me let's start the reconciliation let's start to run all operations required to ensure that the desired stage is the current state of the cluster okay that is nice but if it is new for you probably you are asking now what are the advantages of adopting this approach by using operators it's possible not only to provide our expected resource aiming to install uninstall things on the host but it's also possible to manage them dynamically programmatically and at execution time let's return to our deployment example to help us to illustrate this idea so we have the deployments and we have the pods defining the deployments everything is running and finally on the cluster if we go there and delete the pods what happens do you remember that then in this case the body will be recreated without our intervention it happens because the deployment controller you'll be watching the closer in Juicy oops I need to looking to ensure the desired stage again I need to create report in the same way we can automate our Solutions so if someone or another solution or our solution to self it creates deletes or updates resource on the host that matters for us research that we are watching for then we can go there oops we need reconcile we need to be looking for to ensure that you are on the host as we want to be now that we are all in the same page about opportunity does and its motivations we can share that Jacobi Builder is developed on the top of controlling time and controller tools the projects built with Kobe Builder results in a bond that runs the manager into PG is responsible for managing the controllers controlling time is the leak that provides abstractions meaning to help us to manage the cheese controllers and watch the resource in the controller tools which is used to generate Edge code you see that better after install about best practice it's very common people ask where they can find the good practices where I can find the cheese information so shall we try to do a small overview here let's start by speaking about the golden rules let's call it this way in the introduction we spoke that it is all about automation automation of what of operations with the proposed to do a solution that you'll be looking for to ensure that the currency stage is the desired state therefore our solution must be identity that means for some input we always be looking for the signal to put then we Implement a solution that you adopt the operator pattern we exchange and use the kubernetes API thus it is why the to ensure that we understand its structure and that it's API conventions put yourself in the position of existing a dreaming who is the person who you consume and to use our solution this person knows kubernetes so ensuring our resolution works in the same way you make their life much easier definitely it provides a much better user experience let's think about FM designer solution to run onto the environments why not think is this problem that you need to so we sort out by kubernetes if you saw how kubernetes does it then why not this in the same way for example it is an excellent idea to track the stats of our reconciliation so that it's possible to troubleshoot and to know if something goes wrong and to the reason if you win it is possible how does kubernetes API do each of a resource of the kind deployment do it not check these thoughts conditions of a deployment when something goes wrong in this way let's not try to hang to the hill we can do it the same way on top of that see that we can use kubernetes leaves into implementations to achieve our goals in the spend our efforts in what we need to sort out in this light see that as a tip we are sharing the project API Machinery which provides a helper to work with stats conditioners if you are looking for example by looking at a discount for Generation by the deploying image plugin which will speak a little bit more as part of our updates you can find them the next tip is try to avoid Your Design solution where the semi-controller has the purpose of being responsible for reconciling and to manage more than one principal kind for example suppose we have our operator projects that your manages our resolution and our resolution has more than one component we have the front and the back so in this case it might be better we have a custom resource definition for the front and another for the back as its respective controllers instead of you have something like a one kind install everything install app into one controller to do everything furthermore this might hurt Concepts such as single responsibility by Health Concepts like it is it's much bringing the expected bite side effects such as increase the difficult of growing reusing or keeping maintaining our solution buy less we are sharing here a link from the opening that has the key documentation where you can find a very helpful information about this stock so let's now share the updates we will start by sharing the new deploy image plugin what is the most common scenario when you want to develop a solution to work on kubernetes would it not mean deploy an image therefore the motivation of this booking is to generate the holy code to deploy and to manage a solution on the host following good practice in this way this plugin can help you accelerate to the development of your operators also reduce the learning code it can be used as a good example sorts as you can see on the screen we need to inform the image that should be deployed on the cluster to generate the code it will end up as a deployment with a pod running a container using this image so by running both commands one to create the project in another trisk approach the API and the controller to manage this image we mainly have a whole solution implemented with testis raising events into using status conditioners t solution can be used up added are you sure now the code generate for you have a better idea so here we have an input director we will create the project now let's show did the fiscal foot here is the main you can check that we have a manager to manager our controllers also you can check a making file with tags to help you develop the solution and build it the docker file that you generate the container to run our manager engine now let's create our API here you can see the group that we are using the version of flower API the kind the image that you want to manage and the plugin now you can see that we have it here the API implemented with the stats conditions you can see that all code is generated with comments to help you out and you know further information the spec now let's check the controller see that the reconciliation is implemented with stats conditions finalizers and also comments now the tests here yeah the test is implemented for this controller in the then Tony will let us know about the graph funnel plugin let's talk about the confronto plugin which is Handy tool design to save users Time by simplifying the process of building dashboards to visualize operator status the plugin generates manifest based on default metrics allowing users to easily copy and paste the content into the governor web UI the instant observation of the operator status moreover it features a customizable config entry that enables users to visualize their own user-defined metrics in the governor panels without record from the plugin users can efficiently monitor the performance of The Operators with ease here is a quick demo on the memcache operator to trigger the plugin we use the sub command edits this will give us the performance of the rapturing that contains the Json file of the grafana Manifest we can copy the content of it for instance this resource Matrix Json file and the grafana UI we can click the input button paste the content and select the data source it will give us the CPU and memory usage similarly we can operate on another file the runtime Matrix Json file again let's input the content now you can see there's Rich panels that provides different perspectives of the red constellation and the work queue status for the user-defined custom metrics the plugin provides a config demo file this allows users to provide the name of the metric and the type of it in this example we have predefined two metric one is a histogram the other is a counter now let's trigger the plugin again this time it will load the config email and bring a new Json file let's copy the content again as you can see this time the dashboards will provide two panels that corresponds to the separate metrics given by the user input now let's move on to the next page contributed by Rashmi hello everyone my name is Rashmi gautipati and I am from the operator SDK team at Red Hat I'd like to introduce Phase 2 plugins in queue Builder and operator SDK as you've seen from the introduction and background so far cumulus simplifies building a kubernetes operator and allows you to quickly create and manage and operate a project an important goal of cube Builder is to have an extensible CLI and Scaffolding so what does it mean to have an extensible sale and Scaffolding it means that Q Builder can be imported and used as a library in other projects for example being able to provide customizations such that other projects can use Q Builder's workflow with non-go Lang operators such as ansible or help in order for these projects to have customized content based on their needs we need to have extensions that will modify the queue Builders base scaffolding before scaffolded files are written out to the disk and what helps achieve these extensions are through plugins let's see how Q Builder provides extensible CLI and Scaffolding options through plugins so what is a plugin a plug-in is defined as a software component that adds a specific feature to an existing computer program and that's exactly what the plugins do in Cube Builder so there are like three main sub-commands in it create API and create web hook so when any of these sub commands are called their respective plugins are responsible for implementing the code that needs to be run commands now let's look at how Cube Builder was extended through various phases of plug-in implementation Phase 1 enable key Builder to be more extensible by defining new plugin interfaces and providing a new CLI thereby adding the ability for cube Builder to be imported as a library in other projects currently phase one plugins are all go based so they have to be compiled with the cube Builder binary and they cannot be external phase 1.5 introduce the ability to chain plugins meaning more than one plugin can be executed along with the cube Builder CLI however phase 1.5 plugins are still go based and their internal plugins that need to be compiled in here's the thing what's new in qbuilder now is that we have landed Phase 2 plugins which allow support for external plugins The Phase 2 plugin API is designed in such a way that it discovers external plug-in executables that don't have to be compiled with the cube Builder binary so the source code of the plugins is not compelled to be inside qbuilder Repository so Q Builder Library consumers can now have support for Discovery and chaining of external plugins by just importing and using a sample external Plugin or writing your own plugin in the language of your choice since Phase 2 plugins are out of tray these plugins are going to be external and not in the tree of cube Builder this eliminates the need to have all plugins on the same version of go dependencies another major benefit is that the plugin API is designed to be language agnostic it provides support for other language scaffolds using standard in and standard out mechanism so plug-in developers are no longer restricted to writing in golang and are actually able to write a plugin in any language and since the external plugins are out of trade they will be discovered at runtime which makes Cube Builder easily extensible without having to be rebuilded let's look at the workflow that occurs when a user runs a net command and provides two external plugins with their names and versions so cue Builder discovers those plug-in binaries in their respective name slash version directories once they're discovered cubital constructs and sends a plug-in request Json to both the plugins over standarding the plugin request consists of the command that was initiated all of the raw flags and an empty universe that contains the current virtual state of file contents that is not written to the disk yet after receiving the plugin request the external plugins construct a plug-in response that contains information on the modified Universe based on the new files that was scaffolded by the external plugin and writes it back via standard out in the Json format coupon then receives the plugin response Json which will then be decoded and then writes all the files in the universe to the file system I'd like to show a quick demo of how Phase 2 plugins work with Q Builder for the demo I'll be using a sample external plugin that I've implemented in Python to showcase the ability for Phase 2 API to support other language scaffolds as we have seen earlier cubildo uses name and version scheme to discover external plugins it's most natural for this use case since plugins must have a group like name and version so when any of the sub commands like init create API or create web hook are in both Cube Builder first discovers the external plugins by traversing through the plug-in root directory to match the plugin name and one level down it then matches the plugin version and once it's in the third level it tries to match the plugin executable so for the demo I'm running on a Mac so the default plugins path will be user slash home directory slash Library slash application support and under that we have q Builder slash plugins so this is going to be the plugins root and on Linux the default path for the plugins route would be a different one so if we look at the directory structure of the plugin under plugins we have the my external plugin directory which has V1 as a subdirectory and under V1 this my extern plugin dot py which is the external python plugin so every plugin gets its own directory constructed using the plugin name and plug-in version for the executable to be placed in and Cube Builder will search for the plugin binary with the name in the name slash version directory of the plugin so in Cube Builder main directory let's create a new project now let's run the init command by specifying the name of the external plugin in the dash dash plugins field what happens now is that Cube Builder detects the OS and it discovers my external plugin in the path that we just talked about it also validates the name slash version directory and checks whether the plugin is an executable based on the bitmask and then it runs the plugin so this results in the scaffolds like project file and license header and the domain.py and main.py so the file contents are provided by the external plugin in the universe object it represents the modified file contents that the external plugin writes back to queue Builder as we've talked about in the previous slides so communication between Cube Builder and the plugin happens through plug-in requests and plugin response by passing the updated Universe back and forth to each other so now let's run the create API command by specifying the plugin name and passing in some arcs like group as cache version as V1 alpha 1 and kind as memcached so Cube Builder finds the plugin and runs it but this time by passing the create API command in the plugin request so this results in scaffolds like GVK Dot py so if you look at gvk.py its scaffolds a class memcached and group as cache version as V1 Alpha One and kind as memcached these are the values we specified for the flags when we ran the create API command and also an init command that sets the name and namespace and now let's run the create webhook command by specifying the external plugin name and group as cache version as V1 Alpha One and kind as memcached and this results in the scaffold webhook.py yep so these are three sub commands to initialize the project scaffold kubernetes API definitions and web Hooks and this is how external plugins can be discovered and run with Cube Builder and this concludes the talk and quick demo on one of the recent updates in queue Builder which are Phase 2 plugins thank you for watching thank you Rashmi for explaining about plugins now let's look into controller tools and controller check controller tools is a project that contains a set of go libraries for building controllers it provides us with a binary called controller chain that helps in scaffolding out code for deep copy generation custom resource definition and rbac manifests UI would have probably heard the very common term called Marcus or marker comments which are present in the operator project control agent performs the task of reading those markers and Scaffolding output which could either be yaml definitions or just go code provides us with three different generators it also has the option to specify output rules wherein we could mention the location of the scaffolded content let's walk through a sample code here we see that the BusyBox API spec is defined and contains a field name size on top of this we see the controller chain marker commands that contain the tag queue Builder these are validation markers and here they mean that the minimum possible value should be 1 and the maximum could be 3.

after running make generate we can see that the open API validation is added to the custom resource definition the lesser-known use case of controller tools which we will briefly discuss here is the ability to extend the library and create custom generators the major utilities which controller tools provides us with is the ability to Define markers in the format we want pass them with any custom logic and scaffold out relevant bits of code the first step is to declare the format of a marker and create a definition out of them the commonly used helper for this is marker.make definition the link is to the same is present in this slide markers can be defined to be on a type on a package or on a field like the cube Builder object marker or say a validation or a webhook marker the marker format does accept arguments and these fields can be defined as a struct and passed on to the definition Step 2 is parsing of markers this is done using chengo for which we have wrappers in controller tools and the reference again is present in the slide the step 3 is to generate output now the output can be of any form as mentioned earlier either yaml or just go code the location of the output can also be configured now that we have reached the end of the presentation we would like to welcome you all to be a part of our community we do have a slack Channel under the kubernetes org named Cube Builder and we also run bi-weekly Community meetings please do join the Google group to receive invites and updates on the things happening in the community we have the references here we have the link to the cube Builder documentation the quick start guide the link to different plugins which we discussed and the link to use Cube Builder as a library we also have links to controller runtime controller tools and a few API conventions followed by kubernetes please do have a look at them and feel free to create issues rprs against these projects thank you so much