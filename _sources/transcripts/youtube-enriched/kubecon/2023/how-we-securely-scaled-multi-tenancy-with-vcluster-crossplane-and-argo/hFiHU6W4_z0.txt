so welcome to this coupon presentation about how you used the cluster cross plane and argosity for multi-tenancy I'm kostis capellanis I'm working as a developer Advocate at codefresh and with me I have Ilia our devops engineer at code fresh and the problem we are going to talk about today is how we provided hosted Argos at the instances to the masses we both work for code fresh it's a software delivery platform it has a CI CD and github's component it's if you want to know more about codefresh we have a booth outside but today we're going to talk about a very specific problem that we wanted to solve so customers can go to the contrast website they sign up they open an account with us and then after some minutes they get an Argos in the instance for them just for them credit for them it's open to everybody so we don't know how many users we have in advance and usually it should be fully automated we don't want to open a ticket to create a cluster for them we don't want them to send an email we want to to be completely self-serve so that's the problem we wanted to to solve and essentially give an Argo instance to to everybody if you know the name so what are the possible solutions if you are familiar with Argo CD Argo CD on its own has an installation mode where you install it on a namespace so you split in the installation into two parts first you install the CR this only on the parent cluster and then for its name space you install an Argos in the instance that works only on that namespace and after you do that the customers come in and they can connect their own clusters to this to their own argosity instance so essentially it's customer has a namespace on a third cluster now this could work in theory it's very easy for us because we have a single cluster it's a centralized it's also resource efficient we can use all the auto scaling methods that we have for kubernetes creating a namespace is uh super fast so it's very easy for customers to get what they want right away but it's not secure by default so there is no isolation between them spaces we need to set up policies a set of quotas do something for isolation you also have the usual problems with resource servation if somebody is doing something that gets stuck the whole system will have issues and also specifically for us because we're installing Argo CD and we want in its tenant to get Argo CD Argo CD has its own crd so we would have problems because we can only use one crd for everybody one crd instance so essentially everybody is locked to the same cluster version and the same argosity version which maybe it's not the optimal way to do things now on the other end of the spectrum we could say okay let's give its customer their own cluster so as soon as somebody signs up we launch a cluster just for them we install a narcosity instance on the cluster and then we give them full access uh to the to this cluster so this model is a customer per cluster now on the other hand we get total isolation everybody owns their own classes and they can do whatever they want we are also free to do different versions so if somebody wants a different kubernetes version or a different Argosy version we can do it we don't have any conflicts with Argos CDC or these so as far as the customers are concerned it's perfect for them but for us it's super expensive creating a cluster for its customer remember I said we don't know how many customers we have in advance creating a class that is super slow for some Cloud providers it's 15 minutes 20 minutes and it'll be also difficult for us like we would have so many clusters and we would need a an easy way to manage them so if these are the two choices that we had to make and essentially you can see that nothing is perfect each one has advantages and disadvantages and somebody might say Okay cost is more interesting for me so I will pick this one or a scalability is more interesting for me so I will pick that one but there isn't like a clear winner uh specifically in our case because argosity is not just an application but it's an application where you um connect Target deployment clusters so this would be super secure because if somebody gets access a tenant get access to the argosity instance of another tenant not only they get access to the Argosy Dimensions itself but also to all the Clusters that somebody might have connected there so security issue would compromise the classes of the customers as well not only our own infrastructure so we said maybe there is a third option and this is when we discovered uh V cluster so big cluster if you're not familiar with it it's an open source project um there is a website at bigclaster.com they also have the source code at GitHub it's managed by Loft labs and essentially it gives you the capability to deploy a cluster within a cluster it's clusters all the way down cluster reception and it's fully kubernetes compliant and we're going to talk about this so in this model we have a root cluster like before but on its namespace we deploy another kubernetes cluster like a real kubernetes cluster you have many choices you can install k3s k0s and I think even eks and it's a real kubernetes cluster that passes all the compatibility tests like any other kubernetes cluster it has its own API a fully compatible with standard kubernetes then we install an argosity instance there and Argo CD thinks it's in a real cluster but it's not it's a go CD instance yet its own crd so it's a standard installation not the namespace one and then from that that point onwards the process is the same customers come in and they connect their own clusters so this third choice is a bit better because we get the best from both worlds we have good isolation I'm not saying perfect and we will see why its customer thinks they have full access to a cluster it's cost effective for us because we still have one parent cluster and we can do auto scaling and make sure that everybody has the resources and they have there is no problem with crds or conflicts um anymore it's very easy to share resources if we want creating a virtual cluster is um faster than creating a real cluster and maybe not as fast as creating a namespace but still really fast and it varies for us to give capabilities to the customer if they want a different argosity version and even if they want a different kubernetes very soon so it's possible that the parent cluster is let's say 1.24 but the virtual class inside has a different version there are some issues you need to do some hardening so it's not perfect and you still have a single point of failure so if the parent class breaks for some reason you lose all the children clusters so that's the theory and now Elia will talk about the implementation okay hello so we'll get a little a little bit technical now let's look at the solution architecture from uh above a little bit so we have our host cluster inside our host cluster we deploy a namespace for each customer inside this namespace we deploy V cluster and onto the V cluster we deploy the virtual Argo CD so and to this argue city the customer then connects their own Target clusters and the Argos CD syncs the resources onto their clusters and this is how the architecture looks in general let's dive into the components now let's explore some big cluster Concepts and how we clusters operate and deployed so V clusters are deployed onto the host cluster like any other kubernetes manifest like any other workload you can use you can use play manifest you can use the their official Helm chart and V clusters are entirely in namespace scope so you wouldn't require in the cluster admin privileges to deploy the v-cluster and the way they operate is that high level resources are entirely virtual they exist only the real in the realm of the v-cluster in its API and the low-level resources that are absolutely necessary to act to execute workloads like pods like Secrets config Maps you know mapping to the pods they exist on the host cluster and they're synced by the Sinker which we'll see in the next slide onto the host cluster so this is how vclusters operate they have you have the v-cluster Pod and the v-cluster Pod has two containers one is the V cluster itself which represents the Commerce API and there is the Sinker container which is responsible for syncing API objects from the v-cluster onto the host cluster and basically it syncs by default it will sync only the absolutely necessary resources like Secrets contact spots Etc what I said before but you can also configure it to sync any other resource you might want for example ingresses if you want v-cluster workloads to be accessed from outside so like any Star solution especially our solution because we don't know how many customers we will have and how many of those V classes we will need the provision and we need to worry about scaling and proper automation and so to deploy a single instance of our virtual argosity we need to deploy two things we need to deploy the helmetry for v-cluster and then we need to onto the CV cluster we need to deploy the Manifest for the virtual article CD but the challenge here is that v-cluster is has its own Cube API so it's not just like deploying any other two Helm releases side by side so basically you need to deploy the v-class you need to get the cube config from the v-cluster you need to deploy the Argos CD onto the V cluster so if we look at this in a different way and the v-cluster is actually a piece of infrastructure it's like I would deploy a Nikes cluster in in AWS or in Azure whatever and I would want to deploy some workloads onto it automatically so this takes us to the realm of the infrastructure provisioning tools and here comes cross plane so we were looking for a chromatous native solution to deploy all this and what Crosman does is it it utilizes kubernetes to serve as a general purpose control Loop which means you can use kubernetes to manage any type of resource as long as there is an API we can access and create this resource by some controller you can manage it with crosstalk so you can manage any non-commense resource even pizza orders as we will see in one of the next slides so you get one control pane to rule them all so how does a crosswind provision infrastructure cross plane uses it's very very simplified way of describing cross plane but crossband uses providers and resources resources are represented using chromatic crds and are this and are describing the resources that we want to create for example if we want to create an AWS VPC we will have a spec that for example would have Cedar blocks and providers are the actual Commerce controllers that are responsible for creating those resources on the third-party apis so for AWS we would use AWS provider provider configs are those are the configurations that defined how the provider should create the resources for example they would have authentication they would reference the secret that would authenticate me against AWS so here is a simple example of using cluster interprevision infrastructure on the left hand side we have the provider which is the defines that we are using AWS provider the provider config defines the credentials we would use to access a WS API and the VPC is the actual resource that we are going to create and you can see it under its back it has a region it has subsider block it has tags etc etc and one of the most powerful features of cross plane is the possibility to create compositions and composite resources what that means is that we can create our own crd that would utilize multiple providers and create our own kind of resource for example if my Commerce cluster needs to create needs to include agrocity in it so I would use uh I would use the Google cloud provider to provision the gke cluster and I would use for example the helm provider to deploy Argos CD onto it and I would call this like my cluster and whoever wants to create one of those will create a resource claim and they'll be able to do to deploy such a cluster and there is such an example in the cross-plane repo you can access it later and the presentation is of course uploaded the cncf website so and if you want to learn how to order a pizza from Domino's with cross plan you can scan this QR code it will take you to their blog post it's a really nice blog post so the guy basically created a controller that accesses the Domino's API and by accident he he made a mistake and ordered like half a dozen of pizzas uh so let's see how it all comes together for our solution in coldfish so when a new hosted rbcd is deployed our platform commits a resource claim of the type hosted runtime which which is how we call our virtual agbo cities it deploys it into a git Repository there is a Argos CD on the host cluster that syncs those resources onto the host cluster and there we have the cross timing composition that utilizes mainly one provider which is the home provider it deploys the v-cluster helper list it then creates a provider config yes you can also use crossbank compositions to create cross-plane resources so the provider config has the cube config for the v-cluster and then we use that to deploy our Argo CD onto the speed cluster so how does the end user experience look a user would go into our UI they would click install hosted runtime and they would see this nice progress bar everybody loves those and once this is done they'll see that the hosted runtime is active and they can see their components and once they're connected once the target clusters are connected they can create our applications and sync resources to their target clusters so what are the benefits the benefits for the users of course are that they get one click installation argue CD instance zero configuration zero travel and they have flexibility in managing multiple argue CDs rvcd versions and kubernetes versions they get a friendly management UI and Enterprise grade as a so on all those stuff and how does it benefit us at Cold fresh so we have a centralized setup it's very cost effective the cluster grows and shrinks if customers join groups if they if we shut down runtimes they shrink back we get security isolation out of the box you don't need to worry about isolating pods so they don't access other namespaces and it allows us to have different combinations of kubernetes and agricity versions so we can test out for example new versions of argocd without affecting other customers and how do we monitor all this because pods provisioned by v-clusters are available on the host cluster you can basically use your favorite tools that you use to manage your commands workloads and we use the Prometheus and grafana stacks so everything is like scrapeable we can scrape the slash metrics endpoints and we get all the metrics that we would get as if everything was running without B clusters in addition we built our own proprietary exporter to monitor the runtime Health from the platform side it's some business metrics and to know whether it's synced and like how many of those we have so let's see how it looks this is a grafana dashboard that represents the health of a single hosted runtime at the bottom we can see the Loki and the dashboard from Loki that shows the logs from all the components that we have inside and we have we can see the pods all the logs their statuses whatever you would do for pods and this is the dashboard from the proprietary exporter you can see that at this point in time when we took the screenshot we had 64 active runtimes they're all synth and they're all all healthy okay so now it's time to show a little demo and because because upbound are sponsoring the Wi-Fi for this event I'm gonna yellow it and do it live so let's hope about their own cross planes by the way it doesn't know okay so the demo is inside this repository you can access it by scanning the QR code or or clicking the link and there is a readme business with instructions on how you can run this demo yourself with a few easy steps and let's do it so in the demo I'm going to show behind the scenes how it looks when we provision and deprovision hosted Argo CDs and so here we see the repo let's go over it a little bit we have three folders I will see the applications CrossFit resources and virtual Argo cities so let's start from Custom resources we have here all the resources that are required for our setup for customers so we have the helm provider comments provider I use for a small thing so the namespace gets deployed and removed and the composition for the virtual Argo CD which is the most interesting part which consists of the definition how the crd looks and the composition in the composition we will Define uh for the list of resources you can even try and show that so we have a list of resources like one is the B cluster release and then we would have the observe deleting space and provider config for Helm on the v-cluster and of course the last one would be the Argo CD on the big cluster and so this is the custom resources part and we see the other folder is the virtual Argo CDs the virtual argue CDs is where we hold the resource claims for those compositions so basically this is what we where we would hold the our customer uh Argo cities every instance that they would be created would be created here so at this point in time we have customer one which is currently deployed and we'll see it in a moment and we have customer two which is commented out okay Argos CD applications folder contains the Manifest for the Argos CD applications we have three we'll show I'll show them in Argo CD so the first one is crossband which is basically just a Helm release the official hand release for crossband crossband resources is syncing the folder with the Manifest that we saw earlier and will show Argos it is sync to the other folder okay and let's look at the virtual ago cities at this at this moment it looks like this we have one customer one virtual rocd which interpolates into three resources the provider config and two Helm releases let's add another customer and see what's going to happen and go back and go into virtual audio cities folder uncommon lists commit go to argue City refresh it and we should immediately see that customer 2 was added and let's look at and see what happens inside the cluster we can see that a new namespace was deployed for a customer too and inside we can see the V cluster starting to spin up this is the v-cluster one and the cluster also includes core DNS deployment for name resolution or once that starts and the composition will know that V cluster is deployed and it can start deploying the Argo CD in a moment we should see Argo City starting starting to get deployed okay so we can start we start to see the pods for Argo CD being deployed and if we check and see for deployments in this namespace we for sure know the argocd has deployments and it's all empty why is it empty because deployments are high level resources and if we execute into the v-cluster pod and do Cube CTL minus n Argo CD get deployment we can see they're all here inside the V cluster realm okay now let's try and remove our customer and see what what happens next go here comment it out commit send cargo CD again and we can see it disappeared and if we look at the namespace it's all getting removed and in a second the namespace will also disappear okay so provisioning deprovisioning works and uh and the wife and the Wi-Fi hasn't failed us so we here we have some resources for you to if you want to learn more you can go to the to vcluster.com by Loft and you can read the entire documentation for v-cluster there and crossband.io also at up on the dial if you want to learn more about uh crossband and on our website we have a training for an Argo Studio certification that you can go and have a look at and if you want to give us feedback we would really appreciate that and if you have any questions we're happy to answer now and you can also find us at the code fresh Booth later [Applause] yes can you testing hi um so since everything is deployed like V cluster is deployed using hum charts and you deploy that using cross plane how do you make sure you don't someone doesn't accidentally delete all the V clusters and all the Argo CDs in it what do you have any protections around that or testing so we use the githubs model of course because our you can the only way you can add or remove those is if you log in into that I repeat the question because they encourage on the recording they might not hear it because so the question was is how do we make sure that no one accidentally deletes the entire thing because we have like one composition that controls everything and if you remove like all those all of those compositions the cluster will become empty and we'll lose all of our V clusters and all of our hosted Hardware CDs so in the gitos model and basically which the the its model with Git permissions so the only code owners to the repository is the bot user that uses that that's used by the platform and ourselves and no one has not not even our devs have cluster admins on on this cluster on this on the hosted runtimes cluster they have no reason being there okay so this is how we make sure that's yes can you pass the mic yeah one question for for the V clusters storage uh where those the resources for high level deployments and anything like that are currently stored you you sure that you have access inside the vicrester pod but what happened if I restart the pod okay so big clusters uh the the API actually b-clusters are staple sets they have PVCs and if you restart the Pod the kubernetes API it reads back from the PVC and continues from the from the same spot so you don't lose it's not it's not stateless in that sense and they also have the possibility to use an external postgres database and you can then scale it out um so under the gitups model uh which you basically store all the like if if I press the little button in your in your platform you basically just create another Yama file in your big repository and everything gets deployed is that the case exactly is there any separation between that's that's exactly the case when you click the button our platform like the bot user that that's inside of one of the microservices it just convince this yaml file that's it fair enough and then everything like the the repositories really locked down so nobody can and exactly okay cool thank you uh one question uh you mentioned that you had to do some additional hardening when you started using virtual big clusters could you give some more details about what you had to do so just to make sure that the question was what additional hardening we use uh that so that no one can access like one customer can access another customer uh so the hardening basically what we did is next just to make sure that we cannot do into namespaces uh Network calls okay so just yeah Network policies and and our quotas for resource isolation and that's basically it but because V cluster has its own API there is no chance you would ever be able to read a secret for example to for uh a cluster that's a Target cluster thank you uh just a question how did you solve the single point of failure caused by the root cluster I didn't hear okay what happens if the root clusters I have a problem uh yeah that's so the question was uh how did we solve the single point of failure like which is the host cluster in this case so everything is fully get UPS so if we want to basically everything is stored in yet and our model as well as the virtual argue CDs and what they use which is like more code for specific stuff now but everything is like fully geared ups and even if the cluster gets deleted we can we have it we have the iron we can spin it up on another cluster we will have some really short time there will be some down time there will be sound some down time yeah but yeah it's a question between you know risk and how much you want to to put effort on that risk so you really just have one week cluster then for each customer or do you have weekly customer applications so that in terms of a failure and one weaker so you can switch to another wecaster instance or replica we have yeah the question is like we have a big cluster pair customer each customer has their own like big cluster and basically it can be clusters can scale out you can use like external database so it have has more than one pod so if a pod restarts it all leaves um basically like any other commands workload uh how did you solve the problem with the noisy neighbors that that you referenced in the single cluster approach the question was how did we solve the Noisy Neighbor issue okay so V clusters have isolation modes and either completely resource isolated and because because it's a different Cube API the neighbors don't really affect each other right because uh if uh for example we deploy uh crds for Argo CD in one week cluster we can deploy completely different set of crds and another cluster with the same name it wouldn't disrupt anything it's like noisy Noisy Neighbor problem will would really be prominent in if we had like name spaces for HP cluster and then cluster-wide resources uh with conflict and stuff like that there are some scenarios of maybe you know we don't cover right now again it's the same answer as before it's a question of how much effort we're going to pull to put versus the risk that we're going to to avoid so if somebody does something you know strains or we have malicious actors maybe they will do something but do you have any problems with the the hardware part so in V cluster you're still sharing the the node right so how do you maintain that isolation between the the V clusters so the question is like how how do you um if one customer runs and just on one note and you lose the node and then the all the workflows are lost so we use path topologist uh plus bet constraints on the pads those are also synced to the host cluster and you can spread out the pods on different nodes and so we we spread those so one customer doesn't get affected by a single node failure and so everything remains up thank you so the question is you said you could deploy multiple verses of kubernetes is there compatibility issue when you deploy something that is deprecated between so for example PSPs in the in the host cluster then you have the part security admission controller and the downstream cluster is there some compatibility issue here and yeah the question was is that if you have deprecation issues like on the host cluster if you deploy something on the v-cluster that's deprecated on the host right I get it correctly uh so you might get those if you sync some resource but usually like what you think is pretty basic resources like pods and uh and secrets and containers which are like mature features forever but in theory yes the deprecations can affect you if you if for example use Ingress to V1 beta and the underlying and like the host cluster is uh Commerce 125 it doesn't support those anymore you could get that of course so the cluster is a brand new project on its own and maybe you know this question is better answered by the v-class there are people but essentially what's their recommendation is you might have some misuse and they recommend maybe going one version forward or backward so if pattern is X you should have a child that is X plus one or maybe x minus one but not X plus three or x minus plus three that's the recommendation I think where are we so um you use xrds in Cross plane and to imagine how do you test these xrds see and the question is how do you test a composite compositions and coffee called xrds and so basically you can create another version of the composition and we do it a lot because we have for ourselves we have a hosted virtual aggress ID deploy that we use internally and we deploy a new version of the composition which switch our claimed to use a new version of the composition we test it out on ourselves we're running we run it in our production for a couple of days and then we roll it out the users you can yeah we use we haven't end-to-end test for uh and all of the components we use Cypress like anything like any other end-to-end testing you will do any more questions thank you for the talk um how do you manage the life cycle of the virtual argosity into the liquor store did you hear the question foreign [Music] okay so basically by creating a new composition version with the new ibcd in in the resources like we just bumped the argosity version we again use a test composition we apply it on ourselves and we roll it out to customers if we need to basically this and really easy upgrades any more questions does the upgrade of V cluster has some downtime of view clusters say Cube apis yeah louder please does the upgrading of the big clusters Cube API have some downtime to the cube API so it does the Q API for the v-cluster have any downtime when we upgrade it but we don't upgrade it we do upgrades we want to bump but it's rolling upgrades you you update like one pod and then another you might get short internet is all the way kubernetes all the way think that way kubernetes inside kubernetes standard kubernetes tools you might get short down times if the V cluster is down for like 30 seconds because the pods all run on the host cluster you wouldn't feel it and the problem arises with some crds and stuff like that but for short periods of time it really doesn't affect customers I don't feel it thank you for the great talk I was wondering have you considered using cluster API and if yes why didn't you choose cluster API but cross plane and the question was why didn't why didn't we choose cluster API as far as zyno cluster API is good for deploying uh commence clusters and Cloud providers I don't think there is an option to do something like deploying v-clusters with with this tooling and we really needed like the V cluster thing it was really essential here I think cross-plane when we did like the evaluation was a superset of what you know cluster API was offering maybe I think there is today big cluster support for cluster API there was a representation from Adobe I think that shows but we started you know maybe one year ago so maybe this didn't exist yet thank you for the talk um we have sometimes our time to troubleshooting and debugging some problems with cross-plane compositions especially when they are not ready or healthy you mentioned that you created your own not exported for Prometheus to monitor those cross-plane compositions especially we have sometimes our time with super resources of the compositions how do you manage and what kind of monitoring did you do to debug those so the question is on monitoring is basically the proprietary exporter just uses business metrics it it reads metrics from our platform it doesn't like access the V clusters even V clusters we manage we we monitor them with Prometheus and we haven't had any issues with them really like we didn't see it necessary to monitor the v-cluster itself it was also always stable we had no such issues um but you can in theory you can also deploy collectors inside the big cluster if you want to and have them report somewhere externally I was thinking about like if if we need to I'll add an open Telemetry collector inside as one of the components and we will able and we will send the metrics to one to some external Prometheus for example