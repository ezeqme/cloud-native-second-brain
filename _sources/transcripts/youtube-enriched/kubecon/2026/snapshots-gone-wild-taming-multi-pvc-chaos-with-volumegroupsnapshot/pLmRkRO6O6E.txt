Hello everyone. Um, how many of you have bagged up a multi-olume stateful application and prayed that the restore would work? Yeah. So, us too. And that's what this talk is about. Uh, today we are going to show you why those prayers were necessary and how volume group snapshot solves it. Um, little bit about us. Um, I'm Shuan Pampachar. Um, principal software engineer at Red Hat. Um I work on open shift engineering team part of the open shift API for data protection product and I have been a Valero maintainer since 2022. Um and I'm Scott Sego also principal software engineer at Red Hat also a valero maintainer and a member of the open shift team. >> Okay. Uh so a quick show of hands. How many of you have used Valero? Okay. Great. Uh for those of who haven't uh Vero is a open source tool for backup, restore and disaster recovery on Kubernetes. Uh it handles everything cluster artifacts, resources, persistent volumes, scheduled backups, you name it. Uh it supports multiple uh volume backup methods uh CSI snapshots, file system backups, uh native cloud provider snapshots.

Uh but today we are focused on CSI snapshot path because that's where volume group snapshots plug in. Um, one quick announcement. Uh, Vero's CNCF sandbox application was just approved by the CNCF technical oversight committee last month. So, this is a great moment for us in the project. Thank you. >> Yeah. And before starting uh just like to say that we like to think backup should be simple, reliable and ideally so boring that they never make headlines. So, let's get into it. So this is going to be our road map for today. Um we'll start with why multi volume backups break. Then Scott will walk you through volume group snapshot API the community's answer that solves this. Uh then I'll show you how we brought volume group snapshots into Valero and the design decisions that we took and the implementation aspects. Uh then Scott will share some war stories from real CSI drivers testing like how things broke in ways we didn't expect. and we'll close with some results, benchmarks, and a blueprint for adopting volume group snapshots in your environment.

So the problem uh think about a database application uh like Postgra SQL, MongoDB, Cassandra, anything. So they typically spread data across volumes like you have got your data volume with table and indices. Uh you have a writer head log volume. uh you have a config volume with certificates and settings. So in this example there are three PVCs uh three PVs and this is all a one whole logical application. So this is the norm for um any serious stateful workload deployed on Kubernetes today like uh databases it could be analytics pipelines or message cues. So what happens when you when you try to back this up, right? So here is what happens today. Uh with individual snapshots, uh your backup tool uh snapshots the data PVC at t equal to0 let's say but the application is still running and rightes are still happening on the disk. So a second later the backup tool snapshots the right ahead log PVC at t equal to 1. But here is the key between those two snapshots let's say 10 rightes happened. So your data PVC has a state before those 10 writes and your right write ahead lock PVC has a state after those two 10 writes.

Then more writes happen. The backup tool takes the snapshot of config PVC at t equal to two. So all in all three snapshots at three points in time. So your backup now contains a state that never existed in your application in reality right ever. So the data volume thinks that 10 rightes never happened but the right ahead log volume thinks they did. So when you restore this your database most likely will get corrupted and this isn't theoretical like here is what actually happens in different scenarios when you restore such apps. Uh database with write logs out of sync um you'll see corruption the right side log references data that doesn't exist. Uh in case of Kafka you might see lost messages or duplicate delivery. uh in case of Cassandra you might see split brain behavior like nodes come up in inconsistent state app config doesn't match with the data you might see the app crash at startup so but here is the deeper issue your backup reported success everything looked green right it's your restore that fails so individual snapshots in such scenarios give you a false sense of security so what do we actually need so look Look at the left hand side.

This is what we just talked about. Uh three snapshots, three different points in time inconsistency. Look at the right hand side. One API call create volume group snapshot. The storage then storage layer captures a point in time snapshot of all the volumes in the group atomically. All three volumes are captured at the same exact time. data PVC, write lock PVC or config PVC, same point in time, atomic and crash consistent just via one command. Now Scott will take us through the volume group snapshots API. >> Okay, so the volume group snapshots were introduced via kept 3476. Um they were promoted to V1 beta 1 and Kubernetes 132, uh beta 2 and 134 and they are currently slated for GA and 136. Um basically following the existing pattern volume group snapshot class similar to volume snapshot class volume group snapshots um from volume snapshots and the volume group snapshot content from the volume snapshot content. Um basically the volume group snapshot class will define the driver and the deletion policy.

Um the volume group snapshot itself that's a user request um which identifies the included volumes via label selector. So that's that's the user saying I want to these volumes to be snapshotted together and in one atomic operation. Um volume group snapshot content is then the storage group snapshot which in turn contains the volume snapshot and volume snapshot class. So here we see the workflow under the hood. The user or an application such as Valero will create the volume group snapshot. Then the snapshot controller will create the volume group snapshot content and the CSI sidecar handles the provisioning. Uh it basically makes a create volume group snapshot call to the CSI driver which creates an atomic group snapshot. Um and then the group and individual snapshot ids get returned. The status gets updated for the volume group snapshot content and we create the volume snapshot and volume snapshot class. Let's step back a minute and talk about backup consistency. This can mean different things depending on context.

Volume group snapshot aims to solve the problem of crash consistency. In this case, all volumes are captured at the same instant. This is equivalent to pulling the power plug on a local machine. Um recovery relies on the app's own crash recovery scenario. Right head log replay, journal replay. Uh it's automatic. No app cooperation is needed. However, at a higher level, we have the notion of application consistency. This is an additional level of consistency which requires coordination with the backup tool and the application. Uh and basically the application quiets rights first flushes buffers closes transactions then we create the volume group snap the snapshots to get clean state and then um everything gets restarted. So this requires app specific hooks coordination between the backup tool and the applications. Um best practices here is that the more frequent frequent backups say nightly would may only need crash consistency no need to shut applications down even for a few seconds. Um and that can that but no no freezing is needed.

Um but maybe once a week you have um you want application specific backups might require some small downtime. Um and you can combine that with the pre and post hooks with your application uh to get application consistency. The volume group snapshot handles the storage consistency and the hooks um connected to your your backup application will handle the application consistency. >> Thanks. So uh we had a challenge right um Valero already had a mature CSI snapshot pipeline PVC to volume snapshot volume snapshot to volume snapshot content to storage snapshot. So it works people depend on it and users have been using it extensively. The question that we had to answer was how do we add group snapshot support without breaking anything like for existing users. We followed a simple design principle. We kept uh volume group snapshots optin. Uh no label on your BBC, nothing changes. Everything works exactly as before. And if you have a label for grouping, uh you get atomic group snapshots across the volume group.

And same restore flow either way. And the key inside here that I would like to highlight is uh we use volume group snapshots as an orchestration mechanism in the backup workflow, not as a new backup format. So here is how Valero decides what to do like when a PVC comes up for backup Valero checks two things. One, does it have a volume group snapshot label and is data mo enabled? That gives you four modes. VGS that is volume group snapshots with data mo you get atomic snapshots across the volume group and they are moved to object storage. And this is useful generally for crosscloud disaster recovery. And if volume group snapshots without data mo in that workflow you get atomic snapshots on local storage. They are mostly used for fast recovery. If you don't have volume group snapshot label, you get individual snapshots with or without data mover. Same as today legacy workflow. No changes there. Valero picks the right mode automatically and users don't need to think about it. So here's the backup workflow at a glance for volume group snapshots in Valero.

You start with labeling the PVCs and the step one that is involved here is grouping. So Valero groups them by label value and creates item blocks. Then step two is uh snapshotting. The CSI plugin Velo CSI plug-in creates a volume group snapshot and waits for individual volume snapshots to be ready. And step three is cleanup. That is we delete the volume group snapshot artifacts and keep the individual volume snapshots. Uh the CLI stays the same same as always. Valero create backup create backup name included name space namespace name. No new flags uh no new configs uh just a label on your PBCs. So let's go over each step individually. So grouping uh look at the AML on the right side. Uh we just added one label. That's it. Velor.io/volume groupoup DB1. All PVCs with the same label go into the same group. Velero puts them into an item block so they are processed together. And if you already have labels on your PVCs that group them, you don't even need to add Velo specific label. You can configure any custom label per backup or serverwide.

The applogs PVC here in the example has no label. So it gets an individual snapshot and that's business as usual. Same label value equals same group. That's all the configuration like you need to know for volume groups. Step two is the step where magic happens. The Adam block with all the three PVCs goes to the Velor CSI plug-in. The plug-in creates single volume gra volume group snapshot. Just one CSI call to the driver. The driver talks to the storage layer. All three volumes are snapshotted atomically and the outcome is three volume three individual volume snapshots. DB data DB write a headlock DB config for our example. All pointing to the same instant in in time just one CSI call. Um step three is my favorite design decision in the whole feature. Uh after the snapshots are ready, we delete the volume group snapshot and volume group snapshot content gone on purpose. So you might ask why? So three reasons. First is uh restore portability. Your target cluster doesn't need volume group snapshot CRS installed.

You can restore to any cluster that has standard volume snapshot support. Second is life cycle management. Uh the individual volume snapshots follow Valero's existing deletion flow. So no new cleanup logic was needed there. And thirdly uh no orphans like if we kept the volume group snapshot around accidentally deleting it would cascade delete all the child snapshots. uh we don't want to do that. So the key insight here is volume group snapshots in Valero's backup workflow are transient by design. It does its job during the backup and then gets out of the way. A quick note on how Valero picks the volume group snapshot class. Uh three tier mechanism uh PVC annotation overrides everything then followed by that backup annotation and then default Vero label you can label your volume group snapshot class that you want to use for your backup. uh you only need the tiers for advanced multi-driver setups if you have those in your cluster. U there might be a scenario like what happens when your app has PVCs on different storage back ends right say you have DB data on EBSCSI and DB right log on EBSCSI but your config PVC is on NFS and all of them have the same volume group snapshot label without volume policies the backup fails sees multiple CSI drivers in the group and stops with volume policies the NFSPC gets filtered out before grouping happens so volume group snapshot proceeds uh just with the EBSPCs.

The key thing to remember here is volume policies are evaluated before grouping. That's it. And this was added in Valero 118. Now we're going to talk about the war stories. Basically things we've come across in testing that surprised us. Um sometimes you run into unexpected surprises when you test in a new environment with a new driver, new cluster. Um we deployed VGS backup to a SEF RBD cluster. Uh we had initially done our testing with SEFS. Um everything worked fine with SFS and this RBD cluster. The backup was fine. Um but then we tried to restore. Um we got this error. Volume group snap or volume snapshot content reconciliation failed. Volume group snapshot handle is set but no matching volume group snapshot content found. Here's the problem. Seth's RBD driver uh populates volume group snapshot handle on every volume snapshot content that was created via the group snapshot. However, we didn't retain those between backups as I mentioned earlier. We clean those out because in most cases they're not needed.

Um in addition, if this was restored to a new cluster, it wouldn't even be here if we had retained it. So during restore, the CSI snapshot controller sees this field, requires the matching volume group snapshot content to exist. We deleted it, so it failed. the solution we needed to introduce um stub VGSC's. So in this case during backup um the Valero volume snapshot um plugin basically captures this volume group snapshot handle as an annotation if it's present. So if it's there we we track the annotation. So now we know on restore time if it's needed. Uh we now have a restore action which detects this annotation. If it finds it, create this stub VGSC matching the group snapshot handle, individual snapshot handles, the retain policy, and basically and the restore name, which is an internal error label for cleanup. Um, at this point after the restore, we wait for all the VSC's to become ready to use. Then we delete the stub. We don't need it anymore. End result here is SE RBD restore works. Stub VGSC's are temporary scaffolding.

They exist just long enough for the controller to reconcile and they're cleaned up. Um, one thing to note, um, Shimal just mentioned, we don't necessarily need these C CRDs defined in the cluster. If you're using SFRBD, you need them. Um, this is again one of one of these issues where every driver is different. You got to test. Um, and then that's that's the point here. We learned a few lessons here. Um, drivers are not uniform. There could be subtle or not so subtle differences in behavior from one driver to another. The volume group snapshot API defines what to do. However, each driver decides how to do it. And they might do it in different ways. um that might be externally different from each other. Um some drivers populate the volume group snapshot handle, some don't. Some require the VGSC to be there during reconciliation, some don't. Some return the snapshot handles immediately, some are async. Um so test across multiple CSI drivers. The API spec is necessary but not sufficient for um determining whether everything's going to work for you.

>> Thanks. Uh so we have seen the problem, the API, the implementation, and what can go wrong, right? like so let's talk about results. So here's the comparison uh side by side. Uh individual snapshots give you per volume consistency only. Volume group snapshots give you crash consistency across the entire group of volumes. Individual snapshots make n separate CSI calls. VGS makes only one. Individual snapshots fail per volume. You might get a partial backup, right? But VGS is all or nothing. It either works or not. The trade-off is that for volume group snapshots, you need a CSI driver that supports volume group snapshots and you need to add a label to the PVC for grouping. That's it. I just follow a simple thumb rule here. If you have complex multi volume stateful apps as we saw the example, use volume group snapshots. But if you have a single volume or independent volume app, yeah, you can go ahead with individual snapshots as usual. See, so here are some real numbers from our testing. Uh so we used uh Kubernetes 132.

Uh in the environment for testing, we used Kubernetes 132 and the storage used was FFS. Uh the PVCs were 1 gigs of size and 117 was installed with volume group snapshot support. So at three PVCs, volume group snapshots is 1.3 times faster, modest. At 10 PVCs, it's 1.5 times faster. at 20 PVCs is 1.6 times faster. The speed up is nice, but that's not the point. Let's take a step back. Look at the consistency story here. At 50 PVCs, individual snapshot took 266 seconds. That's 4 minutes and 26 seconds where writes are happening between the snapshots, right? 4 minutes of inconsistency risk. That's a lot. volume group snapshots, one CSI call, zero inconsistency window, all 50 volumes captured at the same instant, same point in time. See, the performance win is just a bonus. I think the consistency win is where you you should use volume group snapshots for um we just have a short demo. Uh let me just Yeah. >> So this is like uh how we implemented uh volume group snapshots in Valero, right? This is a short demo for that uh pre-recorded of course.

So we have a little monitoring tool just to visualize things um properly. Uh you can see the backup name that we're going to use is demo VGS backup and the name space is demo database. Whatever steps we discussed we are going to go through them step by step. Uh the volume group snapshot class used as FFS for this demo and we have labeled it using the Valero label. Uh the nameace has three PVCs. Uh they are labeled for grouping uh with the value as DB cluster one. You can see the PVCs are bound. DB config, DB data, DB write a headlock. Uh we are just waiting the monitoring tool is just waiting for the backup to be triggered. Now let's kick off the backup. As I mentioned earlier, simple same old Valero backup command. Valero backup create backup name and included name space demo database. Uh once kicked off, let's monitor. So you can see the backup is in progress and we are at the grouping state right now. Now next is creation of volume group snapshot. You can see the volume group snapshots highlighted in yellow.

Uh they got created. Now individual snapshots will get created based on that and you can see all three volume snapshots were created in the same instant. Now once they're ready uh we delete the volume group snapshot artifacts during cleanup and the individual volume snapshots right those get cleaned up during the finalizing phase uh in Valero. After that the backup backup is marked as complete. You can see the volume group snapshot artifacts highlighted in red were deleted and you can see the backup completed in 14 seconds. Uh three BBCs were involved, one volume group snapshot and three volume snapshots. That's it. Uh that was a quick demo just to see how things work in Valero right now. and continuing again. Yeah. So let's go to the blueprint on how to adopt volume snapshots today. Uh so you want to try this right? Like so here's the checklist. Five prerequisites. Uh most are one time setup. uh you need Kubernetes 1 1.32 and later that's when volume group snapshots when beta you need the volume group snapshot CR is installed of course and your CSI driver needs to support volume group snapshot best way is to read up the documentation of the driver whether it supports or not uh or you could just check your cluster by fetching the volume group snapshot class u you need a default volume snapshot class uh that's what we observed and this is one of the most gotcha common gotchas u that the snapshot controller requires And obviously you need Valero 1.17 or later.

Uh five steps that's it. Uh this took about a year from design proposal to production. Uh started with a design dock in March 2025. Worked on PBC grouping. Then core workflow implementation documentation followed by that volume policy integration. uh then in February 26 we hit the safe RBD restore issue and just last month wherever CNCF sandbox application was approved. Yeah, every step uncovered new challenges for us. Uh that's what I would like to say and that's we tried that's what we tried fixing. What's next? Volume group snapshots is production ready but still evolving. The volume group snapshot API itself is moving towards GA in the future Kubernetes release most likely 136 I think. Uh we are working on validating more CSI drivers beyond SE. Uh more storage backends we test the more edge cases we uncover. So uh try volume group snapshots in your environment file issues. And that's how every fix in this talk happened. Takeaways uh five things to take away from this talk. Uh number one if you have multi volume apps need atomic snapshots.

Individual snapshots create inconsistent backups that fail on restore. Number two, volume group snapshot is the answer. Crash consistent atomic snapshots via the Kubernetes API. U number three, Valero makes it simple. Just label your PVC. Same CLI, same restore flow. >> Number four, VGS transient by design atomic snapshots during backup standard Valero life cycle after. >> And number five, test across CSI drivers. Uh the APS is necessary but not sufficient. U if you remember two things from this talk, just remember four and five, I would say. Uh we are around for the rest of the conference. Um come find us if you want to talk about data protection, disaster recovery on Kubernetes, volume group snapshots, Valero or if you want to share some war stories from your backup stories. That's it. Let us know if you have any questions. Thank you.