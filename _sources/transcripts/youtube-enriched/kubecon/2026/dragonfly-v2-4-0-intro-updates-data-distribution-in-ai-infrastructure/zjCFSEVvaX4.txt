Hello everyone, my name is Wimbu. You can call me Gio. I'm the maintainer of the drone fly. I hope my introduction can let you know about the current status of the dronefly. I hope that developers can be interested in the dronefly project. Thank you for join us. Okay. Dronely delivers efficient, stable and secure date distribution and acceleration powered by the P2P technology. It aims to be the best practice and the standard solution in the cloud native architectures. It's designed to improve the performance of the large scale cluster uh delivery across the files container image OC artifacts AI models cache logs. You can see that in the container registry the of the sens landscape there are two graduated project. One is harbor as a artifact registry and the other is dronefly as a image acceleration and fail distribution system. Okay. Next let me introduce the nidos a sub project of the drunkfly. It provides a file system on the based on the RAFS format. The most important feature is to make the container image are downloaded on demand in the trunk and the trunk level dduplication to reduce the storage and memory cost.

It can reduce the end to end code launching of the container image from the minutes to the seconds. Okay. Next, now drone fly focus on the three part uh image acceleration, fail distribution and the AI infa in the field of the image acceleration. Don't flies container clans such as the canodi docker cryo oras. It provides the three solutions for the image acceleration. The first solution is to use the drumfly to distribute the image based on the P2P which is suitable for the large scale cluster. The second solution is to use the drumfly and the nidos to distribute the serated image which is suitable for the large scale cluster and the faster container launching. The third solution is to use the Nidus uh to distribute the accelerated image which is suitable for the faster container launching in the field of the fail distribution. Jump flies post the large scale fail distribution and uh use the P2P to eliminate the impact of the origin bandwidth limitations. It supports protocols including the HTTP, HDFS.

It also supports different object storage including the S3, OSS, OBS in the field of the AI infrastructure. Drone flight post accelerated the data distribution during the AI training and AI inference. In the AI inference, drone flight supports the model pack to distribute the AI model. It also supports downloading models and this side from the hugging face by the drone flies uh command line. uh in the future we will pay more attention in the AI infrastructure because we believe that P2P is the best solution to the serated the AI dite and AI models. Next I will introduce why we use drumfly. For example, uh Kubernetes clusters has a thousand nodes and each node needs to download a fail or a image at the same time. For the storage, there are three uh there are a thousand concurrent downloaded requests. One downloading fails in the large scale cluster. The storage bandwidth can easily reach the limitation. This will cause the slower uh image launching uh container launching and the slower file downloads in the cluster.

How to resolve the problem? There are three solution. The first solution is to increase the bandwidth of the storage. But no matter how to increase the bandwidth of storage as a as a backend storage, it must have a limitations. The second solution is to use the P2P to use the idle bandwidth of the nodes to eliminate the impact of the storage. This also the best best solution in the large large scale cluster. The third solution is to reduce the download file size. We can remove the duplication during building and the download on demand during the running to reduce the fail size. Let us the duplicates the files and the downloads on demand. So, dronefly includes the second solution and the third solution. Now let me uh walk you through the three core download modes in our uh content distribution system. First download from origin. There is no catch in the cluster. One appear needs to download a file. It first register with the scheduleuler and then downloads the file directly from the origin. This is the most basic download mode.

Second download from the remote pier. There are some peers in the cluster and have the fail catch. One appear needs to download the fail. It first register with the scheduleuler and theuler will select some candidate parents. Then the pier will download the file from its parent by the piece level. Download from the local pier. The pier has the file in the local disk. When a pier needs to download the fail, it will return the file directly from the local disk without regesting with theuler. Okay. Next I will introduce the exciting updates of the dronefly in the 2.4 uh release. First dronefly provided the new attacks transfer protocol based on the Tway T length and value uh to improve the download performance. use the TL wave format as a uh lightweight protocol to replace the gRPC for data transfer between peers. We can see that use the TCP based vertex the large fail download time is reduced by half compared to the gRPC. Using the quick uh based uh vertex the large fail download time is reduced 40% compared to the gRPC.

Okay. Next I will introduce the scheduling. There are two stage to select the candidate parents for downloading a file. the scheduleuler and the peer in SK in theuler. Drumfly add a load quality feature to score the parent load quality is scored based on the three features pick bandwidth usage bandwidth duration radio and concurrent efficiency. The pier will connect uh one the schedule select the candidate parents. So the pier will connect to the multiple parents and sync each parents bandwidth usage. When downloading a piece the pier will select the best parent based on the bandwidth usage. This solution can resolve the problem of multiple peers downloading from a single pier and reduce the uh risk of the overloading any single pier. Okay. when multiple peers are downloading at the same time. We have optimized the the process to address the issue of the uh root overloading. For example, with uh single state pier uh the pier A will download the piece one, piece three and the piece five from the state pier.

Well, the pier B will downloads the piece two, piece four and the piece six. So the pier A and the pier B then exchange the piece they are missing. This provides uh both peers down to download all piece from the seed at the same time in the two downloaded decks. Uh this optimization resolved the root peer overloading and reduced the longtail downloading by selecting candidate parents in the two- stage uh schedule and pier. Uh we can improve the bandwidth usage and the download performance for large files. Preheating them into pierce is also a solution for reducing the download time. Therefore, we have upgraded the preheating in the drum fly. Now, users can preheat the files flexible based on the IP percentage and the counts which made which makes preheating more efficient. In addition, we added the rate limit for the preheating task to avoid many peers downloading from the origin at the same time and causing the overload on the origin. Okay. plant now supports calculating task ID directly based on the hash of the image blocks instead of the using the uh download URL.

This this enhancement uh prevents uh multiple downloads when the same block is uh used by different ripple. Second dronefly downloads by the piece it will cause multiple the redirect response. So with support for catching the HTTP redirect response. Finally, please note that the go Golan client has been deprecated and uh replaced by the Ras client. Okay. Next part I will introduce how to manage and uh distribute the AI model within the Kubernetes and models service as the artifact across both uh training and inference. This part focus on the inference how to manage and distribute this model within the Kubernetes. AI models have 4K features. Large scale model W has grown from the hundreds of the megabytes to the hundreds of gigabytes even terabytes posing challenges for the model storage and the uh transfer immutability. uh base model only base model used for the inference is a read only uh versioned artifact like uh container image. It follows uh uh build once and distribute everywhere lack of the model management standard.

The community is focused on the speed of the distribution well ignoring the critical governance including the versioning metadata schema and the life cycle management and the cloud native gap. Air infrastructure is based on the cloud native infrastructure. Yet many companies depend on the custom solution uh based on the uh object storage or the G LFS for the model management. This leads uh to nonstandardized uh artifact management and uh lack of the cloud native support. Okay. In the cloud native air, developers have builded a measure and efficient way for software delivery. First, developers commit the code to uh get repo. Manage the code changes through the branch and the tasks. Second, CI and CD pipeline compare and test packaging the output into an immutable container image. Third image are stored in the in container registry. Uh so uh supply chain security oxidize control and the P2P distribution ensure the safe and uh fast delivery. Fourth DevOps engineers use the YML to define the desired state.

The containers life cycle is managed by the Kubernetes. By applying the software delivery and the supply chain to the model life cycle management, we built a efficient system that resolve the challenge of the managing and distributing AI models in the production. First developers push the model ways and the model configurations to the hugging face hub treating it as the G repo. Second CI and CD pipeline package the model waste and the model configuration and the model metadata into a immutable model artifact. Third, the model artifact is managed by the by artifact registry reusing the container infrastructure and the toing. Fourth, engineers use the Kubernetes OCI volumes or model CSI driver model are mounted into the inference container as a volume. In the build stage, we need build the model artifact and push the model artifact to the registry. Mode control is a command to designed to package the AI models into OCI artifact. In the management stage, Harbor serves as the artifact registry uh for the AI models providing a platform for uh storing, versioning and distributing models.

Harbor manager AI models with the six key feature versioning model are OCI artifact with the immutable tax and the hash digest oxidize control harbors RBAC uh will control who can access and manage the models third life cycle management t policy will clean the inactive versions and locking the active versions. Supply chain security uh integration with the cosign notation for generating the signature uh replication. It will sync AI models between and the central and age registry auditing logging of all artifact operations pull push and delete for security and traceability. In the delivery stage we use dronefly for the P2P distribution. This solution has been proven in the high performance AI clusters. It will use the 80% of each nodes idle bandwidth and improving the model deployment efficient for latency sensitive inference service. Harbor will trigger the dronefly to preheat the uh preheat and catch the model in the node before service scale scaling. When the inference engine starts uh inference engine start up, the model loads from the local.

The engine runs as a container. Well, the model is mounted as a volume. This native solution enables multiple post on the same node to share and reuse the model. And uh it also used the preheating and the P2P to reduce the uh latency of the pooling uh large motor ways and native support for the mounting uh OCI artifact as volume by the cryo and kundi. This feature was uh introduced as the alpha in the Kubernetes 1.31 and pro per pro per pro permit pro per pro per pro per pro per pro per pro per pro per pro per pro per pro per promote to beta in the kubernetes 1.33. Uh for compatibility with the Kubernetes older version, we offer the model SI driver to mount and uh deploy models as volumes. In the future, we will continue to optimize management and distribution of the AI models in the Kubernetes include 5K point uh enhance the preheating allow models to be preheated to the specified nodes and the searching catch distribution for the model aware scheduling. Jumpfly RDMA acceleration enable dronefly to use the rookie or the infinite band to improve the speed of the distribution.

Laser loading implement on demand downloading of the model weights to reduce startup latency. Can optimization uh enhance the OCI volumes uh implementation to reduce the decompression overhead for the large layer. Model security scanning implement uh deep scanning feature designed for the AI model ways. Okay. Next I will show the demo. First we find the model in the hugging face and uh clone the model. You can see the model weights and the model configuration is in my uh disk. Next, I will use the model control to generate the model file. Model file just like the docker file. You can see the model files content. This will show what I this this will show the models uh annotation and uh use the model control log in the harbor and uh build the model uh into the OI artifact. Okay. Next, push the uh model to the hover. Next, I will log in the harbor console. We have find the model and the different version. Uh hover will display the models tag annotations and uh summary lessons and fails. Okay, we can use the members to provide the access control and uh create the dronefly P2P provider to prehead the model into the node.

You can select the scope and the policy will uh remove the delete the old version to save the uh space and the audio logging will uh record the operations. Okay. Next I will deploy a VM inference. You can see the poly ML and the volumes is the model and the and the container image is the VM. Okay, apply the YL. You can see the code is running. The container runtime will download the image and the model artifact. And this in this demo uh we use the cryo wait for the pod rider. Okay, the code is ready. I will enter the uh inference code. You can see the model artifact has been mounted into the inference port. I think this solution uh SP separ separates the model from the engine. Okay. The model is the volume and the engine is the container image. Okay. Hey, that's all. If you are interested in the dronefly or AI model management and distribution, please discuss with us in the project community. Okay, thank you.