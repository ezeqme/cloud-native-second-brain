Hello everyone and welcome to KubeCon Europe. My name is Hanuch Chen and my partner my co-presenter is Wan Lai. I work at any scale and any scale is the company behind Ray. The open source standard for distributed computing in Python. Uh due to visa issues, my partner won't be able to give the talk with me in person, but he will record a video to explain the commod related part of the presentation. Today we will share a practical approach for running AI serving reliably at scale on Kubernetes under uneven GPU capacity and regional failure risks. Here is a plan. We will start the production challenges including bursty traffic, strict P95 and P99 latency targets and GPU scarcity and explain why these push single cluster coronetes to its limits. We will then present a two-layer blueprint. The first layer is fleet orchestration across clusters. The second layer is inference serving inside each cluster. Wan and I will split the session. Wan will cover the flea layer and show how commada applies policy for placement replicas spreading per cluster overrides and automated failover.

I will cover the sering layer and show how reserve by kra ray service including requestdriven autoscaling safe upgrades and advanced feature racer provides. By the end, you will have a vendor neutral reference architecture you can adapt. Now, let's start with the challenge. To start, I want to set a clear baseline. At scale, air serving has production requirements that are strict stricter than most dataless microser. First, we need predictable service level objects. That makes tight P95 and P99 latency targets, strong availability and controlled error rates. Even when traffic is bursty. Second, we need elastic capacity and GPU efficiency. Traffic can spike quickly but scaling serving is not instantaneous. Models need one time including loading large weights and inocite initializing GPU memory. We need absorb spikes without building large cues while also avoiding overprovisioning and leaving expensive GPUs idle. That makes right sizing and utilization critical. Third, we need regional locality to meet latency budgets and comply with data residency boundaries.

especially relevant here in Europe. Inference often needs to run close to users and within specific regions. Finally, we need safe operations and visibility. We need rollouts that are gated by health signals, clear rollback paths and model level telemetry such as tail latency, Q depths, and error rates. Many inference requests are long lived. So upgrades should support graceful joining rather than a broad termination. The goal is to meet all of these requirements at the same time. Next, I will explain why a single cluster with standard Kubernetes primitives becomes a bottleneck. Given these production requirements, single cluster Kubernetes and standard primitives fall short in two areas. infrastructure limits and serving orchestration. First is the single cluster boundary. One cluster is one blast radius. An outage, a control plan incident or even planned maintenance can disrupt the entire inference service. GPU capacity also hits cliffs. When a region runs out of GPUs, requests start queuing and latency spikes quickly.

A single physical location also can satisfy both tight global latency targets and regional data resistancy requirements at the same time. Second is the venina kernetes gap standard service and deployments are not designed for inference serving features such as multi-node and multi-GPU execution. Operationally, inference requests are often long lived without serving aware journeying. A normal HPA scale down or raw can interrupt inflight requests and token strings. So the takeaway is simple. A single cluster is a hard boundary and val primitives do not provide the serving life cycle inference needs. This is why we use a two-layer design, a flea layer across clusters and a serving layer inside each cluster. To address both the single cluster boundary and the molina kubernetes gap, we separate concerns with a two-layer architecture. At the top is the flea layer. Karma acts as the multicluster control plan and use policy to decide where workloads run, how replicas are spread, how cluster specific overrides are applied and how failover works when a cluster or region becomes unhealthy.

This answers the where inside each target cluster is the serving layer. We run race serve via coupra ray service to provide inference serving capabilities such as requestdriven scaling health gate upgrades graceful draining autoscaling and some advanced feature I will talk about later. This layer answers the how. Next we will start with the flea layer and go through the command patterns placement spreading overrides and fill over. Hi everyone, my name is Wan live. I'm a software engineer at Bloomberg working on the machine learning platform and an open source contributor to the Carmada in cubri projects. Unfortunately, I'm unable to join the person in join in person today due to a visa issue. So, I've recorded this short introduction. In the next few minutes, I'll briefly share how we build the fleet layer using hermada. After that, my co-presenter Henru will continue the session. Thank you for your understanding. Let's get started. Cara is Kubernetes native. We can keep standards APIs, standard manifests, and the same cute color workflows.

If you can deploy a ray service in one cluster, you do not need a new workload format for me cluster. This model is decoupled. The race service yaml stays state stable and all milly clust cluster topology and fleet logic is expressed as declarative policies. Through policy, kmada provides the fleet capabilities we need placement and replica spreading per cluster overrides and automatic failover. R services is a custom resource. So we use the commada resource interpreter to let commada understand race service status resource requirements and readiness semantics so fleet policy can act on it consistently. Now let's make the fleet layer concrete with the first policy pattern or replica scheduling in karma defined in a propagation policy. First is placement with cluster affinity. We select the clusters that meet latency or compliance requirements such as clusters labeled for a specific region. We do this without changing the rate services yaml. Second is spreading. With spread constraints, we require the service to be distributed across multiple clusters or regions.

This reduce bladius and avoids concentrating capacity in a single failure domain. Third is replica schedule for race services. We mostly mail most commonly use duplicated that means Carrom deploys the same ray service into each selected cluster and each cluster runs its own serving stack. This aligns with our two-layer design because scaling and upgrades are handled inside the cluster by race service while milluster placement and resilience are handled by karmmatada. KMAN also supports divided scheduling when a workload needs to replicate split across clusters but it is less common for our reser setup. Next once the service is running in multiple clusters we need to adapt to differences between clusters that bring us to overrides. The override policy is to handle the heterogeneous challenge. In the real fleet, clusters can differ in GPU labels and scheduling constraints, storage defaults, and the container registry they use. If we encode those environments detail inside the race services YAML, we quickly end up maintaining separate YAML per cluster that increases drift and makes operation harder.

Instead, we keep one race service definition and apply cluster specific adjustment through an override policy as shown in the diagram. The base rate service stays the same and Carmada applies targeted overrides before the resource is applied to each member cluster. Common examples including updating the image restrict prefix for a local mirror, selecting the appropriate storage class for a model caching and applying cluster specific labels or environment settings needed for scheduling integration. This keeps responsibilities clear. The fleet layer adapts environment wiring and race service remains the source of truth for the service definition with placement spreading and overrides in place. The remaining fleet topic is failure handling. Next we'll cover dual level failover. We use two level failover triggers provided by Carmada to limit impact. First is cluster level failover. Carmatada monitors member cluster health. If a cluster becomes not ready or unreachable, it is marked with the corresponding cluster tag.

New scheduling to that cluster stops and after a configurable grace period, affected workloads can be affected and rescheduled onto healthy clusters that still satisfy the placement policy based on the cluster failover policy. Second is workload level failover. A cluster can be healthy while a specific race service is not ready, degraded or crash looping. To avoid relying only on cluster health, we integrate race service with kmada using a resource interpreter. The interpreter maps race service status into health signals that policies can act on so failover decisions can target the effective workload. That concludes the fleet layer. Next, I will hand over hand back to Henry who will cover race serve by race service including autoscaling, safe upgrades and advanced features. Thank you for listening. Now I'm going to talk about challenges of AI serving in production and how race and kra solved it. So the first problem is real world AI serving isn't just one model tensor in tensor out it's prep-processing multiple models post-processing and business logic trend together each with different resource needs.

Traditionally each step or deployment becomes its own service, its own container, its own config, its own deploy pipeline which is very hard to manage. So in reserve we have a feature called model composition. Instead of deploying each step as a separate kernetes service, Rayer lets you compose your entire pipeline as a single Python application with independently scalable components. The next problem is inefficient GPU utilization. Many machine learning models don't need an entire GPU's compute and memory capacity. In traditional serving setups, each model is assigned a dedicated GPU regardless of how much of the model actually use. A model that only needs 30% of a GPU still occupies 100% of it leaving 70% idle. At scale, leeways translate directly into unnecessary infrastructure cost and reserve provides a solution. It supports fractional GPU allocation. Instead of dedicating a full GPU for every model, you can specify the exact fraction each one needs. The model using only 30% of a GPU now requests just 30%.

And the remaining capacity is available for other workloads. One configuration change, no specialized hardware. And in the diagram, this example, you can see that your GPU utilization can jump from 30% to near 100%. The second problem with inefficient GPU utilization is that modern applications often serve many model variants that share the same architecture but have different weights. Most receive sparse, unpredictable traffic. Provisioning a dedicated GPU for each is economically invisible. Reserve supports model multiplexing. A single rec replica can host mult multiple model variants in GPU memory simultaneously. Loading and evicting them on demand by ARU catching. The router knows which replica has which model loaded and directs requests accordingly. So you can register hundreds of model variants but only keep a handful warm at any time. Low rank adaption is a natural feat for model multiplexing because all variants share the same base model weights. Only the small adapter weights typically less than 1% of the base model need to be swapped in and swap out.

This dramatically reduce both the swap latency and the GPU memory footprint. allowing a single replica to catch far more adapters than four models. The next problem is scaling inflexibility. Traditional machine learning serving force you to scale the entire pipeline. It treats CPU processing, GPU inference and CPU postprocessing as a single unit. So scaling for a CPU bottleneck weighs GPUs and scaling for a GPU bottlenecks weighs CPUs. Making matter worse, uh default Kubernetes HPA scales based on CPU or memory utilization, not request loading. It can see how many requests are killed and processing. So it reacts too slowly for bursty inference traffic. Reserve solves this problem by scaling each deployment independently. So prep-processing, inference and post-processing each have their own replica count and autoscaling policy scaling decisions are driven by request Q depths, not CPU utilization. When Q requests exceed the target replicas scale up within seconds when traffic drops, they scale back down after a configurable cool down which leaves no wasted resource.

You can see the right example. When GPU is bottleneck, we can only scale the GPU deployment. The next problem is development and production gap. In traditional machine learning serving, the code you write in development is like a simple Python script. It looks very different from what runs in production. For example, like you need Docker files, Kubernetes manifests, YAML, service mesh, configs, load balancers, monitoring stakes, forcing team to rebuild and repackage your application for every environment transition. With Ray surf, you don't need rewrite anything. You just write a Python file, a surf.py Pi Python file test it with one line Python a single command line and when you're ready for production you wrap it in array service yamo and coup control apply same code but just same uh just different command and all one line. The last problem is framework locking. So traditional model serving tools are tied to a specific machine learning framework or a specific cloud provider. For example, like TensorFlow serving for TensorFlow, torch serving for PyTorch, Smaker for AWS, and Vert.ex AI for GCP.

When your team adopts a new framework or migrates to a different cloud, the entire serving infrastructure has to be rebuilt. Reserve solves this problem. It is framework agnostic and cloud agnostic. It serves any Python code, PyTorch, TensorFlow. Because it's built on Ray, it deploys wherever Ray runs, any cloud and even on prem. You can swap models and frameworks, but the serving layer can still stays the same. Right now I want to spend some time to talk about Ray. Ray surfer is built on top of Ray and Ray is a unified compute framework that turns complex distributed AI workloads into simple Python programs. You can use Ray to run AI workloads on any hardware any infra at any scale. Ray was originally developed at UC Berkeley and any scale has been one of its major contributors and ray now is part of the PyTorch foundation. Here are the problem Ray solved. So building an AI application end to end requires involving and learning a lot of different frameworks from data processing training or tuning to reinforcement learning and serving.

Each has its own framework traditionally. Also every stage of the machine learning life cycle use a different distributed system. So glue code and operational overhead grow very very fast and there is a huge gap between development and production. Kodak works on the laptop needs a lot of rewriting to run on a cluster and ray is a solution. Ray provides a unified compute layer for air workloads. One API scales seamlessly from a laptop to infinite laptop with minimal code change. Ray opensource provide five native libraries cover the full machine learning life cycle under a single framework. Ray data for scalable data loading transformation and even batch inference. Ray tren for distributed training with four tolerance. Ray tune for distributed hyperparameter tuning. Ray surerve for scalable model serving for online inference. Ray reinforcement library for distributed reinforcement learning. And this old library share the same foundation. Ray core. Ray runs on Kubernetes, AWS, GCP, Azure, slurm, yarn or on premise.

So you can run ray at anywhere. Now I want to spend 30 seconds to teach you how to use Ray. On the left is a regular Python program, a function and a class. On the right, the same code with one additional decorator radar remote. That's all you need to learn to write a ray program. After adding the decorator, the function becomes a distributed task and the class becomes a staple actor. You can call that remote to execute radar get to collect the result. Now I want to spend some time to talk about rays architecture and we are going to use it later. This is what array cluster looks like in a very very high level. It looks very very similar to Kubernetes. They both are resource orchestrator but at different layer. Ray is process level orchestration. Kubernetes is container level orchestration. Array schedules Python function and classes. Kubernetes schedules containers. On the left you have the control plan, the head node. It stores cluster metadata and handles scheduling and autoscaling decisions. On the right you have the data plan, the worker nodes.

This is where your actual workloads run. And now let's talk about how to run Ray on Kubernetes. Here is our today's main character Kubate. So really focus on application layer and it handles process orchestration. Kubernetes focus on container layer and handles container orchestration. Kubri bridge these two layers is a kubernetes operator that manage the life cycle of ray applications on kubernetes. Kubernetes prepare resource to run application and ray decides how the application is going to use the resource. Google provides three API or you can understand the three CRD rate cluster for manage rate cluster we just seen before and you can run any air workloads in a ray cluster ray job for workloads like distributed training or batch inference. It creates a cluster, runs a job and cleans up array service which is what we are focused on today. It manage both the cluster and reserve application with zero downtime upgrades and high availability. Next I'm going to talk about key features in ray service.

The first feature is in place update for serving applications. Imagine you're running CHGt 5.3 on a ray cluster and you want to upgrade to CHVt 5.4 with in place update. You don't need to create a new cluster. You simply update a ser config in your ray service manifest and that's it. Kubriate detects the change. It sends an HTTP request to the Ray control plane API server and Ray surf handles the rolling update internally all within the same cluster. So no new cluster, no downtime, no wasted resource. We just need to update the config and apply it. The second feature is zero downtime upgrade. So when you want to upgrade the entire rate cluster during the zero downtime upgrade process coupra will creates the new rate cluster temporarily and waits for it to become ready. Once the new rate cluster is ready the kubernetes service here will switch the traffic to the new red cluster and finally kubra will delete the or cluster. The third feature I want to talk about is high availability. So survey models in production means you have to dealing with failures.

For example, a replica process can crash, a node can goes down or even the whole rate cluster crash. Restart the rake cluster can take a lot of time. Corporate plus reserve handles recovery automatically at every layer. Reserve helps data plans replica to restart. Kubriate help control plan and data plan nodes restart in a kubernetes cluster. Let's talk about autoscaling. So AI inference traffic is bursty and a service can go from near zero to thousands of requests in seconds. Incorporate with reserve autoscaling happens in three layers. Layer one is the race of autoscaler. It scales serve replicas based on request pressure, queue size and ongoing requests rather than just resource utilization. If the rate cluster already has a spare capacity, new repercuss quickly as reactors on existing ray nodes. There too is the ray autoscaler. So when the current cluster no longer has enough logical core resource to place low replicas ray as worker path because ray understand the workloads resource requirements it can scale the appropriate worker group for example GPU workers for the uh large language model deployment and CPU workers for preprocessing.

Layer three is the kubernetes cluster autoscaler. If the new ray worker paths can't be scheduled because the kubernetes cluster is out of capacity, kubernetes provisions new nodes. So these three layers work together. Surf layer scales the application. Ray scales the worker path and kubernetes scales the cluster by adding kubernetes node. I want to mention two points spec specially unlike the default Kubernetes HPA stepup while often skills on CPU or memory utilization reserve is natively request aware and scales based on QES ongoing request and if the building policy is not enough reserve also lets you define a custom autoscaling policy and corate right now I want to talk about is ecosystem corate integrates with the Kubernetes ecosystem you already use for scheduling. We support Q including MulticQ for multicluster job dispatching as well as Volcano, Apache Unicor, Nvidia's Kaiuler and a Kubernetes plug-in. Whether you need Gululing, priority cues or GPU sharing, Kubri works with your best scheduleuler of choice for observability.

Kubra exports Ray metrics to permissions and ships with preview graphana dashboards. So you can monitor cluster health, autoscaling and request latencies through the same monitoring stake you already have. And if you need to provide a running workload, we support pi spy integration for CPU flang graphs directly from the ray dashboard. Finally, I want to introduce a new feature in ray encub the history server. Many teams run rate cluster as epheral units. They spin out a cluster, run the job and tear it down. This saves resource cost. But when a job fails and the cluster is gone, you lose the ray dashboard, your primary debugging tool. The history server fix this. While a cluster is alive, it automatically collects cluster meta data and logs into persistent storage like S3 or GCS. After a cluster is deleted, the history server restores the dashboard from that data. So you can debug a terminated rate cluster as if it were still running with no reccluster resource cost. And the Kubra history server is mainly designed and developed by any scale Google GKE and Alibaba ACK.

Finally, please join the community. Please join our race lake. And here we have a lot of ray masters from NSKL can answer your question. Also please join Kamala's lake to ask more questions. They are in CNCF commanda channel. Thank you everyone.