Hi everyone, thank you for joining. I'm Aaya Garash, software engineer at Preferred Networks. Today I'll be talking about reality of providing shared service in multi-tenant Kubernetes cluster. I'll use Keta as a case study to show how we can achieve namespace tenant isolation even when the service wasn't originally designed for it. Here's our road map for today. First, I'll explain why multi-tenency is needed for our platform and why we choose our specific isolation model. Then we'll dive into KA and look at why we choose the pertinent deployment model. the main part of this talk on four kubernetes constraints which we have to solve at the end I'll come back to the full architecture and design lessons a bit preferred network is a full stack AI company we build everything from research motor down to custom AI chips called M& core we deliver this computing power through the our preferred computing platform a kubernetes based environment serving both internal and external researchers. PFCP is not an anonymous pro public service where anyone can sign up and create workloads but still we share clusters across different organizations.

So multi-tenency is our business requirement. They give us two constraints. Tenant credential must stay separated and fader must stay contained. If something goes wrong, this unlike simple misconfiguration, it should never become another tenants problem. That's why keeping a small blast radius is key to our design. Here's a major Kubernetes multi-tenency patterns. Kubernetes multi-tenency is a spectrum. On the local side, you have a shared control plane with names based isolation. As you move to the stronger isolation, you pay more for virtual clusters, dedicated nodes or physically fully separated classes. Of course, there's no single correct answers here. It all depends on your requirements. And for our perform, we combine two levels. default namespace isolation for everyone and optional node level isolation for those who need it. This give us a balance of flexibility within a single cluster and this is our actual tenency model. Each tenant owner root namespace and its owner can manage its child name spaces.

We use hier h high hierarchical name spaces called h and c to handle that structure and ensure things like resource quarter something like that consistency as some of you might know the kubernetus or hnc project has already been archived but it's essential for our platform so we keep developing our fk version The important point here is our tenant boundary is lived inside a single cluster. This means every shared component where it's a controller and existential APS server or network path must respect the hierarchy. If not, the isolation model breaks. So why did I choose KA for this talk? Because it makes a difficult part of multi-tenency visible. At PFN, we provide many services to our users. When we build an in-house component, we can design for our own platform from the beginning. But of course, KA is a open source. It was not originally designed for us. That makes it a good case study. We have to adapt it to our requirement. Also, KA touches many boundary at once both internal Kubernetes cluster and external cloud integrations.

Before we dive into the design, let's do a quick recap of how Keta works. Here's the architecture. Kai extend horizontal autocaler called HPA using Kubernetes external matrix. When a user creates a resource like scaled object resource, the keta operator reconcile it and creates a new HPA resource. The resource point to the ka matrix. If the event source needs authentication, ka gets the credential from the toga authentication results. Then the HPA controller periodically gets those metrics and handles the actual scaling. Yeah, you don't need to remember everything here, but just remember the K is a system of multiple components, not just one controller. This matters because it means that each pieces has its own identity, its own API surface and its own trust boundaries. So next let's think about the deployment model. Roughly speaking there are two options. The first option is shared ka installing a single instance and using kada building isolation features. The second option is a penant kada installing a separate instance for every tenants.

A first grounds the shared model looks very reasonable. It gives you one installation to manage and one upgrade path. On top of that, KA object model supports namespace level authentication. So if you look at the Kubernetes objects, shared ka seems like the better choice. But finally we choose pertinent kada and the reason comes down to the authentication flow. Now take a look at Kada diagram again and let's look closely how KDA handles authentication for event sources. KA supports many event sources like Promeademius, Kafka, Radies and so on and each one has its own authentication method. In our case we need AWS managed promotious support. So this diagram show how that integration works. As I mentioned, trigger authentication is a resource that define an authentication method. Um because it's namespace resource, the resource is clearly separated between tenants. So, but look at the diagram to see how the token exchange works. First, KDA operator use its own short lied cloud credential in this diagram green one and then operator use the key to query the metrics from the source.

However, when initiating the exchange process, bootstrap identity and the chain still belong to the operator process. This is where the actual trust boundary lives. Ka is designed to be tenant friendly as configuration can be scoped per name space and Albert can isolate the object. If you only look at the object method, the share still looks reasonable, but the situation changes depends on your event source. In our case, and in any case where the operator needs a federated credential exchange, the boost drop tokens that starts the chain belong to the operator process. This means if the operator is shared the trust boundary is also shared. That's why we choose pertinent cada to ensure that each tenant has own isolated trust boundary. So pertinent kada model solves the main identity problem. But we still have another four kubernetes constraints clusterwide single tone constraint aggregated APL authentication dynam dynamic namespace scope and traffic boundaries. So the rest of the talk I will explain the challenge and then show you the solution we use.

The first challenge is external matrix API. The problem is HPA use a single clusterwide API group external.case.io. Even if we install a separate metric server for each tenant, Kubernetes still expects one single front door for all external metrics. On the left, you can see the API service resource. This is where you register backend with the API server. You can have this API service resource for different APA groups, but only one back end can be served as a same API group. If you try to register a second one, it's simply overridden. And on the light side, the HPA resource has an ex type with the metric name. It has no mechanism to specify which back end should return the metrics. So even if we install a separate K and metric server for each tenant, we cannot register each one as an individual back end. The solution was to keep a single API service and put a router behind it and this works naturally because external matrix API path already contains the name space. So router just inspect the path extracts the name name space and forward request to the light tenant to the back end.

No custom needed. It's all in the URL. We implemented using NGX. As you can see, it's just a simple location match using a regular expression. This allows the front door to stay classified while the back end stay isolated per tenant. But once we place proxy in the path, we run into next problem identity. Before we talk about the failure case, let's look at the normal flow for aggregated API. When the HPA needs metric is sent to the cube API server, the API server authenticates the request as usual and then proxy to kametric server. Instead of forwarding the original beer token, the API server sends identity headers like X remote users and X remote group. The back end in this case kame metric server must trust the proxy not just request headers. To do this the backend server verifies the HP API servers client certificate in Kubernetes. These default setting such as CA boundary and header keys are distributed via config map named extension API server authentication and you can see cube cut and cube system name space.

Once the client authentication is approved, the back end grabs the user info from the headers and makes a call to the API server with a subject access lab to see if they are authorized. And now we can see what change when we put our router in the path. The L need to see HTTP pass to route traffic by namespace. So it the traffic terminates TRS and open a new connection to the back end. From the backend's point of view, the caller is now the router, not the cube API server, but the default certificate is fetched from the config mass. So client is still expect API server certificate. When this trust chain break authentication step fails and the collab fall through system anonymous user groups and the subject access review is denied. Of course the whole request fails. To fix this we have to rebuild the trust change. So we need these three things. First the router must present a client certificate when connected connect to upstream to the back end. Second back end must trust the router CA not the default cube APIs CA.

Third router must preserve the forwarded X remote headers without rewriting or dropping them. Since the Kmetric server is built like a standard custom metric AP server, we can configure all trust certificates and header keys using startup plugs. So we just tweak those flags to match our routed settings. Once those three pieces are aligned, the back end can see the original HBA identity again and also authorization works again. The third problem is namespace scope. Each per tenant operator should watch only namespace that belong to its own tenant. This is important for permit performance but even more important for security because the operator raise secrets for authentication. So we wanted to keep this scope as narrow as possible. The difficulty is that name space are dynamic. Tenant owners can create and remove them at any time. But Ka gives us a watch name space environment variable. But it's a static commas separated list and this isn't just a k limitation but most operator use controller runtime library and that library doesn't allow changing the watch scope after the manager stats.

So we need a way to keep operator scope align with the tenant hierarchy without changing K itself. Our solution was very simple wrapper process. We put the operator behind a simple namespace reloader. The reloader does three things. watches tenants name space update watch name space environment and it restart the operator when it namespace set changes the design constraint was to stay close the upstream manifest if we to from the original installation settings the cost of future upgrade grow quickly so we keep the changes to ka deployment manifest minimal just updating image and injecting several environment variables and now the watch scope stays ners and it stays aligned with the tenant hierarchy even as a name space changes. The last problem is the network boundaries. As you know, the default Kubernetes has no network isolation between namespaces. By default, any port can open to connect to any other port. This could be a security risk for multi-tenency. For example, if you look at red dotted line in the diagram, a misconfigure or even a malicious spot in tenant A can directly reach tenant B like metric server operators or even user workload.

To prevent this, we need to use network policy to block any expected traffic. Then how exactly should we design and configure those policies? Uh how should we network boundaries actually follow our town boundaries? Our first step was to classify the traffic and then identified allowed path. We grouped our component into specific scopes like tenant system and tenantwide. For ingress we allow only non sources like l or metric ser. For ingress we strictly limit traffic to tenants own name spaces and required shared services. Everything else including cross tenant traffic is denied. By the way, we use CDM as an our CNI driver and it has been such a huge help for us during this process because when I trying to verify my network design having a visualization tool is a very useful like find misconfigured policy and unexpected network flows that we missed in our initial design. Um once we map the traffic path finally we create a policy resources regarding policy management. Kubernetes building network policy is namespace scope resource.

So this means you have to define policy for every single name spaces. But of course it's harder to scale. Kubernetes official cluster network policy is still alpha and still not yet built in. So instead we use Celium cluster wide network policy. This is much more efficient because it allows us to define rules for an entire organization using namespace levels. This is especially helpful in our environment because we use HNC. So name space are often changed. Now we can pull all pieces back together. Plus a wide we keep one ABS service front door and one shared admission web hook. Per tenant we learn the operator metric server and namespace reloader. The router solved the single API problem. MTS and header request trust keep the caller identity intact. The reloader keeps the operator scope aligned with the tenant name spaces and network proc makes the traffic boundary for the tenant boundary. So to lock things up I wanted to show some key takeaways with an in-house operator you can design for multi-tenency from the beginning but vendor product and open source are different.

This usually means we have to write extra glue code or use some operational hacks to make it work. When we evaluate our shared service for our multi-enant platform, we check these three things. First, we look at operations. We need to make sure the service is easy to manage and listen efficient for our scale. Next, we consider reliability. We have to understand the blast radius and figure out how to protect our tenants from noisy neighborhoods if something goes wrong. Finally, we review security. We check if the service respect our security boundaries like reviewing its permission, network path and authentication methods. Of course, there is no single perfect setting that solves everything. Security is balanced between strong isolation and operational costs. You have to accept tradeoff and decide your configuration based on your actual business requirements. Thank you. So any questions? >> If not, so lap it up and feel free to ask me directly. Thank you.