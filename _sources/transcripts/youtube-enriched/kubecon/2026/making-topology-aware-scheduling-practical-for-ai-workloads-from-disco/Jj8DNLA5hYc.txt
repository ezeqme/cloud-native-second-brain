Thank you for joining my session and today I would like to give a talk give a session about uh making topology aware scheduling practical for AI workloads from discovery to simulation at scale. Uh I'm Wulan and a senior tech lead at docloud and today I what I discussed goes beyond the scheduleuler. It includes network topology discovery and cost effective approaches for function validations. So let's get started. As we know in model chaining scenarios uh networking communication volume is enormous. uh for conventional dance models such as llama or reduce communication uh uh dominates uh in this pattern data exchange typically occurs among the GPUs uh within each single network re as a result most traffic can be handled locally by leave switches in contrast models like deepseek roots data to different experts and generate intensive all-to-out communication. This traffic frequently traverses the appetire switches to reach destination across different rails. Uh consequently uh in a large scale cluster uh workloads must be placed in an orderly manner according to the topology to avoid the network con congestion.

In inference scenarios, a complex RDMA traffic uh patterns are also unavoidable. A typical example is PD disagregation, a prefilled and decoded workloads. Um needed to synchronize KV cache data. Uh so they must be placed on topologically closed nodes. uh this shortens the switch uh forwarding path and helps avoid uh traffic conflicts uh with other jobs. In addition, MOE model uh inference also invoice all toall communication often requiring traffic to traverse multiple layers of network topology. So related tasks should be placed uh close together within the same network domain and unrelated workloads should be uh spread out to uh minimize pass level traffic contention. Network traffic is further challenged by multi-tenant workloads. In a multi-tenant environment nothing is static. uh we are constantly driven by requirements like uh canary releases and SLA to maximize GPU utilization. We cannot afford to isolate resources for each tenant. Instead, um the cluster is treated as a single full pole. uh workloads from different tenants uh must be free to dynamically scale across all nodes.

Uh therefore the flexibility comes with a challenge if loads scale without topology awareness. uh network contention becomes inevitable uh or it can lead to unfair allocation of res network resources uh within certain parts of the topology among tenants to address this challenges we've discussed in in the past slides we need intelligence topologies scheduling it's no longer enough to uh just find the free slots we must find the right slots. We rely on three core strategies. I think first task isolation. We ensure that uh unrelated AI workloads do not uh contend with each other. If a job A and job B are independent, their traffic should never collide on the same network path as much as possible. Second, affinity. For affinity sensitive workloads, uh like the prefilled and decode ports, uh distance is the enemy. We make sure communication paths are as short as possible. Third, h health uh awareness. Network can fail and links can degrade. Our system should constantly monitor network health and triggers uh disaster recovering rescheduling when necessary.

Um um ultimately we should like to uh achieve several objects. um to guarantee job SLAs by minimizing communication latency and to minimize uh ROI ensuring that our uh expensive network infrastructure is fully utilized and to ensure network resource a fairness among the tenants and applications uh based on the native Kuba scheduler uh In some scenar uh simple scenarios, someone might consider using note label combined with pol note affinity to controls scheduling. Uh I think unfortunately um this approach is fundamentally uh as unsuitable for AI workloads. Uh first reason manual orchestration overhead. uh node affinity requires uh us to manually map physical topology uh to logical labels. This depends heavily on human planning. As the cluster grows, static partitions can cause conflicts between applications and operational costs quickly um uh escalates. So second reason topology drift labels are static metadata. They have no awareness of dynamic physical infrastructure. If a network cable is removed or a switch is under maintenance but the label isn't updated in real time.

Uh the scheduleuler is operating on a updated map. Third reason lack of hierarchical global view. Node affinity is essentially set based matching. It only knows whether a pod belongs to or does not belong to a node. This flat view cannot represent a global network topology or quantify complex architecture. As a result, users often cannot uh define node affinity rules that truly reflect the physical network. Final reason G scheduling. It is a all or nothing game. Without scheduling the entire group simultaneously we may end up with the with uh partial allocation. Uh some resources are locked while others are missing. It leads to uh resource deadlocks across the cluster. On this slide, we outline the core requirements for topology aware scheduling. First uh hierarchical topology view. The physical network is modeled as hierarchical key uh tree covering both scale up and the scale out architecture from smallest to largest. The typical hierarchy is node and super and rack and spine. Second hard and soft topology constraints.

First uh hard constraints are sh all parts of a job must stay must stay within a specific domain. If less uh no uh sufficient nodes the scheduling stays pending soft constraints are flexible. The scheduler tries to pack pause tightly in a domain like a rack and only spills over to adjacent domains if necessary. Third, optimize the switch paths for communication for workloads like uh uh mo inference or PD uh aggregated tasks. We should place ports clo uh close together within a same domain. In the open source community, uh, Volcano, Kai scheduler and Q already support the topology scheduling. On this slide, we have Q and enhancement over the native cube scheduler. Q acts as a gatekeeper for job scheduling. We could use node labels to define topology. They are the that replic uh hierarchical network topology. Each cluster uh each cluster node is labeled accordingly. So Q can understand the logical topology. We can specify hard uh hard or or soft topology constraints using um annotation and labels uh in the workload YAML.

When a job is submitted, Q first calculates the remaining capacity within the relevant topology domains. If sufficient resources are available, Q injects node selectors or port affinity to port template. This guides the native coupuler to make placement with decisions that respect the topology constraints. Now we are uh let's look at the volcano. Volcano serves as a replacement for the default coupe scheduleuler. It builds a logical topology chain using hyper node CR. We can manually assign labels to nodes for each switch tire of the topology and volcano will automatically sync these updates to the hyper node instances at periodical intervals. When uh submitting a volcano job, we use the network topology field to specify a topology constance. I think a key advantage of volcano is the integration of infinib banders infini band topology discovery. uh volcano can interface directly with the infinibus UFN uh to automatically pro the network topology. This animate this um eliminates the manual effort of maintaining note labels.

On this slide, we introduce Ceduler uh developed by Nvidia. uh it's a a replacement for the default kuba scheduler castuler use uh topology CRD to understand how a network is uh structured applications can define topology constance either in port group CD or directly through port annotations. This constraints can be specified as required replacement or preferred replacement. A key feature of K scheduleuler is subgroups within a port group. Subgroup allow multiple sets of ports to have their own independent topology constraints while also enabling a groupwide constraints across all ports. This ensures that uh uh affinity sensitive workloads are placed uh uh close together. Uh it is especially suitable for prefield and decode workloads in TD um disagregation. So let's look uh let's look at this u typical topology example that combines with scale up and scale out networks. I show in the diagram within each GB200 every 72 GPUs are interconnected via NV switch to form a super port. Nvidia refers to this as multi node NV link domain or NVL domain.

This super are also interconnected through rack and a spine switch uh forming a scale out fabric. So the topology hierarchy here from smallest to largest is node super port rack and spine. Now suppose we are deploying a group of vm inference instances as show in the yamo on the right of the slide. We can apply a hard topology constraints to ensure that the Intel workload uh stays within a single rack domain uh regardless of how it uh scales. And we adopt a bingpack strategy and apply soft topology constraints to keep instances tightly grouped within a few uh adjacent super port. This allows communication to uh fully leverage performance advantages of of envy switch within the super port. As a result, all ports are deployed in a uh structured manner. So far we would discuss the advanced uh scheduulers. They are powerful but they are all share one critical dependency is a node label for each topology layer. These node labels represent the logical mapping of only network topology hierarchy. If the labels are wrong the scheduling decision will be wrong.

Humans cannot uh reliably uh interpret complex network topologies. It is not reliable to manually label node at scale. That's why we need a topology detector or builder. Think of it as a bridge uh between your physical network infrastructure and Kubernetes. It runs inside the cluster and solves two key problems. First, network out discovery. It connects direct to external systems such as infiniband uh fabric manager or unlink manager uh tools and automatically discovers how the network is wired uh from scale out switch down to scale up GPU internet connects. It builds an accurate topology view and it tells us uh the position of each node within the uh different network hierarchy levels. Second uh network health awareness. It is not enough to know a link exists. We need to know uh it works. If a cable is uh degraded or a switch port fails, the detector should identify it immediately and update node status accordingly. So theuler can make the right decision. Now let's look at a topology detector solution from Nvidia network environments.

Nvidia topograph it it automates the Intel workh workflow. It supports four Nvidia networking stack. When you running uh Rocky on spectrum switches or infini band on quantum switch or leveraging envy switch for scale up connectivity. It has availability across all fabrics. It continuously talk to network infrastructure. It builds an accurate topology map and then it automatically applies multiple topology labels to each node. Each label represents the nodes at each level of the network uh hierarchy like super pod uh rack and the spine. uh because this process is automatically and uh synchronized with uh the uh with the fabric the labels stay accurate. So scheduleuler like a cast scheduleuler can immediately uh make right decision of topology aware scheduling. So what if your network hardware comes with vendors other than Nvidia in a nonvidia network environments? Uh we may need to develop a custom a custom topology builder from the scale out network. Many problems many modern switches uh run Sonic OS.

uh topology builder can query the switch via GMI protocol to collect all the network neighbor information for the SC up fabric. Each GPU vendor rep uh provides CLI tools or API to expose domain ids. By collecting these uh ids we can and determine which GPU are physically interconnected by scale up network. Therefore they can build an accurate topology map and apply multiple uh topology labels to each node for the scheduleuler. Now we are facing um validation challenges across multiple scenarios. First scenario for uh scheduling decision. For instance, after deploying and configuring the scheduleuler, we need a clear we need a clear availability into its scheduling behavior. And uh second uh like scenarios for network topology construction. For instance, if we want to develop a custom topology builder, we need an environment for developing and testing. So, we need a uh network environment uh environment with rich and uh realistic topology layers uh covering both scale up and scale out architectures.

However, it may require a dozens of uh switches or GPU servers. Uh the cost is prohibit prohibitive prohibited. So when it comes to uh validating scheduulers like Kululer uh we can now how we can access a GPU cluster uh with a multi- multi-layer network topology. My answer is cork. It is it mean it means kubernetes without kubate. Functionally uh cork simulates the behavior of the kubate. It allows uh developers to generate thousands of virtual nodes inside a can cluster within seconds. These virtual nodes can be configured with arbitrary resource specification such as GPUs or memory uh regardless of the acture the actual host hardware. When a port is scheduled to a cork node, C clock cork immediately updates the port status to running but no act uh actual containers are uh started. The mechanism operates entirely at the control plane level. uh with capac this with this capa uh capability we can label the virtual nodes with topology tire uh attributes such as spine or rack. So we uh we deploy Kai scheduleuler in the kind of cluster and submit simulated inference workloads to this virtual nodes.

This allows us to accurately val uh validate the scheduling results. When we are developing and testing a custom topology builder for nonvidia in uh network environments, the the builder needs to actually communicate with switch at different network uh layer and construct the network topology. This means we must have real switch uh available. So how do we obtain such a kubernetes cluster? My my practical solution is Nvidia air. Nvidia air is a free s platform to simulating network infrastructure of data center. It it allows us to deploy Linux VM and virtual Ethernet switches with sonic OS or cumul cumulus OS. So we can build a multi-layer network topology exactly as we desire and deploy Kubernetes directly on top of it. We can deploy our custom topology builder on the cluster. It can communicate with the switch to collect uh topology information and generate hierarchical note labels. Uh in addition, Nvidia Air provides APIs to autom automatically create infrastructure based on userdefined topology configurations.

This makes it easy to uh to build a CI pipelines for automatically testing. Okay. Uh uh that's all of my presentation and thank you. And catch you.