Good morning and thank you for being here. My name is Tommoakamra from Corporation and here is my co-speaker Moto Hiroka. Today we want to talk about generalizing command control sharding. This is not a story about clever trick. This is not a story about the performance hack. Our core message is this sharding is not a feature to it is an architectural decision about where you place your replication boundary. At that boundary determines whether determines whether your system scales or quietly cores. We walk through five parts. First why scaring broke horse. Second, what shing can and can't not solve. Third, why our first architecture failed. Fourth, how we pivot it. And finally, the reasonal decision framework we ended to up with. This is not just our story. It is a reason model you can apply to your own controllers. Our team develops and operates a command based internal pass platform at Li Corporation. This diagram shows an overview of the pass environment we provide internally. It consists of a single control control plane Kubernetes cluster and many data plane Kubernetes clusters.

Internal users define up resource in the control is in the control plane cluster and through it they manage resource such post learning in the data brain clusters. My team operates at roughly 66,000 ports 5,000 nodes 3,000 name spaces and 500,000 managed object. At this scale we started observing pain not out of memory not classes instead through initial syn through initial synchronization during restart longer reconcile latency delayed user visible proication. The system was not broken but it was approaching a structure limit and we knew vertical scaling alone would not protect us forever. Let's quickly review how most Kubernetes control are structured. Most controllers follow the same model, shadow inform cache, single active reader and standby replicas. This design is extremely efficient for vertical scaling, one process, one shared cache, minimal deplication, but it is not elastic horizontally. Reader election ensures only one replica reconciles. So when reconcile facial increases you cannot just add replicas and expect throughput scale controllers are vertically efficient but not horizontally asterric that as a structural constraint uh I don't know actually uh how many people are They are struggling with Kubernetes reconcile delays caused by large number of resources but we currently are so we choose Kubernetes contra sharing KCS KCS is an opensource project developed by team EAD you can find it at GitHub its goal to make Kubernetes controllers horizonally scalable KCS works by computing shard selection from a hash of resource identity.

For example, name space name and parent identity for controlled resources. Using res to determine shared ownership, assigning object via raers. Keeping parent and control resources on the same shard. supporting brain during rebirthance. Let me know let me now explain how KCS actually assigned SHs. KCS uses a consistent hashing ring. Each SH gets five 100 B nodes dist distributed across the ring. When a resource is created, we compute its key, hash it with XX hash and use binary search to find the next token on the ring. This approach minimize reverbancing when SHs are added or removed. The key format determines how we compute the hash for main resources like app. The key includes group, kind, name space and name. For control resources like up child A or child B, the use key uh the key uses the parent's name instead of the child's own name. This ensures parent and child are always assigned to the same shs obtain the parent resource name from child's resource owner reference. This is why owner reference with control tool is essential to design the first attempt.

We had to start from one implementation constraint. KCS computes child SH assignment from a parent based hash input such as parent name space and parent name. A child object can use owner reference to resolve that parent identity but a grandchild cannot always resolve the grandparent identity needed for the same hash input. So same shared collocation is natural for parent and child but it does not automatically extend to deeper chains. A pass has complex resource dependencies. It seems that making it difficult to neatly deco compose them into a simple parent child relationships. So our design hypothesis was manag manager isolation by name space could cover dependencies beyond simple parent closure. That constraints shape the next hypothesis. Given that limitation, we shifted from object level corocation to namespace level ownership partitioning. At that point, that still looked practical. At that time, app related resources appear namespace closed. From dependent perspective, links did not cross name spaces.

So, name space look like a natural shadow boundary. My hypothesis was simple. If we spread reconcile ownership by namespace, we could reduce synchronization pressure while keeping product behavior unchanged. Conceptually, this looked green. It looked generic. It looked the user. Our first architecture used namespace oriented sharding. When we decided to adopt namespace level isolation, we faced two architectural choices. Option A, run a dedicated pod per namespace. This is a typical pattern you might see. Option B, use caches to sh by namespace with one pod handling multiple name spaces. Why did we choose option B? We expected that by using KCS nameace charting, one port could handle multiple name spaces and be cheaper than the port per namespace approach. But first, why did we needed to fork adapt KS? Because our internal passive dependency class could not be partitioned only with that parent based ownership rule. We had dependencies that required deeper chain consistency beyond parent child. So to isolate the full operator domain we were forced to use namespace level manager isolation.

We fed KCS and we implemented a manager per namespace design. The approach is to start a controller runtime manager for each name space and provide dedicated controllers per name space. For example, control A, B and C each watches the name spaces assigned to their respective SHs. And when name space is assigned to them, they start a controller controller that maintains the C scop only to that name space. On paper, this looked clean. Our initial expectation namespace shing would be more efficient than running one part per name space. In practice this architecture might have worked if it weren't for the spec fix of our path by maintaining a fixed number of controller replicas and adjusting them according to changes the number of name spaces. We could have achieved horizontally scalability but the reality was different. The improcess manager instantiation cost turned out to be roughly equivalent to the port per name space cost. Ning multiple multiply the already multiplicative structure practical overhead for exceeded expectations.

After roll out the appeta entered OM research loops go routines increased from 3,500 to over 500,000. We observed CPU still. We observed memory saturation at that time name space was 3,000 apps 11,000. This regression did not exist before shing. So shing trigger the amplification that forced to us to stop and measure. We moved to repeated validation measurements. We measured people of heap use multiple times in controlled environments. The simplified model we derived was this formula. The dominant amplifier was namespace correlated growth. Its additional namespace increase manage instances. Each manager instance added its own inform and cash structure. So against the namespace count this was rhina growth. The problem was that namespace count was not something operators could practically cap and at large end absolute resource usage became much higher than expected. Remember our operator was already multiplicative. Before shing we run base manager plus cluster manager times n we around 40 data plane clusters that was 41 manager instances in one process before after shing namespace shing created a manager for every name spaces each of those managers replicated the same structure so the must game before shing we had about 40 manager instances the same number as our data frame kubernetes clusters after shing 3,000 name spaces times 40 manager pop name space equal 120,000 manager instances from holy to 120,000 amplification factor times 3,000 the structure was already multiplicative Name space sharding added another dimension to the product and cause a mistake.

We underestimated the unit cost of each manager instance. This estimate error amplified 3,000 times was catastrophic. Now let's generalize command semantic single name space or all name spaces. There is no native arbitrary name space set watch primitive. This contra mult namespace shing designed in general. However, in our architecture, the primary explosion came home from cache and manager replication. The API constraint explain the outer boundary but the inner explosion came home cache duplication. two layers different programs both matter. A quick note about recent developments just days ago on March 17th K566 was merged into Kubernetes server size shed list and watch. This enables hash range sharding for rest and watch operations. For example, sector equals shard range object metadata UID08. This partitions what streams by UID hash value ranges. The initial implementation supports UID based shing name space based shing is planned for you sorry planned for future versions. This addresses the old or single contained from a different angle.

It's an alpha future inversion 1.36 behind the shed list and what feature gate command control shing shadow behavior runs on parent space hash inputs. Children can follow parents through owner reference but dependent cannot always derive the required ancestor identity for the same hash key. So kum control sharding is not a full graph partitioner for a arbitrary dependency depths. Kum's control sharding is a distributed ownership layer. It is not a distributed cache framework. Config those layers lead to incorrect architecture. So, so we pivoted we split the monos monosic operator into app operator and deploy operator. All resources is concideed by the deploy operator are assigned to a data plane cluster. So we can sh them by data plane cluster. However, the remaining resources that are not yet assigned to any data plane needed to be partitioned using different dimension and we adapted two design principles. First work within Kubernetes API design principles. Our first attempt fought against Kubernetes watch semantics.

Namespace watch is all or single. At that time we validated this architecture. There is no arbitrary namespace set primitive. So creating a manager per namespace meant creating a full inform set per namespace that was fighting the API. In the pivot we use ravel based shing instead. This is a kubernetes contra sharding default. Rael sectors are native to kubernetes watch and list. They work within the API not against it. Second keep manager count per process fixed. Manager start app is expensive. Each manager carries informers, caches, go routines. If manager can't scale with an external dimension, cost becomes um predictable. In our first attempt, manager can't scale with namespace count. 3,200 name space times 41 managers equals 131,000. In the pivoted architecture, app operator uses label based sharding. One manager handles all name spaces. Manager count per pod is one. Deploy operator shs by cluster. Each port manages exactly one data plane cluster. Manager count per pod. One base plus one cluster manager.

For app operator we applied KCS only to the app center domain app and its child resources app B and others. This domain fits KCS assumptions. Parent child relationships are clear. Ownership is well defined and importantly cached replication remain bounded because we did not create manage per name space. Now let's look at deploy operator. Deploy operator takes app resources and created deployment service and other workload resources on the data clusters. Because the pivot one operator port contains the base manager plus one cluster manager for every data data plane cruster. Operator to cluster manager was one to end with 40 clusters that was 40 cluster managers sharing one process or caches or informs or recon loops in one pot. As a pivot, each operator instances manages exactly one data brain cluster. Operator to cluster manager is now one to one. The dominant cost driver per part P changes from n clusters to just one. Cop is bounded per instance and where blast radius is limited to one cluster. This made cost predictable.

Predictability is a form of scalability. So did the pivot actually work? Let us look at resource usage across three architectures before shing everything run in single reader port concentrated but bounded. In the first attempt name space correlated amplification P as to OM in the pivoted architecture we run multiple CH ports. Each shard carries the best overhead manager startup in former baseline but the total resource usage across all SH is comparable to sharding levels. No name space correlated explosion, no OM regression and because uh sorry no uh because SH operates independently fail independently fra is bounded a single shadow reset does not cascade the console latency improves the system has been running stable. in production since the pivot. The pivot worked and because it worked, we could observe new edge cases that only appears at scale and the real traffic. Let me walk through two of those. To explain this issue, let me use a familiar analogy. Think about deployment and replica set. Normally, a deployment creates a replica set.

The replica set gets an owner reference pointing it back to the deployment and case the child's sh is computed from the parents identity via that owner reference. So parent and child runs on the same sh reconation works as expected. But what if a user creates a replica set directly without a deployment? That replica set has no owner reference. So no shad is assigned to it. No sh is set and controllers only reconcile resources that have a sh assignment. So that replica set is reconciled by no one. Nothing happens silently. This is exactly what happened in our past. App creates an app child resources. Normally owner reference is set. Same sh. We had a pattern where an app child was precreated before the app existed. No artifense, no sh assigned. No control reconciled that app child. It simply sat there invisible to every child. The fix was adoption logic. But there's a challenge. An app challenge without child assignment isn't in the cache. So we had to use API reader to query the API server directly. Once the owner ref is set, KCS reassign the object to the correct SH.

The principle is clear. Sh filter reconilation assumes ownership alignment. If any object can exist without a proper owner reference, you must design adaption logic before enabling sharding. In practice, this issue stem from the design of KCS. If we are to design our own sharding logic, we should do so in a way that avoids this pitfall. Maybe K5866 solves this problem. Another issue appear during rolling updates. In KSH is identity by its p name with a deployment. Post names are random. Now when you do a rolling updates, new ports get new random names. The shard identity is changed. Every sh every roller changes who each sh is. That sounds harmless but is not. Every managed object carries a shel. This that driver says which sh owns it. When sh identity change all of these ra must be rewritten. In our case over 11,000 objects needed rael updates at the same time all at once during a single roll out. This is a red storm hitting the API server and at our case that cause API server overload. The fix was simple with a state set pod names are ordinal best after rolling updates the then the name stays the same sh identities are preserved no reli needed there additional API server load we move app operator to statet and tuned reader election related settings sh identity must be operationally stable at scale this is not optional.

We end with five general principles. First, respect API semantic worked within API design principles. Use label pair section respect what semantics. Second, fix manager comp process. Do not let it scale with external dimension like name spaces or clusters. Third, shadow by architecture. Choose your shing access based on your architecture dimension ratio. Fourth, stabilize sh identity. Design rollout. So sh identity does not fluctuate. And fifth operation risk comes first. Evaluate operational risk as a first class concern. This principle generalize beyond our specific case. Now let me connect back. Now let me connect back to this our case. Our conclusion reel P over name space hold from one simple fact. Name space count was 3,000. F count was holy. Name spaces dominated. If we had shed along the name space axis me count would have multiplied along the along the largest dimension. But if your architecture is opposite clusters for outnumber name spaces then name space level isolation could actually be cheaper.

The shing method is not universal. It depends on what dimension dominates your architecture. Shading is not a feature toggle. It is a choice of replication boundary that works within Kubernetes API design principles and keeps per process cost predictable for your architecture dominant dimensions. Thank you.