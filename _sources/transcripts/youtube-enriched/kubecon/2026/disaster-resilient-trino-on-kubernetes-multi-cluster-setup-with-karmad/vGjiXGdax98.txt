All right. Hello everyone. I'm S and I lead the manageo and iceberg platform engineering team at Bloomberg. >> Um I'm Antoine and I'm a engineer in the manage uh Trino and iceberg team uh at Bloomberg. >> All right. Nice to meet you all. Thanks. Thank you all for coming at this late hour today. >> All right. So last year at KubeCon we shared how we built a secure govern platform on Kubernetes by leveraging open policy agent a CNCF project. Over the past year as adoption grew the next set of challenges became very clear. Data governance was no longer our bottleneck. The real challenge had shifted to building a highly available data platform on top of our trainer clusters. That shift pushed us to evolve the platform from a single cluster governed a governance focused architecture to a multicluster deploymentbased design where high availability and data disaster resilience are first class concerns. So today I'll set the context for that evolution and Antoine will walk us through how we built uh and designed this next generation Tuno platform.

Our managed Truno platform serves two primary personas. On one side, we have the Truna service owners, which is typically an engineering team that is defining and managing a Truno service, configuring the cataloges that they want to attach and the resource limits they want to launch the pods with. On the other side, we have the query user who are the analysts, engineers or applications that connect to a query endpoint and submit SQL queries. For both personas, the expectations are very clear. Trina service owners expect predictable life cycle management and query users expect a reliable query endpoint. In our previous architecture, meeting those expectations required those personas to coordinate those failures. The core challenge with Trino is that queries are stateful and tied to a specific coordinator. If that coordinator fails, the query doesn't transparently fail over. It is simply lost. So availability isn't something we can make perfect within the Trina system. But what we can do is to make failures predictable and make recovery and failover of responsibility of the platform instead of leaving it to the users.

Let's briefly recap the single cluster architecture we shared last year. On a high level, Trino service owners express their intent to deploy Trino onto a cluster through an API. And within our platform, that intent is represented at two levels. The Trinos service owners interact with a simplified abstraction called Trinoservice which captures highle intent like the catalogs you want to attach the resource configurations you want to launch the pods with and the platform operator materializes that into the actual runtime coordinator workers and networking configurations through a lower level CRD called the truno deployment. So within our model, the platform owns the deployment complexity while users interact with a simplified API. Once the Trina deployment is running, a query user connects to a single query endpoint exposed by the cluster. Every query request is valued by an open server running inside the cluster which retrieves policy bundles from a central policy store to authorize access. From a security perspective, this model works very well.

But in this model, the platform did not yet factor in availability and resilience into the abstraction. Now we'll see what this means for the two personas. This means that when the cluster is unavailable, whether due to infrastructure issues or maintenance, the entire data platform was impacted. And that's a challenge that needs to be addressed when you start relying on Trina for production critical workloads. It's important to note that even in this uh phase of architecture, we did provide clusters in two availability zones. So, Truno service owners if they really wanted to could deploy their Truno services in multiple zones if they wanted disaster resilience. That also meant that the query users would be able to continue submitting queries against the Trinopod even if one of those zones were unavailable. Unfortunately, this meant the responsibility of achieving high availability and disaster resilience were pushed to these personas and this is because when a turn of service is reconciled, it exposed a zone specific endpoint.

You can see here that the endpoint is returned is return uh returned is tied to a specific zone. So if a service owner deployed into two separate zones, they would retrieve two separate endpoints. And that meant that DR and HA existed if Trina service owners and query users implemented it in their own applications uh themselves. So instead we'd want a TRO service status that looks a lot more like this which returns a unified Trino endpoint regardless of which zone the actual underlying Trin runtimes are launched into. And that meant that our architecture had to evolve. We wanted to evolve from an architecture like this where each persona operated against it own specific APIs, right, into one that conceptually looked like this where they interact with a single unified Trino API. And under the hood, multiple availability zones still exist, but failover, coordination, and high availability should be handled by the platform and not by these users themselves. And in this state, multicluster resilience becomes an implementation detail that is fully owned by the platform.

So how did we actually build this? Well, as the title of our talk suggests, with chromatada and with Trina gateway. Now that we understand the context behind uh the drivers that actually um prompted our platform to evolve, Antoine will now walk through how we designed and built a platform that abstracts away availability zones and provides high availability and disaster resilience by default. >> Thank you, Sun. Um okay so soon walk you through the problems um tied to a single cluster infrastructure. Uh now let me walk you through how to remedy to this. So we introduced the trino API server. So the trino API server is a stretch at CDbacked API that served as the single source of truth for all the trino resources. When a user creates a Trino service on our new federated endpoint, the service provider routes the request to one of our cluster. Since they now share the same stretch Z back end, both cluster are aware of the newly created resources. So we run multiple replicas of our Trino service controller uh for high availability but only one of them actively reconcile at any time through Kubernetes leader election mechanism.

So if a leader goes down um another replica picks up the lead and continues. So we achieve resilience without duplicate work. Our controller will then like create what we call a Trino deployment CR and we need to deploy it down to our stack into our Kubernetes cluster that physically runs the Trino pod. And so to do this we rely on Carmata. Um, Cormada is an open- source Kubernetes management system that enables you to run your cloudnative application across multiple Kubernetes cluster or cloud without change to your application. Um, Cormata is also a CNCF incubition project uh with maintainer from a number of organization uh including Bloomberg. Um so last year at CubeCon China um we presented how we use Cormata across the organization and our Trino platform is one of the key adopter of this technology. Okay. So on this slide um we can see that the Trino API submit the Trino deployment into Carmata. After defining what the propagation policies are for this Trino deployment CR, Cormada will submit the Trino deployment into the member cluster in order to build the Trino pods.

Cormada continuously monitor the state of this of those Trino deployments and if a Trino deployment in a member cluster becomes unhealthy whether it's due to node failures resource pressure or any other issue the controller detects and attempt to recreate the Trino pods. So, Cormata is really the orchestration bridge between our declarative Trino API layer used by the user and the actual multicluster runtime that runs the Trino pods. So, here is an example of a propagation policy. So, it's a chromata resource that tells the Cormata control plane how to distribute Kubernetes resources to member cluster. um think of it as like routing roles for multicluster deployments. So in that spec we are distributing um trino deployments CRS and our approach is to duplicate it across all member cluster. So all member cluster run tropods. So one important piece of our Cormata integration is how we aggregate the status of each cluster. For this we use the resource interpreter customization. So we create a lure script that teacha how to aggregate the status from the different trino deployment across all member cluster.

Okay. So to sum up sorry to sum up the tree the trino service uh owner work uh workflow. So the user creates a trino service um into the tros uh and submit it to the trino api. Our controller creates a trino deployment and submit it to karmmata and then kmata fans out the trino deployment to the member clusters in order to create the tr pod. Okay, so now we have Trinopods um on each data center and here comes the query users uh and they want to query these clusters to access data and so for this this is where we introduced the Trino gateway. This is how users actually connect and run queries. So, Trino gateway is a load balancer, proxy server, uh, and configurable routing gateway for multiple Trino clusters. So, it's backed up by a database that stores the key information that the gateway needs. Um, it doesn't store data, it's just store configurations. The Trino gateway is also exposed through a filler endpoint similar to the submission endpoint. Our service provider routes the request to one of the data center trino gateway pod.

The trino gateway pods then intelligently routes the request to one of the trun pods. So under the hood the trino gateway handles dynamic backend management. So as trino uh pods come and go scale up scale down um disaster happen cluster get upgraded um the gateway automatically registers and they registers them. All right. So that that's a great feature. But so how do we integrate uh the Trino gateway into our stack and how do we register Trino services to the Trino gateway? So when a Trino service is created and it's pod come up healthy the Trino service controller will create a Trino registration CR. This tells the gateway, hey, this I'm a new backend and here's my address. So, this registration will be stored in the backend database that configures the gateway. So, the Trino gateway pot can access it and start routing queries to it. All right. So, now that's the point of this talk. It's like let's walk through uh some component failures one by one and see how the system responds to them. So if there is a gateway failure in one of our data center, our service discovery detect that the gateway in data center 2 is unhealthy and start routing traffic uh only to data center one.

An important an important things to see here is that the surviving gateway in data center one can still dispatch queries um to the trino pod in data center 2. Only the gateway pod in data center 2 is down. So we only lost the gateway but not the compute behind it. So now let's say that a Trino pod fails in one of our data center. Now the Trino gateway detects it and detects that the data center one um part is in unresponsive and automatically routes query um to the Trino pod in the data center one. Once the cluster in data center 2 recovers, the trino pod in data center uh two recovers the gateway rediscover it and resume distributing queries across both data centers. Okay. So now let's say there is um a Trino API server failure or even worse let's say the whole infrastructure is down everything burned um it's not ideal um but what we're going to do is that we're going to bring back the nodes we're going to bring back all the pods up and because the underlining data lives in the external stretched CD cluster which is independent of of our API server no state is lost.

The API reconnects to CD. Our controller resync all the resources and the system resumes operation as if nothing happened. So what are the key benefit for the user? um for trino service owner um they can deploy a single simple manifest definition and they can do it against um a unified trino API endpoint. The trino services automatically recover from cluster failures and the state is persisted in an external stretch service for rapid recovery. For query users, they also get a single endpoint uh via the Trino gateway and this endpoint is highly available since query are automatically routed um to the healthy clusters. All right. So we still this is like our our current release but we still have like a lot of work ahead of us and and we're planning to extend our dual DC architecture to additional data center uh in order to increase fault tolerance and uh geographic distribution. Um we're also planning uh to work on optimizing our resource allocation. So trino warper pot as tatically defined uh today via an algorithm that we built.

Um so we're working towards uh HPA driven uh scaling that adjust workers replica based on query allow query load CPU utilization um GVM metrics uh allowing cluster to scale up uh during peak demand and to reclaim resources during ideal period. Um, and that's a discussion we just had, but we also want to start logging some queries uh via the event listeners and to capture and persist user uh query metadata. And lastly, we want to work on um resource group enforcement to control query concurrency limits and queueing behavior. Um so the the idea behind this is to prevent a single heavy query to monopolizing the the cluster resources. Um yep that's it for our talk. Um happy to answer any question you guys have. >> Sure. >> Yeah. Can you start here? >> Thank you for the presentation. Could you please uh uh tell how do you roll out updates? Uh I mean like without down times do you update firstly one cluster then another one? And uh second question I'd like to ask uh why do you deploy trino gateway in each cluster?

Why not to have the one gateway? >> Yeah, that's such a good question. Um I'm so glad that we're thinking along the same lines with the community here. Um with this current release, we actually leave the uh responsibility of managing blue and green deployment entirely on them. So there's another layer of duplication they're going to have to uh own on top of this um this deployment because our current architecture doesn't factor in it. So if they really want to introduce a change, test it out against the same routing group first in the train gateway, that's on them. But we do have an item later this year where we want to look into how we can leverage crata propagation policy or other tools that are within the landscape to see if we can do exactly that, which is to deploy it to one side first. Uh define some rules to make sure that there is going to be one site that is still active and healthy, right? before we continue to roll out the change to the other zones as well. >> Yeah. >> Hey, hello. Can you hear me?

Uh I wanted to test how we do automated tests of disaster recovery. Do you use any frameworks for this like terror test or how you validate that entire disaster recovery works? >> Could you repeat the question? Sorry, there's a lot of echo. Yeah, >> sorry. How you ensure that disaster recovery in V works? Do you have any automated tests or automated frameworks for this? >> Yeah. So we do have um kind of scheduled end to end tests and release based end to end tests. U by your question did you mean like when when we bring back up a cluster we're verifying that u a zone is still active in the way uh we need. So, Truno Gateway actually already has um um a baked in application logic to detect when a specific backend is healthy or not. So, within Trino Gateway, it actually has the application logic to understand if a back end is healthy enough for the queries to be routed back to it again. That's that's the logic we're relying on for that. Hello, thanks for the talk. How uh you protect against the karmmod cluster being out for example if the cluster karmmada who's karmmod if it's going down the control plane I mean karmmod cluster if it's going down what do you do >> so I guess the the question is like what if kmata goes down right >> yeah yeah >> so so so karmmata at bloomberg uh is a managed service so we do not as a trino team um manage directly Cormata we're using them as a service and they provide SLOs's on their side that we're using their prod um >> but do you know how they do it >> I'm sorry >> do you know how they do it >> how how they do it uh that's going to be a great question for the Carmata folks they're in they're in the room by the way so uh feel free to ask them like uh at the end of this talk >> from from your diagram I understood that as uh Trino service owner my interaction is with the top cluster with a stretch of CD but the deployment is actually done by Carmata in multiple clusters.

How do you transmit if something goes wrong with the deployment of the Trino service in one of the clusters? How do you reflect that information all the way up to my interface which is the top cluster? So, so the the the way the flow works is that once it's down there and there are statuses, our lure script is taking care of the aggregation layer. So then Cormata reflects this in the status of the Trino deployment and then our controller as a watch on Cormata API server to every time there's an event that is kicked off um we are we are taking the status of Trino deployment and updating the status of the Trino service so the user can get that status >> so you have a controller in the top cluster that watches >> that's correct so if you I can go back to the to the design here. Um, and I guess where the Trino service controller is uh here. So we have the Trino service controller there and this controller uh when we set up the manager we make it like look for events that are happening on the Carmata API server.

So if there's a change on the trino deployment, they will emit an event against the karma control plane and our controller will be watching that the API server and then reconcile. >> I want to ask uh what uh new gateway has more than gateway API of Kubernetes. >> Yeah, great question. So um Trino failover is a tricky thing. So in from the client side when you initially submit a query you you don't have a query ID and the coordinator that receives it actually returns you the query ID you're supposed to use for the next page and so forth. So there's a little bit of application logic right uh for routing trunoqueries to begin with and then on the other side for you to kind of manage um the draining process of trino there is also a little bit of application logic that is baked into tr gateway that allows you to tag certain backends as being inactive which signals um the queries that are currently in process right to complete still be routed to that inactive backend. uh while the newer queries are prevented from being routed to that one.

So that if you have certain um query limit to how long your queries can be, you have a window of time that you know if you drain a cluster in this state managed through the trainer gateway, you're guaranteed to not be able not not have to interrupt any of the queries and drain it successfully. So that that was exactly the reason why we decided to go with Trino Gateway because it is an open source project within the Trino community that has developed this application logic and of course if you deem that you want to go with an envoy gateway for instance and implement that logic yourself that is absolutely doable as well. Have two questions. Have you considered using um something more than just deployments? Uh something like Argo CD or even something more u complex than just a deployment Kubernetes deployment. Um and second question is um how easy is it to expand your compute to three cluster instead of two of them just in case two of them is not enough anymore. Um so I guess the the first question is like um why not integrating Argo into our workflow?

Um so there is not a huge need for us to do those. Um cuz this is a pretty simple workflow. Um this is just like a controller submitting a CRD into uh Cormada and then Cormada uh spanning it out onto Kubernetes clusters. Um so they like we didn't see the need of integrating with with Argo. Um and then the second question uh sorry was around >> yes that's correct. Um so it's it would be pretty easy. So um on our side at Bloomberg we rely on something called Cass Kubernetes uh as a service. So the idea is that we can just like take a spec definition of a Kubernetes cluster and just uh create uh um create it by requesting some machines that would deploy our whole stack. So uh we also have a deployment tool that is like um that is called BKA and that help us to um basically just add the the configuration of the cluster and then the whole stack will be deployed on this on that cluster. Then the the last step would just be to uh make sure the Cormada knows about this this cluster. So when we integrate with manage Cormada, we just need to let them know uh by let them know it's obviously uh via u their um their API to be like hey I want to register that cluster because now I extended my my my geographic distribution.

So please register that new cluster in there and then it will uh know that I need to deploy a trino trino um uh trino deployment into that new zone that will register against the gateway and then and then the the the two uh workflow for trino service owner and for quer user will be exactly the same. I have another question about this stretched etc deployment. Can you expand how it is implemented? From what I see it is basically a single point of failure for entire deployment because you just shifted logic in >> sorry I I I didn't get it. >> No, can you expand about stretch at etcd implementation how it works? >> Yeah. So same thing at Boomberg. There's a another managed services which is um stretch as a server where basically you you request uh an allocation and uh they provide HCD uh and then what we did is that um we have um a Helm chart um that is installing an API server. So what you see in there the trino API server is actually just a pod and it's a pod that is configurable uh that act like an API server that where you can enter like the certificates to connect to it and where you can reference um uh like a stretched CD endpoint.

So the idea is that this pod will then swap act like an API server and swap the local uh CD just to point to a CD server. >> Um just to answer that a little bit more there's actually kind of industry parallels uh just to so that um we kind of highlight what you can actually use outside of a managed ecosystem. um kind API server that can run on Postgress, right? Where the actual data is replicated across multiple zones is a good parallel for you to be able to adopt if you're thinking about uh implementing something similar. >> But uh this is the port is deployed in both clusters. So you have like two ATCs. >> Yeah. So we we basically set those two pods to share the same cube config. So to share the same certificate. So if a request is coming to our service provider, it will go to one of the pod and since this share the same like um stretch HD back end, both pod are going to be aware uh about the about this newly uh newly created resources. Um so they they share the same cube config basically. Yeah, I think just to kind of tie the knot uh note on that point, um the key idea there is that different clusters just uh store and share the um the uh custom resource uh persistence into a single source of truth that is still distributed and highly available.

>> And same thing for >> Yeah. Yeah, Postgress and kind offer a similar uh approach as long as the Postgress uh instance itself is actually replicated. Y all right, I think that was all of the questions. Thank you all so much for coming to the talk so late in the day. Thank you.