[Music] from Container Days Hamburg. This video is proudly presented by DAX IT. Each time I go to a conference and I I am about to do a talk about Crow, I never really understand why there is so much interest in this project because objectively Crow is a very very tiny part of Kubernetes as a project. But last time I did this talk was early this month in uh in India, CubeCon India and I had a room of 500 people. There was 750 people in the room and there was people sitting right in front of me and I was like why are you all care about it's not even production ready and people care about it but anyway we're here so let's talk about this what is crow hi everyone my name is Abdell I'm a developer advocate at Google I'm based in Sweden Stockholm originally from Morocco if you care about knowing why my name is Abdell and uh I do this thing called the Kubernetes podcast by Google I don't know if there are any listeners oh a few of you okay I will give the rest of you five minutes for sup come. Um um so I I work on Kubernetes mostly on containers and my background is in infrastructure.

I came from data center world. I worked in data centers for a very long time. So I do fundamentally understand what Kubernetes is and and and what problems it solve. And I am going to preface my session by saying that sorry for everyone in the room who is a big fan. I am not a fan of Terraform and I am not a fan of Palumi. I do not like infrastructure as code at all and I am a huge fan of YAML. I know that there's a lot of debates about this. I know that a lot of people hate YAML. I don't. So for me, Crow represents the solution to the Terraform problem. So I'm going to walk all of us through some fundamentals so we can get to understand what Crow is and what it does. Starting with the fundamental of how Kubernetes work. I think this is should not be new to most of you. If this is the first time you see a diagram like this, you are in the wrong conference. Um, so Kubernetes is what? It's basically a system composed of two components. A control plane and a worker plane. The control plane is the brain of the system.

That's where you tell Kubernetes what to do. The data plane is where things happen. You as a developer, user, platform engineer, DevOps engineer, whatever you want to call yourself. You're running containers. Containers runs inside a thing called a pod. The pod runs on a node but you don't really tell Kubernetes what to do or how to do what it does. You tell Kubernetes I want something to happen. We call that in English intent. You express your intent in the form of YAML and then Kubernetes make that intent happen. One of the best ways that have been uh described to me in the podcast to think about Kubernetes is that Kubernetes is a promise machine. What does a promise machine do? It promises you that something will happen eventually. So you tell it I want this thing to exist and then at some point in the future um it will happen. A different way to think about it is that Kubernetes is an eventually consistent system. Part of the control plane are multiple components but these are the core components that any control plane or Kubernetes has right the API server the thing that you talk to using cubectl or cube cuttle or cube control or however you want to call it but there is only one correct way of saying it.

Let's all say it together. cube C TL good right then you have this thing called controllers or operators so the controllers are the things that execute stuff for you then you have ATCD the database and theuler three of the main core um philosophies of Kubernetes is that it's declarative you don't write code you write configuration for Kubernetes it is extensible which means you can take Kubernetes as a system and add things to it to make it do things it was not initially designed to do, right? Kubernetes is designed to run apps. You can make it cook eggs for you if you want. You can eventually write a piece of controller, run it inside of Kubernetes that you wake up in the morning and you do cubectl create toast and then it triggers the toaster in your home to toast bread for you for breakfast. You can make it do that, right? And it's universal, which means there is only one way and one way only to configure Kubernetes, which is YAML, right? We call this commonly KRM Kubernetes resource model.

Everything in Kubernetes is considered a resource because Kubernetes is based on the idea of HTTPS. In HTTPS or REST, you are you have two main working methodologies which are operations and resources. You define a resource that's the payload and then you tell Kubernetes uh to execute an operation on the resource. put if you want to push the resource toward Kubernetes, get if you want to get the resource from Kubernetes, etc., etc. So this basically means that everything that you declare toward Kubernetes is a resource from your perspective. So a resource could be a pod, it could be a config map, it could be a secret or could be a toast, right? This is typically how a resource looks like. Again, this shouldn't be a surprise for most of you. I'm sorry if I'm boring you with the details, but this is important. Um, a resource is just a set of YAML files. YAML describes what the resource is and resource is essentially a set of fields and values. Right? This basically means that when you are interacting with a Kubernetes cluster, you are creating or managing resources.

But you could also create custom resources. We call those CRDs, custom resource definition. So beyond the resources that Kubernetes knows natively pod Kubernet config map secrets uh persistent volume claim blah blah blah you can also tell Kubernetes to manage new resources new types of resources a car a toast a fridge um something a PC a computer a server whatever so the way you do that is that you create what we call a CRD a custom resource definition and the custom resource definition is a way for you to tell Kubernetes a new type of resource to manage Now CRDs on their own are not very useful because just creating CRDs means that you are just going to fill up the database with new things. In order to make resources or CRDs useful, you have to create what we call controllers. The controllers is the piece of code that will implement the custom logic for the CRD. So let's say for example that you are defining a CRD which is the database. You tell Kubernetes there is a new resource called database and then you create a controller that that listens to this kind of resources and then it will so the the controller will be installed.

It will talk to the API server and tell the API server whenever there is a new resource called database created please tell me because I know how to make that thing happen the promise part of the promise machine. So this is how you define a custom resource definition. Uh this is a very short one. In this particular example, our CRD is called counters. It has a bunch of fields which we don't care about. Let's just go all the way until the spec. And in the spec, we have only one field called value. And the type of that field is integer. Now, in order to make this useful, you have to write controller as I said. And then later on, you can create an object like this. This is what you see as a user. Counter name value 42. What happens in this particular case is that you are executing a push command toward the API server to push that YAML file toward the API server and then a controller will listen to that grab the grab the resource that you created and then go and do something with it execute a piece of code to do something with it.

Right? So controllers are essentially the piece of component that bridges the gap between custom resource definition inside Kubernetes and the external world. So that's the thing that will create a database, create a bucket, create an object, toast your bread, do something, right? This basically as a concept, as a fundamental con concept is the core of how most of Kubernetesdriven um infrastructure management works. Most cloud providers a while ago created a bunch of controllers and a bunch of CRDs that allows you to use Kubernetes to manage the resources of that particular cloud provider. There are these are the main three ones of the main three cloud providers. I assume that other cloud providers have their own things but basically in Google we call them KCC. AWS called them ACK and um Azure called them ASO. So KCC, ACK, ASO are all controllers and when you install them, they install a bunch of resources or custom resource definitions, CRDs inside your cluster that allows you to create an object, let's say S3 bucket, object type or client S3 bucket name my bucket region, you know, Europe West one, and then the ACK controller of AWS will listen to that when you create the object and then go and create that resource for you, create the the the piece of bucket or the piece of infrastructure for you.

Um, one of the early project to uh leverage this ability of Kubernetes is called crossplane. So, crossplane is open source and it is a project that basically was created to leverage the custom resource definition and KRM Kubernetes resource model to allow you to create cloud resources using Kubernetes itself. So, it comes in two formats. You have crossplay open source which you can install yourself and then you have a a single Kubernetes cluster where your engineers are pushing resources that get created later on and then they have a managed version right and then every single crowd provider Google AWS um Azure they have their own version we have KCC managed there is SK managed there is AC manage I think the AWS one is part of CDK um etc etc so basically this means that there are essentially two kind of personas in this kind of model. One persona is the platform admin. That's the person that will create a Kubernetes cluster, install the CRDs, install the controllers and then make that available to the developers and then the developers come in later and use KRM to manage the resources.

So these are just two random examples of how this happens. you are a developer, you create a cloud SQL interface which then generates a MySQL database or you know a bucket or whatever and then the controller will go and create those resources for you. Now one of the main problems I would say with CRDs and operators is that they are very very hard to manage. Um it's very easy to actually ddo a cluster with CRDs and with operators because you just can create too install too many operators create too many resources fill up etc end up with a situation where the controllers are just like constantly uh pulling information from the API server and then the API server go down and then managing them is quite easy because it's code right so you have to manage it secure it patch it do all the stuff that you need to do as part of your operations and that's why crow was created Crow was created as a way to help people natively manage resources and CRDs inside Kubernetes without requiring to install extra stuff.

Now there is a huge caveat which is the fact that to use Crow you have to install extra stuff to start with and that kind of defeats the purpose of my definition but the goal of Crow is to be as close or as natively available in chronises as possible. I think that the hope of crow I I haven't confirmed this theory but I think the hope of crow is that crow will become part of the API server which means when you install kubernetes crow is available out of the box so you don't have to install extra stuff so how does it work well let's take very simple example um I have um here I have a visual studio code which I think you all know what it is and I have um a very simple example I can show you here um so let's say for example that you are trying to create um uh deploy an application right in Kubernetes. Is this visible by the way? Is this good? Cool. All right. So, you're trying to deploy an application inside Kubernetes. So, to deploy an application inside Kubernetes, you need basically three things to make the application useful.

A deployment, right? A service and an ingress or some sort of load balancer. So, your deployment looks like this, right? Your service looks like this. And then your ingress looks like this. Optionally, you might need a config map. You might need a secret, a persistent volume claim, etc., etc. What I'm trying to get into here is that this file, this YAML file, which is 80 78 lines of code, could get even longer and longer and longer. Now, I know that some people who are very very huge fans of Helm, might argue that this can be actually templatized with Helm and Helm can make things easier, which I'm not arguing against. Going back to the how I started, I also don't like Helm. So, so how would you make this simple? Let's say let's take a a a much larger example. Let's say that you are a WordPress provider. Your your business provides WordPress as a service, which means your users or your customers just click a button to deploy a WordPress website. Essentially, every single deployment you're going to execute is the same.

All your WordPress installations are the same to start with. you are just taking the same exact TML file duplicating it for every single customer which means that this 78 um lines of code file will exist in multiple copies right customer one customer two customer three customer four blah blah blah the name might be different uh some configuration settings might be different but the same more or less file will exist in multiple copies so what does how does crow help us so crow introduces this concept of resource resource graph definition. So a resource graph definition allows you to do two things. One is expose an interface that the user gets to define and then two uh define what happens when an instance or a copy of that RGD is created. So in in any RGD you will have basically two sections. There is a spec section which has a schema. In the schema you are defining the type of the object. So the type of the resource in this particular case we're calling it application the API version obviously and then the spec of that resource which is essentially the interface the the thing that ex gets exposed to users in this particular case we're seeing that for this particular RGD resource graph definition of type application we have three fields a name which is a string an image format or an image URL which is a string and it has a default value the default value is engine X which means if users don't define it it will default to engine X right and then an ingress of type um boolean and it defaults to false so if we don't define it there will be no ingress created if we define it there will be an ingress created all of these fields in the spec part of the RGD are actually variables which means we can refer back to them using a very particular schema one um interesting thing that to to talk about here and I'm going to show you another example is that fields can be I think of four or five types if I'm not mistaken.

It's like integer, boolean, um, uh, string, uh, list and array. And they can be nested, which means I can actually create a nest in here, which is ingress. Inside ingress I have enabled and inside enabled I have type. Um and I can say uh uh I can say something like uh type is string and it will default to uh let's say internal right. So I can keep nesting them as I see fit. I don't really recommend it because it becomes very hard to read but you can do that eventually. Then I have the status field which I'm going to get back to in a second. Then there is the second part of my RGD which is the resources. Essentially the resources defines what are the objects that will get created when an instance of my resource graph definition is created. In this particular case I have a deployment and this is where we go back to that idea that fields inside the spec part of the resource graph definition can be used as a variable to refer to a variable. We use the dollar sign and then we just refer to it with schema.spec.name.

If I want to refer to the ingress object that would be um uh where is the ingress object? Yeah, schema.spec.ress.enabled. There is also something called an if statement which essentially if you have a boolean value true or false, you can add this thing called include when and essentially it will only include the object when the the value is set to true. So in this particular case my ingress object will only be created if ingress in the resource graph definition part have been set to true. So um now that I have created this I can basically push it to the cluster. I have already created um uh sorry I have already installed crow. Um it gets create installed in a namespace called crow system. Um and inside that name space there is actually just one um pod. Sorry, it's not crow system, it's crow. And then inside of it, there is only one pod. That's the controller. That's the crow controller. There is also definition like resource graph definitions. So if I do cubectl get crds and then I grab on crow, then I have a bunch of crow um objects that are installed for me.

Um the resource graph definition is this one. These two, we're going to get to them in a second, but these will be created when I installed the resource graph definition. this resource graph definition called uh simple G uh RGD. So I'm going to apply it to the cluster uh like that. And then if I query back my CRDs, I will see that there is actually a new custom resource definition created called applications. Right? So essentially one of the one of the things that crow does is when you create a resource uh graph definition, it creates a CRD for you. It generates a CRD. Now as I said earlier this is not very useful because just creating the RGD or the resource graph definition doesn't do anything because there is no controllers. The object the the goal of crow is not to define the controllers. It's not to see or to sit or to be in the business of defining what happens when an instance of the RGD creates. The goal of crow is just to define the RGDs define the underlying resources. this part here of the RGD definition the resources themselves they should have their own controllers that does something with that particular RGD in this particular case we are defining Kubernetes native resources which are deployments service and ingress so Kubernetes already have a controller for them so we don't have to do anything extra but if you are using crow to abstract away cloud resources management then you have to have that particular cloud controller installed in the cluster right because once an RGD is created then there is a controller that needs to act on that particular RGD to do something with it.

So now back to my example which is to deploy a deployment I need three objects. Now that I abstracted it away with crow I can basically make it as simple as this which is nine lines of code. So to deploy an instance of my engineext ingress or WordPress or whatever example you want instead of writing 78 lines of code or configuration file I write nine uh lines of code. So I am creating an object called application. My object is called my app. It has a name. It has ingress enabled and it has an image. So now let's let's let's see that in action. So if I do a cubectl get pods I okay let me let me remove this because then it will be easier so I'm going to delete this um deployment just to make it simple for us go back I have let's see I let's say that we have zero pods right nothing is created for now so what I will do is I will just apply my um uh instance right so I will go simple RGD and then instance and then apply that now that I applied it if I query the cluster to say get application right because application have been registered as a custom resource definition it will give me an object it's called my app and it says it's in progress which means that the underlying resources under that particular RGD are being created Now that things are active, I can actually query back my cluster and say give me are there any pods?

Yes, there are three pods, right? Because in the RGD definition in the resource section, I said that whenever an object of type application gets created, it will generate a deployment and there are three replicas for that deployment. Right? Then if I query my services, I should have a service somewhere. So I do have a service called my app service, right? And then if I query my ingresses, I should also have an ingress called my app ingress, right? So essentially I took a very potentially very long YAML file. I abstracted it away behind one resource graph definition and then I can create as many instances of that particular resource graph definition as I want. Now this is arguably a very simple example. A much more complex example could be things like this. For example, let's say I want to create an object called a web app and in that object I have multiple underlying objects which could be a combination of Kubernetes native objects but also cloud specific objects. So my application is a deployment, a service, an ingress, a cloud SQL database, uh a bucket, a pubsub topic, um a load balancer, whatever you name it, an like a monitoring configuration, uh a login configuration, blah blah blah.

So I can define all of that in my RGD, which is what you see on the left. So I have a deployment, a service, a Prometheus configuration, a service account, a cloud service account, a cloud storage bucket, a cloud SQL. These are Google specific examples but all cloud providers have this this kind of objects IM policies popsub etc etc. So I only have to define this once as a platform engineer devops engineer and then my developers can instantiate or create instances of this web app RGD which then will trigger the creation of all the underlying objects. What gets created when an instance of my RGT RGD is defined could be a combination of Kubernetes native resources and then external resources. So the Kubernetes native resources will be handled by the Kubernetes native um controllers. External resources should be handled by external controllers. In the case of Google, it will be KCC. In the case of AWS, it will be SDK. In the case of Azure, it will be ASO. So part of what you have to do as a as a DevOps/platform engineer is you have to install those controllers as well and make sure that they work because there is an authentication piece.

You know, controller has to be able to authenticate to the API of the cloud provider to be able to create the resources. I'm going to jump ahead to one other example. Last time I did this talk, one of the um questions we got or I got personally was can we actually define in the RGD what are the allowed values. So I don't want people to choose any random well this is a this is not the right example but say I don't want people to define any random value inside my ingress name. it has to be a list or a type of object. So I actually created a very simple example yesterday um which is this. So you can do it by essentially defining your field as a string and then creating an enum and then the enum says what are the allowed values and this can have also default value. In this particular example I choose machine type which is a very relevant example. Let's say that you as a platform engineer do not want to allow your developers to create any type of machines. you want them to only create very very specific type of machine shapes for cost-saving reasons.

Well, you can define those as a list. So in this particular case, I allow N1 standard 2, N1 standard 4 and it defaults to N1 standards one two if um the user doesn't define a value. N1 standard 2, N1 standard 4, those are very specific Google examples. Again, you can define whatever you want, right? Um this also can be very useful for security reasons. I know that's part a huge part of doing AWS resource management is defining like I um I am I think the identity whatever they call the the the the security ids um so you don't want people to necessarily define any value they want you have them you have to give them like you want to give them very very specific values that they can choose from this also can be used for firewall rules you don't want people to define any firewall rule specifically you don't people you don't want people to create a firewall rule which is 0.0.0 0 to 0/z, right? You want them to choose from a specific list. So, I can show you this in action. Um, I call this playground.

I think I have it already installed. Let me double check. One second. And I think I am going to get very close to my end time. So, I'm going to stop here after this example and then get any questions. What? Come on. Oh. Uh, give me one second. Hopefully I'm not going to show you my password. Um, all right. Good. Now it works. Hopefully. So, I already created my um uh my my my object. So, if I query get playground because object is called playground, I already have it. So now let's see that I want to um apply this object here. So create an instance of my playground object. So I'm just going to apply this one. Uh it's called playground RGD. So this one should work because the value of machine type is within the list of allowed values. So N1 standard 2. So this should be created and yes it was. But now if I um comment this one out and then choose to create this one here, this will not be allowed because obviously uh uh sorry this will not work because I have a value here which is not in the list. So I get an unsupported value N1 standard 8 and then it tells me I can only create N1 standard 2 and N1 standard 4.

So this is just an example of the kind of field types um and what you can do with them. So enum or array is part of what you can do there. So I'm going to skip ahead. Um few things to keep in mind and then I'm open to questions. Crow is experimental. It's not stable. It's not version one. It's version 0.4.1 which is the latest version. So keep that in mind. Um experiment with it. I wouldn't put it in production. Um, Crow is not the answer to all the questions. You will have to bind it with a cloud specific resource thing to make it useful. KCC for Google, ACK or ASO for the other cloud providers. And that's all I have. I hope this was useful. I'm going to stop here and if you have any questions, I'm open to answer them. Thank you so much. >> Um, so yeah, we have two from Chris. uh can we deploy multiple versions of the same RG into one staging cluster so we can implement a dev workflow for the resource graph definition itself or do we need separate clusters? >> You can do it. You just have to go call them different names and then also make them create different kinds.

So if you create a different so if you create RGD called RGD1 and then in the in the in the spec it creates an object called application and then you create RGD2 they will collide with each other they will be conflicting so you'll have just to version the names of the RGDs and then the name of the the kind or the resource kind they they generate but yes you can do it >> um next to the qu to the next question how far away from production readiness is a crow right now >> yes so that's that's a question that comes up very often. There was a hope to make this production ready by the end of the year. Unfortunately, we lost the product manager who was working on this inside of Google. So, we are actually hiring new products manager. Um, I would say probably end of the year, if not hopefully early next year, but it's within the next 6 months, nine months. Don't hold me on this. I might be completely wrong. So, I don't I I I write very very little code. I am more like talking about this as a potential new thing coming to Kubernetes.

I'm not one of the maintainers. But the the the repository is very active. >> Uh I think we have few questions about if Crow is a Helm or crossplane killer or uh how do they compete to each other? >> So if you go on YouTube and look for is Crow uh a crossplane killer. There will be a video of a Victor who works for crossplane saying that this is the next cross or works for out um outbound which is one of the crossplane people. Um I would say no. Actually one of the questions that came up or one of the experiments I am running I haven't finished a demo yet but I'm working with this. Last time I was in CubeCon India and this talk I ended up having a long conversation with someone and we kind of figured out actually that you could templatize crow using helm. So you could use helm as a way to create crow templates cuz you know helm is just a templization platform. Um so you could combine helm and crow. Is it going to be a crossplane killer potentially? Maybe like at some point probably you wouldn't need if if if crow is available in kubernetes out of the box you don't need to install extra things.

So >> um why does crow spec not use open api for the spec? >> That's a very good question. I don't know that's a good question to ask actually on on on GitHub. Just open an issue and ask them why are you not respecting the open API spec. I think they're trying to make it simple. Open AVI spec is quite convoluted quite complex. um support crow different versions of RGDs at the same time. >> I think we answered this question. Yes, you can. You just need to call them different names essentially. So, >> uh can I dynamically create resources? Um >> can I dynamically create resources based on user input? Example for loop uh uh no but it's actually a feature request I opened. Um I did particularly ask this question about uh creating some support for loops and one of the use cases was creating like a cluster of objects. Let's say you want to create like 10 virtual machines, 20 virtual machines and they all um are the same shape, the same configuration, same everything. They just call them different names. So I opened if you go to the GitHub, you'll find an open an open feature by that I created asking for support for loops.

So the answer is no, but hopefully it will come at some point. >> Your podcast is very good. >> Thank you so much. What about secret injections of certificates and keys? Um should we use something other than the CRD for example secret provisioner or adding logic to RGD >> secret injection of certificates and keys? Um well I I would use the secret manager right like the standard secret manager in Kubernetes. Um external secrets is a very good project. I know that it has some problems with maintainers and maintenance and stuff like that and there are some questions open questions about that. Uh but I I wouldn't I wouldn't trust CRDs generally speaking with secrets or keys. I would just use an external secret manager. >> And uh last two questions maybe you can answer them together. What happens when changing an existing definition and what happens when the application is deleted >> in your example? Will it? So yeah, that's a very good question. When you create an RGD and then you create instances based on that RGD, the instances are frozen in time, which means that the instances you create are based off the RGD you defined in the first place.

If you change the RGD later, like changing adding more fields, changing default values, the existing objects will not be touched. But if you delete existing object and create them again, then they will follow the new schema. So, so, so yeah, basically it's the sort of versioning. If you create a new version of the RGD, it will only impact the new objects created out of that RGD, not the existing ones. What happens when the application is deleted? The application you mean the RGD if the RG if you mean RGD then deleting RGD will delete the resources um or will delete deleting the RGD will delete existing objects essentially. That's that's so there's like a kind of a relationship there. >> Okay. I think um it's similar to previous one. Um you can answer one question if um a short one. Uh we have still one minute. Is there a limit on what controllers can do? Maybe this one. >> No, but I mean controllers can do controllers things. I don't think there is a limit to what controllers can do. Like the the the way controllers work and the way Crow works are completely disconnective from each other.

The only relationship is that the fact that crow allows you to define the RGD that defines an object that if there is a controller for it, it will be triggered when that object is created. So there is basically no relationship between the two. But as I said earlier, in order to make crow useful, you will have to have the controller that acts on the objects that are created by the RGD or by Crow. >> Thank you. >> Thank you so much. I'm going to be around if you have any more questions >> from Container Days Hamburg. This video is proudly presented by Ducks IT. [Music]