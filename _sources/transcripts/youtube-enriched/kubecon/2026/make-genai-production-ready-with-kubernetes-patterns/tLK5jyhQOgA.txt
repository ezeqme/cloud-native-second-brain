So welcome everybody to this session about how Kubernetes patterns meet geni. Um yeah first of all let us introduce ourself for a second. You >> go first. >> Yeah but my name is Rulen Hus. I'm a software architect distinguished engineer working for Redhead. working since for Reddit since 10 years nowadays and uh yeah and also on the field of AI and actually we have written also some books together with Bill but also with with Danielle so we come tell a little bit more about that. Um yeah Pat >> yeah hello everybody my name is Bill Gin Ibraham I am principal product manager at DIG grid where we are the primary maintainers of the Dapper project so that spells DAPR it's one of the CNCF graduated projects that lets you create uh durable resilient agents yeah okay so so back to this two books so the both books are available nowadays so you see the QR codes already so they are also available for free for download so if you if you like and the talk here is about really about a a blend of both books actually.

So actually we actually we've just finished. So when I say we in this case Danielle and and I so maybe we can see the the next ones I think this this meme clarified a little bit. So actually we could stay here uh with three persons. So Danielle as well but okay we we created this book on how to run generative AI on Kubernetes and but also have this previous book about patterns which describes a set of repeatable solutions for common problems and we thought about it's a good idea to present how you can apply those common patterns to the field of genai um and this is what we are trying to do in this 30 minutes um I said you can of course buy this this new book on generative AI on Kubernetes is but you can also download it for for free. But before we we start and go into details about generative a quick recap what we mean with patterns what they are and um how they are used these days. Uh right um so this will be a bit of high level but um probably most of you here are either running uh lms on Kubernetes or actually interested in doing that.

So before we dive into that we want to start with the traditional Kubernetes pattern. So here I have a typical web application that you would run on Kubernetes. Probably many of you have run this. Um it has a bunch of patterns. If we if we zoom in for example you see the controller pattern. Uh the job of the controller pattern is to look at the desired state which is your deployment manifest and makes that uh happen. In this case it's starting two replicas of an application. Right? If you look carefully, you'll see there is a init container pattern which in this case could be something like um maybe initializing the database, copying some data um into a database. Then on the right hand side we have the stateful service pattern. Uh maybe you're running a posgress on Kubernetes with multiple replica. So stateful service gives us multiple things right. It gives stable identity, it gives a stable storage, stable scaling behavior, etc. Uh for the down we have the batch job uh pattern and the idea of this uh this pattern was uh ability to run a fin uh finite unit of work until completion.

In this case this can be generating some reports right and at the bottom we have a demon service pattern which allows you to run specific pods on every node. for example, things such as not monitoring and uh scraping the logs, right? And so these are basically six patterns that you can quickly see. But if we if we spend some more time, I'm sure we can identify dozen patterns. Um before we map into a full LM application, let's look into you know a model. So how why is a model different than a web app for example? uh a model represents uh typically you know tens to hundreds gigabyte of read only trained data and one of the first challenges when you want to run this as a workload is how do you get that that data on the node and how do you load it into memory that typically takes uh significant amount of time then you cannot run it on any kind of node you need a specific uh GPU node right and you need specific u runtime it doesn't run on a web server but you need a model server So these are all operational challenges that a typical deployment probably cannot do with a lot of uh without a lot of handholding and a good way to do that is the case project.

Kerf is incubating CNCF project that basically implements uh the controller pattern. Uh you declare your um inference service. You tell what model you want to run, where to get it from, uh on what kind of runtime like VLM, GTI, etc. And it does all of that behind the scene. It will create the deployments, the services, the scaling rules, the routing uh policies, etc., and run that. Basically, that's your controller pattern applied to model service uh as a workload. And if I map the previous application how it looks like for example in a genai world we kind of have the same patterns uh you can see but with different workloads. For example we have uh the controller pattern in this case performed by Kerf on the stateless services side. In this case, we have the uh the LLMs that are running. And you'll see that instead of uh in addition to request and memory, they are also requesting um uh GPU nodes, they have a maybe a longer startup time. They still can use init pattern, sidecar pattern to do some initialization.

In terms of stateful services, rather than uh running a posgress, you could have, for example, a vector database that's running. U instead of a reporting service, a batch job would be a job that maybe processes, documents, chunks them, um gets uh embedding and loads them into this vector database. Similarly, for the demo services, you on each node, you would have demo services in this case that perform labeling the nodes. So these would be GPU nodes and exporting metrics right so you kind of have the same patterns applied the same way but at a much uh uh larger scale and just to bit of summarize what's happening with the uh familiar Kubernetes patterns and primitives we've seen the top four we've seen how controller pattern changes with kserve how the stateless service stateless service demo service they all basically can be applied almost like as it is in a genai world and there are a other patterns that you can apply, right? For example, the predictable uh uh predictable demand pattern. The idea of that pattern was when you are running a workload on Kubernetes, you have to declare what infrastructure demands it has, CPU and memory, right?

Typically in this case, you also have to declare as a minimum what kind of uh you know how much GPU you need and you can do that different ways. Um, for example, you can do that by requesting exclusive access to the GPU with your uh request, but that might not be a good idea. There are more flexible ways. For example, you can use uh you know um you can use affinities uh to select how much GPU needs or even uh DRA dynamic resource allocation which allows you to uh which allows you more flexible templatized way of requesting uh resources from the uh from Kubernetes. Right? This is from the workload point of view. Uh if you go down there is the automated placement pattern and the idea of this pattern is how do you control what what workloads get placed on different nodes here. Now there are new concerns. For example, you have a heterogeneous cluster with uh regular nodes and GPU nodes. So as a minimum you have to think how do you protect these GPU nodes uh from other workloads being placed on them.

for example, as a minimum you can um uh taint them and prevent other applications to be placed on it and other other patterns as well such as the sitecar pattern with um with the adapter. The idea of adapter pattern uh was to offer a unified interface for accessing the workload in case of uh LLM that is accessing the LM. So you can use sitecar such as dapper or such as the lm stack that allows you unified API for accessing the LLM and the common API that is uh emerging in this space is the open AAI responses API. So you can do those right. And one uh last summary slide. The previous patterns were almost applicable u uh the way they are without many modifications. But these patterns here they do slightly change because we're changing the parameters we're changing the scale. So the way these patterns apply uh do do change a bit. So if we start with the init container um the idea uh init container typically has been responsible for decoupling the initialization logic from the application logic. Here in the GNI world it kind of moves a bit more into data movement.

So using init container side containers you move uh the model weights or you bring them to the containers. Um immutable configuration pattern the idea was to bring configurations to your application. Now it's the scale changes significantly and it moves towards bringing uh model weights. Uh moving down help probe typically help probe was in the range of couple seconds up to 30 seconds. Here it moves into multiple minutes and in addition to starting up the model server it's also indicates whether the model weights have been loaded. In some cases it also includes uh warming up for example whether the uh the graph has been computed whether the key value cache has been allocated into the memory. So the responsibility of the health prop also grows. Um moving to the last two patterns declarative deployment. If you want to do deploy declarative deployment for an LLM you have to consider that there might be requests that are running for uh much longer. So you may have to wait for them uh before shutting down.

You have to consider that startup can be you know up to 10 minutes or more. Um and things such as rolling deployment could be more challenging because you have less resources typically uh and it might be better just to do a fixed uh deployment with with a bit of downtime. Right. And and lastly, Backshop. If you're using that um for example for something like fine-tuning um you may you may consider some of the new features coming into Kubernetes such as ganguling which means uh you run the batch job only if you can run all all the task at once if there is sufficient infrastructure right and these are some basically example of existing patterns that uh you can apply today um on genai workbooks but they're also brand new patterns that emerging >> right. So actually now that we have have seen that uh that how we can apply the existing patterns to generative AI workloads. Of course it is not surprised that these patterns are also valid for for generative AI workloads. But as Bill already mentioned on a different scale but there are also new kind of patterns emerging specifically only that only relevant for geni or for for the size of geni workloads.

And one of those is what I want to present here is the model data initialization pattern with some sub patterns. Which means one of the big challenges to run models within a cluster is really the sheer size of the model data itself. So if you have to download 50 gigabytes of model data for every pot start. Of course this is does not scale right. It it affects your startup times or your volume your size constraints and whatnot. And for that there are several techniques that you can apply for um softening the constraints. Here you see a typical phase of a of a startup of a pot in the life of a pot and you can see that uh if the model data is really huge then this is kind of the dominant time slice that is needed here the way. So what can we do to improve uh here actually u there are several things. So the the most trivial thing is just to apply the init contain pattern which means you have your your model runtime running and the an init container downloads the model from somewhere else. This could be either for example an S3 bucket somewhere or it could be also part of the init container's image itself.

So the init container could be already contain the model data and it but it needs to copy over this model data to some shared volume so that the main container which has the runtime can access this data. So this is the classical approach. Therefore, you typically use an empty D volume in between. But um but if you if you use the approach with baking the model into an OCI image here, then of course you only need to pay the the penalty only once by when the image is pulled to the node, but you still need to copy over this empty. This still takes quite some time. But if you use a a direct download from some somewhere else from external storage, you have to pay this penalty every time. And of course this is not really useful if you if you think about it. But here you see also the an example how you would do this within your pot specification. The good thing is of course this works everywhere and it always worked like this. But now let's look what what's the alternative is of course the next step is to use a persistent volume and you can prepopulate the model data on the persistent volume if you like and then people uh people pots can share this persistent volume with the data.

So you have to pay only once for initialization but you still have now your model data on a remote file system usually. So persistent volumes are the distributed file system. So this is also something which affects your latency. But uh actually to be honest this is the the most uh or the most common way how you deploy models in production. But there are better solutions that you can can use. And what I'm showing here is also as well how you specify this in case. So we already briefly mentioned KSURF. KSURF is a controller on top of Kubernetes um that introduces own CRDs and own platform for for running um for running models run uh and bringing runtimes together with the model data oops sorry that's okay yeah so but but as I said we can even even do better now there are come really some new interesting patterns that might be not so well known therefore I'm I'm trying to explain it quickly. So one is called a model car. So this is something kind of you can say it's a little bit heckish to be honest because uh the problem is if you say you have the model already baked into an OCI image, the question always is why can't I access the data directly?

Why do I have to copy this data over into an empty deer volume so that a main container can use it? And there's not really a technical reason. So Docker had this already from the very beginning since 2014. And actually if you look into the Kubernetes uh issue tracker there was one issue which was I think it was older than 10 years at the at the end which requested this kind of image volume type which means that you can directly mount an OCI image as a volume within a pot and can access the data directly but this has not been implemented since for quite some time and so people went creative and they found out so one comment on this very uh lengthy issue about image volumes is um some guy who found out yeah we can switch on share process name space which means that the processes in a pot in the different containers can see each other which is uh of course you you soften a little bit the isolation but then you can access the root file system of other containers via the proc file system and this is what is shown here is that you can just navigate to on on your own proc file system and reach the other container's root file system which means which contain the model data.

And the trick with model cars is that you have so you have no init container like in the first uh approach. But here you have a a model car that starts up as a sidec car not as in container as a sidec car which stays alive during the whole lifetime of the pot which is just a sleep infinity. So it does nothing really. It's just to be kept alive so that the the main container can access the model data via this uh poke file system. And to make this easier, a symbolic link is created during startup of the model car which puts the the pro the model data under a fixed path everywhere. So this is a kind of a neat trick of course on the expense of uh lossing a bit of security because now every sidecar sees also the main containers which might be or might be or might be not an issue but luckily this is only a temporary solution. So if you if you're right now in production, if you're not running Kubernetes 1.35 or something earlier, then this might be still a good solution to a good escape hedge for for getting to model data within a container image.

But the the final solution and we I'm super happy that this nowadays has manifested and is enabled by default in Kubernetes 135 is image volume types. And these image volume types work like any other uh volume in in a pot which means to declare the volume with a specific type. The type here is indicated by this image field. This image field has has a reference to an OCI image and you can u also add some pull policy. So some configuration like there and then you can just mount this the root file system of this image into your container itself with with a volume mount. Here you can also specify an subpath which means you can also go into a directive which is deeper within this uh with this model. As said it's u it has a a long history but now it's still officially declared as beta better but it's enabled by default. So you can use it these days but of course only if um if you're on a very modern Kubernetes version. So to summarize actually the the the ways that you have for for uh getting your model data your model weights into to your runtime here's a quick summary I don't go over them you you can see what what you can choose where but just remember that image volumes is really the endgame and actually if you are able to use image volumes please use that that okay so this was now a pattern which is very specific for for really huge data to to mount there and of course geni is one of But now let's switch to the next pattern which is about uh also very specific to LMS and yeah B tell us more.

Um yeah so Roland showed us how to get the model data into the pod right and before that we've seen how we can run that model uh with different model servers and I think the next thing to figure out is how do we direct the traffic uh to the right pod instance and Kubernetes actually has good ways to do that today for example we can use cube proxy to um randomly distribute traffic between the replicas we can keep the traffic within the same zone, right? With zone of a affinity, we can use gateway API to do PAT HTTP based routing. But actually all of these approaches they they they assume that your requests have equal cost and they evenly distribute requests ac across the uh replicas of a pot. And that doesn't work with really large language models um where where every request looks the same. So they all go to the same path such as completion. And um a request that probably produces couple hundred tokens might take you know few hundred milliseconds looks exactly identical to another request that may produce thousands of tokens and takes uh seconds or minutes.

The same with the thinking models that can take you know um many minutes to execute. Basically the traditional routing approach doesn't work and we need a way to route the request not based on the request data but on the status of the LLM um and this is what the uh inference extension in the gateway API project have come up with and the whole idea is that we are separating the the routing logic instead of doing it blindly uh instead it's delegated to specific components and this solution has three three elements the first one is the um uh the gateway API. Typically this is done by envoy proxy and it faces the incoming request. Then you have a pool of um u language models that can handle that request and the new component is the endpoint picker. The the job of the endpoint picker is to identify which is the best replica to handle this incoming request. And the way it does that is it continuously scrapes the endpoint of the language servers and identifies which one for example has least amount of in-flight request has the shortest queue has the best uh key key value caches to handle the request and based on that it's able to uh to direct the traffic.

Probably one of the most u advanced implementation here is from LMD that lets you also do additional things such as route traffic based on the priority of the request. If you have certain requests that have higher priority, it can route request based on the capabilities of the model. If a model is fine-tuned, right? It can route request based on the load on the model and etc etc. So this is already quite good approach but it can be uh better right if you look into um how LM works they are stateless so they have no prior knowledge of the previous request so each request is kind of brand new that means in a in different kind of applications such as chatbased application every time you send a message you actually have to send the full history of that conversation for the LM to have the full context if you have an agentic application every message to the LM needs to have the full history of the tool calls of the system prompt etc. And if you're using rug every again every message to the DM has to contain the full populated context right and and you can see that with multiple messages they kind of have a growing common part that the LLM has to handle again and again and very little uh a new segment at the end.

The way LMs uh handle that is uh once they process u a request they do store the the the attention tokens in the in the something called key value cache. So any future request can use that but in order to do routing your router has to be aware of that so that it can direct the traffic to the same instance and the endpoint picker approach works with that as well because every time the LLM u computes a new cache key or evicts the cache key. It sends a message to the LM picker and the LM picker has a mapping of of all the cache prefixes and the and the replica. So it is able to identify for any incoming request whe it has uh which is the best instance to handle that. Um and that's basically prefix aware routing. Um it turns out uh this can be improved even more. Right? So far we said LM is handling the whole request at once. But even at very high level the processing can be split into two phases. The uh prefill and decode. So the prefill is the the phase where it processes your incoming prompt and decode is the generation of the response and it turns out the prefill is uh can be parallelized.

So it's actually quite uh compute intensive whereas the decoding phase is sequential. So you you can only produce one token at a time. So it cannot be parallelized. It's lower. It's it's memory intensive which means you can actually split these two faces into different group of pods. So you can split the processing um and uh if your router is aware of this split it can direct the the request to the right um to the right pool. Now this is a simplified diagram the the routing probably doesn't happen directly at endpoint picker level. the pools can direct the traffic among themselves. But the TLDDR is that this request routing moves from the request into the LLM state. So it's based on the LM state and you have multiple levers here. You can use you know the capabilities that based on the LM. You can check the uh which which LM has which LM instance has the best cache prefixes make it aware of the load or even custom policy and the takeaway really is this is I find it quite fascinating and it's still an emerging area routing request to the right LM.

>> Mhm. >> Roland. >> Okay. Now quickly to the to the last pattern that we have and uh actually this is a good example how you have higher level patterns that are based on other patterns like here. So um I'm talking here very quickly in two minutes about rack composition. The rack is retrieval augmented generation which means it's a way how you can include domain knowledge into your queries and this has two phases. It has ingestion phase and that's a query phase. ingestion means prepare your vector databases with your documents and the query phase in short means uh use these your your queries look for similar documents and add those documents that you have identified into your prompt at the end and so that you have a richer context which can be used by the LM for generating answers like here. Um but as as I said there are uh we have seen this already in the first slides by by Belgium that that you that any of of those subcomponents for rec composition pipeline is still a Kubernetes workload a usual Kubernetes workload that you can that we describe also in the book with different patterns like like jobs and u and stateless and stateful services.

Of course, a database is a stateful service like here and but but you see that there that this very specific subcomponents of a wreck pipeline really maps also very well to existing cubernetes workload. So I I would argue that uh rack is very very well suited for to be run within Kubernetes because we have already the primitives that support everything of those and u and yeah another quick example you can everything by the way everything what we talk here described in those books and even much more about GPUs and and and whatnot. Uh but here's interesting way how you make this embeddings. Unfortunately, I have not time to go into into details what embedding is actually, but uh but you can run embeddings alongside with your with a user of the embedding like like the orchestrator directly within the same process. You can have it as a sidecar or you can even go uh further and have it as a separate pot so that you can scale independently between those services. So yeah, but as I said, sorry this was a very very short overview, but but you the gist here is really that is it is a a composed pattern which is based on other Kubernetes primitives and you can easily build up more complex stuff like like these as well.

With that uh we are at the end. So here again um the link to the books there book actually. Do you want to say something to this is a surprising book. So just scan that and and look into that what what it is about. And now we have maybe two minutes for for questions if there are any. Thank you very much.