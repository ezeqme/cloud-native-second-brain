Okay. So, hello everyone. Uh, thank you for joining my session. My session is titled Spiffy meet OS federated identity for cloud native workload. I'd like to talk about how Spiffy and OS can work together to build federated identity across modern cloud native platforms especially when workloads and author authorization decisions spans multiple trust domains. Here's today's agenda. First, I'll explain why modern platforms naturally end up with multiple security authorities or what I'll call trust domains and then uh we'll look at what breaks uh when we try to federate identity across those domains especially for user authorization. After that, I'll introduce a standard stand standardsbased approach to securely building federated identity across domains. And finally, I'll show a demo using keyro and spire. Before we dive in, uh let me briefly introduce myself. My name is Yoshik Tabata and I'm a senior OSS consultant. I've spent over 10 years designing and consulting on secure API platforms and identity systems both in enterprise and cloudnative environments and I'm active in the CNSF community uh currently serving as a tech lead for tax and compliance and I'm also a long time longtime contributor to keyro so let's start with the first topic uh modern platforms have multiple security authorities or trust domain A common way of thinking is that a platform has a single security boundary.

In reality, this is a mice. Modern applications span multiple kubernetes clusters, multiple crowds and often multiple organizations. On top of that, we now have tools and AI agents acting as first class participants. Each of these environments performs its own authentication authorization instead of relying on one central authority. In practice, uh both workload identities and user authorization data are issued and evaluated independently in different parts of the system. Spiffy signing authorities issue workload identities and all authorization servers issue tokens that contain scopes and claims. In this talk, I'll treat workload identity and user authorization as separate concerns. To keep things simple, I'll use the term trust domain. A trust domain is the boundary where identity and authorization decisions are made. Across trust domains, nothing is trusted. By default, O and Spiffy define trust domain slightly differently. O focuses on the authorization server as the anchor of trust. Spiffy focuses on the identity name space and its issuing authority in real platforms.

However, there these are usually operated and governed together. So for the rest of this talk, I'll intentionally treat them as the same boundary for simplicity to reflect how systems are commonly built and operated in practice. Now let's move to the second topic. What breaks federated identity across trust domains. The good news is that workload identity federation already works quite well with spiffy trust is anchored in signing authorities and distributed via trust bundles. Workros can authenticate each other across trust domains using sits without shared secret. From a workload to workload authentication perspective, this problem is largely solved as shown in this figure. Trust is established through specific trust bundles and workloads can verify each other's identities across trust domains. The hard part is user identity and authorization. Preserving fu a request request is for and what that user is allowed to do across multiple service hops is much harder. Simply hoarding an access token across trust domain is strongly discouraged.

It breaks audience constraints and makes it unclear where authorization decisions were actually made. The next idea people usually try is token exchange. At first glance, this seems reasonable. We can change the audience and issue a new token. But this raises a fundamental question. Why should the authorization server in trust B trust an access token that was issued in trust domain A? Token exchange by itself does not answer this question. So what actually makes this trust possible? This brings us to the third topic. How to securely build federated identity across trust domains. The O identity and authorization chaining across domains draft uh defines a clear answer by combining token exchange defined by defined in RFC 8693 and Jot based authorization grants defined in RFC 7523. We can preserve both identity and authorization semantic semantics across trust domains while arrowing each trust domain to reissue authorization independently. This figure shows the identity chaining flow. In this flow, a request crosses trust domains and is ultimately allowed to access an API in another trust domain.

To do this, the flow uses two key elements, token exchange and JTO authorization grants. I'll explain how each of these works in the next sl token exchange. In the identity chaining flow, token exchange is used at the boundary between trust domains. It takes an access token issued in one trust domain and produces a jot that is explicitly intended to be consumed by another trust domain. This step changes the audience and the token context and allows the request to cross the trust domain boundary. The output of token exchange is a jot authorization grant. This jot is a cryptographically verifiable statement issued by an authorization server. It allows the receiving authorization server to understand and verify the upstream authorization decision before issuing its own access token. This is what enables authorization to be reissued when accessing an API in another trust domain. There's still one missing piece. uh identifying the actor that performs delegation. All of these flow requires services to authenticate when requesting or exchanging tokens.

Using static client secrets does not scale and introduces operational risk. We need a way to authenticate workloads securely without manual secret distribution. This is where Spiffy meets OOS. The OOS Spiffy client authentication draft defines how Spiffy credentials can be used for OR authentication. Instead of shared secret, the authoration server verifies a cryptographically strong workload identity. This makes it cryptographically explicit which workload is performing delegation. In the figure, this is a step where the authorization server verifies a cing workload using its S bit to securely support user delegation across trust domains. An authorization server must understand three things. What is being delegated? for whom it is delegated and which workload perform the delegation. Identity chaining covers the first two spiffy based client authentication cover the third together they form a completely picture now let's move on the demo we use keyro as an authorization server spire as a spiffy signing authority first let me briefly introduce keyro key is an opensource identity and access management solution It provides o 2.0 oization server capabilities and single scio features and is currently an incubating project under the CNSF.

Krog's major features include support for industry standards such as o 2.0 open connect 1.0 summer and more. This include support for token exchange jot based authorization grant o identity chaining and all specific client authentication authentication. Next I'll briefly introduce spire. Spire is an open source system for workload management. It implements the spiffy standard and issue sit to workload. Spire is a graduated project under the CNSF. In this demo, Aspire provides the workloadies used for OC client authentication via Jot SBIS. The goal of this demo is to show how user authorization can be safely propagated across trust domains. In particular, uh I want to highlight that authorization is not simply passed through but reissued in each trust domain and that all O related requests are authenticated using spiffy sits. This figure shows the overall system architecture. In this demo, we have two Kubernetes clusters each representing each representing a separate trust domain mini cube A and mini cube B.

Each cluster runs key spire and the sample API server. As a client, we simply use the car command. This figure shows the details request flow. The two spire servers are federated which allows workload identities to be verified across trust domains. Key retrieves spy trust bundles from the spire servers in order to verify the j bit used for client authentication. There is one important point here. Although the spire server in both clusters are federated for all spiffy client authentication and authorization server only trust spiffy authority that are explicitly configured. Because of this key B is configured with two spiffy identity providers. one for the spire server in miniqway and one for the spire server in miniqb. The sample API server consists of two main parts. First, an envoy authorization filter that validates incoming access tokens using token introspection. And second, another authorization filter that validates incoming access tokens using tok That not another auto filter is used for cross domain calls.

When an API needs to call a service in a different trust domain, this filter converts the access token into a token intended to intended for that domain following the OO identity and authorization chaining across domain specification. Now let's walk through the flow step by step. First the client which is car command obtains an access token using the o authorization code grant. When the API request reaches the MBY authorization filter, MBY first fetches a Jot S bit from the Sky agent and then validate the received access token using token introspection. At this time, Jibbit is used for all SCR authentication. Next, when the API implementation determines that it needs to call an API in another trust domain, the envoy authorization filter again fetches a jot sit and exchanges the access token for Jotto authorization grant. Here as well, Jotto s bit is used for client authentication. Then Mboy fetches a Jot S bit once more and uses the Jot authorization grant to obtain a new access token intended for trust domain B client authentication is again performed using the J S bit.

Now in mini cube B when the API request arrives the emboization filter performs the same steps as in mini cube mini cube A it fetch it fetches a J s bit via the spy agent validate the access token using token introspection and authenticates itself using spiffy based client authentication. In this demo uh you can you may notice that Jot S bit is fetched three times in main cube A. This is done purely for clarity in a real system. J S bits can of course be cached and reused. Now let's move to the demo. First uh these are the ports in mini cube A. They are key ports, spire ports and a sample API server bot. The sample API server bot includes a simple API server implementation, a token introspection implementation and a token brokering implementation. Now these are the ports in mini QB. There are also key ports, spire ports and a sample API server port. The sample API server port includes a simple API server implementation and token inter token introspection implementation. There's no token brokering implementation because sample API server B does not need to call another trust domain API.

And this is the API implementation in the sample API server. There are two APIs, the Haro API and the Corby API. The Haro API returns a harrow response. The cor API calls the harrow API on mini cubeb. Next uh let me explain the token introspection implementation. It first obtain an access token from the received API call. Then obtains a jot as bit. After that it sends an introspection request with the chot as bit. Next let me inter let me explain the token brokering implementation. It first obtains an access token from the received API call. The first step is token exchange. It then obtains a JS bit and sends a token exchange request using that JS bit. After that it receives a jot authorization grant. The second step is jottoization grant. It obtains a jot and request a new access token using the Jotto authidization grant. Next, let me walk you through the key configuration. Let A is hosted on key clock in mini cube A and Lum B is hosted on key clock in mini cube B. Railway A has two clients. One is client app which is used by the car command.

The other is spiffy TD1.ample APIs A. This is for sample APIs A. For the sample API server a client we enabled token exchange and we set all spiffy client authentication for it. We set the spiffy identity provider for it and the spiffy ID as the federated subject. Next we review the spiffy identity provider setting. This is for all spiffy client authentication. We set spiffy dress domain for it and IDC JWKs endpoint for it. Rum A has two client scopes Corby and API B. The corby client scope is used to call the corby API. The API client scope is used to call the sample API server B's API. Each scope sets a value to the audience claim. The cor scope sets the sample API server A as the audience claim and the API B scope set B's token end point as the audience claim. Next, let me explain the leum B configuration. B has two clients. One is spiffy TD1.ample API server A. This is for sample APIs server A. The other is SPY TD2.example APIs server B. This is for sample APIs B. We enables all speedy client authentication for both client.

For sample API server A we use a spiffy identity provider and for sample API server B we use a different spiffy identity provider. Let's check identity provider settings. We have two identity provider settings for spiffy. one for trust domain A, the other for trust domain B. We set spiffy trust domain and ODC JWKS endpoint for each and there is one more identity provider setting this is for jot authization grant we set issuer and JWKs URL using ram information so let's start a demo first we obtain original access token we send a an authorization request to the key clock. We set the cor scope to the authorization request. When we send authorization request, the login screen is displayed. We sign in as a pre registered user. Then we can get an authorization code from the authorization response. So let's send token request to obtain access token. Now we can get an access token. So let's inspect that access token. The obtained access token is like this. The client ID of sample API server A is set to the audience frame.

This is because we set the cor scope in the authorization request which allows sample API server A to perform token exchange. So next we call harrow API with the access token. We can see a harrow response that sample API server a returns. We are capturing the interaction. So let's look at the background interaction. We can see token introspection request in the capture and this is a jot s bit that is used for all specific client authentication. So let's inspect the jot as bit. We can see the authorization server identifier for letter A set as the audience claim and the OIDC discovery issuer of trust domain A set as issuer claim and the SP ID of sample server A is set as the subject claim. Then we get the successful introspection response including active is true. As a result the harrow API call succeeds. Then next we call B API. We can see how response that sample API server B returns via sample API server A. We are also capturing this interaction and we can see token exchange request in it. And we can see here APIB scope is set and here uh Jot SB is set and as a result we obtain a jot authorization grant.

So let's inspect a jot. We can see the token end point of B in the audience screen. This is because we set the API bill scope in the token exchange request which enables key clock B to issue an access token using a jot authorization grant. And we can also see the token request of Jottoation grant in the capture. We can see Jot SB bit here too. Then we get a new access token for trust B correctly. And the capture also shows that token introspection was performed again using a J test bit and we get the successful introspection response including active is true. As a result the call API request succeeds. So what you've just seen is that user authorization is reissued in each trust domain and all all interactions are authenticated use using spiffy without any static grant secrets. And that's the end of the demo. Thank you. Yeah, these are the key takeaway of this session and that's all for today. Thank you very much for your time and are there any questions? >> Hello. >> Oh yeah. I was just wondering in Hitachi, do you actually manage uh what you presented today?

Is that how you guys manage it in terms of having a federated um spire and then managing the resting keycloak? >> Uh this is currently experimental phase. So we just uh how to say confirm that this can be done. So yeah that is uh kind that kind of phase. So this is not uh yet uh adapted to the production environment or something like that. >> Thanks. Thank you. Hello. Uh one question about uh I didn't see that in key clock you need to have policies in both of if you have 10 trust domains means that you need to have in 10 trust domains the policy what's the gain on this because what I can see is just STS that you've been using nothing more you could have a opa and that would be centralized when because the identity comes from the bey Anyway, so you have the workload identity if you have a centralized policy uh behind your STS >> uh you can define and be much more scalable I believe because in the end is just OIDC right uh the uh in the min code B you can go just to the key claw or SDS OIDC to validate that token anyway Okay, if you have a federation means that you allow otherwise not.

And also in the STS when you ask the token exchange, you can be blocking uh to create the access token because you have a policy engine behind. So I I'm a bit lost there. >> Mhm. is clear or >> I wonder I can uh understand what you say but uh maybe you mentioned regarding an opus and so that is kind of authorization >> and uh this is uh how to say uh not mentioned about that kind of authorization but only propagate the authorization uh data with authorized data and also work ID So yeah we focus is only that kind of phase. So that kind of aspect. So yeah you can implement authorization using something operable open. So but yeah today's demo uh we yeah remove that kind uh yeah technology here. >> All right thank you. >> Okay so time is up. So, thank you again and have a rest.