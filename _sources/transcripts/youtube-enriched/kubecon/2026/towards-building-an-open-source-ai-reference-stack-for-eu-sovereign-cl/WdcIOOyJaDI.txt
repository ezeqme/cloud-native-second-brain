Welcome to our talk towards building an open-source AI reference stack on U sovereign cloud. I'm Sanji Chari from Nvidia and my colleague >> Hi, I'm Madav Bharu from SAP. >> Let's get started. We are entering the era of sovereign AI and opensource software is the foundation of EU sovereign infrastructure because it is builds trust through transparency. What it really means for the U sovereign cloud is that this infrastructure needs to be rooted with European values such as GDPR compliance, data sovereignty, u you know data being processed locally. So when you build the AI reference stack on top of this infrastructure, it'll include sovereign models that have been trained with sovereign data. Um and data being processed by LM chatbots or AI uh agents will need to be localized within EU sovereign boundaries. So we want to understand the AI landscape a little bit today. So we can see that AI workloads are rapidly evolving. For example, the traditional single node serving model is slowly shifting towards complex multi-node multicomponent systems.

For example, disagregated inference architectures are typically composed of a mix of different kinds of tasks like computebound tasks which are memory bound. some some are latency bound and they all can run on you know not just homogeneous architectures but also heterogeneous architectures with CPUs, GPUs, LPUs, TPUs etc. So now Kubernetes has a new challenge. How to orchestrate multi-node multicomponent AI with disagregated architectures on heterogeneous compute at scale. And this enforces quite a few requirements for example hierarchical gang scheduling multi-level autoscaling topology placement and startup dependencies between these tasks. So in this in this talk we are going to explore how to build the an AI reference stack on Kubernetes that satisfies all of the above requirements in the context of the EU sovereign cloud. Thanks Sanjay. So we start we stand on the shoulders of U CNCF and and and Kubernetes. So SAP has created a foundation called Neoforce Foundation under the EU um under the uh which is funded through the uh European Union is an initiative called the important project of common European interest or IPSI.

This is public money and it unlocks quote unquote somewhere around 8 billion euros of investment. So with the goal of building a multi cloud multi-provider cloud to edge continum. So the participating companies and organizations are collaborating to power cloudnative in a fungeible fashion for completely sovereign purposes. Some of these projects are net new ideas and the others have been used in crucial production environments for some time. So now now let's dive deeper into the lower layers and explain how these projects are entering into joint EU AI cloud initiative. So you you have on the so you have a kubernetous layer in the middle then you have all these layers in the bottom then then these uh components come at the top and on the sides. So now let's dive deep into the air reference stack a little bit more and zoom in on it and let's start from bottom up. So we have data center resources like compute, network, storage and on top of that we have a data center management layer in in um so what we have is something called as a metal automation or a metal API which has several parts for example metal operator boot operator networking submponents and they all the the uh interesting part about this is that it actually uses the kas declarative APIs to actually manage all these resources.

On top of this we have an iss layer which has two variants. We offer one in as cobalt core which is open stack based and offers imperative APIs and then we have another variant which is iron core which essentially offers again a declarative gate as APIs. It has an extension API server which handles natively a lot of these resources and offers it to the end consumers. On top of that we have KS uh services and of uh Gardner provides uh KS manage K's offering and on the left hand side you see garden Linux which essentially is a foundation OS that we use uh all the way from bare metal to to the containers as well. On top you see Nvidia components that we bring in now which is Nvidia GPU operator and Nvidia network operator which essentially enhances the OS level and also the KUS node level to enable RDMA rocky and other network accelerations that are required for all these high performance AI workloads and on top of this Kubernetes layer you essentially have I only uh we have only shown these two but there are many other components that are there.

So you have Dynamo which is Nvidia Dynamo which is an inferencing and serving engine and you have Nvidia group that is developed collaboratively between uh SAP and Nvidia which provides a KUS orchestration layer for hierarchical AI workloads. Now let's dive zoom in on the IAS layer. So what you see in front of you is core and at the top you have core which provides core runtime interface. So there's two categories of interfaces that it provides. One of them is meant for enduserfacing which is to manage compute, storage, IPAM, network etc. and the other is for providers who are um who will actually provide pluggable providers. So by default you have se as an a provider for block and object store. You have um a virtualization provider which is libert which uses chemo and KVM and you have metal net plus DP service which uses DPDK and um uh to to essentially provide accelerated packet processing by bypassing the kernel and enabling enabling direct user space access to the next. So iron core also includes sub projects effectively and that deals with bare metal and network switch and routers.

So which we also talked about and which is highlighted in the in the previous slide in terms of bare metal API which we don't cover in this part of the slides. Now let's look at another component called garden Linux. Garden Linux is a very lean hardened container focused Debian based purpose optimized Linux distribution. So we chose Debian because you you can source support for Debian from your preferred partner anywhere in the world. It is not really locked into a single vendor distribution. Guard Linux runs the latest LTS kernel. It has a very mature build process which means the builds are repeatable and auditable. It is purely systemd based and from security perspective it has um the built-in security modules which are SE Linux which are very popular in US. You have app armor which is popular in Europe and you have uh systemd which is moving to landlock. So we also support that. For security features we have fully signed kernel module and boot path and there you can get your own keys and actually uh get it signed with your own keys.

So we support secure boot, trusted boot. Garden Linux also offers you capability to build flavors of it by choosing feature sets and then you can build flavors of guard Linux and you can see now how we have used such uh features to actually also enable um AI capabilities on top. Ghana Linux runs on servers switches smart nicks and GPU hosts. So as part of the AI reference st implementation we also have enabled and as part of a feature what we talked about in the last slide we have actually developed a feature which provides a garden link flavor which has capabilities to um provide support out of the box for RDMA GPU direct RDMA rocky and from Nvidia we have ening support for that. Now what we include as part of the core kernel modules as part of kernel Linux some of them are DMA DMA buff you have RDMA specific kernel modules you have um Melanox kernel modules then you have Nvidia specific kernel modules some of which are Nvidia and Nvidia UVM and these are proprietary kernel modules at this point of time but we are planning to move to open source uh Nvidia kernel modules u because those those support are actually from architectures which are touring and newer generations For kernel parameters and systemd settings we have uh they are set specific requirements from RDMA to work properly and so IO MMU settings um me lock settings have been have been changed as well for the network configuration we are using jumbo frames because that is also a requirement from RDMA which means MTO has to be anything beyond 9,000 plus then we use the uh Nvidia GPU operator to set up uh the net the Nvidia fabric manager which essentially configures routing uh among NV switch ports and monitors fabric for NV link and NV switch errors.

We also use Nvidia network operator to set up SROV which provides direct hardware pass through for lightweight lightweight virtual functions. So MPI and libraries which are sitting top on top of it like nickel can actually use true zero copy kernel bypass GPU to GPU communication at line rate full line rate. Now let's focus on the next layer which is essentially a gardener which is a hosted control brain uh offering from u from SAP. Now Gardner is a cers as a service product that is even shipped on premise to all the other cloud providers that is run by the new force members and others and beyond. The enterprise characteristics are the life cycle management and upgrades are part of the product. So adopters do not need to worry about fiddling with uh you know uh devops to secure SLAs's. So a self-hosted gardener offering is used for fully airgapped. So we we also have the self-hosted garden offering which can be used for fully airgapped and highly secure environments. So on the right hand side what you see is a shootute cluster which is the end userf facing cluster where you can actually deploy your workloads and the control plane of these shoot clusters are now hosted and been packed into another KUS cluster which is a C cluster.

What makes this KUS cluster into a C cluster is a component called gardenit which is very similar to what you have if you don't want to draw parallel. So you have a cublet which makes a VM into a node. Here the gardenlet makes this cluster into a C cluster. So the the the uh benefit of having this bin packing this shootute control planes into a series is that you don't have to you can optimize in the resource usages which means you don't have to ded start dedicated VMs for hosting control planes or each of these uh shoot clusters. you essentially um have the you leverage the capability of self-healing capabilities or offer out of Kubernetes if you carefully um configure your livveness and readiness probes because all these shoot control pane components are actually started in a name space a dedicated name space and which are running as deployments stateful sets and and so on and so forth. on the right hand side on the left hand side you can actually see the garden cluster which essentially is the heart of the entire hierarchy of these clusters here is when where you have the capabilities so there are two sets of APIs that are offered out of garden cluster it runs an extension server and one is one set is a consumerf facing APIs which you will uh interact with to create projects to create shoots which you see on the right hand side and and which you will operate on and the second set of uh APIs are for extending in gardener.

Um, so Gardner offers extensibility at multiple layers. So as a user, you will use the standard coupube cuttle tools to connect to the garden API server and the exposed cube APS server for the shootute control planes to access your shoot clusters and manage your workloads. Now let's look at the garden extensibility. So gardener core is we have kept the gardener core as quite minimal which means that we now have an extension framework which provides capability to extend gardener at multiple layers. So you can have gardener can can be hosted on any provider. So AWS, Azure, GCP, anorack etc and so on. So you can add your own ex uh infrastructure provider. Similarly OS run container runtimes network etc. For the purpose of the AI reference stack implementation, we use these mechanisms to actually extend Gardner to introduce the Nvidia AI acceleration layer which then automatically installs a lot of Nvidia specific components like the GPU operator, the network operator, Dynamo, Grove, Kaiul and and so on and so forth.

Now for zooming, let's zoom in on the AI acceleration layer now and I'll hand over to Sanjay to zoom in further. >> Thank you Madav. Okay. So now above the Kubernetes layer in this talk we'll focus on the Dynamo and Grove as the components for the reference AI uh inference framework uh for this particular talk. So let's take a typical disagregated inference workload in Dynamo. It it contains modules like uh tasks like prefill decode and a module called front end. Now these pre-filled tasks are essentially something that generates the context of an inference request builds the KV cache. Now this KV cache needs to be transferred over to uh a decode component which will read the KV cache and generate tokens. So what happens is we need to also understand that this KV cache transfer taking place meaning meaning that the prefill and the decode components need to be topology optimized and packed. So, Nixel libraries are packaged into the image to ensure uh to optimize the KV cache transfer over high-speed interconnects uh like NV nlink or any kind of RDMA fabric.

And the front-end module that I talked about basically acts as a API server. It also uh has a KV aare router which will essentially try to maximize the KV cache uh reuse and then all of this the workload finally gets orchestrated through Grove and then Grove works with Kai scheduler to ensure that the gang scheduling constraints the topology aware constraints and all of that gets satisfied uh and the and the uh application gets deployed U on the on the most optimal set of hardware. And finally, there is a another key component in Dynamo Planner. Uh it integrates into Grove as as an SLA based horizontal autoscaler and it can autoscale uh the workloads at different levels and uh it it basically reacts to real-time metrics from the Dynamo application. Now we I was talking about Grove as uh the API the Kubernetes API and that can do hierarch hierarchical gang scheduling. So let's dive a little bit deeper into this API and understand how it does it. So essentially Grove provides a single unified API to the user.

It's called a pot click set. And this allows the user to hierarchically compose the entire AI workload using a primitive called as a pod click which is a pot click is a collection of pods of the same template. And with these pod clicks they can be either you know used individually within an AI workload or it could be composed of multiple port clicks as a scaling group. And the scaling group essentially allows you to uh do something like you can group together multiple port clicks and then scale them proportionately. So with this composition of port clicks and port click scaling groups you create a port click set and the user essentially once that it creates the port click set uh then grove basically takes over and uh you know ensures that its scheduling constraints are satisfied. So uh it is essentially uh a system where you can actually set your scheduling intent using the port click set and and grove works with kai scheduleuler to make sure that these are satisfied. Okay, so now let's look under the hood at what is happening, right?

So when the user creates a pot click set uh the grove operator will create pot clicks and port click scaling groups as sub resources and they will have scale sub resources uh which allow independent scaling of these uh of these objects and then uh it also creates additional resources such as service account secrets u headless service for discovery for example and all of this essentially uh gets created and managed by grove under the hood um for multi-node link uh like multi-node envink architectures like GB200 grove can will automatically generate the compute domains and uh manage you know IMX domains and channels for the user. So that is all abstracted from the user and then finally grove will generate the pods and and put all the scheduling constraints into something called as a pod gang. Now this pod gang essentially captures all of your scheduling constraints for the backend scheduler and grove currently has like a backenduler framework which will basically you can uh you know target any kind of scheduler for example kai scheduleuler volcano coordinator or or or just use the default cubeuler.

So with all of this together uh Grove manages to ensure that it it provides the uh capabilities of hierarchical gang scheduling topology aware optimiz placement and so on. So now I I want to hand it over to Madu and we are going to talk a little bit about the demo. So for the demo we have a lab setup and if you see here we have four B200 NVDA B200 nodes which have two sets of nicks. One of them is the blue field 3 DPU which is essentially used for it's been set up primarily for north and south traffic which has 2x 100 GB bandwidth that essentially forms a data center network fabric. And then we have 8x Nvidia connect x7 uh nyx which essentially are uh 400 GB each uh bandwidth and they are set up to provide um rocky which is RDMA over converge Ethernet. What we have is a single switch which uses um which we use as Nvidia Spectrum X 4 which has 800 GB Ethernet. It's a it's a 800GB Ethernet switch. So that's a very simplistic setup. Of course, for a more complicated setup, we will probably move to rail based optimized network fabric, but we have not done that as part of this lab setup.

So, let's move to the demo. So, we are trying to do a live demo on a lab setup. So, let's hope everything works. >> Oh, it's not visible. >> Can we mirror the screen? All right. Okay. Is it Where is it? >> There you go. >> Ah, there you go. Oh, now I can't see it. Okay. It will be difficult to type like this. Can we mirror the screen? I thought it was mirrored. >> I can't do anything. >> Okay. >> It must be done here. >> All right. So, >> PowerPoint is on. >> Yeah, I can see. >> I think we can see now. Yeah. >> No. >> Yeah. Power. >> Okay. Power. >> All right. All right. All right. All right. Thank you so much. Thank you. >> All right. So on the left hand side, what we have is the um so what you see here is a com is a terminal that's connected to the garden runtime cluster. So the part that you see on the left hand side and just to show you the extension. So wait for it. And there you go. So you have the uh we have created a new extension called the extension AI acceleration. And let's get the extension YAML file just to show you.

Come on. And it's slow. Unable to connect. Well, let's see. Disconnected to the right cluster. Let's try it again. >> Maybe we can play the demo. >> Yeah, let's go to the demo then. So what I wanted to show you was the the AI acceleration extension will have is set up um with a policy called on demand which means that um when you as a when you create a shootute cluster you essentially define an extension configuration and if you go to uh one of the shoot cluster definitions which you see here this is the demo cluster that we have defined and the provider is core that's the ES part and if you see the YAML and it takes time. Okay, in the meantime you have this here. So the policy is on demand. So as you can see which which effectively means that uh when um there is at least one shoe cluster in the seed that that requests for this particular extension then on demand there'll be an operator sitting in the garden runtime cluster which will actually provision the controllers for this extension in the seed cluster and then the seed cluster the controllers that are running in there which will create resources that are going to be uh provisioned eventually on the shoe cluster.

So let's directly go to the seat cluster. And if you can see here, you get a new name space for this particular extension. I hope it comes up. Yeah. And you have the extension AI acceleration here which essentially is running the controllers for this particular extension. So from the purpose of this demo, let's set up graphana. Let's also go to the the shoot cluster. Now let's starts so that we can it's easy for you to view. Everything takes time. There you go. So is it visible? >> Yes. All right. So what you see here is let's start with something called as a Dynamo graph deployment and for that let's explain what we are what we having as a reference application. So we have Dynamo graph deployment which has three services. One is decode, another is front end and then you have prefill. If you go to decode then you will actually see that this particular application is requesting eight GPUs. So as we said in the setup we are using Nvidia B200s and each of these nodes are having a GPUs. So which means this will fit into one node.

Similarly you have uh prefill which also is requesting AGPUs. And as you can see on the right hand side this is the layout of the pot click set. So it's a very simple example that we are taking because in the lab setup we only have four B200s. So we have one port click. So which we represent the front end the router server as one port lick which has replicas one. Then you have another port click which is the prefill worker and in a distributed aggregate this aggregate setup you will actually have this prefill divided into pre-fill leader and a prefill worker. Similarly but for this for this example we are only having one uh and then you having then you have a decode worker which also has a replica of one which also request eight GPUs. Now let's look at the D the Dynamograph deployment which we call in short DGD and here you see that this is actually being reflected here. So it's a pretty large um CR and now what it creates behind the scenes is actually um a port click set which you see here as Mistra large NV FB FB4 disag.

So the Dynamo has a component called as a Dynamo operator which integrates with Grove and and creates the portick set for for Grove. >> So once the portick set has been created now here on Grove now will become active and it'll reconcile and it'll create something called as the pot clicks the connection. Okay. So we back. So it creates pot licks and each of these pot clicks borrowers has a specific naming convention. So it starts the prefix starts with the the the portick set name followed by the replica uh index of the portlick set followed by the name of the portlick. So here the role of this portick is decode then you have front end and then then you have a prefill. Now if you go to the scheduleuler API that actually it created for this particular uh reference application essentially you'll see that there are three pod groups each pod group is now mapped one is to one to a pot link. So you have one uh and it has pod references which actually gets updated run at runtime whenever pod goes down and a new pot comes up and replaces it.

It uh automatically refreshes this particular CR all the time. So this is then used as a construct which is then used by the backing scheduulers to actually gang schedule all of these pod groups together as a as a single gang. Now let's go since we have already deployed this application which is mistral large NPF4 because it takes quite a while for this application to come because uh at startup the init containers loads a lot of weights so it takes somewhere around 5 to 7 minutes sometimes. So we have actually deployed this applications here. So we also have let's start a graphana dashboard. Uh let's put forward second. Okay. So I have put forward the graphana dashboard. I have a chat app ready. Let's go and open the graphana dashboard. Let's just look clos and let's look at the dashboards here. All right, let's move it to the right. or not. And then what we start is we have um we use hey to actually load uh this particular application with a lot of post requests to the chat endpoint. Let's start that.

Okay. Connection refused. That's it's not loading. Okay, then this here application is not loading right now. So let's start um the chat app. At least this will work. All right. So let's open the chat app. Okay. So we created a simple chat app to connect and talk to this inference endpoint. And let's ask it a long question which so let's explain it in great detail the theory of general relativity and let's hope it answers and unfortunately nothing. It'll take some time. >> It'll take some time. Let's look at the dashboard and see if it comes up. Okay. All right. All righty. No, it's not going to come. And in these situations, we can only >> I I think we have also a demo recording that we can actually show during our booth as well. >> Sorry about this. So just to show you a quick um demo and what happens in the end, the end is important. So essentially you can see that As you can see that the front end requests per second are increasing. You have the average time to first token and it essentially stabilizes somewhere around 30 millconds.

ISL to OSL ratios are also reflected properly. Uh so this is what uh these are graphfana dashboards which are provided by Dynamo and this we also have loaded and this were actually working but unfortunately there's no connectivity right now as part of a live demo. Well lab setup so that's all. If you have any questions then please feel free to ask.