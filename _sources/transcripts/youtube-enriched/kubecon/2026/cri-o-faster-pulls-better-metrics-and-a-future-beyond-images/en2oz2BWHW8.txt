Good afternoon everyone. Thank you all for joining us today. Uh I know it's the last day of CubeCon and I hope you had enjoyed some interesting sessions and we are going to make this session also engaging and we have a lot of things to cover in this presentation. So let's get started. Let me let me introduce myself. I'm San Kerkar. I work at Red Hat as a senior software engineer. I'm one of the Kaio maintainers and actually involved in sign activities. I'm also uh contributing to Q which is uh queuing system for batch workloads and I'm the reviewer of the project. With me we have Ayatu Tokobi. He's also the co-maintainer for cryo and actually involved with sig node. So here is the agenda for today's talk. We're going to quickly cover the introduction to cryo for those who are new. Then we'll talk about the cryo updates including the sign node that we are working in the upstream side. Then towards the end we'll see how uh peer-to-peer image distribution fits into cryo system followed by a short demo and towards the end we'll open for question answers.

All right can I get a quick show of hands how many of you know about cryo? Cool. So I'll probably quickly cover what is cryo. Uh it's as you see the acronym CRI it's container runtime interface. O came from the open uh container initiative. So it it is specifically designed for Kubernetes. It implements Kubernetes container runtime interface and supports OCI compliant images, registries and containers. So in a nutshell you can say cryo spins up the pod, creates the containers and dumps a log where it should be. So in cryo we believe uh simplicity balanced approach where we try to make sure we get some of the features which are battle tested both in upstream kubernetes as well as the cryo side and uh cryo release version goes lock step with kubernetes one. So you will always see kubernetes releases uh and the changes in the latest cryo release and last but not the least but very important cryo is a CNCF graduated project. So it's majority defense with the wider adoption across both open shift and kubernetes.

So with this I'll hand it over to who will cover some of the cry updates. Hello everyone. Uh as you probably already know um cryo talks to cublet to know like what containers and what ports are running on that node. And I'm going to talk about some uh Kubernetes upstream related uh features in cryo and also some like cryo uh specific uh features in my part. So first one is CL list streaming. So in some cases if you run a high chain crunch job workloads or CI/CD platforms with many shortlived containers you may have hit a wall where the cublet simply cannot list containers on node anymore. Not a timeout a complete failure the RPC returns an error and the cublet has no visibility into what's running on the node. Here's why that happens. The CI list operations, list containers, list post and box and the static equivalents are unary RPCs. The runtime collects all the results, serial serializes them into single one broadbuff message and sends the entire response back to the cublet in one shot. JC has a hard promises limit currently 16 megabytes defined in the cublet.

It becomes a problem when there's so many resources. When a node when a node hosts 10,000 or more containers or ports, the serialized message uh exceeds that limit and the RPC fails entirely. The limit has already been raised twice from 4 megabytes to 8 megabytes to 60 megabytes. But raising it again would just be kicking the can down the road. This proposal addresses this by introducing serverside streaming RPCs uh such as streaming containers uh stream post and boxes stream images and a few stats related RPCs. Instead of packing everything into a single port buff message, the runtime sends multiple messages over a stream and each contain a chunk of items. Each method accepts the same filter parameters as its unary counterpart. To be clear about what this does and does not solve, streaming addresses the JPC message size limit. It does not reduce memory usage because the cublet currently collects all streamed items into a single list before processing them by design. So the full result set is still held in memory.

The benefit is that list operations no longer fail on high density nodes. The backward cross compatibility story is clean. The cublet attempts the streaming RPC first. If the runtime returns unimplemented, the cublet falls back to the unary RPC. So you don't care about the um order of the update cublet and the content runtime. Uh this feature is entering alpha in Kubernetes 1.36 and we are expecting cryo to have that uh feature in 1.36 as well. The next one is also related to a kubernetes feature and like more of a kernetes but yeah it's rel related to cryo and container runtime interface. Uh this one is cli stats. So this proposes collecting container and photo statistics through the content runtime interface rather than through C advisor. To understand why this matters, let's look at how st collection works. Today the cublet relies on C advisor which is compiled directly into the cublet binary C advisor is sele files scrapes proc and maintains its own inmemory data structures to track CPU memory file system and network usage for every container on the node.

The container runtime also tracks much of this same information independently because it needs it for its own operations. So you end up with two components on the node both reading the same kind of interfaces both maintaining their own state and occasionally disagreeing on the numbers. That's not a great architecture. The idea behind CI starts is straightforward. The cublet asks the runtime for stats via um the CLI CLI RPCs and the runtime responds with the data it collects. This eliminates the duplication and moves stats collection closer to the component that manage actually manages the containers. Where do things stand? CI stats was supposed to graduate to beta in Kubernetes 1.36, but it remains in alpha. There's a reason for that. On the contr runtime side, we implemented the required metrics for graduation, but the performance testing revealed issues that are not on that are not on the runtime side. The benchmarks showed no performance improvement in cube even though it uses CI stats. When we dug into the results, we found that the cub still has the C advisor code path running even when this feature CI stats is enabled.

The C advisor housekeeping code contributes additional resource usage on top of the CI stats. So you are effectively paying the cost of both. So these aren't fundamental design problems. There are issues in the cublet that need to be addressed before the feature can move to beta. The C advisor code path needs to be removed from the cublet and that work is planned right now. The long-term direction is clear that should come from the runtime not from a separate monitoring component compiled into the cublet and this feature is purely a cryo feature uh the additional artifact stores. So up until now cryo stored all OC artifacts in a single hardcoded path under the graph specifically graph artifacts that works fine for small scale use but it becomes a limitation when you need to place artifacts on different storage back ends or share them across nodes. In 1.35 we added a new configuration option called additional artifacts stores under the cry runtime section. This lets you define a list of additional directories where cryo will look for o artifacts.

These stores are read only and are checked in order before falling back to the default location. If an artifact is found in one of the additional stores, cryo uses it directly without copying it into the default path. Why does this matter? A few potential scenarios. First, machine learning workloads. Machine learning models distributed as OCI artifacts can be tens of gigabytes. You may want those on dedicated high throughput storage like NVME volume and network attach file system rather than on the same disc as your content images with additional artifact source. You point cryo at the storage and the models are available without duplication. Second uh prepare prepopulated caches in environments where you control the node image. Uh think immutable infrastructure or customer spills. You can bake artifacts into the image at a known path. Every node boots with those artifacts already available. No p required. This is useful for air gap environment um where nodes may not have access to a registry at all.

Uh third is uh shared storage across nodes. If multiple nodes mount the same read only file system say an NFS export or cloud file store you populated once and every node in the cluster has access. This reduces bandwidth speeds up port startup and simplifies artifact distribution. The idea of what can be achieved by this is quite similar to the persistent volume but it can be used in the environment where the persistent volume can be used or and the control plane doesn't need to prepare the volume during the scheduling the port. The implementation is straightforward. Cryo iterates through the configured stores in order checks if the requested artifact exists and return it it if found. So there's no maring or overlay logic. It's a simple lookup chain. This shipped in cryo 1.35. This is currently an experimental feature and the behavior is subject to change in future releases. But uh we welcome any feedback and the syntax and behavior may evolve based on the feedbacks and I'm going to talk about some uh minor security updates.

The first one is configurable TS um cryosmetrics endpoint and streaming endpoint that is used for exec and port forwarding is uh HTTP endpoint and previously used hardcoded DS settings as of 1.35 you can now configure the min version and the set of allow sati um cipher suites this is relevant for environments with strict compliance requirements uh of RAM, PCIDSS or internal security policies that mandate specific TS configurations. You can for example enforce TS 1.3 only or like TS 1.2 with uh cipher suites approved by your security team. This is also relevant for postquantum cryptography PQC. As PQC cipher suites land in gold TS stack, operators can enable them through configuration. The configuration is in the cryo API section TS minim version and TS cyers suits and applies to both the streaming server and the metric server endpoint. The second one is openvex ports. We will start providing openex vulnerability ports uh for cryo when releasing new versions. This uses a goound check under the foot to scan the go binary and its dependencies against the go vulnerability database then outputs the results in the openvex format.

Open vex is a standardized way to communicate vulnerability status whether reported CV actually affects a given artifact whether it's been fixed or whether it's uh not applicable. This is useful for supply chain security workflows. if your organization tracks uh software compression and vulnerability status across your infrastructure. Uh both of these uh cordial life improvements, they don't change cryo's core behavior, but they make it easier to operate cryo in environments with specific security and compliance requirements. Okay, so next I'm going to pass it. >> Thanks Ado. uh one last signal update before we move to peerto-peer stuff. All right. So let's talk about memory QoS. So I want to discuss about this problem because uh many of you uh doesn't know about this problem when exists in the first place. It's a gap. So imagine a use case where you have a node with 8 gigs of RAM and three pods are running. The first is the guaranteed pod which request for two gigs and consumes one gig which is still within the control.

Pod B is the burstable pod which request of 1 gigs and utilizes one which is still um under control. And pod C is the best effort one which doesn't request anything but ends up eating 3 gigs of memory. Now when the memory pressure hits, which pod do you think will uh evict first or dies first? In a wildest imagination, we think about pod C because it's a best effort one. But here is the deal like uh Kubernetes sends one hint to kernel for saying score and cublet says score low for the guaranted pods and higher for the best effort one. So when kernel tries to kill a pod, it will look for that score and based on that it will uh it will basically kill the pod. So in this case is best effort but omill is like the last result for the kernel. So before that kernel tries to reclaim the memory and uh for reclaiming the memory it can basically evict the page cache or it can do node swap. So kernel doesn't know about the hint in that case and uh request memory request memory is never uh communicated to kernel at all because it's a scheduleular hint.

So uh there's a possibility where kernel can reclaim uh the guaranteed pods memory because uh irrespective of its QoS class. So reclaim is the problem here. It not only spikes the latency but also degrades the entire performance. Uh even if your pod is not getting killed, there's a issue where it might get the degraded performance and whenever you try to access it, it might go into page defaulting access. So what memory QS is doing it is actually closing the gap saying the kernel we have this memory don't reclaim it and that is where I'm going to the ke which we uh reintroduced it uh and right now in v v1 alpha 3 so there's a history behind this ke it was first introduced in kubernetes 1.22 22 where we used to set memory.min for all the pod memory based uh pod and the idea behind that is to bridge the gap between the scheduleuler and the kernel. However, we realized while uh calculating the throttle value that is memory high the calculation was bit off because we were setting the throttling factor as 0.8 which was more aggressive than it was required.

So we made that change make it work but then in 1.27 we hit another issue which was related to kernel which is live block. So what it does is like you know when kernel when container approaches me memory.h high but it's not uh uh when it reaches to memory. kernel will try to throttle that and it will try to bring that down the usage bring that down but the container will actually go into throttle reclaim loop where um container allocates more memory than kernel could actually reclaim it. So this fix was actually introduced with kernel 5.9. So before that you will definitely see that live lock issue and that's where we again retargeted that to 1.36 with alpha version and we changed the way we are defining uh the memory protection. So previously we used to use memory.min for all pods. Now we are going to use memory.mmin only for the guaranteed one and we're going to use the QoS class level protection. So what does that mean for everyone? So guaranteed pod will have memory domin because at the end of the day we have request equal to limit for that.

So the entire requested memory is protected. We don't need memory memory.h high for that. For burstable we are using memory. for the soft protection which indicates that the kernel will reclaim at some point of time but um that is like a trade-off that we said because we don't want to overcome it on uh memory protection budget that we allocate and last thing is the best effort we don't request so there's no protection for that so here is the croup hierarchy and the observability part that I'm going to discuss so when we have done with the um code changes. There was a uh issue that was raised and there if you see like the hierarchy you have cube pods like the ben root C groupoup inside that you have main pod that is guaranteed and you have slices like the burstable and the best effort slices. So for guaranteed QoS class we set the memory.mmin which includes both burstable as well as uh the guaranteed request and somebody raised that request why we are actually inflating uh the memory protection.

So here's the answer for that. uh when you talk about from the kernel docs it says when you think about the cgroup hierarchy you look for the croup protection for your child that is dependent on parents uh cgroup thing and uh you need to have that cushion in place where uh memory should accommodate both uh guaranteed and the bustable request under that otherwise it will squeeze in some of the memory from memory uh from guaranteed pod to the bustable one in order to uh make sure that it will have enough coverage for the croup protection for the children's. So we addressed that uh in alpha and as I said for best effort uh there's no protection. We also added bunch of Prometheus metrics uh to understand how users will use hard and the soft protection. So while doing this we realized we are mostly talking about memory protection. So what happens for the me memory throttling you know. So in kernel decides that with memory events where the usage goes beyond memory. Memory domax we can actually see the counter there.

However, um that feature is still not there and we are thinking about creating a different cape for release 1.37 uh which will not only solve this particular feature but also has some other use cases including the eviction policies and um while having this feature development we also encountered a use case where the unified field under update container resources was not updated with cryo. So what it does it basically sends croup away to field to um runtime. So in this case would be memory. And high and um so think about this in place part resize right when you do that at runtime uh your memory request changes cubit recomputes uh the protection but it was never conveyed to cryo because we were not setting the unified field. So this fix was addressed in 1.36 and hopefully we'll get that uh sooner. All right. So, yeah. So, enough about uh memory. Let's talk about something which is interesting. Let's talk about peer-to-peer image distribution. Um you can see on the slide the traditional image is like 200 MB.

As you do some complex stuff related to AI, the images are getting bigger and bulkier. PyTorch is 8 gigs. Then LLM model is roughly between 20 to 50 gigs. Now you can do the math here. You know you have 100 node GPU clusters and you're pulling 20 gigs of model that is like close to 2 terabytes of egress that is for deployment and uh for every roll out of the model. So um you can also question me like okay recently we uh g OC OCI image volume feature uh with 1.36 it is G8 uh feature where you can mount OCI artifact as a read only uh volumes in in your pods but that cape actually solves you how to use uh use case and not how to distribute images across different nodes you will still end of hitting the image registry across different nodes to pull that image and that is where Spiegel comes into picture. Spiegel is a stateless local OCI cluster image uh registry immers P2P protocol uh to do peer-to-peer uh distribution. So how it works it it it's it's a demon set that runs across different nodes and so there's no uh central repo it's all decentralized.

So if the pod dies you need to spin it up and then again um they calculate the layers and kind of distribute across different nodes. So this might not be a good kind of a high availability registry but certainly a good um use case for solving some of the AI use case that we have. Um so uh talking about Spiegel, Spiegel what it does like when you put the image Spiegel intercepts that using its uh distributed hash table. It stores the layers and when another node doesn't have the image it's pull uh its image pool gets intercepted by Spiegel. It looks for the um respective blobs. It fetches from local uh from local network rather than going to uh egress and that's where we are saving 90% of the bandwidth. Now uh coming back to integrating Spiegel and Cryo. So Spiegel was predominantly developed uh looking into consideration the container D architecture probably for cryo we had to do certain uh changes. So if you think like for cryo we are using container storage as the library to store image layers and we don't uh store compressed layers on the uh layer system.

So uh we introduce a new layer called block cache. So we will keep the same flow. It's just that t- reader will have split into the two things. The first would be the normal flow where cry will pull the image put into the overlay system. We'll have the overlay layers and second would be a blob cache layer. It will store all the compressed layers that we have. And uh it will also track the metadata metadata.json JSON which will have the blobs and from where it's actually pulling from. This work is still work in progress. There are a bunch of things we are discussing but um I want to just kind of give you this overview like how it looks like. I'll show you the demo as well. All right. Yeah. So, I'm going to use the script to make it faster. Just one one disclaimer is um I'm deploying uh Spiegel as a binary and not using Hemchart. So, yeah. So I'm going to have the cluster configuration where I'm going to use the local binary that we with all the changes that I discussed before and I'll go ahead and create a cluster with that config.

I think it might take some time. I'll just fast forward. Yeah. So, yeah, the cluster is up and running. Let's export the cube config. I think the port is all right. So, uh we have all the nodes that we need. Now here is the thing like we are defining the following thing under cryo config we'll be setting the image content cache directory. So that is like a user specific knob that we had given it to users. So they can uh define where they want to put their cache blobs and uh there's one more option to auto reload registry. So if the registry is stale then um cryo can reload that uh and you need to also define the time through which it will reload the registry and it will pull from the latest one and we also as part of the cryoss architecture we also need to provide the mirror uh registry configuration. So, I set it to local host uh 500. And I'm going to copy those two files into the worker nodes, worker zero and worker one. Copying start. So, I'll go ahead and restart cryo to pick those configuration.

And I will also do the similar for block cache config on the worker one. All right. Now I'm going to copy this binary that I built locally for to both the workers and I'm going to get the IP for worker zero because that is going to be that is going to be my seed um node through which I'm going to distribute amongst other workers. So, I'm going to go ahead and uh start Spiegel. There are a bunch of uh flags I need to set to make it work. And um I'm going to do similar for worker, but this time I'm going to connect that to worker zero. So now P2P network is up. Now I'm going to pull an image from worker one, worker zero. It's going to be from the docker.io. So we have that image. Let's see if we see the layers. So we can see the content is there and layers are also present. Now I'm going to pull the image from worker one but that image will be pulled from the local network and not from the eress. So if you could see it already pulled the image and let's see if um it is done via local network or not. So I'm checking the journal D logs.

Yeah, you can see in the logs it is use local 500 to pull that image. All right. Where's the Yeah. All right. So, um I want to leave you with one food of food for thought. Uh in Kubernetes nowadays, um there's a hot topic about aware scheduling. So, uh we were just thinking what if knows about identifies the actual node where the images already exist. So you can think about an uh example where you have a pod with uh 45 gigs of model that you want to spin it up but you already identify that pod before you already identify the pod before you uh scheduleuler could actually put that on a particular node. Um we can think of it as a like you know uh you have 100 uh 100 deployments each is pull each pulling the same image 100 times with P2P without P2P with P2P one image pull and 99 peer-to-peer but with uh P2P aware scheduling you only pull once and uh but at the right time and at the right node if the disc size is pull you still need uh you are still reducing the count for P2P thing like uh instead of doing 99 times you can do it only once and you can find out the node and probably place your part there.

We are still evaluating different uh mechanisms. Uh there's one called we can write scheduleuler plug-in but we need to understand um uh how what it takes for the GPU nodes and the resource constraint. We're also thinking about uh because Spiegel has locality API through which you understand the blob and layers. So probably um and there's also a plug-in uh with cubeuler called image locality. We might extend that. We are still discussing uh some of the options. So feel free to uh kind of uh discuss that and we are going to add that in the future road map as well. And if you have any questions reach out to us on GitHub and Slack. We're always there. And with this here is the QR code. Um give us a feedback. Let us know what you want to hear from Cryioite. And yeah, thank you