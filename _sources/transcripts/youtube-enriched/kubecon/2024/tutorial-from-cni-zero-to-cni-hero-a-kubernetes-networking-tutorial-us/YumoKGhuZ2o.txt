hello okay let's start this tutorial sessions so this tutorial is about the cni from cni Z to cni Hero the kubernetes networking tutorial using cni I'm Tomi and and I'm Doug Smith so I'm tomashi working in the red hat and then works for the open shift engineering as well as the in upstream cni and M cni maintenance as well and join the network Plumping working group as well and I am Doug Smith I'm also a member of the network Plumbing working group uh Tomo and I both work together on multis cni which is a cni plugin that allows you to attach multiple network interfaces into pods and kubernetes um what tomo didn't mention is that Tomo's also created a number of other cni plugins such as ones that you you may might even take for granted like a static cni um cni plugins to override routes um and we've also I've also created another cni plugin called whereabouts which is for like a dynamic uh allocation of IP addresses so today what we're going to look at first is an intro to cni Tomo's going to walk you through everything about how cni Works um in the end really uh don't it might feel like a lot but really it's in the end it's really a few just simple things that you uh need to take a grasp of what we will do is we will be very comprehensive here with that in mind we're also going to walk you through configurations and all of the details and how you can actually configure cni Tomo's then going to take a glance over how you're going to develop cni plugins if you have an interest in that and as Tomo kind of noted uh originally is there are a number of slides that we have as reference material in the slides to download that uh we decided were maybe too much detail but they could be really useful for you when you go there um with that all in place since you've got the basics we're going to do a Hands-On tutorial um where uh you can follow along if you'd like download the deck there's a link to a um GitHub repository that's got configurations for kind and all of the resources that are used in there um there's also kind of a bit of a troubleshooting aspect to that and then I'll kind of um touch on that and last but not least we'll kind of show you how you can get linked up with the cni community and where all the resources are um with that being said I will let Tomo um kick it off with an intro into cni thank you D so let's cover the what the cni is first important stuff right so what the cni does for you is maybe the simply to say in case of the kubernetes cni provides the network connectivity to your kuber part so if you Runing the uh some simple partt at that time if you typing the IPA I mean IP address command with the rux then you can see the loop back and if Val is added right so that is CNA PR Des I mean that uh loot back address and E EET uh interface is created and they put your network Nam space of the bo I'm describe out the network name space later and then in additions of course isoz have IP B4 or some case in your deployment have the IP B6 address as well so this IP assignment is also that's uh but is done by a cni and then of course if some network provider do some specific additional configuration such as the NF table IP table rules or interface MTU changes this seems to be that in the by a cni program as well so when the power is added this stuff is done by cni and then of course you know they open the door and then close the door so if p is tearing down at that time that cni program is removing this IP address and also the interface and then try to the next usage so here is the U rough picture about rough diagram of the kubernetes deployment in the worker note worker Note have CU and the content run time like the cryo continuity and inside the cryo continuity lip cni code is in so lip cni code is from the cni uh GitHub repy and then we have the parts so today we'd like to describe that the cni config and the cni binary so that these stuff is a target to describe but before that let's go focus on the pot so the pot is not container you know so based on the cuetes document the PO is uh shared context of pot is a set of the rux namespace C potentially other faet of the isolation so Linux namespace is the partitioning feature in the Linux conel so this is used for their container or some stuff so the container uses the Linux name space such as the P ID amount and S and then isolate the uh resources from the continer host side and then container is little bit different from the pot so pot is not just a container this poort could have a multiple container inside the one partt object so this means the multiple P ID and name space and also there are multiple Mount name space is in the pot but regarding the network multiple net container sharing the one network name space that is is a little bit unique than the usual container Docker or apartment and so on so this cni is invoked at the mainly two types I mean the pot creation and the PO dire so that this uh you know the you already know about that the cni is the pring architecture so each cni uh implementation is provided as the prod so cni project under the cncf provides the several plugin as the reference implementations Mac B IP B and Al the host local IP management and the third party I mean that the several vendor uh provides the CN pling as the cico open commes and so on so so the when the so how CN programing is used when the aort is created so this picture is showing that the this stuff system for so the okay let's imagine you create uh you do the cube cut create part then this part object is goes to the API and then the cubet recognize this part object and after that kubet sending the uh RAM Pond soundbox grpc code uh through the CN C content rantom interface container one time is recognize this stuff and then try to start the uh P soundbox which means the uh SE out PID name Linux Nam space and of course the network Nam space as well and after that contenter run time call uh lip cni to create the interface lip cni reads the config from the cni config directory so mainly this seems be the cni N then using this config lip CA invo the cni plugin cni plugin get the these informations given by the liip site then creating the network interface so that's the uh whole picture of the how cni plug ised so let's focus on the cni config side so first things cni config is the text Json file not the yam file so yeah of course the if you are good at the kubernetes everything is written in the and then you may thinking why c i using the Y Json not the yamu file so this is this rightly kind of the historical point of I have they have I mean that the CN is not under the kubernetes I mean that the CN is just the uh separate project so this means they are officially KU the how to say cni is pretty independent of the kubernetes and then kubernetes uses the cni this stuff so that that's the stuff so so there yeah unfortunately CN configur the Json text file and then this file name should be end with confl like the Z1 Huber confl so sometimes if you looking in the old implementations deployment at that time they you may find you may find the fua.com but in this presentation I'd like to focus on the com list because the com is a little bit old and then this is replicated at the latest cni version so so that this uh so in if you putting in your multiple uh cni config in the cni directory but the cont and the cryo takes only one file from config directory so that this stuff is choosed by the first file by sorted asky uh code so yeah you need to keep in mind aski is rightly different from the alphabet or some stuff right so keep in mind so cni provides the cni config as a part of the cni specification so cni specification means the this contains the uh how cni prog should Works including the config file format and also the other result other format such as the uh result object format so the cni community upgrade this specification periodically so from Z One 02 02 0 03 01 and 0 40 and then now the 1.0 is the lest this is released at the uh 2021 likly three years old so this is pretty almost same syntax but a little bit different among the versions so maybe the if you uh have a trouble you need to keep in mind sometimes the your version does not uh fit the configuration style but also that this presentation focusing the 1.0.0 as the latest you know latest is good so that is the simple configuration uh cnna configuration example so this you know the Json allows us to the a higher CR structure nested object so here is the the parent uh structure has the cni version name pluging and the inside the plugins we have the another object so the this stuff is the U showing the cni configuration has the higher structure so this configuration is mainly uh for three types of the configuration one is this config for cni runtime which is the Li cni and then next is the config for contenter R runtime for example the cryo Contin and then next is the config for CN plugins based on the this configur uh this sample configuration the first three line is mainly for the config for CN run time cni version specifying which cni spec version is used for this cni config and then the name is the uh config name config identifier for Li cni and the plugins contains the each plugin specific configurations so plugins means of course the pl also this may have a two or more PL configurations I explain about it later so let's let's go inside the prog field we have the type master ipon so type is uh generic uh configuration term in the cni config which specify which cni pling is used so this field value I in this case IP is literally should matched in cni plugin directory binary name so this means the IP B type ipb means IP B progs used so that is the uh common configuration value uh types and value and then Master is the uh I PB specific configuration parameter so if you're using the other uh CN pring at that time the different parameter needs to be configured so so the please take a look into that for each cni plin the document the ipam section is the slightly different I mean that the ipam is also have about nested and that the ipam contains the different cni pring configurations so in this case host local cni is used to ipam IP the ipam is the IP address management so so the IP B plug in creating uh IP address and the interface only without IP address and then host local assigned uh IP address so this is a delegation of the cni so when Li cni is executing the cni pling at that time of course the program pring in this case consuming the input and then do the output so this picture is showing that input and output so when the IP or some other CN executed at that time two types of the input is consumed one is the environment variables which is the of course uni uh systems the enironment varia standard uh stdm which contains the uh several informations one most important things is a command cni and command which contains the what the prog should do so if the cni command indicates the ad so this is the um uh IP buan plin should create the IP the interface and if IP buan is uh C with cni command Dell then IP bu pling be removing the interface and then another cni container ID CNM CN AR so that this stuff is the uh powerport based parameters is provided at the environment variables and then next is a standard input standard contains the real your cni config with if you uh provides the H cni net the 0000 huber.com then the this file contents is goes to the a standard input and then IP will do a job for you and then after that IP pling output there are three types of the information one is how it codes exit code exit code Z means success and then if something is failed then this CA the non zero uh value is added and also the to for a further troubleshooting plugin could add the several element message at the STD error this STD error is uh captured by the cubet and the content time I mean that upper component and then you can see uh this uh error method through joural C or some stuff and if uh this PL succeeded at that time that this result is in the standard alute so this is the uh contains the interface name IP address and the MAC address and than other informations so here is a sample of the stuff so that this output calls the cni result object so this is same as the config and the Json format and then you need to uh keep in mind that the cni version is provided as I told before so this cni version specifying the out output uh types so the different C verion may contain the different uh name and value for inter face so by the way the uh this result object contains the uh interfaces and IP so the so cni spec allows to return the multiple interface in the IP field the interface zero means the uh with this IP address assigned to index zero of the interface so in this case the eso Z have the 10.1.1 three so that's the uh result object so this result object is uh retrieved by the continer onetime side and then at the result cuet captured these information through the CR the port SBO status messages so that they are simple case and then next is a little bit complicated case the plugin chain as I talked before plugin means not the pluging multiple pluging can be added in this field so in this case I we have the two J CN configurations one is ipb and then next is the tuning so so the U the first things is what does it mean so let me explain that so so the LI cni execute a first cni like the as the IP B is executed first IP bam uh plugin will create in the interface and IP address is assigned and after that second cni is executed with these information previous previously created interface in this case the iszero then so in this case the first IP host is create execute and then create the is z in the partt then after the tuning do the some additional configuration to the ISA zero in this configurations the CIS cut is uh executed for the this ethod interface to changing the several attributes of the uh interface such as the uh socket option maxcon PR or filter so how these exe how these progins are executed so this picture is explained about how to do that so of course as I thought plin chains is the first cni is executed and then next second and cni is executed this is not a parallel so first cni C is the pretty same as I explained before but the second CN code is slightly different this means the in the standard input cni config is uh dynamically Changed by the lip cni injecting previous uh output which means the ipb output is goes to the pr result field of this cni config and then tuning cni consuming the uh H cni net config plus PR result then identify which interface is the target to configure for example this the left side is the cni config in disk and the right side of is the adjusted dynamically captured uh about the uh uh cni con consum by the tuning side as I mentioned the right side the red uh rectangle PR result is dynamically added and then this contains the uh result object which is generated by the IP plan in this case interface is is found so the attuning plugin can consuming these parameters and then he know and they it knows which interface should be Target for this comp configuration and then that's the uh the cni config menu but uh we I need to also explain about the capability and the runtime config this is also the feature of the cni config and then if you may see previous the your experiment you can see the uh several notations in the port site like the kuet iio SL Ingress bandb as well as the ESS bandb so of course this annotation is specifying the uh band whe of the Ingress and ESS so this means the this sh touching with interface right so how to do that this is of course done by the cni how to do that is the what the capability and the runtime config is required this runtime config capability field uh feature is uh used to uh cont on time also the upper aay component I mean that in this case the cubet to inject the additional purport specific uh parameters to cni plins so in carore Fr or several implementations uses the feature for example the band base or Port m pluging is used for that so let's see the one example this is the caral deployment the cni config at that time the as I thought pling have three or not two three cni is executed so first is a cal and then carore will create an interface and also the assigned IP address and then next is a port map and then band base for each uh Port map and the band base pring is doing for each jobs and then at the time there are you can see the capability Port mapping through in the port map side and the type the capability band WS through is is in the abound site so that is the also the uh CN features so so if kuet if you creating the port with several bands and notations cuet inject this parameter is the runtime config and then Li cni is invoked Li cni identify the these runtime config parameters and then also the config file side capabilities then inject the appropriate runtime config in based on their capabilities so for example band base capability case right side of the configuration is the adjusted capture the CN config just before band V is executed at that time we can see first ple result contains the generated interface informations then after that runtime config band is Engish rate Engish First Rate first so these additional parameters are dynamically injected based on their part uh annotations so this capability runtime config feature is uh yeah unfortunately this is not in the uh cni specification but the are kind of the optional document conventions.

MD contains the these stuff so that that's the configuration I I've covered the almost cni config so the from the C cni PR perspective cni pling get the standard environment and the cni config then creating the interface or some cni chaining some prog May changing the some attribute or some stuff and then C prog will output as the Json object as the C result and if failed error code is happen as well as error message so that's the configuration and then let's go to the develop CN PL side I mean the so previous talks is about how to use and then here is the how to create the cni plugins as I talked before CN pling input the standard environment cni conf this is pretty same as the last slide right so to creating cni you're just satisfying the this requirement which means your cni pling first P standard environment values and the CN config then these inform these parameters satisfy the enough to create the ERS uh interface to the P so this your prog creating the prog interface by Linux uh netlink API and so on then after that you just the interface informations then just output the cni result as the Json so this is yeah it yeah it's simple to say but of course it's hard you know but so that simply to say you just do this stuff that's that's the whole but so the are in additions you need to care about several stuff so the first is the how to integrate the kubernetes I mean that if CN your cni pluging wants to get the aod object at that time as I told before uh cni is not integrated with the kubernetes so cni pluging does not have a way to access uh kubernetes object so for each C pling you need to get the create a service account or other way to create the some API account to get touch the KU this AP object and also the you need to keeping update the the service account for uh certifications uh expires and then next is the uh uh CN have a little bit unique uh Behavior have the uh command there I mean the when the port is deleted so in the CN specification mentions that command should not return on error even though pling have the some error so so this means the um the first the if the D is failed for some uh dead Ro or some certain stuff but this in this case the cni plug inside they it should not return any error messages and then also the error code otherwise currently the uh container run time is designed that the D command should not so that this means the are unacceptable uh it's happen container run time is getting the m and also the in based of this stuff sometimes CN from the up upper side contenter R time Li cni May invoke uh multiple command to one port object okay let's imagine the from the uh cni PL inside Li cni invoked the uh Commando multiple time to the same object so first Commander will remove the interface successfully and then just the return succeeded but the next invocation side sometimes from the generic programming point of view this may return the error hey the part is no longer exist but this should not happen because as I told before the commander does not return an error so that's the uh you need to keep in mind and then the another things is about the cni version as I told before cni version uh is um in cni config as well as the cni result and then as I thought the for each version cni result is the right we change it so here is the three cni result object based on the cni version 0 40 1 0 and and then interesting things is is the so these information mentions the same IP address and interface informations so this means so 020 has the ip4 and ip6 field is required but on the other side the 04 Z is IPS IPS field is in and then next is the version is removed so for each versions out expected output is slly different so the from if you writing down this cni progam you need to care about these version differences so so this is that seems to be slightly complicated but that is required cni plugin should passing the standard environment config then your you creating the interface and the output you should output the cni result based on the cni version so okay let's go to the hands on from D go ahead all right Tomo thank you very much um so at this particular link there is everything that I'm going to do in this tutorial and here's the thing there might be a lot of detail in what tomo just covered which is extremely comprehensive but in order to go and do what we're going to do at the command line there's really only two things that you need which is a kind cluster so that's kubernetes and Docker um which is a real easy way to try out kubernetes to have a development workflow Etc and how to use the cube cuddle command there's no special magic rocket science or anything else so what we're going to do in this particular tutorial which is kind of um rapid Pace um to uh to keep it rolling but what we're going to wind up doing is two things primarily we're going to install a cni plugin into a cluster I'm going to use flannel which is a way that provides podt pod connectivity so you know Tomo explained what does a cni plugin do well it creates interfaces and gives you a way to get connectivity to a network so we're going to do that and it was also kind of convenient that in this particular scenario Flame didn't install perfectly cleanly so we get an opportunity to see how to debug that um so that's the first part of it and then the second part of it is we're going to create a custom cni plug-in that's created with bash it's you don't need really any special programming language knowledge for it it just uses some basic Linux Prim Primitives as Tomo mentioned like it has to read environment variables and it has to read some data from standard in um so it's just you know a couple dozen lines at Max and it's just one that's uh what we call the dummy plugin um that logs some data so that you can kind of see hey what variables and values came into this cni plug in write it to disk and then we output some uh kind of like fake information about it so that being said I'll move on to the demo yeah thank you Tomo all right cool so the gist here is we've got these three panes on the left hand side what you're looking at is just some like General commands where I'm going to use like a cube cuddle create and stuff like that and then on the right is as if you were debugging a host so it's kubernetes and Docker so like debugging the host is really doing like a Docker exact so that's where I'm going to put those commands and then on the very bottom is really just a like Cube cuddle watch um to check out what pods are there uh all of these files and everything that's created are part of the GitHub repo those in the link and uh I as a first step create uh the kind cluster um in this case it's already created um so that we don't have to wait for it and the main thing to really pay attention to in this config is that we disabled the default cni so typically on a kind cluster this would already be bootstrapped for you but to kind of emulate what would happen on a like vanilla cluster um so when the cluster comes up we go and look at the pods that are there and we see that there's a couple pods that are pending and it's because the nodes are not ready so when I do Cube cuddle um get nodes those are not ready so that is based on cni uh so what I'm going to do to see why that is the case is to exec um into this Docker container like we're on the host and I'm going to look in the Etsy cni net D which is the cni configuration folder there's nothing there and having the presence of a cni configuration in that folder acts as a semaphor to mark that the nodes are ready so the cubet knows oh hey your Network's ready if there's a cni configure there so I'm going to install flannel which I did on the left it's with the cube cuddle create and we can see the flanel pods come up at the bottom um but doesn't actually fix everything we see the con we see the cni config on the right there it is but those CNS pods are still in a uh container creating state so even though it says Cube cutle Get node says those are ready um it might not actually be ready so we got to figure out what it is so it's in this case you can do a uh um Cube cuddle describe pod and we're going to get kubernetes events those were propagated from the cuet to the kubernetes API to your Cube cuddle command so that we can see what's going on so I take this and I go to describe the Pod tell me you can fast forward once thanks yeah so we have here a a error which is failed to find the plug-in bridge in the path so that's what happened it got propagated back up to this these events in the Pod and we can fix this since we know where things are thanks to Tomo's directions about hey um you have a cni config and you have a directory and you have a cni binary directory so I go en list the um cni binaries and there's no Bridge plugin there so that explains why we get this complaint that was failed to find the bridge plugin so there's a number of reference cni plugins that are available from the cni community and flannel happens to use one of them which is the bridge plug-in not all cni plugins work this way flanel uses what we call a delegation pattern where it kind of delegates some work to the to the bridge plugin so what I will do here is go and install the um Bridge plugin so I've got a uh reference cni plugin yo uh Tomo you can fast forward once um we see those cni plugins Damon set comes up and then on the right here I list the plugins and now we've got a bunch of them including the bridge plugin so because of that now those cord DNS pods down at the bottom are running so we've fixed the issue that's there now that we've got a working cluster with a cni plugin that will let our pods come up let's go and customize it and what we're going to do to customize it is we are going to create a uh we're going to create our own cni plugin the first thing that I do here is I am going to write a quote unquote binary onto disk so it's called the cni Bender because usually you would have compiled applications here but in this case I am actually going to just make this bash script that you see you see here which really just does a couple things uh what it does is it has a logging function it logs en environment variables it reads standard in and then it outputs a phony response we just kind of make something up here and you'll be able to see the that kubernetes reacts to it um and yeah that's our kind of phony information that we give we just set a static IP and in order to configure this cni plugin to run we will have to make a configuration for it so what we will do here is we're going to in the cni configuration directory is tell we can do one fast forward here and one more oh thank you yep oh I have to uh make sure that bash script is executable or it won't get executed and we need that cni config so as Tomo mentioned in your cni config directory the um atically first config file is used so I can leave the flannel configuration there but I just add an additional one that's prepended so 00 asically comes before 10 we'll say we want it to type dummy because that's the name of the binary quote unquote on disk now that that's all set up I'm going to spin up a pod because that's when our cni plug-in is going to execute right so on pod creation on pod deletion cni is exercised so we'll create the create the Pod and it comes up down there on the bottom and since this uh um pod executes um on the host and it writes a file I can go ahead and just cat this log file which has the information here so you could take this from the demo execute it and you can see all of those environment variables so you could use that to build a richer plug in that actually did something something you're going to see here down on the bottom on the third row in the IP column is the dummy IP address that we output so we output some Json that said hey this resulted in an IP address of 1920.

two22 um but that's actually just a big fat lie that we told the cuet is just totally fake so what you're going to see if you were to actually execute into the Pod is uh the the reality of what happened so Cube API thinks that's the IP address but when we go and uh do uh a cube cuddle exac and issue the IPA command that would list our uh IP interfaces Etc we actually don't have another interface there we just have the default loop back that you get and no actual IP address um so so let's see how this looks differently if we use flanel instead right so flanel is going to actually create an eth zero and actually give you connectivity um so I go ahead and I delete this pod and once I delete that pod then I remove my dummy configuration so Tomy you can fast forward once yep thank you one more thanks all right so I remove that configuration then I'm going to spin up the Pod again and on the next execution flanel is going to be executed and now we can see down there at the bottom with a sample pod IP address 10 24415 which is actually true and we will check it out this time by executing into the Pod and saying IPA and we actually do have an e zero now and that IP address matches what's in the kubernetes API so that's kind of the magic quote unquote of how that actual IP address uh ends up there when you do uh Cube cuddle exact or get pod so um I highly encourage you to uh give it a shot on on your own and run through the steps and it's kind of a like template that you could use to uh like flesh out an actual um application Tomo thank you very much all right so a few kind of uh tips and tricks for debugging um cni plugin so if you have something going on in your cluster that is related to cni like I did during the tutorial when you do Cube cuddle um describe pod there's a high likelihood that you're going to get kubernetes events that came up so you can check out what's going on once you have some information there then you can kind of dig into where these cni assets actually l on your host which will be in your cni bender and your cni comforter so those that's where you're going to find all those goodies one thing that I have certainly seen happen uh before is that you make some change related to cni you install another cni plugin let's say it writes a configuration and that configuration does not actually wind up alphabetically first before your other configuration so you're like why isn't this operating the way that I thought so check that cni comforter that may be what's going on um definitely the first place to check out another thing to remember is that the type field is a required field in the cni configuration and it's not like some kind of magic that like uh is not representative of something it is the name of the binary in your Bender so if you don't have that exactly right and that binary doesn't exist it is going to fail so this is just an example of like fat fingering it or having a hardto read typo in there that's not exact so that's one of those things and one of the main things that happens all the time just like the demo is that you have some dependency that like wasn't apparent um in your configuration that the cni plugin asked for hey I wanted to use a delegation to this other plugin and it wasn't there so you actually had to install something else um so that's kind of the most the most common one um and last but not least it's not not always apparent that the Readiness state of your nodes is totally a cni thing so if you're you have nodes that are in a not ready State definitely check out what's going on in your um cni cter because that is just a marker and it's only so much information for the cluster like this is as Toma mentioned in one slide is cni kind of predates kubernetes cni was intended to work for a bunch of different container orchestration engines and we live in a world where kubernetes is really the king of container orchestration engines I had Junior guy on my team who I was saying I'm like hey well cni is container orchestration engine agnostic and he said what's a container orchestration engine and this is somebody who worked on kubernetes every day well I realized that his entire career was just working on kubernetes so um there's a little bit of a disconnect in uh some of these places and this was just one thing that was kind of Taken like Hey how do we figure out if the network is actually ready so the decision was made to use that config file just as a semaphor so if you're writing a cni plugin yourself you might want to have considerations about when you write that cni configuration onto your host to mark that that is ready so Tomo and I work on open shift and we in the open shift land we like to have a like really opinionated way about how your cluster is going to be life cycle managed so we have a really delicate dance about how and when the cni configuration gets run on disc so there's really a bunch of like checks in the background to be like oh hey is your actual Network really ready um before we write that on um that config so like what happened with flannel here um wouldn't happen in that situation because we're kind of checking for stuff like that before we Mark the nodes as ready um last but not least there is of course plenty of community surrounding CN and it's really some of the strengths of cni I think are the like community ecosystem behind it um first thing you should know where it is is the cni dodev site which is a kind of pretty print of all of the documentation for cni so it's just all rendered HTML that looks nice as a easy too place where you can find all of the parameters for all of these uh Community developed cni plugins and so in in Tomo's examples we had there's lots and lots of parameters there there's really only three or four of those parameters that are required for every plugin and then each plugin is going to have its own set of uh of parameters that you would use so you can check out cni dodev you can kind of um pick and choose like a fast food menu of the parameters that you like and mix those up and kind of make your own cni meal from them further from that the like that is generated from the documentation if you want to get into the guts of it you can go right into the um github.com container networking which is the cni community that's the the name space where everything happens and there's really two reposit ories to be aware of one of which is cni and cni includes a number of things it has libraries it has debug tools it has um probably even a couple um small plugins maybe like um there's actually a plugin called dummy that will just create a dummy interface stuff like that um but the most important thing I think that's there is these two markdown files called spec.

MD and conventions. MD when you bring up the spec the first time um it's sort of like drinking from a fire hose you're like wow there's a lot of stuff here but I'm hopeful that after coming to this particular talk you have enough of the jargon terminology to be able to go through it because I think once you understand that the basics are really basic like it's environment variables it's standard in you output Json you exit with an exit code it's you have a lot of kind of creativity between those two things and it really does seem like wow there's a lot of stuff here and it also can be since it has that history of um kind of predating kubernetes it's a different API and I think that sometimes people that are really accustomed to kubernetes take a look at it and they're like wow that is is more than I bargained for but it's not that hard um to get in there and once you start kind of picking it apart and making your own customizations um the specification itself um shouldn't be as hard to read and is a really good uh good reference um there's also the plugins themselves which is a bunch of community maintained plugins that do a bunch of uh kind of utility type of functions so say you want to connect to a bridge to have your networking through a bridge that's one way um Tomo use the ipv Lan which is a sort of like net Network virtualization Tomo's using that as an example very handy Mac VLAN similarly so check those out there's a few different ways to also do IP address management there too like DHCP or static IP addresses um and last lastly um there is also a cni community update and Tomo and Casey candrell will be presenting this tomorrow at 2 pm so um go check it out and you're going to see um some of the latest and greatest from the cni community and thank you a bunch I appreciate you taking the time um to check out this [Applause] tutorial any question is welcome you were in few places quite explicitly referring to Lino but I understand that the same applies to kubernetes on Windows the same cni is there so I'm basically looking for you know this type of uh uh tutorial but relating to Windows uh there are some con cases which are different like you know referring to Linux paths on Windows finding the places and that's you know kind of creating problems when you have to deal with it by yourself if there are any documentation on that that would be interesting um that's a that's a great question I believe all of the basics should still apply and um uh I so one I'm I'm not super familiar with um uh with running kubernetes on Windows however there is for example in the cni plugins the community maintained plugins there is a build script for those in Windows and there are windows maintainers that are definitely uh considering what's happening with CN I would guess it's probably similar where you're going to have to like um Tomo if you let's say you had a kuber netti system that you just inherited and you were trying to figure out where your Bender is and where your comforter is would you look at your cubet config to figure out where that is yeah I think so so the first time the difference of the um windows and the Linux is Network stock is the different so so probably uh CN reference uh directory I mean that the uh so cni github.com have the prag repository inside the prag we have the Mac as I told before macb IP this stuff is exist but this is so several stuff is of course built for Windows as well but some CNF programing is only for Linux as far as I remember the Mac V and IP V seems to be only for the Linux so the first things is the from the cni perspective supported pluging is a different Windows have the more limited and the relux seems to be the full features and then in additions the how invoking this stuff is slightly depends on the uh uh cubet side of the windows so so the uh as person know the the cuet car to ooking or actually I'm not sure that the uh grpc for Windows site but yeah the cuet sending grpc call and then the container run time of Windows container care about to invoke the cni right so maybe the uh of course the journal cut is not exist in the windows so maybe the U similar mechanisms is uh exist in the windows so you can capture this stuff by the windows manner one thing I would say to try is if if you're brave enough is to try to look at the contents of the tutorial and go try to find where your cni binary directory and where your cni configuration directory is in your um lab Windows cluster and try running through the same steps because I kind of think that if you know where those two directories are it should mostly apply um you might have to convert the script into a Powershell or something like that um but check your cubet configuration on your windows cluster to find those paths and I think that it should be pretty similar thanks for asking yes please um just a quick question you mentioned cni runs only on creation and deletion I see you have a plug-in for DHCP or do you handle stuff like uh lee renewal and stuff that happen during the lifetime of the container okay than so thank you for the questions so the uh so the yeah as you mentions the DHCP have a different protocol of the cni so the yeah as you mentions the DHCP is the periodically relas and the request is happen and then kind of the heart bsh messages so the so in DHCP case this is only for the dhp D if you running uh if you're setting about DHCP uh cni adop them for each worker node round in the DHCP cni server which care about the cni protocol and the cni DHCP cni pling is interworking with these demon set to sending to request for some certain part or release and then this stuff so this means therefore each uh relas or this command of the DHCP side is care about the uh dhp cni pluging server so that's why the uh dhps worked very well uh is that answer your question yeah perfectly thank you okay cool so question is welcome about how to create or how about writing down the cni in the different languages other than the go for everything is welcome call once then call twice okay let's close this talk thank you for your time appreciate it