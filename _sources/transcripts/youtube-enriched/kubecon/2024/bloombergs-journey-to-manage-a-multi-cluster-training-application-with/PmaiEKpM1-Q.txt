hello everyone um welcome to today's talk so um today we'll be talking about Bloomberg's journey to manage a multicluster training application with kada uh my name is Ean I am a software engineer on the data science platform team at Bloomberg my name is way Li I'm in the same team with Ean also in the data science platform engineering at Bloomberg so here is the agenda for today so we'll be talking about what is data science platform in short DSP at Bloomberg and we'll be talking about what is kada and how we use carada in Bloomberg and finally we'll be closing this talk with a future road map of carada and also a Q&A session so uh let's begin with a brief introduction to the data science platform so DSP at at Bloomberg provides an on Prem bare metal-based kubernetes infrastructure for the entire machine learning life cycle from data experimenting based on jbit Notebook to model training which is built upon the C flow project uh especially training operator and also to uh model inferencing using kerve so today we'll be mainly focusing on the model training part so uh here is a very high level simplified architecture of DSP so DSP is divided into multiple tiers uh according to the network Z Network Zone where the cluster lives so for example we have Dev tier and PR tier so each tier has multiple clusters in it uh the the different clusters are located in different data centers in different GE geographical places to ensure disaster recovery and DSP supports multi-tenancy for different AI teams at Bloomberg by using kubet native namespace and we also managed a quota that uh each name each team is allowed to use um using kubernetes uh resource quota so the quota is duplicated across different clusters within the same tier so that whenever one data center goes down users can submit their jobs to the other dat data center the other cluster uh without facing any quota related issue and finally configurations such as config maps and secrets are managed separately so basically we are allowing users to have different configurations to run their model job training jobs on different clusters and also using different credentials to connect to external services so the uh the uh setup we just talked about has actually brought us a few challenges so the first problem is uh the separate configuration and credential management so because uh we're allowing users to have different credentials configurations on different clusters it is very likely that that uh a certain config or secret exists on one cluster but not on other clusters in the same tier so when user submits a model training job to our DSP uh model training clusters they have to make sure that the specified configuration or credential exists on the target cluster so if it does not exist the job will simply fail to start and solution to that is relatively simple uh we just need to ask users to copy their credentials and configurations uh and submit them create them on all of our clusters uh it is simple but relatively manual and um you can see it's not ideal right and the Second Challenge where are facing is actually due to the nature of how kubernetes resource quota works so every team must based uh must must budget based on their potential maximum resource requirement so that is for example an lln team wants to train large language model on our cluster uh that require like say 40 gpus at the same time so they have to budget enough quota that's equivalent to 40 gpus on our internal budgeting platform so that when such a large job is submitted into our cluster the job the job can get started successfully so this is the nature of batch workload versus long running Services as a shown on the chart on the left hand side so for long running Services uh the resource usage is relatively um stable with smaller fluctuations um so kubernetes resource quota fits well in this use case however for batch workloads even though the resource utilization May remain low for most of the time when a large job comes in that requires a lot of Hardware resources the budget unit the budget quota has to be set correctly according to the maximum maximum resource usage by that job so that the job can get started successfully so as a result over budgeting is inevitable in this setup uh which leads to a waste of resources so this is probably not a big issue for workloads that runs on CPU and memory but gpus are more expensive and uh gpus run into shortage more frequently so um and at this point we're not having a good solution to this issue and finally the current setup also causes unbalanced utilization so this is because people usually tend to use the same cluster to run the job over and over again maybe they're just uh most familiar with that cluster so it's quite likely that one cluster is under heavy workload but the other cluster is relatively idle so as is shown on the diagram there uh you can see that people who submit job to the cluster on the left the dev dc1 cluster might have to wait for a long time before their job gets scheduled while the other cluster the dev dco2 cluster remained idle and is simply wasting money so the solution is also probably simple we can just ask users to evenly submit their jobs to different clusters but you can see that this is uh actually hard to realize so all these challenges makes us think about um is there anything we can do to help us better automate all those stuff to reduce the pain on our users and also reduce the pain on our un call so with that let me hand it over to way to talk about more about what is kada so uh what is kada kada is a m cluster and M Cloud kubernetes manage system which is designed to provide scalable container resource pools enabling developers to use multile clouds and mple clusters as similarly as using a single kubernetes cluster and this platform enables the deployment of workloads from single cluster to M clusters without requiring any Cod refractor it also integrates the kubernetes tool chain without any loss of functionality it also offers out of the box capabilities with building policy sets for various multicluster scenarios such as cross region active active and remote Disaster Recovery kada also supports seamless migration between multiple Cloud vendors avoiding vendor lock in and enabling centralized management through this native kues API orientation it also provides various M cluster scheduling policies which are tailored to its industry scenarios such as M region M cluster groups ensuring optimal resource utilization and workload distribution across clusters this is carada's architecture it includes a control plane deployed on kubernetes comprising the cada API server scheduler controller manager mirroring the kubernetes control plane this setup allows users to create resources and arrate M cluster applications through the native kubernetes API kada supports both push and pool modes to access the member clusters in the push mode kada will directly access member clusters to VPI server to get cluster status and deploy manifests for cluster with limited network access kada supports a pool mode by delegating responsibilities to a cada agent within the corresponding cluster this arcure supports features such as M cluster management cross-cluster application failover Global resource View and M cluster service Discovery and more to better understand upcoming feature sections as SC The Core Concepts of of kada these are the primary resources essential for its C functionality there are three user facing apis first the resource template it is on modified kubernetes resource that users will interact with directly such as pod and deployment next for every resource template it will be bound to a propagation policy by a resource selector this policy defines how M cluster applications are scheduled and specifies which cluster the application is distributed to there are many supported scheduling policies such as cluster Affinity M cluster splitting and M cluster rebalancing finally override policy this policy allows for differentiated configurations across clusters such as using different image URLs or repositories for different clusters and there are two internal API the first one is resource binding it drives the scheduling process within kada the second one is work object which represents the resource in the member cluster at carmal layer ensuring proper resource management and synchronization and take the deployment is example for the high level scheduling process after users create a deployment the corresponding resource bindings will be created with the propagation policy match with the resource based on the resource selector and the scheduler will use the information in the resource findinding to determine the Clusters that the deployment should be distributed to and the kada will create specific work objects based on the resource binding with the override policy match with the binding which represents the actual resources that are created in the member cluster finally the mod that will create the resource based on the work objects in the member clusters understanding this Core Concepts we can effectively utilize car's capabilities to manage and O trate M cluster environments seamlessly at this point I will hand over to Eun to talk about how we use carada and what additional works we have to do to integrate with the Bloomer system um so now that we have known what kada is and uh what kind of problem it can solve uh so let's take a look at how we set up carada in Bloomberg and what actual benefits carada has brought to us so in this chart you can see a highlevel control playing setup of our kada instances so uh we deploy carada instances in two different data centers for high availability and in each data center all Cara components are deployed for example the uh API server uh the uh kada scheduler and carada controller manager so we also set up leader election to make sure that at a point of time there's only one uh component that is actively running so on the top you can see that uh we connect the two kada API servers to a single console based DNS uh service Discovery so that the two kada API servers are essentially agnostic to the carada clients and uh in the middle you can see that we deploying a kind instance in every data center uh which provides an SD like interface for the API server of the carada to store data so uh the two kind instances are connected to a single postgress database so that uh when one side goes down the other side of the carada instance can immediately pick the word work up and uh the high availability of the uh post grass instances are are guaranteed by our Bloomberg database team and at the bottom you can see that each carada instance is connected to all clusters in the dev ti so that even though one side is completely uh unreachable user can still access all clusters um uh through the uh the other uh available kada instance and uh this setup has brought us a few very various benefits so the first one is integrating kada uh makes us uh uh allows us to do automatic fill over so with kada users no longer have to worry about uh selecting a cluster that is properly running so carada knows which cluster is up and running and which is unreachable so when a new job is submitted into kada kada is smart enough to pick a cluster that is available to run that job and uh what's even better about kada is that it is able to schedule uh running jobs from filed clusters to the uh clusters that are still available although this feature M probably makes more sense for long running services and the other benefit that kada brings to us is uh automatic propagation of configurations and credentials so what it means is that uh with the proper setup of a propagation policy user only has to create the the secret or config map in the kada without having to access those member clusters and kada is able to propagate the secret and the configuration with the exact same content into all those clusters that it is connected to and what's even cool is if a new new cluster is added to an existing tier and is on boarding to kada carada is also able to pick it up and propagate the existing secrets and conflict maps to this newly added cluster so that users can use the new cluster out of the box uh kada also gives us a more balanced scheduling mechanism so when a job is submitted into kada kada automatically retrieves the uh available resources of all clusters and select the cluster that has the most resource to run that job so oh you can see that it naturally balances workflows across different clusters and uh there's no manual interference in this process so users no longer have to worry about which cluster have more resource to run my job and their job will not stuck in pending State unless there is really no resource on any of our clusters uh this has greatly improved the uh Hardware utilization especially GP utilization of our clusters and the last benefit is that uh kada provides a global uniform resource view it means that admins and users can now perform uh common operations uh on the uh resources in member clusters without really having to access those member clusters um it also supports common debugging operations such as log and exec so uh this has brought has been very convenient for both users and us as admin uh platform admins and of course carada does not work out of the box in the Bloomberg environment so next I will give it back to which to talk about Bloomberg's efforts to make carada fits into our environment yes I'm going to talk about the resource interpreter the schedule estimator and the future Ro map of our platforms and just from what Ean said there's some customization that we have to do and also some contribution they have to do uh for integrating with our systems so for the custom resource inator there's in the progress of propagating a resource from kada to member clusters kada doesn't need to know uh sorry car needs to know the resource definition and it internally Ed the replica requirements and replica be to do casually for kubernetes Native resources kada knows how to parse them the translations for Native resource are maintained by kada but for custom resources defined by crd as Lake of the knowledge of the resource structure they can only be treated as normal resources therefore the advanced scheduling algorithms such as clust affinities and M cluster splitting cannot be used for them then how to let the camarada understand your custom resource it provides a framework called resource interpreter framework to help achieve the translation it is designed for interpreting resource structure of your crd for example suppose we have a crd called Chrome job with the API version through bar.

iv1 and we want to Leverage The Advan scheduling policy we can define a chrome job interpreter web hook to translate the Chrome job resource which Kamala doesn't need to be understand into a desired replica requirements and replicas this allows kada to apply its Advance scheduling policies effectively in our case we rot a worklow interpreter to help translate our in-house training worklow it converts the spec of our customer inhouse resource including custom resource information such as GPU small to the replica requirements and replica that kada understand so kada is able to apply its Advanced scheduling policies to our workload next is the schedule estimator the Comm scheduler by default calculates free resources based on the resource summary of each cluster enabling rapid scheduling across hundreds of clusters however this approach can Le Precision for instance as shown in the slide suppose we would like to create one replica with 30 CPU cores and 30 GI memory while the total cluster resources might appear sufficient for a replica the actual available resources might not be enough and if we schedule a replica to this death dc01 cluster there's actually no node can run the replica to address this issue caral offers an option to enable scheduling based on the schedule estimator each cluster has a corresponding estimator that uses informers to sync accurate resource information from the cluster during the scheduling process the scheduler uses a GPC client to communicate with this estimators instead of relying on the resource summary the process involves two steps first it will calculate the number of repaso note can accommodate in terms of Hardware resources next it will determine the maximum number of the remaining parts the node can allow and finally calculate the number of replicas that can be assigned by summing the minimum of these values for each node by using estimators the scheduler can have more information to determine the appropriate cluster for the workload and therefore reduce the possibility of a pending replica due to insufficient resources Al this introduced more Network latency and scheduling overhead it is suitable for our use case which involves managing tens of clusters and requires efficient resource utilization however there was a limitation with the estimator as well it didn't consider resource codas during the scheduling process for example if we want to schedule four replicas it's requiring one GPU in the L main space the replica might be scheduled to the def dc01 cluster because it has more overall resources than these def dc02 cluster however the LM Nim space in the def dc01 cluster may not have the capacity to support this replicas causing the job to remain a pending State on member cluster oh sorry until previous job finished and released the gpus in order to make Kata scheduler aware of the resource quota of each name space and also take resource quota into consideration when making scheduling decisions we contributed a resource Cod made plugin to carada inside the plugin there is an resource Coda Informer which listens to Resource Coda events in the cluster and filters out res clusters that do not meet the specific workload requirements by enabling the resource Coda pling we can further reduce the possibility of a pending worklow in the member clusters feel free to check the pr if you're interested finally in our future role of the platform in Bloomberg we want to contribute continue collaborating with the community to provide more functionalities into our Platform One exciting feature in carala we are working on is the priority and preemption this feature ensures that critical workloads have preferential access to resources optimiz resource utilization and availability for exential tasks we are actively collaborating with the community on this feature and hope to deploy in the coming months feel free to check out the proposal thank you um any questions thank you for the talk um one of the one of the reasons that Cube Fed was not very successful um was the fact that we use the same um the same resources that we normally use in a single cluster in a multicluster environment in in CU fed and I think kada was built on top of Cu fed V2 I I believe and it follows the same model so the issue is that you create a deployment the deployment has a label there is this propagation policy that chooses the chooses the deployment so in a way in your deployment you don't specify how you want this deployment to be deployed in in multiple clusters um it's like kind of the opposite way sort of like the propagation policy determines how your deployment will be deployed so basically when the user creates that deployment they need to be aware of maybe existing other propagation policies that could potentially choose their deployment and how it's going to get propagated so imag imag that in a name space if I have like multiple uh propagation policies and they have multiple label selectors there is a possibility that you know I create a deployment and my deployment goes to different clusters because there was some propagation policy in the system and it it choses CH it chose my um my deployment you you see how how things are kind of like backwards in a way so instead of the deployment specifying how how it should be deployed it's like the opposite way you know the deployment is there another object determines how it should be propagated so it this was a major friction um in Cube fed V2 so that that kind of made this whole project kind of fail in a way you know it's now archived and I don't know if you face the same problem at all in with your users who are using carada in actual prod environments or not you can uh uh yeah I can I can probably take this question so I think in our use case so first um we're no our clusters are not like uh for long running services for like deployments we're mainly for batw clothes like model training jobs so um and also we're try uh we tried to make different clusters in our uh in a single tier consistent so they have exactly the same Hardwares and like any other specs so that uh the differences should be ideally um agnostic to users and in our use case we also built a an API on top of carada that allows users to override to specify which cluster they want to submit the job to if they want to really override that but um we haven't seen U um frequent like requirements for that yeah just uh want to add one point and also our propagation we we have predefined propagation policies in our clusters so user just choose like which kind of which tier which cluster they want to deploy to with our predefined propagation policies I guess your is a little B more specific to your Environ yeah yeah uh hi thanks a great talk I have one question how do you compare the project commod and the Q yeah I think this is interest can take it no you no you can take it this is a very interesting point cuz uh we are also looking into the Q but currently for Q mainly for like bad drob workloads and for and it doesn't provide like uh a multic cluster Resource Management kind of features like uh most of the user use PE will mainly for like Distributing training job or inferencing job but for kada you can use it for manage like Secrets config maps and also loan running services like deployments and I think this is the current key Point cool thanks hi um I have um use case where I have like a m closer environment with uh long running services and I use like AR CD to kind of deploy to multiple closers today the problem I have is um if a a team for service a def findes a HPA with replicas 10 for example minimum repors right it gets uh uh if I have three clusters 30 pots because you know like Argos it didn't have like that um capability of like Distributing like kind of Federated HPA something like that uh does kada have something any feature like that they can can I know how many clusters I have and based on what was was defined I can distribute the load because I saw like you mention in in the slides right some about that yeah there's building policies in the propagation policies that you can Define like uh spread constraints by different regions or uh by different uh zones so it does have those capabilities in the propagation policy oh perfect thank you and it also have uh Federated HPA and chrome drop HPA CH HPA okay I take a look that thank you right yeah thanks again for attending this talk