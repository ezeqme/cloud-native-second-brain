but today we want to talk about validation so validation is something I think everybody used in the last 10 years especially when you build things on kubernetes and using cads and this is basically the topic but not only cads but also native types at the end validation so shift left what is shift left cryptic term we will see it in a second and we we will talk about the past so we will have basically a little history session what we added over the 10 years what we're doing at the moment and where we want to go to and why we do all of that like validation has been in kuber since day one basically but we are not finished and we will see why and what the next big topics are so let's start in 2015 so this is like yeah very much in the beginning of kubernetes and shortly later CS came up but it was not there in so everybody knows that when KU was built especially the cube cutle tool Cube cutle create everybody knows that when this was built this was a model like you have a manifest file some deployment jaml and you say Cube CLE create and there was a mistake right you saw you see now I type secret name secret names cannot be right and the server basically not noticed that and tells you okay there's a mistake fix it super fast super nice and kind of a new user experience which was not so common at the time but this is 2015 and 2024 looks very very different so the reality is this probably most people um have the ICD in place so we have this long pipeline you have your developer notebook you type you white gaml manifests and you commit to get to some Branch you proof it pull request and everything your GitHub action start running and they check some something hopefully they they find this mistake but if they don't find it cosplan or Aro CD or some other gups tool will deploy your code into production and the CU will notice maybe will not notice if you mistype the field because it's a typo right a typo is for for the API server it's not a mistake right it can be just U that you have a new version of the Manifest and the server is old so intentionally it it might accept it even even worse so you change has no effect but basically we have no Cube cutle more anymore as a primary tool to apply manifests so shifting left is basically shifting this lightning there at the top that's where you notice the mistake shifted left very visually shifted left feedback after 10 minutes plus is not acceptable it's not an inner loop that's like take two coffees and then you can see the error it's not what you want so we can build it into GitHub actions or any any kind of CI tool like in the steps which are running in CI and find it there maybe you get down to a minute if you're good but not 10 seconds right impossible so this is what we want we want instant feedback again so seconds and that's the um yeah the title of the talk or the reason why we have this talk and why we do all the work even 10 years later on kubernetes to come back to that experience so uh client side validation has been a very long journey in kubernetes we first started looking at this in around 1 1.8 when we added dry run client uh to CP cuddle to try to restore that really tight feedback loop that we had with this with the server unfortunately this has a really limited validation uh support you can only really see that your yaml parses that your Fields types are the correct like a string is still a string an in should be an INT whether you misspelled a field or you left out ANC conditionally required field this also gives different error messages from the server and it's not really it's pretty much a non-starter to use for offline validation in CI because it needs to be connected to a running cluster or other open API endpoint so the other alternative that we've seen people turn to is to dry run server against the staging cluster this has the benefit of having much better error quality than the cube cuddle client side validation but there are a few drawbacks that make this also impractical iCal to use in the CI system uh the first could be that or policy or other things might prevent you from connecting your CI system to your staging cluster or it might be offline another is that this might require you to punch a security hole into your staging environment because you need to elevate your CI to have the same permissions as your CD system you can't dry run the create or an update of a resource unless you have the permissions to do that uh actually and and lastly there's a functionality Gap in that it doesn't test against your current git repository state if you've updated your crd in your git repository next time your CI runs it's not going to validate against that schema or if you've added new name spaces or new crds your CI is going to trip once you tries to go and validate those um and I'd be remiss if I didn't mention that you shouldn't hook your CI up to prod depending on how heavily your API server is getting hit this will limit the availability of other critical workloads and like the security implications for the staging environment there's also uh you might also have to give right access to your prod cluster um to allow CI to to dry run it and if that wasn't enough for you um most of the audit uh logging in kubernetes does not have the context to know if we're being dry run or it doesn't check so your audit logs are going to be filled with um all the requests being made from dry running and if you're ingesting those logs that's going to that cost you extra so since those two options don't work we've seen most people fall back on a temporary test cluster this is the idea that you would spin up a local API server using something like kind or mini Cube and just simply apply all your manifests and see what errors fall out unfortunately this is a little heavy-handed to use just for validation it needs a lot of compute power and RAM resources especially if you have a lot of crds which seeing more with dependencies like crossplane and it's hard to get it right um the you might see errors in your test environment that you don't see in your cluster and vice versa for instance on the cluster updates give different errors than creates so you'd have to make sure that you're applying things in the same order as they would be on the staging cluster and you'd also have to uh make sure that you configure your name spaces and your crds correctly before you try to apply them and eventually this just gets really unwieldy and not worth the effort and last reason why we don't think this is a complete solution for client side validation is it doesn't scale to IDE and it's not portable you can't use it in your controllers and it's takes minutes rather than seconds and a lot of compute power so we need something better yeah so a little history um what have we done in the last years in the last 10 years basically to make this better I mean everybody would think okay schemas open API just plays a role but it's not that simple and we see in a second so this is the first thing which many people might not know like verifying validating a pot spe pot deployment anything which is hardcoded in in goang in kubernetes and the source code it's go right it's nothing um a third party tool or your IDE can know about and um so but it's how Cube started and we will come back to that how to fix that but of course in the history we didn't te that like get open iation but um it's very weak like the the types or the the specs we we export for pots for example it's incomplete it doesn't say anything about which values can be in which field for example it doesn't say that but in custom resource definitions from the very beginning basically we added open API schemas so there's open API V3 and here's an example so it's a custom resource definition and you have to um you have to Define which felds exist what's a type and there are some some more things you can do like minimal values you can have regular Expressions these kind of things so for one field you can specify what you want and this was a start and you can verify a lot of things but of course there's much more than that so here's the timeline of the last 10 years and you see where CDs were introduced in the whole talk we don't talk about validation everybody when talking about more advanced validation you think about we right people misuse admission for that and we come back that so um but we don't talk about Mission here we want purely talk about validation so open API as I've shown course we2 and luckily meanwhile we have P3 so it's expressive as open API is but it's a s so you can actually ask API Ser just going to the to the SL open API end point and you can compete this is like a multi megab document I think you have everything all GRS so it's lots of data lots of Json pretty and it gives you what you just saw with um the fields type and Fields regular expressions and minimal values this exists and the Tooling in the the ecosystem uses that extensively in there was a big switch some of you might remember that in 117 118 somewhere once a time we made this mandatory like we have to write C this was one big step and they must be structural that's in detail I don't talk about that but basically we did that and we added something more and um we realize open AP is too weak we need more right there are things you cannot expect at least you cannot do it in in a short precise way this Ty is one of them I'm not sure who knows this not many that's the reason so we want to promote that you can specify and you you know the Comm types if you have a CO back and you have containers in there every container name so it's actually it's not a list in Array it's actually a map logically it's a map from name to specification and you can specify it for Cs and there is this ex extension so it's extension and you can Define that for every this there are maps there are STS there's something called Atomic if you don't really can I mean it's just one value it's a list but it's really considered bit measure anyway so you can find that you can find names and if you do that and you apply your duplicate name what you will get the ER you would expect like you cannot name containers with the same same name hot specs look a lot like an object right labels annotations deployment the pop defines labels so it's nearly an object is inside that to that excep so you can say the substructure in the CD is actually nearly object and object important and if you do that basically everything which looks like a metadata subru all the fields you know objects they will be validated according to metadata so automatically you don't to specify what the name the label what annotations are it's up all right and if you do that like um deeply embeded in your type say is this dollar dollar something it should be a label and of course it's not label labels have a certain format you get which is see on all right so we've talked a lot about some of the power of that we've been adding to the schemas using ex kubernetes extensions uh they do a lot of powerful things but it's really specific use cases so what about everything else for everything else either crd authors don't include the validation anywhere in the schema or they'll just put it inside of a a validating admission web hook uh this is a this is problematic uh not only because validating or not or yeah not only because admission web hooks cause problems in production but because we can't validate them client side there's no information in the schema or anywhere for us to validate this so this is not a long-term solution sorry uh in response we've added um cell or common expression language to kubernetes you can put this into your your crd schemas it's a portable type safe and interpretable language that has a familiar syntax that anybody who's ever used a programming language before it can express uh complicated conditional logic you can call functions um you can also even have different rules for creates and updates so let's take a look at uh how cell works in practice um so here we have an example crd schema with um a spec property which has three subproperties I replicas Min replicas and Max replicas they're all non zero integers and we can refer to all these fields within an ex kubernetes validation that is attached to the spec so when we add an ex kubernetes validation to spec we are able to refer to the spec as self and access any of its subproperties from the schema and relate them to each other so in this example we have replicas being enforced the constraint that it must be between min replicas and Max replicas we can also have a custom error message to give the user more context and specify a custom field path to attribute the error to if it makes more sense to give it to a subfield rather than the root spec object another powerful use case of cell is immutability so here we were specifying an update only cell validation called the transition rule you have access to old self using cell so you can simply specify that old self is equal to self and kubernetes knows that since you mentioned old self you it should only apply this rule on updates and it won't allow you to change uh the value of replicas so you see in this example on the right we are applying successfully creating this resource with a single replica and then immediately when we try to change replicas to two we get the error message that we had placed in our schema so cell is now available GA in kubernetes 129 new Rel release crds are actively making use of cell uh such as Gateway API and over time as kubernetes 129 leaves the version skew of other projects uh we anticipate that they will begin to also adopt cell and so we're excited about seeing a lot of the validation logic that's currently stored in web Hooks and unaccessible to our local validation systems and inci we're excited that're finally going to be available in the schemas uh so the cell standard library is pretty extensive and we're always we're constantly finding new uh things to do with it uh you have date and time manipulation you can create strings from formats from properties inside your object and then create construct a custom regular expression and parts that object that you created you can do list uh comprehensions and aggregations can par ulate URLs even interpret kubernetes quantities like you can add 10 megab to 2 kilobytes and check things about that and we've also last release uh added optionals to sell so that it's more expressive because we received some feedback that was a bit verbose and this list is always expanding um as we finding more and more validations um that are inside web hooks but not in schemas so the most recent feature that we added to retes to improve the power and expressibility of cards and make schemas more valuable is automated validation ratcheting so let's say you have a crd it has two two properties a replica field and an IP field replica is a number and IP is a string uh you didn't specify any validations on this field so that your users can go ahead and say they have two and a half replicas and specify an IP with five components kubernetes will happily accept this so the crd author sees the user are running into problems with invalid schemas and naturally they move to update their schema to have a stricter requirement now replicas must be an integer that's at least zero and IP must conform to an ipv4 format and as expected if you were to apply a new object onto your kubernetes cluster for the schema uh the cluster would respond with an error but what if I what if we had existing resources are created and updated already created and be updated to a new schema so to be frank you're going to have a bad time you can't edit this you can't edit this resource can't add annotations touch the labels neither can the kubernetes system can't remove finalizers and so your objects are your resources effectively immutable and undeletable until you manually go in and you fix this resource so crd authors who knew about this have avoided for a long time strengthening the validations of their schemas uh and those who didn't um maybe just broke their users workloads uh but now that we have this feature we can maybe start to move uh some of those validations out of web Hooks and into uh schemas so let's take a look at how this actually works in practice we look at each at validation individually and only enforce it if the value that it pertains to in your object had changed so in this example the value in the green box is only used if the green values had it changed the minimum validation on the replicas only cares about the replicas value so unless replicas changes you're not going to get an error you can modify annotations labels crd validation ratcheting is now in beta in the latest kubernetes release that's being cut right now it's enabled by default so we we excited to see that to that um we're hoping uh new CR or existing crds can make use of it very soon once it leaves their version skew and they don't need to hold back on on strengthening their validations in the future so what's what's next so we've already mentioned how schemas haven't been powerful enough to to express our native go validations for kubernetes types so on the screen I have an example of persistent volume specs access mode field the validations that kubernetes has handwritten in go and you can see there's two validations here one is more one is simple and one is more complicated the simple one we simply check to see that the uh access modes has at least one element and if not we'll throw a required error the second one more complicated we Loop through your list of access modes and we check to see if you use read write once pod and if you use it with any other access mode we'll throw a forbidden error so up until now this hasn't been possible to express in open API or declaratively in schemas but now with some of the new features we've been adding like cell we're able to start annotating the native kubernetes go types with cell to encode their validations into the schema so you can see on here we've now we can now we're now annotating required and Min items uh but also we have the cell rule either read write once pot is not in the list or it's the only item in your list and we can see that uses the ex act same error message and error type forbidden as kubernetes would have so after our build process will then ingest this uh go type definition and emit the an open API schema that will be published on the endpoint that stepan mentioned earlier that can be consumed by tools and we can see that it now includes um all the validations we saw under go type and can be consumed on the client side we have the EXC 's validations and mid items ready to go so declar of validation for Native types has been in the works behind the scenes to enable the possibility for a while we're hopeful that we can start to publish the annotations for a few native types starting in kubernetes 131 behind a feature flag but once that's once that's possible you won't really need to enable an app Feature gate to make use of these validations on client side and uh we also hope to uh eventually possibly remove some of the handwritten go validations in the future and use these also on the server side so we've talked how we're making schemas more useful for validation let's check out some tools early tools uh to solve this problem used standard open source open API of validation libraries so most popular example I'm aware of is Cube conform there's Al there's also cub valal and there's some other Json schema tools because these use standard open source open API libraries from the community they give different error messages than kubernetes does and they also don't support any of these new Fe these extended open API features that we've been adding to kubernetes and most importantly probably being cell as that's going to be used to uh to a great extent in the future these these tools also had the benefit of trying to supplement the schemas with handwritten um handwritten schemas for Native types but often times uh open API is simply just not expressive enough and they're either wrong or incomplete so response to this Gap in functionality of of cell and other thing the other things we mentioned last year we released the first Alpha of cube cuddle validate it's a first-party tool to validate um your kubernetes uh resources and um we released it uh last year it basically just uh takes the same code that in the API server and publishes it as an easy to use command line tool and a library so as a result it has all the same error messages all the same kubernetes extension support um it also embeds the native schemas from kubernetes with the caveat that they're still not good yet we're working on it and it can you can use the crd schemers directly from your repo exactly how kubernetes would process them we're still working on adding awareness to the old object so we can uh enforce your custom cell immutability rules or transition rules and also uh ratcheting so the tool doesn't throw errors where the cluster would not have and also helpful to add object reference resolution intelligence uh so this is an example GitHub configuration for cube cuddle validate it or for a GitHub action another CI system would probably use something very similar it's pretty simple just get your get your manifests install go get the tool validate a lot of times uh a lot of people also use templating to define their kubernetes resources in CI so um you probably would also have to maybe add another step in there to render your tool your manifest before sending them into the tool all right so lots of things have which have happened in the last 8 to 10 years schemas are obiously in the the center of everything and we are still not finished like we are we want to bring back the cube cattle create experience seconds until you see erors and improve velocity and make everything smoother developing manifests rting yaml one big motivation here also is to get rid of that books we edit that books years ago and everybody who runs it in production knows ch right the process goes down suddenly resources don't validate or pervert or anything like that so one reason is also using cell to get rid of make C cell more expressive to enable that similarly there is the validating emission policy so we didn't talk about emission but there is this policy thing another object and cl is going in there like CL is the basis of that and C is getting used everywhere when it is for the same reasons basically and Native types as described work in progress we will also get into the schemes and Cube cutle validate is under active development we're working towards full support for kubernetes schemas including cell including all the native type schemes that are being published and it will track uh kubernetes as it develops you can try it out with just a simple standard uh go install command and if you'd like to contribute or help out or learn more you can join us in the kuber slack we have a c validate Channel yeah and um just a thank you um of course if we say we it's not just the two of us it's whole sick G Machinery which works on that so many people um put this to everybody and yeah this is the motivation I mean we talked about that this is why we want to improve skas inside feedback getting back where we have been in 201 thank you