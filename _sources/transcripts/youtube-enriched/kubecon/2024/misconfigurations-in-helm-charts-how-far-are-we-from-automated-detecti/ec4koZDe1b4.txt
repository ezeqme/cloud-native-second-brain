good afternoon everyone welcome back from the lunch break and welcome to this uh talk okay welcome to this talk which is about misconfigurations in hand charts and how far are we from automated detection and mitigation and uh today with me there is a gut so hello everyone so my name is agat blaz I'm a research engineer working at Tes and I mostly work working on the virtualization of network system and the security of such systems and I'm Franchesco and I'm doing a PhD at the Free University of Amsterdam and very brief advertise advertisements before we dive into the technical content this work was actually part of a collaboration in a European project I sure most just finished and the new project SEC for I for sec I put the link there so we're always looking for a collabor operations especially with companies if you're interested in the what we're doing please check it out um so now diving into the content of this talk uh which is misconfigurations in uh the cloud why should we care in the first place well because from recent reports um they found that uh such misconfigurations can be the reason for several incidents security incidents and um because they uh they containers that actually run your applications they also usually run for a very limited time so the the runtime window for detection and the mitigation is short and so at the beginning we we started this work saying okay because we have this very limited time window at runtime let's see how much we can improve uh before uh reaching runtime so at static time uh and also the other reasons where uh because there are a lot of uh tools that can analyze configuration files hand charts as well uh however there are some inconsistencies between the output of such tools and also there is no indication about whether a configuration might break the functionality of your application or not um and because misconfigurations can uh cause bad things so to say uh there are some Frameworks available that uh give a list of best practices and security recommendations that you should follow with your configurations for example the CIS Benchmark or the NSA and cisa hardening guides and they for example suggest that you should not run your container uh as root minimize the capabilities the Linux capability you assigned and so on and uh looking at an example uh on on the left of the slide you can see a a snippet of a yaml file that can be used used to deploy a deployment resource uh on a cluster and as you can see there are uh some misconfigurations that tools are able to detect for example there are no resource constraints for this deployment uh which is also running as root is not using a rolif file system and it can allow uh to escalate it can gain more privileges uh than the ones it started with uh so using tools you can actually detect uh these issues and eventually fix them and some of them are quite easy to fix for example it's a matter of flipping from false to true or or the other way around other uh they require you to actually write a new piece of configuration for example they require you to add some limits and request uh for your memory and CPU uh of your application uh and so you can Define for example memory equal to 64 megabytes uh which seems reasonable however whenever you are asked to to add a new piece into the configuration you can also make a lot of what if questions for example what if we Define memory equal Ellis or or what if we Define memory equal zero will this this uh new configuration Bill still accept it by a tool uh or will still be able to uh deploy it on on the cluster so we were this was our starting point uh for for this uh work and then we selected uh some several uh open source tools uh available out there seven of them uh most of them are part of the CN CF and as you can see they all have a different number of uh policies available uh by default so the numbers are only uh default policies that can be applied on configuration files or uh Helm charts and to evaluate uh these seven tools uh on Helm charts in this case we built an automated pipeline it's a pipeline has six steps you can see uh it's freely available so you can scan the code you can also follow the link I will show a brief demo of the pipeline uh to check it out and also to try it um and I will explain briefly the steps uh so there is a Step Zero is mapping the the policies of the tools this I have a slide later for this so regarding the pipeline the first step it takes an as input a configuration file can be a hel chart but also yaml file that you manually written and it runs a tool to find uh what misconfigurations there are in this file at the Second Step uh we automatically fix all this misconfigurations uh the third step is just the back step to make sure that actually the fixing was done correctly so the output of the first tool will be zero no remaining misconfigurations uh step four we generate a functionality profile aat will explain this later um more into detail but basically we generate a list of the functionalities needed by your application step five we add back uh this functionalities that could have been removed uh during the fixing so it's an update of the configuration file and finally we run another tool to find the remaining misconfigurations so I will show briefly the uh what the the pipeline looks like in practice I hope you can read it uh yes uh so we implemented as uh GitHub actions so this is our GitHub repository as you can see there is the the all the list of folders every folder is a hel chart however we could also take a input uh a yaml file which is uh not generated from a a chart and then we defined the pipeline as actions so we can move to the actions tab and uh we can run a new workflow that uh will allow us to actually uh scan the file with uh two tools and see what misconfigurations there are and to do this we can specify the file in this case we use the MySQL uh chart the First Tool and then we can specify another tool for example KS and then we click we click run workflow uh we upload the page yes and now it is here running we you can also configure this to be automatically executed so this workflow to be automatically executed uh Whenever there is an update in your configuration So to avoid this manual uh interaction uh and without waiting for for it to complete we can look at the previous workflow uh we run with the same output uh and here you can see uh the same steps that were back in the slides and uh zooming in uh there is the first step one uh running check off in this case fixing the chart running check of again as a debug adding the functionalities needed and finally running kicks we can further respect the output and for example for the MySQL uh application uh MySQL chart uh check off uh found 21 uh misconfigurations uh for example the default name space is being used which is a very well-known bad practice so you get a sort of output like this which of course you can also save uh locally and further insed uh there is step two again we fix all this m misconfigurations so in this case 21 and then we run check of again just as a control and we can check check that so now check off run and here you can see there are no more misconfigurations however by removing all the misconfiguration we can also we could also have removed some permissions that are actually needed by my SQL uh and for example because it's a database you can imagine read only file system will not work in this case so that that is what we do at step five we compute the the permissions needed and then we add it back into the configuration and then at step six we uh run in this case kicks so we can also expect the output of this other tool and uh kicks actually find uh more additional misconfigurations for example request storage uh and also the roling uh which however we cannot remove because it's actually needed by by the container so switching back to the uh presentation okay uh so Step Zero I was mentioning at the beginning this happened before we built the pipeline and it was needed because all such tools have different uh policy types and definitions uh so we needed a standardization of uh over the all policies from the different tools uh also because there are some policies in common some policies are equivalent uh on the description but they actually check for different configuration keys in practice and also there are different output formats so if you want to count how many these configurations you already have to adapt to the output of each tool so you can see the table I show you some examples of um what policies are in common between all the tools uh and then policies in common between two of them and so on and now we leave the floor to a gat uh to explain a bit more about the functionality yeah so Francesco just introduced different policies that are under by Checker tools like check off and d and actually each of these tools output 100 of recommendation to fix the M chart and make the configuration more secure so for for example for this example you should put this container as non root but we notice that every tool will have a different output formats and therefore this output must be manually passed by administrators also sometimes the output can be quite long to pass with several thousand of line to pass um and another example is uh when we will remove a functionality it may break functionality so for example if we consider the Falco monitoring tool it actually needs access to the O Network and to be privileged even if it is not secure so it will raise many alerts from Checker tools like check off but it is actually needed because it needs to analyze the network traffic the system call from the containers uh and then we have to design one tool that will automatically identify the minimal set of permission that will be needed by the chart to function and the permission that we cannot remove because otherwise the application will not function anymore so I will now show you the tool that we developed it's named the functionality Oracle and it enables to find the minimal set of functionalities that are needed by the application to function following the principle of f privilege so as a first step I will choose one M chart it could be also kubernetes manifest or a Jon file that yl file that you design with your application and you will run it in its default configuration so with the permission granted by default as defined in the M charts and you will have what we call the Grand truth container because it contains uh the given permission allowed and in parallel you will consider a list of permission so this permission will be outputed and uh will be recommended by Checker tools uh for example you can see uh there is one recommendation to set the user as non Roots also to remove this given Linux capability or to disallow the privilege escalation so the second step is to update and run the P configuration without one given permissions for example I will set the user running as done route so this is step two uh during step three I will recover some indicators linked to the Grand truth containers and the test containers so first of all I will collect and clean the stream of logs related to the Grand truth container so with the given permission and from the test container so without the given permission so I will collect and clean this stream of logs and for the test container I will also recover the statues from the pods the containers and the props so this is step three and as the final step I will test the functionality of the Pod without the given permission so this is step four uh I I have three functionality test case I have first tc1 so tc1 is to check that container can stack start without any errors uh then if tc1 is okay I would check tc2 so during tc2 I will look to the less and Readiness probes of the Pod and check that everything is okay and finally doing step uh TC3 uh I will compare uh semantically the logs from the grand fru container and from the test container and verify that the application behaves the same way and therefore uh if this free tc1 tc2 and TC3 test case are okay I will deduce that the permission was not needed at at the end if one of these test case is failing I will flag the permission as needed and I cannot remove it as recommended by Checker tools so I will repeat this process for each permission and input in the chart and finally I will output what we call the functionality profile that you can see on the right so it's a Jon document where for each permission I will uh say this permission can be removed uh and it will make the configuration more secure but this one I needed for the application to function so some example of functionality test case so we have a data set of 60 M charts that we analyzed so I will give several example for each of the test case so the first one remember was to check whether the container can start so in a kubernetes manifest or in a m chart most of the time we Define a startup command uh along the container conf configuration and we will check that this startup command can run successfully and without errors so one example in our data set was for the p m charts the startup command uh consist in sending a c request to the P web server and this command will run successfully only if the web service is up so it's one way to verify the functionality then if this step is okay I will go to tc2 and this time check that the container resource is in running statee so especially we look to the resource to the Pod of the deployment container then look that it runs it is in running State um so especially we look to livess and Readiness props so they are very useful because they can enable to test basic functionality of the application and in most cases they are already defined in the M chart that we used so for example the my M chart had a Readiness prop that will use the pink functionality from the MySQL admin um executable so with this command I will automatically check that I can access the mql service and it is accessible with the root password and finally if these two test Cas are okay I will compare the logs so first I will clean the logs meaning that I will remove any new numerical value and any context related information and then I will semantically compare the log to see if the application behave the same with and without the permissions and in addition I will also uh check that the logs do not contain any keyword in a preset Blacklist like permission deny operation not permitted or error for example we had an example with the data dog and charts so for this one the container could start correctly the vess and Readiness probs were actually okay but when looking to the logs of the application we can see that we actually modified the uid value it enables to prevent the container to access host files and when I modify this uid value I can see an error permission denied error because we cannot change the owner of the of token document so this is one example of the output of the tool uh it's called the functionality J profile so here is an example for the Falco M chart so you can see the name of the M chart and the container name was Falco it was the main container and we will have a list of functionalities so here we have only one example is fun 21 but we have under of these checks and for example here we check that the container should not be privileged it's recommended by most Checkers but we can see the value is set to false because the Checker so our tool the functionality Oracle Flags the permission as required overwise the container won't start so we can see that tc1 actually failed so I will now show some findings uh on the data set of the 60m chart that we used so here are the names of the charts that required the highest number of functionalities in particular we can find Falco with eight permission longor with seven permission and finally rabbit mq and prom tail with five permission needed so for example for Falco it requires uh it we cannot change the UL value otherwise it would fail and it's the same for all of the other charts uh the same appears for changing the G value we can see also that the longer pod need to be privileged and that Falco and long M charts uh needs to get the privileged escalation uh also for each of these charts you can see the number of Linux capabilities that are needed so by default containers are granted uh 14 capabilities uh and we check with the same process which Linux capabilities were actually needed by the chart to function and which one from the 14 we can actually remove so on average um for one chart 2.21 permission were actually required for each containers so it's a good result because it means we can actually deny many permission to make the configuration more secure and in terms of Linux capabilities so for the non-privileged containers uh we can see that on average only 1.

16 uh capability are needed out of the 14 granted by default and for privileged containers uh there are granted 141 capabilities by default and on average only four of them were needed another results uh here we ask ourself which are the functionalities that are needed most of the time so you can see on the left the results so the first one is changing the high U value so to prevent containers from accessing us files most Checkers tools um ask us to use high uid value but actually it makes the configuration fail for 26 containers the application will not work anymore then we have the same for the using root uh user uh to put the file system as R only and to UNM the service acon tokens so we also saw that we have a limited set of permission that makes the functionality breaks often so it can enable to Quicken the process to First focus on this limited set and we did the same on the right to see the required Linux capabilities that are most likely needed for the application to function and the top one was net bind service Linux capability that is required for seven containers so I will now leave the floor to Franchesco for the rest yes so um for the misconfigurations instead uh these are the 10 most common misconfigurations that we found again it was on the data set of charts from artifact app so of course if you run it on your uh configuration files the result can be different but we found that uh uh dangerous uh cluster roles memory limit missing memory limits and uh using the default name space are the three most common misconfigurations um and in terms of tools instead we also found that uh these seven tools perform significantly different and uh on our data set data three that three was the one that found most misconfigurations on average uh Cube linter actually has no policy that breaks any functionality uh and finally kicks is the one that found most remain misconfigurations that means it is the tool with most unique uh policies so it was actually the one finding a lot of misconfigurations that other tools could not detect um so before we conclude a list of recommendations from us uh to you when you uh deal with configurations uh files uh we really recommend to scan your configurations But first you should create a a a framework to standardize the policies of these tools because there are several inconsistencies and if you don't do that then you're you can have um false negative uh results uh that are not shown in the output um so first standardize the policies of these tools and also uh have a clear definition of what is a misconfiguration in your environment what is a mitigation for it and what is instead a functionality because uh in my environment uh what can be a misconfiguration can be actually a functionality in your in your system uh we also recommend to use one than more than one tool and also to Define custom policies exactly for the reason that every environment is is different uh and finally as aat was also showing before there is on average a very limited set of functionalities that you need on average so we recommend you to start from those and then uh move on instead of looking at the all uh set so uh last slide um uh answering the question of the title of this talk how far are we from automated detection and mitigation in uh with configuration files I think we are still pretty far uh unfortunately um because we found that um they such tools they still require quite a significant amount of manual work uh to uh both uh understand the output and mostly to to actually remove the misconfigurations and then we also found inconsistencies in terms of a configuration that satisfy one tool but do not satisfy another tool um and finally some uh also F false positive and uh NE negative results uh from the tools so there is a long way ahead uh but um I think I'm optimistic so I think uh standardizing standardization is really what we need as a first step and uh I think as a as a community we can start from there and have more secure configurations that was it uh thank you for listening and joining