test test okay all right we're going to get started D there's still room at the front if people do want to come I promise we don't bite okay uh it's early morning for me because I'm from the US so show of hands from everyone who in the audience loves virtual machines okay pretty good all right who in the audience show fans loves containers a lot more people okay I saw some people who didn't raise their hands though you must be here to see us talk about scale and performance so I I appreciate that okay so I'm Ryan hesy I work at Nvidia as an engineer this is my colleague Al Patel also from Nvidia and we're here to talk about cicd driven benchmarking measuring scale and performance that that we do in cubert and we're excited to share with you today h talk to you about why this matters and share with you how you can do it too okay so first uh we'll begin I'll talk about uh at a high level cubert what it is uh and then I'll hand the mic over to LA and and he'll go through through talk about scalability how we measure scalability uh and then this idea of a control plane as a shared resource something really important to keep in the back of your mind as we're going through that control plane is a shared resource and then we'll talk a little bit about ke verts performance and scale stack and Benchmark so things in ways we measure and then we'll share with you how you can do this too as as someone who writes an an API someone who writes an operator how you can do the same thing that we're doing in cubert okay a virtual machine is a custom resource right so what does that mean it's it's an it's an extension of the kubernetes API so really when you're asking as the user for a virtual machine you're going to the kubernetes API server and what are you going to get you're actually going to get a container right so this interesting so for all the people who rais their hands for both that love virtual machines and containers just a shout out to you guys we also love virtual machines and containers in cubert because we rely on them very heavily so we get this container and we have the cubert control plane that does some work we have inside this container we've got a cumu process we've got Levert and then on the host we've got kvf we've got the hypervisor and so we do the cuber control plane does some work generates an XML and voila we're in France voila we have a we have a virtual machine and from there now you have a you think about it it's a virtual machine running inside of a container so that's what cubert is it's it's a virtual machine running inside a container cubert provides that control plan provides the API to interact with the user to extend the ku's API to give you that experience that you can run a virtual machine just like you would in other in other scenarios and other stacks and runs it in a ku's native way okay so we work in Nvidia and we use cver very heavily and we rely on a lot of things not just cuber we rely on a lot of things in the ecosystem so what's our use case so we have a cloud that we we operate our use case is gpus right we want to provide and users with gpus and so we uh particularly with with gpus we want to provide users Graphics we want to stream Graphics over the network to provide users uh different experiences particularly we focus a lot on is GeForce now GeForce now is streaming Graphics over the network so that you can play games there's probably some people here who are familiar with gForce now you can play AAA games on your phone so we we we do the rendering in the cloud and we'll stream the graphics to you wherever you are and so that all is powered by kubernetes powered by cubert and a bunch of other things we've got oven we use um for our Network we've got gatekeeper we use Prometheus grafana uh flux um fluent vent so a lot of things that go into our stack and so something to think about when when laay is talking um this is what um we know when when she's going to show some examples and uh that how we've measured and and tested scale in our environments this is these are the things that we use um this is what our stack looks like thank you Ryan for that introduction to our stack so when we are running um this stack in production we need to make sure that stack scales well um when there is high load uh for GF F so in order to think about uh per and scale of that stack we rely on the great work that uh kubernetes six scale has done um to think about uh per fense scale the six scale kubernetes has introduced something called scalability Dimensions which act as a guidance on how to think about peran scale so three major things that include in that Guidance the first thing you need to think about is the environment in which um the the stack is running in so it includes things like uh kubernetes version what kubernetes version you are running on uh what kind of Hardware resources you have for the control plane how it is configured and the environment um impacts greatly how the scale of the um how um the stack scales the second thing and the most important thing to think about is the scalability thresholds so this me means that it's the number of objects that the cluster needs to scale at um the scalability thresholds as you can see in the in the diagram um they will differ for each um cluster use case for us uh for the stack that we run the scalability thresholds that matter are uh object counts for PS VMS uh nodes uh PVCs those are the key Focus for us um but it's it's really important to understand that the scalability envelope that six scale um kubernetes six scale has provided gives you a great way of thinking about um thinking about peran scale and this diagram will look different for each um kubernetes cluster then the third thing is um if you're using extensions almost all production um kubernetes clusters use extension in one form or another another for example mutating validating web hooks um those should have those should be used um wisely all the extensions should be used wisely in the sense that if you're looking for high scale or low latency the web hook should provide low latency for instance um the crds or the CRS of the extension um should be within the the scalability thresholds um in this envelope so once you have all of these three dimensions you can determine the SLO for the kubernetes cluster that that you are running it is also important to understand that when we are thinking about the scalability thresholds some of the work that the um Upstream kubernetes 6 scale is doing can be used here um so if you look at the diagram um the the Upstream six scale provides Dimension or scalability guidance on nodes pods PVC secretes but then when talking about extensions there are other things in the cluster that could impact scalability so what we have found is that all the extensions like uh Cube word ovs gatekeeper that we run U they compound the number of CRS and hence compound the number of uh hence compound the scale um of the cluster not only number of CRS but but they also um create some kind of um client load on the API server so in terms of the hdp requests so in order to reconcile those CRS they will send hdp request to API server and again that has a compounding effect on um on how control plane scales so the first four or five things are provided by six scale but then the hdp request and um the the scalability of cube word with respect to CRS is what we in in the cube word six scale focus on in the rest of the presentation I'll focus on how these things can be monitored um and measured to get an idea of scale just to prove um showcase an example of how these things um cause failures at scale um we were seeing frequent of API server uh SS in in our stack in order to and and one thing that was observed um was that when when we find an OM um chances were that there were a large number of um Secrets existing in the cluster so in order to root cause the actual problem behind those um API server SS uh we did an experiment the experiment um involved U creating around 5,000 Secrets um 0.2 MB um each and then um sending a list request against um secrets so a client is just asking for a list of those um resources and what we found is you can see in the U charts that at that scale in our environment two or more concurrent list calls cause an API server outage there is some kind of an inefficiency in the way API server handles list calls this is a well-known problem um in in the kubernetes community and it's being handled by an enhancement um cap 3157 if you want to learn more on what kinds of um list calls are susceptible to um these kinds of Ms it's mentioned um and explained very well in in the cap and the cap actually proposes a solution to work around those problems but going back to the problem you can see that in a large scale distributed environment you could have an extension client that could potentially U make those two concurrent list calls and because it makes those list calls in our environment um we were getting those API server HS so it's important to monitor what kind of um load extensions and other clients on top of kubernetes are creating against the API server so when we found out about that experiment we actually plotted a graph um you can see on the x-axis number of requests on sorry on the y-axis number of requests and on the x-axis um number of Secrets multiplied by size of each Secret in this graph anything in the green was perfectly okay for us if you're on the graph on the line or outside the green domain that's when um there were outages so in order to scale up let's say you want to have more than two concurrent requests in the cluster you can either reduce the size of the secret or reduce the U maximum amount of um scale in terms of number of secret the cluster can support either of those will help you um increase the number of concurrent um calls uh the AP eror can handle so hopefully this helps set the context that it's not just about the number of objects it's also about the size of each object and then on top of that how many concurrent um request the API server is serving to the clients okay so with that background if you are an if you're writing an operator and if you want to measure a performance and scaling characteristics of that operator how do you do that so as Ryan explained earlier Cube word is just um a CR and there is a set of controllers um behind the scenes that would make that VM um running so as part of cube word 6 scale what we do is we Define the scalability threshold in terms of the number of VMS um and then we monitor the client inside load that Cube word generates in order to U make those VMS U go into running state so to do that uh we at Cube word 6 scale have developed a benchmarking stack that helps uh Cube word monitor perence scale metrics for each release and at the end of each release um those graphs will be shipped to the users so they can have an idea of what kind of perf and scale um uh they can expect talking a little bit more about the benchmarking stack it consists of three major layers the first is the basic set of tools um so this layer includes U things like um workload generator so in order to per and scale test we need some um workload generator and that will create a bunch of vmis in in our test environment um and then once we have those vmis we need certain kinds of metrics um and metrics monitoring so um we have a set of tools that creates um a bunch of vmis when those vmis are being reconciled by Cube word um there is a monitoring stack which will continuously monitor the interesting metrics uh from our perspective once we have this um basic building blocks we have wired this up into CI um so these end to-end tests are run um daily through a pro and the results from that monitoring is um dumped into an S3 bucket um so every day at certain point three jobs get run and the results for those um are dumped into an S3 bucket once we have the results into the S3 bucket the next thing we need to do is is aggregate those results and plot it in a graph so the third layer will include um scraping things down from the S3 bucket and plotting it in a graph once we have a graph we just um release those graph on on the um Cube word release as benchmarks okay so that benchmarking stack will produce a graph like this so you can see this graphs are p50 of um VMI creation to running um in terms of seconds so how many seconds Cube work um stack takes in order to um send a VMI or VM into running State you can see that the graph is plotted over time and at some point the trend line for the graph changed uh at at the green vertical line we started to see that the performance uh improved a little bit and the observations were um highly concentrated around um whereas previously those were little bit more dispersed so then in order to figure out you know what caused this problem um we started digging in and we found out that um at that time time we actually had changed the underlying uh kubernetes uh provider uh from 1.25 to 1.27 and because of that um there was um increase in performance in the the way pod gets to running and because Cube word relies on on a pod to provide you a VM um because of that Cube wordss metric started to improve so so you can see that over time if something changes in the environment then that would lead to better performance and having benchmarks like this can help understand exactly what change caus those uh metrics to improve or degrade so for example all of those vertical lines in the in the graph they Den note some kind of a change in in the environment um apart from the green one all the three vertical lines are um change in Cube word um releases so we have plotted this for last three release um and we have shipped this as Benchmark for V12 not only that um as we as cube word uh runs into its development cycle using this graph a six scale periodically monitors if there are regressions in the um or improvements in the metrics and finds out what exactly caused those um to happen so for example you can see further along the time there was a time when uh the trend line improved a whole lot U and basically half um what we found out at that time was that a monitoring change was introduced in the cube word um benchmarking St stack which actually broke our collection of metrics so this second change that was identified was not a performance Improvement but actually a bug in in The Way We Were collecting metrics so these graphs are used to find out a bunch of problems in the cluster and um not not just in the cluster in uh Cube word development cycle and um we continuously monitor that as part of the6 scale effort okay so now that we have seen the performance Benchmark I want to introduce the scalability benchmarks and the way these are different is that in the performance benchmarks you get to see exactly how much performance is being introd U increased or decreased but in the scalability benchmarks you get to see how much low Cube Word Stack generates against the API server so the idea here is that the exact cost of that uh call um is not something we could determine um the user of cube word will have to figure out if they their environment can Scale based on this benchmarks so you can see um in the graph above that the graph above shows a patch pod counts for a VM so what these what this means is that if CU word if a user starts 100 VMS how many patch calls does uh Cube Word Stack makes against the uh API server and in in one of the releases we doubled the amount of patch calls that were made so we went from around one patch call to two patch calls and you can see that that is easily identifiable based on um the benchmarks and at that time six scale um figured out that the feature that was being shipped as part of this release was causing this uh to increase and it was actually okay uh because we were getting a good uh benefit from that feature okay so hopefully that showcases how to how we Benchmark um our Cube Word Stack um now the question is how do you benchmark um your operator right so there are three building blocks as I explained one is um having a set of tool set to start um some amount of load in the test cluster um there are well-known um tools that are getting popular one of the tools is uh Cube burner Cube burner allows you to U declaratively specify a yaml and say okay I want to create 100 vmis put it in a yaml and then Cube burner will start and and create those 100 vmis for you there are other tools um for example uh cluster loader um is something that is being maintained by kubernetes um six scale so even that could be used to generate load once you have the load generator the second part is U monitoring controller runtime has a well-defined set of metrics that you could Monitor and if need be um you can also add more metrics on the client side so what we have done in Cube word is in production we don't gra um we don't collect a lot of metrics but just for the end to end test we have an environment variable that would collect much more in-depth uh metrics um during when the tests are executed so this helps in making sure that uh Prometheus does not have to collect a lot of Det detailed metrics in production and you can keep the scalability of Prometheus same um at the same time understand the scalability behavior of um code changes through end to end test then once you have these set of tools um uh you can set up a CI Automation and instead of putting things into an S3 bucket which we had to do um you can actually dump metrics directly into um Prometheus or Thanos um and if that option is available as a Next Step um for your upstream or Downstream project you can actually wire up some kind of alerting rules that when these metrics um they fall below or fall above a certain threshold you get notified um of a performance degradation yeah so that's how um you get can take advantage of this um performance and monitoring stack um that we have developed and the ideas that that we are trying to um share with folks thank you um if you have uh feedback um please share with us and then we have few minutes for [Applause] questions hi um how do you created the the group by component graph in the first slide I'm sorry the the graph that you created in the in the group component um yeah in the first slide so this is not really representative in the sense okay so repeating the question how did I um how did we create um this graph um this graph is just to um explain that there are other things in um in terms of HTTP request and the number of CRS um and the extensions that Cube word six scale sorry kubernetes six scale does not Monitor and it's important to look at those when you're running a production stack it's not actually representative of the actual numbers it's just um uh you know yeah it's just denoting an idea yeah okay looks like there's no more questions we'll be here for a few minutes afterwards if you guys want to come talk at all thank you very much everybody