hi everyone um my name is uh Abdullah and I'm here to present um our work on the leader workers set API for distributed inference um this is a joint work with uh my colleague ruping and um and other uh people in the community so before I get started let's just show hands how many people are familiar with llm serving okay that's good and how many of you are familiar with um distribu multi node multi-host serving okay that's good and how many are familiar with disaggregated serving only three okay and how many of you have a flight at 7:45 okay um so most likely I'm going to run out of here right after this talk I'm sorry very sorry but I had to catch my flight um but I'm really happy to to discuss anything related to distributed inference and leader worker Set uh through our like you know Community uh channels all right so what is the problem we're trying to solve here well large language models are getting bigger and bigger by definition they are large but they're getting larger and larger so we start like right now average model size is 70b like if you ask people they either use 7B Lama for example and now they are more frequently using 70b Lama models those models still fit in the GPU memory um of one or two chaps like if you think about an a100 for example or h100 GPU and a100 for example have like 40 GB of memory right so 7B um uh model uh with um you know uh full Precision for example 16 bit it it can fit easily however now we're crossing that into a new territory even the open moders like we all we already know that for example GPT uh even Gemini they are probably one trillion plus right so that's like that's not a secret but even the open models that we have out there uh are getting larger and so the latest one is Lama 3 405b that meta released um a few months back those models they barely fit the GPU memory of a single machine most machines the largest one for example on any cloud provider you got eight a100 or8 h100 gpus so if you do a math like for 405b you need 800 GB full Precision uh s so that that would not fit the GPU machine um a single GPU machine so instantly you need to have at least two um even if you do like you know quantization and um make it like you know 400 GB size it would fit a single machine but you wouldn't have enough space on the GPU memory to do a lot large enough batches right um and so we expect that this 405p is just also the beginning it's going to continue to grow granted that gpus will have more memory will have bigger machines but models will also grow larger and larger now on the TP side we even have a more like you know critical problem like with tpus we scale horizontally like a single TPU device has like you know uh 32 or even 16 gab of of memory um and a single node actually has only four chips and so at at Google we like you know quickly require multi no multi-host serving so that's why we're trying to think about how to solve this problem um and so that's generally the the the the the problem the issue here is that we don't have an automated way of deploying such large language models um on on kuber let me explain why so a single model server is a single pod right so it fit single machine even if you give it eight gpus on like it's going to work just fine like you could you could like you know uh use a deployment you create a pod per per server and that server could you know uh um occupy all eight gpus however once you go multi-gpu multi multi node you don't have the concept of a pod that runs containers across nodes right and so now we have a we need to have a New Concept okay how do I deploy a model several multiple Parts you can do it right you can use a stateful set run for example in the case of 405b on on uh an A3 machine for example on on Google Cloud um you can create state of two pods but then okay how do I scale up like I want another one the third one fourth one are you going to manually try to create new stateful set or script it you need to hook it up to HPA right like how how is that going to work and so that's why we propos this new API called leader workers set so the leader worker said API is as just as I described um it's an API that basically tries to create a deployment of super pods by a super pod I mean a pod a super like a think of it like as a pod that runs processes across multiple nodes but it acts as a unit and so the idea here is that I want to be able to create groups of part as a unit scale them up and down behave exactly like a deployment as much as possible to Plug and Play like a deployment like if you if you are doing single horse you use deploy deployment if you want to use mult if you have a multi-host uh workload you just basically remove the deployment plug in leader worker set and everything should work just as fine so how did we do this take a look uh at the right side so what we did was basically as as like you know all custom apis are we use crd the way that we did it is we create we we structur it as like two level so we have built on top of stateful set API when not trying to reinvent the wheel always trying to compose from components that work and prove to be working really well so that stateful set that we created at the top we call it the leaders stateful set is the one that's going to manage the the groups notice that in each group it's not homogeneous we have what we call like a dual template group there's one pod with one template and then a group of PODS we call them call them the workers that are that can be created from a different template the leader pods are again created using the state like the leader state will set at the top so when you create an lws a stateful set leader state is going to be created state will set API and controller kicks in is going to create however many replicas you want now the leader worker set controller what it's going to do is okay for each leader part that gets created I'm going to create an a stateful set the worker state will said and assign the owner assign that pod the leader pod to it as an owner and so the leader pod is going to each each pod in the leader state for is going to own a stateful set that represents the workers why did we use a stateful set again we're composing but also because we need stable indices we need stable DNS host names as well that is extremely necessary to enable distributed um uh distributed um inference because if you think about it like each group is basically a distributed job of some sort so usually like probably most you are familiar here you use if you want to use VM you're going to uh instantiate Ray underneath it and then you've got like you know the rhead is going to run on the leader stateful set and then the workers are going to run on the workers uh um stateful Set uh now as I mentioned I want to go quickly about like the set of features that we Implement that we have here but I I will also later hand it over to ruping to go into detail how we implemented them um so we we have also group startup policy again um the idea here is that we're only going to create the workers state will set only when the leader is ready because a lot of Frameworks require that the leader is ready basically it's running because they are going to connect to it back and so that makes it easier um for for these applic for these Frameworks to uh start up quickly um we also support as I mentioned horizontal scaling um again as each group as a unit gets scaled up and down um we have group restart we basically we're restarting the super part as a unit again we always want to think about that group as a unit we don't scale up and down the group itself we scale up up and down the number of groups um we also have support for Rolling updates as we like we don't roll do a rolling update within a group we we roll updates the group so it's a group as a unit is going to be updated and then we're doing it one by one and grouping is going to talk in detail how we do that and implement it and finally it's we have placement policy again usually with multi-host serving what you need to do is you need to place the workers on within the same network topology with tpus is usually a TPU slice where the nodes are connected with an ICI link just special high high high high high uh communication uh interconnect with gpus you probably want to place them on the same rack and then with the new GPU generation it's probably going to be the same NV NV link domain um so we have that as well uh with that I'm going to hand it to Ring to talk in detail about um horizontal scaling I oh second Al Sor it works yeah howo everyone uh I'm uh first join C so a little L if I yeah so um today I will be cover mostly the informtion detail of the leader work Set uh yeah for the Autos scaling part so leader work set actually exposes uh scale sub resources to allow HPA to adjust the number of replicas and that way uh the way that replicas uh scaled up and down is through managing the number of leader ports in the lader St set uh below is a example how we configure um the uh HP uh Le work set for uh using HPA to Auto scale so uh this is a end to end flow chart and once we configure The Matrix um for hp2 scale for example like the prefer U log size uh or of a motel server and then once like we see this Matrix SP up then HP will trigger the sub resources of the Elda base and set the little work as replicas to scale up and as we can see previously only have one po group here once uh little work um replica number is adjusted it will spin up a new leader port and the new worker step set will be created as well uh which will share the same life cycle and the DU Port group will come up and running uh next part is fure handling nowadays most distributed INF workloads needs to be reinitiated when adding process or worker in the group fails so this is a common property of this distributed M Frameworks like a mode controller jacks or distributed pie torch and how we make it works and little work said is that when any port or container restart in the single serving replica and it will require the entire replica to be recreated or restarted for example here as we can see in the port group one we have a worker running here and when adding port or container restarted in this worker or like in adding like the worker Port here or leader Port here so leader work set will detected those and restart the entire Port group one which uh this port group will be recreated and then the the initialization process will uh run again for this uh Distributing inference workflow next one is topology aware scheduling so in distributed inference we need to schedule the same group of ports on nodes within the same network topology to minimize a cross node communication overhead so one port group will be exclusively scheduled to one topology domain for example a note group a load pool so one port group is a single multi-host serving instance and a topolog domain here is a group of nodes that shares the same node ENB which C categorize them to be the same group for example a multihost TP slice can be a topology domain here and also a slice of a GPU rank or any like a uh a group of those that have a very high uh nwork collected uh in the picture below as we can see we have two pole group here and we have two node group as well uh which is a TPU multihost slice and this TPU host are collected by the ICI which is very fast for data transferring and each leader and worker Port will be scheduled on one TP host or TP node yeah and those are belongs to the a replica and those are belongs to The topolog Domain this is how we implemented the to OFW scheduling so we use um port a fin and Port anti Affinity uh with like a a unique K and unique value to ensure like the uh port in the same group will only be scheduled on the same topology domain yeah next one is ring update uh support So leader work said support ring update uh strategy with Max unvailable and Max search setting so max unvailable here means like let's say we we have five uh replicas so we only ensures uh at most uh Max number a Max available number of replicas is not available right now and with Max search supports that when we are running the rolling update we will always have Max SE number of replicas uh like being brought up before we proceeding with the running upgrade and each replica is a unit for updating the triggering of the up upgrade is a template hash that be generated through the leader work template and some API fails that we believe is important uh that we need to have the all the replica to be upgraded so this hash will be uh stored as a label on the leader St Port template and when this hash ble changed it will like trigger the leader step to start the Drone update and upgrade process um site like the leader work set actually relies on the leader stoc set to manage the process of the upgrade the upgrade will stop when the upgrade replica already upgrade replica on Hy for example let's say we have five replicas and we already upgraded a two replica but like the first replica is UN Healy right now at this time we will stop the upgrade and and like stop there and like the process of the this is managed by the leader St partition so so whenever we are upgrading right and uh we reach let's say we reach a thir replica and we will use partition to manage like a uh the progress of the uh upgrade only if we change the little partition number then the upgrade will pered and for the uh upgrading of a single replica the mechanism here is like we will restart this uh upgrading replica and so like recreate the leader port and the because of worker C is have a same life cycle with the leader Port so when we deleted the leader Port the workers of state of will also be deleted uh at the same time so uh when we are upgrading this single replica like we are always doing this through recreating the entire Port group on the right side is a picture of how this upgrade process happen uh we always upgraded the uh replicas from the largest index to the small smallest index so for example here we upgrade Port group three and then Port group two all the way to Port group z this is a larger picture of how up grade works I will skip this uh for now yeah next we will uh show several example how we run the multi node inference on GP and TPU this is a seta for uh running multi Noe inference on GPU uh as as we can see from right side U we have we have two A3 nodes with h100 GPU chips using compact placement and the the good side of compact placement is that we were collocating this GPU nodes to minimize the cross node communication latency especially this decision transfer and the m r time we use here is like we use the V plus r and we are running n 345b model with tens Paris equals to 8 which is as we can see here number of gpus per node because we use remember two A3 nodes with eight h100 GPU chips so the tensor power here we use eight and this will ensures the all together happen which requires a lot much larger uh data transfer uh throughput will only happen within one GPU node and we use the piper paron as two which means like we split the model layers between two uh GP nodes and we in each model of order paath we only ensure like the latency uh penalty to this cross node communication is very minimum when we are doing the uh INF request and ideally this GPU node should have RDMA enabled which is like a accelerated uh direct memory access next one is the example for TP multi host inference right side is how we set it set it up so we will be using two TP slid of V5 V16 which will have four TP host uh each TP host will have four TP chips as well uh we will have five ports per replica running on four nodes one one uh one node will be running one leader port and as well as like one worker Port as on the worker Port we will running the passway worker and one leader Port will be run in the model server which is Ja program and some like proxy R manager which is like the M Stack uh so the model Ser uh model server we will be using is is the jream TP inference server um password is a distributed compute uh run time here and we will still be running the Lama 3 45b model with only tensor prism because uh all tpus are conncted with ICI it's a two-dimension topology so it's is very good at uh transferring data uh so using tensor PR here is like already good enough and cross host communication only will happen through inter chip interconnect next we will discuss about another major uh Distributing invance uh work type which is called this ACC serving so uh the process of a LM inference can be splitted into two phases which is pref and decode prefill is much compute bound and decode pH is much more memory bound and the two faces will interfere with each other running on one server because they will compute for resources and um and like when we are running the decode Loop whenever there's a compute L inference reest comes in the decod will be interrupted and to do the prefitting of the request so all this kind of thing will cause a RoR efficiency and unpredictable latency as well so to address this problem uh we uh could run this a serving which will split the pref and decode stages uh to run on separate servers this will brings the following benefits so first one is reduce the prefer decod interference so preferes you usually work at bad size one and decode pH running with a much larger batch size so you know the prefer stage when we're doing prefitting with the new incoming request we will always interrupt in the decod loop and this will uh no matter make the hbm to be occupied for a amount of time and as well as like a stop the de C Loop for some time and decod loop will not use all the computer resources in the TP chips as well so which will kep the uh computer reses to be ideal for some time so the ideal solution here is like to split this two faces and which to optimize both memory usage and computer usage the ne uh next benefits is like it can also reduce time to First token latency under a highr latency situation uh and first token deny is very important because it is a c customer perceive latency and it is the first token is a time like one customer first time seeing the return token right so um optimizing that will have customer to make customer to have a better user experience as well and a thir part is enable heterogeneous VM types for prefer and decode that matches better the computer characteristic of each phas so remember prefer and decode have different um bound right uh so for prefer we can use a power computer powerful machine for decoder we use a memory powerful machine and the good side for that is like this machine can sometimes have different prices right enable heterogeneous V types could ask could have like uh Cloud providers to use different V types to with even lower cost to have this combination of uh infrastructure to do a inference requ to work around INF workload and with like extra benefits as well uh the last one is uh better overall re efficiency and lower cost because uh actually we have like some benchmarking and also experiment uh running like we can see actually two point two times uh cost reduction uh with head genius and dis serving uh with like pretty similar uh lat CSL um which uh we run like a no matter uh h100 uh combining with TP V5 uh TP V5 we use that for decode and um h100 we use that PR and we have see actually pretty good overall cost reduction as well so um for running this serving here we use l work set to help oate this kind of serving Paradigm um this is a picture of how exactly we set up the thisx sering with the work Set uh this is a more detailed uh one replica of like uh serving for code so here we have two prefill TP slice and we have one decode TP slice and which which like when the um inference request comes in to the this serator it will get prefilled once is finished pref we will transfer the data uh k k Cas to the uh decode uh TP size here and this all happens uh and the otion of those assignment of thisp slices um for prefer decode is done by the model server or dis oor and all the computation task dispatching and all the like Jack API C is handled by the a proxy and also the passway worker here and this is a second replica of the data serving uh so all the service will only communicate with leader ports model server uh container here okay so next one we will go through a quick demo of like uh how to use the work set to run mot host inference uh on TPU right now let's do a quick demo this is a kuber is Manifest for deploy the jream password serving instances in this file we have specified the number of rcas as one and enable exclusive placement let's ensure one GK not Port will be one replica we configure the restart policy to recreate group on P restart it ensures gun restart for individual port or container failure this deployment targets TPU V 5e Port slice with four hosts in this single replica there will be five ports being created one leader port and four worker port each worker Port will consume four TP chips let's deploy this manifest into our kubernetes cluster we can see all five ports are created now the first one is a leader Port others are the worker ports let's send a sample request to the jream server this is a response we get with leader worker set we also support Auto scanning failure handling and rolling update right now I will show the auto scaling part this is using cor horizontal Port Auto scaler component in the HPA manifest we have set up the minimum number of replicas as two Maxim number of replicas as five and the target metric as just stram prefill backr size this is a custom metric we have added to the ging orchestrator it shows the current node of the model server we set the average value as 10 which means for the past 50 seconds if the average number of requests in the gam prefill backr is 10 we should scale up the number of replicas in the Target reference section we have configured to the leader worker set object named ging passw now let's Deport this HPA manifest as we can see the original number of serving replica is one after zero seconds the scale up was triggered by the HPA component to increase the number of replicas to two if much more requests are sent to the jream servers which can't be handled by the existing replicas timely the scale up will be triggered again automatically next let's rout to the feed recovery support when any hardware or workload feeder happens to recover this serving replica all PS in the Sam group should be restarted by the controller here I will delete one of the worker PS to simulate the fature scenario in this replica as we can see after leader workers set detected one port in this group has been restarted it will automatically restart the entire group of ports yeah I think that's a demo here I think pry much it thanks I have few minutes for questions but then I have to run go ahead one for prefill and decod and right but how are these two supposed to be connected so the TPU slies are actually connected via dcn so you have two TPU slices one is running decode the other one is running prefill if they are not on the same TPU pod right they are going to be on two different pods the communication between them is going to be through DC and D Center networ so the KV cach transfer is going to go through DCS yeah so the first one is doing a prefill and then generates the KV cache it will transfer it to the other one which does the decode so it yeah think of it as a pipeline parallelism basically yeah yes how does this intersect with Dr it doesn't or it does to a degree Dr is single node focused this is multi node did you see Kevin did a lightning talk today about the new BG 250s uh Nvidia yeah yeah that the Nvidia new Nvidia Hardware you mean yeah yeah that's that's going to work on it really well show that yeah how does topolog um so um it's not at that level like the the like the you think about it as exclusive placement like you want to collocate them on the same topology zone is way too big right like set I understand for the four parts right in a set but if I want three replicas oh right just like you're doing with deployment think about it as a super pod those are going to act together as a unit right uh because they have the same template so when we inject any node Affinity Etc they going to go together right with the exclusive placement the leader actually the way that we Implement exclusive placement is actually interesting so we first schedule the leader pod and then we know where it's being scheduled and then for the workers we're going to inject a n Affinity to the topology where the leader landed right so we we ensure that the workers are following the leader one question your pay model seems to be stick to All or Nothing so when one worker goes you restart the whole group is this really an intentional Des decision or you think to make this configurable later it's configurable it's already configurable but like 99.9% of the Frameworks they're not intelligent enough to handle like individual worker failures they have to get everybody restarted to start from the same uh um State basically but the crd basically allows yeah it does basically you could you could get this normal stateful set Behavior with one caveat if the leader is is dead the work is going to be dead why because the leader is the owner of the of the so so they are connected so we're thinking about that as a like a scheduler feature you see like here we are claiming exclusive placement but we're not actually doing the scheduling we are injecting scheduling constraints to inform the schedule what we want to do um beyond that it's going to have to be like you know uh a gang scheduler um we do have that in Q or we will have it in q um ideally we will have something in like you know in Cube schedu like you know as a plugin correct so we're the group is uh immutable think of it like it's it's a unit when you when you um created the leader worker set right you said I have four workers you can't change that but those do not scale so each superp is fixed to four and then I'm going to autoscale the number of these superpods yeah right add another yeah but it's not an inference server that like the group the whole group is a single inference server that is running on a distributed set of nodes so this is fixed right because it depends on your model size so you say 405b requires two um you know nodes that's it like you you like that's what's going to happen now if you want want a horizontal scale you're going to create another group of two notes another group of two notes right so that's how and that's supported that's how we do it because I got conf no no no we're not increasing the number of workers we're increasing the number of groups GRS yeah exactly exactly exactly it's those are like one unit right yeah no problem you have one one service Per Mar right because that that was my question number of workers per model then basically if you have much bigger models you'll have to like even if you're scheduling smaller models which don't need as many workers you'll be scheduling the maximum number of workers yeah it's fixed so basically you you you want to know like what is your model size and say okay this is is it same thing as when you decide how many gpus you need right just want to go here any other questions it's pretty much the same so you want to think of it again as a superp with the leader board being your facade right so it's your representative of the service so when you configure your service you're going to set up um a selector right so that's the is going to only select the leaders and the leader is going to receive the request fanner the the like starts the distributed job basically like the distributed inference uh processing it will collect the result and then will return it even if a service mesh or something is involved the request will always flow through the leader yeah you want to present this as a single unit all all the time like with your service mesh setup you want to forget about the workers and just think about the leaders right so the workers are basically the workers for that uh for that leader yeah so we have a PO template right for the um for the workers separate from the leader so it depends on how you set it up so you can it's it's yeah you should be able to yeah exactly it depends on the storage like you know itself characteristics if it can be shared between the yeah and yeah but we don't actually we don't expose the stateful Set uh characterist um knobs with state for I think you can also create you know create like you have a actually you can if you oh no you don't the problem is that with the leader worker set template you don't have the stateful set template to create a PVC template yeah we we can expose that we can we can try to yeah if it is needed but I I don't think it is so leader worker set is going to do the orchestration right it's going to set things up it's up to the disaggregated serving implementation um so with like what grouping uh described the jet stream which is like the new open source model server we have on tpus um it's going to have like a an orchestrator that will know like it will assign okay this slice is decode this slice is prefill and you have to configure it like with some parameters right yeah that say be heterogeneous so a small GP right but not like it's not going to be within a single so that that that's that's a very good question so the way that we envision this working you have to set up two lws right and then because the the um each replica has an index you can all and you can match them exactly right yeah so because I can we want to simplify the like the for the simple CA like the more traditional case if we go into that level like heterogeneous workers the complexity is going to be 10x super complex exactly I work sometimes with team anding ated support yeah but with this I think with two that's that's a very good question and we thought about it from the beginning for hetrogeneous setup like the way that again we we're we're following some sort of like a a model where we're composing right so we compose this lws from stateful set and the idea is like we're going to compose this aggregated serving orchestration from two lwss a followup to that might be too complicated an easier followup you also mentioned rtma that in we're just hyp like you know this is has nothing to do with lws itself it's depends depends on the provider you have the full P template so whatever you you're able to do with a state for set you should be able to do here yeah can someone order an Uber for [Laughter] me good how does observability look for lws when you look at uh measuring performance or it is again the same it depends on the framework um like Ray for example you're going to have rayad running on the leader and Ray workers running on the on the workers right so if the rayad is the one that is emitting the metrics you're going to have to set up like you know but when health is reported does it report as oh Health right so you again it's it like it depends on your framework uh in our case for example jet stream server itself is on the leader it's the one that's going to be responsible for declaring that the superp is healthy or not right so you you're going to do Health checking on the on the on the on the leader at least that's the pattern that we have in mind and we've been using right thank you so much