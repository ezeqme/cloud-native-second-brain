welcome everyone thank you for joining for this Friday afternoon session uh today we are going to have stop kubernetes revolving doors and some tutorial to secure a cuetes cluster so my name is May I'm working as a software engineer at isov villance uh let me introduce Savita thank you thank you mah My name is Savita ragunan I am a senior software engineer at redart I also work with Mah and Reay on the SEC kuity Sig Security Group I'm the lead for six security documentations of project and now I'll pass it over to Ray hey folks my name is Ray Lano I'm a Solutions architect at Red ha hats I'm also uh one of the co-chairs of kubernetes Sig docs I'm also one of the sub project leads for Sig security so I help run the external security audit of for the project and if you don't know um Upstream kubernetes by default is not secure guard rails are not in place when you install Upstream kubernetes so that means pods can freely talk to other pods even on other nodes uh if we could easily Traverse across namespaces as well uh if a user has somehow breached a pod or container they can also be able to access the the cube API server and also the the worker node itself so here we're talk we're here to talk about how to secure kubernetes and we did publish an upstream security guide checklist on how to do that thanks Ray uh yeah the security check list was merged almost two years ago now you can see in this picture that many people actually participated in its creation you have a bunch of reviewers a bunch of authors on the on the pr and we had a lot of discussion around this like uh we created some draft and then we had like something like 200 uh conversation on this uh PR so the idea the goal was to provide the Community with like a basic but solid official checklist uh that is not maybe sufficient but it's it's a nice start for starting your security Journey with kubernetes and because quite often um so it looks like that uh quite often the question about Security in kubernetes is like where to start so this checklist give you a bunch of list of pain points and how to De deep into like this uh pain point and and learn more details directly in the kubernetes documentation right once again there's a link to this kuet security checklist uh like myad said it is uh a basic security guidance uh it may not and is not sufficient for production but it is a good starting point uh so some of the sections of that security checklist is authentication and authorization uh we're going to go a little bit high level and we're going to go dive deep in the tutorial so we want want to go into the tutorial uh pretty quick to get folks started in the workshop so with the security checklist topics we have authentication authorization like to make sure uh service accounts are being used by your workloads making sure the super User Group uh system Masters is not used and if it is used why there might be why so we have to take a look in the workshop uh make sure the arback good practices are actually uh use there is an arback good practice guide as well then we'll go into network security uh make sure to you can or you are using uh a cni plugin that uh that can do Network policies so we so we can secure Nam spaces uh as well uh make sure we have default deny all uh Network policies as well we can control the Ingress and egress also we want to make sure we don't expose uh ETD or the API server or the CU API out to the internet then we'll go into pod security uh we'll learn how to uh create arbac or use arbac to create uh to use like a pod viewer role and to make sure only the the folks that actually need the crate uh ability or the patch ability the delete ability actually have those uh next we'll go into Mission controllers we'll take a look at the Pod security Mission controller and take a look at pod security standards as well which replace pod security policy uh what we we won't go through all of the sections through today's tutorial we won't we won't go through logs and auditing uh but if you do have uh audit logs enabled to make sure that they are protected from General access we will do pod placements so we'll take a look on how to uh Place pods uh securely into or to isolate to to isolate your highly sensitive applications uh running on nodes we'll also take a look on how to place pods on specific nodes as well uh we won't go into secrets too much because well we're not going to do an external Secret store and we won't go through uh images like like scanning for scanning your images so for this tutorial we are going to use kind um we have developed in such a way that it can run in your local machine um so you can try it out later if you want to go home and you know have a refresher or you want to pass it on to your friends you can we have the repo shared in the um little paper that we have put there and we will also upload the slides um the prerequisites for this is doer and kind is Essen e communities in Docker so each notes runs as a Docker container and we have provision the kind cluster with celium celium is going to help us do the network policies and um kind is also a kubernetes project and it is used for local development and integration testing and everything it's a really cool project if you want to test new features it's always up to date so I think Whenever there is a kues releases made the next day or immediately the updated version of kind is available so it's cool um we are also going to use instrict um as our platform this is a browser BL browser based platform which lets you do copy paste or click and run so that uh to provide a really good um experience for you all today going to go on here and we are going to pause here for a few minutes so please um feel free to log on U and if you have any questions or anything please let us know all right so there's going to be eight modules we'll give some folks some time to log in uh there are sheets uh with the same login or the same short uh URL link and with the QR code as well so please feel free to log in as well so we're going to try to go through slowly uh the steps as well uh but you can go on your own pace uh there are eight modules like I mentioned uh so I will go through hopefully I'll I can enlarge this I'm going just going to start uh wait a few more minutes to get uh to wait for folks to okay got it and does anyone need a copy of the access let me put it go back to that slide for now okay all right sounds good looks like we have a fair amount of people logged in um so we'll start and like I mentioned I'm going to try we're going to try to go through this slowly uh and explain some of the concepts as well and there are some loading times as well so I'm going to go to starts you have your terminal on the left let's see how it looks okay on the screen and you have instructions on the right uh so the format is there's some concept explanation uh what each module is going to cover in terms of the of the security checklist uh and also some steps and there is a click to run functionality as well uh there are some modules that does have checks so you do have to make sure that you do all the steps uh in that module so the first one is authentic and authorization about the system Masters group so uh one uh such default group is a system Masters and uh the security checklist says make sure that we're not using system Masters uh for any user or uh in a group uh for for authentication after bootstrapping and to also make sure the uh role based Access Control good practices are followed for guidance uh so in that same arbac practice guide uh we have a concept of granting lease privilege so we want to make sure that there is minimal rback rights given and there is a module on how to create those minimal rback rights so if you don't know system Masters group is a hardcoded group and uh it actually bypasses all all authorization checks so it gives uh someone who is using or in the system Master groups uh super user rights uh in the ku9 cluster so you are cluster admin a little bit more into uh some Concepts on how kubernetes does uh rolls and uh roll bindings uh so uh kubernetes does use rback by default um so we with our back there are uh four types of kubernetes objects uh two are namespace two are not Nam space uh so uh roles and cluster roles is where we actually Define the set of permissions uh and the permissions are addtive there's no deny roles to them roles are Nam space cluster roles are not Nam space and to actually Grant a user uh or a group those defined roles in a in a role or cluster role uh we use a um binding so they could be a role binding which is namespace or a cluster role binding so with cluster Ro binding or or Ro binding is what we will use to Grant permission defined in those roles or cluster roles to users groups or service accounts and a role binding uh we're going to go through some of the concepts here it's used to Grant uh the Pod reader role to user if we do create a role called pod reader for like get watch and list and then we could apply that bind it with a role binding so back to the security checklist we're going to check to see if the system Masters group uh is actually being used and we'll verify it uh with if any cluster Ro bindings or role bindings Grant those permissions to a group so first one is indirect to inspect the cluster R bindings and and roll bindings so we're going to uh take a look to see what the cluster roll bindings are actually created in the cluster with key control get cluster roll bindings and these are cluster these are cluster wide so we see a very uh long list here of cluster roll bindings I'm not going to go through all of it so there is a long list cluster Ro binding so we're going to try to find uh better ways on how to actually find to see uh if the uh if the system Masters group is being used so we're going to check the description of one of these cluster Ro bindings the system Public public info viewer and we see here uh the expected outputs here on the left here we do Cube control describe on that cluster Ro binding on system public info viewer so we can see the name we see see the the role that it's binding and we see the name of that role as well and as we see the subjects here we have the system authenticated and system unauthenticated so with this we see the cluster Ro binding here binds the permissions uh that are set in the system public info viewer uh to two groups that system authenticated and system unauthenticated and both the system authenticated and system un ticated are builting groups so we're going to now check the Manifest of the same cluster rule binding here of the public info viewer and we got a bigl file here uh we could also same get the same manifest in Json as well so with uh the ability to use Json we could use a command line uh Json processor called JQ to use to par to search for any cluster Ro bindings and R Bindings that bind that system Masters group so we're going to try to find a way to retrieve that easily so first let's get all cluster R bindings in Json so this is all the cluster roll bindings in Json so it's a lot to go through we're not going to go through all of it of course but it's a little bit much to uh to go through all of them so the way we're going to actually find to see if the cluster Ro binding binds the system Master groups uh we're going to use GP it's simple to use so we're going to do the same thing uh get the cluster all cluster Ro bindings the outputs to Json and just grap for system Masters to see if it's actually being used and we see it actually being used so do we expect that maybe so we'll take a look and see uh what is what role or what group is using the system Masters so to verify uh if this output is correct uh or to get more info we're going to use a the before context with grap and we get some nice trace on here so now we can see that the cluster admin cluster roll binding binds the cluster admin cluster roll to the system Masters group and uh and the cluster admin uh is a default uh one of the default cluster roles and it is supposed to use the system Masters group so it's typically used uh during the bootstrapping process so we're going to check to see if cluster roll bindings or R bindings uh that bind to the system Master groups with using JQ so here we're going to J use JQ get all the cluster Ro bindings again and Json uh we're going to use J J J Q to iterate over the items and we're going to use the select function in J Q to uh to find if the group if there is a group that equals system Masters so we do have an output of course we have that cluster admin uh that we expect uh so there is an error as well which we uh we cannot iterate over null uh and we get that error because uh there are cluster Ro by that do not have Subs uh so we could actually fix that if you want to make it more cleaner we could add a question mark uh to any of the the close bra open and close brackets uh so we could we to not output that error now we have a clean outputs that we can use for further iteration so now we could now we know how to parse using JQ now we could uh take a look with using Cube control and using custom columns and using subshell we could make a nice pretty output of custom columns to see if there are any cluster roll Bindings that uh used and the bind system Masters and we could take a look at that and we do have the cluster roll binding as we expect uh with cluster admin uh which binds the cluster admin cluster roll to the system Masters so now it's easy to use uh we have easy to read columns so we could we could use further on now we could also take a look to see for the cluster uh for for cluster BR bindings as well and we use the same we use JQ to iterate and we do expect the cluster admin which we do expect now we could also use the subshell with that same query to Output the names of the cluster R Bindings that have the system Masters group in the sub and the subject uh to the C control command that outputs to the custom columns and and we have a command that outputs the cluster R bindings with the assistant Masters groups in the subject and also outputs the name of the cluster Ro bindings in the cluster role and we can also add uh in any subjects possible subjects and also any labels so here we do have the nice outputs of the cluster roll cluster roll biting with a subject system Masters any possible labels as well that actually tell us that it's actually used for bootstrapping and part of the rback defaults now we have a cluster Ro binding but there might be a ro binding that actually uses system master so let's use that same Curry to see if we have any Ro Bindings that might uh use system Masters which it should not and we do not so we have that as your expected output uh just to summarize so system masteress is a special super user group that is uh used for bootstrapping and should not uh be used by any cluster roll binding or any R binding outside of the default and to check it uh we have we use JQ to iterate uh over all the cluster roll bindings and also all the r bindings as well all right so I'm going to it's going to take a few seconds for the next module to load up so we'll give some time yeah I wanted to ask thank you right I wanted to ask like does anybody uh have any isue joining the workshop is it me oh yeah no yes yes you can actually uh do an Opa policy um as your external admission controller as well so you can prevent uh any cluster rooll bindings Ro bindings to use system master and that's probably very advisable to them so yeah I assume that it works for everyone in the room yeah nobody has an issue joining the thing just want to make a note it's not just open use keboard and kyber other admission controllers as well all right then let's move on to the next one so here we're going to see about applying netor Network policies and it is from the network security section of the checklist so we're going to see how to apply Ingress and egress Network policies and default Network policies within each Nam space selecting all parts denying everything or in place um to give a little bit of background given the distributed nature of kubernetes networking is a critical component there is container to container communication part to part communication part to service service to external and YC versa so there is a lot of networking to deal with um in this section we're going to see how to apply Network policies on part toart Communications so in a typical cluster there could be a number of name spaces and each name space can pertain to a team or a critical workload and we would want to ensure that they stay safe if something is going to expose uh pii information we want to make sure only the authenticator only the allowed Parts can reach out to this one um where we can get that information that is the use of network policies and it can go beyond that uh in this we are just going to see very simple how to apply it between two parts running in a source name space and a Target name space we're going to create two name space is first and we are going to create two anic spots and then we are going to create a service for each of those spots so that they can be accessible and then we're going to see if we can reach out from one part to the another so let's get started here I'm going to first create a source Nam space and then a Target name space and I'm also going to create a um pod that runs engine X 1.27 image it's a very simple web server part and I'm going to create another one in the Target name space running which is going to run the same um image and now that we have all the parts running let's just verify it yes we have the parts running in the source name space same goes the target name space and and uh let's create services on each of the namespace so that these parts can be accessible so when we create a service it's going to create a a good um friendly DNS um URL for us which uses the cluster DNS that can be accessed in general the format the part name the um uh Nam space and SVC do there there is a format for it I think that cluster.

local and um let me see if I did the target let me just do the source as well yes we have two of the services created going to see if it's up all right okay so this is a format that we have um service. namespace SVC cluster. looc and now that we have these two up and running let's see if we could um reach from the andine next SP that's running in the source name space to the one that's running in the Target name space we are just going to use Cube gutle exit and then just do a simple call so yes it can reach there is no network policies in place and every part can reach out to every other part using services now let's get started by applying a simple Network policy in the Target name space what we're going to do is we are going to um deny all traffic Ingress egress Ingress is any traffic that comes into the part from external entities and then ESS is any top any traffic that goes of the part to external entities so we're going to deny all of them so none of the part should be able able to reach out to any parts running in the Target name space let's create this network policy once we have that we are going to run the same command that we ran before we're going to try and reach out to this part in the Target name space from the one in the source name space so you have a time out of 10 seconds and should not be able to reach to that yes the connection Ed out and we shouldn't be able to reach out that is because we already have a policy in place what we're going to do is now we are going to create another policy to just allow Ingress from The Source um name space to the Target name space here you can see we are creating a policy in Target name space and then the type is ingress and then we are um adding a namespace selector saying that anything from the source name space any part from the known source name space should be able to reach out to the ones and the target name space let's go ahead and create this and see that the policy is created and let's now do the same test and yeah so now we are able to reach out because the policies are in place um that's great let's just test this by Crea creating a new namespace called new source and we will re repeat the same process we did create a pod expose the Pod using a service so that it's accessible and from that pod we will try and reach out to the Target name space just to verify if you're are able to reach out uh to anything in Target name space since we have two Network policies there created that and I think the part is ready going to just out and see yeah so this confirms that we are able to apply the network policies the network policies that we applied are working as intended so if we just redo the test again from our source to the Target uh the pods from The Source name space to the one in the Target name space we should be able to access the data so now we have a two policies a default deny Ingress and egress from the target name space so any of the traffic that is going out of the target name space to any of the name space should not not work because we already have uh we are blocking the egis let's just confirm that so in this we are reaching from the pod in the Target name space to the pod in the source name space so we see that this errors out and now let's go ahead and create the policy that we did before but this time we are going to say it's an e type and we will select the source name space so once we have this policy technically we should be able to reach out to the part in the source name space let's go and repeat our test so we are again reaching from the T part in the Target name space to the one the source name space it's taking a while it shouldn't take a while it should be able to we should be able to access we have all the policies in place so let's try and um do take a step back and think why this might be happening this could be because it's not able to disolve the DNS um records Let's test our Theory by getting the IP of the part in the source name space and we will use that IP to reach out to from the part in the targetting space so we are able to do it so it is definitely DNS like always um the reason why it is not working here is that all the core components of the kuties get gets deployed in the cube system that's where whatever the DNS service that you're using like by default it comes with codan you can have your own uh other softwares that you can use um and since we don't have any Ingress or ESS that is uh going out uh from the target name space to the cube system we are not able to access the DNS records so the if you try this out in your kind local cluster you might also see another error saying that could not resolve the host but in this plat form we are getting the timed out error so in case if you're trying it out on your own and if you see that error that is also a valid error that it is much more straightforward so here we are going to create a network policy that's going to allow Ingress and ESS from the target name space to the cube system so let's go ahead and create that one once it's created now we can go ahead and test um again that if we are able to reach out to the source namespace part from the target namespace part so yes we are so in this little exercise that we learned how to apply basic Network policies and we have also seen um how the networking works that in order to Sol DNS it needs to have access to the cube system um so that is lot of something and after this it's going to be Port security yeah so yeah Port security so the idea behind Port Security is to restrict like the most sensitive Fields uh that PO can use and like that could decrease the security posture of your cluster um and also like to enforce the best uh security uh the best practice on on the port so you may be familiar with the port security policies it was actually the admission controller that was REM moved in kubernetes 125 let me just do that skip this one all right um it mostly was removed because uh it was very INF famous for being very uh hard to use and very challenging so it was replaced actually by two things so there is now this thing called Port security standards um you can see about that here uh there are like three uh security standards privileged Baseline and restricted so these things are actually very generic you can Implement that with any uh admission controllers those are just like specification on which field should be allowed or which field should be used on a specific uh pod um the good thing is that the kubernetes organization also implemented an admission controller on themselves which is called the Pod uh security admission that is really easy to use basically you can just like label uh your namespace to uh do three different things oh sorry for the noise all right um You can use it in enforcement so uh every violation will be rejected in audit mode just to see like what would happened and in word mode uh so that users receive a warning directly so we'll go to this uh in this challenge oh sorry all right so first we'll use the Baseline policy in one mode so you can see uh the Baseline thing like host name SPAC are disallowed privilege container are disallowed this is the basic stuff like um many of the specification of like this Baseline security configuration so uh yeah let's do it so we we'll first do with one mode so so first thing we create a namespace called secure namespace right and then what we do is we actually label that namespace the SEC namespace with PO security.

kubernetes doio with one equal Baseline it's labed and now if we want to create a pod that actually uh um violate that Baseline policy so this one will just try to create a privilege pod um what will happen is that the pod should be created but like the user will receive a warning um like that this pod will violate like the post security basine latest uh because of like the security cons. privilege equal true so this is really nice for your uh cluster users to have like some uh actual feedback on what they do before applying the policy for real um again we can see that the Pod is created and that it worked so let's select that pod now all right so now uh for enforcement is as simple as the previous one we just like override the label with the enforce uh equal Baseline label so the namespace is labeled and now if we create exactly the same p with exactly the same security context with privilege equal true what happens is that it gets rejected so this is like the same thing as before but then like any violation actually rejects the action so you can see it's forbidden and if we if we check the Pod running in the space we don't found any resources great so now if we create a pod that satisfies actually the Baseline policy by setting privilege mode uh to force explicitly and we create the P the Pod is created again we can verify right [Music] and then delete the Pod all right so now we can move on to like restricted uh restricted will be uh more restricted as the name say and uh as usual it's the same you just like use one equal restricted and overwrite the label on the namespace we can even like see the label on the namespace um right you can see enforce Baseline and Oneal restricted so in this mode basically you enforce like the basic stuff but you can warn your users on like what it would be if we switch eventually to the restricted mode so now if we create that same pod with the privilege container equal false we could see that the Pod was created but it would violate the restricted uh uh standards restricted profile can check that the Pod is running again and delete the Pod all right uh now there is one other thing uh it's called dry run um because most of the time what happens is that when you cre label a namespace you already have like a few pods running in that namespace so what we will do here is that we will dry run on the server side so on the kubernetes API side and overwrite um the label with like enforce restricted to see what will happen and so if we do that we can see that like existing pods will actually violate that uh that uh restrict sorry some pods in the Nam space would actually violate this uh enforcement so workload will be affected so conclusion like um I think the bo security standards Define uh like very easy to use policies uh for like deploying pods uh the PO security admission make it very easy to actually apply that to any namespace just by labeling a namespace so yeah that is that is pretty Endy and you could actually use these standards with different admission controllers like kerno Opa and things like that those are implemented by the port security admission but yeah did you have have a question I no okay sorry so yeah we're good anybody has a question maybe so I guess we can move on to the next one let's check so the next one is SE comp so I will try to do a bit of explanation before starting so yeah pods are containers containers are regular processes on the Linux we are just using a lot of mechanism to restrict the view of the system and their access to the resources and system Calles are the main interface between the processes and the operating system so they are used to perform like all the basic actions like uh opening a file creating a network socket mounting a file system creating a new process etc etc um containers already restrict this uh the access to the H file system by uh restricting uh the resources like CPU memory by using actually like c groups uh they are restricting the view of um the H by using name spaces and uh some system calls are already like uh uh restricted right but um as you may know like because system calls are the interface between these processes and the operating system uh it it it might happen that they have like a bug and because like the operating system actually controls the hardware like uh if those are subject to bugs it can be eventually exploited for abuse so a very good idea is to actually try to restrict which system calls uh the process in our kubernetes pods containers can call um to restrict the potential attack of phace so how we can do that um a mechanis to to do that is called SE comp it's pretty old in the Linux canel and it has like two mods uh there is like the stre mod which is very restrictive you can only access like the read right and exit system code so you can't really do much with like uh workload with that but there is the filter mode uh the filter mode permit to use something called cbpf so classic BPF not to be confused with ebpf extended BPF right so this is just like very simple filters to allow arbitrary uh system calls so it is often referred as SEC comp BPF um my colleague Duffy and Natalia just gave a talk I think just before about like the difference between SEC comp and BPF but yeah and and the thing is that like writing your own filters might be tricky because you need to know the cises you need to know like which cises are the most sensitive uh all the details about this Cs and everything so the very good thing is that the container run times uh Docker started that but now like on on container the cryo you you can find um like runtime uh defaults so those are like a most more restricted uh filter that you can apply for all your workloads and that is already installed uh on each nodes and that you can use directly so what are the CIS call they restrict in this set so um most like some of the calls are like obsolete CS that nobody use anyway but are just for compatibility so we just like restrict them because we don't really need them some of them are like non-n spaced resource on Old kernels like the date time or carrying like the Kel carrying uh it change on recent canels but like on Old canels it's still like noname space so it kind of breaks like the isolation given by containers and uh the rest of the calls are actually mostly like highly privileged operations like uh uh most of them already guarded by capabilities like mounting creating a new namespace like loading a canel module loading like a BPF program these kind of things so this can be useful for certain workloads but most of your workloads will not use that so a good idea is to use this default profile to um reduce this uh surface so let's do that so here you have a bit of explanation in Secom BF you have a few modes you can use act allow and act know to specify like what is allow or what like cisola explicitly denied depending on how you want to write your filters uh yeah runtime default so this is basically what I explained those are implemented by the container run times because they usually need um to access some of the c c during the creation of the containers so those are specific to the the Run so how to do that it's actually pretty simple so here we just like create a new namespace as usual and we will do two things we will deploy like a normal Pod without any restricted S profile this is just like a normal one right and we will deploy another one that has like a security context uh enforcing like this second profile the runtime default so if we do that it was created as well we can check that they are both running and what you can see if you describe the Pod with the profile we should be able to see that uh uh where it should be somewhere here second profile runtime default so let's try first into the one that is not using the runtime default so if you check uh we can Cil exact into that pod uh with like a shell we run ID we are running as root and we can use like the unshare binary that will use the unshare system code that is actually restricted by the runtime default second profile so if we run it you see that the shell uh changed uh it actually worked we went from like ID route to uh ID nobody which is like uh 65 uh 540 uh 34 sorry so it was successfully executed and if you want to exit you need to exit twice you need to like go out of the shell inside the username space and go out from the cube C pods so now what will happen if we do the same into the one that has like the second profile that is runtime default immediately if we just like try to un user we end up with like operation not not permitted so like the the good thing is that seom just like uh overrided like the return value of that CIS and return minus one in this case so yeah you can read more about like the unshare user common um this is just an example it's actually restricting a lot of other cises like I said like for example like reboot loading BPF program and things like that so in conclusion by following this step you can use like this runtime default that is always like that it's basically effortless you just need to add like this little field in your citus deployment and uh it will actually reduce quite well the attack surface so in case of like vulnerability in the future on some culls if you block them you are basically pretty safe here we are so does anybody has a question on that maybe Ray you want to join all right all right next we'll go over some arback so it's part of the uh security good practice guide to uh the use the concept of lease privilege so make sure that uh you're not freely using Create update patch delete or not granting those rights uh to uh just anyone so you want to make sure you want limit access of create update patch delete so we're actually not going to use those we're going to create a viewer role and we're going to go through the steps on how to create a pod viewer role so we'll use both all four rolls uh cluster rolls R bindings and cluster R bindings on how to do that so we're create a new Nam space because we're going to create a a CL a ro binding that's actually be namespace so let's create a new namespace the rback namespace so once the namespace is created uh just a review roles are namespace cluster rols are cluster wide but you could actually use uh roll bindings for cluster rolls even though you don't have to use cluster roll bindings for cluster rols so we're going to define a set of permissions in our cluster role and it's going to be a we're going to call it the Pod viewer role and we're going to give it uh the rights to get watch and list and once we actually create it and use it we're going to test to make sure it could only do get watch and list as well so once the Pod viewer role or cluster role is created that means this role is available cluster Ro wide we'll use a ro binding now it's namespaced to bind that pod viewer cluster role to user just called Dev comp in the Pod View group so we're going to use uh dual roll binding uh for that we're going to call it view pods as well and that same namespace we're going to use a user we're going to use a a group as well called pod View and we're going to use that cluster rle that we just created called pod viewer and we're going to test this out so we created that Ro bindings now it's namespace that namespace and we're going to test it out we're going to do Cube control and you can do the-- as command for a user or do the D- as- group uh option as well for to use to use a group so we could test out uh our back here so we're going to use the Pod View group and the dev comp user uh try to do a get pods on the same namespace so we should we should be able to do this and we and we can we don't have any running pods right now so we're going to do uh different tests we're going to use the same group uh same user but on a different namespace the cube system namespace we're going to try to use uh do a get pods with the Pod view uh group and depc user uh on the cube system names space so we expect this not to work and it is not working so we get an error uh forbidden so now let's go check to see the same do the similar same test if we could get pods in the default namespace so we're going to use a the Pod View group use uh the dev comp user and see if we get pods on the default namespace and we don't because we use that uh that role binding which is namespace even though we use that cluster roll uh which is uh cluster wide it is still uh restricted to that specific Nam space so let's make sure that this user cannot actually create a pod remember this is a pod viewer user uh so this this user should only be able to uh to to watch and get and list pods so should not be able to create a pod so we will use the C control run command that will immediately it's the imperative command to create a pod so Cube control run uh the name of the Pod and the image and with the namespace and with the options the D- as as a user the def comp user D- as- group the Pod View group so we should actually we should expect this to not to run and we actually get the error that it is forbidden we actually tells you the user cannot create the resource pods in this Nam space so great it's working so let's create a Creator role let's say so someone actually does need to create a pod so now let's create a role now this is the namespace the Pod Creator role uh we give it this is for both pods and deployment so uh and we give it uh the get what list watch create an update uh permissions so let's create this uh role which is namespace to the rback namespace so once that uh Ro is created uh like you know just to review this rooll is actually doesn't actually work unless we have some kind of binding a roll binding or cluster Ro binding uh so we're going to do the create the role binding that binds a pod creat a role to the the same uh user uh but to a different group so once that Ro binding uh so let's uh test this as well so let's create a pod now in this namespace we're using the same user but as as a different group and this is just restricted to users and groups it could also be applied to service accounts which we'll go over uh last or next module so this pod is created now we have uh the ability to create a pod in this namespace and just some quick uh helpful command comms as well uh Cube control off Ki is one of my favorite commands uh it tells you explicitly if you can actually do certain things like Cube control off can I get pods in a specific namespace add a user adds a group um and we'll take a look and it's simple yes or no so I can actually get pods as this user devcom as a group pod view now we'll see if we could actually delete pods in this namespace uh using that same user using that same group using Cube control off can I delete pods and this namespace and we have an explicit no so yes you could iterate through all the different uh commands with Cube control with C control aleni but you could actually list it out so U so Cube control off can I list D- list we could actually list out what we can or can't do so one of my favorite commands to use to see if and if my if the user in question uh can or cannot uh do certain task to summarize uh make sure to use a principal leas privilege as uh uh throughout your kubernetes cluster uh make sure to not uh to explicitly only you create roles and cluster roles with the create and update and delete um command or WR uh only for specific groups or users or service accounts that actually can do those actions all right next one is going to be pretty quick it's about uh service accounts uh I'll just go through the concepts of the service account and so we want to use service accounts so uh service accounts are used to provide identity for your workloads so your workloads have identity in a cluster they can actually do certain actions so they could uh Reach the the kubernetes API server uh and they might be able need to to do certain actions so they might so uh think of as instance kubernetes is uh we use kubernetes with many many microservices that have to talk to each other so uh so certain microservers need certain information from let's say the cube API server or from uh other uh pods in certain namespaces or other microservices so we could give the same or similar um rights to this to the servers accounts uh servers accounts so should be used and so in this uh scenario or this uh checklist security checklist item is to make sure that your Cube control controller manager is running with the used service account credentials enabled so this is a fairly fast uh simple module just to make sure that service counts are actually used by default and uh taking a look to see if a service count is being used so uh let's say for since we do have a cluster up and running already uh let's take a look at where the cube controller uh manager is and how we can actually take a look uh if you're using QBE ADM uh to uh to install your kubernetes cluster it would normally be in a file called Etc kubernetes Cube controller manager.

in your control plane node uh in a kind cluster and in many other distributions of kubernetes you may not have access to that yaml file uh so we need to explore uh through the CBE controller manager pod so this lab or this module goes through on how to check and how to explore through that Cube controller manager pod and just to verify that service counts are being used so the cube controller pod should live in the cube system namespace so we'll uh do Cube control uh get pod in that Cube system namespace for that Cube controller uh manager pod and in this case we're using kind it's named a little bit different and it is running we have one pod running now like I mentioned other distributions this pod can be called something else so it's typically uh uh it's typically something with cpon controller manager but uh this is we do have a query here using uh JQ uh we could do c control get pods and we could pipe it to JQ uh to actually take a look to see if there is any pod with the name uh Q controller manager it might have a different uh prefix or suffix but this this is a quick oneliner just to like take a look to see if there is any pods uh with Cube controller manager and yes we do and it's called CBE controller manager kind Dash control plane so now we should check what options are set um with the key controller manager and uh a lot of this exercise as well is also can be helpful to use uh if you have any feature Gates you want to use or see if any feature gates are uh actually enabled or not enabled we would go through the same steps but with the cube API server pod as well uh since you may not have access to the yam manifest of the QB API certified uh you need to inspect that Cube API server pod just like how we're inspecting the cube controller manager pod so we're going to take a look uh in this Cube controller manager pod just to see what options are Set uh in the cube controller manager pod so we're going to uh out output uh this into yaml uh it's great we could it's a lot of it's a lot of yaml to digest uh and we could parse this manually of course to look through all the options uh and we see options here and of course we're going to go through easier way or another uh U I like making these fun queries on liners to make it easy to actually uh to to look for this option so C controller manager is the command and we have many options that are used uh in uh for this C controller manager and this keep controller manager we could just par it check to see if the Das use service accounts credentials and it is uh set to true so that's great we're using service accounts uh so we do satisfy that security checklist item and like I said we have a quick oneliner uh to just to check to see what those options are uh in the command keep controller manager the container it's in also the options so we get pared for this used service accounts uh right here credentials is true so let's to actually check to make sure how service accounts are actually if it's being used uh as well so let's create a a namespace uh called service- accounts so there's a brand new namespace we haven't done anything to it we're going to uh get a service account like I mentioned we just created this service account is there a service a default service accounts yes there is we it was created 9 seconds ago when we created this namespace so let's see if we actually run a pod in this namespace uh let's run an enginex pod here we'll do the impera of command uh we just imply just the namespace not anything else and we could retrieve the Pod info and of course there's a lot of yaml here but if you take a look to see uh we could actually take a look to see there is a a field here for service accounts so it's inpec do containers and service accounts that's are conditions speec do containers and service accounts where is that here default and you could actually set this to any service account that you create if you don't set it uh a service count it will use that default service count so it's best to actually create service counts in advance with the right permissions uh as you did with roles and Ro bindings and cluster roles and cluster Ro bindings and use those service accounts to make sure that the application that you're using has the right permissions uh to do whatever it's necessary for that application and so just to summarize uh there is a default uh service account uh that is used by pods if you don't spec specify and we could actually access the kuber API with that default service account as well uh do you need any other permissions uh you would need to create a a new uh service account for that pod or if you want to restrict permissions which is even better uh you would want to create a service account so most folks would create service count to restrict permissions and only Grant uh the permission they want for that workload all right that's it for this module any questions all right the next thing that we going to see is about certificate experation this is an educational uh section um in this it says the checklist says the intermediate and the leave certificates have an expiry date no more than three years in the future so if you have used QB adum or kind to provision your cluster you get to leave the intermediate certificates issued for about an year and if you look at the CSS on the other hand they are issued for a default of 10 years the main reason is it's really um uh hard to rotate those certificates so they are issued for 10 years and uh whenever a cluster is updated using Q QB ADM it automatically rotates the certificates for you and if you have used some manage service managed communities providers the certificates main the ca certificates could be there for like the validity would be for like 30 years or so but at least the intermediate and leave certificates the validity would always be one year um so let's go ahead and check in this um section you're going to see where the certificates are stored and how we can check the validity so generally the when whenever a cluster is provision using QB ADM tool the certificates stored in Etsy kues pki and uh the use certificates would be in ETC kues and um in the section um the the cube adum as a nice little utility built into it you can just check Cub adum s it's check expiration it's going to spit out uh list of of certificates the expiration uh date printed next door work but since our uh environment is provision using kind we are going to see how we can um we can list the certificates available in our control plane and how we can U check the validity first we going to see the notes um that are running and we have like two worker nodes in one control plane control plane is the one that's going to have the certificate stored we going to um the the the ones the API the server the client certificate stored so we're going to go look into it more it will also have the hcd certificate stored and um since these are deployed the nodes are deployed as Docker containers let's just check what is running here so you can see there is a control plane here a kind kind worker and um another kind worker so let's just do this much more cleaner um so you can see like three of three containers corresponds to the three notes in the client cluster for us so we're going to exit into the control plane and then we are going to just list this directory to see what are the certificates available and you can see like there is this API server s API server key and then there's going to be like a caer ca key and so on the ET CD s it's going to be stored in this atcd directory right here we'll get to it um in a while so here um so you can see the each certificate has um its own purpose the API server HD client is used authenticate to hcd and so on and let's just exit into the part and we going to fetch the API server at client s and just going to cut it out so you can see a bunch of things printed here um thir data but we don't get to see the validity from here so for that we going to use an um open SSL tool and we are just going to uh list the start and um we going to make it list the start and end date for us and there are like three options um for it not before not after and then like the start date end date and the um let me just quickly see start dat end date and the no out option no out is just not going to print the certificate for us so if we go ahead and do that here you can just see um that there is the certificate that just got issued when we started this lab today and this one expires right in like one year so it is as a validity of one year if you go and check um the certificates you would be able to see um all of them will have a validity of one here let's go and check the one for the API server cuet client s um so it's the same so there's bunch of similar things listed out here and can try it out and the ETD certificates so ETD uses certificates for all kinds of communications between the HD members between the server client and between um also for authenticating the request client request so it is actually located in the ETC communi pki pki ETD folder let's go ahead just list those certificates out okay okay yeah so we can see a bunch of um the issuing the CSR tier and then like pier and the server here if we go and C the um healthcheck client certificate and check the validity for it so we can see that it is listed for about in here the validity is about in here so this is mostly educational so if you are already using any tool it will by default provision the leave an intermediary for about one year this just to see how you can um check um on your own if you're interested and um that's it in this um yeah do you want to add as so I've I've I I've worked with many end users and I've have seen many clusters have expired Sears and that stops the functionality of kubernets uh so you should be aware of when your searchs expire uh and as to be to mention when you upgrade kubernetes it'll rotate your searchs as well so uh folks should also upgrade kubernetes corlink there's uh lots of updates and CV patches uh with kubernetes so when they do upgrade kubernetes uh does rotate their the Sears as well and also that we have three releases per year and at any given point of time only like uh three versions are supported three right the current and the uh three versions are supported so it is almost like an year so if you are keeping up with your upgrade cycle you're automatically rotating your certificates and if you're using the manage service provider they take care of that for you when you upgrade your cluster there was also an interesting discussion on um one of the um Cub adum that I saw like someone had asked for the extension of Cort for like 100 years or so you know but um you can have like if you are going to have 100 100 Years of expiration for a CER but you're not going to upgrade your communities you're going to run into a lot of uh security issues and there would be no patches for the versions that are not supported anymore um yeah that is a little thing that I learned today um any questions on this okay moving on all right so the last module is on uh pod placements so uh when you do have applications running pods uh and if they're sensitive uh applications and you want to isolate them on nodes or specific nodes uh we can use taints on those nodes uh so that'll prevent any new pods or running on those nodes and we could use and if you do want uh those sensitive applications running on those isolated nodes on those tainted nodes you use a toleration and that Toleration should match the taint in order for the application uh to run on that isolated node or that tainted node uh and we'll also look into how we place pods uh using the node selector so in case if you have uh pods that need to run on spef on specific nodes with uh with access to to spec to specific Hardware like gpus uh you could use uh node selector or maybe a node has a certain security guard wheel set up for that node you could use a node selector so since AI is big in kubernetes right now there is a feature called the node feature Discovery it's uh external to it's external to the kubernetes project but uh folks use the node feature Discovery to see and find what Hardware is attached to those nodes and the node feature Discovery applies a label on those nodes and pods that need access to to that Hardware can use a node selector uh on that node that node feature Discovery applied to that node and also another uh use case or to use to make sure that we're using pod placement uh is is still on the principle of zero trust uh to make sure that we always assume that there will be a breach uh and to minimize the blast radius uh so we want to make sure uh if you if there is a breach in your in your in a container you want to make sure it's not running alongside on the same node that is running on the control plane of your kubernetes cluster so we'll look at node selectors taints and tolerations in this module so let's take a look uh at our cluster itself so we have three nodes one control plane and two worker nodes as we expect we're going to create a new uh namespace to uh create our pods in the Pod placement Nam space uh we'll create a Damon Set uh so Damon set is used in cases where we want a pod a to run on every node in the cluster so this is used for like log collection storage or monitoring so we'll create a Damon set here just using engine x uh and we're just this is the PO template so this is just pretty basic just using engine X and exposing Port 80 so we have a Damon Set uh created so uh what do we expect we should uh expect this pod or a pod a copy of the Pod from the stam set to run on uh every node in the cluster so let's take a look of course uh we only see them running on the worker nodes so we see two pods running uh in the cluster and we could take a look at why of course and like I mentioned taints allow a node to repel pods uh and while the Toleration allows those pods to uh to be scheduled with those matching taints uh and we could imperi uh do use a command Cube control taint to actually taint uh a a node and it there's a format of key equals value it does not need a value and the effect and we'll take a look at what is used and what the possible effects uh are so we're going to just use a cube control command here to cube control get nodes using custom columns uh to see uh there is a A taints in the in the spec of the nodes so we could take a look at them uh see what the effects are uh and see what the taint key is and if there's value there is no no value for this one so I didn't add it in here so we do see the taint on the control plane node uh and so that's why the Damon set was not is not running on the control plane node so the taint is uh no schedule uh so no schedule means that's not going to affect any Runing nodes currently when that taint was affected on that on that node uh and it's going to uh the taint uses the key node roll uh kubernetes doio control Plan and there's other three total effects as well uh so the no executes if this was no executes and that means if there were any running pods in this uh in this node then it will actually evict those running pods unless they had a matching Toleration uh so in this one with no schedule it does in fact running pods only uh new pods and prefer not uh no schedule meaning that there's a preference so this just a soft version of no schedule uh so there's no other place to place the Pod the Pod it will actually place it there and kind as we see here default suain the control plane node uh Cube ADM does the same as well so if you start a QBE ADM cluster from Upstream it will also Tain the control plane node as well because we don't want uh any application uh to be running on the same uh know that our control plane is running in case there is a container Escape uh from the application and they have access to the kubernetes control plane components so we're going to delete uh this Damon set here uh and let's actually use um uh let's actually deploy a dam set that actually tolerates the taint on the control plan now this is typically a big no no but this is just to uh uh to uh to to show how we can use a toleration uh on this Damon set or in this pod for this Damon set here so we see that here in this in the spec field here under tolerations uh it matches that same key that we see in the tank key here uh no-roll do kubernetes do icontrol plane uh and it just you want to make sure it just exists and the effect is mat has to match as well no schedule so let's take a look to see if this St set is actually running now on all three nodes and since we have the Toleration we do expect it and we do see it uh running in all three nodes here so let's delete that D sets uh so now because we're not supposed to be running applications um other applications on the control plan node so now we're going to look into a a node selector uh and we could use node selector to make sure pods are placed on specific nodes or group of nodes since we're using labels and labels are a way to identify and group things like nodes or pods or other kues objects here so we look at uh node labels since the labels the the node selector uses labels we'll see the existing labels uh on the nodes here uh and labels are often used for we could identify the architecture uh we could do the the type of of os as well we see Linux here uh the host name as well and there are other things like node feature discovery that will actually add labels to tell uh to tell the user or the cluster what Hardware is accessible on that node as well so now we're going to add a label uh two labels here we're going to label one of the workers uh security equals low because the label is a key value pair so we're going to say the first worker node it has low security I'm going to say the the second node uh the second worker node has high security and the labels do not prevent pods from being scheduled on noes it's just a it just used to identify and group nodes so let's just confirm the labels are created with uh Cube control get nodes D- show- labels and we see those labels here in the very bottom and we're going to assign let's say a pod that we deem high security so it should be running on a node that has certain guard wh uh that we deem has high security as well so here we have a pod manifest here uh and we have a node selector uh which and which under node selector has a key value pair SEC security equals high so with this with node selector here we expect it to run uh only on the second worker node where we have the matching label uh security equals high and so let's uh since that pod is created let's go take a look and get uh check where that pod is placed it looks like it's correct here it's on worker node 2 here so we do expect uh where we do have that pod running here uh and let me see here did that check and we're going to do a little clean up here uh can we'll delete the Pod and delete the the Nam space as well and just to summarize here um always have the concept of zero trust uh uh whenever you are creating pods or you're placing pods you want to minimize that blast radius uh always assume that there will be a a container Escape uh so you want to make sure that the uh the high highly sensitive applications are are either isolated on nodes or or other applic or other pods are not scheduled on the same node as those a highly sensitive application so all right uh so thank you everyone for joining I hope you learn a few things following this workshop and uh I wanted to say a little thanks to isent for providing the instruct like infrastructure and uh again like the the workshop will not you will not be able to like retake this Workshop specifically on instruct but you can use the repository that is like at the bottom of the little uh page we distributed to replay those uh things locally on your cler so yeah the repository is not public so thank you very much for your time thank you for attending