I think it's time so let's get started thank you for all being here today's session is on distributed cash and PR a cloud on kubernetes crust we' buil a distributed Cash System on kubernetes and use it for a cloud in our own premise we've uploaded our slide so if you need please check it he's a u from Japan he works for preer networks which is a AI ml company in Japan I'll explain our company's cust in detail later I'm T which is colleagues also I'm a founder of Yi sh shf SBX project and the Mainer of some OS related to cncf this is today's agenda so we first introduce background Ai and the M workload on kubernetes cruster second we talked about the main topic simple cast services that is developed for AI workloads to solve the problem that we talk about the background start we describe the Lear world use cases next in deoy consideration we describe techniques to achieve higher performance in the end we conclude the attack okay next which explains our background yeah thank you uh first we start the talk from artificial intelligence and machine learning workloads so let's consider the training or machine learning model that recognize a numerical digit we have a data set of pictures with numbers and the training job of deep newer networks here it is a correspond to a two commity spots so KU spots will access the data set and get the data samples and F the Deep new networks with the data samples so it will optimize the model then by continuing the iterations the Deep new network will become to be able to recognize the numbers here I'd like to look at the strategies to store the data set so we are Ai and ml company with onpl cluster so we need on premise solutions for that first one is a NFS so we can mount NFS storet to KU spots by using a host bu volume NFS is fast if the NFS server has a fast red drives fast CPUs uh yeah so if the number of client is limited the performance is acceptable however the large number of client access the NFS you can easily notice the performance slowdowns second one is object stretch we have on premise S3 compatible object stretch with open source software apure zones we are using hard drives as a back end so by adding a new hard drives we can scale the performance and its capacity actually the performance of hardk drives are not sufficient to perform a random deed of data set so it cannot be a solutions for the data set Rolling S is the no loal stres so our compute node with AI accelerators has additional nbme drives so it is designed to use as FM volumes of AI and ml workloads so the problem here is the workload is moves to different compute note the data will be unreachable so for example we have two compute nodes A and B so firstly the data set has scheduled to a compute node a the workload CES the data set to the no loal strates after that workload may be preempted so fortunately we have another compute node B so the workload will be moved to the another node we have a cach data in compute node a but we cannot access there without any service in compute node a so we have to fetch the data set again it is very time consuming so therefore no loal Str itself helps the training but cannot be a final Str solutions for that also if the data uh if the training requires a larger data set than its capacity we cannot use it as a cash storage so it is not scalable this is our grand design of uh storages for AI and ml workload we are going to combine a object St and non local stres to construct a hierarchical St systems the port will access the crowd of nbme drives first and if the data exists in the cloud we can skip the access to the object stres if the data does not exist in the cloud we have to access the object stres the capacity of the stres is scalable by adding new hard drives to object stres and the throughput is scalable by adding a new compute noes or adding a new nbm drives therefore I think it is very suitable solutions for AI and ml workloads so in this session we'd like to introduce our hierarchical Str Solutions on the top of uh Cloud native Technologies so developing stage Solutions seems difficult but utilizing kuus features like discoveries and authentications and Eno features like its Road baning Technologies and its flexibility uh we have developed the strategies very easily next uh we'll introduce our simple cast Services SS SS is a simple and first and Shing architecture since SCS has a very basic HTTP G on P interface and just DET no no local FES when you f the data it's incredible simple and first also SCS is designed for cloud native do SCS on kubernetes CL and use a lot of kubernetes feature to reduce our own implementation also SCS adapts share ning architecture is essential because this enables SCS to be scalable another important thing to note is that SCS is a just cash services not persistent stret in other words there is no need to keep the data forever and it's acceptable to delete the cash dat this is how you can actually use SC using a car command it's easiest isn't it the first C St the cash data uper the J in SCS and the second one with the data in first call stored okay let's take a closer look first look at HTP method first car uses a put to store the data and the second one uses a get to fetch it this is a natural next is a URL the first thing the first thing you notice is that is use HTTP and cuent services right and if you look at the PA of URL it's specify the bucket and the object in this example the bucket is project fber and the object is Apple okay next is authentication part we'll discuss this later uh please keep in mind uh that o header is required in this part we reduce our own implementation by making good use of the bound service account token this is the future of kubernetes let's look at the path of these C commands this is a diagram showing how users access the SCS users part do something similar to the carard comma and we looked SCS has the actual data of cash as a file as mentioned before the action that SGS application does is just return this file the first thing you notice is that there are kubernetes services and Eno between users PS and SCS before we go into more detail let's take a closer look at SCS itself s SS is made of three main components first is a go language application and then NM or other storage that's hold the cach data and SQ write which is hold the metadata like access time stamp or something like that each SS application has its own SQ right in this example there are for SQ light because there for SS application of course each SCS is learning as a p in our cluster they are split it out using demon set back to the diagram okay one of the most significant feature is the share n architecture it means that nothing like ldb is shared between each SS SP despite this users can access the cach data from any network Zone in order to achieve this ler seven load balancing uses a consistent Hing okay next this is a for of alization in SGS it's similar to kuet outb proxy if you have used it you can understand it this Smo first a user P should Mount the service account token and send out a request with it the verifying part it is SCS is charg of it is is a token Lev AP to verify the token the service account can tell as the name space of the request Source this can be used for authentication therefore users can specify the name space that can access the buet well let's define your bucket you can define a bucket like this there are two types of a bucket public and private the top orange part is a bucket uh called the public buckets which anyone can access the blue part below is called private bucket and that is authenticated based on kubernetes name space it's possible to list the name spaces that allowed to access the buet for instance project kubernetes and use of TAMU name spaces are allowed to access the cubicon bracket it's also possible to set storage limits for each bucket in the bucket quarter field if the specified capacity is exceeded the bucket Will T to delete cash data on L basis this figure shows how data is deleted in the UE firstly the color so each color represent represents a bucket they are loaded with more and more data with sifting the start time the do CLE is the maximum capacity allocated for each bucket once there each bucket has no further increas in capacity this means that L based data deletion had started here next we talk about the lar world use cases we'll introduce two use cases one use case is used SCS as a cach for slow the motivation of developing SCS has started from here it accelerates data loading through put of data set the Second Use case is SCS as a storage backround for yet another cash service let's dive into case one first let's consider the data loading from object storage like S3 or or something like this in order to select the iol layers we developed the PF for our research researches since it's OSS you can check it out now the source code in Python is a sample code to get a JP file from object stage with only 10 lines of code also PF has a transparent cach feature just adding HTP cach parameter to form URL function pile automatically catch the content and get it from SGS next time if the file is hit in SCS the lead latency improved because we could skip getting from object stretch and uh put it into SGS next let's look at case two how to use SGS as a storage backend for yet another cash servic there are several lar files we haven't mentioned yet first is a container image container images for AI and M Rock CL are larger and larger our only Mar container image that the first choice for our researchers uh is more than 30 gab we have already build it and uh last week 94% of continent mes PS hit the SCS the other the model you know llm is large and sometimes our researchers evaluate public llm in a hugging phase so we have its Capac cash uity both FES both files have characteristic like being AAR large and hot normally we use the latest contenter image so the outdated contenter image should be deleted this get me done by SS the L policy therefore the combination of SCS and the cash service for these files works very well this is an example of implementing yet on Cache of course SCS is used as a storage backround yet another cach must be implemented several features like access to origin service access to SGS user interface URS mapping from origin key to USS however storage management like cash eviction and the capacity control which is difficult to implement is already implemented in SCS so lighting yet another cash is easier so in this sections we are going to talk about the deployment of simple cash service here we have two questions to optimize the deployment one is how can we optimize the network traffic between user ports and enoy we are using kubernetes service to discover the enoy port here we consider the configurations of kubernetes service and the deployment of enroy the other is how can we configure enroy to rot the traffic to the SCS by considering the two questions we can optimize the end to endend data flow from user port to the SCS ports okay so let's start from the question one so how can we optimize the network traffic so first I'd like to describe our Computing infrastructure where the SCS is running right now so we are pround networks uh Ai and machine Learning Company so we are developing machine learning learning models like large language models and many solutions to the industries so these activities uses our on premise Computing infrastructures so we have three kues clusters for Productions and several evaluations and staging clusters as a total there are more than 400 kubernetes notes with are 30,000 CPU course uh 300 tab memory and also a 2,000 gpus for machine learning models training so also we are developing and operating our own a accelerator chip called MN core so we are doing almost everything of AI accelerators from rtls uh both designs server designs uh drivers and also device plugins for kuties and also a graph compilers so I'd like to introduce the data center Network also so our company freefile networks uses a cross Network topology cross Network topology uses multi stage switches uh deep switches spine switches and external super spine switches this switch is the most nearest switch from the compute node so here we can define a four Network zones for each the swiches uh Network Zone A B G and D's so because uh there are three nodes in the same d s can communicate with a nonblocking performance however the communication between different zones like Zone a and Zone D requires diff and spine switches Communications so Network dink between de stitches and spine stitches is over subscribed in other was the up link is narrower than down links so if the old note in your uh Network Zone try communicating to a different zones congestion happens therefore we have to avoid the interzone communications as much as possible to save up links okay so I have described the background so let's consider the network graphic from user port to enoy port first as assumptions SCS is deploy to the all comput no to use all local nbm drives effectively also user Port may be scheduled to any no because all compute no has a expensive accelerators so there is no meaning of keeping the computer resource idle so where to deploy enoys here we decided to deploy enoy to all compute nodes by doing so we can deduce the network traffic between a user port and enoy Port however we cannot optimize the inter Zone traffic uh between enoy and SCS because SCS has deployed to all computer noes so we cannot optimize about the interone traffic so next I'd like to consider the configurations of kubernetes service to reduce inter Zone traffic in other words uh performer Communications in a topoi a waye we have a user port and enoy port in all computer nodes so there are two method to deduce the interone traffic one is a intern traic policy and the other is a topology a rooting so let's start talk from internal traffic policies internal traffic policy limits the communication to the service internally so if the service have the service have many enoy ports as back end but the internal traffic policy only Lo the traffic to the port within the same node so so we don't have communications between computer nodes topology routin def from internal traffic polies to routing Lo the traffic to the same zone so in this case we have a three enoy port to lot the traffic now it requires communication in the same Zone but not in the different zones therefore it seems internal traffic policy is better than topos rooting from the perspective of uh networking however the Viewpoint of Eno CPU road bance we have a different conclusions internal traffic policy doesn't root the traffic to the other two ports so therefore if some not uses SCS heavily uh we can see the high CPU usage of uh enoy in the node since we dis deployed enoy as a demo set so we cannot increase a CPU resource request with A fine grain manner however topology hour routin utilize three Eno back end so the CPU L balance is deluxed therefore we can see more consistent latency numbers in ouro a rooting therefore we are using Topo routing to improve the CPU Road urance of enoy that is a good balance of network traffic and road balancing so next question is how can we configure the enoy to Lo the traffic so we'll consider the rest part of data flow so here let's consider the load braning of keys so in SCS the key correspond to a bucket and object so in this design we want to rot the traffic from Eno to SCS consistently so consistent means when we put the object to a first SCS we want to get it from the same SCS otherwise we cannot get the object we have put it before the easiest way to achieve that is a introduce a mapping from backet and object to SCS backend ID it introduces a share DB so sometimes share DB performance slows down so here consider sharing yeah yeah blah blah yeah this can be a Solutions but we want to use more simple and more scalable way so here we don't share the database but the shares the functions to determine which is responsible for we introduces the hash functions from bucket and object to some numbers then we decide the Les responsible back end from the number the simplest calculation to determine the responsible back end is just divide the hash value by the number of back ends then use the ER the problem of that is that when the number of backend changes almost every key Dem Maps so typically it happens during node failure and node installations so we should avoid this consistent hushing is a method which solves this problem it designed to uh deduce such a depping so we can expect more lower de mapping uh so the key count divided by the back end count is the IDE numbers emboy has two consistent hashing implementations one is ding hash first It prepares a ding with a responsible back end by hashing the back end information when access happens it calculates the hash of keys then search responsible backend from the ding the other is MRE which manages a set of responsible hashes both mappings are computed from the backend information so all enoy part share this mapping therefore we can see the consistent mapping in all enoy ports load balancing of keys is very important the back end with a larger key responsibility how have several problems so in this figure you can see the length of AR or back and three is 1.5 times longer than back end four therefore backend 3 is 1.5 times of responsibility of a bu and for so it should be avoided because it affects the performance so CPU usage of backend 3 is 1.5 times of back and forth so it may deserve the longer lates in numbers also the lifetime of each data in back and three is shorter than back and force so more possibility of delions in back and three nod here we want to see the consistent resource usage and its lifetime uh we need a consistent Hing with a consistent size of key mappings so let's check the load imbalance of both consistent hashing algorithms in this figure a ding hash has a load imbalance up to 1.5 times however MB doesn't introduce any load imbalance so let's see the screenshot of object count per node the screenshot is taken from our production environment each line correspond to a number of object per node so as you can see when using ding has you can see the multiple lines with some whe however when using a MAG you can see only the single lines but actually there are multiple lines so it means uh load balance on mag algorithm is perfect that is why we are using magb okay so we have deoy the SC to the production environment so let's check the performance numbers this is our API course per second in the last 30 days as you can see uh the highest is 37,000 of De per second also uh this is aggregate throughput per second in the last 30 days as you can see the highest is a 75 GB per second as aggregated through uh traffic so as a summary we achieved 37,000 request per second with 75 gab per second with only 55 backend servers I think it is not so much number of backend servers also this number is comes from the de world not the synthetic Benchmark So currently we are having 268 million object almost deest to SCS is a de heavy and uh we are continuously watching the not found rate to expand the cash capacity in a timely manner so let's conclude the talk so we have introduced our cash Service uh simple cash Services it is designed to with a share nursing architecture to see the linear scating this is easily implemented by invo consistent Hing algorithms also we are following uh Cloud native best practice like a bound service account token from kubernetes to implement authentication easily so our system can be used as a transparent cash of object stretch with pfio our iio obstruction libraries we are using SCS in real world like data set loading and large model servings so we use several optimization techniques like Topo routing and mrebs so our project is supported by Cloud native Technologies and our three internship members so that's all so thank you for paying [Applause] attentions if you have any feedback please uh scan please scan it and uh anyone can any questions out of curiosity is is there a reason why you didn't go with a cluster file system we've been doing X scale storage for a long time and it's not it's much more scalable than uh NFS is I've got 80 pedabytes of storage back at my cluster today and I have over a th000 notes which you know 120,000 cores so I'm curious what why you ruled that out thank you sorry sorry come again I'm curious why you didn't use a cluster file system not NFS something like gpfs or luster or WCA or whatnot there's probably a reason I'm curious why you have not you decide not to I think just using AUST some as dpfs like a shared file system can be a Solutions but uh it's a I think actually I have not tried such as shared file systems but it have some difficulties to operate such a large St class but by applying Crown native Technologies to have a our own kuet cluster uh we can uh get our strategies more easier I we we I we think that is more easier uh to use such this solutions thank you other questions okay okay that's all thank you for all being here