Okay. Hello everyone. Good afternoon. I hope you all are on your second set of coffees and that it wouldn't be too boring and we'll make this fun. I know it's 3 p.m. But with that said, I am Kosha Mahersi and I am a senior software engineer at Bloomberg. Today I'll be talking about how we used distributed trace to generate end-to-end latency metrics at scale. And before I really get into the nitty-gritty of it all, I want to kick us off with a little story time. So I work on the telemetry infrastructure team, which means that our job is to provide observability and monitoring solutions to the rest of the firm through telemetry data. And that means that our clients are other engineering teams. So these teams are building and maintaining trading systems and messaging systems, news infrastructure, middleware, so on and so forth. And with all of these products, they must handle large volumes of super high stakes data with very low latency. And especially in the financial sphere, um just even milliseconds of delay or latency can equate millions of dollars lost.

And that is why engineers must make sure that they prevent latency from creeping in to these systems. And so they have to monitor the complex distributed systems that actually back the userfacing products. And as a result of that, we saw a recurring request pop up which was that teams and engineers wanted latency metrics for end toend workflows. So for example like let's say we have something super simple like this where you've got your users making some requests that hit service A service A talks to service B which talks to service C and then some users receive a response right in that case engineers want to know well how long does it take for requests to go from A to C and that's your end to end workflow so previously what would happen is either teams would use some amalgamation of like servicewide metrics So your application level metrics or they would have some custom end-to-end uh tracking system. So like maybe think passing around a session ID or a request ID some sort of state management that can let you kind of put together requests across different services.

But then obviously with the increasing adoption of if you didn't already guess it distributed tracing we decided to adopt that as the unified centralized solution for doing this. And very quickly as our team decided to build this out we realized that I will actually take a quick pause to do a quick interlude for what is distributed tracing. I know a lot of this crowd might be familiar with it but if you're not just going to highlight some salient features which is in this case you see you've got let's say someone making an API request and then a service parses the request and then you know make some calls to a database to read some data and return something to the user right in this case every single little hop in this path is a span which is uniquely identified by a span ID and the entire path itself is a trace case uniquely identified by a trace ID. And then you will also see that there's this timeline up top. That is because every span holds timing information for when a certain operation or service call started and ended.

And we are here to talk about latency metrics. So these timestamps will be key for what we talk about later. And finally, each span also holds references to its parent spans. That is to say that a call call service will know who the caller was and that's what you see here in these yellow arrows. So that's a super quick overview of distributed tracing which of course is part of the open telemetry standard and this standard allows us to visualize the path that requests take through complex distributed architectures. Okay. So going back to my story um we decided to build that you know we want to build this solution but very quickly we learned that our firm's trace data was rich with very complex parent child relationships between spans with a lot of longunning spans and traces and of course was at a high volume of 50 billion and counting um spans a day. And we also learned that due to the complexity and scale of this data, open source or vendor solutions would not quite work for this end-to-end metric generation.

And so we decided to build a homegrown solution. And that's what we're going to talk about today. So before I get into the technical details, I'll do a quick agenda overview for the rest of the presentation. I'm going to start by sharing an example workflow from the real world. We will then look at what the corresponding trace for such a workflow looks like and then we will see what it takes to get the enter and latency from just one trace. We will extrapolate that to scale and we will dig into two specific approaches for how to do that at scale and may kind of summarize the things that worked for us and did not work for us and then finally I'll end the talk and we'll have time for Q&A. Okay. So with that said, let me introduce an example end to end workflow. This is from the finance world and it represents a very contrived generalized version of what an equity order execution system looks like. If you're not familiar with financy things, I'm right there with you. I'm an infrastructure engineer, so this is also me doing my best.

But essentially in the system you can think of clients being traders and think of people like buying let's say an Apple or a Microsoft stock. So we've got these orders coming in from our different clients, right? These orders go through what's called brokers. Brokers generate something called a fill message. Fill message essentially as the name suggests is um storing information about how much of an order was fulfilled. Then that goes through an order management system which does the things an order management system does. And then from there those messages messages are put on to a Kafka queue. And then we've got this publisher service which is going to pull messages in batches off this queue and publish them out for users to see. And the end result is traders being able to see the status of their orders on a UI. Now, if there is a delay in viewing the status of your orders, then traders can quite literally lose monetary opportunities. And that is exactly why we need to get end to end latency metrics to have this end toend latency monitoring so we can ensure a good user experience and prevent these delays.

Now to kind of drive that hope drive that point home a little bit more um we want to be able to track the requests going through complex distributed architectures that back these userfacing products and end to-end metrics allow us to do that. uh we can then also alert on any spikes in latency that we see which then allows engineers to debug quickly before there is any vis measurable visible client impact. Finally, we can also build service level objectives on top of these metrics and an SLO essentially allows us to set a measurable target for reliability and performance. So we can say things like hey 99.9% of the requests should finish within one second and then such uh such a target can be tracked by engineers managers executives alike to understand how your system is doing is your system healthy and then finally these end to-end lency metrics also help in the design and development phase for things like AB testing. So with that said, I will come back to the example I just introduced and I want to I want to zoom into a specific subworkflow and I will use this throughout the rest of the presentation.

Here the stuff that you see circled in red is us going from messages into the Kafka queue pulled out by the publisher service and then pushed out to subscribers. So this is what the distributed trace corresponding to such a workflow could look like, right? Uh I've got three main points in this workflow. You've got your consume cued messages in batches, process them, and publish them out to clients. Now, if you take a look at this top trace right here, you will notice these spans. You'll notice consume, process, a couple publish spans for the a couple different subscribers. You will also note something interesting here which is that I've got this yellow arrow pointing to two different trace ids. These are this trace and that trace. What's that? So note that we are pulling messages from this Kafka queue which are representing some user requests all together in batches by the publisher service. Then you know the publisher service massages them and does something with it. So this type of pattern emerges a lot in high volume queuing or batching systems where you've got like a bunch of requests coming together and then they get processed and massage downstream altogether.

And this specific pattern is represented in trace um through this little yellow arrow thingy that you're seeing here which is called a link in open telemetry lingo. And so that's what we see right here. what a trace or a you know three different traces for example workflow can look like. So now that I have shown you all an example trace I want to take it one step further and equate it to uh a graph. So that's what we have right here. So a trace is essentially a directed asyclic graph and you can think of every single span as a node in this graph. The directionality here comes from these uh child spans or child nodes pointing to parent spans or parent nodes. Each span or node is identified by just for notation here is identified by a trace ID let's say T1 a span ID let's say S11 and a span name consume which is representing what operation it was. So that's what we see here. And then again you can see that this process node has two children. It's a commonly seen fan out pattern in networking distributed systems and many things.

And additionally we see this process node have multiple parents and that's a fan in which is as you can see quite literally the opposite of fan out. And fan in is what we just looked at as a link in open telemetry lingo. All right. So spent a lot of time talking about terminology and like how we interpret trace as telemetry providers. I want to now talk about how our users which is other engineering teams talk to us about interpreting trace. So users can define what's called a rule which essentially um is able to identify an end toend workflow. So for example, we looked at that equity order execution management system. Oh, mouthful. And the team that is handling that can come to us and say, "Hey, get me the latency of requests going from consume to publish so that I can then view those metrics in a nice time series on my dashboard as you see right here." So they can come to us, they can say that and they can be like, "Okay, I have a rule. Let's name it consume to publish. It's got a starting point, all the consume spans.

It's got an ending point, all the publish spans. Um, we refer to this as events. These points are essentially events. And an event just identifies a certain set of attributes matching a span. I know event is an overloaded term. So that's why I'm clarifying here. And then you know you've got your start and end event within a trace. So that's how users can visualize what their end toend workflows are and how they kind of correspond to distributed trace. Okay, so now that we know how a user can talk to us and what a rule is, let's see what it would take to actually get the end toend latency from just one trace. Okay, so we have our trace T1, the example we've been working with so far and in red you will see the start event. In green you will see the end event, consume and publish. The red path that I've highlighted here is representing essentially like the path across which we need our latency. Right? So how can I get that? Simply running a depth for search through this directed asyclic graph which would then also include this other path.

And what we're trying to see is well hey if we like follow these different paths in the system matching the start and end events then we can get the latency between the start and end events. because at that point all I need to do is get the difference between the start of my start event the end of my end event. So in this case let's say path one had a latency of 3 microsconds or path two had for 3.5 micros then these latency values can be aggregated into a nice little time series that you can then view on a dashboard. So to summarize all of that to get end to end latency from just one trace you can run a DFS and you can put that into a time series. Now obviously things are not so easy and we don't just work with one trace. So how do we handle this at scale? So let's say this is what's going on. I've got um this architecture diagram representing how traces are handled at scale. Essentially we've got spans from all over our firm from many different machines coming into this ingestion cafka topic.

So this topic right here this is not partitioned by anything in particular. Um any span from a given trace can end up anywhere. Then there is a partitioner service. The goal of the service is to essentially put all spans from the same trace in the same partition. So that's why your like as you see here these four spans not together four spans all together now in the same partition. From here we've got a service called Dagger because it works with DAGs. Um that was my one pun for the presentation. And then the Dagger service is going to run your DFS, generate the metrics and put them out on the dashboard. Easy enough. Not quite. So that does not really work because of what we just talked about. Drum roll please. Because of finance. Why is that? As you see there is a path highlighted in red here. Right? So this is a valid path across which I want a latency. I want to see how long it takes to go from this consume to this publish. I cannot run a DFS across three different traces that are connected to each other if I don't have access to them in the same Dagger pod.

And that's why what we need to ensure is that if you're a graph graph theory nerd, what we need to ensure is that all spans from the same weekly connected directed asyclic graph end up together. For the rest of this presentation, I will just call it a trace bundle. So all interconnected traces trace bundle. So now this path we want the latency for this is a valid path that as well and so is that. So that's the problem we're trying to solve now at scale. So let me now go into two specific approaches that we took to solve this problem. The first one is where we aggregate trace bundles by role. This is going to be a mouthful and I will absolutely take questions later and talk about it in the hallway but just try your best to follow. So let's consider this input Kafka topic. We've got this time the topic that is partitioned by trace ID. These spans are going into a service called bundler. Bundler has twoprong goals. One, put all the spans from the same trace bundle all together into one partition as you see right here.

Goal number two, put all the different trace bundles that potentially match a role in the same partition. So that's why you see that this Kafka topic is partition by role. What do I mean when I say potentially match a role? I mean that if a trace has just the start event, then send it into this uh partition. So it can potentially match it. And then Dagger will run the final matching with the end event and the start event. Run the DFS, generate metrics, put it out onto the dashboard. If that did not make any sense, I got you. Let's work through this small example. Right. So, I'm going to just zoom in on two different bundler instances. We're going to work with these like four spans coming into the into the system. We've got a small CFKA topic actually which is used for doing just uh passing messages between different bundler instances for some gossip. Now let's say that bundler one receives this consume span. It says oh that's a match that matches the start event of the consume to publish rule. Then it looks at the process span and it sees uh oh that's got a fan in.

I've got these two traces that I don't have, but I do need to run my DFS on them. So, what it's going to do is it's going to send some messages over to bundler 2 saying, "Okay, T1 and T2 are fanned in, T1 and T3 are fanned in, and T1 potentially matches the consume to publish rule." And by doing so, we can actually get a view. We can make sure that all bundlers have a view of all the trace bundles in the system at a given point in time. So they can all communicate this information with each other. And after they have this information, they essentially maintain these in-memory maps of uh these bundle and potential rule matches for these bundles. For the sake of time, I will not go super deep into this, but simply put, this allows us to identify that a given trace ID is, you know, part of a given trace bundle which potentially matches a row and then I can use that to put them into the relevant Kafka partition. Okay, so just to review, um the key takeaways from this approach should be that we've got two-prong goals for our bundler service.

put all the trace bundles together and put all the trace bundles potentially matching a rule in the same cafka partition so that we can then run the DFS across the entire set of weekly connected directed as graphs. Now to quickly summarize that um this approach works well when your traces match a small portion of like when your rules sorry match a small portion of your traces. So for example, if you had a rule that maybe included middleware spans that show up in like pretty much all the traces [snorts] in your firm, right? That would be a very heavy rule with a lot of matches. So that can lead to load imbalance in the dagger pods and this imbalance can be further exacerbated by fan blow. So not only do you have your traces but you also have these fanned in traces. So one uh downside of this is load imbalance for uh Dagger's memory. And so to kind of take this scalability one step further, we ended up exploring this other approach to kind of uh help fight the load imbalance issue. So this is approach number two in which we simply hand off what we call a partial DFS.

So let's take a look again. You've got all your spans coming in from all over the firm in this Kafka topic partitioned by trace ID. This time, Dagger is going to consume straight from this topic. Dagger is going to be like, I'm going to start running my DFS and oops, if I run into a fan in for a trace that I don't have, I will just hand off information about my partially executed DFS to a different Dagger instance. And then uh we have a lightweight metrics aggregator layer. I will not go into that too much. And then we can you know generate our metrics out to a dashboard. So to make that make more sense very quickly, let's work through a small example. This time I've got two different Dagger instances. We've got our four spans coming in. Dagger one and Dagger 2 also communicate with each other using a small Kafka topic. So, Dagger 1 sees this publish span, right? It starts doing a DFS and then it say and then it says, "Oh, wait. This process span has parents in trace 2 and trace 3. I don't have those." So what I'm going to do is I'm going to pass on information about the work I've done so far being like okay this was like the timestamp that I saw in the publish span and I am working on a DFS for the rule consume to publish.

So Dagger 2 can you please continue the DFS for me and then Dagger 2 will continue that DFS for these trace two and trace threes. One quick note, um I recognize I didn't mention it before, but these DFS that we're running, we're running them bottom up because naturally spans store their like references to their parent spans. Um and there's a lot of nuance in there which we can get to in Q&A if you want. Okay, so that's kind of how this second approach works. Now the second approach is more of a lazy approach compared to the first one because we only send information regarding a specific fan in when a DFS runs into that when there is an actual match for a rule as opposed to approach one which preemptively puts all these trace bundles together. And then this approach helps avoid the load imbalance issue. However, one of the constraints of this approach is if you've got really deep chained fans, which means on one DFS path, you have multiple fans that you encounter, which can then mean that many, you know, gossip messages sent over Kafka and those delays.

Okay, so that was approach number two. So just to summarize all of it and to elency metrics from complex traces can be generated at scale. We did that. It was very fun. Um but you should pick the best approach for you from operational knowledge as well about the traces at your firm. And end to latency monitoring is crucial to ensure the best user experience in the microservicesdriven world that we work in now. And finally, if you did not get anything else, distributed streaming DAGs are fun. And if you want to talk to me more about it, let's trace our way together. With that, we are done with the presentation. Um, the QR code is for feedback and we have time for Q&A. Thank you. [applause] >> Hi. Uh, thank you for the presentation. My question is in uh relation to the U bundler memory map that you referenced. Uh I'm assuming it's either running on a dedicated uh virtual machine or a pod. U in either case if it goes down how do you manage uh state? Did you look into readers as a separate thing that handles that memory?

>> Yeah, good question answer. Currently it's best effort. We would like to do something like having maybe like a write ahead log or you write to disk or you write to a persistent volume. Right now if it goes down we do lose the metrics from that bit. So it's best effort for now but yeah that would be an ideal state we want to get to. >> Thank you. >> Yeah. >> Hey thanks for the talk. It's always great to see some real world implementations. Um question. >> Sorry I can't quite understand. >> Sure thing. Sorry. >> Uh question on timing. So across systems there can be time drift that happens making just a simple start end. Is that something that you've run into? And if so how did you address that? >> So great question. Um so for clock skew and drift there are some systems that are more latency sensitive than some others. Those systems actually often like some of those systems can't even use trace because they're like the network like the amount of data you're sending over the network is so small and they have like a lot of other constraints.

So there are some super sensitive systems that will use their sort of custom solutions still. Um this does not quite handle clock skew. Um and I will say there is actually a research paper if you want to look it up online on someone who uh tried to handle clock skew specifically with distributed tracing if that's of interest to you. But yes uh aware of that problem. We don't handle it in here. And the systems that the bulk of systems that do use this are not that like level of latency sensitive. Yeah, [snorts] we have time. We have four minutes. >> Yes. >> Sorry. Oh, or I'll I'll just I'll just repeat it. Go for it. So it's kind of the other two questions. >> Yeah. Yeah. Sorry. So yeah, let me talk a little bit more about these Kafka topics in between. Right. So this is the dagger scenario. what they're using when they're sending these messages is this little Kafka topic. Um so in this particular case uh we've got a small communication Kafka topic which is partitioned by the trace ID of this like pair and fan in.

So for example let's say I want to go to the Dagger instance that has trace T2. So I will send it into the partition for trace t2 obviously hashed um and then that is what dagger 2 is already consuming from. So we are using a message cue kafka in particular which is partitioned in a way that we know which instance is going to receive what. Okay. I could not understand. Sorry. >> Using trace ID. >> Yeah. Abs. No, no, no. We're not using trace ID as a topic. We are hashing the trace IDs into specific partitions. So for example, if you have like 50,000 trace ids, you have like five partitions and 10,000 are going into one partition. >> Yes. message whatever. I mean, do you consider using attributes to >> um we don't actually use baggage. It's something we are currently exploring. Historically speaking, it's because of having constraints on the amount of data we could send over the network to certain systems. Um, so when users tell us what their like start and ends are, they can use span attributes also to like identify these starts and ends, they don't have to tell us anything more than that.

Uh, we do have some like configurations like okay, you can give us a hint. You can be like you don't need to execute a DFS. You can do a simple match or you give me like uh the number of traces which did not meet a certain threshold of latency. Um but outside of that just to identify these paths all they need to do is give us attributes to define the start and attributes to define the end. >> That is correct. Um ideally we would have uh CI/CD time validation when your like critical workflows are changing like instrumentation around that but right now it is very much in the hands of the user to inform us about it. >> Um >> sorry take that question >> one question u the rules that you could uh use for filtering the partitions is that based on text is does is it based on reax does it offer both? Yes, tags, reax, um, and or whatever like your classic rule matching sort of logic. >> Thank you. >> Yeah. >> Why also metric generator? >> Yeah. >> Did you look at using span gener? So the yeah the key issue with that is right like with span metrics they give you servicewide operationwide metrics and for us especially when you have a lot of these like pub subm models and queuing systems you can't like easily have a top span that is like waterfalling and including your end toend time.

So when that happens, you really do need to be able to get these metrics across different spans. And that's the part which span metrics often is not able to cover, especially when you're going across like different trace ids and fans. But yes, I would have loved to just use it out of the box. I have I am aware that we're out of time, so we can talk about any more questions outside in the hallway. Thank you everyone. >> [applause]