[Music] so hello everyone my name is Kristen Hoffman and before I actually dive into the technical part of my talk I've got a quick question who has heard of Conway's law ok surprisingly much people I'm still going to give my interpretation of Conway's law it basically says the system designs are going to resemble the organizational structures at some point in time so this is why I'm going to give a short introduction into the structure of our company which is the cool book we are a German influence company targeted as consumers we are the Lockett largest kauri ensure efficient you know consumers we've got about 12 million customers and 10,000 employees within October there are multiple departments even multiple IT related departments and we've got about 800 people working there in IT related shops so we are certainly not some kind of startup rather we have lots of teams lots of policies lots of regulatory stuff high data security policies and so on one of these IT related departments is the Linux platform development team and we would describe ourselves as some kind of managed infrastructure as a service provider so we provide Linux machines to our internal customers and we manage around 900 service based on wretched Enterprise Linux these services are distributed across two main datacenters I am one of the 10 people in exactly this team I join in 2016 and I would describe myself as some kind of Linux and open source and fields and SUSE yes so much for the organizational part where I work we've got lots of other teams as I said so we've got application owners who run databases who run rep servers who run insurance software and these application owners are the ones who use our Linux servers as the base we've also got an dedicated operations team which handle the on-call situations which I'm going to describe later so now let's dive into the actual technical parts I'm going to describe our Prometheus setup how we run it and what our basic configuration looks like but I'm going to focus on the parts where I think our separate setup is special so let's start with scraping or with the actual Prometheus service when we started we thought about how many Prometheus servers are we going to run and where should we place them the common recommendation seems to be run your Prometheus service as close to the monitoring target as possible but what does close actually mean we thought about this and we came up with multiple ideas what this could mean we first thought about firewall zones and we quickly noticed that we've got lots of firewall environment environments and some of these environments don't even contain any virtualization or content container in fact infrastructure they only contain two or three servers and it certainly would not make sense to run Prometheus instances for I will abilities there so we thought about other meanings of the word closed should we run it on the same network switch cluster should we run it in the same data center which which most people seem to do and what we actually decided is that we should run from a responsibility perspective so we decided that we should run a central Prometheus instance and we would run this as a pair of two servers distributed across to our two main datacenters so this is how it looks and we monitor all servers from both data centers so there's not - two pairs of these as just one pair this has the benefit of providing insight into the network so if we really should have some discrepancies between the data centers we are going to see this using these instances so I'm going to put up some numbers and you will notice that our Prometheus setup is actually not that large and we're running on two virtual machines with four cores with two and friendly gigabytes of RAM and one point terabyte well disk space we handle 1.7 million times serious and with about 30,000 samples per second we've got a pretty standard scribe interval of 60 seconds we use the file service discovery for for service discovery and have half about our to 2006 entries there we've got lots of alert rules which we write by by hand obviously and we've got lots of recording rules these are partly generated from other systems so these are used to provide for the meter data into Prometheus so now that we we've got a Prometheus service where should we scrape the metrics how should we reach our our monitoring targets as that we have a high security environment everybody's going to talk about well you can't do the plain text formats so what the common recommendation seems to be well place a reverse proxy in front of it use TLS and get all these certificates and stuff we thought about this approach and we quickly noticed we've got already lots of monitoring on agents on our servers we've got security scanners with we've got license minute manage like license management tools and it's only getting worse that we really try to keep the number of extra agents on these servers down so how do other well monitoring systems solve this problem or what does a typical little server look for us we were an nmap and we noticed there's one service running there already it's SSH hmm other monitoring tools use SSH and run some kind of command on the target server to get their data and we thought well can we do something similar maybe not running a command but using at least SSH SSH provides us authentication encryption it's easy to lock down and we have an established mechanism for SSH keys we don't have to well get TLS certificates from different CAS in our environments so SH looked like a good idea but for me this does not support Sh luckily Prometheus is really flexible so we've got our Prometheus server we've got our monitoring target and the monitoring target runs SSH E and it runs all the exporters so how do we get Prometheus to use this way for access all we need is to use Prometheus proxy feature it has a config option which says proxy URL and we can well plug in anything we want in this case and we came up with a small daemon which we call SS 85 which simply an HTTP listener to Prometheus and translates all requests from Prometheus to an SSH tunneling request so this way we rep all the metrics excesses within this SSH tunnel which is easy from a buy one point of view which provides us the authentication and encryption of course this also only makes sense if we limit access to the actual exporters so node exporter will probably start with binding to the public interface by default and exposing all the metrics on a public port this is something we had to keep down so we look all the exporters to localhost and we even deploy IP tables rules to further restrict access to only some specific user on the system so what exporters do we actually run well no export is something you probably all know we also make heavy use of the text file collector feature we mainly use it to fill gaps where note exported does not yet support specific parts such as and certain error situations in LBM in multipath constructions in sound related monitoring we've also got a small script framework which makes it easy to plug in scripts so that other people from the other application teams can just write some small script which outputs the metrics and note exporter will pick them up we do run node exporters route this may surprise you as there are recommendations not to do this but we have reasons for that one of the reason is that we like to lock down or a file systems really match so a normal user would not be able to see each file system on the server we would have to teach people to configure monitoring access for them and we would not even notice if someone forgets to do that so prometheus or the not exporter in this case is is able to see all file systems this way also we make heavy use of this system D exporter to monitor services and the systems exporter has a custom feature which uses a private soccer - well accesses to T so the the connection between node exporter can either you go over T bus or it can go over the private socket and privates could turn out to be much more frequently reliable for us especially under load situations we still about me we still have lots of services which are not within system D we also got some legacy system so this is where we run closes exporter to do some process based monitoring but we really like to get rid of this part we use a model exporter called mighty logic spotter which is used to extract metrics from well proprietary application locks and this is comparable with the MTL exporters from Google or the graphics part that we support and a custom matching support we also use a blackbox exporter and we do not run it in a central place mainly only as well for fievel reasons that we run this part as close to the target as possible so a machine exporting of exposing web server may get all the black box exported and we will at least see if the web server works correctly as this is okay because we have other monitoring tools which monitor the whole well placed from the customer view to the actual when load balancers which which return the actual data so now we then we've got the premier setup covered I'm going to dive into the alerting part we use a read manager as probably most of you do and our Prometheus instances send all the other dementia and we've got a fairly simple routing tree our routing is based mainly on the algorithm we've got three categories we've got Linux platform alerts which are targeted at my team and we've got Linux server application alerts which are targeted at the application owners so a custom example would be a platform a lab could be something like well SSH daemon going down or the connection to our Active Directory going down this is something that we would resolve but we also have lots of alerts which the application owners have to solve such as filling filesystem or something like that we use two integrations we use a receiver and the email receiver and the receiver is also the place which we use to forward all alert manager other words to to our central monitoring so in our case for me this is targeted at the NOx monitoring but at work or book we run lots of other systems like Network Devices and Windows servers and so on and this is all collected in a central place and we have a custom daemon which translates translates the alert manager webhook requests into a suit locally went for this central event management the central event management is also the place where incident creation happens where paging happens and where we've got some money trying to catch the case when to me this time we've also we also make use of silences and this is done by using our proprietary stop inventory and the server inventory' uses some kind of maintenance mode and we swing this maintenance mode and translated to silences in using a custom Python script for graphing it might surprise you but we use graph Anna our main challenge was building some kind of multi-tenancy sale so we have this graph Anna setup but what happens if john x's excesses well our graph Anna instance we wanted him to see only the service which are relevant for him on the dashboards which are relevant for him and this is a little solution we came up with so john excesses web server hthe HTTP in our case we authenticate a museum what leop and what of Kirk and after that we took off farah where this is John so kuffara knows this is John we've got an organization in Havana fortune and we've we've got this for all our employees which use Ravana and we do this because we do not point the this organization's data source and Ravana directly to Prometheus but instead we pointed to a custom service called from it is filter proxy and from easiest filter filter proxy translates all these requests and maps a special label onto all parts of the query so if John is going to ask for the app metric he for me the filter proxies going translated to up but only for time serials labeled with owner John this is not some idea which I came up with this an idea which I stole or borrowed from some of the tickets it may be Brian who posted it there so this is Oh some implementation of this idea of course we do not do with this all manually and we've got lots of employees and so we've got lots of gravano users so we use some kind of template organizations where we do our dashboard and changes and we've got a job which managers of the dashboards of the actual owner organizations we also run this setup twice basically for high availability so that we can perform maintenance work and we accomplish this by running floating a P address so this is always on the left side except when there's maintenance it is moved to the other side and we keep Gravano in sync by just are swinging the curve on a SQLite database we know that they're more sophisticated ways such as running a product and together with some kind of database which is highly available on its own but we really wanted to cut down dependencies at this place so and lastly these were the parts about the the common Prometheus infrastructure but I also want to provide some insight into how we integrate Prometheus in our into our environment so I've got two examples there and the first example is about integrating Prometheus into our existing configure manage node we use puppet others have used chef in their talks we use puppet and we use puppet to deploy all this service so it was a natural choice to use puppet to deploy all the exporters we IND on these machines and it was also a natural choice to use puppet to configure Prometheus itself so puppet takes its own will database of flat file database which is called Huayra in puppet speak and this contains all our escaped convicts and our platform alerts so these Linux platform where let's come from this place and we can also use puppet to distribute them server all specific alerts in this case so that we can have custom alerts for databases or custom alerts for that service this is an example for Prometheus being integrated into the conservation management but if a second example where we made use of the features of Prometheus in another process I hope that most companies do have some kind of patch management patch management process at least we do and we try to automate as much as possible this means that our patch management process should be automated as well so we've got some internals the development and staging environments where we pull new operating system patches or leases new system software and so on and we use Prometheus to detect well common problems there like the server no longer coming in coming online after such an update but we also run custom benchmarks to see if there are some performance regressions and we started to introduce this and when all the hyper inspector and measurement came up so this is the place where we run from easiest and can detect anomalies before they we roll them out this is the first place where we use Prometheus in the patch management and we use it also in a second place and when we actually update application servers such as main website then the changes will first go out into a development machine and if there is anything which which comes up in the alerting then our patch management process talks to Prometheus asks is there anything special about the development server and if there is it's going to stop the deployment into the other environments and this even works in production so we can ensure that if one server is down because of well other maintenance or some problem then we are not going to patch the other one and leave us with with with no service lose of the actual customers so these are the parts which were good for us but we are also not done so we do have some ideas about long-term storage we currently keep data for about one year and this can be problematic if people try to well process large time time ranges and we would really love some some solution for down sampling the data we currently have some kind of channeler process which talks to the Prometheus API and release all metrics which are not used in any dashboards or are not used in any rules so that these these metrics are only available for 30 days and only the other metrics are canceled we also want to look into dashboard performance optimizations and have lots of other ideas so to sum it up and we are very happy to have implemented prometheus Prometheus provides us flexibility even in our regulated environment in high security environment with lots of policies it made it possible to off about my two tenancy for our teams so that each person only sees the relevant parts for them and for me this is really good to integrate into other processes which also helped us so this being said thank you very much to the Prometheus developers community the prompt con organizers and thank you for listening [Applause] hi I have two questions about the filter the Prometheus filter proxy the first is descent work does it inject the correct owner label until all like any kind of complex query that that comes through and the second is how do you ensure that the owner labels are actually part of the scraping logic how do you make sure they actually get added get it added in okay first question I hope that we have a robust solution in this place and we are using the actual prom killer passes so this is not some kind of a regular expression Hickory or something we use the official from parser and try to match each prom cool well data type and so on and inject this label there and we have no found cases yet but this project is open source you can have a look at it and if you find something please tell me so that we can work it out and for the second part of how we ensure that the honest label is attached to all the metrics this is part of our file finest D service discovery making mechanism so we use puppet to pull this data from our server inventory and use popper to write a config file which is used by Prometheus and contains this owners label third shope oh thank you oh yeah I'm here yeah thank you I have a question about this fight so I call 85 and I notice the name is really good fun okay I have two questions first question do you restrict access on the target host mmm I don't know that you don't have every every comment maybe or something to exist and the second question is why not on some maybe white brace it to a spokes you on the target host as well not giving for such access for someone who has access to parameters host and the first part how we lock down access well basically we use basic normal OpenSSH methods so we use public key authentication and we can put some restrictions in the authorized keys file such as disabling all command usage and only permitting the the port forwarding which is what what is required in this case well there's no no shell or anything open is chapter the port forwarding yeah okay the second part was about or why not use something like a jinx on my bill wine jinx on the target host yeah well basically because we would have to run it in on all the target host and as that this is the common best practice you can do that and in our case we decided that it would be too much overhead even by using puppet we would have to run it everywhere they can be failures it would have to been updated and so on so we just want to try to keep the number of agents on these servers low so we went for this solution which places in a central place one more question here do you find any hard limit on this sh pipe you try and you using like how much can you ingest not yet in production and the CPU usage is also ok and we ran into one problem once and it was related to the file descriptors since then we have implemented the metrics in phone for SSA chief ID so we know about its state and we have an alert on it and it doesn't happen anymore and regarding the actual throughput which works I don't have have any numbers ready but the github page maybe contains the actual numbered numbers at least it contains some scripts for benchmarking so you can quickly run it yourself and try to see how how many connections it will handle hey sorry with satified do you have a way to restrict only the port that exports matrix or do like does the user have access to all the ports on the machine on do you mean the machine where whether we are monitoring the target yes and we use a exporter extreme restriction for binding to localhost only which limits all access to the machine itself and in addition we deploy a features rules where we have an own image target which limits all exes only to the specific user user which we use for SS 85 access anymore 3 2 1 thank you very much [Applause] [Music] you [Music]