[Music] and everybody my name is Madhuri I am the founder makers of nodelist kubernetes prior to that I've been a systems engineer for 20 years worked on Flocka which some of you might remember from the early container ecosystem days where we helped run stateful applications inside containers and prior to that worked on resource management for the investig-- on initial placement load balancing of virtual machines you know VMware truster and also VMware hypervisor product prior to that worked on oracle database server technologies in cluster way software so mostly systems and distributed systems engineering how many of you use kubernetes currently with prometheus Wow kubernetes capacity planning on public cloud is super hard when you take a regular kubernetes cluster instead of focusing on your applications your pods your resource manifests you as the DevOps and asari folks spend a lot of time on hand curating the worker nodes you have to select the base worker node type and what price point of the worker node you want to select whether it's on demand pre-emptive spot or forget kind of of compute launch types and also you have to hand curate the auto scaling knobs and maintain monitor and update the auto scaling knobs based on your applications workload changes and also when a worker node dies it is a pet worker node so you have to someone has to be on on-call rotation to make sure that the the compute capacity is still enough to run your worker nodes the compute capacity on public cloud is ephemeral and it doesn't have the same static sticky qualities of server blades on on-premise data centers so why are we continuing to to manage and provision compute on public cloud as if we owned the machines is sitting in a private datacenter so with the over-provision the compute capacity nodes we are wasting resources we are wasting money if we under provision the parts will be in pending state and won't have the compute node available when it wants to run and unexpected spikes during rolling up upgrades and horizontal part of scaling will be very lazy to react no less kubernetes is flavor of kubernetes which basically makes sure that you consume public cloud capacity as and when it is needed for your kubernetes clusters and focus on your kubernetes applications instead of focusing on managing the operational complexity of your kubernetes cluster worker nodes so what noodle s communities does is that the control plane looks down and sees a worker node which advertises very large capacity it advertises let's say thousand 24 V CPUs 4 terabytes of RAM tens of GPU devices and it says that I can run up to 1,000 parts but in actuality it is the virtual capacity of the cloud provider that it is advertising it's not pre provisioned always on work a node that is that is advertising this capacity so when a pod is provisioned to this worker node the worker node will provision just-in-time right-sized cost optimized compute launch type for the pod on the cloud provider that you've configured and the just-in-time compute it started up and the pod is dispatched over to the compute node as long as the pod is running the pod appears to be running on the large virtual work an old and once the pod terminates the underlying compute capacity is automatically terminated the computer launch type could be sourced from all the different flavors of launch types that are available on your cloud provider so it could be an on-demand compute node it could be a spot instance it could be a far gate launch type or it could be a newer better cheaper launch type that the cloud provider has brought to market three months from now so you don't have to keep like a hand curated list of the various computer launch types that are available so one of the questions that we get asked quite a lot is that here how do we manage node less kubernetes applications does Prometheus work but notice kubernetes and the short answer is is yes it works with node less kubernetes just like it works with a regular kubernetes cluster so there is absolutely no change from the user point of view and this is a question that gets asked most often when we talk about nodelist kubernetes so let's go ahead and see a demo of how this is going to work so out here I have a single master single virtual worker node cluster that is an odorless worker so we have we have single control plane node and a single virtual worker node let's describe node on the virtual worker node so if we look at the capacity of the virtual work alone it's saying that it has 1024 V CPUs it has 4 terabytes of RAM it has 10 GPU devices and it can run up to 1k pods so let's actually look at this host name in our AWS console it's actually running on a t2 medium instance which has 2 V CPUs and 4 gigs of ram so the worker node actually has very little capacity but it's advertising 1024 V CPUs 4 terabytes of RAM so as the first workload let's go ahead and deploy our Prometheus stack sorry so we are trying to create Prometheus start with graph on R cube state matrix and Prometheus deployment so we have services associated with the graph on R cube state matrix and Prometheus and I chose to externally expose all these three services for demo purposes and we have deployments associated with again graph on R cube state matrix and Prometheus and we have the three replicas sets corresponding to graph on R cube state matrix and Prometheus so all of these three components are being deployed into monitoring namespace so what is happening under the covers is that in our single node cluster the three deployment requests enter the kubernetes control plane the control plane looks down and sees just this one worker node which has this very large capacity so it schedules all three all three pods on to the virtual worker node and virtual worker node will make the call as to what is the right compute launch type that fits the resource requirements for each of these parts and adjusting time launched just-in-time compute launch type is provision and each pod is is is scheduled to the just-in-time provisioning compute launch type as of now no less kubernetes can only run stateless applications so what it does is there is an admission webhook that will dispatch the stateless applications in an odourless fashion and the stateful apps are started up on the virtual worker nodes compute node itself support for stateful apps will be coming in a couple of months so this is a temporary dispatching hybrid model that is being implemented right now so what is what will happen now is that the control plane is going to start the Prometheus deployment on the worker node itself and just in time compute cells noodler cells are provision to run cube state matrix and graph owner so let's see if our parts are running all the parts are running so let's try to access a graph on our dashboard it typically takes a little longer time for the external load balancer to be available so let's see if that is have been running right now all right so let's go ahead and add the data source for Prometheus since all the three components are running in the same cluster I don't need to use the external IP so I will use the service address for internal communication and there is a very simple graph on our dashboard for monitoring Nautilus cells so let's go ahead and import that dashboard so what this dashboards shows is there are four columns so it the first column is the number of nodes in the cluster which is two we have one control plane node and one which will work a node and the second column is components that are running on control plane so a control plane node name and the number of pods that are running on the control plane it's mostly the control plane components API server queue proxy etc and they said on the third column is the worker node components there are seven parts that are running on the worker node itself including Prometheus deployment because it's a stateful application it's running on the worker node itself and the interesting column is the fourth column where which lists the node list pure cells that are running in the system so there are three nor less compute cells they correspond to graph on R cube state matrix and cube proxy which is a system part that we can ignore for the purpose of this demo so as if we want to create an engine X deployment for example which is stateless to start with let's look at the engine X deployment which is asking for three replicas and it doesn't have any persistent volume needs once we create this deployment once the pods are up and running once the parts are up and running because the the control plane is going to schedule the work the engine exports or no Lascelles because it is not it doesn't have they don't have any persistent volume needs once the three node Lascelles are up and running and the ports are dispatched over to the node Lascelles the graph on our dashboard should should reflect that our compute cells no less compute cell count should go up from 3 to 6 eventually I'm going to pause here for questions this is the gist of the demo the key takeaways that if you're using kubernetes on public cloud not having to hand curate and babysit these pet worker nodes is a cleaner way to consume public cloud compute capacity and Prometheus and Ravana are the perfect fit for monitoring notice kubernetes applications and there is no difference in monitoring no less kubernetes applications versus your regular vanilla kubernetes applications so if you want to give this a try i'm going to share the github location for running this tutorial yourself thank you so much and I look forward to the questions [Applause] yes I am thankful talk yes this is more talked about notice I just wonder how you do all the affinity and definitive stuff as the scheduler doesn't know how to work with it compute node isolation so you get 90 affinity out-of-the-box affinity there is there can be an admission controller with schedules the if party and pod we have an affinity rule they become one notice pods so they scheduled as one notice basically so they go locate on the same computer questions or the a.m.

is that are running on the nodes are those special notice ones or just the barebone ones that we would run for you guys for example yeah that's a good question one of the motivations for node less is to eliminate the need for maintaining the operating system the security patches and all of that overhead so by default the node Lascelles run an Alpine base image but some enterprises have pre listed lest images that they want to consume instead of Alpine so you can override that ami by default it's an alpha Nami so the the key idea is to keep the over system overhead of the cell to keep it pretty minimal so that you get majority of the resources in the cell for your application thank you very much it was hi great talk so what is the latency of provisioning the actual compute nodes because we're advertising a lot of compute but we don't have it exactly what is the latency and as a follow-up if you are not able to provision that compute what would happen yeah that's a good question the the computer launch type latency varies between cloud providers and even on a single cloud provider it varies between the various launch types so for example on demand on AWS can take anywhere between five seconds up to two minutes whereas fire can launch really quickly so there is a knob that you can turn on in an odourless kubernetes cluster where you specify the SLA and depending on the SLA for your application startup if your app has a five-second delay between cube CTL create and the app responding then the node less engine will maintain a set of pre-warmed cells that are treated so that your SaaS on it so it's more of an SLA driven capacity management rather than having to provision it beforehand I am if you configure a daemon set to have four node exporter running on each node what happens in this node list scenario with that yeah so that's a good question so 'man sites odd so daemon sets came into existence with the assumption that you have these very large server blades that are comprising of your worker nodes so the idea of the daemon set is to collect all of so to represent the compute nodes agent correct so that is what we are trying to achieve so what you can still run a daemon set on the virtual worker node itself but it will run on the virtual worker node it won't run on every single cell so depending on the motivation for your demon set you can have for example you can run elastic beads file beat I think node beat etc if it is something super lightweight then it can be injected into the cell itself so it totally depends on what is the purpose that your demon set is trying to address does that make sense if you have a specific use case I'd love to chat afterwards just a second a question similar direction what about running a custom interest controller on this infrastructure basically same problem as with the diamond set but also regarding networking so do you have some extra hops in it no so the one of the key design goals of nodelist is for the behavior for networking and storage and everything else to be exactly the same as regular kubernetes cluster so we in fact have there's a blog post that that talks about how is tio would work so your site cars are still running on the cells they're not running on the virtual work a node so each cell at all the networking site cars will run on the compute cell they aboard is running so it it works just like your vanilla kubernetes basically anyone else thank you [Applause] [Music]