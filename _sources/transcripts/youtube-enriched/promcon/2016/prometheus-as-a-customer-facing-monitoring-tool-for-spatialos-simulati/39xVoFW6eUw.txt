cool thank you all right so my name is dima i work at improbable I'm one of the infrastructure engineers there and today we'll be talking about how we use Prometheus Adam Pro Bowl the talk will cover three main parts one of them will be our general setup how we do the sharding how we do the Federation the other part will be using Prometheus as a user facing tool in a multi-tenant environment so we'll talk a little bit about that and the third part is going to be bonus part I'll talk about a little bit of em for magic that we have to do for recording rules okay so before I get into that I quickly want to talk about what we do it will explain some of the design choices that we have made so speacial OS is a distributed operating system the distributed operating system is marketing talk I just lifted it from our promo slides but the idea is we basically are a platform that allows users to run large-scale online simulations that's what we do ok so how does it look from the above so we have put the purpose of this talk two main clusters that we are operating there is the platform cluster so that's where all our front ends and the user facing api's are so there we have the API server the authentication server the proxy server i'll talk about them in detail a little bit later and we have the platform on the train so this is what monitors this particular cluster our internal services in there we have about three of them up to production ones one stage in one the other part is the deployment cluster so the deployment cluster is the heart of kind of spatial OS this is where users simulations are running and normal deployment of simulation consists of a bunch of things the main ones are the wrong time that's what we provide this is our code and then the workers that users provide so the workers is what calculates the next simulation step a user worker can be a game engine or maybe some crazy simulation engine and for that we have two separate instance of Prometheus we have infrastructure monitoring they in from home and we have the simulation launched in the symbol so inform on as it says on the tin it's for our monitoring infrastructure services sim mom is what will later be kind of exposed to the user that's what monitors their simulations again the other thing that's kind of weird and that causes some issues for us is that deployments are kind of not standard long-running job they can be very different they can be super short lived in the order of minutes like when you're trying to do an experiment where you're trying to make this go to work at loss or they can be something extremely long-lived for example a production game this can be weeks or month or maybe if you're successful years let's hope the other option is a big sale I've City simulation again really long running things the scale of it is also known to us at the beginning it's defined by the user it can be something really small for example one machine again for testing or something really big when they're running those big simulations those big games and the thing that connects to the prometheus is that they're dynamically named and that's what causing quite a few issues for us because we don't know the name of it this gets to the labels but I will talk about that a little bit later okay so what does prometheus do for us as the engineering organization we use it to monitor our production at station services we use it to monitor our corporate environment for example fabricators stash get etc and obviously for alerting because everyone loves waking up at 2am at night and what does it do for the users okay so something we actually didn't realize up until later like a year ago is that users really like metrics and they really want them because if you're running something big like I don't know couple of hundred machine simulation you have a small distributed system on our work and distributed system and a really good way to figure out if it's working if it's behaving the way you expected is the metrics so we need to provide this to the users so that they can debug QA or see the performance of their game the other thing that we discovered is its acts quite a useful tool for game design so a lot of games that we have they have an evolving behavior they can have some ecology in the game and to figure out if the ecology is kind of doing okay you want to see some of those metrics for example if you have trees that have been spawned due to some rule you want to know when the trees overtake the world and you have to tweak something in terms of your game design or if there is too much gold being generated I don't know and alerting if the users run in a production game they want to be alerted when things go wrong okay so all of this monitoring pipeline we call them months monza everywhere they in from on global platform on etc and so next slide this is how the monitoring setup generally looks so at the very top we have the global cluster this cluster has the global monitoring the Prometheus this is kind of meta monitoring so it's monitoring the Prometheus instances on other clusters and also some federated metrics make it there but it's a very limited number then as I mentioned the corporate cluster monitoring the platform cluster monitoring and now the interesting one the deployment cluster so there is a split between our stuff so that if users suddenly decide to really hammer the metrics we at least have access to our internal infrastructure long train and now the monitoring of the deployments so there are this is where the Federation hierarchy really comes in so we have the symbol that sits at the top and then at the bottom we have multiple simulation leaves that are the ones that actually scrape in the metrics from the deployments themselves we would love not to run this federated setup but at the beginning we realized that given a large deployment to multiple large deployments we can't fit it on one box so we have to go for this federated setup okay so how do all this most generally work was the general setup for it so we decided to store the context in remote git repos this is pretty convenient because you always who change the configuration and it's an easy way to iterate on it so this conflict contains the launching targets the rules and the alerts and by this together with the docker that runs the parameters we have another process that monitors this git repo and whenever there are changes it just loads the new repo checks whether the configuration is correct using there I think pronto or something sick hubs the parameters and then the new config is loaded the other thing that we have we have loads of magical and for recording rules there but that's the bonus part comes later ok and this is basically the description of how we load this config into the Prometheus makes it to github bowl for changes loaded into prometheus okay so um quick enter into the recording roles why do we need them we have quite a few really frequently used metrics so we want to pre-compute them we have some expert expensive expressions again recompute that convenient so this is an example of sound recording rule and also the main metrics that we scraped you in the Federation are the recording rules so that's how we use them ok so Federation the idea behind the Federation is that you just it's a way to scrape a subset of metrics from a Prometheus instance the way you do it is you just make a request to slash shredder eight and point you provide the query inside the match and it picks the labels the metrics that match this query so what we decided to do is we decided to introduce our own convention there so if a metric has double colon in it it is considered a federated metric so for us it was a simple way to distinguish between the metrics that were federated or that were not federated and again since it's all code generated no one has to even know about this convention unless you're trying to debug something ok so why why did the scale of just one box not work for us we have loads loads of unique time series mainly because there are a lot of things that are user-defined so this metric like play latency it looks fine like there is nothing obviously wrong and it may be except from node ID but the deployment main project name and deployment tag all those things are user defined so they start exploding really quickly in terms of cardinality okay so at the very top we have the symbol this is what scrapes the federated matrix from the same leaves the way it discovers the same leaves you just use the DNS discovery you have your naming convention to figure out which of the metrics should be scraped during the Federation and has a bit longer data retention around 14 days seems to be okay for us now we might end up tweaking it don't think you can see it from the back but this Simone currently has something between like 2.1 and 2.5 million metrics and I think it's just kind of from a normal day so there were no big production deployments going on and interesting thing you can actually see here is you can figure out what time we finish work because you can see the number of metrics going up until like 6 630 and then because of the data retention it starts going down and then we have the nightlies from one to two and then starts going down again and people make it to work so even this is fairly guess confidential metric you don't want people to know when you work okay and the sig leaves that's what's at the bottom so they scrape the metrics from the deployment endpoints a deployment can have loads of metrics endpoints different nodes different modules expose their own metrics because we have multiple sim leaves we need a way to figure out how to recharge the data between them so the way we do it we use the deployment name and the project name to figure out which sim leaf is responsible for which deployment and the sim leaves have much shorter data retention it's I think in the order of hours and again this is basically number of series the spikes is when big deployments are coming up okay and this is basically an example of sharding so there is nothing magical behind sharding you just need to filling the number of leaves in our case you have to modulus we have four of them that's why we have before you figure out which labels you're going to be sharding on and then you decide which ones to keep it's pretty convenient because we insert all those values in there journaling one of the generation parts so some of the clusters from forcing leaves they are the ones from too and we can theoretically scale it up almost live with some of the things but we actually haven't tried that so I don't know that will work okay an interesting thing that we discovered is that similes could not rely on DNS for service discovery that's what we used at the beginning it was super convenient we used sky DNS as our kind of DNS back end are we used it for service discovery all the endpoints that export matrix would just register there and sing leaves would pick them up the problem is at some point the metric started randomly disappearing and then reappearing is really strange took us a while to figure it out and then we realized that we actually ran out of the DNS response sighs so sky Deanna's i think it was around 64 k and we couldn't fit all the endpoints that needed to be scraped into 64k partly because we have really long domain names and we all have loads of them so we decided to use Prometheus's blog post to figure out how to fix this so they have a really nice write-up about how you can do your custom service discovery so what we ended up doing is instead of clearing the DNS server we just looked up the registrations inside the tcd because sky DNS uses egcg as the back end it has well-defined structure as to kind of where the things go so we read data CD ourselves pull the data out then we can convert it into a file that's understandable by Prometheus and that's what it uses as targets so that was quite fun again so how does the metric make it from a deployment all the way to the simulation monitoring so on the simulation point you can have something improbable migrations finished with a project and a deployment there will be a recording rule for this on the on the sim leaf and you can see that what i do know if you can see but there is a double colon in there and that signifies that this is a federated metric and the actual metric that will be exposed on the same leaf will again look something like improbable migrations some with some number and then the symbol just has a config that says that all the metrics that have double colon have to be picked up by the same one and that's how it makes it all the way from the deployment onto the simulation monitoring ok so this is the part I really wanted to talk about so we said that we use Prometheus as a user facing to the issues that we are facing here is that we have multi-tenant environment and with the multi-tenancy you want to protect users metrics so we know that metrics can be very sensitive data you can get a competitive advantage by looking at someone else's metrics like players online or ours plate you know how well the company is doing or if you want to figure out who is a really weak player you can just look up their flags and then you know who you want to hunt in that game again really important really sensitive so we need two types of data isolation we need to isolate the data between the customers so they can't see each other's data but we also don't want customers to see our internal platform metrics because again this is our data we don't want it to we don't want to share it with everyone okay so there are three main moving parts to make this multi-tenancy not much than see this data is elation happen there is the authentication server so this is kind of homemade homegrown ACL server uses Google is identity provider implements oath to flow and it uses GWT tokens as a way to describe the user's permissions so the GWT token will encode something like this user can read anything in the in the project best game ever this is obviously not the actual format of the token but this describes what the bearer of the stokin is allowed to do the other moving part is the proxy server so we actually wrote a whole bunch of proxies it's one of them are this proxy server the main purpose of it is it gives you access to our internal services it's both the customers that use it for example when they try to go to graph on ax because we don't want anyone to be able to go to graph on that we want to say that if you have access to any monitoring only then you can go to graph on it if you don't have monitoring access you shouldn't be allowed to go there so they have to go through the proxy to hit the graph allah graph anna will use that token that the user got from the earth server to figure out the permissions and the other useful thing that we can put there is throttling so you don't want user ddosing you by going to some endpoint so we can limit the number of requests they can make because by the time they get to the proxy server we should already know who they are or if we don't we can send them back to the authentication server for the oath to flow for them to get redirected back okay and the third part that is actually tied into prometheus is the Prometheus guard the prom guard yet another proxy ah this guy actually understands the semantics of the request that goes to the prometheus it will inspect this request and it will check the permissions against what the user is trying to request so together with the request of the primitives we will get the access token then we will use prompt ql to figure out what the user is actually requesting so prom feel will be able to parse this request and then we'll just compare what do they want against what they are actually allowed to see ok so this is approximately how this looks at the very top the user will decides to go to graph honor they go to the proxy server if they need they might get real through authentication server then as the dashboard starts loading up graph Anna will make a request in to the prom guard and then the prom guard will decide Oh users trying to access improve matrix and they don't have permissions for that deny this request and you will get the sad read !

and yoga fauna you click on it and you'll see permission denied if you're trying to access your own metrics it will just let you through if you're trying to excel else's matrix again access denied you can't do that okay so how does the actual rewriting happen inside the prom guard so to pretend to be a Prometheus server you need to expose three main endpoints the rom all but like these are the ones that take care of the queries there is the query the query range and I think the series as you get this request you don't need to pull out the query the query can be part of the I think it's cool the query parameter or the match so you just pull out the string then given the string you can use the prompt you from prometheus to figure out all the selectors so in our case the reason why it's possible is all the users are associated with a certain project and the permissions will have a project in that so you know that this user is allowed to see project as a secret project and also all the metrics that are coming from the simulation will have the project as a part of the label so looking at the metric we can say that this metric belongs to this particular project and we can then look at the access token to figure out which projects the user can see so this is what kind of clues this all together so let's get a couple of examples so request number one you want to figure out those splines reticulated for project gain your permission that you have inside your access token says that yes you can read anything in project game then we don't need to rewrite anything it's all fine now books on number two you try to do something a bit more interesting you say i want this metric for all the projects that start with G now this is more interesting you also have multiple permissions you can see project G a G B and a a so what happens you parse the request with chrome pol you pull out the matrix and vector selectors I think you match all of those selectors that our project selectors on the things that you are allowed to see and because it's a regex you just regex the project name you're allowed to read against the project that's being selected so in this case g and g be our match the regex so they will be inserted as a selector into the query so now the query has been rewritten the user doesn't know that that actually happened because in there they don't need to know that they're in a multi-tenant environment it doesn't matter for them they just say I want everything that starts with G I know I have projects that start with G just give me those so that's how it does the rewriting now the third example where the user requests something they actually don't have access to so again they say anything that starts from G but they only have access to a a and a B now this is kind of one of the design choices we've made theoretically you can say that well you just don't have projects that match this so we can return an empty result and say that there are no metrics that match this query but we thought that if the user explicitly specifies the project that they are looking for all the right of regex it means they kind of know what they're doing so you need to indicate to them that they've made an incorrect request because they don't have access to anything like this the other are a part that also happens is user doesn't even have to specify the project as part of their request because they think that they were they assume they only can see their own metrics which is a reasonable assumption so they just request splines reticulated with no selectors there and then we will inject all the projects that they allowed to see inside that query and they will get their answer the way they expect it to ok so this is the last part the bonus part I've mentioned we do recording rules and we have quite a few of them so this blue line that's not an artifact this I think a screenshot of all the recording rules we have it's about 2,000 of them no one wants to write two thousand recording rules by hand it's really boring really bad idea you got very upset as you do it so and we also wanted to expose this to the users in some way and again you don't want to make sure that your users know what's your standard for writing those recording rules what are the conventions with the double colon it's not great and also the syntax itself is not super user friendly for kind of people who are new to the monitoring so we decided to write some kind of code generation the other thing that makes it possible is for our particular use case there is a pretty small set of patterns that we want from every metric we usually know what kind of things you want to be recording so how do we do this some kind of templating system some people think it's a good idea others really don't like it I I'm kind of in between I think so how should it look so say you want to record the metric for splines reticulated you should be able to write something like standard gauge and then the things you want most of the time and these are the things well that will get generated for you is average some min max etc for let's say counters normal things you often want to other rates so we generate those and now the issue is we decided to implement it in am for this is maybe not the best decision ever maybe but the interesting thing about is it took about two or three hours to write this and it has not been changed for about a year there might be two reasons why this has not been changed one other everyone is terrified of it that could be the case or it actually covers the use case i don't know but also it gave us really good data about the fact that users do want these metrics they really like it they really want it so it was kind of a cheap way to iterate tonight and figure out do we actually and it looks like we do and obviously you should change it and a convenient way to do it is may be exposed some web UI for the user to say actually add this particular metric to the cogeneration behind the scenes and I just want to see it so next part is a little bit scarier think yeah there we go so this is the actual implant implementation some bits have been removed but yeah you can see it's pretty obvious you can read through it you don't need any knowledge of em photo actually understand what's going on Frida maintainable as well yeah so this is how we do the templating for record your rules that's where 2,000 recording rules actually come in yeah questions questions when you have all these proxies what's the kind of effect on light and safer is a request I don't have a concrete data but I don't think when we added the proxy and none of the users have explicitly complaint saying that it's getting too slow and also so yeah I don't have the particular numbers but I don't think it has added much because the only two proxies that as you get your data there are only two proxies that you go through and they are also not chained so first time you hit the graph on when you go through one you go through a proxy and then as you hit the prom guard you go through a different proxy but the request still originates from your browser so it's only one proxy per request so it wasn't too bad we didn't notice in significant impact there the prom guard is that free software looks quite interesting so we we want to open source it we haven't the problem with the open sourcing it is there isn't actually that much logics in it because we just use the prom ql to parse the query and then it really depends on how you do the oath what's your permission model so our implementation is really tied in to our permission a model so i guess we can open source it so that the users can insert their own kind of oath rules into that it will be like changing a very small project i think the whole prom guard the logics that deals with that is i don't know like 15 lines of code somewhere there like it's really small it was really convenient to write it using the prom ql but a lot of people have asked about it so i think we'll work on just open source and us another question as you might have things running on your systems that take quite a lot of resources of the system and you have quite a lot of metrics would be interesting if the impact of the instrumentation under these circumstances is measurable do you have any numbers about that I don't think the instability so it depends which parts of the instrumentation so I don't think the proxies will actually care about the load underneath because they just they're stateless they just inspect the incoming requests so that thing it doesn't really matter there we do want to we're actually talking to Prometheus developers yesterday about this we want to add instrumentation for some of the user metrics to track the cardinality because that's what causes quite a few issues for us so we want to track that want to alert that on that maybe we don't know this is something that's still coming up any other questions Thank You demon thank you very much