[Music] foreign [Music] [Applause] So today we're going to talk about slow queries and in particular we would like to share with you some lessons learned building some query acceleration techniques shared between Griffin Mir and Thanos my name is Marco I'm a software engineer at graphene labs hello my name is Susanna also a software engineer at grafana now this topic is not new actually three years ago Tom was on the stage of kubecon North America to present what I think at that time was the first queer acceleration technique in cortex which is the range query time splitting we will share in details later on how it works and this talk is a sort of follow-up so you know fast forwarding two years later what's the current state of the queer acceleration techniques we use in systems like grafanimir Thanos and cortex as well now to understand these techniques first of all we have to understand why a query could be slow and this talk is intentionally oversimplified we just want to focus on the key Concepts and we are just going to focus on one single query what's probably the most common query the sum rate of a metric so when we run a query in Prometheus graphene or Thanos the first step is always to parse the query and in particular we want to find all the instant orange Vector selectors so in this case for this query we have the range Vector selector metric namespace SQL app then given our range Vector selector we want to get our label measures in order to find all the matching series all the series matching these labeled matches so the way this is lookup in storage is first we will look up all the series matching name equal metric then all the series matching namespace equal app we intersect the two sets and the result of the intersection will be our matching series then for each of our matching series we want to fetch the samples within the queries time range so in this case our samples will be our data points our value timestamp pairs such as we have here uh once we fetch all of our samples we will process them so we will run the query in this case we will run the sum rate over all the samples of our matching series and just to note that this process is done sequentially because from ql The Prompt QR engine is single threaded once the query completed we can send the result back to the client which can be grafana or princess of Prometheus UI where is the time actually spent you know in this process well the answer is it depends it depends on the query you run it depends on the label matches whether it's a high cardinality query or not when I talk about icardinality query which is something you will hear a lot here in this talk I mean the number of the matching series of your label matches like in grafana and we consider a Nike card analytic query anything above 50 100 000 matching series and it also depends on the system you use like in Prometheus all your historical data is stored on a local disk if you run Griffin mirror Thanos typically your historical data is stored in object storage like AWS S3 which has a different latency compared to the local disk but what we see at Griffon Labs you know running mimir across all our customers is that roughly the 30 percent of the time is spent finding the series matching your label matches and fetching the samples by fetching the samples I mean fetching the chunks which are the compressed samples and then the 70 of the time is actually spent to processing these samples like decoding the sample discoding the chunks sorry and then you know Computing the rate and the Sun uh so that's why for several optimization techniques and we actually focus more on uh techniques to reduce the time it takes to process the samples rather to fetch this data um I also would like to share with you what we see being um the biggest factors which impact on the query execution time so when it comes to looking up the series which again means giving your label matches finding the matching series most of the time spent is a function of the query time range whether we are creating just one hour one day one week one month the actual label matches you use for example if you use the not equal or not matching a regular expression they are less performant due to how tsdb Works compared to the equal or matching regular expression and also the total number of series you have in tstp uh why uh well because the the rule of thumb is that the more serious you have the bigger the tsdb index of each block is and typically the bigger the tsdb Hicks is for many um label matches uh it's a little bit slower to query the index but when it comes to fetching the actual data and processing the samples what we see is that the time spent is mostly dependent on the actual number of matching series whether you know it's just matching one single series or one million series that's completely different and the actual number of processed sample I would like to focus on process sample because that's actually what we see being probably the biggest impact what do I mean by process sample let me show you through an example we have our sum rate query and we run this query as an instant query which is evaluated at a given point in time let's say this query match a 10 000 series and we have a time interval of five minutes in the query and again let's do another assumption that you configure Prometheus or the agent with 30 seconds scrape interval so over the five minute time interval you have 10 samples the total number of samples we have to fetch from the storage and process is 10 10 000 which is the number of series multiplied per 10 10 samples per Series so 100k now let's run the same exact query but as a range query when you run this as a range query we have to compute basically we have to evaluate this query for every step the step is basically you know um for each timestamp in output of your query now if you run this query over the last six hour in grafana the number of steps requested by graphene is a thousand and 400.

so since each sample needs to be processed multiple times basically within the time interval falling within the given step five minute time interval the total number of samples to process is actually the previous number 100K multiplied for the number of steps we have in the query which is a thousand in front so it's 144 million samples to process which is way bigger than the previous one right again it's very fast to run to process 300 million samples in prom ql but sometimes we we see some wild queries um and just as an example uh if you instead of five minutes uh you put the bigger time interval in your range query the number of samples to process just linearly increase if you set the five minute you put 24 hour the number of samples to process to iterate over over and over it's 41 billion and what we see is that the time it takes to process the sample need nearly linearly increase with the number of samples to process so that's something to keep to keep in mind uh when you're going to run your next range query so in uh it's actually very easy to write hyperdenality queries so as you see queries that can touch over billions of samples and we as users expect these results to be quite fast we want them fast so in this section we will be talking about some of the fast Square performance techniques that are used in refer many mirror and antennas so let's assume that you have a dashboard with a panel with this range query over the last six hours so when the panel loads for the first time it will execute the query and it will have to process all the samples from the six last six hours up until now but then we can store this result in cash however due to possible delays on the remote right it's we never actually cache the last 10 minutes of data so that means that the next time the panel refreshes we will run the query again but this time only the last 10 minutes of data the samples corresponding to those 10 minutes of data are actually processed by The Prompt ql engine the the rest of the the remaining remaining portion of the query can actually be fetched from Cache so this means that with this technique we are able to quash query results and and get them subsequently so for instance if we have an incomplete to cash cash query result such as these disks we just need to calculate the required partial query in this case the last 10 minutes and execute them separately then we just need to merge the result back with what's in Cache and send it back to the client so these are some of the our benchmarking results that we see in grapheneck Cloud for all of our customers in production and we see that around 40 percent of our range queries are cachable of this 40 we have a 92 percent cash it ratio with around the five millisecond cash lookup latency so this technique is probably the best and the most effective optimization possible because basically we are just fetching the results from Cache we don't need to run the codes and all the Futures requests will be a lot faster but the thing is that this doesn't apply to first-time queries so a query can only benefit from this if it's a Time range interval was already processed by a previous query uh it also requires queries to have the start and the end alignment to the step this is because as you know time is a moving window so we need to store it in store the query results in fixed time blocks in order to increase the cachability of the results and this is actually the only way that we can store the the results with the accuracy but if you use grafana you don't have to worry about this it does automatically for you again the last 10 minutes of data are never cached this is because it's a these are push-based system so it's always possible that with the remote right we we don't have the most up-to-date data right now and it only applies to range queries so let's now assume that we have the same query the same range query but instead of the last six hours it's spending the last three days so basically it will have to process the samples from the last three days up until now and this is a very simplified overview of the of the steps that that we showed previously on the query execution workflow and first we will pass the query then we will process the query sequentially and we will send back the response so we cannot and we don't want to paralyze the the first and the last steps but we are able to paralyze the step that takes most of the time so processing the query and this is how we can do it so we can we can split a multi-day query into multiple partial one-day queries that can be executed in parallel so in our case we will still pass our query our three last range query for over the last three days we will split it into three partial queries each one with one day time range interval all of these two partial queries will be executed in in parallel concurrently once they all finish we merge their partial results into a single result which is then sent back to the client and as you can see here from the graph below it's the execution time is reduced significantly so some of the again benchmarking results that we see in grafana cloud is that around 10 percent of our range queries get split by time of this multi-day range queries we see that on average the time range interval is seven days and we also observed the query latency reduction on of 75 on multi-day Range queries uh so this this technique and like with the query results caching it works with any range query including first time queries however it only applies to query spending multiple splits intervals in our example it was based on one day but this is configurable so this means that if we get a query with with a Time range interval of less than one day we will not be able to split it and again it only applies to range queries so what about instant queries it's also very common for us to have the instant queries where the instant Vector selector the time range on the instant Vector selector is quite high so in this case the value in vectors in Brackets the 24 hours and in here we have a slightly different query than the sun right we have some some Max over time and I'll explain you why in a couple of slides but here the process is exactly the same so in here we will have to process to last 24 hour 24 hour samples of data up until now and we we have the same sequential steps so how can we paralyze these steps um the the processing of the query in uh sequentially uh in mimir we recently introduced an experimental feature um where we are able to split an instant query into multiple uh partial queries by using the offset operator and so in our case we have the the sumex over time over 24 hours and we can split it with it with a split interval of six hours into four partial queries each one composing the last the six hours interval of six hours so this means that the first for instance the first partial query from now it will take into account the last six hours of data the second second partial query will go will do an offset of six hours and then look at those six hours and so on so forth and we can see that again we are able to paralyze all of these partial queries in parallel uh similar to what we see with what we saw on the Range query with time splitting however there's a limitation to this to this feature that affects the correctness of rate increase in Delta and this is why we use the max overtime instead of the rate so this is because the composibility is not guaranteed property in from ql so this means that the rate over the last 12 hours is not the same as the sum of two rates over the six hours because basically the the two samples uh the two intervals the difference between the two intervals is not taken into account so for instance just to clarify in this graph if you if you look into the first interval between the zero and the four hours it has a very slight increase and the same happens between the six and the 12 hours however if you look at the sample immediately before the six hours in the sample immediately after the six hours we see that there is a very high increase that is not thinking into account when when we do this this sum over rate now the techniques we've seen so far um are all about partitioning the workload by the time dimension um but actually there's another dimension we can partition on uh which are the series um and that's basically uh the main idea behind query sharding or I think in tunnels is called the vertical query sharing and the idea of query sharding is to partition your query by series so we have our sum rate query and this query can be split into multiple partial queries in this example three shards but we can run with any number of shots and each partial query process a different subset of the series like with three charts each partial query process roughly the 33 percent of series each these partial queries are again executed concurrently and then we rerun an aggregation on top of the result um we keep showing you know the sum rate but this works with the some by labels some without labels minimum maximum we also made it work with the average we basically we write the average as a sum divided by count so we slightly rewrite the query to make it work but that's basically the the main idea uh behind query sharding now query sharding works both with instant and range queries and we are using it in production at grafana Cloud since several months what we see across all our customers queries is that 68 of the queries are actually shardable um the reason why there are some queries which are not shoutable is because not every single promptual function can be shorted I don't know standard deviation we can short it um I mean we can't push it down um among the high cardinality queries which are sharded we see a query latency reduction between two and access and the biggest gain we see is for instant queries with large time durations in the vector selector and the reason is basically because this technique is very effective when it comes to parallelize the samples processing so how do we do query time splitting and query sharding come together well it's actually quite simple we can combine these two techniques and use them both in instant and range queries we'll look up for the range query example just for for Simplicity so basically we will first split our query over the range interval of three days so we will split it into three partial queries and then this each one of these partial coils will be further split into multiple partial partial sharded queries in this case with the sharding factor of three we will have three queries for each day so this means that in the end we can split this query into nine partial queries where each one of these special query can be executed in parallel and all of them will be looking into specific parts of the data I mean this story uh you know we just shared the key ideas of three main techniques we use uh both in mimir and Thanos there are some implementation differences between the two projects but you know the the idea uh is shared between between the two projects um both projects and you know other projects we didn't cover uh use other techniques as well I don't know both my ventanos have several layers of caching tunnels support down sampling as well if you think about done samplings yet another way to reduce the number of samples you have to process in your career that's all about it um or in mimir when it comes to query sharding we also Shard at the storage level so basically we are able to map the shards into different tstd blocks so when we create a given shot all the series for that shot is in a dedicated tstp block this is an optional feature we have in vimeir what's about the future well both communities have a bunch of ideas um so keep watching because bartek and Philip will will share something very cool next day I would like to share a couple of ideas that we keep thinking about in the mirror we think that you know with the experimental instant queries pre-result time splitting we may be able to Cache the partial the result of the partial queries if we align them to the time boundaries another idea we are talking since a long time and sooner or later I will build it is a sort of query planner basically I would like to collect statistics about how the metrics we ingest and the queries we run and then use these statistics to better plan the query execution like how many time intervals to use how many Shard which technique should we use in order to squeeze out the performance of the query and that's all um I know this talk was a little bit you know concentrated so if you want to get the slides and watch them again offline there's a QR code so I'm I'm guessing there will be questions who's one here okay hello uh thanks for the talk I was wondering now that we will have out of order samples support we will that change the caching and yeah yes it does um otherwise support is a computation for cashing um right now it's not handled very well um so the the act or the workaround is that if you configure out of order time window to an hour you can configure mimir do not cache the last hour of data this may work if you have you know a short out of order time window like 10 minutes one hour two hours maybe it doesn't work if you want to ingest any sample with any timestamp for real um what we are brainstorming but we haven't built yet is a caching validation when we ingest out of order samples the typical use case we see with auto-order injection at least given our customer use cases is that the stream of outdoor border samples is not constant over time we're not just ingesting you know one millionth of out of order samples per second sometimes we have some spikes in how to order ingestion and then for a few hours there's nothing ingested out of order so you know caching validation may work but yeah that's a good question we should have included in the slides we will update the slides thanks all right more questions I'm at the back here and do you think these techniques are only useful for things like Vermeer or would they be useful or possible in Prometheus itself [Music] I'm not part of you know Prometheus maintenance so I can't speak for for Prometheus um my experience with Prometheus is that so far they've been quite focused on getting Prometheus you know working very well but running the query as a single core so basically not scaling out the query execution either to multiple cores or even as a distributed system on multiple machines [Music] um uh the query result caching is implemented in a dedicated component both in cortex linear and mimir which is called the query front end and the query front end both in cortex and nimir can run as a standalone component that you can run if you want in front of Prometheus so basically you instead of running your your queries against Prometheus you run your period against it the creative front end and then the creative front end around the query against Prometheus catch the result and returns back the result um quickly shutting or uh yeah pretty shouting you know it's something which is you know deeply coupled with how both member and tunnels work so it's not something you can get out of the box from Prometheus hi thanks for the talk my question is about this Kodi shot factor in memer particularly uh can this short Factor be dynamic currently like uh can mimir figure out based on how many workers available and figure out the short Factor dynamically or is it like static where you have to go and configure yeah good question this is dynamic and the main reason why it's Dynamic is because when you both apply the time splitting and the query charting you may end up with an explosion of partial queries so think about instead of running the last three days query you learn the last year period so split one day queries 365 players and then maybe you also shot by a factor of 8 or 16.

so you have thousands of queries to run I mean it would be even way slower than just running the query as is without any optimization at all um so we we dynamically compute the number of shards to use based on right now based on the time range of the query and the portion of the query which have not been cached yet so if you run a query I don't know over the last three days and the last two days of query are actually picked up from the cache the LA we have just run the last one day of query so we can run at full parallelization um with the query chart but if the last three days of queries um are not cached we reduce the number of shards for each daily query so that the total number of partial queries we run is nearly constant um however we can do better and that's why I'm quite keen on the query planner because the missing information we have right now is before running the query knowing how many matching series The query will have is going to be an eye coordinated query or a very low calorie degree well unfortunately we don't know yet um and that's why I would like to know because we would be able to take better decisions before running the query so like this like you know picking up the right number of shots in order to get squeeze out the best performance without overloading the system you know with an over scheduling of many many queries you just don't need all right anymore okay I'm not seeing anyone thank you [Applause] [Music]