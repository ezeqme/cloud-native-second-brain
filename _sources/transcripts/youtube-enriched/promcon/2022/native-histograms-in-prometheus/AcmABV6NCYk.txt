[Music] foreign [Music] I'm going to speak about the new native histograms in Prometheus I am Ganesh vanekar I'm a Prometheus team member and I work as a software engineer at grafana labs before starting work to see what are native histograms let's see what are histograms first histograms is a distribution of all your observations in specified time ranges we call them buckets in this particular example the First Column tells you there are 15 requests that took less than 0.1 seconds similarly there's a blue column tells you like 25 requests were between 0.1 to 1 seconds and last bucket is a special one which encloses everything that was more than two seconds and this is how we store it in Prometheus right now we have a separate time series for each individual bucket and the last book and we use something called the Le label which means less than or equal to and all the time series are cumulative for example uh if you look at the time series which says L E equals to 1 it says 40 and not 25 which means it's everything that's less than one so if you want the count between 0.1 to 1 you have to subtract 40 with the previous time series and we have two more time series for this which is the overall count and the overall sum of observations so this is how it is done in Prometheus right now but this is how Native histograms will be stored it's a single data structure with everything that has to do with histograms uh we will take an exam I will show an example of how the native histogram looks like but to go over these very briefly the histogram has count and some like before it has a schema which defines uh what the bucket boundaries will be the 0.1 seconds and one second all those things we call as bucket boundaries and yeah schema defines what the bucket boundaries can be the positive spans and negative spans are just something which tells you which buckets are filled and positive spans are for positive observations negatives fans are for negative observations and positive buckets and negative buckets hold the actual observations in them we'll ignore the other two parameters for this talk but now let's see what how do we store these native histograms let's start with schema because that's where everything starts so with schema we get something called a ratio who's formula is 2 power of 2 power of minus schema we'll see why this 2 power of 2 power is uh required later and this ratio defines uh what's the ratio between a bucket boundary and the bucket boundary of a previous bucket for example if the bucket boundary of the current bucket like the upper Bond was one then the upper Bond was of previous bucket would be the one divided by the ratio and schema goes from 8 to -4 and the lower the ratio higher the resolution of the histogram like the jump between two bucket boundaries is very small uh we'll take an example we'll see how this schema actually works like if you just look at the numbers it's very confusing let's take scheme of zero which gives us the ratio of 2 and the bucket boundary of 1 is where it all starts so to get the the next boundary you just multiply it with the ratio which is two so you get two and then four and then eight and so on and if you want to lower the resolution it means you have to make the buckets bigger so we go to the schema minus 1.

and the ratio becomes 2 power of 2 which is 4. if you look at the first number line and the second number line it is similar to just merging consecutive buckets together and you get the new bucket boundaries for the new histogram similarly you go on reducing the resolution you just merge the consecutive buckets together let's see in the other direction where we are increasing the resolution of histogram we again start with the ratio 2 which is schema 0 so you have the bucket boundary one and two when you want to uh increase the resolution the ratio now becomes 2 power of 1 by 2 so you just add another boundary in between those two buckets and now you have two buckets uh between the boundaries one and two if you want to increase the resolution again you just add another boundary in between the previous buckets so that's how it goes on uh we'll see why this power of 2 is required later like this power of 2 keeps the ratio between each boundary is same and now because for a schema the bucket boundaries are predefined now you can give an index for every bucket we start with the index 0 for the bucket which has the upper bound of 1.

and we just increase the number on the right side decrease the number on the left side are you all with me here right now okay so now that we have built up this knowledge base of how we index the buckets let's finally take an example of a histogram this histogram has a scheme of zero which means the bucket boundaries jump with a ratio of two ah and uh you see there are four buckets that are filled then there are two buckets that are empty and then there is another bucket that is filled so we have schema 0 count equals 11 and sum which is very trivial so now the let's look at the positive spans if you look at the slice of positive spans it has two things zero comma four and two comma 1. so the zero comma 4 tells that the first bucket starts at index 0 and from there there are four buckets that are filled so zero comma four and then two comma 1 tells that after the previous set of buckets there are two empty gaps in between and there is another bucket field so that is how encoding of these buckets work if there were no gaps then you just start with the zero and the zero or whatever index the first bucket is and the length of how many buckets you have and that precisely uh gives you the layout of bucket because with the help of this index and the schema you can tell what are the bucket boundaries and then we store the actual values itself we Delta encode which means if you look at 2 and the second bucket is one which means the Delta is -1 so we store 2 comma minus 1 and so on it's a Delta with the previous value so this is how we encode histograms and all the time series is now combined into a single time series uh all this time Prometheus just stored a float value with the time series The timestamp was end the value was float now we are overhauling the entire storage to support this new data structure where it will be a single time series and the value stored here will be this complex data structure that we saw right now and coming to the significance of the powers uh the 2 power 2 power of minus schema you can take any histogram and decrease its resolution by combining the buckets if you look at the first diagram there are four buckets and which is of schema 0 and if you wanted to reduce the resolution you would go to schema minus 1 which will make the ratio as 4.

and for that you just add up the buckets and create a new histogram so one plus three got added to four two plus four got added to six and now you have the new histogram which just has a lower resolution but the same observations if you want to decrease the resolution further you do the same thing why this is important so if you have a histogram of a one schema and you want to add or do arithmetic of another histogram which has a different schema it is similar to having two currently it's similar to having two histograms of different bucket layout right now you cannot combine them or do anything between them if the buckets are not compatible right now but with this native histograms you can lower the resolution of a histogram which has a higher resolution and then combine it in this example on the top left it has a higher resolution compared to the histogram on the top right and I have color coded such that they are all the on the same side like the blue columns on the left side are both merged together so 1 plus 3 and that becomes the a single bucket in the lower resolution and you add that up with the histogram on the right side and you get the new histogram so now you can do mix and now you can mix and match different histograms of different resolutions by converting the higher resolution to a lower resolution okay now that we know about Native histograms how do we actually instrument this so from client golang 1.14 onwards which is not out yet but I think it will be out soon or you can use the main branch of client golang you just remove the buckets field in your instrumentation like you don't have to care about defining your buckets anymore and you add a new field called native histogram bucket Factor here I have given the number of 1.2 let's see what this is it refers to the ratio here like you don't have to precisely choose the ratio or the schema you just have to give a rough estimate of what's the ratio you want between two consecutive buckets because we chose one by two the closest schema which comes to this is schema number two which has the ratios ratio of 1.189 so the client will automatically choose schema 2 for you and it will build the histograms with the schema too but with just this uh if the number of buckets can grow indefinitely and it can just take up a lot of memory so there is another production here you can Define what's the maximum number of buckets you want to store so what happens if it goes beyond 150 buckets the client will just switch to a lower resolution so let's say you cross 150 buckets with the ratio 1.2 which shows the schema uh uh sorry which shows the schema 1 it just switches to schema 2 where the ratio is bigger 1.414 and hence you get a lower resolution but you don't blow up the memory it just goes to a lower resolution there are few other uh instrumentation options in this which are also considered before going to a lower resolution but we won't discuss it here okay now that we have instrumented our code how do we use this in Prometheus so Prometheus adds a new feature flag called native histograms so what happens when you enable this is Prometheus will negotiate protop of scraping as the primary thing with the target and these native histograms are only served as protobuf because they are more efficient this way we are still exploring ways to expose this as a text format so not only as the native histograms served in the protobuf all your metrics will now be served as protobuf yeah so this is how you enable it in Prometheus and it will just automatically start scraping native histograms for you and store them in Prometheus so how are these native histograms better it gives you more affordable partitioning so what is partitioning here for example if you had HTTP requests seconds and you wanted to have a breakdown time series into status codes for example has to be require seconds of status 200 then status 500 that's called partitioning so how is it more affordable right now so previously you saw that uh one time series was created for every bucket so it creates coordinative problems it takes a lot of index space now the index space is reduced quite dramatically because let's say you had 15 buckets all of the 15 or 17 time series are now converted into a single time series and because we can store all the histogram buckets together it gives you a better compression so it takes less space though you have way more buckets than previous and you saw in the span representation like how you represent the buckets it is very concise it's a sparse histogram so there are zero cost for unused buckets for example previously if you had defined 15 buckets whether it be a status quo of 200 or 500 it will consume all the 15 buckets but now if for example you have a histogram for 500 requests and let's say you are only facing a timeout request like timeout problem then only one bucket will be filled for the timeout and you won't pay for all the other buckets that are not used and now because you can control the accuracy between the the ratio between the buckets and that now we don't have the plus infinity bucket uh your quantiles will be more accurate than usual most of the time and because of the power of two example that we saw like addition of two histograms now you can aggregate histogram across different label space or across time without worrying about they being incompatible and the configuration is very easy I myself I have found myself like I don't want to define the buckets because what if I need a different bucket later so that anxiety already always remained in me so I'm very excited that the configuration for this is very simple you just Define a number and you have all the buckets filled uh that you want but there is one con here there are no predefined buckets like you just give the schema and the client will Define the buckets for you so it's possible if previously you defined a bucket exactly for the SLO that you are defining uh that will give you 100 accuracy for your control estimations here there might be a small error it's you cannot predefine it but let's see our goals Milestone and what we are planning next I think I I have some time so I'll go over them in a little bit detail so phase one which is done right now uh is we can scrape and store the integer histograms when I say integer histograms uh uh the bucket values are just the count which are integers you cannot have float but when you query like if you take a rate of histograms the the bucket values can be float though we can represent them in the queries we cannot store them right now and you have basic promptical support which Beyond is going to talk more about and alerting rules will work but if you are using uh if you are templating your alerting like annotations with the value of the query and if the query produces native histograms uh that will have the float value of zero so you can't template native histograms properly right now and you can have recording rules right now which produces float for example if you take a quantile of histograms it's going to give you a float value but if the recording rule produces a native histogram right now you can't store it because that's a float histogram which we don't support it and remote right is supported for these native histograms but I think remote storage is hard to catch up on this development first right now client golang supports it and client Java has a separate branch called Fast histogram because supporting this breaks quite a few things so if you want to try it in Java you can go to The Spar system branch in the client Java repo so phase two the thing that we are working on right now we first want to add support for float and gorgeous histograms Gauss histograms is like the bucket values are not a counters uh a float in the sense like the bucket values can be float and with some extended from Kill support now the adding support to for float histograms will automatically add support for recording rules that produce native histograms like recording rules will produce float histograms and then we can store it that's the number one priority that we are working on right now and there are few features that we left out of the initial iterations like Federation in memory snapshot during shutdown and autofortal ingestion those don't support native histograms right now there are few extended goals I won't go into much details like it is possible we can do efficient counter detection because we give space to save some metadata with Native histograms where you can jump a lot of samples and make queries efficient by not checking every sample for counter detection account reset detection and and we will have graph view in Prometheus UI which is mostly going to be heat maps and some other things and support in lots of other clients and we hope to add custom bucket boundaries in these native histograms okay so native histograms will be in version 2.40 but when is it coming out the answer is no so I'm actually doing a live publish [Applause] yay so we so you can use native Instagrams right now uh the client go like 1.14 is not out so until that is released you have to use the main branch of client golang and you can try it out yes yeah really oh nice okay so client golang 1.14 is out okay so that is it for my talk thank you for attaining this I think we have two minutes for questions before Beyond can take over yes you can use the mic you said that you already have support for prompt care for new histograms but you didn't show how to uh use uh promkeo for new Instagram yes that will be next talk by Beyond hi hi uh how does the native Instagram would look like in uh text format like when we do slash metrics right so it's not available in text format right now it is only served by a protobuf okay okay so you have to like Prometheus when you enable it in Prometheus it will negotiate protograph to you it will negotiate protocol for you and it will scrape the native Instagram so I have Proto buff uh maybe if you can just do a get request for product of you can see product but I don't think it's it will be readable okay at least for now thanks okay okay I wanted to ask like about the number of buckets you said there is like you can impose a limit but would will there be reset like between scrapes so whenever yeah good question so there are there were few parameters in the client that I didn't talk about I think you can set how often do you want it to be reset so that you can make space for uh new buckets for example if your Precision got very low because of lot of buckets I think you you have option to reset periodically for let's say every half an hour or one hour you reset it so you start the histogram fresh so it's that inclined um when you switch the resolution um automatically do you have to compact the spans inside the data structure somehow in the background so can you repeat that again when you switch the resolution between the spam um because the client says you are above the 150 buckets that you said you have to compact on the server side the spans Okay so I will go to this one so when the resolution is I think your question was in the client when the number of bucket limit is crossed how does it change to a lower resolution is it when you switch it on the client side you set the new resolution to the server so on the server side you have stored the old resolution right yeah so the resolution is part of every histogram like the schema tells you the resolution so Server doesn't need to worry about what the client is doing if the client sends a particular resolution it will just store that resolution if the client decides to change the resolution for the next grip the Prometheus server will happily just ingest that resolution so changing the resolution is decided by the client and not the server uh did you consider using uh parameters Texas position format for new histograms because I think that part above format isn't very convenient comparing to Texas position format from user's perspective and many exporters premises experts don't support part above format for exporting so I think that it would be better to add support for new histograms for position format okay I don't think I understood that question very well uh can you repeat the question the question was did you consider using uh Prometheus Texas position format for new histograms parameters expression yes so we have something in progress to add uh you can say experimental text format in open metrics which which will be a very simple representation of the Native histograms that's the thing that we are looking at next to add a text format right thank you [Music] [Applause] [Music]