[music] Now Fiona is going to tell us about Hotel Delta Temporality in Vegas. >> Cool. Uh is is the mic working? Checking. Yeah. Okay. Awesome. Uh yeah. So, hi, I'm Fiona >> with a mic >> like this. >> Yeah. >> Okay, cool. Yeah, I can hear my >> That's what the mic's for. >> Okay. >> Yeah, I just need to press it to my mouth like here. Here. This. Good. Okay, cool. Well, let me know if it goes wrong. Um, but yeah. Okay, let's start again then. I'm Fiona. I'm a software engineer at Grafana Labs. And yeah, I'm going to be talking about Delta temporality support in Prometheus. um today. So um let's first uh define temporality. So this is how additive data types like counters are expressed in time whether uh values incorporate previous measurements or not. So just as a bit of aside when I say counter here I mean something that records something like the number requests. So Prometheus cost is a counter and open telemetry calls like the closest equivalent a sum. Um but I'm just going to be using counter to refer to both Prometheus counters and hotel sums from uh the rest of the talk.

So in the hotel world uh a sample has two time stamps. It's got a mandary time stamp uh which is of when the measurement was taken just the tiny Unix nano and I'm just going to be calling that the reported time from now on. And it also has an optional start time stamp which indicates when the measurement started. Um so we're just going to call that but yeah start time and uh cumulative temporarity hopefully we all know this we all love this right um because this is what Prometheus supports today. So in cumitive mode this means each reported value is the running total since we started measuring and all the previous values are incorporated too and with the hotel start times this means successive samples share the same same start time until there's a reset like you can see how in this example all four samples share the same start time t0 and then you see at t time t1 the value is one at t2 it's five which means between uh t1 and t2 we've added uh four samples. No, sorry, not small samples. Um account form.

Um and in graph form, the samples will look something like this if you draw them by their reported time. Um in the case of counters, uh you want usually want to see how much the count has increased in a time range and maybe get a per second rate. So with pumitive ones you find the increase by taking the difference between the start and end values in the range. But one thing to take into account is that counters might not just keep going up up forever. So we might start taking measurement when the application starts right but then it restarts and we lose all the d well the inmemory data. So at this point a cumulative counter would reset and it will start from zero again. So if you want to work out the sum now, you can't just subtract the first sample from the last value. You've got to sort of uh detect the resets and treat the range as multiple segments, get the diff segment and then add them up to get the total value. So if you got start time, it's fairly easy to detect when a reset has happened.

uh if the start time from the current sample is greater than or equal to um the reported time of the previous sample. Um Prometheus counters uh by definition are cumulative. So and they also have to be monotonic. So the counter should only decrease in value if a counter reset happens. Um each sample only has a single time stamp. Uh the time at which the value was reported. Um there is also the created time stamp concept that's been mentioned a bit before in a few talks. Um but it's not stored as part of each sample. Um so to detect resets, we can't use the start time for now. Um instead um discounter functions like increase and rate detect resets by checking if the previous sample is um is lower than the current sample. So this isn't foolproof. we can miss resets if the counter increases really really fast after re uh starting. Um but yeah, so um yeah, the process we just described with all the pretty grass and stuff, that's basically how Prometheus does the um does the increase calculation. Um though the increase function also does some extrapolation.

Um so unfortunately the first and last time stamps in a select range might not match with the boundaries of the time range we're querying for and this is this is fairly frequent I guess um how Prometheus deals with this is that it extrapolates to guess what the values at the boundaries are. So we want to do this because otherwise if we just take the diff from the uh first and last value it will be an underestimation of the actual increase. Um yeah so and then when you're looking at the disc itself and at the extrapolated points um you take them into account and you then do the summing and stuff. Cool. Um so finally actually onto your delta temporarity in some respects. So with delta temporarity uh the reported value for sample is the change since the previous reported value. So it's supported by hotel um but not really by Prometheus. Um in OT case this means that successive samples wouldn't have the same start time. Uh start times are exclusive uh by definition. So in this example for example you can see that the samples don't have overlapping uh intervals.

So finding the increase of a delta counter over range is different uh to counters since all the sample values are independent. um each sample here with a delta um sample just contains their increase within their interval. So one way to define the increase is you just sum up all the samples in the range which is which is fine. There is a consideration though that is the result might not specifically be for the selected range. So like I said uh each delta value tends to be collected over interval. Um I drew some I don't know reddish purplish lines to try and show the interval for which each sample was uh collected. And you can see how the interval for the first sample is not completely within the range. So in this case it means that the actual increase that you're returning isn't quite um what you requested. Um whether this is good or bad um it depends. It depends on what you want the increase to mean. Um there are also methods to um sort of like extrapolate or interpolate to just get the proportions of the samples within the range you want.

So um so yeah now onto the I guess the hotel compatibility side of things. So Prometheus does have that industri um but initially and by default it rejects deltas. Um and there are downsides to using delta temporarity but but there are also use cases like batch jobs serverless and of course um the just being able to uh inest stuff from support more things from hotel. So for this it would be good to be able to support deltas in some form in Prometheus. So the first iteration of a delta support in Prometheus was to convert delta metrics uh to cumulative on ingest. So the converted cumulative version is stored. Um the conversion is just done by keeping a running total. So when a new delta sample arrives, it's added to the total and then the resulting value is forwarded. There are a few problems with this though. One is it lacks out of order support. So the conversion process requires adding the current sample to the sum of all the older delta samples. So deltas that arrive out of order can't be added without rewriting new samples that were already ingested.

So in practice this means we have to reject uh samples that arrive out of order. Um another problem is that the shape of delta data tends to uh differ from cumitive data. So while delta can be can be emitted at regular points uh in some case like with the hotel SDKs we only emit these points when there is a change in the collection interval. Uh in this example since the counter wasn't incremented between t2 to t4 um those samples are missed and it leads to very irregular gaps between delta samples. Um and this means the converted cumitive data can interact awkwardly with Prometheus counter functions. So for Prometheus if you want to calculate how much a counter has increased um it needs to diff the first and last samples in the range. So this requires at least two samples for a result. Um if your hotel delta counter only updates occasionally you might get empty result when that counter has increased but only once in the selected range. Um this is an issue also that affects cumitive counters in some ways.

Um and the other issue is that Prometheus does the edge extrapolation to cover the entire range but it also tries to uh avoid overestimating when it thinks the series has started or ended within the window and it tries to guess this based on the typical sample interval. Um delta converted series can be irregular. So uh the intervals uh might not be a good way to estimate the start and end of a series. So given these problems, it would be nice to avoid doing the conversion. And like I do have to say I did lie a bit earlier or at least I wasn't quite precise. So if we flash back to I don't know five or so minutes ago, I said uh Prometheus doesn't support delta temporality. Uh what I meant then was support is like in the context of scraping and querying uh semantics. So the standards u metrics pulling scraping path is cumulative only. Uh we can't scrape deltas. Um and Prometheus counter functions like increase in rate are designed for cumitive counters but it's always supported just storing any old values.

So like delta values um the storage itself is type agnostic. So if you push deltas into Prometheus with like remote write um it will happily just store it and then these can be read back. Um so to support deltas ingested via the OTP endpoint in uh Prometheus um we've done a pretty simple thing of allowing them to be ingested natively uh under feature fat flag. So this means instead of being uh rejected or converted into cumin value the delta sample will be written as is. So this is the current state of delta support and it gets around the out of order problems we have with doing the conversion and it does help with the querying problem um but that not that's not perfect. So for the rest of the talk I'm just going to walk through some of the current issues with the querying and uh idea ideas to um improve that. So I said it's queryable but there are caveats. So if you just query for the metric itself you get the raw delta values. That's nice. That's fine. Um but what if you want to get the increase over like a selected range.

So like we said before Prometheus's increase function is designed for getting the increase in the range for cumitive metrics. If you try and query for a delta metrics with increase okay Prometheus will run a function on it. Um but what we'll try to do is we'll try to take first sample away from last sample value and it just will resets in a way that doesn't make sense for deltas. So to actually get the increase over the range for native delta samples you have to add up all the values in the range and you can do this. There's a sum over time function in Prometheus. Note that this just adds up all samples. It doesn't do any extrapolation or interpolation to adjust for any ranges. This does mean that different functions are needed for delta and cumulative metrics. Um and yeah, whether this is a problem or not is yeah, sometimes it might be better to have something that is clearer that these are separate types. Um but yeah uh the problem here is also there's no set way of distinguishing whether a metric is delta or cumitive at query time.

Uh to use a correct function you need to know how a metric is collected. Uh but even that's not foolproof. So when you're ingesting say via hotel the temporality can change in your ingestion pipeline um with things like the delta decumitive process to confuse things. So one option we considered to alleviate the query problem was enforcing some sort of typing. Prometheus does have defined uh metric types like counters, gauges and so on. And when you instrument application, you need to define a type and that is exposed at scrape time. Um, and there are suffixes like total or sum added for counters by convention, but they're not strictly necessary, especially with hotel metrics which have a convention of not adding type suffixes. Um, and also like the typing isn't persisted properly as its own sort of like field and type information isn't really used at query time. Um, this has changed a bit for native histograms, but in general it's not really um, this is like the current state anyway. But there is a newly introduced Prometheus proposal that adds type information as metadata labels to series.

Um so for example counter series it would add a type equals counter label. So for deltas we thought about piggybacking off that. So we could have metadata labels to distinguish between um deltas and cumitive metrics. Um there are a few ways to do this. For example adding a new temporality label with the values cumitive delta. So users then could then see the temporary metric and then choose the right function. And we could also have the query engine check the label and add warnings if needed. But this still requires yeah like I said different functions. Uh separate functions do keep things simple and it allows us to you know adjust each function uh independently based on the different characteristics of each temporarity. But this can also be a bit of a operational headache. For example, if you're migrating from one priority to another, you're going to need to update all your dashboards, alerts, recording rules, and so on. Or if you're ingesting same data, different teams or users, but different temporarilities, you still need to maintain two version of of the same query.

Um, so one way around this is to introduce function overloading so that by having temporarity metadata as labels on the series. So you we could uh extend increase to work transparently with both delta and cumitive metrics. So the engine could check the temporarity label and if the series is cumitive then do what it does today. If it's delta then take the delta path and do some over time. With everything there are trade-offs though. Um that does add engine complexity and the same query could return different results depending on temporarity even if it's the same source being measured. Um because the cumitive path would do edge extrapolation while the delta path would just sum everything in the range. We could try and mimic the cumitive extrapolation logic for deltas but this might not be desirable for delta users and the sparseness of deltas can make extrapolation less reliable anyway. It also creates inconsistencies between functions like so for deltas if we do this then both increase and sum of time will work for deltas but then some if you try a sum of time over cumitive then that would just not give you the right result.

Okay. And then we could then overload sum of time and make it work for uh cumitives like increased does. But would that be more surprising or helpful? And then it goes on and on. there might be more more functions that we had to decide do we want to also have this dual path and overloading. Um and it just adds a bunch of complexity. So for now um we've uh decided against this overloading. Um so the query engine has historically been like type agnostic. The same expression over the same samples gives the same result regardless of any labels. So we want to avoid disrupting that for now. um especially since persisting types on series as labels is still new and experimental. It's not something I guess to discuss whatever maybe like like in future major releases if we get stricter typing and stuff we could revisit overloading. Um but for now it's something where like uh we'll see how far we can get without it. So, so far we have been treating delta and cumitive metrics as sort of distinct types, but really delta metrics are basically cumulative metrics even without the conversion process we talked about earlier.

So, regular cumitive metrics give you many samples of a running total and then they occasionally reset. Um, and but you can think of deltas as a special case of cumitive that resets after every sample. So the graph on the right tries to demonstrate this like each of the blue dot is a delta sample point. The value accumulates till the blue dot and then resets to zero and repeats for the next step. But we have said we can't use increase on native delta samples. This is because Prometheus counter functions identify resets um using drops between uh consecutive sample values. But for delta that's not a very good indicator. For example, uh 0 2 on the slide is um greater than 0.1. Um we know since these are delta values that between 0.1 and two there is a reset, but there's nothing in the data for Prometheus to figure that out. So here's where Otel's notion of a start time for a sample becomes useful. Um as a reminder, sample has two start times with the start time indicating when a measurement started or another way to look at this is this is where the reset happens.

So when ingesting hotel samples into Prometheus um for now synthetic zeros can actually now be inest injected for hotel time stamps. Um so essentially one hotel sample will turn into two samples for the same series in Prometheus a zero value at this at the start time and then the actual value at the actual time. So with that increase would be able to identify resets properly for deltas as there's always going to be a drop between each actual sum values and then you could basically use the same functions uh for delta and cumage counters without any label based function overloading. But if you have delta samples in consecutive intervals, then the start time of the current sample would be set to the reported time of the previous sample. And we can't have two samples at um the same time stamp for a single series. So an evolution of the zero sample ejection is storing time stamp or created time stamp as it's currently known in Prometheus per sample alongside a pre-existing reporter time stamp and that can then be used in queries.

the tighter coupling between the start time and the reported time means that increased calculations can be made better. So the store start time can be used to detect resets instead of just guessing and the estimation for the sample interval for extrapolation can be improved and yeah the resets can't be lost as they could be in the zero injection case. Additionally, this isn't just uh useful for deltas. Uh cumulative counters also get more reliable and it this can also help make native histogram reset detection more efficient. Additionally, it could also possibly allow increase to handle non monotonic counters which are also a thing in hotel. So there are questions on introducing this though because there's impact of basically adding this additional field to each sample on performance and it requires some like benchmarking and testing in real world situations. But it seems like a promising approach and the one we're pursuing for deltas for now. So whatever we do, I I do think that accepting deltas will always add some level of like friction and complexity.

Uh especially if you're trying to store metrics of both temporarity um created time stamp per sample doesn't solve everything for deltas. There are edge cases like missing start times and considerations on whether extrapolation should be the default for deltas or not. And also um it could also affect how down sampling works because you start missing data if you miss delta samples. Um but it's probably the best balance between OTL compat compatibility and and in complexity to Prometheus at least in uh the short term. So we basically got to end the talk now and uh just to sum up everything that um I've said. So today you can ingest OTEL deltas as is with Prometheus if you enable a feature flag um to aggregate them. you'll have to use some over time and divide by the range to get the rate. Um, but remember the caveats on the whole querying process and if you ingesting a mix of both delta and cumitive values, you need your own convention for being able to distinguish them. There are a few new and upcoming features in Prometheus that will also help with improved delta support.

So, in particular implementing the created time stamp per sample. So that will allow delta counters to be treated just like any old Prometheus counters and have functions like increase just work uh without needing any special metadata labels for the engine to read. But adding labels might still be useful um in an information sense. It might be useful for debugging or also roundtpping uh data back into when needed even if it's not a strong dependency on the query path. So yeah, just to wrap up, um want to give a big thanks to everyone who's contributed to the discussions and PR so far. Um I've linked the Delta proposal. Uh if you're interested in learning more and if you're interested in contributing, there is a Delta uh support working group that meets up every couple of weeks. Uh that's in the Prometheus calendar. And yeah, of course, thanks um everyone for listening as well. And any questions? Thank you. And I would say Bjorn already get wired up if you want to. >> Hello. Uh so you mentioned that delta sorry uh the start time optional.

>> Yeah. >> But what's the plan when the start time is >> um give up? No. Um so hello like the the idea I guess that I'm currently floating around is um to set like if the start time is optional um that the start time to the basically the reported time as well which you could do if we had created time stamp per sample. So the idea is that it's like oh if you don't have start time it must mean that the data reporting is for this infinitely small interval of just now and that should when you put that in something like rate essentially what it would do is do a sum over time. Um yeah possibly with some adjustments. Um that's the sort of general idea anyway. >> All right. Any elders questions? Oh yeah, there's one. >> Um, hello. Um, thank you for the talk. Um, I'm just curious. I haven't followed this all delta versus cumulative work that has been going on. Thank you for the uh, you know, for the summary. I was curious about what's the advantage of storing deltas versus uh, you know, converting from delta to cumulative at inest time.

So what's the advantages of >> yeah of storing delta directly instead of just converting it. >> So I guess um if you uh if you if you convert them at ing inest time the the big problem is you can't support out of order data um because when you convert you need you depend on all the old data. So when you have the new data then that's not there. There's also some like like I guess possible usability concerns because um you you write data and it's not quite the same as the data you write and that can be confusing. Um yeah, >> any others, one more. Okay, one last one. >> Thank you for talk. uh when when you mentioned the the approach for the uh increments uh basically like in in Victoria metrics there are now three implementation of increments if I remember correctly uh and the fourth one is the Prometheus or like that's that's implemented in Victoria as well and like neither of them is doing the interpolation so like basically what you are suggesting is to to make the fourth option to to make the increments And uh in my opinion when when you would do that for every query uh it would simply cost too much on on CPU like maybe for the end users is uh good to make them decide if they want to use It's broken.

So in the end what they did in victor matrix is that there is uh increase increase pure increase prometers and the user is the one who is deciding what should be the algorithm. So like isn't that better to to like decide based on the use case because most of the time I just want some data and I don't care if the if the metrics difference is done from the first in the interval and last in the interval or the one before one after or in uh or or something different. >> Uh yeah so I guess um yeah the so basically giving the power to users to decide how they want to have these uh increase calculations work essentially is the is >> yeah so I guess there's always a balance between oh like doing something magical and it just works but then there's always edge cases and things that don't um that don't um so actually um Prometheus has done something similar to Victoria like has started investigating something similar to Victoria metrics in terms of um the rate and increase calculations in terms of like and like so there's a couple of uh additional modifiers under feature flag for doing uh different ways of extrapolating or interpolating uh with uh with cumulative metrics um with with modifiers.

So in um so yeah I guess so I guess the idea there is actually if those modifiers you know they're tested and they work then we'll probably follow a similar path of like um we you can yeah here's how increase works by default. Um, but there's also these other uh modifiers you can add to make it um do something slightly different like a sum over time or more u something more like that or do something more inter interpolating and looking at samples outside um the time range. Um so yes I guess I think it's it's sort of like we we are sort of following that path actually um with Prometheus and with deltas that does um that can help too if we do the creative time stamp stuff. All right. Thank you again. [music] >> [music]