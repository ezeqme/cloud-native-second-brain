[music] Yes. All right. Okay. Good morning everyone. I'm very happy to be with you here today. We can finally share um what we've been working on for the most part of the year by now. Um which is about introducing Paret into the Prometheus ecosystem to see if we can handle long-term Prometheus data with it. Um but before all that let's introduce ourselves. My name is Jesus Vadk. I'm a senior software engineer at Grafana and I am joined by Michael Hoffman, senior software engineer and Alan Protasio, senior software engineer at Amazon. Um what brought us here? Well, we the three of us are maintainers of uh three different downstream projects of Prometheus, Cortex, Mimir and Thanos. When I first joined the community, I remember watching a talk where they were sharing that um Thanos was influencing uh Cortex with ideas and vice versa. And I'm happy that that continues to happen today. Um uh throughout our experience running and maintaining um clusters of these technologies, we've all experienced similar pains on the gray path.

uh more on that later. But perhaps the thing that uh inspired us to work on this and talk about this today here is this talk about uh Philip Powski which did this awesome work at Shopify where he modified Thanos squers to use spit. So that talk happened around November last year. Um, and unfortunately the code is not open source, but the contents of the dog were great. And we didn't know at the time, but we were all working um on this separately. Um, and in my case, it wasn't until March this year where I used one of the hackathon that we have at Grafana to kind of work on this a little bit and I joined Grajo and a few others in the company to to fork Prometheus and see where we could fit paret uh inside it. Um that same week someone told me I believe it was Gotham that Cloudflare had open source a repository where they were trying to do a similar thing that Philip did and so I immediately reached out uh to to Michael and I created this CNCF Slack channel Prometheus Park um and I went to sleep um and the day after people from Red Hat, Reddit and uh Amathon showed up on the channel um and that's when we met Alan and he also said he was running a PC internally.

Um so the next step was clear for us. We had to join forces and we created parket common which is a library that exists in the Prometheus community organization. Um it passes 100% of the promql acceptant tests. So any any sort of promql queries should run. Um the library is low level. It's meant to read and write pocket files. But we thought it would be useful for people interacting with it to have like a queryable implementation primeus interface. So you can just generate a set of cities and and and run a a pro primeus query against them. So that's useful. Then all the downstream projects will have their own implementations. But uh yeah, this is good for the library. And other things it includes, it's able to generate parquet files from a set of CDs. Um and it can also convert uh tsdbs to what we call parket blocks. Um we've got we've got great progress since April. We've got like 90% better 80 uh was it 80 80% faster 90% less get range calls 70 less% memory usage and 40% less allocations um but these are benchmarks against the library itself not against Prometheus so this is just how it's improved overall right so why why are we doing this uh I'm pretty sure people in the room have experience running or using um Mimir Cortex Antanos and perhaps others um but in the case of these three you're probably familiar with the comp with the component store gateways which has a lot of state.

It's a it's a staple component. It has to pull the indexes of all the blocks to resolve the queries against long-term data. Um and over time at certain scale this uh this this this component starts to get big big discs and these discs become expensive. Um and also when you add a new pot to your replica set um it takes time. We have customers taking up to 15 minutes or half an hour to to sync a single pot. Um so this also becomes an an availability risk because if the pot goes down you lose access to query that part of the the data and to make up for that um you typically add more zones or overprovision the bots to make sure that this doesn't happen. So overall it's an operational challenge to run and you're typically bleeding a bit of money here. Um another thing is that if we assume a fixed retention like 12 months um data is only increasing because people is instrumenting more and more. So these problems are not going away. if anything they are getting worse. Um and then more motivation we also wanted to like we were thinking we know the limits Prometheus has with high cardality.

So we were thinking if there is a format that perhaps can help us uh do handle better cardality because the index grows a lot with high cardality or high dimensionality more labels um we will win in that front as well. Um and as we learned from the talks on OTEL uh we know that uh the hotel spec is still evolving with the resource attributes with the entities model. We know that Prometheus has its issues with it. So perhaps if we had a more flexible storage of course there's a lot of work to be done in the Prometheus itself like what's a resource in Prometheus there's no sort there's no struct saying what it is uh but but if the storage was more flexible I think it will be great. Um and last uh but not least we also wanted to explore we've seen in the ecosystem people playing with column in our store and getting good results. So we wanted to see for ourselves. So now I leave you with Michael. He will explain how the TSDB index works and what are the problems we see in the store gateways when we query longer longterm data.

Yeah. So um is the microphone working? Yes. Okay. Cool. Um, let's explore why I think or we think that the TSDB index kind of gets into our way at at times. And let's do that by working through an example select call from the select call for some vector matcher to the final materialized series set while keeping track of the consecutive read operations that the time series DB index forces us into. So um given some vector selector and assume for simplicity just one equal matcher the first thing we have to do and they don't count us as a read necessarily because that's highly amortized we have to look up postings for it and to know where those postings are stored in the index we have to know the offset for the postings that match this particular equal matcher I read that from the postings offset table by by section it's sorted that's quick that's And um but for my first read I will read the postings list from the index. Postings list is the uh amount the set of series ids that u correspond to series that match this equality matcher.

So uh first read. I do that for all matches. I intersect the series ID postings lists and that leaves me in this example at least with um one series with ID 23. That's great. I I have all the series that match my query, but I cannot yet hand that to the query engine who materialize time series data and labels still. So, we're not yet done. Uh the series ids also function as offsets into the series table if I multiply them by 16. So, I now know where to start reading data in the series table, but I kind of don't know yet where to end reading data, right? I only have the offset where it starts but it could be arbitrarily long. So the second read I need to do is uh read an integer at that offsite offset to get to that uh that tells me how how much data I need to read uh subsequently to get all of the serious uh data for this time series. I do that. in the third read and uh end up with all of the um label key value pairs for that series and uh some chunk meta data. No chunks yet, only metadata. This chunk metadata contains chunk references which again are offsets that I can follow into a chunk file to um read the actual time series data that's stored there.

But again, it doesn't tell me where to end reading. So I go to the offset, read an integer that tells me how much data to read next. And um finally I do one last read for that size at that offset and I end up with um sweet sweet time series data. I mash it together with the labels and build a series set and return it to the query engine. So if I count it right, that's five consecutive uh read operations I need to do to get from vector selector to series set. That kind of works okay for Prometheus if I have local fast disks in the memory mapped index and got us this far. Prometheus works well. But as soon as I try to do the same thing on object storage, things get get u that's nice because uh object storage generally I would say has a pretty high time to first bite at least compared to disks and okay throughput but we're not using any of the strength and we're paying for all of the weaknesses here. um these these happened before edges that the index forces us into makes us pay the time to first bite that's relatively high uh sequentially which is which is not great and um the time series database index stores data ordered in in classic series order lexographically by its labels but that's not necessarily how I want to read the data right um maybe Consider being Cloudflare and you operate a federated Prometheus that um pulls data from all of our edge network dismbiguated by like data center ID and and maybe a region label.

And now if I want to query that let's say application CPU usage there might be an application name label that lexographically comes before data center right and if application name is sufficiently high cardality then the series that corresponds to like one data center get shredded by that cardality into essentially random looks ups into the large part of the of the index file. So I cannot even take advantage of like throughput because I have to do many small writes reads sorry and that's not great and um that's yeah finally that's that's a very naive way of doing things of course a real production storage gateway does not really do those five reads there are plenty of caches and heristics involved but I think the point I want to make is that time series database index does not really help us Here we need to be be clever. We need to come up with caching strategies to kind of make it work and uh heristics can break and caches can be cold, right? So I do pay for for this with high P99 latencies and with high startup times because I need to memorize maybe the the postings offset table on disk and download it if I if I start a new object gateway.

So I hope all of you are now sufficiently scared of index files and object storage. But uh don't worry, we think we we have a better way. Um why instead of storing data in a way that I have to access it through consecutive essentially map lookups, we just store it in a flat table and in some columnar format. In our case, we chose spet, but that kind of does not matter. but we stored in a flat table and Ellen will work you through in a minute how to actually read that data in pocket format. But let me quickly introduce the the schema we came we came up with. That's uh essentially the first thing you would do if you would write your data in a table. Every row um contains one time series. Every column is corresponds to a label name. We do shard our data into uh blocks horizontally, one of length one and we have three data columns that contain also the chunk data but are sharded into um eight hour ranges to have some balance between because if I shard it in eight hour ranges, the the smallest amount of data I can read is eight hours.

There is a balance between a read query, a range query that I can schedule reads concurrently for eight hours and like an instant query where I have to overfetch eight hours. But in practice, it works fairly well. And that's it. It's it's a fairly fairly simple schema, but it already helps a ton. And yeah, Ellen, why don't you walk us through a time series look up? >> Yep. Uh, is it working? >> Uh, thank you, Michael. uh so yeah uh let's try to do the same exercise that Michael did to uh walk step by step and how uh we are fetching the data on the parket files but a little bit before that let's talk in a very high level uh how the parket file is the layout of a parket file uh and also some concepts that we'll be using. So this is how we store the data in a parket file. Uh the first thing that we can see is is called row groups. Uh if you think that your data is a you visualize your data as a table that has rows in our case each row is a time series. A row group is basically a partition a hor horizontal partition of your data.

When you're writing the park file you can say look my row group size maximum is 1 million. So every one million rows the writer will create a new row group and you start creating uh the same structure over and over again. Inside each row group we have like optitional bloom filter that is a option in the in the common library that uh Jesus uh talked about. So is the first byes will be like a bloom filters for every uh every column or the columns that you choose create and then you have the column chunks. So for each column in your schema you have the chunks uh it's a column of format. So like the data will be uh written by column and inside of each column chunk you have the page. So the page is like the minimum amount of data that you can read in the park file. Data inside of the page is compressed together encoded together. Uh depending how you are encoding uh your your column you can have different types of pages as well. So let's say if I are doing a dictionary encoding the first page is a dictionary and the remaining page will be referenced to that dictionary uh and so on and so forth.

So we have this structure uh and then we have multiple row groups and at the end of the file you have the footer. In the footer you have metadata uh about the file like how is the schema uh the number of row groups the statistics of each row group uh and all like number of number of rows in general and we have like three things that uh we'll be using to run a query uh the row group statistics are like row group index the page index and the offset uh offset index. We're going to go one by one in the next slides. Uh so let's say that you have like this proml very simple like >> prematchers or predicates or constraints whatever you want to call we have metric name container CPU usage container DB postgres and namespace cache. So the first thing that we do is like in a single call we fetch the footer of the parket file. Uh in the footer of the parket file we have the row group statistics. uh this this information will have like the mean and max oh sorry you have the mean and max uh of every uh every column and row group uh in that row group and then you can use that information to start pruning row group.

So let's say for the first matcher uh using that information I know that I may have data in row group one and two and then I do the second matcher I have maybe information in row group zero and one and I do the third one so at that point I can prune all the row groups that is a in a proc is a end constraint so I can only get the intersection intersection so at that at that point I know that like the only row group that I need to open is row group one so you do a very similar thing. Now inside of the pages of the row group one we have the you have the page index you have the same information for it every page you have the min and max of every column. So you do the same thing like you do like uh metric name equals container CPU whatever then you do the second one the third one and now you have like okay in the I know that I need to open the row group one and the page one of the row group one I do all of this just looking at the footer of the park file then I there is no other way I really need to materialize that uh columns to apply to filter out the rows that I'm interested So to do that I use another another thing in the footer called like uh offset index.

So in that index I have like the page location. So like where this page is located in the parking file. So uh I know that I'm doing three uh constraints or three measures. I need to download those three columns that page for those three columns. I do that. Now I have all the rows for that page and then I apply the filters like uh container CPU like all the rows match and then I apply I apply and then you say like at that point I know exactly what series I need to fetch uh that match that uh prom. At that point I I finish my index lookup what we would call in the test DB. Uh and now I only need to to uh fetch the samples. For that I go to the second parket file that we call chunks. Uh we do the same thing. We found the page for that rows like these two park files has a one to one uh relation. So I found the I find I found I find the page for the prospective uh time series. I download I find the locations and I download uh all the page that I need to fetch the samples. There's two things that's interesting here like uh the first one is that like before doing any call I have like all the locations that I need to fetch.

So I can plan how I I'm going to issue my calls to the object store. So I I I have a way better way to like plan my access to object store which is has huge advantage. The second thing is that we do projections here like if if this queries has an aggregation so I don't really need to materialize the extra labels for that uh series so I just skip it. Uh if we have like a by job clause like that then I go and fetch the extra column that has the by clause and then I give to the profile engine. At that point we did we did everything that we needed. uh we hand over the data to the profile engine uh and then prom engine like do the rest. Uh that's basically it. Uh it's it's a very simple flow like all the data is kind of partitioned. You are scoping down with your query all the time. uh and we don't have like this global symbols table and we don't have like the uh global postings that I have I don't know envy equals to prod and I have a huge posting list and things uh that's basically it for the fetching part uh this is just like a comparison uh this is a random block that we pick in some of the clusters this block had like 13 million time series I guess uh and we can see like the chunks files that the sum of all the chunks is 26 gigs in this case and like the chunks.park which has the same data in the park format only have 14 gigs.

It's a huge uh change in size but is even bigger in the index size. The index in this case had like almost five 4.5 uh 4.6 gigs and the labels.park A park which is has the same data and is the is the file that we uh used to look up the series had has only 153 meg. Uh that's basically it. Uh we were going to do a demo but your >> yeah your laptop is not >> but we have a screenshot. >> Oh >> I didn't update the let me connect it to the internet. Yes, we we took a safety screenshot of a demo before as this happens. But uh if you want to see a live demo, just hit us up in the lobby at some point and show. >> I need to reverse here uh it's uh same range, same data on the left on the right. It's our productive uh Kubernetes setup tunnel setup. It's 3.5 seconds on the left when read through format and like 17 on the right. Much better. And that that's a general picture. It's it's really much better. >> And also no estate. >> Pardon? >> No estate. >> Yeah, no state. They just start instantly. And >> that's like a day I think.

Yeah, that's that's just fetching data for one day. And I think it's u some CPU usage for some name space some container. I don't know that's it [applause] >> questions. >> Hello. Hello. Besides the great optimizations, uh do you see the packet files unlocking new features for permissions? >> Uh yes. Uh for example, the first one that comes to mind is metadata. Um in the design that we that we chose, we split already into two files. Uh uh well, we can see that there well over here. So we have zero labels. at the bottom the bottom two and then the chunks metadata could be a separate file and something that's been a problem in Prometheus has been making it re uh redraw backwards compatible. So every time we introduce something we have to first introduce a change that's in case you revert to the previous version nothing blows up. Um with parquet I think it will be easy because you just add a file that has a onetoone relationship with the others and if you go back a version it doesn't know about the file it doesn't open it doesn't break yeah so I think yes meta data and I think it makes things easier as well >> hi uh thanks for the amazing um presentation and amazing optimizations um I was wondering about the query materialization so is Is it possible to um like when you're writing a query to like just fetch up until like the label pages just so can have a count of amount of way execute?

Is that something that you've thought about? >> That's how we implement uh limits, right? uh as soon as you are done with this uh creating filter, you know exactly how many rows, you know exactly the bite ranges. So you can sum up to uh how much data you would need to fetch and you can implement really sharp limits on like the amount of rows time series and the amount of bytes that you want to download that that's actually done in the library. >> Yeah, that would be really good for like limiting really bad queries. >> Yeah, you know everything in front of doing >> in the limits, right? Huh? >> Is there a red limit? >> Some limit. >> We have some >> very bare minimum. >> Okay. Thank you. >> Oh yeah. Um my >> uh >> you can go. >> Oh, is it not green? Okay. Uh can you >> hi uh my question is more towards the implementation path I'm also using Thanos as in my um S3 bucket doesn't have that amount of data what you at cloudfare might have uh but in general how do you proceed let's say this goes live and uh I want to use pocket files now so do I do like an initial migration of the existing objects uh on the S3 and that kind [snorts] of look So um the library has a package called converter I think.

Um what we've done the three projects have a a version of this not the same but very similar. We have a another service called the the converter and so the converter looks for the blocks that are on object storage and generate the parket files and you need to make a few changes but then your query know which blocks are uh have parket data which don't and use one engine or the other based on that. Okay. So over the period of time I would clean it up, right? >> Yeah. So you start converting today and over time until I mean you could convert backwards if you want but that's a lot of work. Uh but over time if you wait a year you will be migrated. >> Yeah. >> And specifically for Tanos there's a Tanos community project called Pocket Gateway that implements a series and query RPC and you can just point your query at it and that should work. I personally configure it with the distributed carry engine. >> And do you see this benefit only for let's say companies or consumers who have big stores or let's say on S3 I have a 1 tabyte data would it make a big benefit for me?

Have you done that kind of compared as in pitching would also take time as you told >> I mean in the end you will store less data as you see it's more compressed and you do less lookups and maybe you don't face issues where your storage gateway starts for an hour right but you still save on requests to object storage >> thank you >> one quick question over here to your left um the um meta data in the footer is that like a convention with pocket files or >> yes it's part it's part of parket >> so you always store it at the end in parket like it's just >> yeah when you open a parket file what you're actually doing is reading the footer >> and then you say I want to read 10 rows and you >> offsets and you read >> seems seems like it would be classically a header I'm just confused by the naming I guess >> yeah it's not a header it's a footer >> is the same I guess because They write after you are writing the codes the all the rows and the row groups and at the end they write the food. >> Yeah, you keep track of the minax >> probably they put they put the the reference of the footer in the beginning of the file >> so you know exactly where to read.

>> Okay, really yeah really amazing work. Thank you for joining forces together. I think you know every projects every user wants to have one project that do those things. Now we have sort of kind of combined work together. My question is like from the user perspective, we have cortex users, tanos users, mim users. Do you see this thing to be really a default very soon and what people can when when they cannot adopt it when can they use it like what we can they what how they can help maybe to make this happen like what's what's the next step uh a bit tricky I don't know the full answer to that uh we are at the stage where all of us are testing this with at scale uh and fixing things um and then I guess we need to have a conversation on whether uh do we want to adopt this in Prometheus or not or is there a way that Prometheus can say how to interact with it with park like a formal so that users can rely on this and make a decision that on cortex is already experimental feature so you can deploy >> good luck thank you again that was amazing Look.

[music]