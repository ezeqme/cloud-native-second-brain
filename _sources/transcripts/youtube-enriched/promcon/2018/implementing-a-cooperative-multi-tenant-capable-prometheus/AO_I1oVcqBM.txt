[Applause] so welcome to this talk implementing a corporate multi-tape about Prometheus or as we renamed renamed yesterday easy multi-tenancy so first of all implementing a cooperative multi-tenant capable Prometheus you might be wondering what are we doing there so the first thing I would like to talk about what is our target audience what are our target users the system was built with users in mind that runs small-scale infrastructure most of them run like single server setups but you still want to have monitoring for them right on the other hand like getting a second monitoring server just to monitor one other server might be a little bit of overkill so our intention is to share monitoring infrastructure the other thing of our users we intend that our users must not be monitoring experts but the system should be very easy for them and very easy very approachable so that everyone can can basically use it with with a low entry bar so the goal that I've formulated here is we want to have a drop that simple fire-and-forget low resource monitoring or alerting solution that just works sounds sounds pretty cool right so let's see if we can do that so the roadmap for this talk will be will we will first speak a little bit about how we implement that on Prometheus we also as we want to have alerting we have an alert manager on the system after that we will speak a little bit about the system architecture and some additional services that we that we also provide for the system we of course have some requirements or requirements we are we want to have one parameters per machine we want to have a low memory footprint and the machine we want to run this on should be as cheap as possible and we also do not want to like have the operational overhead of managing multiple Prometheus instances we also obviously want to have multi-tenancy because that's a more system used by multiple tenants we want to have ansible compatibility because a lot of our users are running although the setups are small they're running it on ansible so we want to have ansible be capable of dropping monitoring targets in there and we want to have patch free operation we will not patch around in the in the code components of Prometheus because that will be a pain to operate and we're not gonna go down that road so that's what that was the preamble and now we need to talk about how to get data into Prometheus and usually when we speak about getting data into Prometheus people think about scraping but in our case this is more about how our users getting data and prometheus so scrape targets and the like what we do what we did there is every user gets SSH login onto the Machine and they have just have a folder in their home directory called scrape targets and they can just drop their scrape target in here for example we have two node exporters a database and a mail exporter in there and the according snippet in the Prometheus configuration is this one so we have a job for Alice and there are listed there is a file service discovery listed and this file service discovery is just reading these script targets out of Alice home directory so the job label is a little bit special here usually the job label has this notion of your group similar things together for example you put all the database exporters or all the database metrics into one job you put all the web servers into one job for our case as our setups are so small we just don't have this notion so we just hijack the job label for our multi-tenancy but you can use any label that you want and the approach will still work you just have to add the label in the jobs in addition to that we also as you see down there we also provide some basic off if the users want to have HTTP basic authentication so possible so in addition to deploying the spread targets we also have rules rules go in a similar way we have some normal rules and we have some more serious rules in 2fast the users can just drop that into another folder in their home directory and will also be read from that from that folder so that was a getting data into prometheus but now we have to speak about how we get data out of Prometheus and out of getting data out of parameters obviously means querying so if I if I'm on the instance and I just query the app metric we see that we have four users on the system we have Daniel Jonas Meredith and Valerie and I am primarily interested in my own metrics so if I only want to see my my own metrics what I do is pretty simple I just add the job equals yonas selector to the query and then I only get my own metric and this is perfect so if we are wondering well you do that in the front end right you have the Prometheus front-engined you type your query and somehow the Prometheus back and then knows what to render and what to send back so how does the Prometheus front I'd communicate that over if we take a look at the URL bar we will see oh we have some the expression just embedded at an HTTP GET parameter here right in the right in the URL so what we could do is we could just put an HTTP proxy in the middle and we could just dissemble the request take the according parameter and just rewrite that right pretty simple and it works pretty well so injecting a label so we want to inject our tenancy label which in our case is the job label so how do you do that I mean it could be pretty simple you just hook an HTTP proxy there and you just do it like some some string substitution on that on that query right it could be couldn't be so hard so you have your metric you have a job you just do a Rex matching and you replace the job with you with the according what it should be if there is no job present well it's still pretty simple you just replace open curly braket with open curly braket job equals job name comma right yeah it still works for my metric but then we have your metric and now I have to insert the curly brackets as well and I have like this I have like the divide operator and now I have to insert that in both metrics and it start I start to like implement prompt UI logic into that if I have functions in there thing get ugly and I would say the technical term for this approach is dumpster fire so let's just not do that so the so what we also could do is we would implement a prompt to a parcel that actually really knows the notion of prompt QL and use that for for injection of the labels fortunately there is a project out where there where someone already has done that there has been there is a prompt to a parser that has been written it's a total niche project maybe you have heard of it it's called prometheus so we can just use that and we do more or less the similar thing a thing that was done two years ago with Vulcan we just use Prometheus or the prompt QL sub component of that as a library and just use that for force for parsing the expression and if we do that we can take a look at how we retrieve data and prompt QL so there are two you see two metrics here and what you have is in the utmost metric you have a vector selector this is called a vector selector the HTTP request total because you get a vector of values so basically you get the label sets and then a vector of values for that the second one is where you have a metric over time this is called a matrix elector because you basically get like vectors over time so this is a matrix and those two are the only ones that are actually able of retrieving data from the Prometheus everything else in Pronk really is just doing things with these metrics so what we can do once we have the prompt your parser we can just walk over the abstract syntax tree of that and look out for all the vector selectors and all the matrix selectors and just in inject the label there because that's exactly the position where we need the labels and I can tell you this works pretty decent so another thing is the rules we have the similar issue with the rules so what we do is the users specify as you have seen the rules in their home directory and then we have a parse rule rules command which also tells you if a rule is faulty for example and this parses the rules into a different directory and in between we do some modifications to the rule first of all we do the same modification for the expression we have to add the job label at all positions where we have vector or matrix selectors and in addition to that we also add the label here down here so we ensure that this alerting rule always has the drop label present because in principle you could you could create a prompt Kuehl expression that would get rid of the job label and then this alerting rule because in the end we are routing based on our multi-tenancy label in the end we are not able to route the the rule correctly one example for that would be if you do a sum sum in front of the app then the job label is gone so we ensure that the label is present as well so now we have spoken about the Prometheus and how we get data out of the Prometheus by querying and by Prometheus pushing data out manually for alerts speaking of alerts we of course want alerting in the system so we have an alert manager the alert manager has a similar issue when we have the alerts we can see like every alerts that are present but I'm primarily interested in alerts that are actually that are actually for my machines so what I could do is I could add a custom matcher for example the one that sue Prometheus thought that the alert manager is proposing to me the environment production so and for example I could do the same thing with a multi-tenancy label and I would only see the alerts that are actually applicable for our for our mode for our tenant so what worked once can work a second time so how does the alert manage of front-end communicate to the backend what act what filters are set we take a look in the URL bar and we see well we have a filter argument where we have exactly this in there so we can use actually we can use the same code that we used for the Prometheus to parse that as well and to to put the modified version in ensuring that we always have a filter by based on the channel model there is one thing that in the alert ninja which is crucial which we need to do so that things don't go wrong any ideas what that might be silence restrictions exactly we could have this one I could set this over this this silence and would like silence my alarm clock and by accident everyone else's which is perfect because we all get to sleep long and the next day it gets not so perfect like two minutes after we woke up and realized that this has happened so we need to ensure that that this will not happen the idea is again the same we have our our label sets here so we again inject our job label in our heads or job label or our multi-tenancy label into into that well because we are hooked in between with the HTTP proxy anyways we can do that in this case it's not a get request but it's a post request but the principle is the same so this brings us to the architecture overview so the system at as we have it is set up the following the things I have just told you the HTTP proxy is this PAP the Prometheus authentication proxy which sits in front of a Prometheus instance and the front of an alert manager instance and does and is doing these label injections we see here the folders where premiership Prometheus reads the configuration and we have Prometheus speaking to the alert manager which is also behind the premiere authority of indication proxy and we have an engine X in front of the system so that's the end of next takes care of the TLS for example and as it's doing routing based on if you call like Prometheus dot your domain or if you call alert manage object dot your domain the Prometheus authentication proxy is then then doing HTTP basic off to determine your username and if you are if that's a valid username and you know the password for that and then gets you on the prometheus where you can use the premises as if it would be your own and the road manager of course then alerts someone if things are broken in addition to what I've told you so far you see this black box there which is a black box exporter we also provide that which brings us to the additional services that we have which is currently a black box exporter the black box is Porter is a very nice thing because we can do a lot of things with that we can check if our of our TLS certificates are valid we can check if things are actually up we can get some probing light if our website is slow we can check if we get a proper HTTP status code so it's a super awesome thing it's not if you're not familiar with Prometheus at all it's not the easiest one to configure but still pretty powerful so we want to provide that in an easy way so we decided to like we did with scrape targets we provide a black box targets folder in the users home directory where we have subfolder folders based on black box modules so for example we have an HTTP 200 status code checker for the ipv4 in version IP before we have a TCP connect for ipv6 and users can just drop in their their targets and everything else is taken care of by the Central Prometheus configuration that's the appropriate configuration for the HTTP 200 ipv4 module you get a lengthy lengthy job name and there you have as a parameter this this is the black box exporter configuration you hand over which module you want to use those modules are predefined we provide a couple of predefined modules for that and as you already have seen from my home directory I get all the Jason yellow and yellow files that I provided as black box targets for this module of course as we use the drop in label for our multi-tenancy this would not end up being accessible for me because another job label is injected so what we do we just replace the job label with what we actually want to have so technically everything runs in this job but the metrics end up being in my main job that that all that collects all my metrics which is accessible for me and in addition to that we also add a target label black box modules so that the user if there is if there is a probe failure for example can directly see which module it is and also which IP version that is so that's the system that we have and we have as a conclusion we have I think chief the features that we wanted we have user separation we have a low memory profile we have ansible compatibility this this is why we needed the SSH access because ansible requires as Ajax's as it as it seems to drop in labels we of course have alerting that was important because that's the one of the primary uses of the system and I would argue that with this just drop in your just drop in your your targets we also have an easy way of using that we of course have certain limitations on the system this is a cooperative system we have no resource isolation so everyone is asked to be nice on the system and just not spam it with like ten millions of of targets we only have one set of credentials for the HTTP basic off in this setup possible because for security reasons you cannot provide HTTP credentials via a service discovery so we have to include them in the users job which has has to be done by the operation team and of course because the center of configuration is managed by the operation team we have a pre-selected feature set that this the system provides but I think we are already getting pretty far with that everything what I've shown you is implemented in a software called promos proxy it's on github you can take a look at it you can use that you can try it out you there is a flag that allows you to set the label that you want to use for for the multi-tenancy it defaults to the job label because that one's most likely to be present and it also has some some it can also do a TLS on its own if you want to do that I personally would do that in the nginx but if you want you can also provide certificates for that and this brings me to the end of my presentation and it would be open to questions if there are any cool thank you [Applause] so first first question would just be one for me you mentioned in the beginning some properties of your users but what is the actual environment where you're using this this are private servers most most users have their private service and we just want to I mean if you're hosting your own website for example if or if you are hosting your own mail server then you want to have some some alerting if things go down so these most of these servers are private servers so it's not another corporate environment it's like some collective you're running with friends yeah exactly yeah this is also why we want to have the setup as low memory as possible so that we can keep the the system cheap because that's an additional cost that in ideally you would never need but of course at the idea world we have monitoring because the world is not ideal thanks for sharing do you have any isolation for alerting templates because those can execute arbitrary problems well we provide one central alerting template that we have written that should not leak any information but we have so we have one one pre provided a lodging template that we use that is not user definable alerting templates not alert notification template so inside a new version rule in those annotations and labels you can run arbitrary from QL I should check if that actually locked down if not it will be very soon anyone else hey thanks for the talk so I wanted to ask about how do you protect from like bad from QL in for example alert Andrew because our our for example for some like bad target because when like Prometheus starts it checks all the if correctly if configuration is corrective and if it's not it will just yeah so the users provide the rules in their home directories in the rules folder and we parse them into a central central place with the within modifications we made we only parse the rules file if prompt to tells us it's a valid rule file if not we ignore it and notify the user that something is wrong so we should always ever get valid rules for the Central Prometheus anyone else three - Oh what about dashboards your users also asking you to provide some kind of way of visualizing their metrics currently we only have the Prometheus front-end for that we don't have for example a graph on oh we have thought about adding a gravano to the system but the primary primary focus of this system right now is alerting is making sure that you can sleep safe so we currently do not have a dashboarding solution available yet but that's something that we are thinking about if we want to provide that as well as part of the system okay Thank You Eunice you're welcome [Applause] you