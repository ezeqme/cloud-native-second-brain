hey everybody alright uh so yeah I'm Paul Dix I'm the founder and CTO of influx data we are the makers of influx DB so this talk is quite a bit about flux which is a new language that we're building it started as a query language it was called previously I fql it has since been renamed because of a couple of things one it's no longer a query language and two it's no longer tied to M flux TB specifically so right now I'm thinking of it more as like a data scripting language it will be turning complete you could write programs in it but we're scoping it to focus specifically on working with data it's functional in style all the code is MIT licensed and this applies not only to the language itself so the spec is is out there it's open source it's public but also the runtime and the engine that we're building all written in go so as most of you are probably Prometheus users I thought you know your question we so what I don't really care this has nothing to do with me and when I first proposed this talk there were a couple of things I thought might be interesting to this crowd one was I was thinking about high availability right if you could query multiple Prometheus servers in one engine you could engineer some sort of high availability thing that would be kind of cool and the thing is as I started writing code for this I was like okay there are far easier ways to create a high availability Prometheus set up that hits your requirements than this so I kind of scrapped that and then I thought okay well what about sharded data you have separate Prometheus servers running in different data centers and you could use this engine to combine that data and then I thought well actually and and this is my intuition I don't know if this is actually true but my thought was for most people that have sharded data with Prometheus they actually combined the data in graph on ax right they have multiple data sources and they graph it in their graphs and they can see it but they actually they rarely have need to query and process those pieces of data together as one cohesive unit so I thought okay well that's not very interesting although I will show that later uh and then maybe Federation but again I came back to this situation where I think most people show it in graph on ax so I went back to the drawing board and I thought okay what what can I say or what can I do that would be interesting for this crowd and I went to the Prometheus repo and I thought okay I'll look at issues I'll see what people are asking for I don't I didn't want to look at bug bugs I wanted to look at like feature requests right so specifically I wanted to look at what people were asking for in prom q well and what was labeled is an enhancement right what are the things that people want to get out of the query language so they can't currently do I know that people love prom ql' but my assumption was it is not the answer to every problem you could ever possibly have so I uncovered a couple of interesting items this is this one sub query for range selection so it's like a sub query thing and this was like a long issue it's still open it was opened a couple of years ago and there was like a discussion on a google group that was linked in there and this is like somebody asking hey why do something like this right and this show it's kind of what they wanted to do they want to calculate the rate in two-second intervals over the last 60 seconds which would be 30 data points and then they want to get the minimum value of those ultimately this is kind of like this is basically sub queries is what they're asking for they want to do some query and then they want to wrap that with some other query that processes that result right so here towards the end of the issue we have Brian saying yeah don't hold your breath on this one I'm gonna flag it as maybe and also not as easy as it looks which I agree with on the latter part for sure ultimately the answer to anything that is related to sub queries it seems is recording rules right so you configure recording rule to calculate those rates and then you can do a prom ql query against that resulting series the problem with recording rules is it doesn't do anything for you in terms of ad-hoc exploration right you need to know ahead of time what questions you're going to ask which kind of sucks all right we'll come back to this one don't worry so here's a request for covariance and correlation features and again you're Brian coming in and saying well that's like heavy statistical analysis that's you know the kind of thing that you want to do in our Python so really do it there and you know I bring these things up not because I want to malign Brian and the other Prometheus developers I actually think they do a really good job and the truth is with Prometheus I think that it's focus this pure focus on metrics and solving this metrics monitoring problem is actually a key strength of the project and ultimately like myself being an open-source person as well I think saying no is an asset projects are as much defined by what the maintainer say yes to as what they say or as what the they're defined by what they say no to as what they say yes to so but ultimately there's still this problem of their our query workloads that people want to do with their Prometheus data there was this talk from last year about doing data science he kind of stuff with Prometheus and basically the talk was you know query your data out of Prometheus and then work with it in R or Python so I thought okay there's an idea here there's something that Prometheus users want to do that maybe we can help out with with the new language so let's liberate the data silo let's pull the data out of Prometheus and make it so that we can process it in other interesting ways then maybe don't exist in the database itself so enter flux this is where I think actually the more interesting pieces of our new language plus Prometheus kind of reside so let me get into the language elements we're gonna go through like a whirlwind introduction of it so here's what a query looks like this is going from in flux TB The Telegraph database we're looking at the last hour worth of data and then we're filtering it down by these these tag Keys underscore measurement is basically our version of metric field is not something that exists in Prometheus but underscore field is basically a special thing and in flux for how you would work with in flux data in flux as you can see there are comments in the language there are functions these different things there's this crazy-looking pipe forward operator thing which if you work with the lick sir you've seen I've also seen people propose this for JavaScript I don't know whether or not that said we're actually going to happen but basically the idea is that you take the output from one function and send it to the other function so we're piping the data or the output from a function forward to the other function we've named arguments flux is a language does not support positional arguments and that is by design I like named arguments because as a newbie coming into the language I don't need to know what the what the parameter specification is for a function to know what's being passed in or know what's being done it's I think it's a bit more readable for novices it's maybe a bit more verbose for people who are creating queries but you read far more code than you write so let's optimize for the common path there are string literals their duration literals this is like a relative time here we can also have time literals actually I put those in quotes time literals do not need to be quoted there are anonymous functions that syntax looks similar to stuff you'd see in JavaScript and ultimately the language as a whole looks very very similar to JavaScript if you've done a lot of work with it there a bunch of different operators there are a bunch of different types that the language supports the ones on the Left standard types some of the ones on the right I think start to get more interesting object function namespace table and more importantly this idea of a table stream is a first-class citizen within the language so there are different ways to run flux as a language there's the interpreter which I'll be showing primarily in most of my examples here which is basically like Python or Ruby or whatever it's basically just a runtime where you can run a script from your laptop or from a server there's an API server which has an HTTP API it's basically a stateless application server that can query whatever data source combine the results together do the query and send the end result back to the back to the API caller and in the future the next release of influx DB we will have flux as a supported thing and also we'll it will be in the 2.0 alpha both of which we hopefully will be releasing next month there is a user interface currently in the works in cronograph which is our UI for for the stack it's basically builder where you have a code editing screen but there the idea is also you'll be able to build a query without knowing the language at all you won't have to know the language to create that we've partnered with the Gryphon alabs folks because we want flux to be to have first-class support in Griffon a-- and they've started a builder there as well all of this stuff is very super early it's very like it's it's basically pre alpha but we're trying to get all the pieces together so we can start to get feedback from the community so wise flux why do we need another language ultimately I think again I think flux is interesting because it's all about data the thing that we're trying to do is we're trying to work with data and do transformations on it and do analytics on it and do all sorts of other stuff uh so let's pull this back to prometheus let's look at time series data from Prometheus now I don't do live demos because live demos freak me out but just to show you like my live demos are actually slides basically but to show you that I'm not completely lying here yeah so there's my node exporter there's my Prometheus server running and then over here I'm not even going to type it out I'm just gonna go back up all right so I've the flux interpreter I'm telling you to execute the script I'm just handing it on the command line I have the from crop prom function and I'm just actually passing in a raw Prometheus query and then I'm saying I want only the last minute worth of data execute that and there's not enough real estate here to show it but there are results I promise all right we're gonna switch back over to slides now okay so basically here's here it is kind of scoped where you can see the results on one screen the actual results all aren't important but what you can see there is you have a table of results and here we have the actual query that we have before in this case we're actually saying get us a note CPU seconds total and match to the zero CPU and the idle mode and then look at the last minute worth of data so that's what this query looks like in a bigger font with nicer stuff but going back here we can see that we returned a table of results and the table has these keys named CPU host instance job mode start and stop these keys basically serve as an identity for the table actually the value of these keys serve as an identity for the table if you look at their values below they're actually all the same so there those are right but we also have the actual time series samples themselves which are stored in special columns called underscore value which is a float and underscore time which is a time so now I just want show like let's limit what gets shown on-screen because some of those columns I don't really care about so we're gonna pipe it in to this funk called keep where I specify here the columns I want to keep in the result and send those back and there we can see here like we've got less noise there and the result all right here's what multiple time series in Prometheus looks like so we're going to use this query here where we're gonna do the second total again the zero CPU but this time we're gonna look for either idle or user as the mode and we're gonna keep the same columns and in the result set what we can see is we have two separate tables and that's I mean that's a key thing in this whole thing which is each time series gets split out as separate tables so here you can see the keys they're either the identifier x' for the table and if you look at mode there's one table for user one table for idle and then here we have the actual samples themselves tables are basically the base unit that we're working with as we work with data influx and the nice thing about this this abstraction is it's not tied to a specific like data model or schema or anything like that it's not tied to say influx D B's idea of a measurement and tags and a field or Prometheus's metric and labels or you know whatever it likes equal databases it actually Maps perfectly with because there are all tables alright so let me talk before I get into more of that and how we actually transform the data I wanted to talk a little bit about the filter function so before what I was doing is I was handing in a raw query into Prometheus to have it process that that's kind of that's not ideal like what I'd actually rather do is compose my entire query in flux and how the engine just intelligently translate whatever it can down to Prometheus and then whatever it can't do post-processing once it gets the result so here we have the filter function we're saying match this name match this mode and match this CPU and there we see these there's the actual filter function that we're using and importantly down below we can see the Prometheus query that actually sends to the Prometheus server and also what the range does is that send sets the start and end time that's sending to Prometheus as the query so I also want to show this one because I think it's a little weird but in flux we have you have like an in match so if you see in the filter function we're saying our top mode in and then we pass it an array of idle or user and what that does in prometheus is it translates that into this kind of like regex matching thing which is I think a pretty ghetto hack but I don't I don't think you can do ores ultimately if the cardinality of whatever it is you're regex matching against is high your this is going to end sadly for you all right so let me talk about some aggregate functions and how they work within this like table space right so here we're grabbing again named mode idle either from the 0 the 1 CPU and we're grabbing the count of those things so as we would expect there are two separate tables one for the 0 CPU one for the one CPU and then we're seeing the count which is 4 samples the other thing to note is that the time is the same as the end time for the query so basically what the aggregate does is it will compute that aggregate but an aggregate doesn't make sense in terms of the time stamp for a single sample so what it does is it sets the time stamp by default to whatever the end is of the range but you can actually configure that to tell it what time stamp you'd want to use so again like you have this idea of the keys that define the table know the stop is part of the definition the table the tables identity is the table that has those specific keys for name CPU and mode but also this specific stop time and there's everything all in one place again so in like in the language we have like within the tables that we send around we have this concept of an underscore start and underscore stop like columns anything with an underscore is basically like a system-level column and those columns are basically about windowing data in terms of time and basically like slicing and dicing the different tables into different components of time so I wanted to dig into how the window what the window looks like so basically we have this query here right where we're just grabbing the last minute worth of data and there we can see we're pulling we pull back one table and you can see that the start and stop time is based on the range of the query right so the range is the last minute and the start and stop time or a minute apart based on when the query was executed so now we're gonna call the window function and we're gonna say window it to every 20 seconds so every 20 seconds I want to create a window marker and actually what it what you can conceptually think of this as is taking that one table and splitting it into tables where you have 20 seconds worth of data for each one of those right so it's all the same samples still but now they're split across four separate tables and then we can then we can send an aggregate function at it and when you send send that to an aggregate function it will compute that for every table that you have in the stream so then we can see okay now the tables have all been reduced to a single record where the record is the actual aggregate result and then finally we can unwind those tables this looks kind of weird we're probably gonna change this syntax a bit but we can unwind those tables and roll them back into a single table of results by calling window and saying the every boundary is infinity we don't care basically combine them all together again and this would work so with the Cree that I had the initial table that I had was one table but say we had pulled back to separate time series worth of data this works across multiple different time series so if I pull back idle as one and mode is another what I would get back after all of this is I get two tables one with the idle aggregates one with the mode aggregates so basically what window does is it converts end tables into end tables based on time boundaries right like that could be fewer tables more tables and by default underscore start underscore stop and underscore time are the ones that get used so let me cover group really quick and then I'm just gonna blaze right through I think I've like five minutes and I'll Stella five minutes of questions for okay so group group is the other piece of this which is it converts end tables to M tables based on values right so say we have this here again CPU zero or one idle we have two tables they're like we'd expect and now we want to group it based on the name and the mode so we want to combine those tables together so what we get is a single table of results with both the 0 and the 1 CPU in a single table now say we want to now if you look at that there they're arranged and basically it's look it's like a union they just Union them together so now say we want to actually sort those by time so now they're sorted by time and you can see they're all interleaved ok so let's get back to this sub query support for range selection this is gonna be really fast this is what that query it looks like this actually works so I'm querying from Prometheus here I'm actually showing that I can pass in the host by default it queries the local host but it can be any Prometheus host anywhere I'm doing the filtering so I'm looking at no disk written by its total I'm doing the range I'm transforming using a non-negative derivative I'm breaking those results out into 10-minute when I'm getting the max on each 10-minute window grouping it all back together and then finally just for fun I decided to show mapping over the records in the table to do some sort of transformation on them right so I'm converting into kilobytes here's an example of according from multiple Prometheus servers so I have data center one data center - I construct a query for those and then I call Union to you to cross the streams and then I can compute a sum or some aggregates on that so with flux you can actually work with data from you will be able to work with data from many sources in flux itself Prometheus my sequel CSV s3 and the idea is you'll be able to combine those results at query time to do things that you weren't previously able to do at least not that easily you can define functions in the language so this weird pattern where we have window then an aggregate then another window to Unwin dough the results that would be very very common so you could just say let's define a function called window aggregate we'll take every we'll take a function and this this last thing at the end is basically that that stream thing that says like this function is pipe forwardable and the if you pipe forward something into it it will get assigned to this stream variable which then you can reference in the function right so we call stream 5 forward window PI forward to the function PI forward window and we have the result they're also going to be packages and namespaces we don't have this yet and we're still kind of like narrowing in on how exactly we want this to work but this is an example of how maybe it could so we have flux helpers we define the function and then in a separate script I could import it and then I can call that function and the idea is we want people to be able to share code easily so project status everything in this talk is a prototype there is some working code it's all on a branch which I will tell you about a second the end of this month we want to have a proposed final spec for the language there is a spec that's publicly viewable now neck month we want to release these things as basically alphas so that people can play around with it we want to iterate with the community and then finally we want to do optimizations we've done no optimization on this yet so there's gonna be a lot of room for that there are a few avenues of future work that I think are particularly interesting for working with the Prometheus crowd one is right now the way a flux query like compiles down into prometheus is pretty basic so we could have more complex complex compilations into prompt QL we also have been thinking about adding a prompt qaul parser for the flux engine so basically parser prompt ql query flux the idea of the language is decoupled from the actual engine itself what the engine gets is basically a dag that's represented as JSON so if we can parse the prompt ql language into that dag then it could execute it we could potentially add flux into Prometheus I doubt this would happen but or would it be accepted I don't blame you but here's actually the most important one that I think is the most interesting is adding a narrow API for Prometheus twenty seconds so Apache arrow for those of you don't know it's a cross language development platform for in-memory data it specifies a standardized language independent columnar memory format for flat and hierarchical data it's for our analytics friends so the idea here is that we could add an API to Prometheus where you could stream data out of it but stream it as arrow buffer data so it would support not the entirety of prompt QL but only the minimum amount of stuff that it could do where you could guarantee that everything could be sent as a stream so anything that you would send to it would have no risk of basically being the Prometheus server or anything like that and with in flux that's actually how we're building in flux DB plus the flux engine is we're decoupling the idea of that pure storage from the actual engine itself all right these can these can come in later yeah there were more I thought I had 30 minutes I'm at 26 30 yes so thank you questions okay thank you so we have performance analysts who have approached me because in the past they were able to make use of data from say graphite and kind of wrangle it in spreadsheets from a sort of business perspective and I've been wondering how between this and that what would be the best way to kind of meet that use case making data available to data scientists so they could work with it in spreadsheets yeah I mean if so we would add so I guess if we the famous people could probably talk about it but one thing I was thinking about is this here which is basically flux has this idea of inputs and also outputs so the idea is you could do from prom do some data and then you could write it to a CSV file or to feather feather is basically the on disk format for arrow and R and Python actually work really cleanly with either CSV or feather formats feather is much much faster because it's a zero copy kind of thing but yeah the idea is you they could run this at the command line and then have the data to work with in a format that works for them so again I'm learning so for your four fluxes for data scientists and analytics correct it's it's for all the people all the people who want to do things with data thank you so is there any possibility I guess using with core fauna like putting like a UI in front of this for visualizations yeah so they've already started the work David who is here somewhere Heidi right there you can talk to him about that because he built it yeah yeah we want it to be we want flux to be supported in ingre fauna and our user interface and I mean the reason why we MIT license did is we want it to be supported and used as widely as possible whether or not people are working within flux right so like Telegraph is I think our most successful open source project and the reason so successful is there's no requirement that you run in flux at all if you want to use Telegraph and we want flux to be the same way kind of following up on that do you see the arrow API being something you can implement for other databases so maybe you want to combine data within flux from a dupe or somewhere else and instead of having you know from s3 you have this kind of from arrow yeah yeah yes I do and actually I think future databases will be based on arrow and the thing is like the Big Data platforms already have a lot of support built in for arrow so if you want to work with arrow like in spark or anything like that it's already kind of first-class like West did a bunch of work while you was West McKinney the guy who created one of the creators of arrow did a bunch of work while he was at Cloudera to basically try to integrate those systems and the idea is it's a format that's cross-language cross-platform so you can get zero copy semantics for these things and work with whatever tool you want to work with but do efficient analytical processing hi there I'm over here so the language that you presented over here is I think it's something called concatenative programming right where you keep on smacking stuff at the end of a script to map data through it do you by any chance know how it relates to some of the other languages in that field I'm not an expert on concatenative program that is the first time I've heard that phrase so I would have to be educated on yeah I mean the thing is you don't necessarily have to just tack something on to the end but I do think because it's kind of like flow based and functional it does lend itself nicely to building up a query over time like in theory you could do the same thing in Lisp but instead of tacking it on to the end you'd be wrapping it right and there there is actually a way to call to you to show flux to call these functions and make it look like Lisp although I don't know why you'd want to do that you could okay thank you very much Thanks [Applause]