Uh thank you everyone for attending the talk today and I hope everyone has the uh glad sumi so far today. So for this talk we will be focusing on open telemetry collector or hotel collector for short and I'm speaker Jang. I work for data dog and have been contributing to collector upstream for a while and in today's talk we are going to cover uh some broader topics of basics of the hotel collector and what consists of hotel collector the component and architecture and lastly I want to talk a little about uh the shared helper library called as porter helper which happen to be kind of the topic of last talk like half an hour ago. And before I go, uh I actually want to ask uh raise your hand if you are completely new to hotel collector. Oh wow, nobody. Okay. So I'll try to go faster for the basis part then. So yeah for the basics part as we all know uh or as most of us in the room know like hotel collector is a vendor neutral agent that works via data pipelines and the data pipelines are represented as de internal uh like graph representation inside of collector and those data pipelines are defined with like yaml files in the configuration.

So here's a very simple sample of uh hotel collector config uh which consists of a OTP receiver, a batch processor and the hotel uh esporter kind of the simplest config you can ever have and collector basically normally can be deployed in two mode. One is called like demon set where your collector is running on the same host as your application code and the other is gateway where you manage a fleet of hotel collectors where and you can do load balancing and routing and chaining find out all kind of stuff. And in this case your hotel collector does not have to be run on the same host like it can be run on other host or like even a fleet of uh nodes. So moving on to the next what makes the hotel collector. Uh the so the components uh of hotel collector are can be uh various and the five on the top are probably more common and most of you in the room may already be familiar with them which are receiver, processor, asporter, connector and extensions.

Those are all configurable in the user end user configurations and I want to also talk about two less com uh common components. One is converter uh which does manipulation on the user provided configs. For example, if you want to always add a for example extension to users uh config to make sure that uh extension is always present, you can make use of the converter and provider provider is the kind of interface or the component for providing the configuration. So configuration can uh most of the configurations are from the YAML file but it can also be supplied via environment variables or from the HTTP or HTTPS uh kind of thing and converter and provider uh like are not visible uh in the user configuration. So that's why most of the end users don't get to interact with them. But for like people who build the hotel dro this kind these two components can be uh particularly useful. And once you have all this uh hotel components, you will want to build a collector with OCB or the collector builder uh to generate the artifacts and from like uh OCB is kind of the most popular tool to build collector but there are other tools uh out there as well and I believe some of uh today's talk uh covers some other tools uh but still OTB is the most common one and most supportive one and OCB can build your artifacts either on the bare metal environments or like build and publish uh docker images in containerized environments and OCB works with uh in what we call manifest yamo file.

An example of this file looks like this. So you define the distro uh with like the name uh distri uh dis description and also you include the components uh of your choice. Here you can see that I I can include some random like kind of random uh component from my own repo and mix and match it with official uh components from the official like upstream repos. And you can do the same like your components does not have to be hosted in the upstream and it can be hosted at like anywhere in any repos which brings us to the next topic. uh want to talk a little bit bit about some common misunderstandings like uh I kind of used to receive a lot of these questions like do my components like my exporter receiver etc had to be hosted in the upstream open telemetry collector or collector country repo do they have to be included in the like a what we call official hotel upstream releases in the releases this artifacts or like as a library uh developer, do I have to recommend my user to use those artifacts from hotel uh collector releases?

And the answer to all of these are no. So like I said, the hotel components like any receiver or any exporter can be hosted anywhere as long as there are a uh go module. It can be in the upstream country core repo or it can be in your own repo or it can even be in a private repo uh with some trick. And if you are a library developer and you want your component to be easily discovered by your user uh there's a registry what we call hotel registry on our official website open telemetry.io and you can register your components there. Uh so that like anyone who go to the website can search and find your components and again like this hotel registry does not only include the components from the official repo. uh even if your components is in your own repo, you are free to submit a request to register yourself uh in the hotel registry and uh with all your own components you can build your own dro with uh OCB and OCB gives you a lot of flexibility for example like you don't have to bind to the upstream release schedules and you don't have to be constrained with some of the uh build tag requirements from Austrian for example like hotel official uh artifacts does not allow SEO but if you build your own dro you are not constrained to that you're free to use any build tags and also you can do your own versioning etc and like the other advantage for like building your own uh artifact with OB is you can only include the uh components of your interest unlike like the contra artifact which includes everything and the resulting artifact is usually like uh smaller in by size and potentially can have better performance as well.

And next I want to talk a little bit about like what happens when the hotel collector starts. Uh the whole start uh sequence quite complex. So here are three already simplified steps. Uh first the hotel collector will try to construct what we call a conf map which consists of the configurations of data pipelines from the fisher flags environment variables and the configs. And then once we have the config uh the collector will try to build the source telemetry as well as the data pipelines and the data pipelines will be like I mentioned will be represented as a deck as a graph in the collector and once we have all the pipelines ready we will start the pipeline in reverse topologic order which means if you have like receiver processor exporter pipeline the exporter starts first and then processor and then receiver. So whenever your receiver is up and running your data will be will be sure to be sent to the exporter.

So no no data loss there. And by the way like when you shut down your sport uh collector the shutdown sequence is the rewards of the start sequence uh which means receiver will stop first and then the processor and lastly the exporter. So if you still have remaining data in your pipeline, those remaining remaining data will still be sent through the pipeline through the exporter to your destination. And here's the uh like flowchart of the second step here like building the deck. Uh the whole flowchart is too big and credits to the upstream repo. Uh so I only show the graph build part. As you can see, we will read the kind of iterate through the graph uh read from the config and create the nodes and edges and also build all the components. So once you have this uh data pipelines, you should be easy to tell how the how the data flows through your hotel collector. Uh one very useful tool I find is like this hotelbin.io io which can visualize your pipeline uh in like very simple flow graphs.

Uh so here I have kind of a very simple uh three pipelines trace matrix and logs uh which only consists the receiver processor and exporter. In practice, your pipeline can be much more complex uh which includes uh which can include routing, fan out, fing and other stuff. Uh but they should be once you have the like full configuration and definitions, you should be able to visualize them uh as deck on the hotel bean.io. However, like hotel bean, one thing that bean.io IO doesn't do for you is it does not tell you how the internal service telemetry flows in hotel collector. So here I have another graph uh which shows especially the internal matrix of hotel collector. How does that work? Uh for traces and logs those are kind of simpler uh but for matrix there are some catches. Uh as you can see there are three layers. The users pipeline part is covered in previous slide which you can v visualize with hotel.io and it gives you a nice flow graph and what consists uh what the other two uh layers do here is kind of both serving and scraping the internal matches of hotel collector.

So by default the internal matches of hotel collector are served with the promeuse interface. Uh as you can see here all the userdefined uh components can emit metrics and the hotel collector process itself can emit some like uh infra metrics like process uptime and other things and all of these are collected with the internal meter uh which is from the hotel go SDK and then is posted with the Prometheus server and this Prometheus matrix are exposed by default as a8. Then you can configure like with the uh permut Prometheus receiver uh the scripts from this uh this port this interface periodically and then like uh you will need to set up another pipeline to process and forward this internal matches to like other backends and uh this by so far is still the default but uh I know the community uh hotel community is trying to push people to uh use native OTP native for the internal matches reporting uh which uh I don't think we have enough time to go into too much details.

So next I want to also talk a little bit bit about a hidden but yet very useful library uh the exporter helper library uh which can be overlooked by many customer uh many users and this happened to be talked a little bit about uh in the last in the last talk. If you were in this room half an hour ago uh many of the contents may be the same. So the exporter helper consists of a few uh opt uh configurations. Uh by default the exporter helper uses the in-memory five for sending queue. This this queue holds up the telemetry batches to be consumed by the consumers in each uh exporter. And this queue can like you can configure this queue to batch based either on the uh request or the items in each request or even the bite size. Although the uh items and by size configuration is still uh experimental. And in addition to the sending queue, you can also configure the retry settings in as water helper. You can configure like uh by default like hotel collector as water helper has five seconds initial delay and 300 seconds uh overall retry timeout.

Uh and all of those can be tuned with the uh configuration like this. So here's the very simple yet yet another very simple configuration showing how to uh configure the sending queue as well as the retries. There are some more advanced configurations uh like block onflow and others uh which you can find on the documentation in uh asporter Q sorry asporter helper and third uh the exporter helper can also configure to use a persistent queue instead of a inmemory Q which again happened to be the focus of last talk. uh if you are in this room half an hour ago the last talks this in very much details. So the persistent Q uh is often used together with a file extension file storage extension to enable persistent. So uh a sample config again look like this like you will enable the you will define a file storage extension and link it to the sending Q storage part in the asporter helper config. And the good thing about persistence stories of course like whenever your uh hotel collector crashes and restarts you can resume the uh like uh exporter uh export on telemetry data from the last read and write index.

And here's a like uh overview flow graph from the uh asporter helper library showing how this uh persisting queue works. And if you are a experience hotel collector user, you may be wondering like uh some of the exporter helper functionality may sound like very much similar to a commonly used component called uh batch processor. So why do we want to use asporter helper rather than the batch processor? So here are some reasons uh in general like asporter helper has more flexibility in the configuration. For example, you can configure the retry settings on failures for asporter helper but you do not have such option for batch processor. Batch processor on the other hand kind of always drop data is if data is rejected by the espoders and also another common use cases for different vendor backend they may have different requirements on the uh on the batch sizes or on the bite sizes. If you use batch processor, you will have to define multiple instances of batch processors to meet the requirements for different exporters.

But with exporter helper, you can easily define it just within the exporter configs. And continue continuing on the comparison. Exporter helper also allows you to uh batch on different settings either on the items or on bite sizes or by default on the request while the batch processor only support like bash on requests and lastly like you had to define the batch processor while for exporter helpers you kind of get it for free in the exporter. Uh that being said, asporter helper and batch processor are not mutually exclusive. So we often see uh customers defining both the uh asporter helper especially the sending Q and a batch processor. Uh so by today the sortter helper is still in active development and uh our general recommendation is like if you want if you prefer stability for now you can continue use uh batch processor but if you are like you want to kind of be a early adopter of this new features in s water helper feel free to do so as well.

And lastly want to share some uh community updates from the hotel collector uh since we have been doing and coming index. So the community has been working on what we call the core collector 1.0 zero uh which is mainly like to stabilize the uh OTLP components mainly OTLP receiver and asporter including their dependencies uh including the exporter helper that I just mentioned and also the hotel go community has been working on the OTLP native profiling signal and for collector uh especially we are introducing a EVPF profiler It's uh distribution of hotel collector. It's supposed to be out already but there has been some problems. So hopefully it should be released next Monday. And we are also working on various improvements on the documentation from our website. If you have any feedback on the website uh feel free to leave a issue or like leave something in our slack channel. And yeah, I think I have four minutes for question and answer.

If anyone has questions uh profiling, let me get Oh, provider. Yeah. So, provider is for providing the configurations to the collector. Yes. the does does the does auto collect current support dynamic reloading of configurations? Yes, that's uh if you want to provide your own provider, you will need to implement that interface like watching for config changes uh from the different provider sources, config sources for example from the files or from the HTTP. And so but this is supported or do we need to um modify on on our own? Uh we have so collector contrib has a a few built-in like official providers but if you want to provide your own provider uh like customized provider you can do so as well like by implementing the provider interface. Was your question about whether you have to implement the hot reloading yourself too or so for normal config files that is supported not necessarily like as soon as the file changes you have to put a give a signal to the collector but then it will reload everything as and as long as all the providers under the hood also have implemented this reloading interface that all should work.

I actually don't have a question. I was just answering that. Sorry. Yeah. Thanks. Uh what does the collector used to instrument itself? Is that completely using the open telemetry go or does it have something else? Yeah, exactly. It uses the hotel go SDK. How how you mentioned that the metrics are exposed via Prometheus. But how does the internal logs flow? So like does it go back to the same loop as if it's a log coming from external or does it have a separate path the logs from the collector itself logs from collector itself can like I think it's just uh written to uh sender out but you can scrape it with uh some other log receiver as well. The collector supports the hotel file configuration. The collector also supports the hotel go file configuration. So if you want to export the collector's internal telemetry, its logs, traces, metrics, uh you can use OTLP or any of the supported file configuration exporters to export those data types.

So it can be like routed back to the collector. You you could do that. Although the recommendation is especially if you're exporting directly using the Go SDK, uh do not use the collector itself to monitor its own internal telemetry. You should export it to some other collector or to a back end. Yeah, there's a configuration for you to directly as part the internal telemetry to a back end without going going back to this uh uh collector pipelines. Uh on this slide you mentioned that the Prometheus server is on by default. Is that still true? Like I think there was a change in like just the last release that got rid of the ability to configure that and so is the is it still hosted on that by default? It's still on by default unless you explicitly use uh chooses OTLP for internal telemetry. Got it. Was the configuration the configuration? Yeah. What changed was the service telemetry metrics address is deprecated.

It's not if it doesn't do anything unless you turn the feature gate back off. uh if you want to configure the Prometheus the default Prometheus exporter you have to use the file configuration and if you don't want to use the Prometheus exporter you could supply an OTLP exporter instead yeah uh I think we are at time so I'm happy to take any questions offline and lastly if you have more question and want or want to get involved uh you can reach out uh to us either on Slack or on GitHub. Thank you.