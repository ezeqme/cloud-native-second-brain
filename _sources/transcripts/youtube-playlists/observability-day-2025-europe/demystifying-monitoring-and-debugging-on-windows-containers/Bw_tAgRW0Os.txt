hello everyone welcome to a talk on demystifying Summits for monitoring and debugging on Windows containers my name is Mani Kar I a software engineer at red hat and I have my co-speaker rtia Gupta with us today uh both of us are an active member of the kubernetes Sig Windows community so you can find us there so we are going to cover the agenda here I hope you were able to take a peek at the abstract but if not uh we are going to answer some questions firstly why Windows containers how to set up Windows nodes on kubernetes introduction to host process containers and what they are uh monitoring Windows nodes and containers debugging Windows nodes then we are also going to move on to a demo where we are going to do some debugging on a Windows node that we have in a cluster and we are going to see some container and node metrics on a grapher dashboard we will also have some time for Q&A so keep your questions for that uh let's start with answering the first question why windows can containers many Enterprise applications have been built for Windows OS you may be aware of many Legacy applications that we use in our day-to-day lives and behind the scenes they use net and other windows specific apis if you want to run these applications in a containerized manner and take advantage of the kubernetes orchestration capabilities we need Windows containers Windows containers allow these applications to be run in a containerized manner without needing a complete reite for Linux a point to note here is that Windows containers are usually tightly coupled to the OS so you would have a Windows Server 2022 container that can only be run on a Windows Server 2022 host for example so once we have the windows containers where can we run this uh we need Windows nodes for that so let's move on to discuss where we can bring up Windows nodes so if you have a Windows VM which is in a cloud that you use or you have a instance that is on premise you can use those to join as a kubernetes node to the kubernetes cluster that you're using uh we have quite a few ways of doing that uh the first one is capy which is a cluster API provider then we have Cube ADM which you may be more familiar with if you are bringing up instances and clusters on your local machine we also have a miniq prototype that has been developed by someone from the Sig window Community if you have issues or questions running any of these you can reach out to us on the read me that I have wanted to uh there are resources for the slack Channel as well and finally if you're interested in using a manage kubernetes service because you're running a production workload and you need someone to take care of your workloads you have open shift AKs and eks which are going to help you to join your instances to the node uh as a node to your kubernetes cluster automatically so let's move on to discuss the gaps okay so once we have the windows containers there are some gaps in our kubernetes ecosystem that we're trying to bridge you would know of many solution architects who specialize in Linux containerization right uh they're helping the organizations through rep platform Legacy Linux applications and move them to Cloud but do we know of any architects who would do it for Windows maybe not lack of expertise is a problem in this field second data for Windows on monitoring is scarce so we cannot create a unified monitoring interface for both windows and Linux if you don't have this knowledge Windows exporter which is uh something that we're going to see in a bit is also not an official exporter that is maintained by Prometheus but it is created and maintained by the Prometheus community so we constantly need to improve on that to give you an example of this Gap in the open shift console that I work on and the team that I work on we had the Linux container metrics uh specifically hardcoded and uh for the Linux uh console that we have but uh when we tried to introduce the windows node we had to create a promethus rules and and service monitors which which can map this Windows metric names to the Linux counterpart so we can have a unified interface for our customers to put it in a nutshell Windows applications are almost always an afterthought but we think it doesn't need to be like this and windows applications shouldn't be left behind in the modernization journey let's go over some features now that are added by the Sig Windows community and how they're helping us so first is the Windows host process containers host process containers specifically in the context of kubernetes on windows are containers that operate as normal processes but run on the host Mach host machine and they have access to the host network uh storage and devices this concept is pretty similar to the Linux privilege containers if you are used to using them but why are they important so you see before we had this feature in place services like cuet QBE proxy had to be run as Windows services on the not itself and had to be registered within the windows service registry so it was very difficult to run them with this feature addition you have a base image which is very lightweight and you can use this image to build any service and deploy it as a demon set port on the Node directly so it doesn't need to be a Windows service you would agree that this process has made it come on par with Linux and makes makes our life very easy so let's go over some important points I would we would need to uh learn about monitoring uh we need this so we can make informed decisions optimize performance and strategically maintained cost for our uh infrastructure workflow to name a few okay let's go over the Prometheus architecture diagram many of you may be familiar with this already but we have a Prometheus server which is running on the Node this would be a Windows node in this case it is going to retrieve the metrics uh this could be container metrics and the node metrics it would be cuet for collecting the node metric and a container runtime interface like container D for retrieving the container metrics uh this is usually C advisor in the case of Linux so there's only this container metrics difference uh in the architecture over here uh so these metrics will be retrieved and stored in a Time series database and be retrieved by HTTP server you can use a promql query language to visualize the metrics and grafana or Prometheus you can also uh put some thresholds and push alerts using alert managers so this comes in handy and finally because we are uh collecting metrics from so many different sources we need something that will help Prometheus to understand them in a format that it understands so we are using Prometheus exporters for that so these exporters are nothing but an agent uh for Linux you may be familiar with node exporter for Windows we have Windows exporter so let's go over the windows exporter a little bit it is nothing but a Prometheus exporter for Windows machines and we have some collectors collectors are nothing but a set of metrics that you can use the collector that are enabled by default are CPU memory and Os this may be something that um may be suitable for your use case but other interesting collectors are container and process and also some net collectors uh you can enable them explicitly by giving some command line arguments uh if you need them okay so this was all from the monitoring side now I'll pass on the mic to rtia so she can cover the debugging part for us sure thank you Mani appreciate it now to understand how does one debug a Windows node we need to understand the components that sit on a Windows node Windows node in kubernetes is responsible for running Windows workloads and it houses several components that enable container orchestration a few things to note over here you've got Windows Services which sit on the Windows host kernel these Services interact with services like graph driver hns HCS HCS is kind of the host compute system that enables management of containers and um it orchestrates the life cycle on the Windows operating system hns is the host network service and the host network service is responsible for creation of IP addresses for the containers alloc of IP addresses to them um things like creating the overlay Network so on and so forth we also do notice that the cni and HCS shim sit on top of that and they help container D interact with the host services on the Windows host we also have cuet up there cuet is one of the most important things on any node you need to have cuz that's going to help you register this node with kubernetes and it's also going to keep a track track of what containers are you running what stage of life cycle they are in um all of that now that we understand the key components and how they play with each other let's take a look into the containers that are supported in Windows and kubernetes we have two types of containers the first one is a process isolated container a process isolated container is a lightweight sandboxed user space that runs on the host kernel what we're more interested in today is the host process container the host process container is extremely powerful cuz this container starts up as a process on your host and could access the host Network the volumes other peripheries that have been collected to your node um there are multiple uses of a host process container and as man talked about using them in monitoring we can also use them to debug Windows noes let's move on to try to understand how can we use host process containers to debug Windows notes so if you've ever tried to debug a Windows node historically we had the struggle of either having to RDP onto the windows node or doing a proxy jump from the master node onto the worker node just a bit of a hassle it's super cumbersome copying files over bringing it back and forth painful that's where host process containers come in host process containers are a simple oneline solution which is streamline with with Linux at par at the cube Kettle debug node command you just type that in and boom you're on the Node you can access everything that's available to the node the two main benefits of this is reduced complexity and the second one is synergy with kubernetes ecosystem it streamlines the developer experience it's going to be same on Windows as well on Linux right now let's try to see this command in action so yeah as you take a look up there you see that when you use the cube Kettle node when worker debug command two things to call out over here the first thing is we're using The Host Windows host process container base image why is this important this is a super lightweight image built by Sig windows that you can use to get onto the node and the reason this is useful is it's roughly just about 6 to8 KB and has a very small startup time if you've worked with Windows containers you know most of them are not the same they're usually in the range of 200 MB Nano servers the smallest we start with right the second thing up there is the custom um flag that we using and the custom yaml that we use the custom yaml is what tells the cube Kettle debug command that this is a host process container we said that in the windows options and it also gives us flexibility to use the run as username field to set how this container starts up on your not node so you could change that to anything you're comfortable with and manage the level of access this container has on your host um wonderful resource on there is the Windows host process container base image repository that I've linked that repository has a Docker file on how that image is buil and how you can build it for yourself or use that to add on other things and build a custom image well now that we understand how to use C Cube CLE debug for nodes and we're on the Node once you run that command how do you use other debugging tools to analyze what's going on on your windows node I've attached a few resources up there the first one is networking specific so if you want to debug the host network service that's a very useful resource um with a bunch of toolings and scripts available online and we're going to take closer look in the demo on what you can do with it the second one up there is how do you capture the post compute service traces and logs and this one's going to be if you really don't know what's going on and you need to engage with Microsoft you probably would have to go through this route capture traces reach out to the support team work with them and debug what's going on a shout out over here to a custom windows debug image that's available so if you don't want to um add your own tools to the node you can start with this image James St stent who is a fellow Sig Windows member M has published an image that is prepackaged with a bunch of tools on there uh it's a wonderful starting point for someone who's just getting their feet wet in debugging Windows nodes well enough chat let's move to the demo and see a few of these things in action thanks okay so here we get a list of all the notes that we have on the cluster if you take a look at the last two you can see that we have two Windows Server 2022 data center uh noes running on the cluster we also see that container D is the container runtime that is configured for those nodes now let's take a look at the routes that we have available uh we have already set up a grafana route and that is going to be our URL for the grafana dashboard now let's take a look at the routes in the monitoring name space uh this is going to give us a list of all of the Prometheus uh URLs that are created so this is the Prometheus route we have also already set up some Prometheus rules and service monitors for the path that is/ metrics that are we that we are going to see uh one thing to note here is that Prometheus metrics for windows exporters are usually available on the port 9182 which is different from Linux uh if you try to get the setup and now we are going to use the summary stats API uh the support for this was also added for Windows noes so you can take a look at what metrics are created with this API and you can see everything in one place so first we are going to take a look at the Pod metrics you can see the containers that are created and yeah you will have everything in one place which is the memory uh you can see the CPU Network file system and now we are going to scroll up to the node so this is the name of the node and yeah these are the relevant metrics that you may need to access and we are going to project the same thing on the grafana dashboard now we are just checking that everything is available from the Prometheus side before doing that so yeah we are moving to the Gana dashboard let's add a data source that is going to be Prometheus for us and now we are just going to use the Prometheus URL that we saw in the routes and since we are using https URL we are going to set up some authorization headers so that's going to be your Bearer and token with which you have created your Prometheus service I used a shortl token but you can use another one in your case this process is very similar to what we would do for Linux but this is just a demo that we wanted to show okay let's see use a dashboard this is the dashboard that is already created for Windows nodes this is publicly available this number but you can use another one you can customize it uh for the things that you want to see in the dashboard here you can see everything the memory CPU Network details yeah so that's everything for the grafana dashboard and now we are moving to the debugging part yeah so this is the command that rtia showed earlier yep we're going to use that and get on to the node and we're going to take a quick peek into how we get access to the node all right you see on there that the container attached successfully and now you're on the Node and you have access to all the host files on the host file system uh that's a quick look into what the k directory looks like you're going to see all the EXs that are available CU bled Q proxy so on and so forth configuration files um nothing too interesting moving on to the logs we we can see that we have access to container D logs to CU proxy logs cuet logs this is super useful when it comes to debugging what's going on a Windows node and why containers are not able to come up or a node is not able to register onto a kubernetes cluster these are super useful all right now we're going to quickly download one of the scripts we talked about that would help us debug the host network service that's linked up there it's called collect logs.

PS1 and we're going to run that and it's going to get a bunch of traces networking traces for us specifically uh as you can see the magic happening it's going to run a bunch of PS scripts and we ran them and it's created a directory out there for us um and that directory is going to house all of the traces that we've collected now we can do one good thing over here I think we're going to move on to showing that in a bit is we can copy over the directory using the cube Kettle copy command from the node to the developers machine and this is extremely useful in scenarios where you're looking at event logs that can be analyzed by Event Viewer or WPA or W shock which you cannot open via CLI only access on Windows nodes all right we're just coping the logs over [Music] and that's the folder that got generated on the host go plug that in and well voila there it is on the developers machine you can see that we have a ETL file which was copied over as well as the folder I think it's from a old demo run the ETL file you can ignore that um we're going to walk into the folder all right sweet you see the level of networking traces that we've captured we' captured the name spaces the ports the routes everything in there so everything that was created when the containers come up that are leveraged by the host network service is in there and that can be used to analyze if you're seeing packet drops if you're seeing connections being refused um when you're deploying Windows uh application s on the host that's the route table yep you see a few ETL files here we don't have wire shock right now but on a developer machine that has wire Tru you can import those files and look at them um or use the windows tools which are supported and that's what the networking txt looks like well that's a quick example of what debugging a Windows node can look like and the power that you have to look at the logs that get generated directly on the host let's move on and all right well feel free to reach out to us that's a summation of our talk uh we are available at Sig windows on the kuber slack those are our LinkedIn you can connect with us or catch us after the talk if you have any questions for us um Sig Windows is a pretty active Community we have meetings every Tuesday at 9:30 PST I encourage people to join uh and we always looking for contributors to come help us please feel free to scan the QR code to share feedback on this talk we would appreciate it we're open for any questions if you have okay thank you very much for the talk uh we have time for one question so if anybody has a question please run to the microphone in the front uh yeah otherwise find uh our speakers around to ask any questions or share your experiences yeah you can also share your user stories if you have yeah I think we have one question I'll be the one um y'all were gathering the windows metrics using Prometheus right yes uh did y'all explore open tary no we haven't we haven't yet okay yeah thank you thank you sure all right thank you very much and uh we now prepare to the next talk please give Round of Applause to our speakers