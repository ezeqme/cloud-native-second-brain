Okay, so good morning everyone. Um, super excited to see all of you here at Promcon today and glad to be chatting about um, Percy's with you. Now, let's be honest, the title of the talk is a little bit of rage bait for those who really love JSONet. Um, but does that mean we actually hate JSONet? Probably not. Um, I think JSONet certainly has its uses as a templating language. It allows for some higher order functional abstractions um which definitely reduces this uh sprawl of JSON or YAML config that you might have especially for things like um Kubernetes manifest. JSONET might actually be a step up from tools like customizer Helm when it comes to templating. However, maybe JSONet isn't meant to handle all types of config. So for things like monitoring dashboards um and rules, we felt that we could just do a whole lot better, especially in ways that would um end up making the dashboard construction process much more end user friendly and much more um durable.

So what's a better way? That's what Helia and I are here to answer. But before that, some short introduction. >> Hi everyone. I'm very happy to be here. This is my first prom. So I'm Elliot Bos. I work as an observability engineer at 59. I'm also open source contributor primarily towards Prometheus operator and the persist project. I'm also a community organizer at my local cloud native community group. And when I'm offline, I like to do what I call granny hobbies. So kneading, stitching and also reading books and music >> and yeah, this is my first promong as well. And my name is Saswata Mukharji. I'm a senior software engineer at Red Hat where I mostly work on multicluster observability systems. I'm also a maintainer of a few projects you might have heard of like Tannis and Percy's itself um and several other adjacent tools and libraries. Um you can find me online as update sa time code pretty much anywhere and I occasionally love to do some photography sometimes.

So let's intro into Percis then. And what is Persis? For those who aren't familiar, Persis is an open source visualization tool that allows you to explore and your telemetry data and build dashboards with the main focus of being cloud native and GitHubs friendly. This project join CNCF sandbox in 2024 and it currently supports all your telemetry uh signals. from metrics with Prometheus and Prometheus like long strings but also I had already has support for traces logs and profiles but from the beginning the persist was first and foremost fully compatible with Prometheus. Not only you can build your dashboards but also get detail information about all your metrics and promql queries pretty much the same way you are used to do in the Prometheus UI itself. And for example, one of the latest releases of Percy's also brought the quick query viewer that as you can see mimics the view that you have in Prometheus for when you're running your queries.

So to get information about numbers series available labels and so on but also one of the core features of Percy's is sensibility because plugins and data sources or visualization and data sources are developed as plugins. So outside of the core persist code is a lot easier to add new plugins and data sources as needed. It also embedible so you can add a panel from one of the other sports or whole dashboards into your application and keep all the customization in terms of themes that you want to have. As we saw before is GitHub friendly. So you are able to manage your dashboards pretty much as you do any other code through pull requests and then do all the checks that you need to enforce labels and so on and also then automate the deployment of your dashboards. And how do you build those dashboards? Currently Percy supports building the dashboards using Go and Q lang which is a templating language which probably are less familiar and basically you can build your own dashboards with these tools.

Percys is also Kubernetes native thanks to the Persis operator project. You can man your dashboards, data sources and deployments as customer resource definitions and then the operator will continuously monitoring and reconcile those resources. But also if you're coming from coming from pers or starting to use pers versus chances are you're coming from a graphana backgrounds as most of us do and you're having built so much graphana dashboards you might want to keep yourself those dashboards that you already put a lot of work into and persis also has a solution to to that you can use the peri which is the peris command line tool interface that you can use to then pick up on your graphana dashboards and generate the format uh the peris formatting but also per CLI has a bunch of other tools that you can use to manage your dashboard life cycles and also interact with the peris API and let's dive now into the goal so yeah we learned about um peris and perci and how we can migrate our existing graphana dashboards but we promised a better way and the first step towards that is the periscore SDK so with every version of Percy's.

We also release a Go SDK module um alongside it which contains a few builder pattern um packages to allow users to essentially construct panels, groups of panels and full dashboards directly in Go itself. These SDK methods are basically um for constructing the same Go um configuration strcts that exist in Percy's codebase. And as IA mentioned earlier, Percy's is very much driven by its ecosystem of featurerich um plugins. So all of our data sources as well as types of visualizations are plugins and we separately maintain all of these in a repository called Percy's plugins and individually release versions of them. Now of course talking about Go SDKs like this is a bit confusing. So instead I kind of wanted to draw out the overall structure of this SDK and how we use it. Um and it looks something like this. So as you can see from this diagram to construct uh any panel of any kind in Percy's you can call panel group add panel um with several arguments the major ones being a panel description then a panel addp method where you can pass in um a constructed or slightly templatized puml but more on that later um and finally you can add another argument for the type of panel you would like and these um come directly from our plug-in SDK which is why I've highlighted them in do.

And these could be anything from like a time series, a gauge, a stack panel, or even a table. And of course, each layer ends up having its own option type that it returns. And the layer above consumes as an argument to make this builder pattern work overall. So, as an example of this, um you can see this Kubernetes API server availability panel right here. Um it calls the add panel um method with a description um a stat panel chart from our plug-in SDK and finally adds a query with add query and add query is a builder by itself which um allows you to pass in promill as a string but can also take in options for things like the data source of that particular query or query resolution or query minep. Now going a layer over this, we actually have the concept of panel groups. And this construct sort of allows you to group together several related panels. Yeah. Um, uh, okay. The monitor no longer works.

Um, should I try plugging it in again? Uh, okay. It's not working. Yeah, it's working. It's back again. At least looks back. >> No, the monitors are still loading. >> Monitors are still loading. Okay. Yes. Um so to create one we use the dashboard addp panel group method which takes in um multiple ad panel arguments and also has some customization options for dictating the number of panels that you would like to fit in a line or the height of the panels themselves. And as with everything in the builder pattern library each layer has its own option. So as an example of this um you can see this read stats panel group which actually calls our previous panel example along with three others. Um and it also has options for fitting like four panels per line and setting the height of each panel to eight. Now with these final groups um we are finally able to construct a full dashboard. Um the dashboard new method helps us to do this which and it will not only take multiple ad panel arguments um but also other dashboard level options like dashboard names and even um dashboard variables.

So most dashboard functions end up being a bit large which is why I've split this example. Um but this is how you would call add variables um to actually add uh a dashboard level variable in this case cluster. Here we are using the list variable um method from our Prometheus data source plug-in to build a list of clusters. And as you can see it uses the same underlying proml query stuff. >> The odds again >> fun. Okay. Uh, anything I can do from my side to keep this from happening because I'm not touching my machine. Uh, maybe like there's some sleep mode. I don't know. Okay, we're back again. Okay. And finally we Okay. Yeah. And finally we can call dashboard new to return the actual Percy's dashboard config. So we saw how we could create dashboards. um create durable and accurate version pin dashboards and their panels via easy to use builder methods from our Go SDK which would throw up errors if you do something wrong.

But we are still passing in Promql as a string and are actually just concatenating strings when we need to templatize those um queries with variables. So as you can see over here we actually templatize via a get cublet matcher which is which calls a global string matcher. Right now, this isn't really an improvement over JSONet as it's just as easy to break and it's not that durable. So, what can we do better here? >> Yeah. So, then PromQL builder enters the chats. And as the name says, PromQL builder is basically a way to build a quer prom queries in Go. And we have in here as an example a very simple uh PromQL expression with a metric and labels. But we can go from the simplest to the most complex and it has a lot of advantages when compared to building dashboard in the particularly in the the dashboards as code queries as a strings because builder is built using promql abstract syntax tree which by design will going to prevent you are off again.

>> Okay. >> Okay. We are back again. So let's start from the beginning on this slide. Uh so as I said before, PromQL builder has a lot of advantages because it's going to be built using ProMQL abstract syntax tree. So by design, it helps prevent syntax errors before you even write your query, which is going to impose correct syntax in your expression as you're writing them. So they will be correct before you testing at runtime. And this is going to make your queries a lot more reusable and res resilient because basically you write them once and if you had proper senders as the talk uh before alluded to that basically means that you can write your pumpkill expressions once and then reuse across dashboards recording holes alerting holes and so on. But we didn't reinvent the wheel because the code is already out there. We just put it in builder basically. So we use the same pro prometheus code. uh so we can take advantage of all the validations that are already in Prometheus to enforce proper standards and then this will make sure that from the future on the prom builder is going always going to be correct with Prometheus standards and query language.

So if you go back to the previous schema that talked about prom code builder is now the last piece of the puzzle to have everything built as code. And with that so with all these different SDKs out of the way let's talk about um one of the really exciting initiatives we've taken in the Percy's community which is community mixels. By now you've learned um that about the various building blocks we have for constructing um production ready dashboards directly in go. Um so we have the go SDK um the plugins the promill builder and even the schema from Percy's and Percy's operator itself. So can we build mixins with these building blocks? Yes we can. Um but before that what exactly is a mixin though? So as a monitoring mixer, so monitoring mixin is basically a bundle of um dashboard and Prometheus rule configuration that an end user can reuse and choose to extend with their own um custom things. Now traditionally this used to be fully made in JSONet and was imported with the JSON bundler but that had a few durability issues and also generally low ID support.

However, with the building blocks that we have and the dependency management of Go itself, we can basically build mixins directly in Go uh that Percy's users can simply um import and get up and running with. And that's what we did. Um we've created the Percy's community mixins repo as a single place for all Percy's dashboards and Prometheus rooms that a cloud native s sur would need to monitor their um environments. These mixins are consumable not only as go methods built from the Percy's SDKs but we also do commit um the generated YAML for it in case you want to directly apply and we are already a long way in. We have most of the widely adopted mixins um already converted like humanities at CD, STL, Tanos, Prometheus and others. Um and we also have some convenience functions in Go that breaks in these Percy's dashboard configuration strcts into the Percy's operator CRDs like Perseus dashboards.

Um we also commit these operator YAMLs in case you would like to simply call Q apply on them. But you can also import them directly in case you're building some downstream operator that syncs per dashboard CRS. And we have started adding in alerts as well. We've basically relied on the upstream um Prometheus operator and Prometheus rule CRD and crafted similar um builder methods for the same. So they also use the same uh Percy's prom builder libraries for better um durability across versions. And we already have Thanos and Black Wolf's exporter runes with more coming very soon. The goal of this um community mixins effort has been um to create effective durable production monitoring um for the Prometheus ecosystem where we not only rely on the type safety of a language like go itself but also actually import the exact configuration strcts from our monitoring tools like Percy's and Prometheus in a versioned manner.

Um this creates a durable go mixin package that can effectively m your production monitoring environment on a code level and can even in theory live across your actual um workload side by side resolving with it. So to showcase all of this we have prepared a demo. Do you want to click through? >> Yeah probably know >> exactly >> on. So we have recorded a demo. So we have in here a very simple environment that has a Prometheus data source versus okay as always >> yeah you just >> I clicked okay >> yeah it's very quick clicker so we have Percy's with the Prometheus data source and we already have in here a dashboard that we built using the Percy's SDK so very simple dashboard you have in here the code As you can see here, we have a couple of the expressions built. We using the ProMQL builder and then a bit below we have a function where we are creating the dashboard for the demo application and where we are the panels that we created with those queries in here.

Just a bit of a rundown of the code to create all those panels. Don't worry, we have the demo hypo that then you can access. But because our environment has a paraces, a Prometheus and also Xbox exporter, we don't want to recreate those such parts. So we are just going to import them in our main go functions and then we are going to use those that are already pre-built and then a bit below we are going to generate okay is we're going to generate all the MLS to then apply those recording holes and those dashboards as well. So a bit rundown. So we have in here our own function and also the functions from the mixins repo and then we just have here the make file to generate those into YAML and then like I said the PR CLI to then apply the dashboards to the Percy's instances and we are going to just first of all generate those dashboards and then going to apply those dashboards into our Pers application and now as you go below a bit below we already have all the dashboards that we imported alongside inside the one that we have created just a bit of examples because everything was already up and running and we also have the recording alerting holes that we imported and that's it.

Okay, so that's uh really cool to see but of course there's a lot of work left to do. Um there's a lot that we are working on within Percy's community. We've been really busy at our keyboards especially for community makes sense. We want to make this um the def facto standard way of using makes sense in the long run. And to that end we have a lot of things pending. So uh we have things from finishing the migration to prom builder to adding more alerts to actually working with other upstreams to get their mixins ported to go as well. And we want to make these mixins way more adoptable. So we've been exploring how to include Percy's operator and the community mixins config directly in stacks like cube prometheus which is super popular and um as I I kind of wanted to reference the previous talk because the idea was so cool but um I think it might be even possible to go a few steps further. Arthur and Dominic did an amazing job um explaining the concept of trying to generate instrumentation code and dashboards from hotel semantic conventions.

Um I think we could leverage this much further by generating and go package with all the instructs we need um to both power instrumentation but also our go mix. Um so instrumentation alerting rules and dashboards would all be speaking the same metrics on a code level and community mixins could even track the upstream semantic conventions. So I'm really excited for the possibilities that such integrations bring to Prometheus and what peries can do with them. So join us um even if not as a contributor then at least as a user who would love to make their monitoring um more useful and durable. Um we have our community channels open on Percy's dev on the CNCF Slack. All of our repos have issues and um discussions enabled. Thank you. >> Thank you. Okay, first question but yes. Hi. Uh, since you age baited me so hard, I I have to ask the first question. Um, so one one of the things because why I'm coming back to JSON is always if there's something that I need for my deployment that isn't part of the spec yet in your upstream uh definitions, right?

I can always override that without coming first up to your upstream making those changes, which was always the problem with Helm charts and go definitions, etc. So is that something that you're also solving with >> mix? Um so with these um methods that we've um built once you import them you basically get the structures out output. So you can override that as you wish with your own custom values. That's what we have to do because a lot of down streams have different names, different conventions, different historical context that we can't really dictate. So you won't just get the strct as an output. So you can override that like >> Okay, that's nice. A follow-up question. Um are there any uh considerations of like integrating all of this more deeply with like generating Kubernetes manifest cuz like one of the things always were like deeply integrated with >> Kubernetes um in a slide somewhere uh >> but yeah essentially we can we have some convenience methods that allow you to basically bake that in into the Percy's dashboard CRDs.

So if you run Percy's operator, you can just simply call that method, get the Percy's operator um CR definition and apply that. Um this is actually what we have to do at Red Hat for certain downstream operators where we want to apply it on a user environment, right? >> Thank you. Nice work. >> Uh hi. Yeah, thank you both for the uh great talk. >> Hi all the way in the back. Um, I have a question about um, consuming the dashboards because I know in one of the more recent Percy's releases, we got this download also for the custom resources directly. Um, and then I I considered creating dashboards with the the Go SDK and I realized I was too lazy to do that and downloaded the uh, YAML CRS. Um, and uh, yeah. Do you have any any plans also maybe with the adoptability for >> Yeah. Yeah, exactly. So uh as I kind of mentioned a bit before uh yeah so essentially we have the go methods that would output the per dashboard as a strct that you can then call in your downstream go module.

However we are also committing the entire um generated per dashboard yl in the in case you want to directly call cube apply. both work at the moment. We definitely prefer if you use the Go SDK because that way you have more customizability on your end. Um, yeah. Does that answer your question? >> Yeah, feel free to like catch me later. >> Yeah. >> All right. Maybe we have time for one more. Uh, while Fiona can already plug herself. >> Yeah. >> Uh, over there. Perfect. >> Thank you for the talk. It was really impressing. I love how structured it is. Um but at the same time the code is a bit verbose to define dashboards. Did you see how AI and VIP coding are changing? What makes a language dashboarding language good efficient for a practitioner to to define? Is it verbosity? Is it some >> Yeah. The thing with um LLMs, right, is that they have a knowledge cut off date, which means they don't know anything about what we just picked.

Um so you have to teach it a lot of stuff. Um we definitely try commenting with go do, but I've tried and it doesn't generate it accurately enough for my liking where I end up having to do more work just to um ask it to build stuff for me. So, it's not there yet, but I think like with more um instructions, it could do a good job. There is also, incidentally, I'm looking at a somewhere in this room, but there's also a Percy's MCP server if that's something you're interested in um which can reply with existing dashboards on your end. But yeah, for generating all of this stuff, better to write manually for now up until it knows about SDK. Um, and >> okay, >> thanks. >> Perfect. Thank you so much again and