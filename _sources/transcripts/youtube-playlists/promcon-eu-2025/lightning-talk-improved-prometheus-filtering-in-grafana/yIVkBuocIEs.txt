Hello. Uh my name is uh Victor. I'm a software engineer at Grafana Labs. And uh I want to show you a bit more uh front-end things uh with with Prometheus. So I want to uh I want to show you the our newest additions uh to Prometheus filtering in uh in Grafana. So the first thing that has changed uh is the interface itself. Uh we're moving away from this UI that can potentially occupy quite a bit of uh of the dashboard space when dealing with many filters. to a more clean and compact design. Uh you can see for example the labels with many value selections are being concentrated in a uh in smaller pills and the full value list is uh now now visible on hover. The new design also uh allowed us to enhance uh functionality quite a bit. So one enhancement is now we have better keyboard accessibility. So you can cycle between uh the labels uh uh better uh tap cycle between it add edit or remove uh remove and uh next we have the dashboard default filters.

Uh so they allow you to set default labels on a dashboard uh through the JSON uh the dashboard model and they are always present on the dashboard. You can edit them. Uh you can clear the uh clear the value but you cannot remove the the label from from the UI bar. You also have the ability to restore back to your original default uh default value. Uh next we have uh the group by variable which is a new variable type. Uh it also supports default values. uh as both group buy and ad hoc filters they go hand in hand quite quite nicely and it works very similarly to ad hocs from a technical uh technical point of view. So basically what the group by does is it'll take the uh values from the from the input and basically send that to the Prometheus data source which is also going to look at all the queries uh in the dashboard find the aggregate prom kill queries and basically attach a by clause using that that values uh grouping selection.

So currently this is behind a feature toggle uh but we're planning on ging this soon. Another enhancement is around the suggestions. We now have uh contextaware filter suggestions. The context is provided by the existing filters and queries on uh on your dashboards. So basically the Prometheus data source will take all of these uh existing filters and the queries. It's going to get the metric name from the queries and it's going to attach the filters the existing filters on those metric names and send that uh in a request to the Prometheus server which is then going to have the context to bring back uh a valid list of dimensions for for the front end and we have uh we have a bit more in store. Uh currently there is work on not applicable filters. So not applicable filters are basically uh labels that do not apply in any of the queries. So they will appear as crossed over and grayed out. So they are visibly showing that they will not uh they will not work in in the dashboard.

Um there is also work plan on some performance optimizations. For example, uh value retrieval on dimensions that have high cardality is uh uh is a it's lower because it basically returns a full uh uh uh list of potentially large uh like a large uh list of uh of values while uh uh and we're trying to like batch them basically uh to have faster faster results. Recent filters is basically a history of earlier selected filters that would appear uh on your dashboard. Recommended filters is similar design-wise, but instead it will return results based on a recommendation system. And lastly, we have per panel filtering, which would allow you to filter uh the entire dashboard by uh selecting some label on some panel series. And that is it. Uh thank you so much. Uh there's a QR code with uh that will lead you to a play dashboard where you can try out the new uh the Dashboard experience.