What I would like to share with you today are some of the lessons that we learned during the process of stabilizing the OpenTelemetry system semantic conventions and why they matter in observability. So, let's jump into the first lesson that couldn't be no other than learning how to breathe in and breathe out because naming is very hard and very time-consuming. Actually, we formed the system working group more than 2 years ago and although we are at the very end of the road if it's a stabilization, we are just about to make the release candidate of the process namespace. So, the question is why did it take so long? Well, some if sometimes it's hard enough to find common naming patterns within a single team, now imagine an open source repository with more than 200 contributors from 40 different companies all with their current existing infrastructure that depend on existing naming trying to agree on new semantics, right?

And in fact, the OpenTelemetry semantic conventions repository, it's one of the of the OpenTelemetry community with more comments per PR. So, the level of detail there it's insane. So, what are the conventions that we tried to agree on the system working group? For example, we would like to name how we should name uh the memory utilization of a server. Should we call it mem use bytes? Should we call it use memory? These are actual names used by some vendors. Or should we follow the OpenTelemetry dot notation and separate the area from the resource being monitored from the type of data point and most importantly, use a state to distinguish between the different states that the resource, in this case the memory, can be. And the state it's very important because the dimension actually because it allows you to perform mathematical operations like aggregations while keeping the same name or the same reference.

But, we also try to define values for attributes. For example, for the OS type, should we be very generalistic and just have Unix and Windows or should we actually be more specific, more fine-grained and provide some value to the users by having specialized attributes like the Linux, FreeBSD, and Darwin because they are very different kinds of kernel. So, the lesson number two is that maths and abstractions end up defining your metrics. For example, there's generic concepts, right, that apply to all operating systems that they are universal like the process identifier. So, we can define it as it is as process PID. But, then there's some others that are technology specific like for example, the C groups that it's a feature just available on Linux, right? So, we include the OS name as part of the attribute name. And we do the same for metrics. For metrics, there's some general generic concepts like the memory usage that can should be able to monitor in all operating systems, but then there's some others like the slab memory that this is a kernel feature and we decided to include the OS name as part of it.

You will see some blue numbers on each slides. These will bring you to the actual discussions where these semantics were agreed. For example, in this case it took a lot to figure out where to actually place the OS name within the structure if before the resource, before the area, etc. etc. And sometimes it's even impossible to agree on what use means. For example, everyone wants to collect the CPU usage and CPU utilization, but the truth is that we just made them opt-in and we are only recommending to collect the CPU time. And the reason is that the first two are derived metrics from the third one and we are currently not shipping enough information for the backends to properly downsample those those values correctly. So, by just recommending CPU time, backends should be able to properly get the downsampling. So, in these cases the math was the arbiter of truth. And yeah, so if abstractions and shape your metrics, then also your metrics end up shaping your tooling.

By continuing with the example of the system CPU time, this used to be the definition in the semantic conventions. It used to have these two attributes, CPU mode and CPU logical number. And what this means is that for each core and CPU mode in your system, you will have a CPU time data point. But, the truth is that many users they don't care about the per core precision and they just care about the overall system CPU utilization or CPU time. And that's why we made uh the CPU logical number optional. We made it recently opt-in. But, then we reviewed the host metric configuration in the collector and we saw that there was no way to actually enable or disable the attributes that are shipped within a metric. You could you were just able to enable or disable a metric. And that yeah, led to a new feature in the collector for all the receivers actually that it was merged just a couple of weeks ago. So, it will be just available on the latest versions that you can define now the set of attributes uh that basically the collector will ship with that metric.

And for example, in this case if you skip the CPU logical number on the on your collector configuration, it will be automatically aggregated over all the other attributes. So, in other words, you will have the overall system CPU time by free. All right. And then semantic changes and tooling changes, they are not only code changes. They actually have an impact on the user experience. And let me show you a real example. This used to be the formula for calculating the memory utilization within a Linux server for many, many years. It was total minus free minus cached minus buffers. Those were values provided by the Linux kernel. And a few months ago, we changed it to this one instead in the semantic conventions and also we made the change in the collector. And when users started adopting it four, five months months ago, they saw an increase of a 7% on the reported value of the new formula compared to the old one.

So, it seemed like suddenly all their servers they were consuming 7% more of memory. But, the truth is that this is not a bug. That's actually a more accurate reflection of reality. So, hopefully with these semantic changes, we provide better alerting and better resource planning in the end. And also, what's very important is how do we provide a smooth user experience given this constantly changing ecosystem with new names, new metrics, new attributes, new formulas. And that it's going to keep evolving because stabilization is just a milestone, right? So, at Elastic, what we embrace is um basically versioned dashboards. And by having these versioned integrations, we can perform breaking changes in our dashboards while making sure we don't break our current users' view. And always they can roll back to previous versions of the dashboards if they haven't changed or updated their infrastructure yet.

And finally, while naming takes a lot of time, I still encourage all of you to keep contributing because systems can be observed in many ways that we haven't even considered yet. Just a 1 week ago, someone contributed the Linux pressure metrics that they provide an insightful and incredible insightful um information whether if you need to scale in or scale out your servers. And I specifically for each resource if you need more CPU, more memory, more IO for and also for each of your tasks. And we are already working on adding them in the conventions to the collector and finally to our dashboards. So, the final lesson basically is that great user experience is fueled by your contributions. And I would like just to end by uh giving a huge kudos to all the system working uh group because all that I shared is basically thanks to them. And also thanks all of you for listening me today.