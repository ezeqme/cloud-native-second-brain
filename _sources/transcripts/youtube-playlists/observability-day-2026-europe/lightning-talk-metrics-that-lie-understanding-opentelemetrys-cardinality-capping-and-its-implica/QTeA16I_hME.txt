All right. Have you ever had a situation where you felt like open tele elementary telemetry metrics are lying to you? For example, you ask it how many errors I hit on this application and it says zero, but you know it's failing. Your users are complaining and you look at other signals like logs and you know it's failing. But how? Open telemetry is a trustable product. But how can it lie to you? So in today's topic in today's talk we'll cover what's going on and how what you can do about it. So hello everyone my name is Sijou. I work as a software engineer in Microsoft focusing on observability and in open telemetry I work as a maintainer and approver and also as a specification sponsor across various six. So let's go back and find what happened to our metrics. So to understand this we need to define some terms like metric points and cardinality. So for that I'll be using a very simple example throughout the next 8 or 9 minutes.

So what I'm going to do is I'm going to define a simple metric called HTTP server request. All it does is count the number of requests the application is getting and it uses three attributes. HTTP URL, the HTTP method and success true or false. So each unique combination for these attributes results in what we call a data point. uh in most backends you may be using the word time series but inside the SDK it's called like metric point or data point now cardality is how many metric points or how many data points you have so if you have in this case if you have 100 unique URLs and three different values for HTTP method and two outcomes for success then you just multiply the individual cardality to get the cardality of the overall metric so it will be like 600 for this case now cardality itself is not an issue the issue is what happens when your cardality goes high. What we call as cardality explosion.

So imagine that if a developer by mistake or by intention put user ids or order ids into the URL. So it's no longer just API/order, it's APA/order/order id. Now there can be hundreds some thousands of value for this attribute. Now that can result in like huge like cardality can explode really well. Now why is it a problem? Because in open telemetry SDKs are supposed to aggregate metrics. So it has to keep each and every data point in memory. So the more data points you have, the more memory it takes. And in this case, if it's unbounded, it's going to eventually start growing its memory until it gets killed. So all it takes is just a single bad attribute to take down an entire application because of open telemetry. Uh to see how easy it is to replicate. uh if you run this this example in any application using open telemetry uh what it does is it's very simple it just iterates a billion times reporting a metric each time it uses a attribute which is a guided so it's a completely new metric point each time um so SDK has to keep it in memory so it keeps accumulating all those points in memory so depending on how much memory your application has it may not even like get to finish the billion it may get killed like much much before or if it's a powerful machine.

Yeah, it may survive a little bit more. But what is going on here? This is not very good for Open Elementary, right? So, Open Elementary folks decided we need to solve it. So, they introduced some safety net to prevent the application from crashing even when a developer makes a mistake. Uh it's a stable specification for over a year or maybe two. Multiple languages have implemented it. Uh let's take a look at how it works. Um I'm going to use a very simple example. Uh so I'm going to put a limit of cardinality limit of three. So even though in real life it's around 2,000 is the default limit. So what it means is that after you after the SDK sees three data points, it will stop accumulating anymore. It will stop adding any more data points in memory. Uh so I have an example here where uh these are the requests, various requests which the application is receiving. So there is a get request to your homepage which is succeeding.

All good. So you track it and then you get another request, you track it. So first three ones you're good because the limit is three. Now that you receive a new combination which was not seen before. So at this point SDK says no I cannot store it because I was told my limit is three. So what does it do with the ext? What does it do with the new metric point? It puts it into something called overflow which I'll show in the next slide. Uh but one thing to note here is that it only puts things into overflow. It's a if it's a newly uh newly unseen combination or it's a neverseen combination. If it's an existing thing like the request to the homepage which is succeeding it just continues to work fine. What happens to the overflow things? So as you all know like when like every few seconds SDK starts uh exporting metrics. So this is what gets exported at this point. So it exports three data points which looks like normal like just reflecting the reality.

But the fourth one is very special. It uses an attribute which is not not something you provided. It just invented a new attribute called hotel overflow and puts all the things which did not fit into the original one into the overflow. So what really happened was that one overflow data point now represents two completely independent requests like one was a post request which failed another was a get request which succeeded but now they are bundled into this overflow. Now this is done for a good reason because this was done to protect the SDK from crashing. But now this introduces a very subtle problem especially when you are cing and that's where the entire topic is of metrics lying. So imagine you ask your back end can you give me the count of requests which were made to the URL API orders. The real answer is 15 but if you do a query you'll get zero because the 15 requests to the API orders they went to the overflow bucket.

It never made it to the back end with the URL equal to API orders. Now it may seem like obvious right because I I already hit the limit and I'm trying to put a different order or different URL which we have never seen before but is it just limited to those queries let's see what happens if I do a very simple query what if I ask the system hey can you give me the number of requests that were successful in our case we would be expecting around 280 based on the numbers before but you would only get 260 because 20 of them went to overflow or in the extreme case which is what I started the talk with. If you ask the system how many failures I have, it will come back and tell I don't have any failures for the system. It says zero. And this is the problem because by the time your application hit the very first failure, you already exceeded your cap. So the system will never track any failures leading to situations like metrics are just lying.

So it may not always appear as zero. It may usually appear as very much underounting the real thing. Uh one thing to note here is the real uh the total count of requests they will always remain accurate. It doesn't matter whether you are in uh cardality capping situation or not. So one additional insight here is that it's not just the bad attribute which is going to be incorrect. So once you are in the overflow situation any query which involves attributes are going to be incorrect not just the high cardality one. Uh there is an assumption here which is in open telemetry. There is another place where you can put attributes which is on resource. Typically that's where you put service name and host name. So they are unaffected by this particular uh cardality capping. Now how to detect it? Uh it should be obvious because as I mentioned open telemetry always uses a well-known attribute to uh indicate that overflow has occurred.

So whenever you query for metrics you need to ask is there a presence of overflow in that duration. If yes, then you need to be aware that that metric may not be telling the whole truth. If not, yeah, all good. Uh, but this is so simple, right? I mean, you can just query for overflow. In fact, it is true. It is very simple. Uh, it's very standardized. So if you are looking at a dashboard you can write either you write a query yourself or you can use the uh your alerting system whenever it queries for metric ignore or don't act if you see if you detect overflow or if you're using an AI agent to query the metrics you can tell that okay if you see this overflow then don't trust that metric uh and I don't think there are any tools which does this automatically today so you have to be like explicitly uh writing this in your query or instructing your tool to do uh if at all like if there was a tool like I'll show you one example tool this is not a real production tool this is something which I use for local testing it's a tool called dash aspire dashboard it just accepts OTLP and displays metrics locally uh so this is one example like I'm cing a metric and it detects that okay there is overflow uh so it just gives you a like nice warning at the top saying that you may be looking at potentially uh incorrect or uh invalid metrics this is just an example not real system uh I want to I conclude with uh the impact of temporality on cardinality capping.

As you most of you know open telemetry operates in two modes for temporality delta and cumulative. With cumulative a data point has a almost like an infinite lifetime. Once it's once you see a data point it can never forget it. So over time the cardality pressure accumulates and once you hit the cardality limit if you're using a cumulative mode there is no escaping. You cannot recover from that ever un unless you restart your application. But if it's delta cardinal delta temporality SDKs are free to forget after every cycle. So if you don't use something uh then it gets reclaimed and you will go back to your normal mode. Uh it doesn't mean you can simply switch to delta and be done with this problem because your back end sometimes forces you to pick one. So I'll conclude u there is a uh in cardality capping is introduced to do a good thing which is to prevent your application from crashing but it introduces a very subtle observability blind spot if you're not careful and it all it takes is just a single attribute to poison your entire matrix query and since it is standardized go back and see if you have a tool which can automatically do this for you instead of like relying on a human coding it by hand all the time.

Uh so it exists for a very good reason. Don't ignore it. You can even alert on it. You can alert set an alert on overflow attribute. Uh don't ignore it. Just monitor it. I think that's all. Uh thank you and thank you for waiting till the last moment. Yeah, please feel free to scan this if you want to share feedback. Okay.