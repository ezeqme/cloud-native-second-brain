Live from Observability Day, it's the spectacular game show. >> All right. Thank you for that game show voice. I'm not going to try and do the same, but that's all right. Um, hello. I am one of your uh co-presenter, Alex Bowden. Thank you for coming to the spectacular game show. Uh, with me I have my co-presenter. >> I'm Adriana Vila. We're going to do this the whole time. Hey. >> Oh, totally. >> I like it. I like it. >> Um, and with us we have three willing participants. Uh, we'll start on this side. >> Hey, uh, in public. >> No game. No game show voice. >> All right. Next time. >> Telemetry. What's your role in open telemetry? >> Um, I do collector things, so not spec, but I'll I'll do my best. I am Marelia. I do a lot of things and I hope that I don't get fired from being a GC if I don't win the game. >> And I'm Tyler. I work on the collector in the Helm charts. >> All right. Thank you for joining us on this fantastic journey.

So, we have here a series of incorrect statements about the open telemetry specification that we're going to read verbatim. They are long statements, so bear with us as we're reading them. Um, contestants will buzz in to try and correct statements to earn points. If a contestant doesn't know the answer, uh, we're going to ask the audience. And, uh, at the end, whoever has the most points, as you can see by these wonderful, uh, LED panels controlled by our, uh, official scorekeeper, will get the winning I I would say prize, but we don't actually have a prize. So, >> bragging rights. >> Bragging rights. Uh the rules you can buzz in at any time. However, we will finish reading the statement for the audience. Participants must start their corrections with actually the spec says. If a participant uh buzzes in and even if they have the right correction and they don't start with actually the spec says, another participant can buzz in, steal the point.

So if you don't know much about the spec, this is how you make your points. This is uh also guessing is totally allowed. If you guess and it's uh vaguely correct, we might ask for someone else if they have a more specific answer. If they don't, then you get the point. The the point of this is nobody knows all of the spec. And uh that's totally fine. So guessing is totally welcome here. So without further ado, I'll pass it to Adriana with our first statement. >> First statement. SDK packages consist of the crosscutting public interfaces used for instrumentation. Any portion of an open telemetry client which is imported into third-party libraries and application code is considered part of the SDK. >> Actually the spec says this about the API. >> That is correct. Correct. We'll have a dog. >> Yes. >> Yes. And would our contestants like to talk about the difference between the hotel spec and API? >> The SDK Candy API.

>> Damn it. >> I know. >> Yes. >> That's okay. >> Sure. Yeah. The the API is responsible for producing telemetry and the SDK is responsible for um exporting or maybe processing the data before it's exported out of the application, right? All right. The second question has to do with instrumentation. A logical unit of software which with which the emitted telemetry can be associated is referred to as the instrumentation library. It is uniquely defined by its name, version, schema URL and attributes with only the name being required. Tyler, >> I don't think instrumentation scope has a schema URL. I think that's higher up in the data model. >> Actually, the spec says whatever he said. >> Incorrect. >> Nice try, though. I like the I like Yep. Let's look again. >> Oh, actually the spec says the name is not required on an instrumentation scope. >> That is also incorrect. >> All right. Would anybody in the audience like to chime in?

>> Yeah. >> That's right. It is instrumentation scope and not library. >> The audience gets one point. I don't know. >> It's funny because your answer included the instrumentation scope which Um >> I was too I was too used and I couldn't read it behind me. >> That's right. So the spec defines ination scope to allow differentiation between the source of telemetry emitted. Uh this allows instrumentation library to identify themselves as a source of the data in the telemetry and the rename was started because of a conversation that started in about 2022 where the meter and the tracer providers needed to be associated with some kind of scope. uh and the name instrumentation library was just uh maybe too a little bit too broad for and didn't quite fit. Next question. >> Okay, the topic is metric instruments in the open telemetry metrics API. Both synchronous and asynchronous instruments support associating measurements with the current active context for synchronous instruments.

This happens when the value is recorded for the asynchronous instruments. This happens when the register call back is invoked. >> Actually, the spec says async instruments can't have exemplars. >> That is correct. >> Yes. Good job. >> Would we like to elaborate on that further? >> Yeah. So exemplars are really cool thing in open telemetry where uh when you are recording a metric if that if that recording is part of like a trace context like a user did a thing and now your API is doing something normally it's hard to associate a metric with that transaction but in hotel you can record a metric and say hey I have a trace happening at the same time like put this trace ID on that particular exemplar on that data point and you can associate that measurement with the the trace that happened. Uh you can do that in synchronous instruments because it's all happening like at the same time. But async instruments are happening asynchronously.

So you can't pass the context into them. So that's I'm pretty sure that's why you can't have exemplars on async. >> Yeah, that's right. You can't you don't know what the context is when the when the operation happens. So all right, nicely done. The next topic is span processors. Builtin span processors are responsible for conversion of spans to exportable representation and passing batches to exporters. One such defined processor, the simple span processor, exports spans asynchronously to avoid blocking the application thread. Um, actually the spec says the simple span processor just passes the spans along. doesn't do anything. >> So, one more time, >> the simple spot, sorry, actually the spec says the simple span processor just is no op, doesn't do anything any processing and it is not asynchronous. >> That's correct. It is not asynchronous. Uh, in fact, it is synchronously exporting every span that you emit, which means that as soon as you're putting the simple span processor in your application, you're going to have to wait every time you're trying to export a span.

Please don't use a simple span processor in your production workloads. Unless you really need those spans to be emitted in maybe your uh like a cloud function that's going to get terminated or something. All right, >> the next topic is span context. Open telemetry specification defines the span context as the portion of the span that needs to be serialized and propagated such as span ID and trace ID to facilitate head and tail sampling. The span context is mutable. Maria. >> So actually the spec says the so the span ID and trace ID are attributes. Uh and also I don't think is mutable. >> That's correct. >> The span context is immutable. >> Yay. >> Yes. In as much as everyone wishes span context to be mutable for the sake of proper propagation and sampling, it is not. And in addition to the ids, the span context also includes trace flags and trace state as pro part of the W3C trace context specification.

>> All right, moving on to cardality limits. The number of unique unique combinations of attributes is called cardality. For any given metric, the cardality limit is hard is a hard limit on the number of metric points that can be collected during a collection cycle. Although the default value is 1,00 it is only a suggested value and implementations should make this configurable. I don't think the hotel spec sets any limits on cardality at all. It just lets you put whatever you want. >> Actually, the spec says there's no limit on cardality, although it's encouraged. >> Thank you for trying to steal the point. That is also incorrect. >> Come on. >> You should just never try to steal his point. >> There there is indeed a limit on the cardality. Actually the spec says the limit is not one touch. >> That is correct. >> Would anyone like to guess at what the limit is for the point? >> More than that. It's less than that.

>> All right. >> Pablo Pablo gets a point. >> The default value is 2,00. So cardality explosion has long been a concern of metrics collection. Open telemetry specification tries to ensure that limits are in place to prevent end users application from using too much memory. The limit can be configured one of three ways. Either through setting an aggregation cardality limit on a view, setting a limit on the reader or fall back to the default value that we just learned about. >> Okay, the next topic semantic conventions. The attribute server address can represent either the server domain name, IP address or Unix socket name. When the IP address is available, it is recommended for instrumentations to perform a reverse lookup and cache the results to mitigate the performance concerns of such operations. Actually, the spec says something about that name is wrong. I'm just guessing. It's not server.add address.

It's like something.server.add address or there's an s somewhere or the name's wrong. >> Not quite. >> Any other takers? >> All right. throwing it to the audience. >> That's correct. >> Defending champion Miller for the win here. Actually, the spec says that the instrumentation should by default avoid doing a reverse lookup. a technicality >> that is that is a technicality. Our questions do cover semantic conventions and the protocol as well. But the spec for the semantic convention says uh speaking of which the next question is about the open telemetry protocol. One of our favorites, open telemetry protocol. This time we did not put line in the middle of it. Uh, OTLP specification describes the encoding, transport, and delivery mechanism of telemetry data. For optimal network efficiency, OTLP exporters use gzip compression by default unless explicitly configured otherwise. Actually, the spec says I don't think it's gzip as the default.

>> That is that is correct. Would anyone like to take a guess as to what the actual default is? >> I want to say JSON. >> No. >> But it's not J. >> Set std. >> So the actually spec said std. I don't know. Tyler, are you going to take a guess? >> I forget what the third one is, so I I won't. >> All right. Actually, the spec says that the default value is none. Um, but I will give the point to Maria for that one. >> Yay. >> Does anybody want to guess why the or think through why the default value is none? That's all right. Um so the default has no value because uh the different language sigs can choose their own default based on what makes sense for their language and their platform. For example, mobile SDKs may choose gzip to save bandwidth while server SDKs may choose none for better performance. >> The next topic is profiles. Original PROF format is birectionally compatible with open telemetry profiles allow allowing existing PROF tools to parse OTLP profile data without modification.

The only other format supported by the profiling signal specification are JFR Java SDK's flight recorder and Linux Perfia. >> Actually the spec says is not uh birectional compatible. I think is just forward correct. That's right. The it is forward compatible, meaning that you can convert between Prof and OTLP profiling format. However, existing profiling tools do not support the OTLP profiling format, which means hopefully someday we would like in the future for profiling tools to support it. Yes. >> Or maybe there's some already in the works if we're lucky. >> Sure. >> Yeah. Does anybody here know if there's tooling that supports OTP for profile formats? No. It's all right. >> Pyroscope. Oh, very nice. There we go. >> Today I learned. >> Today I learned. There we go. Are you sure you don't want to run this panel? You could. >> All right. The next topic is resource detectors. Resource detection logic is expected to complete quickly since this code will be run during application initialization and must never return an error.

Detectors should have a unique name for reference in configuration. >> Actually, the spec says they must have a unique name for reference. >> That is incorrect. Actually the spec says so it can return an error and also it never m says quickly is never quickly is very slow. >> I'll take the first part as the real answer. Uh it's correct. Uh resource detectors can return an error. Um, this one got me just a few weeks ago when I was testing out the host ID detector. And failure to detect any resource information must not be considered an error. However, an error that occurs during an attempt to detect resource information should be considered an error. I don't know that I fully understand this delineation. Um, maybe I have not read the spec enough, but that's what the spec says. Okay, now we're going to take a little break from all these really, really hard statements and uh we're at this section called real or Hulu.

In this special question, we're going to show our contestants a series of functions that should be that may be defined in the open telemetry API specification. Our contestants have very high-tech paddle paddles and they can tell us if the method is real and really defined in the hotel API or if it's a hallucination. And whoever gets the most of these correct gets the point. So it's only one point for six six guesses. All right. And I'm going to try and manually record the guesses. So here we go. >> Span.attribute attribute real or hulu. >> Yes. >> Good job. >> Easy one. >> Well done. A little softball to start. You know, we don't want to discourage. >> Hope they're all that easy. All right. Next one. Baggage. Clear. We've got Pablo gets a point. The baggage API does not specify a clear method for the baggage. It does require implementation to have a mechanism to support clearing the baggage but it doesn't specify how this should be done and it could be implemented as a method that just sets an empty baggage.

So >> next one span context is remote real or hulu? Anyone? No. Just >> give me a thumbs up. >> I think this one's real. >> Correct. >> Nice >> function exists and it returns a boolean value which is true if the span context was propagated from a remote parent. >> All right. My favorite updown counter has a subtract method. Well, >> Tyler is jump in. You're locked in. All right, >> that's correct. The API for updown counter is just an ad function and you can add negative values to decrease. That's right. >> Next one. Logger.enabled real or Hulu. >> That is correct. To help users avoid performing computationally expensive operations when generating a log record, a logger should provide this enabled API. >> All right, last one. I think this is the last one. There might be another one. I think this is the last one. Configuration not parse. This is my favorite part of the spec and I should really know the answer to this.

>> Well, so I kind of go want to go like this. Yeah, >> because depends who is implementing it is like this depending on the SDK. It is like this. >> Well, the question is is it in the API or not? >> Well, yes. >> All right. >> This was a trick question unlike unlike all the other questions we've asked so far. Um because there is a definition for configuration parse method. However, it is in the configuration SDK. >> See, >> so it is not an API. See, I was very thorough with this. >> So, final score of real or Hulu. Pablo gets five, Merilia four, and Tyler has four. So, Pablo gets the point. Well done. >> Shout out to the configuration. If if you haven't tried to configure an OTEL SDK with the YAML file, it is the best experience, the best configuration experience in open telemetry is using that file and just instrumenting all of the configuration that way. Uh if you haven't tried it, try it in the collector.

Uh it's amazing. It's so good. >> It's also stable now. >> Stable. It's on JavaScript. >> I work on that. So it's working. >> If you want working, it's not me. >> You can join the session tomorrow. Um >> yes, >> on the project update. >> It's also on the demo. >> It's on the demo. Auto demo as well. >> On the demo. >> All right. Back to the last couple of statements here before we throw it to audience with some more statements. Okay, the topic is entities. The open telemetry semantic conventions must define a set of identifying attribute keys for every defined entity type. If an observer cannot reliably obtain all the identifying attributes for an entity, it should emit partial identifying attributes to maintain some visibility. Actually the spec says um you should not emit partial identifying attributes. >> That's correct. >> And can you elaborate? >> I mean if you cannot identify your entity then what's the point?

>> Yes. Correct. >> All right. I think we're on to the last prompt or at least the last one I printed. So I hope it's the last one. Uh this is about auto instrumentation. For production grade automatic instrumentation to function correctly, application owners should update their source code to include the open telemetry SDK as a dependency to ensure proper instrumentation scope resolution. >> Actually this spec says that is not a requirement especially depending on the languages. You can just initialize with the instrumentation not necessarily have to add the package. That's correct. The automatic instrumentation does not require modifying application code. In fact, ideally, you really shouldn't have to to use it. Otherwise, what's the point of calling it auto instrumentation? Um so auto instrumentation uses runtime code manipulation or monkey patching or EVPF and uh in fact even the instrumentation libraries that auto instrumentation uses don't generally take a dependency on the SDK because they try to limit their dependence on APIs to avoid any kind of breaking changes across implementations.

And now we're at the winner stage. Let's see what do we have. Nicely done, Aurelia with six points. >> Well done. >> I can keep my job. >> Also, I appreciate nobody going over nine points, so thank you. >> Yeah. Yeah, we had a requirement to not >> in case you wonder what happens if you get to nine, it just explodes. >> Uh these are the sources for all of the prompts. And now since we still have I think some time. Yeah. All right. Awesome. Um we're gonna ask the audience to chime in. Does do we have a microphone for them? >> We need to use one of these. >> All right. >> Yeah. And you have to do your make your own buzz sounds. You have to speak. >> You have to make your own sound effect. >> Okay. Uh since >> Do you want to >> I can there there's a better stand there. >> All right. Um, since it is recommended that OTL specification implementation has a global error handler to guarantee exceptions are not leaked to the user code, it is okay for the SDK to throw unhandled exceptions for errors its own operation.

Anyone like to try guessing what's wrong with this statement? >> It's it's it's probably not okay. Would anyone like to steal his point? >> That's correct. The SDK must not throw unhandled exceptions. All right, we have a couple more here. Py is the topic. The open telemetry protocol support is used to transport the signals of open telemetry traces, logs, metrics, and baggage. Each of these signals is equally important. >> Yeah. >> No, incorrect. >> John stolen. >> Stolen. Well done. Uh that is correct. Profiles is the other signal that gets transported. Baggage is meant for internal kind of application to share context not for data to be exported back end. However, there is a baggage span processor if you really want your baggage to be associated with span that you can use. Next topic is context. A context propagation mechanism which carries execution scope values across API boundaries and between logically associated execution units.

Crosscutting concerns access their data in processing using the same shared context object. A context must be mutable and a single context object shared across an application's lifetime. Correct. >> Done. >> That's right. Write operations must result in a new context object containing the original values and the values to be updated. You want me to take this one? I'll take this one. Next one is about compatibility. This this is very very recent. Um the specifications are with the merger of open tracing and open census. Support for these formats is required by any implementation that wants to be compliant. >> Oh, I I heard a duck. >> Sort of. I want to I want to give you a point, but I also feel like there's like a slightly more accurate answer if anybody wants to come in. >> The spec does say some stuff. >> All right. Actually, uh currently only support for open census is required for implementations because open tracing is officially deprecated as of like a three days ago, four days ago, I think.

>> So, it's it's happening. As of March 2026, open tracing is going away. Uh it will be in the spec at least until uh March 2027. So, if you're if you really care about open tracing format, uh yeah, get on it. >> And that brings us to the end. But before we leave you, some housekeeping. First of all, you can find open telemetry in the project pavilion in booth P19A. So come tomorrow, all day Tuesday to see us for open telemetry everything. We will also be having some onsite SIG meetings happening throughout the week. So come to the project pavilion area. There will be some picnic tables that will be sitting down at where the project pavilion sorry where the maintainer meetings will be taking place. >> Thank you for coming. Uh if you have any feedback, please scan the code and send us your feedback. >> Thanks for being a great audience. Thank you to the participants. It was fantastic. And to our scorekeeper, thank you.