Welcome to this session about evolving schemas in observability. Uh my name is Bina. I'm a software developer working at Redhead contributing to the open telemetry project and I'm also an organizer of the CNCF meetup in Ar. >> Uh my name is Sebastian. My name is Sebastian. I work as a staff engineer of observability specifically um logging and event storage and ingestion at Shopify. And yeah, I'm also an organizer of the CNCF chapter and meetup in a Um let's see what we are going to talk about today. Um we will give you a short introduction to schemas and semantic conventions in the context of ser in the context of observability. Then we will look at how open telemetry is doing semantic conventions um and show you some some changes and related challenges and techniques to address this and um tradeoffs and then we will show you a real world example not specific to open telemetry um at at Shopify how Shopify is doing um schema management management at ingestion time and after that you can ask questions and we will try to answer them.

Okay, first let's start with a simple example um to understand why we need schemas and semantic conventions. Look at this three services, services A, B and C and maybe even more services uh created in a big company um from different teams over different time and there was no agreement or even no communication at all between these teams. they somehow have the same metric, a metric that um tells you how many HTTP requests are there. Um but they don't have the same naming. They don't have the same attributes or labels. Um so it's a bit of chaotic. And if you want to understand the RPS going through those services, um you get a query like this, which is not pretty handy. And um with more services, uh it gets even worse. So um here you could um agree on a unified schema. This is u an example from hotel. Um a metric that does the same thing or even more HTTP server request duration. Um it has a common it defines a common schema name.

It defines known attributes and it defines known keys. So with a unified schema you would then end up here. same services, but now you don't have to think about how do I name my metric, what is my metric doing, what are the attributes, labels, um all of the services use the same um schema and then you can have a really nice query like this. >> Yeah. So this brings us to a situation where we never no longer need some weird application logic in our queries which prevents us from failure in our dashboards and all the things in the future which if the story would be told here it would be quite nice. Unfortunately or fortunately there is some development in all kinds of semantic conventions or also in specification um which yeah have has changing things over time. We can see this here based on the example of the semantic convention in open telemetry where things get adopted quite early like HTTP semantic convention and at some point they um get some stabilization phase and then you will face some breaking changes.

So we have always there some change potentially this is now stable but there are other things like kubernetes metrics where this happened um and there is always new things like jai namespace which got added and so on and so forth and even from the history uh you can see that open talent tree um got uh yeah started by a merger out of open tracing and open census and then more things came in over time. So even there we start uh we'll always address some change. Um same thing with uh applications that we run our own inside of our uh yeah own development cycles. Um we will always have this. How does this change look like? Uh the simplest form would be potentially just a renaming. Uh so for example this was done here with HTTP server uh duration was renamed to HTTP server request duration. The unit changed from milliseconds to seconds which is then not too uh that easy anymore. But there are more things which can happen like deprecations, attribute splits or other value transformations um that we need to address.

So there is luckily a schema which has at least some of this information already there like you can see the renaming or when some attributes are rename uh added for example uh so that we can keep track of this but um it unfortunately doesn't reflect all the things like having um unit changes in it. So does it make then things actually easier if we have a schema which helps us to keep track and uh yeah semantic convention which is changing and also for our internal services we have also some change that we need to address all the time. Yes. So if we agree on something first it gives us at least some consistency. So which means we have eventually different names and different unit meanings and different attributes but we can pin this down to some specifics which um allows us also to have some predictable cardality on certain serieses so that we don't um yeah kill our database accidentally. This also helps us with having dashboards which are reliable to at least one specific version.

So once stuff works we can have dashboards and queries. Um and then we need to be careful that they don't fail silently at some point. That's something that we will discuss then. And uh also with the instrumentation it's a bit safer because what we have seen on the first slide not everyone is inventing the we completely new. So we can just go to the specification and just follow it. Final step is we can enforce some automated um validation at some point potentially even in the CI pipeline to catch things early so that we don't have a service which reports a different metric name is no longer represented in our overall dashboards or alerting uh so that nobody noticed this. So speaking about querring and alerting um this is the first option that we potentially have to solve this problem. We see here a Kubernetes cluster which multiple Kubernetes clusters in VM. Some signal sources which report data in different semantic convention versions.

In reality it looks a bit different because potentially in one cluster you will also have multiply workload which also reports data in different versions. This is then sent to your back end where you store the data and now you need to write a query similar to what we had before. A query which matches different naming conventions uh that people had now with different um version conventions. We can see this basically here on the next slide on the bottom right. Um if Sebastian moves a bit um so you can see basically we can write a query with an or statement and then just um yeah continue let it continuously work. There's some problems with this. Um, luckily there is some movement in the or some ongoing effort in the progress community for versioned reads uh in a great talk that was presented at CubeCon in London. Um, you can go with to figure this out a bit but it needs to be implemented next to your query engine.

So potentially you have a schema engine or something which then does handle this observation and you need a good schema for it also for your internal tooling. Another option would be to handle those things at instrumentation time. So which means all our services use the same SDK version for example that report the same semantic conventions. Um same thing with the collector. So we can do some control change from there. It's potentially also quite hard because often you're not in control of the service that is reporting the data and uh yeah there are other issues like if you for example go with auto instrumentation at some point um potentially this is an older version is deployed a new version is deployed or you just went with the SDKs ahead we for example in the operator hold back the um currently the Java and net or instrumentation regarding this HTTP breaking change and uh yeah to uh yeah so you will run into this issue at some point and even there it might be that your SDK is already ahead so like instrumentation time might be hard same thing with the collector there is no way to stabilize um the semantic convention version for a while uh I guess this will not now long last forever so you always need to move on there too so we need to coordinate this change somehow but there is another option so we could also potentially try to upgrade this in uh at the back end.

Uh so when we ingest the data so like we have different signal sources again they all report in different versions to our back end and then it gets uh translated into 139. So why can we do this? We can take the schema and potentially some extra information because it doesn't reflect all the things and then migrate to a higher version. There is some development on the schema processor. It sticks around for quite a long time. So potentially you need to build something on your own but you get the sense we could potentially do upgrade there and then we just need to navigate and organize the um alerting and quering over it. We still have some issues when uh during the migration period if we have some requests which track for example the uh average response time over the last 28 days. It's a sliding window. So we need then at least to match it in the queries for two versions. Um speaking about this we have multiple options to handle this.

We can once move forward so like go always to the newest version or go backward to for example an internal version where we have a representation of data that we just need for our internal um things. Yeah to more or less summarize what our options are. So we can handle things at instrumentation time which is potentially the cleanest way but also the hardest way which is depending on your setup not even possible. Um then you can do things at ingestion time where it's also depends if this is possible for you because you potentially need a lot of um yeah extra effort for your platform team to follow these changes all the time and be ahead of the rest of the um environment or do the things at query time which might be also a good bet but it's not implemented in every data store so there is yeah still some parts missing >> and I will show you now an example at how we do this at Shopify for logs or events at ingestion time.

So let's have a look at how um observability looked at Shopify. That's really like uh uh uh an overview. So most of our signals, metrics, logs, events, traces, and profiles come from Kubernetes services uh from Kubernetes clusters because this is where our services and workloads are running. But we also have virtual machines and external services like um like cloudflare for example. Observe itself it's it's it's multiple components. First it's the storage for matrix we have uh prometheus thanos for the rest we use click house and then on top of that um we implement alerting queries dashboards and analytics. So our users um interact with observe through a a custom graphana build and um they are alerted through page at YouTube and yeah if you're interested in more details about this there are a lot of nice talks out there can find easily. Um now let's zoom in on logs and events. I will use logs and events interchangeably because for us it's the same thing.

Um again we have the uh source on the left and logs and events are coming in via HTTP or Kafka depending. So this is um push versus pull like for example cloudflare it pushes metrics um within the clusters uh logs within the clusters logs go into um Kafka and then we can pull them from there and we currently support um OTLP JSON and message pack while JSON is the most widely used format. So this goes into collectors and collectors that's that's uh that's that are custom uh collectors written in go that what they do is they they batch the um events or logs columnarizes them uh or transcode them into format so we can ingest them into click house right click house is a columnat data um base if you like they I think they also have a stand outside um so then this is ingested into click and for click house we have multiple tenants And um like one tenant is one cluster. We just group um several services in in into a tenant or some big um services have a single tenant like our core service has a single tenant and then there's a catch all tenant for everything that don't has all tenant um for example.

Yes. And then this is queried through um investigate API. This is just our query layer. So imagine it as query from Grafana um comes in as JSON and is translated to SQL is time slice and there's more processing like caching happening and then um this is sent to click house. Click house returns it and um it's presented to the user. Then um all these three components in the box has some one thing in common. They are configured by the schema manager. The schema manager knows the canonical logical schema of our logs and tells the observe collector how to um transcode or columnarize our events. So they can be ingested in click house where the schema manager also creates the physical schema like the actual tables and the investigate API also gets uh the the structure of the tables in click house from the schema manager so it knows how to run queries. Um let's have a look at the physical storage to make uh so it makes more sense.

Um this is the physical uh the schema of the physical storage as we have it in click house like you have a time stamp column because every should have a time stamp if not we we're just set it to now you have separate we call them promoted columns. So, Clickhouse is most efficient. It it's a column database. So, you can do uh uh uh operations or analyt analytics on columns and um so if you have attributes that you use a lot or that are part of every event and you want to run analytics on or you want to use for filtering, make them separate columns because then you will get really good performance out of this. But since you can't tell which I mean we allow any attributes so we can't have a column for every attribute uh in in reality like the the promoted columns are like I I would say about 100 but we for the attributes we don't know about they go into maps so here you see typed maps right so according to their type they go into this map and then their performance is still really good you could have like a single string column but if you want to run then analytics on top of that you have to pass them to numbers and so on.

So um typen maps they just improve performance. So that's our physical um schema in click house. Now let's look at the logical schema definition. The logical schema defines it's the actual definition of our um logs and events schema. It's it's it's it's pretty simple. So first it has a name obviously right- which is like the ID and then it has a tenant. So we know to which tenant we want to apply the schema and it has fields. So a fields have field has first the name which is the column name in in click house. It has paths which point to the um uh to the path in the payload path for JSON and for OTLP it would be the attribute name and then it has a column type column type because we need to know okay um not everyone might send request ID as a string maybe they send it as an integer so we before ingesting it we coers it so at query time we have really good performance like this makes more sense for attributes that are numbers.

Um here's an example about how ingestion works. Um you get your events from Kafka HTTP consume or receive them. We have a batch of um an events here. It's three events and then we build a column representation. This is Click House native format that goes into Click House. So for the um for the uh for the columns like for each each um promoted column we just put the like each row like each event becomes one row and then the um attributes that has uh its own columns that goes into columns otherwise it just goes into the map and it's written into click house which is pretty efficient. So now how how do we how how can uh our uh users define those schemas or update those schemas. So what you do is what we do is we have a git repository where those schemas lives like the YAML file with a bit more of information. I just showed you uh uh two slides before and what you do is you create or update one of those schemas and create a PR.

So the first thing we do is we we validate if if this will work right. So in a GCS bucket we have a specification of a YAML schema and we have a status of a YAML schema. If there is if if there doesn't exist, this is like Kubernetes spec and and status a bit. If if there's no status, we just can go ahead because this means the schema doesn't exist yet. if there is a status. Um, we see if the operations like one operation you can do is like adding a new column. It's it's pretty simple. It's like um it's it's it's nearly for free in Click House. It it cost you nothing. What you can't do is or what you could do is you can change the type of a column in Click House, but that's a very expensive mutation, especially if you have pabytes of data. You you would have to rewrite all those data. And if you have an GCS, it gets even more expensive. So we don't allow this like um you can't change the type of a column and we would reject this.

Okay, let's go with the example of adding a column. Now this PR is ready. It's validated. It's planned and PR get merged and it get uploaded into um the GCS bucket as specification of the schema. What now happens we again we have our click house tenants here and each click house tenant has a schema manager running which watches those specification yaml and and what it does it just um if there's a change to the hash of the file it downloads it and then it it reconciles click house this works it it gets the schema from click house it um it calculates the schema it it wants to um see in click house like um the specification it passes them into an a compar shares them and calculates the um queries that need to be executed against click house. Once this is done, it will just upload the spec yl as the status y because it's the new status now of the of the schema. And then the UI query layer and the collector both um use this information.

Okay, there's a new column now. I can like the collector now put will put the new uh will put a attribute in a column now instead of the map and the UI and train allows. Okay, I can now query this new column uh in addition to the map, right? So I have I have to I have to look into two places. M maybe one more example um how this works. So we have three events incoming and we have a schema, right? Schema for the first two events one two um there's HTTP uh request status code column and the HTTP status code goes into this column as type integer. For the first request it goes into the column. Uh for the first event it goes into this column. second event the name changed and it won't go into the column it will go in the strings attributes map now so what we do is then we add to the existing path we add the new path and then it will go into HTTP response status code column again I mean usually you try to not let step two happen and change it in advance this is just for illustration so um we promised a blueprint here are some takeaways um that's that's not exhaustive but these are some um yeah some some steps you could follow to um get schema management working for you.

You define a canon canonical logical schema. This is the YAML file you saw, right? Um and version schema changes explicitly as I showed you with the um with this pull request example. Um you preserve compatibility during migrations, right? So you saw this first we have the path then we have paths and you can have more than um two paths right so you like your old and your new names will work um choose a primary normalization layer um we we we started with ingestion um validate changes before roll out CI it's it's hard right it takes time but I think it's worth it because uh in the end the cost will be higher if you don't do it keep dashboards and alerts aligned That's easily said, but I think it's it's it's it's really the hardest part. If you don't have them um centralized and versioned, so you should also centralize and version them. Then this is much simpler and measure progress and retire old mapping safely.

So what we do is we just keep track of how often um a log or any signal is queried and how often it's ingested and after 30 days without a query or without an ingestion we assume uh it can go away. So the goal here is not to avoid change because like I mean you can keep change back right but eventually it will come at you and it will come at your heart. Um so better start doing it in a control in a way you can control it >> which is something that might occur be easy. Uh so we've seen that standardization is important even for our own internal uh tooling. We have multiple options to handle this. Right now there is nothing which is super satisfying just works out of the box. Um and there is one project we would like to outline which can potentially help you with this which is the open central weaver project. It lets you define um schemas even for your internal usage. It lets you um validate things in CI and so on and so forth.

And with that said uh we would also like to make some advertisement for tomorrow for a session from Archa Nicholas uh basically about treating um schemas as a first class citizen. >> Yeah. with it, I guess. >> Yeah. Thank you. >> That's it. >> Any questions? Okay, if there are no questions, then maybe if you would like feedback, give us feedback. Follow this. There's a question. The light is really bright. Sorry. Just speak up if you >> have a question. >> Yeah, just just come down. >> Yeah. Yeah. Just just go there. It's okay. >> Okay. So maybe this is quite simple for you but um how do the producers of the data when their data is normalized? How do they query it? So for example I have a path a in my metric log whatever >> and then it gets normalized right so how how do you query it how do you know what to query in the end do you go look up the schema I know that's the question >> I mean we as you see in the in the in the in the query layer right the query layer knows about the schema so it can map it accordingly >> oh so you also allow >> I mean that's that's another part we we don't only do this we started with ingestion but then but you also So I mean the query layer also needs to be aware.

So it's >> I guess the guy behind you can explain you this quite well how it is. Um where is the slide? >> Which one? >> Uh for queryip >> this one. Um so the idea was I you can also explain it yourself if you want to otherwise the idea is like with the version read uh you will have uh transformation upwards and downwards if possible um and then you can write your query with the own metric name or own old metric name or old attribute and you will get still the response from the new one. It's just a storage problem and the matter where you would like to sort it. Yeah, and I have kind of similar question like all of this is something that I guess everyone needs um if they want to really version their schema kind of um well attributes um be metrics, logs, tracing and maybe profiling one day. So what we can do to I don't know standardize some translation layer mechanism on query side and I guess ingestion right um and something that is tailored for maybe popular I don't know languages like SQL and and promql and um you know for popular kind of um I don't know injection formats.

So how how we can start this? >> I think with generating um presets already and store them somewhere similar like what we have with the schema that we have seen earlier which I guess we can maybe in the future I guess there was also some ongoing work already on this having a new version which contains more information which allows us to do this transformation up and downwards. Yeah. Um it's a bit tricky because in the process I was thinking of how can you sometimes um the path downwards is not always clear. So if you have some information loss like there is a change in the scope of an attribute or something and then how can you figure out if this was previously IO read and write or was it something else you will get it from the context but then you add again this metadata on top. Um I guess that's the way to find the clear solutions. >> Yeah. Amazing. So it sounds like you did um some kind of mapping on their own on your own just so it makes sense for click house.

uh we did in our demo in promos last year literally our custom kind of YAML that transl gives mapping which is efficient for promql and um and maybe that's a root like we have you know per language per specific solution mappings that at least can be translated from the same source but sounds like there's so many things we can share and another thing I want to point out like you were you built some registry of schema and then you actually synchronize that with bucket so you're building terraform essentially right which is which is amazing like to kind have some tool that maybe we can do together um to synchronize those changes safely, right? Yeah. >> Yeah. >> Uh >> I think I mean we we started building this before I know what we is and I think now most of the use cases I just was talking about you could get from Wever also. >> Yeah. But let's contribute that and and do it together. So um anyway, thank you very much and have amazing.

>> Uh hey, quick question about the schema migration you were showing. So I was wondering how can that perform well? So when you're changing column names so is it like just >> so is it like renaming columns in click house or >> no like as I said like you can't rename columns I mean you can have alias that's not a problem. >> Um but you can't change the type of a column. >> You can we do renaming but under the hood it's it's the same column. So you're like what you want to avoid is mutating data because then it gets really expensive like >> so you're just renaming the in the queries but under the hood uh the column names just >> I think you can also define an alias >> okay click house yeah but like if like we have one day of data on disk and 29 days of data in GCS and you don't want to mutate the data in GCS so we just don't allow that. >> Got it. Thank you. Okay. Yeah. Thank you very much then.